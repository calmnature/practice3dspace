var __defProp = Object.defineProperty;
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);
var __privateWrapper = (obj, member, setter, getter) => ({
  set _(value) {
    __privateSet(obj, member, value, setter);
  },
  get _() {
    return __privateGet(obj, member, getter);
  }
});
var _a2, _b, _c2, _d2, _Ky_static, normalizeSearchParams_fn, _abortController, _retryCount, _input, _options, _originalRequest, _userProvidedAbortSignal, _cachedNormalizedOptions, _Ky_instances, calculateDelay_fn, calculateRetryDelay_fn, decorateResponse_fn, retry_fn, fetch_fn, getNormalizedOptions_fn, assignRequest_fn, wrapRequestWithUploadProgress_fn;
(function polyfill() {
  const relList = document.createElement("link").relList;
  if (relList && relList.supports && relList.supports("modulepreload")) {
    return;
  }
  for (const link of document.querySelectorAll('link[rel="modulepreload"]')) {
    processPreload(link);
  }
  new MutationObserver((mutations) => {
    for (const mutation of mutations) {
      if (mutation.type !== "childList") {
        continue;
      }
      for (const node of mutation.addedNodes) {
        if (node.tagName === "LINK" && node.rel === "modulepreload")
          processPreload(node);
      }
    }
  }).observe(document, { childList: true, subtree: true });
  function getFetchOpts(link) {
    const fetchOpts = {};
    if (link.integrity) fetchOpts.integrity = link.integrity;
    if (link.referrerPolicy) fetchOpts.referrerPolicy = link.referrerPolicy;
    if (link.crossOrigin === "use-credentials")
      fetchOpts.credentials = "include";
    else if (link.crossOrigin === "anonymous") fetchOpts.credentials = "omit";
    else fetchOpts.credentials = "same-origin";
    return fetchOpts;
  }
  function processPreload(link) {
    if (link.ep)
      return;
    link.ep = true;
    const fetchOpts = getFetchOpts(link);
    fetch(link.href, fetchOpts);
  }
})();
/**
* @vue/shared v3.5.25
* (c) 2018-present Yuxi (Evan) You and Vue contributors
* @license MIT
**/
// @__NO_SIDE_EFFECTS__
function makeMap(str) {
  const map = /* @__PURE__ */ Object.create(null);
  for (const key of str.split(",")) map[key] = 1;
  return (val) => val in map;
}
const EMPTY_OBJ = {};
const EMPTY_ARR = [];
const NOOP = () => {
};
const NO = () => false;
const isOn = (key) => key.charCodeAt(0) === 111 && key.charCodeAt(1) === 110 && // uppercase letter
(key.charCodeAt(2) > 122 || key.charCodeAt(2) < 97);
const isModelListener = (key) => key.startsWith("onUpdate:");
const extend = Object.assign;
const remove = (arr, el2) => {
  const i = arr.indexOf(el2);
  if (i > -1) {
    arr.splice(i, 1);
  }
};
const hasOwnProperty$2 = Object.prototype.hasOwnProperty;
const hasOwn = (val, key) => hasOwnProperty$2.call(val, key);
const isArray = Array.isArray;
const isMap = (val) => toTypeString(val) === "[object Map]";
const isSet = (val) => toTypeString(val) === "[object Set]";
const isDate = (val) => toTypeString(val) === "[object Date]";
const isFunction = (val) => typeof val === "function";
const isString = (val) => typeof val === "string";
const isSymbol = (val) => typeof val === "symbol";
const isObject$4 = (val) => val !== null && typeof val === "object";
const isPromise = (val) => {
  return (isObject$4(val) || isFunction(val)) && isFunction(val.then) && isFunction(val.catch);
};
const objectToString = Object.prototype.toString;
const toTypeString = (value) => objectToString.call(value);
const toRawType = (value) => {
  return toTypeString(value).slice(8, -1);
};
const isPlainObject$1 = (val) => toTypeString(val) === "[object Object]";
const isIntegerKey = (key) => isString(key) && key !== "NaN" && key[0] !== "-" && "" + parseInt(key, 10) === key;
const isReservedProp = /* @__PURE__ */ makeMap(
  // the leading comma is intentional so empty string "" is also included
  ",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"
);
const cacheStringFunction = (fn) => {
  const cache = /* @__PURE__ */ Object.create(null);
  return (str) => {
    const hit = cache[str];
    return hit || (cache[str] = fn(str));
  };
};
const camelizeRE = /-\w/g;
const camelize = cacheStringFunction(
  (str) => {
    return str.replace(camelizeRE, (c) => c.slice(1).toUpperCase());
  }
);
const hyphenateRE = /\B([A-Z])/g;
const hyphenate = cacheStringFunction(
  (str) => str.replace(hyphenateRE, "-$1").toLowerCase()
);
const capitalize = cacheStringFunction((str) => {
  return str.charAt(0).toUpperCase() + str.slice(1);
});
const toHandlerKey = cacheStringFunction(
  (str) => {
    const s = str ? `on${capitalize(str)}` : ``;
    return s;
  }
);
const hasChanged = (value, oldValue) => !Object.is(value, oldValue);
const invokeArrayFns = (fns, ...arg) => {
  for (let i = 0; i < fns.length; i++) {
    fns[i](...arg);
  }
};
const def = (obj, key, value, writable = false) => {
  Object.defineProperty(obj, key, {
    configurable: true,
    enumerable: false,
    writable,
    value
  });
};
const looseToNumber = (val) => {
  const n = parseFloat(val);
  return isNaN(n) ? val : n;
};
const toNumber = (val) => {
  const n = isString(val) ? Number(val) : NaN;
  return isNaN(n) ? val : n;
};
let _globalThis;
const getGlobalThis = () => {
  return _globalThis || (_globalThis = typeof globalThis !== "undefined" ? globalThis : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : typeof global !== "undefined" ? global : {});
};
function normalizeStyle(value) {
  if (isArray(value)) {
    const res = {};
    for (let i = 0; i < value.length; i++) {
      const item = value[i];
      const normalized = isString(item) ? parseStringStyle(item) : normalizeStyle(item);
      if (normalized) {
        for (const key in normalized) {
          res[key] = normalized[key];
        }
      }
    }
    return res;
  } else if (isString(value) || isObject$4(value)) {
    return value;
  }
}
const listDelimiterRE = /;(?![^(]*\))/g;
const propertyDelimiterRE = /:([^]+)/;
const styleCommentRE = /\/\*[^]*?\*\//g;
function parseStringStyle(cssText) {
  const ret = {};
  cssText.replace(styleCommentRE, "").split(listDelimiterRE).forEach((item) => {
    if (item) {
      const tmp = item.split(propertyDelimiterRE);
      tmp.length > 1 && (ret[tmp[0].trim()] = tmp[1].trim());
    }
  });
  return ret;
}
function normalizeClass(value) {
  let res = "";
  if (isString(value)) {
    res = value;
  } else if (isArray(value)) {
    for (let i = 0; i < value.length; i++) {
      const normalized = normalizeClass(value[i]);
      if (normalized) {
        res += normalized + " ";
      }
    }
  } else if (isObject$4(value)) {
    for (const name2 in value) {
      if (value[name2]) {
        res += name2 + " ";
      }
    }
  }
  return res.trim();
}
function normalizeProps(props) {
  if (!props) return null;
  let { class: klass, style } = props;
  if (klass && !isString(klass)) {
    props.class = normalizeClass(klass);
  }
  if (style) {
    props.style = normalizeStyle(style);
  }
  return props;
}
const specialBooleanAttrs = `itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly`;
const isSpecialBooleanAttr = /* @__PURE__ */ makeMap(specialBooleanAttrs);
function includeBooleanAttr(value) {
  return !!value || value === "";
}
function looseCompareArrays(a, b) {
  if (a.length !== b.length) return false;
  let equal = true;
  for (let i = 0; equal && i < a.length; i++) {
    equal = looseEqual(a[i], b[i]);
  }
  return equal;
}
function looseEqual(a, b) {
  if (a === b) return true;
  let aValidType = isDate(a);
  let bValidType = isDate(b);
  if (aValidType || bValidType) {
    return aValidType && bValidType ? a.getTime() === b.getTime() : false;
  }
  aValidType = isSymbol(a);
  bValidType = isSymbol(b);
  if (aValidType || bValidType) {
    return a === b;
  }
  aValidType = isArray(a);
  bValidType = isArray(b);
  if (aValidType || bValidType) {
    return aValidType && bValidType ? looseCompareArrays(a, b) : false;
  }
  aValidType = isObject$4(a);
  bValidType = isObject$4(b);
  if (aValidType || bValidType) {
    if (!aValidType || !bValidType) {
      return false;
    }
    const aKeysCount = Object.keys(a).length;
    const bKeysCount = Object.keys(b).length;
    if (aKeysCount !== bKeysCount) {
      return false;
    }
    for (const key in a) {
      const aHasKey = a.hasOwnProperty(key);
      const bHasKey = b.hasOwnProperty(key);
      if (aHasKey && !bHasKey || !aHasKey && bHasKey || !looseEqual(a[key], b[key])) {
        return false;
      }
    }
  }
  return String(a) === String(b);
}
function looseIndexOf(arr, val) {
  return arr.findIndex((item) => looseEqual(item, val));
}
const isRef$1 = (val) => {
  return !!(val && val["__v_isRef"] === true);
};
const toDisplayString = (val) => {
  return isString(val) ? val : val == null ? "" : isArray(val) || isObject$4(val) && (val.toString === objectToString || !isFunction(val.toString)) ? isRef$1(val) ? toDisplayString(val.value) : JSON.stringify(val, replacer, 2) : String(val);
};
const replacer = (_key, val) => {
  if (isRef$1(val)) {
    return replacer(_key, val.value);
  } else if (isMap(val)) {
    return {
      [`Map(${val.size})`]: [...val.entries()].reduce(
        (entries, [key, val2], i) => {
          entries[stringifySymbol(key, i) + " =>"] = val2;
          return entries;
        },
        {}
      )
    };
  } else if (isSet(val)) {
    return {
      [`Set(${val.size})`]: [...val.values()].map((v) => stringifySymbol(v))
    };
  } else if (isSymbol(val)) {
    return stringifySymbol(val);
  } else if (isObject$4(val) && !isArray(val) && !isPlainObject$1(val)) {
    return String(val);
  }
  return val;
};
const stringifySymbol = (v, i = "") => {
  var _a3;
  return (
    // Symbol.description in es2019+ so we need to cast here to pass
    // the lib: es2016 check
    isSymbol(v) ? `Symbol(${(_a3 = v.description) != null ? _a3 : i})` : v
  );
};
/**
* @vue/reactivity v3.5.25
* (c) 2018-present Yuxi (Evan) You and Vue contributors
* @license MIT
**/
let activeEffectScope;
class EffectScope {
  constructor(detached = false) {
    this.detached = detached;
    this._active = true;
    this._on = 0;
    this.effects = [];
    this.cleanups = [];
    this._isPaused = false;
    this.parent = activeEffectScope;
    if (!detached && activeEffectScope) {
      this.index = (activeEffectScope.scopes || (activeEffectScope.scopes = [])).push(
        this
      ) - 1;
    }
  }
  get active() {
    return this._active;
  }
  pause() {
    if (this._active) {
      this._isPaused = true;
      let i, l;
      if (this.scopes) {
        for (i = 0, l = this.scopes.length; i < l; i++) {
          this.scopes[i].pause();
        }
      }
      for (i = 0, l = this.effects.length; i < l; i++) {
        this.effects[i].pause();
      }
    }
  }
  /**
   * Resumes the effect scope, including all child scopes and effects.
   */
  resume() {
    if (this._active) {
      if (this._isPaused) {
        this._isPaused = false;
        let i, l;
        if (this.scopes) {
          for (i = 0, l = this.scopes.length; i < l; i++) {
            this.scopes[i].resume();
          }
        }
        for (i = 0, l = this.effects.length; i < l; i++) {
          this.effects[i].resume();
        }
      }
    }
  }
  run(fn) {
    if (this._active) {
      const currentEffectScope = activeEffectScope;
      try {
        activeEffectScope = this;
        return fn();
      } finally {
        activeEffectScope = currentEffectScope;
      }
    }
  }
  /**
   * This should only be called on non-detached scopes
   * @internal
   */
  on() {
    if (++this._on === 1) {
      this.prevScope = activeEffectScope;
      activeEffectScope = this;
    }
  }
  /**
   * This should only be called on non-detached scopes
   * @internal
   */
  off() {
    if (this._on > 0 && --this._on === 0) {
      activeEffectScope = this.prevScope;
      this.prevScope = void 0;
    }
  }
  stop(fromParent) {
    if (this._active) {
      this._active = false;
      let i, l;
      for (i = 0, l = this.effects.length; i < l; i++) {
        this.effects[i].stop();
      }
      this.effects.length = 0;
      for (i = 0, l = this.cleanups.length; i < l; i++) {
        this.cleanups[i]();
      }
      this.cleanups.length = 0;
      if (this.scopes) {
        for (i = 0, l = this.scopes.length; i < l; i++) {
          this.scopes[i].stop(true);
        }
        this.scopes.length = 0;
      }
      if (!this.detached && this.parent && !fromParent) {
        const last = this.parent.scopes.pop();
        if (last && last !== this) {
          this.parent.scopes[this.index] = last;
          last.index = this.index;
        }
      }
      this.parent = void 0;
    }
  }
}
function effectScope(detached) {
  return new EffectScope(detached);
}
function getCurrentScope() {
  return activeEffectScope;
}
function onScopeDispose(fn, failSilently = false) {
  if (activeEffectScope) {
    activeEffectScope.cleanups.push(fn);
  }
}
let activeSub;
const pausedQueueEffects = /* @__PURE__ */ new WeakSet();
class ReactiveEffect {
  constructor(fn) {
    this.fn = fn;
    this.deps = void 0;
    this.depsTail = void 0;
    this.flags = 1 | 4;
    this.next = void 0;
    this.cleanup = void 0;
    this.scheduler = void 0;
    if (activeEffectScope && activeEffectScope.active) {
      activeEffectScope.effects.push(this);
    }
  }
  pause() {
    this.flags |= 64;
  }
  resume() {
    if (this.flags & 64) {
      this.flags &= -65;
      if (pausedQueueEffects.has(this)) {
        pausedQueueEffects.delete(this);
        this.trigger();
      }
    }
  }
  /**
   * @internal
   */
  notify() {
    if (this.flags & 2 && !(this.flags & 32)) {
      return;
    }
    if (!(this.flags & 8)) {
      batch(this);
    }
  }
  run() {
    if (!(this.flags & 1)) {
      return this.fn();
    }
    this.flags |= 2;
    cleanupEffect(this);
    prepareDeps(this);
    const prevEffect = activeSub;
    const prevShouldTrack = shouldTrack;
    activeSub = this;
    shouldTrack = true;
    try {
      return this.fn();
    } finally {
      cleanupDeps(this);
      activeSub = prevEffect;
      shouldTrack = prevShouldTrack;
      this.flags &= -3;
    }
  }
  stop() {
    if (this.flags & 1) {
      for (let link = this.deps; link; link = link.nextDep) {
        removeSub(link);
      }
      this.deps = this.depsTail = void 0;
      cleanupEffect(this);
      this.onStop && this.onStop();
      this.flags &= -2;
    }
  }
  trigger() {
    if (this.flags & 64) {
      pausedQueueEffects.add(this);
    } else if (this.scheduler) {
      this.scheduler();
    } else {
      this.runIfDirty();
    }
  }
  /**
   * @internal
   */
  runIfDirty() {
    if (isDirty(this)) {
      this.run();
    }
  }
  get dirty() {
    return isDirty(this);
  }
}
let batchDepth = 0;
let batchedSub;
let batchedComputed;
function batch(sub, isComputed2 = false) {
  sub.flags |= 8;
  if (isComputed2) {
    sub.next = batchedComputed;
    batchedComputed = sub;
    return;
  }
  sub.next = batchedSub;
  batchedSub = sub;
}
function startBatch() {
  batchDepth++;
}
function endBatch() {
  if (--batchDepth > 0) {
    return;
  }
  if (batchedComputed) {
    let e = batchedComputed;
    batchedComputed = void 0;
    while (e) {
      const next = e.next;
      e.next = void 0;
      e.flags &= -9;
      e = next;
    }
  }
  let error;
  while (batchedSub) {
    let e = batchedSub;
    batchedSub = void 0;
    while (e) {
      const next = e.next;
      e.next = void 0;
      e.flags &= -9;
      if (e.flags & 1) {
        try {
          ;
          e.trigger();
        } catch (err) {
          if (!error) error = err;
        }
      }
      e = next;
    }
  }
  if (error) throw error;
}
function prepareDeps(sub) {
  for (let link = sub.deps; link; link = link.nextDep) {
    link.version = -1;
    link.prevActiveLink = link.dep.activeLink;
    link.dep.activeLink = link;
  }
}
function cleanupDeps(sub) {
  let head;
  let tail = sub.depsTail;
  let link = tail;
  while (link) {
    const prev = link.prevDep;
    if (link.version === -1) {
      if (link === tail) tail = prev;
      removeSub(link);
      removeDep(link);
    } else {
      head = link;
    }
    link.dep.activeLink = link.prevActiveLink;
    link.prevActiveLink = void 0;
    link = prev;
  }
  sub.deps = head;
  sub.depsTail = tail;
}
function isDirty(sub) {
  for (let link = sub.deps; link; link = link.nextDep) {
    if (link.dep.version !== link.version || link.dep.computed && (refreshComputed(link.dep.computed) || link.dep.version !== link.version)) {
      return true;
    }
  }
  if (sub._dirty) {
    return true;
  }
  return false;
}
function refreshComputed(computed2) {
  if (computed2.flags & 4 && !(computed2.flags & 16)) {
    return;
  }
  computed2.flags &= -17;
  if (computed2.globalVersion === globalVersion) {
    return;
  }
  computed2.globalVersion = globalVersion;
  if (!computed2.isSSR && computed2.flags & 128 && (!computed2.deps && !computed2._dirty || !isDirty(computed2))) {
    return;
  }
  computed2.flags |= 2;
  const dep = computed2.dep;
  const prevSub = activeSub;
  const prevShouldTrack = shouldTrack;
  activeSub = computed2;
  shouldTrack = true;
  try {
    prepareDeps(computed2);
    const value = computed2.fn(computed2._value);
    if (dep.version === 0 || hasChanged(value, computed2._value)) {
      computed2.flags |= 128;
      computed2._value = value;
      dep.version++;
    }
  } catch (err) {
    dep.version++;
    throw err;
  } finally {
    activeSub = prevSub;
    shouldTrack = prevShouldTrack;
    cleanupDeps(computed2);
    computed2.flags &= -3;
  }
}
function removeSub(link, soft = false) {
  const { dep, prevSub, nextSub } = link;
  if (prevSub) {
    prevSub.nextSub = nextSub;
    link.prevSub = void 0;
  }
  if (nextSub) {
    nextSub.prevSub = prevSub;
    link.nextSub = void 0;
  }
  if (dep.subs === link) {
    dep.subs = prevSub;
    if (!prevSub && dep.computed) {
      dep.computed.flags &= -5;
      for (let l = dep.computed.deps; l; l = l.nextDep) {
        removeSub(l, true);
      }
    }
  }
  if (!soft && !--dep.sc && dep.map) {
    dep.map.delete(dep.key);
  }
}
function removeDep(link) {
  const { prevDep, nextDep } = link;
  if (prevDep) {
    prevDep.nextDep = nextDep;
    link.prevDep = void 0;
  }
  if (nextDep) {
    nextDep.prevDep = prevDep;
    link.nextDep = void 0;
  }
}
let shouldTrack = true;
const trackStack = [];
function pauseTracking() {
  trackStack.push(shouldTrack);
  shouldTrack = false;
}
function resetTracking() {
  const last = trackStack.pop();
  shouldTrack = last === void 0 ? true : last;
}
function cleanupEffect(e) {
  const { cleanup } = e;
  e.cleanup = void 0;
  if (cleanup) {
    const prevSub = activeSub;
    activeSub = void 0;
    try {
      cleanup();
    } finally {
      activeSub = prevSub;
    }
  }
}
let globalVersion = 0;
class Link {
  constructor(sub, dep) {
    this.sub = sub;
    this.dep = dep;
    this.version = dep.version;
    this.nextDep = this.prevDep = this.nextSub = this.prevSub = this.prevActiveLink = void 0;
  }
}
class Dep {
  // TODO isolatedDeclarations "__v_skip"
  constructor(computed2) {
    this.computed = computed2;
    this.version = 0;
    this.activeLink = void 0;
    this.subs = void 0;
    this.map = void 0;
    this.key = void 0;
    this.sc = 0;
    this.__v_skip = true;
  }
  track(debugInfo) {
    if (!activeSub || !shouldTrack || activeSub === this.computed) {
      return;
    }
    let link = this.activeLink;
    if (link === void 0 || link.sub !== activeSub) {
      link = this.activeLink = new Link(activeSub, this);
      if (!activeSub.deps) {
        activeSub.deps = activeSub.depsTail = link;
      } else {
        link.prevDep = activeSub.depsTail;
        activeSub.depsTail.nextDep = link;
        activeSub.depsTail = link;
      }
      addSub(link);
    } else if (link.version === -1) {
      link.version = this.version;
      if (link.nextDep) {
        const next = link.nextDep;
        next.prevDep = link.prevDep;
        if (link.prevDep) {
          link.prevDep.nextDep = next;
        }
        link.prevDep = activeSub.depsTail;
        link.nextDep = void 0;
        activeSub.depsTail.nextDep = link;
        activeSub.depsTail = link;
        if (activeSub.deps === link) {
          activeSub.deps = next;
        }
      }
    }
    return link;
  }
  trigger(debugInfo) {
    this.version++;
    globalVersion++;
    this.notify(debugInfo);
  }
  notify(debugInfo) {
    startBatch();
    try {
      if (false) ;
      for (let link = this.subs; link; link = link.prevSub) {
        if (link.sub.notify()) {
          ;
          link.sub.dep.notify();
        }
      }
    } finally {
      endBatch();
    }
  }
}
function addSub(link) {
  link.dep.sc++;
  if (link.sub.flags & 4) {
    const computed2 = link.dep.computed;
    if (computed2 && !link.dep.subs) {
      computed2.flags |= 4 | 16;
      for (let l = computed2.deps; l; l = l.nextDep) {
        addSub(l);
      }
    }
    const currentTail = link.dep.subs;
    if (currentTail !== link) {
      link.prevSub = currentTail;
      if (currentTail) currentTail.nextSub = link;
    }
    link.dep.subs = link;
  }
}
const targetMap = /* @__PURE__ */ new WeakMap();
const ITERATE_KEY = Symbol(
  ""
);
const MAP_KEY_ITERATE_KEY = Symbol(
  ""
);
const ARRAY_ITERATE_KEY = Symbol(
  ""
);
function track(target, type, key) {
  if (shouldTrack && activeSub) {
    let depsMap = targetMap.get(target);
    if (!depsMap) {
      targetMap.set(target, depsMap = /* @__PURE__ */ new Map());
    }
    let dep = depsMap.get(key);
    if (!dep) {
      depsMap.set(key, dep = new Dep());
      dep.map = depsMap;
      dep.key = key;
    }
    {
      dep.track();
    }
  }
}
function trigger(target, type, key, newValue, oldValue, oldTarget) {
  const depsMap = targetMap.get(target);
  if (!depsMap) {
    globalVersion++;
    return;
  }
  const run = (dep) => {
    if (dep) {
      {
        dep.trigger();
      }
    }
  };
  startBatch();
  if (type === "clear") {
    depsMap.forEach(run);
  } else {
    const targetIsArray = isArray(target);
    const isArrayIndex = targetIsArray && isIntegerKey(key);
    if (targetIsArray && key === "length") {
      const newLength = Number(newValue);
      depsMap.forEach((dep, key2) => {
        if (key2 === "length" || key2 === ARRAY_ITERATE_KEY || !isSymbol(key2) && key2 >= newLength) {
          run(dep);
        }
      });
    } else {
      if (key !== void 0 || depsMap.has(void 0)) {
        run(depsMap.get(key));
      }
      if (isArrayIndex) {
        run(depsMap.get(ARRAY_ITERATE_KEY));
      }
      switch (type) {
        case "add":
          if (!targetIsArray) {
            run(depsMap.get(ITERATE_KEY));
            if (isMap(target)) {
              run(depsMap.get(MAP_KEY_ITERATE_KEY));
            }
          } else if (isArrayIndex) {
            run(depsMap.get("length"));
          }
          break;
        case "delete":
          if (!targetIsArray) {
            run(depsMap.get(ITERATE_KEY));
            if (isMap(target)) {
              run(depsMap.get(MAP_KEY_ITERATE_KEY));
            }
          }
          break;
        case "set":
          if (isMap(target)) {
            run(depsMap.get(ITERATE_KEY));
          }
          break;
      }
    }
  }
  endBatch();
}
function getDepFromReactive(object, key) {
  const depMap = targetMap.get(object);
  return depMap && depMap.get(key);
}
function reactiveReadArray(array) {
  const raw = toRaw(array);
  if (raw === array) return raw;
  track(raw, "iterate", ARRAY_ITERATE_KEY);
  return isShallow(array) ? raw : raw.map(toReactive);
}
function shallowReadArray(arr) {
  track(arr = toRaw(arr), "iterate", ARRAY_ITERATE_KEY);
  return arr;
}
function toWrapped(target, item) {
  if (isReadonly(target)) {
    return isReactive(target) ? toReadonly(toReactive(item)) : toReadonly(item);
  }
  return toReactive(item);
}
const arrayInstrumentations = {
  __proto__: null,
  [Symbol.iterator]() {
    return iterator(this, Symbol.iterator, (item) => toWrapped(this, item));
  },
  concat(...args) {
    return reactiveReadArray(this).concat(
      ...args.map((x) => isArray(x) ? reactiveReadArray(x) : x)
    );
  },
  entries() {
    return iterator(this, "entries", (value) => {
      value[1] = toWrapped(this, value[1]);
      return value;
    });
  },
  every(fn, thisArg) {
    return apply(this, "every", fn, thisArg, void 0, arguments);
  },
  filter(fn, thisArg) {
    return apply(
      this,
      "filter",
      fn,
      thisArg,
      (v) => v.map((item) => toWrapped(this, item)),
      arguments
    );
  },
  find(fn, thisArg) {
    return apply(
      this,
      "find",
      fn,
      thisArg,
      (item) => toWrapped(this, item),
      arguments
    );
  },
  findIndex(fn, thisArg) {
    return apply(this, "findIndex", fn, thisArg, void 0, arguments);
  },
  findLast(fn, thisArg) {
    return apply(
      this,
      "findLast",
      fn,
      thisArg,
      (item) => toWrapped(this, item),
      arguments
    );
  },
  findLastIndex(fn, thisArg) {
    return apply(this, "findLastIndex", fn, thisArg, void 0, arguments);
  },
  // flat, flatMap could benefit from ARRAY_ITERATE but are not straight-forward to implement
  forEach(fn, thisArg) {
    return apply(this, "forEach", fn, thisArg, void 0, arguments);
  },
  includes(...args) {
    return searchProxy(this, "includes", args);
  },
  indexOf(...args) {
    return searchProxy(this, "indexOf", args);
  },
  join(separator) {
    return reactiveReadArray(this).join(separator);
  },
  // keys() iterator only reads `length`, no optimization required
  lastIndexOf(...args) {
    return searchProxy(this, "lastIndexOf", args);
  },
  map(fn, thisArg) {
    return apply(this, "map", fn, thisArg, void 0, arguments);
  },
  pop() {
    return noTracking(this, "pop");
  },
  push(...args) {
    return noTracking(this, "push", args);
  },
  reduce(fn, ...args) {
    return reduce(this, "reduce", fn, args);
  },
  reduceRight(fn, ...args) {
    return reduce(this, "reduceRight", fn, args);
  },
  shift() {
    return noTracking(this, "shift");
  },
  // slice could use ARRAY_ITERATE but also seems to beg for range tracking
  some(fn, thisArg) {
    return apply(this, "some", fn, thisArg, void 0, arguments);
  },
  splice(...args) {
    return noTracking(this, "splice", args);
  },
  toReversed() {
    return reactiveReadArray(this).toReversed();
  },
  toSorted(comparer) {
    return reactiveReadArray(this).toSorted(comparer);
  },
  toSpliced(...args) {
    return reactiveReadArray(this).toSpliced(...args);
  },
  unshift(...args) {
    return noTracking(this, "unshift", args);
  },
  values() {
    return iterator(this, "values", (item) => toWrapped(this, item));
  }
};
function iterator(self2, method, wrapValue) {
  const arr = shallowReadArray(self2);
  const iter = arr[method]();
  if (arr !== self2 && !isShallow(self2)) {
    iter._next = iter.next;
    iter.next = () => {
      const result = iter._next();
      if (!result.done) {
        result.value = wrapValue(result.value);
      }
      return result;
    };
  }
  return iter;
}
const arrayProto = Array.prototype;
function apply(self2, method, fn, thisArg, wrappedRetFn, args) {
  const arr = shallowReadArray(self2);
  const needsWrap = arr !== self2 && !isShallow(self2);
  const methodFn = arr[method];
  if (methodFn !== arrayProto[method]) {
    const result2 = methodFn.apply(self2, args);
    return needsWrap ? toReactive(result2) : result2;
  }
  let wrappedFn = fn;
  if (arr !== self2) {
    if (needsWrap) {
      wrappedFn = function(item, index) {
        return fn.call(this, toWrapped(self2, item), index, self2);
      };
    } else if (fn.length > 2) {
      wrappedFn = function(item, index) {
        return fn.call(this, item, index, self2);
      };
    }
  }
  const result = methodFn.call(arr, wrappedFn, thisArg);
  return needsWrap && wrappedRetFn ? wrappedRetFn(result) : result;
}
function reduce(self2, method, fn, args) {
  const arr = shallowReadArray(self2);
  let wrappedFn = fn;
  if (arr !== self2) {
    if (!isShallow(self2)) {
      wrappedFn = function(acc, item, index) {
        return fn.call(this, acc, toWrapped(self2, item), index, self2);
      };
    } else if (fn.length > 3) {
      wrappedFn = function(acc, item, index) {
        return fn.call(this, acc, item, index, self2);
      };
    }
  }
  return arr[method](wrappedFn, ...args);
}
function searchProxy(self2, method, args) {
  const arr = toRaw(self2);
  track(arr, "iterate", ARRAY_ITERATE_KEY);
  const res = arr[method](...args);
  if ((res === -1 || res === false) && isProxy(args[0])) {
    args[0] = toRaw(args[0]);
    return arr[method](...args);
  }
  return res;
}
function noTracking(self2, method, args = []) {
  pauseTracking();
  startBatch();
  const res = toRaw(self2)[method].apply(self2, args);
  endBatch();
  resetTracking();
  return res;
}
const isNonTrackableKeys = /* @__PURE__ */ makeMap(`__proto__,__v_isRef,__isVue`);
const builtInSymbols = new Set(
  /* @__PURE__ */ Object.getOwnPropertyNames(Symbol).filter((key) => key !== "arguments" && key !== "caller").map((key) => Symbol[key]).filter(isSymbol)
);
function hasOwnProperty$1(key) {
  if (!isSymbol(key)) key = String(key);
  const obj = toRaw(this);
  track(obj, "has", key);
  return obj.hasOwnProperty(key);
}
class BaseReactiveHandler {
  constructor(_isReadonly = false, _isShallow = false) {
    this._isReadonly = _isReadonly;
    this._isShallow = _isShallow;
  }
  get(target, key, receiver) {
    if (key === "__v_skip") return target["__v_skip"];
    const isReadonly2 = this._isReadonly, isShallow2 = this._isShallow;
    if (key === "__v_isReactive") {
      return !isReadonly2;
    } else if (key === "__v_isReadonly") {
      return isReadonly2;
    } else if (key === "__v_isShallow") {
      return isShallow2;
    } else if (key === "__v_raw") {
      if (receiver === (isReadonly2 ? isShallow2 ? shallowReadonlyMap : readonlyMap : isShallow2 ? shallowReactiveMap : reactiveMap).get(target) || // receiver is not the reactive proxy, but has the same prototype
      // this means the receiver is a user proxy of the reactive proxy
      Object.getPrototypeOf(target) === Object.getPrototypeOf(receiver)) {
        return target;
      }
      return;
    }
    const targetIsArray = isArray(target);
    if (!isReadonly2) {
      let fn;
      if (targetIsArray && (fn = arrayInstrumentations[key])) {
        return fn;
      }
      if (key === "hasOwnProperty") {
        return hasOwnProperty$1;
      }
    }
    const res = Reflect.get(
      target,
      key,
      // if this is a proxy wrapping a ref, return methods using the raw ref
      // as receiver so that we don't have to call `toRaw` on the ref in all
      // its class methods
      isRef(target) ? target : receiver
    );
    if (isSymbol(key) ? builtInSymbols.has(key) : isNonTrackableKeys(key)) {
      return res;
    }
    if (!isReadonly2) {
      track(target, "get", key);
    }
    if (isShallow2) {
      return res;
    }
    if (isRef(res)) {
      const value = targetIsArray && isIntegerKey(key) ? res : res.value;
      return isReadonly2 && isObject$4(value) ? readonly(value) : value;
    }
    if (isObject$4(res)) {
      return isReadonly2 ? readonly(res) : reactive(res);
    }
    return res;
  }
}
class MutableReactiveHandler extends BaseReactiveHandler {
  constructor(isShallow2 = false) {
    super(false, isShallow2);
  }
  set(target, key, value, receiver) {
    let oldValue = target[key];
    const isArrayWithIntegerKey = isArray(target) && isIntegerKey(key);
    if (!this._isShallow) {
      const isOldValueReadonly = isReadonly(oldValue);
      if (!isShallow(value) && !isReadonly(value)) {
        oldValue = toRaw(oldValue);
        value = toRaw(value);
      }
      if (!isArrayWithIntegerKey && isRef(oldValue) && !isRef(value)) {
        if (isOldValueReadonly) {
          return true;
        } else {
          oldValue.value = value;
          return true;
        }
      }
    }
    const hadKey = isArrayWithIntegerKey ? Number(key) < target.length : hasOwn(target, key);
    const result = Reflect.set(
      target,
      key,
      value,
      isRef(target) ? target : receiver
    );
    if (target === toRaw(receiver)) {
      if (!hadKey) {
        trigger(target, "add", key, value);
      } else if (hasChanged(value, oldValue)) {
        trigger(target, "set", key, value);
      }
    }
    return result;
  }
  deleteProperty(target, key) {
    const hadKey = hasOwn(target, key);
    target[key];
    const result = Reflect.deleteProperty(target, key);
    if (result && hadKey) {
      trigger(target, "delete", key, void 0);
    }
    return result;
  }
  has(target, key) {
    const result = Reflect.has(target, key);
    if (!isSymbol(key) || !builtInSymbols.has(key)) {
      track(target, "has", key);
    }
    return result;
  }
  ownKeys(target) {
    track(
      target,
      "iterate",
      isArray(target) ? "length" : ITERATE_KEY
    );
    return Reflect.ownKeys(target);
  }
}
class ReadonlyReactiveHandler extends BaseReactiveHandler {
  constructor(isShallow2 = false) {
    super(true, isShallow2);
  }
  set(target, key) {
    return true;
  }
  deleteProperty(target, key) {
    return true;
  }
}
const mutableHandlers = /* @__PURE__ */ new MutableReactiveHandler();
const readonlyHandlers = /* @__PURE__ */ new ReadonlyReactiveHandler();
const shallowReactiveHandlers = /* @__PURE__ */ new MutableReactiveHandler(true);
const shallowReadonlyHandlers = /* @__PURE__ */ new ReadonlyReactiveHandler(true);
const toShallow = (value) => value;
const getProto = (v) => Reflect.getPrototypeOf(v);
function createIterableMethod(method, isReadonly2, isShallow2) {
  return function(...args) {
    const target = this["__v_raw"];
    const rawTarget = toRaw(target);
    const targetIsMap = isMap(rawTarget);
    const isPair = method === "entries" || method === Symbol.iterator && targetIsMap;
    const isKeyOnly = method === "keys" && targetIsMap;
    const innerIterator = target[method](...args);
    const wrap2 = isShallow2 ? toShallow : isReadonly2 ? toReadonly : toReactive;
    !isReadonly2 && track(
      rawTarget,
      "iterate",
      isKeyOnly ? MAP_KEY_ITERATE_KEY : ITERATE_KEY
    );
    return {
      // iterator protocol
      next() {
        const { value, done } = innerIterator.next();
        return done ? { value, done } : {
          value: isPair ? [wrap2(value[0]), wrap2(value[1])] : wrap2(value),
          done
        };
      },
      // iterable protocol
      [Symbol.iterator]() {
        return this;
      }
    };
  };
}
function createReadonlyMethod(type) {
  return function(...args) {
    return type === "delete" ? false : type === "clear" ? void 0 : this;
  };
}
function createInstrumentations(readonly2, shallow) {
  const instrumentations = {
    get(key) {
      const target = this["__v_raw"];
      const rawTarget = toRaw(target);
      const rawKey = toRaw(key);
      if (!readonly2) {
        if (hasChanged(key, rawKey)) {
          track(rawTarget, "get", key);
        }
        track(rawTarget, "get", rawKey);
      }
      const { has } = getProto(rawTarget);
      const wrap2 = shallow ? toShallow : readonly2 ? toReadonly : toReactive;
      if (has.call(rawTarget, key)) {
        return wrap2(target.get(key));
      } else if (has.call(rawTarget, rawKey)) {
        return wrap2(target.get(rawKey));
      } else if (target !== rawTarget) {
        target.get(key);
      }
    },
    get size() {
      const target = this["__v_raw"];
      !readonly2 && track(toRaw(target), "iterate", ITERATE_KEY);
      return target.size;
    },
    has(key) {
      const target = this["__v_raw"];
      const rawTarget = toRaw(target);
      const rawKey = toRaw(key);
      if (!readonly2) {
        if (hasChanged(key, rawKey)) {
          track(rawTarget, "has", key);
        }
        track(rawTarget, "has", rawKey);
      }
      return key === rawKey ? target.has(key) : target.has(key) || target.has(rawKey);
    },
    forEach(callback, thisArg) {
      const observed = this;
      const target = observed["__v_raw"];
      const rawTarget = toRaw(target);
      const wrap2 = shallow ? toShallow : readonly2 ? toReadonly : toReactive;
      !readonly2 && track(rawTarget, "iterate", ITERATE_KEY);
      return target.forEach((value, key) => {
        return callback.call(thisArg, wrap2(value), wrap2(key), observed);
      });
    }
  };
  extend(
    instrumentations,
    readonly2 ? {
      add: createReadonlyMethod("add"),
      set: createReadonlyMethod("set"),
      delete: createReadonlyMethod("delete"),
      clear: createReadonlyMethod("clear")
    } : {
      add(value) {
        if (!shallow && !isShallow(value) && !isReadonly(value)) {
          value = toRaw(value);
        }
        const target = toRaw(this);
        const proto = getProto(target);
        const hadKey = proto.has.call(target, value);
        if (!hadKey) {
          target.add(value);
          trigger(target, "add", value, value);
        }
        return this;
      },
      set(key, value) {
        if (!shallow && !isShallow(value) && !isReadonly(value)) {
          value = toRaw(value);
        }
        const target = toRaw(this);
        const { has, get } = getProto(target);
        let hadKey = has.call(target, key);
        if (!hadKey) {
          key = toRaw(key);
          hadKey = has.call(target, key);
        }
        const oldValue = get.call(target, key);
        target.set(key, value);
        if (!hadKey) {
          trigger(target, "add", key, value);
        } else if (hasChanged(value, oldValue)) {
          trigger(target, "set", key, value);
        }
        return this;
      },
      delete(key) {
        const target = toRaw(this);
        const { has, get } = getProto(target);
        let hadKey = has.call(target, key);
        if (!hadKey) {
          key = toRaw(key);
          hadKey = has.call(target, key);
        }
        get ? get.call(target, key) : void 0;
        const result = target.delete(key);
        if (hadKey) {
          trigger(target, "delete", key, void 0);
        }
        return result;
      },
      clear() {
        const target = toRaw(this);
        const hadItems = target.size !== 0;
        const result = target.clear();
        if (hadItems) {
          trigger(
            target,
            "clear",
            void 0,
            void 0
          );
        }
        return result;
      }
    }
  );
  const iteratorMethods = [
    "keys",
    "values",
    "entries",
    Symbol.iterator
  ];
  iteratorMethods.forEach((method) => {
    instrumentations[method] = createIterableMethod(method, readonly2, shallow);
  });
  return instrumentations;
}
function createInstrumentationGetter(isReadonly2, shallow) {
  const instrumentations = createInstrumentations(isReadonly2, shallow);
  return (target, key, receiver) => {
    if (key === "__v_isReactive") {
      return !isReadonly2;
    } else if (key === "__v_isReadonly") {
      return isReadonly2;
    } else if (key === "__v_raw") {
      return target;
    }
    return Reflect.get(
      hasOwn(instrumentations, key) && key in target ? instrumentations : target,
      key,
      receiver
    );
  };
}
const mutableCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(false, false)
};
const shallowCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(false, true)
};
const readonlyCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(true, false)
};
const shallowReadonlyCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(true, true)
};
const reactiveMap = /* @__PURE__ */ new WeakMap();
const shallowReactiveMap = /* @__PURE__ */ new WeakMap();
const readonlyMap = /* @__PURE__ */ new WeakMap();
const shallowReadonlyMap = /* @__PURE__ */ new WeakMap();
function targetTypeMap(rawType) {
  switch (rawType) {
    case "Object":
    case "Array":
      return 1;
    case "Map":
    case "Set":
    case "WeakMap":
    case "WeakSet":
      return 2;
    default:
      return 0;
  }
}
function getTargetType(value) {
  return value["__v_skip"] || !Object.isExtensible(value) ? 0 : targetTypeMap(toRawType(value));
}
function reactive(target) {
  if (isReadonly(target)) {
    return target;
  }
  return createReactiveObject(
    target,
    false,
    mutableHandlers,
    mutableCollectionHandlers,
    reactiveMap
  );
}
function shallowReactive(target) {
  return createReactiveObject(
    target,
    false,
    shallowReactiveHandlers,
    shallowCollectionHandlers,
    shallowReactiveMap
  );
}
function readonly(target) {
  return createReactiveObject(
    target,
    true,
    readonlyHandlers,
    readonlyCollectionHandlers,
    readonlyMap
  );
}
function shallowReadonly(target) {
  return createReactiveObject(
    target,
    true,
    shallowReadonlyHandlers,
    shallowReadonlyCollectionHandlers,
    shallowReadonlyMap
  );
}
function createReactiveObject(target, isReadonly2, baseHandlers, collectionHandlers, proxyMap) {
  if (!isObject$4(target)) {
    return target;
  }
  if (target["__v_raw"] && !(isReadonly2 && target["__v_isReactive"])) {
    return target;
  }
  const targetType = getTargetType(target);
  if (targetType === 0) {
    return target;
  }
  const existingProxy = proxyMap.get(target);
  if (existingProxy) {
    return existingProxy;
  }
  const proxy = new Proxy(
    target,
    targetType === 2 ? collectionHandlers : baseHandlers
  );
  proxyMap.set(target, proxy);
  return proxy;
}
function isReactive(value) {
  if (isReadonly(value)) {
    return isReactive(value["__v_raw"]);
  }
  return !!(value && value["__v_isReactive"]);
}
function isReadonly(value) {
  return !!(value && value["__v_isReadonly"]);
}
function isShallow(value) {
  return !!(value && value["__v_isShallow"]);
}
function isProxy(value) {
  return value ? !!value["__v_raw"] : false;
}
function toRaw(observed) {
  const raw = observed && observed["__v_raw"];
  return raw ? toRaw(raw) : observed;
}
function markRaw(value) {
  if (!hasOwn(value, "__v_skip") && Object.isExtensible(value)) {
    def(value, "__v_skip", true);
  }
  return value;
}
const toReactive = (value) => isObject$4(value) ? reactive(value) : value;
const toReadonly = (value) => isObject$4(value) ? readonly(value) : value;
function isRef(r) {
  return r ? r["__v_isRef"] === true : false;
}
function ref(value) {
  return createRef(value, false);
}
function shallowRef(value) {
  return createRef(value, true);
}
function createRef(rawValue, shallow) {
  if (isRef(rawValue)) {
    return rawValue;
  }
  return new RefImpl(rawValue, shallow);
}
class RefImpl {
  constructor(value, isShallow2) {
    this.dep = new Dep();
    this["__v_isRef"] = true;
    this["__v_isShallow"] = false;
    this._rawValue = isShallow2 ? value : toRaw(value);
    this._value = isShallow2 ? value : toReactive(value);
    this["__v_isShallow"] = isShallow2;
  }
  get value() {
    {
      this.dep.track();
    }
    return this._value;
  }
  set value(newValue) {
    const oldValue = this._rawValue;
    const useDirectValue = this["__v_isShallow"] || isShallow(newValue) || isReadonly(newValue);
    newValue = useDirectValue ? newValue : toRaw(newValue);
    if (hasChanged(newValue, oldValue)) {
      this._rawValue = newValue;
      this._value = useDirectValue ? newValue : toReactive(newValue);
      {
        this.dep.trigger();
      }
    }
  }
}
function unref(ref2) {
  return isRef(ref2) ? ref2.value : ref2;
}
function toValue$3(source) {
  return isFunction(source) ? source() : unref(source);
}
const shallowUnwrapHandlers = {
  get: (target, key, receiver) => key === "__v_raw" ? target : unref(Reflect.get(target, key, receiver)),
  set: (target, key, value, receiver) => {
    const oldValue = target[key];
    if (isRef(oldValue) && !isRef(value)) {
      oldValue.value = value;
      return true;
    } else {
      return Reflect.set(target, key, value, receiver);
    }
  }
};
function proxyRefs(objectWithRefs) {
  return isReactive(objectWithRefs) ? objectWithRefs : new Proxy(objectWithRefs, shallowUnwrapHandlers);
}
class CustomRefImpl {
  constructor(factory) {
    this["__v_isRef"] = true;
    this._value = void 0;
    const dep = this.dep = new Dep();
    const { get, set } = factory(dep.track.bind(dep), dep.trigger.bind(dep));
    this._get = get;
    this._set = set;
  }
  get value() {
    return this._value = this._get();
  }
  set value(newVal) {
    this._set(newVal);
  }
}
function customRef(factory) {
  return new CustomRefImpl(factory);
}
function toRefs(object) {
  const ret = isArray(object) ? new Array(object.length) : {};
  for (const key in object) {
    ret[key] = propertyToRef(object, key);
  }
  return ret;
}
class ObjectRefImpl {
  constructor(_object, _key, _defaultValue) {
    this._object = _object;
    this._key = _key;
    this._defaultValue = _defaultValue;
    this["__v_isRef"] = true;
    this._value = void 0;
    this._raw = toRaw(_object);
    let shallow = true;
    let obj = _object;
    if (!isArray(_object) || !isIntegerKey(String(_key))) {
      do {
        shallow = !isProxy(obj) || isShallow(obj);
      } while (shallow && (obj = obj["__v_raw"]));
    }
    this._shallow = shallow;
  }
  get value() {
    let val = this._object[this._key];
    if (this._shallow) {
      val = unref(val);
    }
    return this._value = val === void 0 ? this._defaultValue : val;
  }
  set value(newVal) {
    if (this._shallow && isRef(this._raw[this._key])) {
      const nestedRef = this._object[this._key];
      if (isRef(nestedRef)) {
        nestedRef.value = newVal;
        return;
      }
    }
    this._object[this._key] = newVal;
  }
  get dep() {
    return getDepFromReactive(this._raw, this._key);
  }
}
class GetterRefImpl {
  constructor(_getter) {
    this._getter = _getter;
    this["__v_isRef"] = true;
    this["__v_isReadonly"] = true;
    this._value = void 0;
  }
  get value() {
    return this._value = this._getter();
  }
}
function toRef$3(source, key, defaultValue) {
  if (isRef(source)) {
    return source;
  } else if (isFunction(source)) {
    return new GetterRefImpl(source);
  } else if (isObject$4(source) && arguments.length > 1) {
    return propertyToRef(source, key, defaultValue);
  } else {
    return ref(source);
  }
}
function propertyToRef(source, key, defaultValue) {
  return new ObjectRefImpl(source, key, defaultValue);
}
class ComputedRefImpl {
  constructor(fn, setter, isSSR) {
    this.fn = fn;
    this.setter = setter;
    this._value = void 0;
    this.dep = new Dep(this);
    this.__v_isRef = true;
    this.deps = void 0;
    this.depsTail = void 0;
    this.flags = 16;
    this.globalVersion = globalVersion - 1;
    this.next = void 0;
    this.effect = this;
    this["__v_isReadonly"] = !setter;
    this.isSSR = isSSR;
  }
  /**
   * @internal
   */
  notify() {
    this.flags |= 16;
    if (!(this.flags & 8) && // avoid infinite self recursion
    activeSub !== this) {
      batch(this, true);
      return true;
    }
  }
  get value() {
    const link = this.dep.track();
    refreshComputed(this);
    if (link) {
      link.version = this.dep.version;
    }
    return this._value;
  }
  set value(newValue) {
    if (this.setter) {
      this.setter(newValue);
    }
  }
}
function computed$1(getterOrOptions, debugOptions, isSSR = false) {
  let getter;
  let setter;
  if (isFunction(getterOrOptions)) {
    getter = getterOrOptions;
  } else {
    getter = getterOrOptions.get;
    setter = getterOrOptions.set;
  }
  const cRef = new ComputedRefImpl(getter, setter, isSSR);
  return cRef;
}
const INITIAL_WATCHER_VALUE = {};
const cleanupMap = /* @__PURE__ */ new WeakMap();
let activeWatcher = void 0;
function onWatcherCleanup(cleanupFn, failSilently = false, owner = activeWatcher) {
  if (owner) {
    let cleanups = cleanupMap.get(owner);
    if (!cleanups) cleanupMap.set(owner, cleanups = []);
    cleanups.push(cleanupFn);
  }
}
function watch$1(source, cb, options = EMPTY_OBJ) {
  const { immediate, deep, once, scheduler, augmentJob, call } = options;
  const reactiveGetter = (source2) => {
    if (deep) return source2;
    if (isShallow(source2) || deep === false || deep === 0)
      return traverse(source2, 1);
    return traverse(source2);
  };
  let effect2;
  let getter;
  let cleanup;
  let boundCleanup;
  let forceTrigger = false;
  let isMultiSource = false;
  if (isRef(source)) {
    getter = () => source.value;
    forceTrigger = isShallow(source);
  } else if (isReactive(source)) {
    getter = () => reactiveGetter(source);
    forceTrigger = true;
  } else if (isArray(source)) {
    isMultiSource = true;
    forceTrigger = source.some((s) => isReactive(s) || isShallow(s));
    getter = () => source.map((s) => {
      if (isRef(s)) {
        return s.value;
      } else if (isReactive(s)) {
        return reactiveGetter(s);
      } else if (isFunction(s)) {
        return call ? call(s, 2) : s();
      } else ;
    });
  } else if (isFunction(source)) {
    if (cb) {
      getter = call ? () => call(source, 2) : source;
    } else {
      getter = () => {
        if (cleanup) {
          pauseTracking();
          try {
            cleanup();
          } finally {
            resetTracking();
          }
        }
        const currentEffect = activeWatcher;
        activeWatcher = effect2;
        try {
          return call ? call(source, 3, [boundCleanup]) : source(boundCleanup);
        } finally {
          activeWatcher = currentEffect;
        }
      };
    }
  } else {
    getter = NOOP;
  }
  if (cb && deep) {
    const baseGetter = getter;
    const depth = deep === true ? Infinity : deep;
    getter = () => traverse(baseGetter(), depth);
  }
  const scope = getCurrentScope();
  const watchHandle = () => {
    effect2.stop();
    if (scope && scope.active) {
      remove(scope.effects, effect2);
    }
  };
  if (once && cb) {
    const _cb = cb;
    cb = (...args) => {
      _cb(...args);
      watchHandle();
    };
  }
  let oldValue = isMultiSource ? new Array(source.length).fill(INITIAL_WATCHER_VALUE) : INITIAL_WATCHER_VALUE;
  const job = (immediateFirstRun) => {
    if (!(effect2.flags & 1) || !effect2.dirty && !immediateFirstRun) {
      return;
    }
    if (cb) {
      const newValue = effect2.run();
      if (deep || forceTrigger || (isMultiSource ? newValue.some((v, i) => hasChanged(v, oldValue[i])) : hasChanged(newValue, oldValue))) {
        if (cleanup) {
          cleanup();
        }
        const currentWatcher = activeWatcher;
        activeWatcher = effect2;
        try {
          const args = [
            newValue,
            // pass undefined as the old value when it's changed for the first time
            oldValue === INITIAL_WATCHER_VALUE ? void 0 : isMultiSource && oldValue[0] === INITIAL_WATCHER_VALUE ? [] : oldValue,
            boundCleanup
          ];
          oldValue = newValue;
          call ? call(cb, 3, args) : (
            // @ts-expect-error
            cb(...args)
          );
        } finally {
          activeWatcher = currentWatcher;
        }
      }
    } else {
      effect2.run();
    }
  };
  if (augmentJob) {
    augmentJob(job);
  }
  effect2 = new ReactiveEffect(getter);
  effect2.scheduler = scheduler ? () => scheduler(job, false) : job;
  boundCleanup = (fn) => onWatcherCleanup(fn, false, effect2);
  cleanup = effect2.onStop = () => {
    const cleanups = cleanupMap.get(effect2);
    if (cleanups) {
      if (call) {
        call(cleanups, 4);
      } else {
        for (const cleanup2 of cleanups) cleanup2();
      }
      cleanupMap.delete(effect2);
    }
  };
  if (cb) {
    if (immediate) {
      job(true);
    } else {
      oldValue = effect2.run();
    }
  } else if (scheduler) {
    scheduler(job.bind(null, true), true);
  } else {
    effect2.run();
  }
  watchHandle.pause = effect2.pause.bind(effect2);
  watchHandle.resume = effect2.resume.bind(effect2);
  watchHandle.stop = watchHandle;
  return watchHandle;
}
function traverse(value, depth = Infinity, seen) {
  if (depth <= 0 || !isObject$4(value) || value["__v_skip"]) {
    return value;
  }
  seen = seen || /* @__PURE__ */ new Map();
  if ((seen.get(value) || 0) >= depth) {
    return value;
  }
  seen.set(value, depth);
  depth--;
  if (isRef(value)) {
    traverse(value.value, depth, seen);
  } else if (isArray(value)) {
    for (let i = 0; i < value.length; i++) {
      traverse(value[i], depth, seen);
    }
  } else if (isSet(value) || isMap(value)) {
    value.forEach((v) => {
      traverse(v, depth, seen);
    });
  } else if (isPlainObject$1(value)) {
    for (const key in value) {
      traverse(value[key], depth, seen);
    }
    for (const key of Object.getOwnPropertySymbols(value)) {
      if (Object.prototype.propertyIsEnumerable.call(value, key)) {
        traverse(value[key], depth, seen);
      }
    }
  }
  return value;
}
/**
* @vue/runtime-core v3.5.25
* (c) 2018-present Yuxi (Evan) You and Vue contributors
* @license MIT
**/
const stack = [];
let isWarning = false;
function warn$1(msg, ...args) {
  if (isWarning) return;
  isWarning = true;
  pauseTracking();
  const instance = stack.length ? stack[stack.length - 1].component : null;
  const appWarnHandler = instance && instance.appContext.config.warnHandler;
  const trace = getComponentTrace();
  if (appWarnHandler) {
    callWithErrorHandling(
      appWarnHandler,
      instance,
      11,
      [
        // eslint-disable-next-line no-restricted-syntax
        msg + args.map((a) => {
          var _a3, _b2;
          return (_b2 = (_a3 = a.toString) == null ? void 0 : _a3.call(a)) != null ? _b2 : JSON.stringify(a);
        }).join(""),
        instance && instance.proxy,
        trace.map(
          ({ vnode }) => `at <${formatComponentName(instance, vnode.type)}>`
        ).join("\n"),
        trace
      ]
    );
  } else {
    const warnArgs = [`[Vue warn]: ${msg}`, ...args];
    if (trace.length && // avoid spamming console during tests
    true) {
      warnArgs.push(`
`, ...formatTrace(trace));
    }
    console.warn(...warnArgs);
  }
  resetTracking();
  isWarning = false;
}
function getComponentTrace() {
  let currentVNode = stack[stack.length - 1];
  if (!currentVNode) {
    return [];
  }
  const normalizedStack = [];
  while (currentVNode) {
    const last = normalizedStack[0];
    if (last && last.vnode === currentVNode) {
      last.recurseCount++;
    } else {
      normalizedStack.push({
        vnode: currentVNode,
        recurseCount: 0
      });
    }
    const parentInstance = currentVNode.component && currentVNode.component.parent;
    currentVNode = parentInstance && parentInstance.vnode;
  }
  return normalizedStack;
}
function formatTrace(trace) {
  const logs = [];
  trace.forEach((entry, i) => {
    logs.push(...i === 0 ? [] : [`
`], ...formatTraceEntry(entry));
  });
  return logs;
}
function formatTraceEntry({ vnode, recurseCount }) {
  const postfix = recurseCount > 0 ? `... (${recurseCount} recursive calls)` : ``;
  const isRoot = vnode.component ? vnode.component.parent == null : false;
  const open = ` at <${formatComponentName(
    vnode.component,
    vnode.type,
    isRoot
  )}`;
  const close2 = `>` + postfix;
  return vnode.props ? [open, ...formatProps(vnode.props), close2] : [open + close2];
}
function formatProps(props) {
  const res = [];
  const keys = Object.keys(props);
  keys.slice(0, 3).forEach((key) => {
    res.push(...formatProp(key, props[key]));
  });
  if (keys.length > 3) {
    res.push(` ...`);
  }
  return res;
}
function formatProp(key, value, raw) {
  if (isString(value)) {
    value = JSON.stringify(value);
    return raw ? value : [`${key}=${value}`];
  } else if (typeof value === "number" || typeof value === "boolean" || value == null) {
    return raw ? value : [`${key}=${value}`];
  } else if (isRef(value)) {
    value = formatProp(key, toRaw(value.value), true);
    return raw ? value : [`${key}=Ref<`, value, `>`];
  } else if (isFunction(value)) {
    return [`${key}=fn${value.name ? `<${value.name}>` : ``}`];
  } else {
    value = toRaw(value);
    return raw ? value : [`${key}=`, value];
  }
}
function callWithErrorHandling(fn, instance, type, args) {
  try {
    return args ? fn(...args) : fn();
  } catch (err) {
    handleError(err, instance, type);
  }
}
function callWithAsyncErrorHandling(fn, instance, type, args) {
  if (isFunction(fn)) {
    const res = callWithErrorHandling(fn, instance, type, args);
    if (res && isPromise(res)) {
      res.catch((err) => {
        handleError(err, instance, type);
      });
    }
    return res;
  }
  if (isArray(fn)) {
    const values = [];
    for (let i = 0; i < fn.length; i++) {
      values.push(callWithAsyncErrorHandling(fn[i], instance, type, args));
    }
    return values;
  }
}
function handleError(err, instance, type, throwInDev = true) {
  const contextVNode = instance ? instance.vnode : null;
  const { errorHandler, throwUnhandledErrorInProduction } = instance && instance.appContext.config || EMPTY_OBJ;
  if (instance) {
    let cur = instance.parent;
    const exposedInstance = instance.proxy;
    const errorInfo = `https://vuejs.org/error-reference/#runtime-${type}`;
    while (cur) {
      const errorCapturedHooks = cur.ec;
      if (errorCapturedHooks) {
        for (let i = 0; i < errorCapturedHooks.length; i++) {
          if (errorCapturedHooks[i](err, exposedInstance, errorInfo) === false) {
            return;
          }
        }
      }
      cur = cur.parent;
    }
    if (errorHandler) {
      pauseTracking();
      callWithErrorHandling(errorHandler, null, 10, [
        err,
        exposedInstance,
        errorInfo
      ]);
      resetTracking();
      return;
    }
  }
  logError(err, type, contextVNode, throwInDev, throwUnhandledErrorInProduction);
}
function logError(err, type, contextVNode, throwInDev = true, throwInProd = false) {
  if (throwInProd) {
    throw err;
  } else {
    console.error(err);
  }
}
const queue = [];
let flushIndex = -1;
const pendingPostFlushCbs = [];
let activePostFlushCbs = null;
let postFlushIndex = 0;
const resolvedPromise = /* @__PURE__ */ Promise.resolve();
let currentFlushPromise = null;
function nextTick(fn) {
  const p2 = currentFlushPromise || resolvedPromise;
  return fn ? p2.then(this ? fn.bind(this) : fn) : p2;
}
function findInsertionIndex(id2) {
  let start = flushIndex + 1;
  let end = queue.length;
  while (start < end) {
    const middle = start + end >>> 1;
    const middleJob = queue[middle];
    const middleJobId = getId(middleJob);
    if (middleJobId < id2 || middleJobId === id2 && middleJob.flags & 2) {
      start = middle + 1;
    } else {
      end = middle;
    }
  }
  return start;
}
function queueJob(job) {
  if (!(job.flags & 1)) {
    const jobId = getId(job);
    const lastJob = queue[queue.length - 1];
    if (!lastJob || // fast path when the job id is larger than the tail
    !(job.flags & 2) && jobId >= getId(lastJob)) {
      queue.push(job);
    } else {
      queue.splice(findInsertionIndex(jobId), 0, job);
    }
    job.flags |= 1;
    queueFlush();
  }
}
function queueFlush() {
  if (!currentFlushPromise) {
    currentFlushPromise = resolvedPromise.then(flushJobs);
  }
}
function queuePostFlushCb(cb) {
  if (!isArray(cb)) {
    if (activePostFlushCbs && cb.id === -1) {
      activePostFlushCbs.splice(postFlushIndex + 1, 0, cb);
    } else if (!(cb.flags & 1)) {
      pendingPostFlushCbs.push(cb);
      cb.flags |= 1;
    }
  } else {
    pendingPostFlushCbs.push(...cb);
  }
  queueFlush();
}
function flushPreFlushCbs(instance, seen, i = flushIndex + 1) {
  for (; i < queue.length; i++) {
    const cb = queue[i];
    if (cb && cb.flags & 2) {
      if (instance && cb.id !== instance.uid) {
        continue;
      }
      queue.splice(i, 1);
      i--;
      if (cb.flags & 4) {
        cb.flags &= -2;
      }
      cb();
      if (!(cb.flags & 4)) {
        cb.flags &= -2;
      }
    }
  }
}
function flushPostFlushCbs(seen) {
  if (pendingPostFlushCbs.length) {
    const deduped = [...new Set(pendingPostFlushCbs)].sort(
      (a, b) => getId(a) - getId(b)
    );
    pendingPostFlushCbs.length = 0;
    if (activePostFlushCbs) {
      activePostFlushCbs.push(...deduped);
      return;
    }
    activePostFlushCbs = deduped;
    for (postFlushIndex = 0; postFlushIndex < activePostFlushCbs.length; postFlushIndex++) {
      const cb = activePostFlushCbs[postFlushIndex];
      if (cb.flags & 4) {
        cb.flags &= -2;
      }
      if (!(cb.flags & 8)) cb();
      cb.flags &= -2;
    }
    activePostFlushCbs = null;
    postFlushIndex = 0;
  }
}
const getId = (job) => job.id == null ? job.flags & 2 ? -1 : Infinity : job.id;
function flushJobs(seen) {
  try {
    for (flushIndex = 0; flushIndex < queue.length; flushIndex++) {
      const job = queue[flushIndex];
      if (job && !(job.flags & 8)) {
        if (false) ;
        if (job.flags & 4) {
          job.flags &= ~1;
        }
        callWithErrorHandling(
          job,
          job.i,
          job.i ? 15 : 14
        );
        if (!(job.flags & 4)) {
          job.flags &= ~1;
        }
      }
    }
  } finally {
    for (; flushIndex < queue.length; flushIndex++) {
      const job = queue[flushIndex];
      if (job) {
        job.flags &= -2;
      }
    }
    flushIndex = -1;
    queue.length = 0;
    flushPostFlushCbs();
    currentFlushPromise = null;
    if (queue.length || pendingPostFlushCbs.length) {
      flushJobs();
    }
  }
}
let currentRenderingInstance = null;
let currentScopeId = null;
function setCurrentRenderingInstance(instance) {
  const prev = currentRenderingInstance;
  currentRenderingInstance = instance;
  currentScopeId = instance && instance.type.__scopeId || null;
  return prev;
}
function pushScopeId(id2) {
  currentScopeId = id2;
}
function popScopeId() {
  currentScopeId = null;
}
function withCtx(fn, ctx = currentRenderingInstance, isNonScopedSlot) {
  if (!ctx) return fn;
  if (fn._n) {
    return fn;
  }
  const renderFnWithContext = (...args) => {
    if (renderFnWithContext._d) {
      setBlockTracking(-1);
    }
    const prevInstance = setCurrentRenderingInstance(ctx);
    let res;
    try {
      res = fn(...args);
    } finally {
      setCurrentRenderingInstance(prevInstance);
      if (renderFnWithContext._d) {
        setBlockTracking(1);
      }
    }
    return res;
  };
  renderFnWithContext._n = true;
  renderFnWithContext._c = true;
  renderFnWithContext._d = true;
  return renderFnWithContext;
}
function withDirectives(vnode, directives) {
  if (currentRenderingInstance === null) {
    return vnode;
  }
  const instance = getComponentPublicInstance(currentRenderingInstance);
  const bindings = vnode.dirs || (vnode.dirs = []);
  for (let i = 0; i < directives.length; i++) {
    let [dir, value, arg, modifiers = EMPTY_OBJ] = directives[i];
    if (dir) {
      if (isFunction(dir)) {
        dir = {
          mounted: dir,
          updated: dir
        };
      }
      if (dir.deep) {
        traverse(value);
      }
      bindings.push({
        dir,
        instance,
        value,
        oldValue: void 0,
        arg,
        modifiers
      });
    }
  }
  return vnode;
}
function invokeDirectiveHook(vnode, prevVNode, instance, name2) {
  const bindings = vnode.dirs;
  const oldBindings = prevVNode && prevVNode.dirs;
  for (let i = 0; i < bindings.length; i++) {
    const binding = bindings[i];
    if (oldBindings) {
      binding.oldValue = oldBindings[i].value;
    }
    let hook = binding.dir[name2];
    if (hook) {
      pauseTracking();
      callWithAsyncErrorHandling(hook, instance, 8, [
        vnode.el,
        binding,
        vnode,
        prevVNode
      ]);
      resetTracking();
    }
  }
}
const TeleportEndKey = Symbol("_vte");
const isTeleport = (type) => type.__isTeleport;
const isTeleportDisabled = (props) => props && (props.disabled || props.disabled === "");
const isTeleportDeferred = (props) => props && (props.defer || props.defer === "");
const isTargetSVG = (target) => typeof SVGElement !== "undefined" && target instanceof SVGElement;
const isTargetMathML = (target) => typeof MathMLElement === "function" && target instanceof MathMLElement;
const resolveTarget = (props, select) => {
  const targetSelector = props && props.to;
  if (isString(targetSelector)) {
    if (!select) {
      return null;
    } else {
      const target = select(targetSelector);
      return target;
    }
  } else {
    return targetSelector;
  }
};
const TeleportImpl = {
  name: "Teleport",
  __isTeleport: true,
  process(n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, internals) {
    const {
      mc: mountChildren,
      pc: patchChildren,
      pbc: patchBlockChildren,
      o: { insert, querySelector, createText, createComment }
    } = internals;
    const disabled = isTeleportDisabled(n2.props);
    let { shapeFlag, children, dynamicChildren } = n2;
    if (n1 == null) {
      const placeholder = n2.el = createText("");
      const mainAnchor = n2.anchor = createText("");
      insert(placeholder, container, anchor);
      insert(mainAnchor, container, anchor);
      const mount2 = (container2, anchor2) => {
        if (shapeFlag & 16) {
          mountChildren(
            children,
            container2,
            anchor2,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        }
      };
      const mountToTarget = () => {
        const target = n2.target = resolveTarget(n2.props, querySelector);
        const targetAnchor = prepareAnchor(target, n2, createText, insert);
        if (target) {
          if (namespace !== "svg" && isTargetSVG(target)) {
            namespace = "svg";
          } else if (namespace !== "mathml" && isTargetMathML(target)) {
            namespace = "mathml";
          }
          if (parentComponent && parentComponent.isCE) {
            (parentComponent.ce._teleportTargets || (parentComponent.ce._teleportTargets = /* @__PURE__ */ new Set())).add(target);
          }
          if (!disabled) {
            mount2(target, targetAnchor);
            updateCssVars(n2, false);
          }
        }
      };
      if (disabled) {
        mount2(container, mainAnchor);
        updateCssVars(n2, true);
      }
      if (isTeleportDeferred(n2.props)) {
        n2.el.__isMounted = false;
        queuePostRenderEffect(() => {
          mountToTarget();
          delete n2.el.__isMounted;
        }, parentSuspense);
      } else {
        mountToTarget();
      }
    } else {
      if (isTeleportDeferred(n2.props) && n1.el.__isMounted === false) {
        queuePostRenderEffect(() => {
          TeleportImpl.process(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized,
            internals
          );
        }, parentSuspense);
        return;
      }
      n2.el = n1.el;
      n2.targetStart = n1.targetStart;
      const mainAnchor = n2.anchor = n1.anchor;
      const target = n2.target = n1.target;
      const targetAnchor = n2.targetAnchor = n1.targetAnchor;
      const wasDisabled = isTeleportDisabled(n1.props);
      const currentContainer = wasDisabled ? container : target;
      const currentAnchor = wasDisabled ? mainAnchor : targetAnchor;
      if (namespace === "svg" || isTargetSVG(target)) {
        namespace = "svg";
      } else if (namespace === "mathml" || isTargetMathML(target)) {
        namespace = "mathml";
      }
      if (dynamicChildren) {
        patchBlockChildren(
          n1.dynamicChildren,
          dynamicChildren,
          currentContainer,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds
        );
        traverseStaticChildren(n1, n2, true);
      } else if (!optimized) {
        patchChildren(
          n1,
          n2,
          currentContainer,
          currentAnchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          false
        );
      }
      if (disabled) {
        if (!wasDisabled) {
          moveTeleport(
            n2,
            container,
            mainAnchor,
            internals,
            1
          );
        } else {
          if (n2.props && n1.props && n2.props.to !== n1.props.to) {
            n2.props.to = n1.props.to;
          }
        }
      } else {
        if ((n2.props && n2.props.to) !== (n1.props && n1.props.to)) {
          const nextTarget = n2.target = resolveTarget(
            n2.props,
            querySelector
          );
          if (nextTarget) {
            moveTeleport(
              n2,
              nextTarget,
              null,
              internals,
              0
            );
          }
        } else if (wasDisabled) {
          moveTeleport(
            n2,
            target,
            targetAnchor,
            internals,
            1
          );
        }
      }
      updateCssVars(n2, disabled);
    }
  },
  remove(vnode, parentComponent, parentSuspense, { um: unmount, o: { remove: hostRemove } }, doRemove) {
    const {
      shapeFlag,
      children,
      anchor,
      targetStart,
      targetAnchor,
      target,
      props
    } = vnode;
    if (target) {
      hostRemove(targetStart);
      hostRemove(targetAnchor);
    }
    doRemove && hostRemove(anchor);
    if (shapeFlag & 16) {
      const shouldRemove = doRemove || !isTeleportDisabled(props);
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        unmount(
          child,
          parentComponent,
          parentSuspense,
          shouldRemove,
          !!child.dynamicChildren
        );
      }
    }
  },
  move: moveTeleport,
  hydrate: hydrateTeleport
};
function moveTeleport(vnode, container, parentAnchor, { o: { insert }, m: move }, moveType = 2) {
  if (moveType === 0) {
    insert(vnode.targetAnchor, container, parentAnchor);
  }
  const { el: el2, anchor, shapeFlag, children, props } = vnode;
  const isReorder = moveType === 2;
  if (isReorder) {
    insert(el2, container, parentAnchor);
  }
  if (!isReorder || isTeleportDisabled(props)) {
    if (shapeFlag & 16) {
      for (let i = 0; i < children.length; i++) {
        move(
          children[i],
          container,
          parentAnchor,
          2
        );
      }
    }
  }
  if (isReorder) {
    insert(anchor, container, parentAnchor);
  }
}
function hydrateTeleport(node, vnode, parentComponent, parentSuspense, slotScopeIds, optimized, {
  o: { nextSibling, parentNode, querySelector, insert, createText }
}, hydrateChildren) {
  function hydrateDisabledTeleport(node2, vnode2, targetStart, targetAnchor) {
    vnode2.anchor = hydrateChildren(
      nextSibling(node2),
      vnode2,
      parentNode(node2),
      parentComponent,
      parentSuspense,
      slotScopeIds,
      optimized
    );
    vnode2.targetStart = targetStart;
    vnode2.targetAnchor = targetAnchor;
  }
  const target = vnode.target = resolveTarget(
    vnode.props,
    querySelector
  );
  const disabled = isTeleportDisabled(vnode.props);
  if (target) {
    const targetNode = target._lpa || target.firstChild;
    if (vnode.shapeFlag & 16) {
      if (disabled) {
        hydrateDisabledTeleport(
          node,
          vnode,
          targetNode,
          targetNode && nextSibling(targetNode)
        );
      } else {
        vnode.anchor = nextSibling(node);
        let targetAnchor = targetNode;
        while (targetAnchor) {
          if (targetAnchor && targetAnchor.nodeType === 8) {
            if (targetAnchor.data === "teleport start anchor") {
              vnode.targetStart = targetAnchor;
            } else if (targetAnchor.data === "teleport anchor") {
              vnode.targetAnchor = targetAnchor;
              target._lpa = vnode.targetAnchor && nextSibling(vnode.targetAnchor);
              break;
            }
          }
          targetAnchor = nextSibling(targetAnchor);
        }
        if (!vnode.targetAnchor) {
          prepareAnchor(target, vnode, createText, insert);
        }
        hydrateChildren(
          targetNode && nextSibling(targetNode),
          vnode,
          target,
          parentComponent,
          parentSuspense,
          slotScopeIds,
          optimized
        );
      }
    }
    updateCssVars(vnode, disabled);
  } else if (disabled) {
    if (vnode.shapeFlag & 16) {
      hydrateDisabledTeleport(node, vnode, node, nextSibling(node));
    }
  }
  return vnode.anchor && nextSibling(vnode.anchor);
}
const Teleport = TeleportImpl;
function updateCssVars(vnode, isDisabled) {
  const ctx = vnode.ctx;
  if (ctx && ctx.ut) {
    let node, anchor;
    if (isDisabled) {
      node = vnode.el;
      anchor = vnode.anchor;
    } else {
      node = vnode.targetStart;
      anchor = vnode.targetAnchor;
    }
    while (node && node !== anchor) {
      if (node.nodeType === 1) node.setAttribute("data-v-owner", ctx.uid);
      node = node.nextSibling;
    }
    ctx.ut();
  }
}
function prepareAnchor(target, vnode, createText, insert) {
  const targetStart = vnode.targetStart = createText("");
  const targetAnchor = vnode.targetAnchor = createText("");
  targetStart[TeleportEndKey] = targetAnchor;
  if (target) {
    insert(targetStart, target);
    insert(targetAnchor, target);
  }
  return targetAnchor;
}
const leaveCbKey = Symbol("_leaveCb");
const enterCbKey$1 = Symbol("_enterCb");
function useTransitionState() {
  const state = {
    isMounted: false,
    isLeaving: false,
    isUnmounting: false,
    leavingVNodes: /* @__PURE__ */ new Map()
  };
  onMounted(() => {
    state.isMounted = true;
  });
  onBeforeUnmount(() => {
    state.isUnmounting = true;
  });
  return state;
}
const TransitionHookValidator = [Function, Array];
const BaseTransitionPropsValidators = {
  mode: String,
  appear: Boolean,
  persisted: Boolean,
  // enter
  onBeforeEnter: TransitionHookValidator,
  onEnter: TransitionHookValidator,
  onAfterEnter: TransitionHookValidator,
  onEnterCancelled: TransitionHookValidator,
  // leave
  onBeforeLeave: TransitionHookValidator,
  onLeave: TransitionHookValidator,
  onAfterLeave: TransitionHookValidator,
  onLeaveCancelled: TransitionHookValidator,
  // appear
  onBeforeAppear: TransitionHookValidator,
  onAppear: TransitionHookValidator,
  onAfterAppear: TransitionHookValidator,
  onAppearCancelled: TransitionHookValidator
};
const recursiveGetSubtree = (instance) => {
  const subTree = instance.subTree;
  return subTree.component ? recursiveGetSubtree(subTree.component) : subTree;
};
const BaseTransitionImpl = {
  name: `BaseTransition`,
  props: BaseTransitionPropsValidators,
  setup(props, { slots }) {
    const instance = getCurrentInstance();
    const state = useTransitionState();
    return () => {
      const children = slots.default && getTransitionRawChildren(slots.default(), true);
      if (!children || !children.length) {
        return;
      }
      const child = findNonCommentChild(children);
      const rawProps = toRaw(props);
      const { mode } = rawProps;
      if (state.isLeaving) {
        return emptyPlaceholder(child);
      }
      const innerChild = getInnerChild$1(child);
      if (!innerChild) {
        return emptyPlaceholder(child);
      }
      let enterHooks = resolveTransitionHooks(
        innerChild,
        rawProps,
        state,
        instance,
        // #11061, ensure enterHooks is fresh after clone
        (hooks) => enterHooks = hooks
      );
      if (innerChild.type !== Comment) {
        setTransitionHooks(innerChild, enterHooks);
      }
      let oldInnerChild = instance.subTree && getInnerChild$1(instance.subTree);
      if (oldInnerChild && oldInnerChild.type !== Comment && !isSameVNodeType(oldInnerChild, innerChild) && recursiveGetSubtree(instance).type !== Comment) {
        let leavingHooks = resolveTransitionHooks(
          oldInnerChild,
          rawProps,
          state,
          instance
        );
        setTransitionHooks(oldInnerChild, leavingHooks);
        if (mode === "out-in" && innerChild.type !== Comment) {
          state.isLeaving = true;
          leavingHooks.afterLeave = () => {
            state.isLeaving = false;
            if (!(instance.job.flags & 8)) {
              instance.update();
            }
            delete leavingHooks.afterLeave;
            oldInnerChild = void 0;
          };
          return emptyPlaceholder(child);
        } else if (mode === "in-out" && innerChild.type !== Comment) {
          leavingHooks.delayLeave = (el2, earlyRemove, delayedLeave) => {
            const leavingVNodesCache = getLeavingNodesForType(
              state,
              oldInnerChild
            );
            leavingVNodesCache[String(oldInnerChild.key)] = oldInnerChild;
            el2[leaveCbKey] = () => {
              earlyRemove();
              el2[leaveCbKey] = void 0;
              delete enterHooks.delayedLeave;
              oldInnerChild = void 0;
            };
            enterHooks.delayedLeave = () => {
              delayedLeave();
              delete enterHooks.delayedLeave;
              oldInnerChild = void 0;
            };
          };
        } else {
          oldInnerChild = void 0;
        }
      } else if (oldInnerChild) {
        oldInnerChild = void 0;
      }
      return child;
    };
  }
};
function findNonCommentChild(children) {
  let child = children[0];
  if (children.length > 1) {
    for (const c of children) {
      if (c.type !== Comment) {
        child = c;
        break;
      }
    }
  }
  return child;
}
const BaseTransition = BaseTransitionImpl;
function getLeavingNodesForType(state, vnode) {
  const { leavingVNodes } = state;
  let leavingVNodesCache = leavingVNodes.get(vnode.type);
  if (!leavingVNodesCache) {
    leavingVNodesCache = /* @__PURE__ */ Object.create(null);
    leavingVNodes.set(vnode.type, leavingVNodesCache);
  }
  return leavingVNodesCache;
}
function resolveTransitionHooks(vnode, props, state, instance, postClone) {
  const {
    appear,
    mode,
    persisted = false,
    onBeforeEnter,
    onEnter,
    onAfterEnter,
    onEnterCancelled,
    onBeforeLeave,
    onLeave,
    onAfterLeave,
    onLeaveCancelled,
    onBeforeAppear,
    onAppear,
    onAfterAppear,
    onAppearCancelled
  } = props;
  const key = String(vnode.key);
  const leavingVNodesCache = getLeavingNodesForType(state, vnode);
  const callHook2 = (hook, args) => {
    hook && callWithAsyncErrorHandling(
      hook,
      instance,
      9,
      args
    );
  };
  const callAsyncHook = (hook, args) => {
    const done = args[1];
    callHook2(hook, args);
    if (isArray(hook)) {
      if (hook.every((hook2) => hook2.length <= 1)) done();
    } else if (hook.length <= 1) {
      done();
    }
  };
  const hooks = {
    mode,
    persisted,
    beforeEnter(el2) {
      let hook = onBeforeEnter;
      if (!state.isMounted) {
        if (appear) {
          hook = onBeforeAppear || onBeforeEnter;
        } else {
          return;
        }
      }
      if (el2[leaveCbKey]) {
        el2[leaveCbKey](
          true
          /* cancelled */
        );
      }
      const leavingVNode = leavingVNodesCache[key];
      if (leavingVNode && isSameVNodeType(vnode, leavingVNode) && leavingVNode.el[leaveCbKey]) {
        leavingVNode.el[leaveCbKey]();
      }
      callHook2(hook, [el2]);
    },
    enter(el2) {
      let hook = onEnter;
      let afterHook = onAfterEnter;
      let cancelHook = onEnterCancelled;
      if (!state.isMounted) {
        if (appear) {
          hook = onAppear || onEnter;
          afterHook = onAfterAppear || onAfterEnter;
          cancelHook = onAppearCancelled || onEnterCancelled;
        } else {
          return;
        }
      }
      let called = false;
      const done = el2[enterCbKey$1] = (cancelled) => {
        if (called) return;
        called = true;
        if (cancelled) {
          callHook2(cancelHook, [el2]);
        } else {
          callHook2(afterHook, [el2]);
        }
        if (hooks.delayedLeave) {
          hooks.delayedLeave();
        }
        el2[enterCbKey$1] = void 0;
      };
      if (hook) {
        callAsyncHook(hook, [el2, done]);
      } else {
        done();
      }
    },
    leave(el2, remove2) {
      const key2 = String(vnode.key);
      if (el2[enterCbKey$1]) {
        el2[enterCbKey$1](
          true
          /* cancelled */
        );
      }
      if (state.isUnmounting) {
        return remove2();
      }
      callHook2(onBeforeLeave, [el2]);
      let called = false;
      const done = el2[leaveCbKey] = (cancelled) => {
        if (called) return;
        called = true;
        remove2();
        if (cancelled) {
          callHook2(onLeaveCancelled, [el2]);
        } else {
          callHook2(onAfterLeave, [el2]);
        }
        el2[leaveCbKey] = void 0;
        if (leavingVNodesCache[key2] === vnode) {
          delete leavingVNodesCache[key2];
        }
      };
      leavingVNodesCache[key2] = vnode;
      if (onLeave) {
        callAsyncHook(onLeave, [el2, done]);
      } else {
        done();
      }
    },
    clone(vnode2) {
      const hooks2 = resolveTransitionHooks(
        vnode2,
        props,
        state,
        instance,
        postClone
      );
      if (postClone) postClone(hooks2);
      return hooks2;
    }
  };
  return hooks;
}
function emptyPlaceholder(vnode) {
  if (isKeepAlive(vnode)) {
    vnode = cloneVNode(vnode);
    vnode.children = null;
    return vnode;
  }
}
function getInnerChild$1(vnode) {
  if (!isKeepAlive(vnode)) {
    if (isTeleport(vnode.type) && vnode.children) {
      return findNonCommentChild(vnode.children);
    }
    return vnode;
  }
  if (vnode.component) {
    return vnode.component.subTree;
  }
  const { shapeFlag, children } = vnode;
  if (children) {
    if (shapeFlag & 16) {
      return children[0];
    }
    if (shapeFlag & 32 && isFunction(children.default)) {
      return children.default();
    }
  }
}
function setTransitionHooks(vnode, hooks) {
  if (vnode.shapeFlag & 6 && vnode.component) {
    vnode.transition = hooks;
    setTransitionHooks(vnode.component.subTree, hooks);
  } else if (vnode.shapeFlag & 128) {
    vnode.ssContent.transition = hooks.clone(vnode.ssContent);
    vnode.ssFallback.transition = hooks.clone(vnode.ssFallback);
  } else {
    vnode.transition = hooks;
  }
}
function getTransitionRawChildren(children, keepComment = false, parentKey) {
  let ret = [];
  let keyedFragmentCount = 0;
  for (let i = 0; i < children.length; i++) {
    let child = children[i];
    const key = parentKey == null ? child.key : String(parentKey) + String(child.key != null ? child.key : i);
    if (child.type === Fragment) {
      if (child.patchFlag & 128) keyedFragmentCount++;
      ret = ret.concat(
        getTransitionRawChildren(child.children, keepComment, key)
      );
    } else if (keepComment || child.type !== Comment) {
      ret.push(key != null ? cloneVNode(child, { key }) : child);
    }
  }
  if (keyedFragmentCount > 1) {
    for (let i = 0; i < ret.length; i++) {
      ret[i].patchFlag = -2;
    }
  }
  return ret;
}
// @__NO_SIDE_EFFECTS__
function defineComponent(options, extraOptions) {
  return isFunction(options) ? (
    // #8236: extend call and options.name access are considered side-effects
    // by Rollup, so we have to wrap it in a pure-annotated IIFE.
    /* @__PURE__ */ (() => extend({ name: options.name }, extraOptions, { setup: options }))()
  ) : options;
}
function markAsyncBoundary(instance) {
  instance.ids = [instance.ids[0] + instance.ids[2]++ + "-", 0, 0];
}
function useTemplateRef(key) {
  const i = getCurrentInstance();
  const r = shallowRef(null);
  if (i) {
    const refs = i.refs === EMPTY_OBJ ? i.refs = {} : i.refs;
    {
      Object.defineProperty(refs, key, {
        enumerable: true,
        get: () => r.value,
        set: (val) => r.value = val
      });
    }
  }
  const ret = r;
  return ret;
}
const pendingSetRefMap = /* @__PURE__ */ new WeakMap();
function setRef(rawRef, oldRawRef, parentSuspense, vnode, isUnmount = false) {
  if (isArray(rawRef)) {
    rawRef.forEach(
      (r, i) => setRef(
        r,
        oldRawRef && (isArray(oldRawRef) ? oldRawRef[i] : oldRawRef),
        parentSuspense,
        vnode,
        isUnmount
      )
    );
    return;
  }
  if (isAsyncWrapper(vnode) && !isUnmount) {
    if (vnode.shapeFlag & 512 && vnode.type.__asyncResolved && vnode.component.subTree.component) {
      setRef(rawRef, oldRawRef, parentSuspense, vnode.component.subTree);
    }
    return;
  }
  const refValue = vnode.shapeFlag & 4 ? getComponentPublicInstance(vnode.component) : vnode.el;
  const value = isUnmount ? null : refValue;
  const { i: owner, r: ref3 } = rawRef;
  const oldRef = oldRawRef && oldRawRef.r;
  const refs = owner.refs === EMPTY_OBJ ? owner.refs = {} : owner.refs;
  const setupState = owner.setupState;
  const rawSetupState = toRaw(setupState);
  const canSetSetupRef = setupState === EMPTY_OBJ ? NO : (key) => {
    return hasOwn(rawSetupState, key);
  };
  if (oldRef != null && oldRef !== ref3) {
    invalidatePendingSetRef(oldRawRef);
    if (isString(oldRef)) {
      refs[oldRef] = null;
      if (canSetSetupRef(oldRef)) {
        setupState[oldRef] = null;
      }
    } else if (isRef(oldRef)) {
      {
        oldRef.value = null;
      }
      const oldRawRefAtom = oldRawRef;
      if (oldRawRefAtom.k) refs[oldRawRefAtom.k] = null;
    }
  }
  if (isFunction(ref3)) {
    callWithErrorHandling(ref3, owner, 12, [value, refs]);
  } else {
    const _isString = isString(ref3);
    const _isRef = isRef(ref3);
    if (_isString || _isRef) {
      const doSet = () => {
        if (rawRef.f) {
          const existing = _isString ? canSetSetupRef(ref3) ? setupState[ref3] : refs[ref3] : ref3.value;
          if (isUnmount) {
            isArray(existing) && remove(existing, refValue);
          } else {
            if (!isArray(existing)) {
              if (_isString) {
                refs[ref3] = [refValue];
                if (canSetSetupRef(ref3)) {
                  setupState[ref3] = refs[ref3];
                }
              } else {
                const newVal = [refValue];
                {
                  ref3.value = newVal;
                }
                if (rawRef.k) refs[rawRef.k] = newVal;
              }
            } else if (!existing.includes(refValue)) {
              existing.push(refValue);
            }
          }
        } else if (_isString) {
          refs[ref3] = value;
          if (canSetSetupRef(ref3)) {
            setupState[ref3] = value;
          }
        } else if (_isRef) {
          {
            ref3.value = value;
          }
          if (rawRef.k) refs[rawRef.k] = value;
        } else ;
      };
      if (value) {
        const job = () => {
          doSet();
          pendingSetRefMap.delete(rawRef);
        };
        job.id = -1;
        pendingSetRefMap.set(rawRef, job);
        queuePostRenderEffect(job, parentSuspense);
      } else {
        invalidatePendingSetRef(rawRef);
        doSet();
      }
    }
  }
}
function invalidatePendingSetRef(rawRef) {
  const pendingSetRef = pendingSetRefMap.get(rawRef);
  if (pendingSetRef) {
    pendingSetRef.flags |= 8;
    pendingSetRefMap.delete(rawRef);
  }
}
getGlobalThis().requestIdleCallback || ((cb) => setTimeout(cb, 1));
getGlobalThis().cancelIdleCallback || ((id2) => clearTimeout(id2));
const isAsyncWrapper = (i) => !!i.type.__asyncLoader;
const isKeepAlive = (vnode) => vnode.type.__isKeepAlive;
function onActivated(hook, target) {
  registerKeepAliveHook(hook, "a", target);
}
function onDeactivated(hook, target) {
  registerKeepAliveHook(hook, "da", target);
}
function registerKeepAliveHook(hook, type, target = currentInstance) {
  const wrappedHook = hook.__wdc || (hook.__wdc = () => {
    let current = target;
    while (current) {
      if (current.isDeactivated) {
        return;
      }
      current = current.parent;
    }
    return hook();
  });
  injectHook(type, wrappedHook, target);
  if (target) {
    let current = target.parent;
    while (current && current.parent) {
      if (isKeepAlive(current.parent.vnode)) {
        injectToKeepAliveRoot(wrappedHook, type, target, current);
      }
      current = current.parent;
    }
  }
}
function injectToKeepAliveRoot(hook, type, target, keepAliveRoot) {
  const injected = injectHook(
    type,
    hook,
    keepAliveRoot,
    true
    /* prepend */
  );
  onUnmounted(() => {
    remove(keepAliveRoot[type], injected);
  }, target);
}
function injectHook(type, hook, target = currentInstance, prepend = false) {
  if (target) {
    const hooks = target[type] || (target[type] = []);
    const wrappedHook = hook.__weh || (hook.__weh = (...args) => {
      pauseTracking();
      const reset2 = setCurrentInstance(target);
      const res = callWithAsyncErrorHandling(hook, target, type, args);
      reset2();
      resetTracking();
      return res;
    });
    if (prepend) {
      hooks.unshift(wrappedHook);
    } else {
      hooks.push(wrappedHook);
    }
    return wrappedHook;
  }
}
const createHook = (lifecycle) => (hook, target = currentInstance) => {
  if (!isInSSRComponentSetup || lifecycle === "sp") {
    injectHook(lifecycle, (...args) => hook(...args), target);
  }
};
const onBeforeMount = createHook("bm");
const onMounted = createHook("m");
const onBeforeUpdate = createHook(
  "bu"
);
const onUpdated = createHook("u");
const onBeforeUnmount = createHook(
  "bum"
);
const onUnmounted = createHook("um");
const onServerPrefetch = createHook(
  "sp"
);
const onRenderTriggered = createHook("rtg");
const onRenderTracked = createHook("rtc");
function onErrorCaptured(hook, target = currentInstance) {
  injectHook("ec", hook, target);
}
const COMPONENTS = "components";
const DIRECTIVES = "directives";
function resolveComponent(name2, maybeSelfReference) {
  return resolveAsset(COMPONENTS, name2, true, maybeSelfReference) || name2;
}
const NULL_DYNAMIC_COMPONENT = Symbol.for("v-ndc");
function resolveDynamicComponent(component) {
  if (isString(component)) {
    return resolveAsset(COMPONENTS, component, false) || component;
  } else {
    return component || NULL_DYNAMIC_COMPONENT;
  }
}
function resolveDirective(name2) {
  return resolveAsset(DIRECTIVES, name2);
}
function resolveAsset(type, name2, warnMissing = true, maybeSelfReference = false) {
  const instance = currentRenderingInstance || currentInstance;
  if (instance) {
    const Component = instance.type;
    if (type === COMPONENTS) {
      const selfName = getComponentName(
        Component,
        false
      );
      if (selfName && (selfName === name2 || selfName === camelize(name2) || selfName === capitalize(camelize(name2)))) {
        return Component;
      }
    }
    const res = (
      // local registration
      // check instance[type] first which is resolved for options API
      resolve(instance[type] || Component[type], name2) || // global registration
      resolve(instance.appContext[type], name2)
    );
    if (!res && maybeSelfReference) {
      return Component;
    }
    return res;
  }
}
function resolve(registry, name2) {
  return registry && (registry[name2] || registry[camelize(name2)] || registry[capitalize(camelize(name2))]);
}
function renderList(source, renderItem, cache, index) {
  let ret;
  const cached = cache;
  const sourceIsArray = isArray(source);
  if (sourceIsArray || isString(source)) {
    const sourceIsReactiveArray = sourceIsArray && isReactive(source);
    let needsWrap = false;
    let isReadonlySource = false;
    if (sourceIsReactiveArray) {
      needsWrap = !isShallow(source);
      isReadonlySource = isReadonly(source);
      source = shallowReadArray(source);
    }
    ret = new Array(source.length);
    for (let i = 0, l = source.length; i < l; i++) {
      ret[i] = renderItem(
        needsWrap ? isReadonlySource ? toReadonly(toReactive(source[i])) : toReactive(source[i]) : source[i],
        i,
        void 0,
        cached
      );
    }
  } else if (typeof source === "number") {
    ret = new Array(source);
    for (let i = 0; i < source; i++) {
      ret[i] = renderItem(i + 1, i, void 0, cached);
    }
  } else if (isObject$4(source)) {
    if (source[Symbol.iterator]) {
      ret = Array.from(
        source,
        (item, i) => renderItem(item, i, void 0, cached)
      );
    } else {
      const keys = Object.keys(source);
      ret = new Array(keys.length);
      for (let i = 0, l = keys.length; i < l; i++) {
        const key = keys[i];
        ret[i] = renderItem(source[key], key, i, cached);
      }
    }
  } else {
    ret = [];
  }
  return ret;
}
function createSlots(slots, dynamicSlots) {
  for (let i = 0; i < dynamicSlots.length; i++) {
    const slot = dynamicSlots[i];
    if (isArray(slot)) {
      for (let j = 0; j < slot.length; j++) {
        slots[slot[j].name] = slot[j].fn;
      }
    } else if (slot) {
      slots[slot.name] = slot.key ? (...args) => {
        const res = slot.fn(...args);
        if (res) res.key = slot.key;
        return res;
      } : slot.fn;
    }
  }
  return slots;
}
function renderSlot(slots, name2, props = {}, fallback, noSlotted) {
  if (currentRenderingInstance.ce || currentRenderingInstance.parent && isAsyncWrapper(currentRenderingInstance.parent) && currentRenderingInstance.parent.ce) {
    const hasProps = Object.keys(props).length > 0;
    if (name2 !== "default") props.name = name2;
    return openBlock(), createBlock(
      Fragment,
      null,
      [createVNode("slot", props, fallback && fallback())],
      hasProps ? -2 : 64
    );
  }
  let slot = slots[name2];
  if (slot && slot._c) {
    slot._d = false;
  }
  openBlock();
  const validSlotContent = slot && ensureValidVNode(slot(props));
  const slotKey = props.key || // slot content array of a dynamic conditional slot may have a branch
  // key attached in the `createSlots` helper, respect that
  validSlotContent && validSlotContent.key;
  const rendered = createBlock(
    Fragment,
    {
      key: (slotKey && !isSymbol(slotKey) ? slotKey : `_${name2}`) + // #7256 force differentiate fallback content from actual content
      (!validSlotContent && fallback ? "_fb" : "")
    },
    validSlotContent || (fallback ? fallback() : []),
    validSlotContent && slots._ === 1 ? 64 : -2
  );
  if (!noSlotted && rendered.scopeId) {
    rendered.slotScopeIds = [rendered.scopeId + "-s"];
  }
  if (slot && slot._c) {
    slot._d = true;
  }
  return rendered;
}
function ensureValidVNode(vnodes) {
  return vnodes.some((child) => {
    if (!isVNode(child)) return true;
    if (child.type === Comment) return false;
    if (child.type === Fragment && !ensureValidVNode(child.children))
      return false;
    return true;
  }) ? vnodes : null;
}
function toHandlers(obj, preserveCaseIfNecessary) {
  const ret = {};
  for (const key in obj) {
    ret[preserveCaseIfNecessary && /[A-Z]/.test(key) ? `on:${key}` : toHandlerKey(key)] = obj[key];
  }
  return ret;
}
const getPublicInstance = (i) => {
  if (!i) return null;
  if (isStatefulComponent(i)) return getComponentPublicInstance(i);
  return getPublicInstance(i.parent);
};
const publicPropertiesMap = (
  // Move PURE marker to new line to workaround compiler discarding it
  // due to type annotation
  /* @__PURE__ */ extend(/* @__PURE__ */ Object.create(null), {
    $: (i) => i,
    $el: (i) => i.vnode.el,
    $data: (i) => i.data,
    $props: (i) => i.props,
    $attrs: (i) => i.attrs,
    $slots: (i) => i.slots,
    $refs: (i) => i.refs,
    $parent: (i) => getPublicInstance(i.parent),
    $root: (i) => getPublicInstance(i.root),
    $host: (i) => i.ce,
    $emit: (i) => i.emit,
    $options: (i) => resolveMergedOptions(i),
    $forceUpdate: (i) => i.f || (i.f = () => {
      queueJob(i.update);
    }),
    $nextTick: (i) => i.n || (i.n = nextTick.bind(i.proxy)),
    $watch: (i) => instanceWatch.bind(i)
  })
);
const hasSetupBinding = (state, key) => state !== EMPTY_OBJ && !state.__isScriptSetup && hasOwn(state, key);
const PublicInstanceProxyHandlers = {
  get({ _: instance }, key) {
    if (key === "__v_skip") {
      return true;
    }
    const { ctx, setupState, data, props, accessCache, type, appContext } = instance;
    if (key[0] !== "$") {
      const n = accessCache[key];
      if (n !== void 0) {
        switch (n) {
          case 1:
            return setupState[key];
          case 2:
            return data[key];
          case 4:
            return ctx[key];
          case 3:
            return props[key];
        }
      } else if (hasSetupBinding(setupState, key)) {
        accessCache[key] = 1;
        return setupState[key];
      } else if (data !== EMPTY_OBJ && hasOwn(data, key)) {
        accessCache[key] = 2;
        return data[key];
      } else if (hasOwn(props, key)) {
        accessCache[key] = 3;
        return props[key];
      } else if (ctx !== EMPTY_OBJ && hasOwn(ctx, key)) {
        accessCache[key] = 4;
        return ctx[key];
      } else if (shouldCacheAccess) {
        accessCache[key] = 0;
      }
    }
    const publicGetter = publicPropertiesMap[key];
    let cssModule, globalProperties;
    if (publicGetter) {
      if (key === "$attrs") {
        track(instance.attrs, "get", "");
      }
      return publicGetter(instance);
    } else if (
      // css module (injected by vue-loader)
      (cssModule = type.__cssModules) && (cssModule = cssModule[key])
    ) {
      return cssModule;
    } else if (ctx !== EMPTY_OBJ && hasOwn(ctx, key)) {
      accessCache[key] = 4;
      return ctx[key];
    } else if (
      // global properties
      globalProperties = appContext.config.globalProperties, hasOwn(globalProperties, key)
    ) {
      {
        return globalProperties[key];
      }
    } else ;
  },
  set({ _: instance }, key, value) {
    const { data, setupState, ctx } = instance;
    if (hasSetupBinding(setupState, key)) {
      setupState[key] = value;
      return true;
    } else if (data !== EMPTY_OBJ && hasOwn(data, key)) {
      data[key] = value;
      return true;
    } else if (hasOwn(instance.props, key)) {
      return false;
    }
    if (key[0] === "$" && key.slice(1) in instance) {
      return false;
    } else {
      {
        ctx[key] = value;
      }
    }
    return true;
  },
  has({
    _: { data, setupState, accessCache, ctx, appContext, props, type }
  }, key) {
    let cssModules;
    return !!(accessCache[key] || data !== EMPTY_OBJ && key[0] !== "$" && hasOwn(data, key) || hasSetupBinding(setupState, key) || hasOwn(props, key) || hasOwn(ctx, key) || hasOwn(publicPropertiesMap, key) || hasOwn(appContext.config.globalProperties, key) || (cssModules = type.__cssModules) && cssModules[key]);
  },
  defineProperty(target, key, descriptor) {
    if (descriptor.get != null) {
      target._.accessCache[key] = 0;
    } else if (hasOwn(descriptor, "value")) {
      this.set(target, key, descriptor.value, null);
    }
    return Reflect.defineProperty(target, key, descriptor);
  }
};
function useSlots() {
  return getContext().slots;
}
function getContext(calledFunctionName) {
  const i = getCurrentInstance();
  return i.setupContext || (i.setupContext = createSetupContext(i));
}
function normalizePropsOrEmits(props) {
  return isArray(props) ? props.reduce(
    (normalized, p2) => (normalized[p2] = null, normalized),
    {}
  ) : props;
}
function mergeModels(a, b) {
  if (!a || !b) return a || b;
  if (isArray(a) && isArray(b)) return a.concat(b);
  return extend({}, normalizePropsOrEmits(a), normalizePropsOrEmits(b));
}
let shouldCacheAccess = true;
function applyOptions(instance) {
  const options = resolveMergedOptions(instance);
  const publicThis = instance.proxy;
  const ctx = instance.ctx;
  shouldCacheAccess = false;
  if (options.beforeCreate) {
    callHook$1(options.beforeCreate, instance, "bc");
  }
  const {
    // state
    data: dataOptions,
    computed: computedOptions,
    methods,
    watch: watchOptions,
    provide: provideOptions,
    inject: injectOptions,
    // lifecycle
    created,
    beforeMount,
    mounted,
    beforeUpdate,
    updated,
    activated,
    deactivated,
    beforeDestroy,
    beforeUnmount,
    destroyed,
    unmounted,
    render: render2,
    renderTracked,
    renderTriggered,
    errorCaptured,
    serverPrefetch,
    // public API
    expose,
    inheritAttrs,
    // assets
    components,
    directives,
    filters
  } = options;
  const checkDuplicateProperties = null;
  if (injectOptions) {
    resolveInjections(injectOptions, ctx, checkDuplicateProperties);
  }
  if (methods) {
    for (const key in methods) {
      const methodHandler = methods[key];
      if (isFunction(methodHandler)) {
        {
          ctx[key] = methodHandler.bind(publicThis);
        }
      }
    }
  }
  if (dataOptions) {
    const data = dataOptions.call(publicThis, publicThis);
    if (!isObject$4(data)) ;
    else {
      instance.data = reactive(data);
    }
  }
  shouldCacheAccess = true;
  if (computedOptions) {
    for (const key in computedOptions) {
      const opt = computedOptions[key];
      const get = isFunction(opt) ? opt.bind(publicThis, publicThis) : isFunction(opt.get) ? opt.get.bind(publicThis, publicThis) : NOOP;
      const set = !isFunction(opt) && isFunction(opt.set) ? opt.set.bind(publicThis) : NOOP;
      const c = computed({
        get,
        set
      });
      Object.defineProperty(ctx, key, {
        enumerable: true,
        configurable: true,
        get: () => c.value,
        set: (v) => c.value = v
      });
    }
  }
  if (watchOptions) {
    for (const key in watchOptions) {
      createWatcher(watchOptions[key], ctx, publicThis, key);
    }
  }
  if (provideOptions) {
    const provides = isFunction(provideOptions) ? provideOptions.call(publicThis) : provideOptions;
    Reflect.ownKeys(provides).forEach((key) => {
      provide(key, provides[key]);
    });
  }
  if (created) {
    callHook$1(created, instance, "c");
  }
  function registerLifecycleHook(register, hook) {
    if (isArray(hook)) {
      hook.forEach((_hook) => register(_hook.bind(publicThis)));
    } else if (hook) {
      register(hook.bind(publicThis));
    }
  }
  registerLifecycleHook(onBeforeMount, beforeMount);
  registerLifecycleHook(onMounted, mounted);
  registerLifecycleHook(onBeforeUpdate, beforeUpdate);
  registerLifecycleHook(onUpdated, updated);
  registerLifecycleHook(onActivated, activated);
  registerLifecycleHook(onDeactivated, deactivated);
  registerLifecycleHook(onErrorCaptured, errorCaptured);
  registerLifecycleHook(onRenderTracked, renderTracked);
  registerLifecycleHook(onRenderTriggered, renderTriggered);
  registerLifecycleHook(onBeforeUnmount, beforeUnmount);
  registerLifecycleHook(onUnmounted, unmounted);
  registerLifecycleHook(onServerPrefetch, serverPrefetch);
  if (isArray(expose)) {
    if (expose.length) {
      const exposed = instance.exposed || (instance.exposed = {});
      expose.forEach((key) => {
        Object.defineProperty(exposed, key, {
          get: () => publicThis[key],
          set: (val) => publicThis[key] = val,
          enumerable: true
        });
      });
    } else if (!instance.exposed) {
      instance.exposed = {};
    }
  }
  if (render2 && instance.render === NOOP) {
    instance.render = render2;
  }
  if (inheritAttrs != null) {
    instance.inheritAttrs = inheritAttrs;
  }
  if (components) instance.components = components;
  if (directives) instance.directives = directives;
  if (serverPrefetch) {
    markAsyncBoundary(instance);
  }
}
function resolveInjections(injectOptions, ctx, checkDuplicateProperties = NOOP) {
  if (isArray(injectOptions)) {
    injectOptions = normalizeInject(injectOptions);
  }
  for (const key in injectOptions) {
    const opt = injectOptions[key];
    let injected;
    if (isObject$4(opt)) {
      if ("default" in opt) {
        injected = inject(
          opt.from || key,
          opt.default,
          true
        );
      } else {
        injected = inject(opt.from || key);
      }
    } else {
      injected = inject(opt);
    }
    if (isRef(injected)) {
      Object.defineProperty(ctx, key, {
        enumerable: true,
        configurable: true,
        get: () => injected.value,
        set: (v) => injected.value = v
      });
    } else {
      ctx[key] = injected;
    }
  }
}
function callHook$1(hook, instance, type) {
  callWithAsyncErrorHandling(
    isArray(hook) ? hook.map((h2) => h2.bind(instance.proxy)) : hook.bind(instance.proxy),
    instance,
    type
  );
}
function createWatcher(raw, ctx, publicThis, key) {
  let getter = key.includes(".") ? createPathGetter(publicThis, key) : () => publicThis[key];
  if (isString(raw)) {
    const handler = ctx[raw];
    if (isFunction(handler)) {
      {
        watch(getter, handler);
      }
    }
  } else if (isFunction(raw)) {
    {
      watch(getter, raw.bind(publicThis));
    }
  } else if (isObject$4(raw)) {
    if (isArray(raw)) {
      raw.forEach((r) => createWatcher(r, ctx, publicThis, key));
    } else {
      const handler = isFunction(raw.handler) ? raw.handler.bind(publicThis) : ctx[raw.handler];
      if (isFunction(handler)) {
        watch(getter, handler, raw);
      }
    }
  } else ;
}
function resolveMergedOptions(instance) {
  const base = instance.type;
  const { mixins, extends: extendsOptions } = base;
  const {
    mixins: globalMixins,
    optionsCache: cache,
    config: { optionMergeStrategies }
  } = instance.appContext;
  const cached = cache.get(base);
  let resolved;
  if (cached) {
    resolved = cached;
  } else if (!globalMixins.length && !mixins && !extendsOptions) {
    {
      resolved = base;
    }
  } else {
    resolved = {};
    if (globalMixins.length) {
      globalMixins.forEach(
        (m) => mergeOptions(resolved, m, optionMergeStrategies, true)
      );
    }
    mergeOptions(resolved, base, optionMergeStrategies);
  }
  if (isObject$4(base)) {
    cache.set(base, resolved);
  }
  return resolved;
}
function mergeOptions(to2, from, strats, asMixin = false) {
  const { mixins, extends: extendsOptions } = from;
  if (extendsOptions) {
    mergeOptions(to2, extendsOptions, strats, true);
  }
  if (mixins) {
    mixins.forEach(
      (m) => mergeOptions(to2, m, strats, true)
    );
  }
  for (const key in from) {
    if (asMixin && key === "expose") ;
    else {
      const strat = internalOptionMergeStrats[key] || strats && strats[key];
      to2[key] = strat ? strat(to2[key], from[key]) : from[key];
    }
  }
  return to2;
}
const internalOptionMergeStrats = {
  data: mergeDataFn,
  props: mergeEmitsOrPropsOptions,
  emits: mergeEmitsOrPropsOptions,
  // objects
  methods: mergeObjectOptions,
  computed: mergeObjectOptions,
  // lifecycle
  beforeCreate: mergeAsArray,
  created: mergeAsArray,
  beforeMount: mergeAsArray,
  mounted: mergeAsArray,
  beforeUpdate: mergeAsArray,
  updated: mergeAsArray,
  beforeDestroy: mergeAsArray,
  beforeUnmount: mergeAsArray,
  destroyed: mergeAsArray,
  unmounted: mergeAsArray,
  activated: mergeAsArray,
  deactivated: mergeAsArray,
  errorCaptured: mergeAsArray,
  serverPrefetch: mergeAsArray,
  // assets
  components: mergeObjectOptions,
  directives: mergeObjectOptions,
  // watch
  watch: mergeWatchOptions,
  // provide / inject
  provide: mergeDataFn,
  inject: mergeInject
};
function mergeDataFn(to2, from) {
  if (!from) {
    return to2;
  }
  if (!to2) {
    return from;
  }
  return function mergedDataFn() {
    return extend(
      isFunction(to2) ? to2.call(this, this) : to2,
      isFunction(from) ? from.call(this, this) : from
    );
  };
}
function mergeInject(to2, from) {
  return mergeObjectOptions(normalizeInject(to2), normalizeInject(from));
}
function normalizeInject(raw) {
  if (isArray(raw)) {
    const res = {};
    for (let i = 0; i < raw.length; i++) {
      res[raw[i]] = raw[i];
    }
    return res;
  }
  return raw;
}
function mergeAsArray(to2, from) {
  return to2 ? [...new Set([].concat(to2, from))] : from;
}
function mergeObjectOptions(to2, from) {
  return to2 ? extend(/* @__PURE__ */ Object.create(null), to2, from) : from;
}
function mergeEmitsOrPropsOptions(to2, from) {
  if (to2) {
    if (isArray(to2) && isArray(from)) {
      return [.../* @__PURE__ */ new Set([...to2, ...from])];
    }
    return extend(
      /* @__PURE__ */ Object.create(null),
      normalizePropsOrEmits(to2),
      normalizePropsOrEmits(from != null ? from : {})
    );
  } else {
    return from;
  }
}
function mergeWatchOptions(to2, from) {
  if (!to2) return from;
  if (!from) return to2;
  const merged = extend(/* @__PURE__ */ Object.create(null), to2);
  for (const key in from) {
    merged[key] = mergeAsArray(to2[key], from[key]);
  }
  return merged;
}
function createAppContext() {
  return {
    app: null,
    config: {
      isNativeTag: NO,
      performance: false,
      globalProperties: {},
      optionMergeStrategies: {},
      errorHandler: void 0,
      warnHandler: void 0,
      compilerOptions: {}
    },
    mixins: [],
    components: {},
    directives: {},
    provides: /* @__PURE__ */ Object.create(null),
    optionsCache: /* @__PURE__ */ new WeakMap(),
    propsCache: /* @__PURE__ */ new WeakMap(),
    emitsCache: /* @__PURE__ */ new WeakMap()
  };
}
let uid$1 = 0;
function createAppAPI(render2, hydrate) {
  return function createApp2(rootComponent, rootProps = null) {
    if (!isFunction(rootComponent)) {
      rootComponent = extend({}, rootComponent);
    }
    if (rootProps != null && !isObject$4(rootProps)) {
      rootProps = null;
    }
    const context = createAppContext();
    const installedPlugins = /* @__PURE__ */ new WeakSet();
    const pluginCleanupFns = [];
    let isMounted = false;
    const app = context.app = {
      _uid: uid$1++,
      _component: rootComponent,
      _props: rootProps,
      _container: null,
      _context: context,
      _instance: null,
      version,
      get config() {
        return context.config;
      },
      set config(v) {
      },
      use(plugin, ...options) {
        if (installedPlugins.has(plugin)) ;
        else if (plugin && isFunction(plugin.install)) {
          installedPlugins.add(plugin);
          plugin.install(app, ...options);
        } else if (isFunction(plugin)) {
          installedPlugins.add(plugin);
          plugin(app, ...options);
        } else ;
        return app;
      },
      mixin(mixin) {
        {
          if (!context.mixins.includes(mixin)) {
            context.mixins.push(mixin);
          }
        }
        return app;
      },
      component(name2, component) {
        if (!component) {
          return context.components[name2];
        }
        context.components[name2] = component;
        return app;
      },
      directive(name2, directive) {
        if (!directive) {
          return context.directives[name2];
        }
        context.directives[name2] = directive;
        return app;
      },
      mount(rootContainer, isHydrate, namespace) {
        if (!isMounted) {
          const vnode = app._ceVNode || createVNode(rootComponent, rootProps);
          vnode.appContext = context;
          if (namespace === true) {
            namespace = "svg";
          } else if (namespace === false) {
            namespace = void 0;
          }
          {
            render2(vnode, rootContainer, namespace);
          }
          isMounted = true;
          app._container = rootContainer;
          rootContainer.__vue_app__ = app;
          return getComponentPublicInstance(vnode.component);
        }
      },
      onUnmount(cleanupFn) {
        pluginCleanupFns.push(cleanupFn);
      },
      unmount() {
        if (isMounted) {
          callWithAsyncErrorHandling(
            pluginCleanupFns,
            app._instance,
            16
          );
          render2(null, app._container);
          delete app._container.__vue_app__;
        }
      },
      provide(key, value) {
        context.provides[key] = value;
        return app;
      },
      runWithContext(fn) {
        const lastApp = currentApp;
        currentApp = app;
        try {
          return fn();
        } finally {
          currentApp = lastApp;
        }
      }
    };
    return app;
  };
}
let currentApp = null;
function provide(key, value) {
  if (currentInstance) {
    let provides = currentInstance.provides;
    const parentProvides = currentInstance.parent && currentInstance.parent.provides;
    if (parentProvides === provides) {
      provides = currentInstance.provides = Object.create(parentProvides);
    }
    provides[key] = value;
  }
}
function inject(key, defaultValue, treatDefaultAsFactory = false) {
  const instance = getCurrentInstance();
  if (instance || currentApp) {
    let provides = currentApp ? currentApp._context.provides : instance ? instance.parent == null || instance.ce ? instance.vnode.appContext && instance.vnode.appContext.provides : instance.parent.provides : void 0;
    if (provides && key in provides) {
      return provides[key];
    } else if (arguments.length > 1) {
      return treatDefaultAsFactory && isFunction(defaultValue) ? defaultValue.call(instance && instance.proxy) : defaultValue;
    } else ;
  }
}
function hasInjectionContext() {
  return !!(getCurrentInstance() || currentApp);
}
const ssrContextKey = Symbol.for("v-scx");
const useSSRContext = () => {
  {
    const ctx = inject(ssrContextKey);
    return ctx;
  }
};
function watchEffect(effect2, options) {
  return doWatch(effect2, null, options);
}
function watchSyncEffect(effect2, options) {
  return doWatch(
    effect2,
    null,
    { flush: "sync" }
  );
}
function watch(source, cb, options) {
  return doWatch(source, cb, options);
}
function doWatch(source, cb, options = EMPTY_OBJ) {
  const { immediate, deep, flush, once } = options;
  const baseWatchOptions = extend({}, options);
  const runsImmediately = cb && immediate || !cb && flush !== "post";
  let ssrCleanup;
  if (isInSSRComponentSetup) {
    if (flush === "sync") {
      const ctx = useSSRContext();
      ssrCleanup = ctx.__watcherHandles || (ctx.__watcherHandles = []);
    } else if (!runsImmediately) {
      const watchStopHandle = () => {
      };
      watchStopHandle.stop = NOOP;
      watchStopHandle.resume = NOOP;
      watchStopHandle.pause = NOOP;
      return watchStopHandle;
    }
  }
  const instance = currentInstance;
  baseWatchOptions.call = (fn, type, args) => callWithAsyncErrorHandling(fn, instance, type, args);
  let isPre = false;
  if (flush === "post") {
    baseWatchOptions.scheduler = (job) => {
      queuePostRenderEffect(job, instance && instance.suspense);
    };
  } else if (flush !== "sync") {
    isPre = true;
    baseWatchOptions.scheduler = (job, isFirstRun) => {
      if (isFirstRun) {
        job();
      } else {
        queueJob(job);
      }
    };
  }
  baseWatchOptions.augmentJob = (job) => {
    if (cb) {
      job.flags |= 4;
    }
    if (isPre) {
      job.flags |= 2;
      if (instance) {
        job.id = instance.uid;
        job.i = instance;
      }
    }
  };
  const watchHandle = watch$1(source, cb, baseWatchOptions);
  if (isInSSRComponentSetup) {
    if (ssrCleanup) {
      ssrCleanup.push(watchHandle);
    } else if (runsImmediately) {
      watchHandle();
    }
  }
  return watchHandle;
}
function instanceWatch(source, value, options) {
  const publicThis = this.proxy;
  const getter = isString(source) ? source.includes(".") ? createPathGetter(publicThis, source) : () => publicThis[source] : source.bind(publicThis, publicThis);
  let cb;
  if (isFunction(value)) {
    cb = value;
  } else {
    cb = value.handler;
    options = value;
  }
  const reset2 = setCurrentInstance(this);
  const res = doWatch(getter, cb.bind(publicThis), options);
  reset2();
  return res;
}
function createPathGetter(ctx, path) {
  const segments = path.split(".");
  return () => {
    let cur = ctx;
    for (let i = 0; i < segments.length && cur; i++) {
      cur = cur[segments[i]];
    }
    return cur;
  };
}
function useModel(props, name2, options = EMPTY_OBJ) {
  const i = getCurrentInstance();
  const camelizedName = camelize(name2);
  const hyphenatedName = hyphenate(name2);
  const modifiers = getModelModifiers(props, camelizedName);
  const res = customRef((track2, trigger2) => {
    let localValue;
    let prevSetValue = EMPTY_OBJ;
    let prevEmittedValue;
    watchSyncEffect(() => {
      const propValue = props[camelizedName];
      if (hasChanged(localValue, propValue)) {
        localValue = propValue;
        trigger2();
      }
    });
    return {
      get() {
        track2();
        return options.get ? options.get(localValue) : localValue;
      },
      set(value) {
        const emittedValue = options.set ? options.set(value) : value;
        if (!hasChanged(emittedValue, localValue) && !(prevSetValue !== EMPTY_OBJ && hasChanged(value, prevSetValue))) {
          return;
        }
        const rawProps = i.vnode.props;
        if (!(rawProps && // check if parent has passed v-model
        (name2 in rawProps || camelizedName in rawProps || hyphenatedName in rawProps) && (`onUpdate:${name2}` in rawProps || `onUpdate:${camelizedName}` in rawProps || `onUpdate:${hyphenatedName}` in rawProps))) {
          localValue = value;
          trigger2();
        }
        i.emit(`update:${name2}`, emittedValue);
        if (hasChanged(value, emittedValue) && hasChanged(value, prevSetValue) && !hasChanged(emittedValue, prevEmittedValue)) {
          trigger2();
        }
        prevSetValue = value;
        prevEmittedValue = emittedValue;
      }
    };
  });
  res[Symbol.iterator] = () => {
    let i2 = 0;
    return {
      next() {
        if (i2 < 2) {
          return { value: i2++ ? modifiers || EMPTY_OBJ : res, done: false };
        } else {
          return { done: true };
        }
      }
    };
  };
  return res;
}
const getModelModifiers = (props, modelName) => {
  return modelName === "modelValue" || modelName === "model-value" ? props.modelModifiers : props[`${modelName}Modifiers`] || props[`${camelize(modelName)}Modifiers`] || props[`${hyphenate(modelName)}Modifiers`];
};
function emit(instance, event, ...rawArgs) {
  if (instance.isUnmounted) return;
  const props = instance.vnode.props || EMPTY_OBJ;
  let args = rawArgs;
  const isModelListener2 = event.startsWith("update:");
  const modifiers = isModelListener2 && getModelModifiers(props, event.slice(7));
  if (modifiers) {
    if (modifiers.trim) {
      args = rawArgs.map((a) => isString(a) ? a.trim() : a);
    }
    if (modifiers.number) {
      args = rawArgs.map(looseToNumber);
    }
  }
  let handlerName;
  let handler = props[handlerName = toHandlerKey(event)] || // also try camelCase event handler (#2249)
  props[handlerName = toHandlerKey(camelize(event))];
  if (!handler && isModelListener2) {
    handler = props[handlerName = toHandlerKey(hyphenate(event))];
  }
  if (handler) {
    callWithAsyncErrorHandling(
      handler,
      instance,
      6,
      args
    );
  }
  const onceHandler = props[handlerName + `Once`];
  if (onceHandler) {
    if (!instance.emitted) {
      instance.emitted = {};
    } else if (instance.emitted[handlerName]) {
      return;
    }
    instance.emitted[handlerName] = true;
    callWithAsyncErrorHandling(
      onceHandler,
      instance,
      6,
      args
    );
  }
}
const mixinEmitsCache = /* @__PURE__ */ new WeakMap();
function normalizeEmitsOptions(comp, appContext, asMixin = false) {
  const cache = asMixin ? mixinEmitsCache : appContext.emitsCache;
  const cached = cache.get(comp);
  if (cached !== void 0) {
    return cached;
  }
  const raw = comp.emits;
  let normalized = {};
  let hasExtends = false;
  if (!isFunction(comp)) {
    const extendEmits = (raw2) => {
      const normalizedFromExtend = normalizeEmitsOptions(raw2, appContext, true);
      if (normalizedFromExtend) {
        hasExtends = true;
        extend(normalized, normalizedFromExtend);
      }
    };
    if (!asMixin && appContext.mixins.length) {
      appContext.mixins.forEach(extendEmits);
    }
    if (comp.extends) {
      extendEmits(comp.extends);
    }
    if (comp.mixins) {
      comp.mixins.forEach(extendEmits);
    }
  }
  if (!raw && !hasExtends) {
    if (isObject$4(comp)) {
      cache.set(comp, null);
    }
    return null;
  }
  if (isArray(raw)) {
    raw.forEach((key) => normalized[key] = null);
  } else {
    extend(normalized, raw);
  }
  if (isObject$4(comp)) {
    cache.set(comp, normalized);
  }
  return normalized;
}
function isEmitListener(options, key) {
  if (!options || !isOn(key)) {
    return false;
  }
  key = key.slice(2).replace(/Once$/, "");
  return hasOwn(options, key[0].toLowerCase() + key.slice(1)) || hasOwn(options, hyphenate(key)) || hasOwn(options, key);
}
function markAttrsAccessed() {
}
function renderComponentRoot(instance) {
  const {
    type: Component,
    vnode,
    proxy,
    withProxy,
    propsOptions: [propsOptions],
    slots,
    attrs,
    emit: emit2,
    render: render2,
    renderCache,
    props,
    data,
    setupState,
    ctx,
    inheritAttrs
  } = instance;
  const prev = setCurrentRenderingInstance(instance);
  let result;
  let fallthroughAttrs;
  try {
    if (vnode.shapeFlag & 4) {
      const proxyToUse = withProxy || proxy;
      const thisProxy = false ? new Proxy(proxyToUse, {
        get(target, key, receiver) {
          warn$1(
            `Property '${String(
              key
            )}' was accessed via 'this'. Avoid using 'this' in templates.`
          );
          return Reflect.get(target, key, receiver);
        }
      }) : proxyToUse;
      result = normalizeVNode(
        render2.call(
          thisProxy,
          proxyToUse,
          renderCache,
          false ? shallowReadonly(props) : props,
          setupState,
          data,
          ctx
        )
      );
      fallthroughAttrs = attrs;
    } else {
      const render22 = Component;
      if (false) ;
      result = normalizeVNode(
        render22.length > 1 ? render22(
          false ? shallowReadonly(props) : props,
          false ? {
            get attrs() {
              markAttrsAccessed();
              return shallowReadonly(attrs);
            },
            slots,
            emit: emit2
          } : { attrs, slots, emit: emit2 }
        ) : render22(
          false ? shallowReadonly(props) : props,
          null
        )
      );
      fallthroughAttrs = Component.props ? attrs : getFunctionalFallthrough(attrs);
    }
  } catch (err) {
    blockStack.length = 0;
    handleError(err, instance, 1);
    result = createVNode(Comment);
  }
  let root = result;
  if (fallthroughAttrs && inheritAttrs !== false) {
    const keys = Object.keys(fallthroughAttrs);
    const { shapeFlag } = root;
    if (keys.length) {
      if (shapeFlag & (1 | 6)) {
        if (propsOptions && keys.some(isModelListener)) {
          fallthroughAttrs = filterModelListeners(
            fallthroughAttrs,
            propsOptions
          );
        }
        root = cloneVNode(root, fallthroughAttrs, false, true);
      }
    }
  }
  if (vnode.dirs) {
    root = cloneVNode(root, null, false, true);
    root.dirs = root.dirs ? root.dirs.concat(vnode.dirs) : vnode.dirs;
  }
  if (vnode.transition) {
    setTransitionHooks(root, vnode.transition);
  }
  {
    result = root;
  }
  setCurrentRenderingInstance(prev);
  return result;
}
const getFunctionalFallthrough = (attrs) => {
  let res;
  for (const key in attrs) {
    if (key === "class" || key === "style" || isOn(key)) {
      (res || (res = {}))[key] = attrs[key];
    }
  }
  return res;
};
const filterModelListeners = (attrs, props) => {
  const res = {};
  for (const key in attrs) {
    if (!isModelListener(key) || !(key.slice(9) in props)) {
      res[key] = attrs[key];
    }
  }
  return res;
};
function shouldUpdateComponent(prevVNode, nextVNode, optimized) {
  const { props: prevProps, children: prevChildren, component } = prevVNode;
  const { props: nextProps, children: nextChildren, patchFlag } = nextVNode;
  const emits = component.emitsOptions;
  if (nextVNode.dirs || nextVNode.transition) {
    return true;
  }
  if (optimized && patchFlag >= 0) {
    if (patchFlag & 1024) {
      return true;
    }
    if (patchFlag & 16) {
      if (!prevProps) {
        return !!nextProps;
      }
      return hasPropsChanged(prevProps, nextProps, emits);
    } else if (patchFlag & 8) {
      const dynamicProps = nextVNode.dynamicProps;
      for (let i = 0; i < dynamicProps.length; i++) {
        const key = dynamicProps[i];
        if (nextProps[key] !== prevProps[key] && !isEmitListener(emits, key)) {
          return true;
        }
      }
    }
  } else {
    if (prevChildren || nextChildren) {
      if (!nextChildren || !nextChildren.$stable) {
        return true;
      }
    }
    if (prevProps === nextProps) {
      return false;
    }
    if (!prevProps) {
      return !!nextProps;
    }
    if (!nextProps) {
      return true;
    }
    return hasPropsChanged(prevProps, nextProps, emits);
  }
  return false;
}
function hasPropsChanged(prevProps, nextProps, emitsOptions) {
  const nextKeys = Object.keys(nextProps);
  if (nextKeys.length !== Object.keys(prevProps).length) {
    return true;
  }
  for (let i = 0; i < nextKeys.length; i++) {
    const key = nextKeys[i];
    if (nextProps[key] !== prevProps[key] && !isEmitListener(emitsOptions, key)) {
      return true;
    }
  }
  return false;
}
function updateHOCHostEl({ vnode, parent: parent2 }, el2) {
  while (parent2) {
    const root = parent2.subTree;
    if (root.suspense && root.suspense.activeBranch === vnode) {
      root.el = vnode.el;
    }
    if (root === vnode) {
      (vnode = parent2.vnode).el = el2;
      parent2 = parent2.parent;
    } else {
      break;
    }
  }
}
const internalObjectProto = {};
const createInternalObject = () => Object.create(internalObjectProto);
const isInternalObject = (obj) => Object.getPrototypeOf(obj) === internalObjectProto;
function initProps(instance, rawProps, isStateful, isSSR = false) {
  const props = {};
  const attrs = createInternalObject();
  instance.propsDefaults = /* @__PURE__ */ Object.create(null);
  setFullProps(instance, rawProps, props, attrs);
  for (const key in instance.propsOptions[0]) {
    if (!(key in props)) {
      props[key] = void 0;
    }
  }
  if (isStateful) {
    instance.props = isSSR ? props : shallowReactive(props);
  } else {
    if (!instance.type.props) {
      instance.props = attrs;
    } else {
      instance.props = props;
    }
  }
  instance.attrs = attrs;
}
function updateProps(instance, rawProps, rawPrevProps, optimized) {
  const {
    props,
    attrs,
    vnode: { patchFlag }
  } = instance;
  const rawCurrentProps = toRaw(props);
  const [options] = instance.propsOptions;
  let hasAttrsChanged = false;
  if (
    // always force full diff in dev
    // - #1942 if hmr is enabled with sfc component
    // - vite#872 non-sfc component used by sfc component
    (optimized || patchFlag > 0) && !(patchFlag & 16)
  ) {
    if (patchFlag & 8) {
      const propsToUpdate = instance.vnode.dynamicProps;
      for (let i = 0; i < propsToUpdate.length; i++) {
        let key = propsToUpdate[i];
        if (isEmitListener(instance.emitsOptions, key)) {
          continue;
        }
        const value = rawProps[key];
        if (options) {
          if (hasOwn(attrs, key)) {
            if (value !== attrs[key]) {
              attrs[key] = value;
              hasAttrsChanged = true;
            }
          } else {
            const camelizedKey = camelize(key);
            props[camelizedKey] = resolvePropValue(
              options,
              rawCurrentProps,
              camelizedKey,
              value,
              instance,
              false
            );
          }
        } else {
          if (value !== attrs[key]) {
            attrs[key] = value;
            hasAttrsChanged = true;
          }
        }
      }
    }
  } else {
    if (setFullProps(instance, rawProps, props, attrs)) {
      hasAttrsChanged = true;
    }
    let kebabKey;
    for (const key in rawCurrentProps) {
      if (!rawProps || // for camelCase
      !hasOwn(rawProps, key) && // it's possible the original props was passed in as kebab-case
      // and converted to camelCase (#955)
      ((kebabKey = hyphenate(key)) === key || !hasOwn(rawProps, kebabKey))) {
        if (options) {
          if (rawPrevProps && // for camelCase
          (rawPrevProps[key] !== void 0 || // for kebab-case
          rawPrevProps[kebabKey] !== void 0)) {
            props[key] = resolvePropValue(
              options,
              rawCurrentProps,
              key,
              void 0,
              instance,
              true
            );
          }
        } else {
          delete props[key];
        }
      }
    }
    if (attrs !== rawCurrentProps) {
      for (const key in attrs) {
        if (!rawProps || !hasOwn(rawProps, key) && true) {
          delete attrs[key];
          hasAttrsChanged = true;
        }
      }
    }
  }
  if (hasAttrsChanged) {
    trigger(instance.attrs, "set", "");
  }
}
function setFullProps(instance, rawProps, props, attrs) {
  const [options, needCastKeys] = instance.propsOptions;
  let hasAttrsChanged = false;
  let rawCastValues;
  if (rawProps) {
    for (let key in rawProps) {
      if (isReservedProp(key)) {
        continue;
      }
      const value = rawProps[key];
      let camelKey;
      if (options && hasOwn(options, camelKey = camelize(key))) {
        if (!needCastKeys || !needCastKeys.includes(camelKey)) {
          props[camelKey] = value;
        } else {
          (rawCastValues || (rawCastValues = {}))[camelKey] = value;
        }
      } else if (!isEmitListener(instance.emitsOptions, key)) {
        if (!(key in attrs) || value !== attrs[key]) {
          attrs[key] = value;
          hasAttrsChanged = true;
        }
      }
    }
  }
  if (needCastKeys) {
    const rawCurrentProps = toRaw(props);
    const castValues = rawCastValues || EMPTY_OBJ;
    for (let i = 0; i < needCastKeys.length; i++) {
      const key = needCastKeys[i];
      props[key] = resolvePropValue(
        options,
        rawCurrentProps,
        key,
        castValues[key],
        instance,
        !hasOwn(castValues, key)
      );
    }
  }
  return hasAttrsChanged;
}
function resolvePropValue(options, props, key, value, instance, isAbsent) {
  const opt = options[key];
  if (opt != null) {
    const hasDefault = hasOwn(opt, "default");
    if (hasDefault && value === void 0) {
      const defaultValue = opt.default;
      if (opt.type !== Function && !opt.skipFactory && isFunction(defaultValue)) {
        const { propsDefaults } = instance;
        if (key in propsDefaults) {
          value = propsDefaults[key];
        } else {
          const reset2 = setCurrentInstance(instance);
          value = propsDefaults[key] = defaultValue.call(
            null,
            props
          );
          reset2();
        }
      } else {
        value = defaultValue;
      }
      if (instance.ce) {
        instance.ce._setProp(key, value);
      }
    }
    if (opt[
      0
      /* shouldCast */
    ]) {
      if (isAbsent && !hasDefault) {
        value = false;
      } else if (opt[
        1
        /* shouldCastTrue */
      ] && (value === "" || value === hyphenate(key))) {
        value = true;
      }
    }
  }
  return value;
}
const mixinPropsCache = /* @__PURE__ */ new WeakMap();
function normalizePropsOptions(comp, appContext, asMixin = false) {
  const cache = asMixin ? mixinPropsCache : appContext.propsCache;
  const cached = cache.get(comp);
  if (cached) {
    return cached;
  }
  const raw = comp.props;
  const normalized = {};
  const needCastKeys = [];
  let hasExtends = false;
  if (!isFunction(comp)) {
    const extendProps = (raw2) => {
      hasExtends = true;
      const [props, keys] = normalizePropsOptions(raw2, appContext, true);
      extend(normalized, props);
      if (keys) needCastKeys.push(...keys);
    };
    if (!asMixin && appContext.mixins.length) {
      appContext.mixins.forEach(extendProps);
    }
    if (comp.extends) {
      extendProps(comp.extends);
    }
    if (comp.mixins) {
      comp.mixins.forEach(extendProps);
    }
  }
  if (!raw && !hasExtends) {
    if (isObject$4(comp)) {
      cache.set(comp, EMPTY_ARR);
    }
    return EMPTY_ARR;
  }
  if (isArray(raw)) {
    for (let i = 0; i < raw.length; i++) {
      const normalizedKey = camelize(raw[i]);
      if (validatePropName(normalizedKey)) {
        normalized[normalizedKey] = EMPTY_OBJ;
      }
    }
  } else if (raw) {
    for (const key in raw) {
      const normalizedKey = camelize(key);
      if (validatePropName(normalizedKey)) {
        const opt = raw[key];
        const prop = normalized[normalizedKey] = isArray(opt) || isFunction(opt) ? { type: opt } : extend({}, opt);
        const propType = prop.type;
        let shouldCast = false;
        let shouldCastTrue = true;
        if (isArray(propType)) {
          for (let index = 0; index < propType.length; ++index) {
            const type = propType[index];
            const typeName = isFunction(type) && type.name;
            if (typeName === "Boolean") {
              shouldCast = true;
              break;
            } else if (typeName === "String") {
              shouldCastTrue = false;
            }
          }
        } else {
          shouldCast = isFunction(propType) && propType.name === "Boolean";
        }
        prop[
          0
          /* shouldCast */
        ] = shouldCast;
        prop[
          1
          /* shouldCastTrue */
        ] = shouldCastTrue;
        if (shouldCast || hasOwn(prop, "default")) {
          needCastKeys.push(normalizedKey);
        }
      }
    }
  }
  const res = [normalized, needCastKeys];
  if (isObject$4(comp)) {
    cache.set(comp, res);
  }
  return res;
}
function validatePropName(key) {
  if (key[0] !== "$" && !isReservedProp(key)) {
    return true;
  }
  return false;
}
const isInternalKey = (key) => key === "_" || key === "_ctx" || key === "$stable";
const normalizeSlotValue = (value) => isArray(value) ? value.map(normalizeVNode) : [normalizeVNode(value)];
const normalizeSlot = (key, rawSlot, ctx) => {
  if (rawSlot._n) {
    return rawSlot;
  }
  const normalized = withCtx((...args) => {
    if (false) ;
    return normalizeSlotValue(rawSlot(...args));
  }, ctx);
  normalized._c = false;
  return normalized;
};
const normalizeObjectSlots = (rawSlots, slots, instance) => {
  const ctx = rawSlots._ctx;
  for (const key in rawSlots) {
    if (isInternalKey(key)) continue;
    const value = rawSlots[key];
    if (isFunction(value)) {
      slots[key] = normalizeSlot(key, value, ctx);
    } else if (value != null) {
      const normalized = normalizeSlotValue(value);
      slots[key] = () => normalized;
    }
  }
};
const normalizeVNodeSlots = (instance, children) => {
  const normalized = normalizeSlotValue(children);
  instance.slots.default = () => normalized;
};
const assignSlots = (slots, children, optimized) => {
  for (const key in children) {
    if (optimized || !isInternalKey(key)) {
      slots[key] = children[key];
    }
  }
};
const initSlots = (instance, children, optimized) => {
  const slots = instance.slots = createInternalObject();
  if (instance.vnode.shapeFlag & 32) {
    const type = children._;
    if (type) {
      assignSlots(slots, children, optimized);
      if (optimized) {
        def(slots, "_", type, true);
      }
    } else {
      normalizeObjectSlots(children, slots);
    }
  } else if (children) {
    normalizeVNodeSlots(instance, children);
  }
};
const updateSlots = (instance, children, optimized) => {
  const { vnode, slots } = instance;
  let needDeletionCheck = true;
  let deletionComparisonTarget = EMPTY_OBJ;
  if (vnode.shapeFlag & 32) {
    const type = children._;
    if (type) {
      if (optimized && type === 1) {
        needDeletionCheck = false;
      } else {
        assignSlots(slots, children, optimized);
      }
    } else {
      needDeletionCheck = !children.$stable;
      normalizeObjectSlots(children, slots);
    }
    deletionComparisonTarget = children;
  } else if (children) {
    normalizeVNodeSlots(instance, children);
    deletionComparisonTarget = { default: 1 };
  }
  if (needDeletionCheck) {
    for (const key in slots) {
      if (!isInternalKey(key) && deletionComparisonTarget[key] == null) {
        delete slots[key];
      }
    }
  }
};
const queuePostRenderEffect = queueEffectWithSuspense;
function createRenderer(options) {
  return baseCreateRenderer(options);
}
function baseCreateRenderer(options, createHydrationFns) {
  const target = getGlobalThis();
  target.__VUE__ = true;
  const {
    insert: hostInsert,
    remove: hostRemove,
    patchProp: hostPatchProp,
    createElement: hostCreateElement,
    createText: hostCreateText,
    createComment: hostCreateComment,
    setText: hostSetText,
    setElementText: hostSetElementText,
    parentNode: hostParentNode,
    nextSibling: hostNextSibling,
    setScopeId: hostSetScopeId = NOOP,
    insertStaticContent: hostInsertStaticContent
  } = options;
  const patch = (n1, n2, container, anchor = null, parentComponent = null, parentSuspense = null, namespace = void 0, slotScopeIds = null, optimized = !!n2.dynamicChildren) => {
    if (n1 === n2) {
      return;
    }
    if (n1 && !isSameVNodeType(n1, n2)) {
      anchor = getNextHostNode(n1);
      unmount(n1, parentComponent, parentSuspense, true);
      n1 = null;
    }
    if (n2.patchFlag === -2) {
      optimized = false;
      n2.dynamicChildren = null;
    }
    const { type, ref: ref3, shapeFlag } = n2;
    switch (type) {
      case Text:
        processText(n1, n2, container, anchor);
        break;
      case Comment:
        processCommentNode(n1, n2, container, anchor);
        break;
      case Static:
        if (n1 == null) {
          mountStaticNode(n2, container, anchor, namespace);
        }
        break;
      case Fragment:
        processFragment(
          n1,
          n2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
        break;
      default:
        if (shapeFlag & 1) {
          processElement(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else if (shapeFlag & 6) {
          processComponent(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else if (shapeFlag & 64) {
          type.process(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized,
            internals
          );
        } else if (shapeFlag & 128) {
          type.process(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized,
            internals
          );
        } else ;
    }
    if (ref3 != null && parentComponent) {
      setRef(ref3, n1 && n1.ref, parentSuspense, n2 || n1, !n2);
    } else if (ref3 == null && n1 && n1.ref != null) {
      setRef(n1.ref, null, parentSuspense, n1, true);
    }
  };
  const processText = (n1, n2, container, anchor) => {
    if (n1 == null) {
      hostInsert(
        n2.el = hostCreateText(n2.children),
        container,
        anchor
      );
    } else {
      const el2 = n2.el = n1.el;
      if (n2.children !== n1.children) {
        hostSetText(el2, n2.children);
      }
    }
  };
  const processCommentNode = (n1, n2, container, anchor) => {
    if (n1 == null) {
      hostInsert(
        n2.el = hostCreateComment(n2.children || ""),
        container,
        anchor
      );
    } else {
      n2.el = n1.el;
    }
  };
  const mountStaticNode = (n2, container, anchor, namespace) => {
    [n2.el, n2.anchor] = hostInsertStaticContent(
      n2.children,
      container,
      anchor,
      namespace,
      n2.el,
      n2.anchor
    );
  };
  const moveStaticNode = ({ el: el2, anchor }, container, nextSibling) => {
    let next;
    while (el2 && el2 !== anchor) {
      next = hostNextSibling(el2);
      hostInsert(el2, container, nextSibling);
      el2 = next;
    }
    hostInsert(anchor, container, nextSibling);
  };
  const removeStaticNode = ({ el: el2, anchor }) => {
    let next;
    while (el2 && el2 !== anchor) {
      next = hostNextSibling(el2);
      hostRemove(el2);
      el2 = next;
    }
    hostRemove(anchor);
  };
  const processElement = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    if (n2.type === "svg") {
      namespace = "svg";
    } else if (n2.type === "math") {
      namespace = "mathml";
    }
    if (n1 == null) {
      mountElement(
        n2,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    } else {
      const customElement = !!(n1.el && n1.el._isVueCE) ? n1.el : null;
      try {
        if (customElement) {
          customElement._beginPatch();
        }
        patchElement(
          n1,
          n2,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      } finally {
        if (customElement) {
          customElement._endPatch();
        }
      }
    }
  };
  const mountElement = (vnode, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    let el2;
    let vnodeHook;
    const { props, shapeFlag, transition, dirs } = vnode;
    el2 = vnode.el = hostCreateElement(
      vnode.type,
      namespace,
      props && props.is,
      props
    );
    if (shapeFlag & 8) {
      hostSetElementText(el2, vnode.children);
    } else if (shapeFlag & 16) {
      mountChildren(
        vnode.children,
        el2,
        null,
        parentComponent,
        parentSuspense,
        resolveChildrenNamespace(vnode, namespace),
        slotScopeIds,
        optimized
      );
    }
    if (dirs) {
      invokeDirectiveHook(vnode, null, parentComponent, "created");
    }
    setScopeId(el2, vnode, vnode.scopeId, slotScopeIds, parentComponent);
    if (props) {
      for (const key in props) {
        if (key !== "value" && !isReservedProp(key)) {
          hostPatchProp(el2, key, null, props[key], namespace, parentComponent);
        }
      }
      if ("value" in props) {
        hostPatchProp(el2, "value", null, props.value, namespace);
      }
      if (vnodeHook = props.onVnodeBeforeMount) {
        invokeVNodeHook(vnodeHook, parentComponent, vnode);
      }
    }
    if (dirs) {
      invokeDirectiveHook(vnode, null, parentComponent, "beforeMount");
    }
    const needCallTransitionHooks = needTransition(parentSuspense, transition);
    if (needCallTransitionHooks) {
      transition.beforeEnter(el2);
    }
    hostInsert(el2, container, anchor);
    if ((vnodeHook = props && props.onVnodeMounted) || needCallTransitionHooks || dirs) {
      queuePostRenderEffect(() => {
        vnodeHook && invokeVNodeHook(vnodeHook, parentComponent, vnode);
        needCallTransitionHooks && transition.enter(el2);
        dirs && invokeDirectiveHook(vnode, null, parentComponent, "mounted");
      }, parentSuspense);
    }
  };
  const setScopeId = (el2, vnode, scopeId, slotScopeIds, parentComponent) => {
    if (scopeId) {
      hostSetScopeId(el2, scopeId);
    }
    if (slotScopeIds) {
      for (let i = 0; i < slotScopeIds.length; i++) {
        hostSetScopeId(el2, slotScopeIds[i]);
      }
    }
    if (parentComponent) {
      let subTree = parentComponent.subTree;
      if (vnode === subTree || isSuspense(subTree.type) && (subTree.ssContent === vnode || subTree.ssFallback === vnode)) {
        const parentVNode = parentComponent.vnode;
        setScopeId(
          el2,
          parentVNode,
          parentVNode.scopeId,
          parentVNode.slotScopeIds,
          parentComponent.parent
        );
      }
    }
  };
  const mountChildren = (children, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, start = 0) => {
    for (let i = start; i < children.length; i++) {
      const child = children[i] = optimized ? cloneIfMounted(children[i]) : normalizeVNode(children[i]);
      patch(
        null,
        child,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    }
  };
  const patchElement = (n1, n2, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    const el2 = n2.el = n1.el;
    let { patchFlag, dynamicChildren, dirs } = n2;
    patchFlag |= n1.patchFlag & 16;
    const oldProps = n1.props || EMPTY_OBJ;
    const newProps = n2.props || EMPTY_OBJ;
    let vnodeHook;
    parentComponent && toggleRecurse(parentComponent, false);
    if (vnodeHook = newProps.onVnodeBeforeUpdate) {
      invokeVNodeHook(vnodeHook, parentComponent, n2, n1);
    }
    if (dirs) {
      invokeDirectiveHook(n2, n1, parentComponent, "beforeUpdate");
    }
    parentComponent && toggleRecurse(parentComponent, true);
    if (oldProps.innerHTML && newProps.innerHTML == null || oldProps.textContent && newProps.textContent == null) {
      hostSetElementText(el2, "");
    }
    if (dynamicChildren) {
      patchBlockChildren(
        n1.dynamicChildren,
        dynamicChildren,
        el2,
        parentComponent,
        parentSuspense,
        resolveChildrenNamespace(n2, namespace),
        slotScopeIds
      );
    } else if (!optimized) {
      patchChildren(
        n1,
        n2,
        el2,
        null,
        parentComponent,
        parentSuspense,
        resolveChildrenNamespace(n2, namespace),
        slotScopeIds,
        false
      );
    }
    if (patchFlag > 0) {
      if (patchFlag & 16) {
        patchProps(el2, oldProps, newProps, parentComponent, namespace);
      } else {
        if (patchFlag & 2) {
          if (oldProps.class !== newProps.class) {
            hostPatchProp(el2, "class", null, newProps.class, namespace);
          }
        }
        if (patchFlag & 4) {
          hostPatchProp(el2, "style", oldProps.style, newProps.style, namespace);
        }
        if (patchFlag & 8) {
          const propsToUpdate = n2.dynamicProps;
          for (let i = 0; i < propsToUpdate.length; i++) {
            const key = propsToUpdate[i];
            const prev = oldProps[key];
            const next = newProps[key];
            if (next !== prev || key === "value") {
              hostPatchProp(el2, key, prev, next, namespace, parentComponent);
            }
          }
        }
      }
      if (patchFlag & 1) {
        if (n1.children !== n2.children) {
          hostSetElementText(el2, n2.children);
        }
      }
    } else if (!optimized && dynamicChildren == null) {
      patchProps(el2, oldProps, newProps, parentComponent, namespace);
    }
    if ((vnodeHook = newProps.onVnodeUpdated) || dirs) {
      queuePostRenderEffect(() => {
        vnodeHook && invokeVNodeHook(vnodeHook, parentComponent, n2, n1);
        dirs && invokeDirectiveHook(n2, n1, parentComponent, "updated");
      }, parentSuspense);
    }
  };
  const patchBlockChildren = (oldChildren, newChildren, fallbackContainer, parentComponent, parentSuspense, namespace, slotScopeIds) => {
    for (let i = 0; i < newChildren.length; i++) {
      const oldVNode = oldChildren[i];
      const newVNode = newChildren[i];
      const container = (
        // oldVNode may be an errored async setup() component inside Suspense
        // which will not have a mounted element
        oldVNode.el && // - In the case of a Fragment, we need to provide the actual parent
        // of the Fragment itself so it can move its children.
        (oldVNode.type === Fragment || // - In the case of different nodes, there is going to be a replacement
        // which also requires the correct parent container
        !isSameVNodeType(oldVNode, newVNode) || // - In the case of a component, it could contain anything.
        oldVNode.shapeFlag & (6 | 64 | 128)) ? hostParentNode(oldVNode.el) : (
          // In other cases, the parent container is not actually used so we
          // just pass the block element here to avoid a DOM parentNode call.
          fallbackContainer
        )
      );
      patch(
        oldVNode,
        newVNode,
        container,
        null,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        true
      );
    }
  };
  const patchProps = (el2, oldProps, newProps, parentComponent, namespace) => {
    if (oldProps !== newProps) {
      if (oldProps !== EMPTY_OBJ) {
        for (const key in oldProps) {
          if (!isReservedProp(key) && !(key in newProps)) {
            hostPatchProp(
              el2,
              key,
              oldProps[key],
              null,
              namespace,
              parentComponent
            );
          }
        }
      }
      for (const key in newProps) {
        if (isReservedProp(key)) continue;
        const next = newProps[key];
        const prev = oldProps[key];
        if (next !== prev && key !== "value") {
          hostPatchProp(el2, key, prev, next, namespace, parentComponent);
        }
      }
      if ("value" in newProps) {
        hostPatchProp(el2, "value", oldProps.value, newProps.value, namespace);
      }
    }
  };
  const processFragment = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    const fragmentStartAnchor = n2.el = n1 ? n1.el : hostCreateText("");
    const fragmentEndAnchor = n2.anchor = n1 ? n1.anchor : hostCreateText("");
    let { patchFlag, dynamicChildren, slotScopeIds: fragmentSlotScopeIds } = n2;
    if (fragmentSlotScopeIds) {
      slotScopeIds = slotScopeIds ? slotScopeIds.concat(fragmentSlotScopeIds) : fragmentSlotScopeIds;
    }
    if (n1 == null) {
      hostInsert(fragmentStartAnchor, container, anchor);
      hostInsert(fragmentEndAnchor, container, anchor);
      mountChildren(
        // #10007
        // such fragment like `<></>` will be compiled into
        // a fragment which doesn't have a children.
        // In this case fallback to an empty array
        n2.children || [],
        container,
        fragmentEndAnchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    } else {
      if (patchFlag > 0 && patchFlag & 64 && dynamicChildren && // #2715 the previous fragment could've been a BAILed one as a result
      // of renderSlot() with no valid children
      n1.dynamicChildren) {
        patchBlockChildren(
          n1.dynamicChildren,
          dynamicChildren,
          container,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds
        );
        if (
          // #2080 if the stable fragment has a key, it's a <template v-for> that may
          //  get moved around. Make sure all root level vnodes inherit el.
          // #2134 or if it's a component root, it may also get moved around
          // as the component is being moved.
          n2.key != null || parentComponent && n2 === parentComponent.subTree
        ) {
          traverseStaticChildren(
            n1,
            n2,
            true
            /* shallow */
          );
        }
      } else {
        patchChildren(
          n1,
          n2,
          container,
          fragmentEndAnchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      }
    }
  };
  const processComponent = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    n2.slotScopeIds = slotScopeIds;
    if (n1 == null) {
      if (n2.shapeFlag & 512) {
        parentComponent.ctx.activate(
          n2,
          container,
          anchor,
          namespace,
          optimized
        );
      } else {
        mountComponent(
          n2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          optimized
        );
      }
    } else {
      updateComponent(n1, n2, optimized);
    }
  };
  const mountComponent = (initialVNode, container, anchor, parentComponent, parentSuspense, namespace, optimized) => {
    const instance = initialVNode.component = createComponentInstance(
      initialVNode,
      parentComponent,
      parentSuspense
    );
    if (isKeepAlive(initialVNode)) {
      instance.ctx.renderer = internals;
    }
    {
      setupComponent(instance, false, optimized);
    }
    if (instance.asyncDep) {
      parentSuspense && parentSuspense.registerDep(instance, setupRenderEffect, optimized);
      if (!initialVNode.el) {
        const placeholder = instance.subTree = createVNode(Comment);
        processCommentNode(null, placeholder, container, anchor);
        initialVNode.placeholder = placeholder.el;
      }
    } else {
      setupRenderEffect(
        instance,
        initialVNode,
        container,
        anchor,
        parentSuspense,
        namespace,
        optimized
      );
    }
  };
  const updateComponent = (n1, n2, optimized) => {
    const instance = n2.component = n1.component;
    if (shouldUpdateComponent(n1, n2, optimized)) {
      if (instance.asyncDep && !instance.asyncResolved) {
        updateComponentPreRender(instance, n2, optimized);
        return;
      } else {
        instance.next = n2;
        instance.update();
      }
    } else {
      n2.el = n1.el;
      instance.vnode = n2;
    }
  };
  const setupRenderEffect = (instance, initialVNode, container, anchor, parentSuspense, namespace, optimized) => {
    const componentUpdateFn = () => {
      if (!instance.isMounted) {
        let vnodeHook;
        const { el: el2, props } = initialVNode;
        const { bm: bm2, m, parent: parent2, root, type } = instance;
        const isAsyncWrapperVNode = isAsyncWrapper(initialVNode);
        toggleRecurse(instance, false);
        if (bm2) {
          invokeArrayFns(bm2);
        }
        if (!isAsyncWrapperVNode && (vnodeHook = props && props.onVnodeBeforeMount)) {
          invokeVNodeHook(vnodeHook, parent2, initialVNode);
        }
        toggleRecurse(instance, true);
        {
          if (root.ce && // @ts-expect-error _def is private
          root.ce._def.shadowRoot !== false) {
            root.ce._injectChildStyle(type);
          }
          const subTree = instance.subTree = renderComponentRoot(instance);
          patch(
            null,
            subTree,
            container,
            anchor,
            instance,
            parentSuspense,
            namespace
          );
          initialVNode.el = subTree.el;
        }
        if (m) {
          queuePostRenderEffect(m, parentSuspense);
        }
        if (!isAsyncWrapperVNode && (vnodeHook = props && props.onVnodeMounted)) {
          const scopedInitialVNode = initialVNode;
          queuePostRenderEffect(
            () => invokeVNodeHook(vnodeHook, parent2, scopedInitialVNode),
            parentSuspense
          );
        }
        if (initialVNode.shapeFlag & 256 || parent2 && isAsyncWrapper(parent2.vnode) && parent2.vnode.shapeFlag & 256) {
          instance.a && queuePostRenderEffect(instance.a, parentSuspense);
        }
        instance.isMounted = true;
        initialVNode = container = anchor = null;
      } else {
        let { next, bu: bu2, u, parent: parent2, vnode } = instance;
        {
          const nonHydratedAsyncRoot = locateNonHydratedAsyncRoot(instance);
          if (nonHydratedAsyncRoot) {
            if (next) {
              next.el = vnode.el;
              updateComponentPreRender(instance, next, optimized);
            }
            nonHydratedAsyncRoot.asyncDep.then(() => {
              if (!instance.isUnmounted) {
                componentUpdateFn();
              }
            });
            return;
          }
        }
        let originNext = next;
        let vnodeHook;
        toggleRecurse(instance, false);
        if (next) {
          next.el = vnode.el;
          updateComponentPreRender(instance, next, optimized);
        } else {
          next = vnode;
        }
        if (bu2) {
          invokeArrayFns(bu2);
        }
        if (vnodeHook = next.props && next.props.onVnodeBeforeUpdate) {
          invokeVNodeHook(vnodeHook, parent2, next, vnode);
        }
        toggleRecurse(instance, true);
        const nextTree = renderComponentRoot(instance);
        const prevTree = instance.subTree;
        instance.subTree = nextTree;
        patch(
          prevTree,
          nextTree,
          // parent may have changed if it's in a teleport
          hostParentNode(prevTree.el),
          // anchor may have changed if it's in a fragment
          getNextHostNode(prevTree),
          instance,
          parentSuspense,
          namespace
        );
        next.el = nextTree.el;
        if (originNext === null) {
          updateHOCHostEl(instance, nextTree.el);
        }
        if (u) {
          queuePostRenderEffect(u, parentSuspense);
        }
        if (vnodeHook = next.props && next.props.onVnodeUpdated) {
          queuePostRenderEffect(
            () => invokeVNodeHook(vnodeHook, parent2, next, vnode),
            parentSuspense
          );
        }
      }
    };
    instance.scope.on();
    const effect2 = instance.effect = new ReactiveEffect(componentUpdateFn);
    instance.scope.off();
    const update = instance.update = effect2.run.bind(effect2);
    const job = instance.job = effect2.runIfDirty.bind(effect2);
    job.i = instance;
    job.id = instance.uid;
    effect2.scheduler = () => queueJob(job);
    toggleRecurse(instance, true);
    update();
  };
  const updateComponentPreRender = (instance, nextVNode, optimized) => {
    nextVNode.component = instance;
    const prevProps = instance.vnode.props;
    instance.vnode = nextVNode;
    instance.next = null;
    updateProps(instance, nextVNode.props, prevProps, optimized);
    updateSlots(instance, nextVNode.children, optimized);
    pauseTracking();
    flushPreFlushCbs(instance);
    resetTracking();
  };
  const patchChildren = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized = false) => {
    const c1 = n1 && n1.children;
    const prevShapeFlag = n1 ? n1.shapeFlag : 0;
    const c2 = n2.children;
    const { patchFlag, shapeFlag } = n2;
    if (patchFlag > 0) {
      if (patchFlag & 128) {
        patchKeyedChildren(
          c1,
          c2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
        return;
      } else if (patchFlag & 256) {
        patchUnkeyedChildren(
          c1,
          c2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
        return;
      }
    }
    if (shapeFlag & 8) {
      if (prevShapeFlag & 16) {
        unmountChildren(c1, parentComponent, parentSuspense);
      }
      if (c2 !== c1) {
        hostSetElementText(container, c2);
      }
    } else {
      if (prevShapeFlag & 16) {
        if (shapeFlag & 16) {
          patchKeyedChildren(
            c1,
            c2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else {
          unmountChildren(c1, parentComponent, parentSuspense, true);
        }
      } else {
        if (prevShapeFlag & 8) {
          hostSetElementText(container, "");
        }
        if (shapeFlag & 16) {
          mountChildren(
            c2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        }
      }
    }
  };
  const patchUnkeyedChildren = (c1, c2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    c1 = c1 || EMPTY_ARR;
    c2 = c2 || EMPTY_ARR;
    const oldLength = c1.length;
    const newLength = c2.length;
    const commonLength = Math.min(oldLength, newLength);
    let i;
    for (i = 0; i < commonLength; i++) {
      const nextChild = c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]);
      patch(
        c1[i],
        nextChild,
        container,
        null,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    }
    if (oldLength > newLength) {
      unmountChildren(
        c1,
        parentComponent,
        parentSuspense,
        true,
        false,
        commonLength
      );
    } else {
      mountChildren(
        c2,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized,
        commonLength
      );
    }
  };
  const patchKeyedChildren = (c1, c2, container, parentAnchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    let i = 0;
    const l2 = c2.length;
    let e1 = c1.length - 1;
    let e2 = l2 - 1;
    while (i <= e1 && i <= e2) {
      const n1 = c1[i];
      const n2 = c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]);
      if (isSameVNodeType(n1, n2)) {
        patch(
          n1,
          n2,
          container,
          null,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      } else {
        break;
      }
      i++;
    }
    while (i <= e1 && i <= e2) {
      const n1 = c1[e1];
      const n2 = c2[e2] = optimized ? cloneIfMounted(c2[e2]) : normalizeVNode(c2[e2]);
      if (isSameVNodeType(n1, n2)) {
        patch(
          n1,
          n2,
          container,
          null,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      } else {
        break;
      }
      e1--;
      e2--;
    }
    if (i > e1) {
      if (i <= e2) {
        const nextPos = e2 + 1;
        const anchor = nextPos < l2 ? c2[nextPos].el : parentAnchor;
        while (i <= e2) {
          patch(
            null,
            c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]),
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
          i++;
        }
      }
    } else if (i > e2) {
      while (i <= e1) {
        unmount(c1[i], parentComponent, parentSuspense, true);
        i++;
      }
    } else {
      const s1 = i;
      const s2 = i;
      const keyToNewIndexMap = /* @__PURE__ */ new Map();
      for (i = s2; i <= e2; i++) {
        const nextChild = c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]);
        if (nextChild.key != null) {
          keyToNewIndexMap.set(nextChild.key, i);
        }
      }
      let j;
      let patched = 0;
      const toBePatched = e2 - s2 + 1;
      let moved = false;
      let maxNewIndexSoFar = 0;
      const newIndexToOldIndexMap = new Array(toBePatched);
      for (i = 0; i < toBePatched; i++) newIndexToOldIndexMap[i] = 0;
      for (i = s1; i <= e1; i++) {
        const prevChild = c1[i];
        if (patched >= toBePatched) {
          unmount(prevChild, parentComponent, parentSuspense, true);
          continue;
        }
        let newIndex;
        if (prevChild.key != null) {
          newIndex = keyToNewIndexMap.get(prevChild.key);
        } else {
          for (j = s2; j <= e2; j++) {
            if (newIndexToOldIndexMap[j - s2] === 0 && isSameVNodeType(prevChild, c2[j])) {
              newIndex = j;
              break;
            }
          }
        }
        if (newIndex === void 0) {
          unmount(prevChild, parentComponent, parentSuspense, true);
        } else {
          newIndexToOldIndexMap[newIndex - s2] = i + 1;
          if (newIndex >= maxNewIndexSoFar) {
            maxNewIndexSoFar = newIndex;
          } else {
            moved = true;
          }
          patch(
            prevChild,
            c2[newIndex],
            container,
            null,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
          patched++;
        }
      }
      const increasingNewIndexSequence = moved ? getSequence(newIndexToOldIndexMap) : EMPTY_ARR;
      j = increasingNewIndexSequence.length - 1;
      for (i = toBePatched - 1; i >= 0; i--) {
        const nextIndex = s2 + i;
        const nextChild = c2[nextIndex];
        const anchorVNode = c2[nextIndex + 1];
        const anchor = nextIndex + 1 < l2 ? (
          // #13559, fallback to el placeholder for unresolved async component
          anchorVNode.el || anchorVNode.placeholder
        ) : parentAnchor;
        if (newIndexToOldIndexMap[i] === 0) {
          patch(
            null,
            nextChild,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else if (moved) {
          if (j < 0 || i !== increasingNewIndexSequence[j]) {
            move(nextChild, container, anchor, 2);
          } else {
            j--;
          }
        }
      }
    }
  };
  const move = (vnode, container, anchor, moveType, parentSuspense = null) => {
    const { el: el2, type, transition, children, shapeFlag } = vnode;
    if (shapeFlag & 6) {
      move(vnode.component.subTree, container, anchor, moveType);
      return;
    }
    if (shapeFlag & 128) {
      vnode.suspense.move(container, anchor, moveType);
      return;
    }
    if (shapeFlag & 64) {
      type.move(vnode, container, anchor, internals);
      return;
    }
    if (type === Fragment) {
      hostInsert(el2, container, anchor);
      for (let i = 0; i < children.length; i++) {
        move(children[i], container, anchor, moveType);
      }
      hostInsert(vnode.anchor, container, anchor);
      return;
    }
    if (type === Static) {
      moveStaticNode(vnode, container, anchor);
      return;
    }
    const needTransition2 = moveType !== 2 && shapeFlag & 1 && transition;
    if (needTransition2) {
      if (moveType === 0) {
        transition.beforeEnter(el2);
        hostInsert(el2, container, anchor);
        queuePostRenderEffect(() => transition.enter(el2), parentSuspense);
      } else {
        const { leave, delayLeave, afterLeave } = transition;
        const remove22 = () => {
          if (vnode.ctx.isUnmounted) {
            hostRemove(el2);
          } else {
            hostInsert(el2, container, anchor);
          }
        };
        const performLeave = () => {
          if (el2._isLeaving) {
            el2[leaveCbKey](
              true
              /* cancelled */
            );
          }
          leave(el2, () => {
            remove22();
            afterLeave && afterLeave();
          });
        };
        if (delayLeave) {
          delayLeave(el2, remove22, performLeave);
        } else {
          performLeave();
        }
      }
    } else {
      hostInsert(el2, container, anchor);
    }
  };
  const unmount = (vnode, parentComponent, parentSuspense, doRemove = false, optimized = false) => {
    const {
      type,
      props,
      ref: ref3,
      children,
      dynamicChildren,
      shapeFlag,
      patchFlag,
      dirs,
      cacheIndex
    } = vnode;
    if (patchFlag === -2) {
      optimized = false;
    }
    if (ref3 != null) {
      pauseTracking();
      setRef(ref3, null, parentSuspense, vnode, true);
      resetTracking();
    }
    if (cacheIndex != null) {
      parentComponent.renderCache[cacheIndex] = void 0;
    }
    if (shapeFlag & 256) {
      parentComponent.ctx.deactivate(vnode);
      return;
    }
    const shouldInvokeDirs = shapeFlag & 1 && dirs;
    const shouldInvokeVnodeHook = !isAsyncWrapper(vnode);
    let vnodeHook;
    if (shouldInvokeVnodeHook && (vnodeHook = props && props.onVnodeBeforeUnmount)) {
      invokeVNodeHook(vnodeHook, parentComponent, vnode);
    }
    if (shapeFlag & 6) {
      unmountComponent(vnode.component, parentSuspense, doRemove);
    } else {
      if (shapeFlag & 128) {
        vnode.suspense.unmount(parentSuspense, doRemove);
        return;
      }
      if (shouldInvokeDirs) {
        invokeDirectiveHook(vnode, null, parentComponent, "beforeUnmount");
      }
      if (shapeFlag & 64) {
        vnode.type.remove(
          vnode,
          parentComponent,
          parentSuspense,
          internals,
          doRemove
        );
      } else if (dynamicChildren && // #5154
      // when v-once is used inside a block, setBlockTracking(-1) marks the
      // parent block with hasOnce: true
      // so that it doesn't take the fast path during unmount - otherwise
      // components nested in v-once are never unmounted.
      !dynamicChildren.hasOnce && // #1153: fast path should not be taken for non-stable (v-for) fragments
      (type !== Fragment || patchFlag > 0 && patchFlag & 64)) {
        unmountChildren(
          dynamicChildren,
          parentComponent,
          parentSuspense,
          false,
          true
        );
      } else if (type === Fragment && patchFlag & (128 | 256) || !optimized && shapeFlag & 16) {
        unmountChildren(children, parentComponent, parentSuspense);
      }
      if (doRemove) {
        remove2(vnode);
      }
    }
    if (shouldInvokeVnodeHook && (vnodeHook = props && props.onVnodeUnmounted) || shouldInvokeDirs) {
      queuePostRenderEffect(() => {
        vnodeHook && invokeVNodeHook(vnodeHook, parentComponent, vnode);
        shouldInvokeDirs && invokeDirectiveHook(vnode, null, parentComponent, "unmounted");
      }, parentSuspense);
    }
  };
  const remove2 = (vnode) => {
    const { type, el: el2, anchor, transition } = vnode;
    if (type === Fragment) {
      {
        removeFragment(el2, anchor);
      }
      return;
    }
    if (type === Static) {
      removeStaticNode(vnode);
      return;
    }
    const performRemove = () => {
      hostRemove(el2);
      if (transition && !transition.persisted && transition.afterLeave) {
        transition.afterLeave();
      }
    };
    if (vnode.shapeFlag & 1 && transition && !transition.persisted) {
      const { leave, delayLeave } = transition;
      const performLeave = () => leave(el2, performRemove);
      if (delayLeave) {
        delayLeave(vnode.el, performRemove, performLeave);
      } else {
        performLeave();
      }
    } else {
      performRemove();
    }
  };
  const removeFragment = (cur, end) => {
    let next;
    while (cur !== end) {
      next = hostNextSibling(cur);
      hostRemove(cur);
      cur = next;
    }
    hostRemove(end);
  };
  const unmountComponent = (instance, parentSuspense, doRemove) => {
    const { bum, scope, job, subTree, um: um2, m, a } = instance;
    invalidateMount(m);
    invalidateMount(a);
    if (bum) {
      invokeArrayFns(bum);
    }
    scope.stop();
    if (job) {
      job.flags |= 8;
      unmount(subTree, instance, parentSuspense, doRemove);
    }
    if (um2) {
      queuePostRenderEffect(um2, parentSuspense);
    }
    queuePostRenderEffect(() => {
      instance.isUnmounted = true;
    }, parentSuspense);
  };
  const unmountChildren = (children, parentComponent, parentSuspense, doRemove = false, optimized = false, start = 0) => {
    for (let i = start; i < children.length; i++) {
      unmount(children[i], parentComponent, parentSuspense, doRemove, optimized);
    }
  };
  const getNextHostNode = (vnode) => {
    if (vnode.shapeFlag & 6) {
      return getNextHostNode(vnode.component.subTree);
    }
    if (vnode.shapeFlag & 128) {
      return vnode.suspense.next();
    }
    const el2 = hostNextSibling(vnode.anchor || vnode.el);
    const teleportEnd = el2 && el2[TeleportEndKey];
    return teleportEnd ? hostNextSibling(teleportEnd) : el2;
  };
  let isFlushing = false;
  const render2 = (vnode, container, namespace) => {
    if (vnode == null) {
      if (container._vnode) {
        unmount(container._vnode, null, null, true);
      }
    } else {
      patch(
        container._vnode || null,
        vnode,
        container,
        null,
        null,
        null,
        namespace
      );
    }
    container._vnode = vnode;
    if (!isFlushing) {
      isFlushing = true;
      flushPreFlushCbs();
      flushPostFlushCbs();
      isFlushing = false;
    }
  };
  const internals = {
    p: patch,
    um: unmount,
    m: move,
    r: remove2,
    mt: mountComponent,
    mc: mountChildren,
    pc: patchChildren,
    pbc: patchBlockChildren,
    n: getNextHostNode,
    o: options
  };
  let hydrate;
  return {
    render: render2,
    hydrate,
    createApp: createAppAPI(render2)
  };
}
function resolveChildrenNamespace({ type, props }, currentNamespace) {
  return currentNamespace === "svg" && type === "foreignObject" || currentNamespace === "mathml" && type === "annotation-xml" && props && props.encoding && props.encoding.includes("html") ? void 0 : currentNamespace;
}
function toggleRecurse({ effect: effect2, job }, allowed) {
  if (allowed) {
    effect2.flags |= 32;
    job.flags |= 4;
  } else {
    effect2.flags &= -33;
    job.flags &= -5;
  }
}
function needTransition(parentSuspense, transition) {
  return (!parentSuspense || parentSuspense && !parentSuspense.pendingBranch) && transition && !transition.persisted;
}
function traverseStaticChildren(n1, n2, shallow = false) {
  const ch1 = n1.children;
  const ch2 = n2.children;
  if (isArray(ch1) && isArray(ch2)) {
    for (let i = 0; i < ch1.length; i++) {
      const c1 = ch1[i];
      let c2 = ch2[i];
      if (c2.shapeFlag & 1 && !c2.dynamicChildren) {
        if (c2.patchFlag <= 0 || c2.patchFlag === 32) {
          c2 = ch2[i] = cloneIfMounted(ch2[i]);
          c2.el = c1.el;
        }
        if (!shallow && c2.patchFlag !== -2)
          traverseStaticChildren(c1, c2);
      }
      if (c2.type === Text && // avoid cached text nodes retaining detached dom nodes
      c2.patchFlag !== -1) {
        c2.el = c1.el;
      }
      if (c2.type === Comment && !c2.el) {
        c2.el = c1.el;
      }
    }
  }
}
function getSequence(arr) {
  const p2 = arr.slice();
  const result = [0];
  let i, j, u, v, c;
  const len = arr.length;
  for (i = 0; i < len; i++) {
    const arrI = arr[i];
    if (arrI !== 0) {
      j = result[result.length - 1];
      if (arr[j] < arrI) {
        p2[i] = j;
        result.push(i);
        continue;
      }
      u = 0;
      v = result.length - 1;
      while (u < v) {
        c = u + v >> 1;
        if (arr[result[c]] < arrI) {
          u = c + 1;
        } else {
          v = c;
        }
      }
      if (arrI < arr[result[u]]) {
        if (u > 0) {
          p2[i] = result[u - 1];
        }
        result[u] = i;
      }
    }
  }
  u = result.length;
  v = result[u - 1];
  while (u-- > 0) {
    result[u] = v;
    v = p2[v];
  }
  return result;
}
function locateNonHydratedAsyncRoot(instance) {
  const subComponent = instance.subTree.component;
  if (subComponent) {
    if (subComponent.asyncDep && !subComponent.asyncResolved) {
      return subComponent;
    } else {
      return locateNonHydratedAsyncRoot(subComponent);
    }
  }
}
function invalidateMount(hooks) {
  if (hooks) {
    for (let i = 0; i < hooks.length; i++)
      hooks[i].flags |= 8;
  }
}
const isSuspense = (type) => type.__isSuspense;
function queueEffectWithSuspense(fn, suspense) {
  if (suspense && suspense.pendingBranch) {
    if (isArray(fn)) {
      suspense.effects.push(...fn);
    } else {
      suspense.effects.push(fn);
    }
  } else {
    queuePostFlushCb(fn);
  }
}
const Fragment = Symbol.for("v-fgt");
const Text = Symbol.for("v-txt");
const Comment = Symbol.for("v-cmt");
const Static = Symbol.for("v-stc");
const blockStack = [];
let currentBlock = null;
function openBlock(disableTracking = false) {
  blockStack.push(currentBlock = disableTracking ? null : []);
}
function closeBlock() {
  blockStack.pop();
  currentBlock = blockStack[blockStack.length - 1] || null;
}
let isBlockTreeEnabled = 1;
function setBlockTracking(value, inVOnce = false) {
  isBlockTreeEnabled += value;
  if (value < 0 && currentBlock && inVOnce) {
    currentBlock.hasOnce = true;
  }
}
function setupBlock(vnode) {
  vnode.dynamicChildren = isBlockTreeEnabled > 0 ? currentBlock || EMPTY_ARR : null;
  closeBlock();
  if (isBlockTreeEnabled > 0 && currentBlock) {
    currentBlock.push(vnode);
  }
  return vnode;
}
function createElementBlock(type, props, children, patchFlag, dynamicProps, shapeFlag) {
  return setupBlock(
    createBaseVNode(
      type,
      props,
      children,
      patchFlag,
      dynamicProps,
      shapeFlag,
      true
    )
  );
}
function createBlock(type, props, children, patchFlag, dynamicProps) {
  return setupBlock(
    createVNode(
      type,
      props,
      children,
      patchFlag,
      dynamicProps,
      true
    )
  );
}
function isVNode(value) {
  return value ? value.__v_isVNode === true : false;
}
function isSameVNodeType(n1, n2) {
  return n1.type === n2.type && n1.key === n2.key;
}
const normalizeKey = ({ key }) => key != null ? key : null;
const normalizeRef = ({
  ref: ref3,
  ref_key,
  ref_for
}) => {
  if (typeof ref3 === "number") {
    ref3 = "" + ref3;
  }
  return ref3 != null ? isString(ref3) || isRef(ref3) || isFunction(ref3) ? { i: currentRenderingInstance, r: ref3, k: ref_key, f: !!ref_for } : ref3 : null;
};
function createBaseVNode(type, props = null, children = null, patchFlag = 0, dynamicProps = null, shapeFlag = type === Fragment ? 0 : 1, isBlockNode = false, needFullChildrenNormalization = false) {
  const vnode = {
    __v_isVNode: true,
    __v_skip: true,
    type,
    props,
    key: props && normalizeKey(props),
    ref: props && normalizeRef(props),
    scopeId: currentScopeId,
    slotScopeIds: null,
    children,
    component: null,
    suspense: null,
    ssContent: null,
    ssFallback: null,
    dirs: null,
    transition: null,
    el: null,
    anchor: null,
    target: null,
    targetStart: null,
    targetAnchor: null,
    staticCount: 0,
    shapeFlag,
    patchFlag,
    dynamicProps,
    dynamicChildren: null,
    appContext: null,
    ctx: currentRenderingInstance
  };
  if (needFullChildrenNormalization) {
    normalizeChildren(vnode, children);
    if (shapeFlag & 128) {
      type.normalize(vnode);
    }
  } else if (children) {
    vnode.shapeFlag |= isString(children) ? 8 : 16;
  }
  if (isBlockTreeEnabled > 0 && // avoid a block node from tracking itself
  !isBlockNode && // has current parent block
  currentBlock && // presence of a patch flag indicates this node needs patching on updates.
  // component nodes also should always be patched, because even if the
  // component doesn't need to update, it needs to persist the instance on to
  // the next vnode so that it can be properly unmounted later.
  (vnode.patchFlag > 0 || shapeFlag & 6) && // the EVENTS flag is only for hydration and if it is the only flag, the
  // vnode should not be considered dynamic due to handler caching.
  vnode.patchFlag !== 32) {
    currentBlock.push(vnode);
  }
  return vnode;
}
const createVNode = _createVNode;
function _createVNode(type, props = null, children = null, patchFlag = 0, dynamicProps = null, isBlockNode = false) {
  if (!type || type === NULL_DYNAMIC_COMPONENT) {
    type = Comment;
  }
  if (isVNode(type)) {
    const cloned = cloneVNode(
      type,
      props,
      true
      /* mergeRef: true */
    );
    if (children) {
      normalizeChildren(cloned, children);
    }
    if (isBlockTreeEnabled > 0 && !isBlockNode && currentBlock) {
      if (cloned.shapeFlag & 6) {
        currentBlock[currentBlock.indexOf(type)] = cloned;
      } else {
        currentBlock.push(cloned);
      }
    }
    cloned.patchFlag = -2;
    return cloned;
  }
  if (isClassComponent(type)) {
    type = type.__vccOpts;
  }
  if (props) {
    props = guardReactiveProps(props);
    let { class: klass, style } = props;
    if (klass && !isString(klass)) {
      props.class = normalizeClass(klass);
    }
    if (isObject$4(style)) {
      if (isProxy(style) && !isArray(style)) {
        style = extend({}, style);
      }
      props.style = normalizeStyle(style);
    }
  }
  const shapeFlag = isString(type) ? 1 : isSuspense(type) ? 128 : isTeleport(type) ? 64 : isObject$4(type) ? 4 : isFunction(type) ? 2 : 0;
  return createBaseVNode(
    type,
    props,
    children,
    patchFlag,
    dynamicProps,
    shapeFlag,
    isBlockNode,
    true
  );
}
function guardReactiveProps(props) {
  if (!props) return null;
  return isProxy(props) || isInternalObject(props) ? extend({}, props) : props;
}
function cloneVNode(vnode, extraProps, mergeRef = false, cloneTransition = false) {
  const { props, ref: ref3, patchFlag, children, transition } = vnode;
  const mergedProps = extraProps ? mergeProps(props || {}, extraProps) : props;
  const cloned = {
    __v_isVNode: true,
    __v_skip: true,
    type: vnode.type,
    props: mergedProps,
    key: mergedProps && normalizeKey(mergedProps),
    ref: extraProps && extraProps.ref ? (
      // #2078 in the case of <component :is="vnode" ref="extra"/>
      // if the vnode itself already has a ref, cloneVNode will need to merge
      // the refs so the single vnode can be set on multiple refs
      mergeRef && ref3 ? isArray(ref3) ? ref3.concat(normalizeRef(extraProps)) : [ref3, normalizeRef(extraProps)] : normalizeRef(extraProps)
    ) : ref3,
    scopeId: vnode.scopeId,
    slotScopeIds: vnode.slotScopeIds,
    children,
    target: vnode.target,
    targetStart: vnode.targetStart,
    targetAnchor: vnode.targetAnchor,
    staticCount: vnode.staticCount,
    shapeFlag: vnode.shapeFlag,
    // if the vnode is cloned with extra props, we can no longer assume its
    // existing patch flag to be reliable and need to add the FULL_PROPS flag.
    // note: preserve flag for fragments since they use the flag for children
    // fast paths only.
    patchFlag: extraProps && vnode.type !== Fragment ? patchFlag === -1 ? 16 : patchFlag | 16 : patchFlag,
    dynamicProps: vnode.dynamicProps,
    dynamicChildren: vnode.dynamicChildren,
    appContext: vnode.appContext,
    dirs: vnode.dirs,
    transition,
    // These should technically only be non-null on mounted VNodes. However,
    // they *should* be copied for kept-alive vnodes. So we just always copy
    // them since them being non-null during a mount doesn't affect the logic as
    // they will simply be overwritten.
    component: vnode.component,
    suspense: vnode.suspense,
    ssContent: vnode.ssContent && cloneVNode(vnode.ssContent),
    ssFallback: vnode.ssFallback && cloneVNode(vnode.ssFallback),
    placeholder: vnode.placeholder,
    el: vnode.el,
    anchor: vnode.anchor,
    ctx: vnode.ctx,
    ce: vnode.ce
  };
  if (transition && cloneTransition) {
    setTransitionHooks(
      cloned,
      transition.clone(cloned)
    );
  }
  return cloned;
}
function createTextVNode(text = " ", flag = 0) {
  return createVNode(Text, null, text, flag);
}
function createStaticVNode(content, numberOfNodes) {
  const vnode = createVNode(Static, null, content);
  vnode.staticCount = numberOfNodes;
  return vnode;
}
function createCommentVNode(text = "", asBlock = false) {
  return asBlock ? (openBlock(), createBlock(Comment, null, text)) : createVNode(Comment, null, text);
}
function normalizeVNode(child) {
  if (child == null || typeof child === "boolean") {
    return createVNode(Comment);
  } else if (isArray(child)) {
    return createVNode(
      Fragment,
      null,
      // #3666, avoid reference pollution when reusing vnode
      child.slice()
    );
  } else if (isVNode(child)) {
    return cloneIfMounted(child);
  } else {
    return createVNode(Text, null, String(child));
  }
}
function cloneIfMounted(child) {
  return child.el === null && child.patchFlag !== -1 || child.memo ? child : cloneVNode(child);
}
function normalizeChildren(vnode, children) {
  let type = 0;
  const { shapeFlag } = vnode;
  if (children == null) {
    children = null;
  } else if (isArray(children)) {
    type = 16;
  } else if (typeof children === "object") {
    if (shapeFlag & (1 | 64)) {
      const slot = children.default;
      if (slot) {
        slot._c && (slot._d = false);
        normalizeChildren(vnode, slot());
        slot._c && (slot._d = true);
      }
      return;
    } else {
      type = 32;
      const slotFlag = children._;
      if (!slotFlag && !isInternalObject(children)) {
        children._ctx = currentRenderingInstance;
      } else if (slotFlag === 3 && currentRenderingInstance) {
        if (currentRenderingInstance.slots._ === 1) {
          children._ = 1;
        } else {
          children._ = 2;
          vnode.patchFlag |= 1024;
        }
      }
    }
  } else if (isFunction(children)) {
    children = { default: children, _ctx: currentRenderingInstance };
    type = 32;
  } else {
    children = String(children);
    if (shapeFlag & 64) {
      type = 16;
      children = [createTextVNode(children)];
    } else {
      type = 8;
    }
  }
  vnode.children = children;
  vnode.shapeFlag |= type;
}
function mergeProps(...args) {
  const ret = {};
  for (let i = 0; i < args.length; i++) {
    const toMerge = args[i];
    for (const key in toMerge) {
      if (key === "class") {
        if (ret.class !== toMerge.class) {
          ret.class = normalizeClass([ret.class, toMerge.class]);
        }
      } else if (key === "style") {
        ret.style = normalizeStyle([ret.style, toMerge.style]);
      } else if (isOn(key)) {
        const existing = ret[key];
        const incoming = toMerge[key];
        if (incoming && existing !== incoming && !(isArray(existing) && existing.includes(incoming))) {
          ret[key] = existing ? [].concat(existing, incoming) : incoming;
        }
      } else if (key !== "") {
        ret[key] = toMerge[key];
      }
    }
  }
  return ret;
}
function invokeVNodeHook(hook, instance, vnode, prevVNode = null) {
  callWithAsyncErrorHandling(hook, instance, 7, [
    vnode,
    prevVNode
  ]);
}
const emptyAppContext = createAppContext();
let uid = 0;
function createComponentInstance(vnode, parent2, suspense) {
  const type = vnode.type;
  const appContext = (parent2 ? parent2.appContext : vnode.appContext) || emptyAppContext;
  const instance = {
    uid: uid++,
    vnode,
    type,
    parent: parent2,
    appContext,
    root: null,
    // to be immediately set
    next: null,
    subTree: null,
    // will be set synchronously right after creation
    effect: null,
    update: null,
    // will be set synchronously right after creation
    job: null,
    scope: new EffectScope(
      true
      /* detached */
    ),
    render: null,
    proxy: null,
    exposed: null,
    exposeProxy: null,
    withProxy: null,
    provides: parent2 ? parent2.provides : Object.create(appContext.provides),
    ids: parent2 ? parent2.ids : ["", 0, 0],
    accessCache: null,
    renderCache: [],
    // local resolved assets
    components: null,
    directives: null,
    // resolved props and emits options
    propsOptions: normalizePropsOptions(type, appContext),
    emitsOptions: normalizeEmitsOptions(type, appContext),
    // emit
    emit: null,
    // to be set immediately
    emitted: null,
    // props default value
    propsDefaults: EMPTY_OBJ,
    // inheritAttrs
    inheritAttrs: type.inheritAttrs,
    // state
    ctx: EMPTY_OBJ,
    data: EMPTY_OBJ,
    props: EMPTY_OBJ,
    attrs: EMPTY_OBJ,
    slots: EMPTY_OBJ,
    refs: EMPTY_OBJ,
    setupState: EMPTY_OBJ,
    setupContext: null,
    // suspense related
    suspense,
    suspenseId: suspense ? suspense.pendingId : 0,
    asyncDep: null,
    asyncResolved: false,
    // lifecycle hooks
    // not using enums here because it results in computed properties
    isMounted: false,
    isUnmounted: false,
    isDeactivated: false,
    bc: null,
    c: null,
    bm: null,
    m: null,
    bu: null,
    u: null,
    um: null,
    bum: null,
    da: null,
    a: null,
    rtg: null,
    rtc: null,
    ec: null,
    sp: null
  };
  {
    instance.ctx = { _: instance };
  }
  instance.root = parent2 ? parent2.root : instance;
  instance.emit = emit.bind(null, instance);
  if (vnode.ce) {
    vnode.ce(instance);
  }
  return instance;
}
let currentInstance = null;
const getCurrentInstance = () => currentInstance || currentRenderingInstance;
let internalSetCurrentInstance;
let setInSSRSetupState;
{
  const g = getGlobalThis();
  const registerGlobalSetter = (key, setter) => {
    let setters;
    if (!(setters = g[key])) setters = g[key] = [];
    setters.push(setter);
    return (v) => {
      if (setters.length > 1) setters.forEach((set) => set(v));
      else setters[0](v);
    };
  };
  internalSetCurrentInstance = registerGlobalSetter(
    `__VUE_INSTANCE_SETTERS__`,
    (v) => currentInstance = v
  );
  setInSSRSetupState = registerGlobalSetter(
    `__VUE_SSR_SETTERS__`,
    (v) => isInSSRComponentSetup = v
  );
}
const setCurrentInstance = (instance) => {
  const prev = currentInstance;
  internalSetCurrentInstance(instance);
  instance.scope.on();
  return () => {
    instance.scope.off();
    internalSetCurrentInstance(prev);
  };
};
const unsetCurrentInstance = () => {
  currentInstance && currentInstance.scope.off();
  internalSetCurrentInstance(null);
};
function isStatefulComponent(instance) {
  return instance.vnode.shapeFlag & 4;
}
let isInSSRComponentSetup = false;
function setupComponent(instance, isSSR = false, optimized = false) {
  isSSR && setInSSRSetupState(isSSR);
  const { props, children } = instance.vnode;
  const isStateful = isStatefulComponent(instance);
  initProps(instance, props, isStateful, isSSR);
  initSlots(instance, children, optimized || isSSR);
  const setupResult = isStateful ? setupStatefulComponent(instance, isSSR) : void 0;
  isSSR && setInSSRSetupState(false);
  return setupResult;
}
function setupStatefulComponent(instance, isSSR) {
  const Component = instance.type;
  instance.accessCache = /* @__PURE__ */ Object.create(null);
  instance.proxy = new Proxy(instance.ctx, PublicInstanceProxyHandlers);
  const { setup } = Component;
  if (setup) {
    pauseTracking();
    const setupContext = instance.setupContext = setup.length > 1 ? createSetupContext(instance) : null;
    const reset2 = setCurrentInstance(instance);
    const setupResult = callWithErrorHandling(
      setup,
      instance,
      0,
      [
        instance.props,
        setupContext
      ]
    );
    const isAsyncSetup = isPromise(setupResult);
    resetTracking();
    reset2();
    if ((isAsyncSetup || instance.sp) && !isAsyncWrapper(instance)) {
      markAsyncBoundary(instance);
    }
    if (isAsyncSetup) {
      setupResult.then(unsetCurrentInstance, unsetCurrentInstance);
      if (isSSR) {
        return setupResult.then((resolvedResult) => {
          handleSetupResult(instance, resolvedResult);
        }).catch((e) => {
          handleError(e, instance, 0);
        });
      } else {
        instance.asyncDep = setupResult;
      }
    } else {
      handleSetupResult(instance, setupResult);
    }
  } else {
    finishComponentSetup(instance);
  }
}
function handleSetupResult(instance, setupResult, isSSR) {
  if (isFunction(setupResult)) {
    if (instance.type.__ssrInlineRender) {
      instance.ssrRender = setupResult;
    } else {
      instance.render = setupResult;
    }
  } else if (isObject$4(setupResult)) {
    instance.setupState = proxyRefs(setupResult);
  } else ;
  finishComponentSetup(instance);
}
function finishComponentSetup(instance, isSSR, skipOptions) {
  const Component = instance.type;
  if (!instance.render) {
    instance.render = Component.render || NOOP;
  }
  {
    const reset2 = setCurrentInstance(instance);
    pauseTracking();
    try {
      applyOptions(instance);
    } finally {
      resetTracking();
      reset2();
    }
  }
}
const attrsProxyHandlers = {
  get(target, key) {
    track(target, "get", "");
    return target[key];
  }
};
function createSetupContext(instance) {
  const expose = (exposed) => {
    instance.exposed = exposed || {};
  };
  {
    return {
      attrs: new Proxy(instance.attrs, attrsProxyHandlers),
      slots: instance.slots,
      emit: instance.emit,
      expose
    };
  }
}
function getComponentPublicInstance(instance) {
  if (instance.exposed) {
    return instance.exposeProxy || (instance.exposeProxy = new Proxy(proxyRefs(markRaw(instance.exposed)), {
      get(target, key) {
        if (key in target) {
          return target[key];
        } else if (key in publicPropertiesMap) {
          return publicPropertiesMap[key](instance);
        }
      },
      has(target, key) {
        return key in target || key in publicPropertiesMap;
      }
    }));
  } else {
    return instance.proxy;
  }
}
const classifyRE = /(?:^|[-_])\w/g;
const classify = (str) => str.replace(classifyRE, (c) => c.toUpperCase()).replace(/[-_]/g, "");
function getComponentName(Component, includeInferred = true) {
  return isFunction(Component) ? Component.displayName || Component.name : Component.name || includeInferred && Component.__name;
}
function formatComponentName(instance, Component, isRoot = false) {
  let name2 = getComponentName(Component);
  if (!name2 && Component.__file) {
    const match = Component.__file.match(/([^/\\]+)\.\w+$/);
    if (match) {
      name2 = match[1];
    }
  }
  if (!name2 && instance) {
    const inferFromRegistry = (registry) => {
      for (const key in registry) {
        if (registry[key] === Component) {
          return key;
        }
      }
    };
    name2 = inferFromRegistry(instance.components) || instance.parent && inferFromRegistry(
      instance.parent.type.components
    ) || inferFromRegistry(instance.appContext.components);
  }
  return name2 ? classify(name2) : isRoot ? `App` : `Anonymous`;
}
function isClassComponent(value) {
  return isFunction(value) && "__vccOpts" in value;
}
const computed = (getterOrOptions, debugOptions) => {
  const c = computed$1(getterOrOptions, debugOptions, isInSSRComponentSetup);
  return c;
};
function h(type, propsOrChildren, children) {
  try {
    setBlockTracking(-1);
    const l = arguments.length;
    if (l === 2) {
      if (isObject$4(propsOrChildren) && !isArray(propsOrChildren)) {
        if (isVNode(propsOrChildren)) {
          return createVNode(type, null, [propsOrChildren]);
        }
        return createVNode(type, propsOrChildren);
      } else {
        return createVNode(type, null, propsOrChildren);
      }
    } else {
      if (l > 3) {
        children = Array.prototype.slice.call(arguments, 2);
      } else if (l === 3 && isVNode(children)) {
        children = [children];
      }
      return createVNode(type, propsOrChildren, children);
    }
  } finally {
    setBlockTracking(1);
  }
}
const version = "3.5.25";
/**
* @vue/runtime-dom v3.5.25
* (c) 2018-present Yuxi (Evan) You and Vue contributors
* @license MIT
**/
let policy = void 0;
const tt = typeof window !== "undefined" && window.trustedTypes;
if (tt) {
  try {
    policy = /* @__PURE__ */ tt.createPolicy("vue", {
      createHTML: (val) => val
    });
  } catch (e) {
  }
}
const unsafeToTrustedHTML = policy ? (val) => policy.createHTML(val) : (val) => val;
const svgNS = "http://www.w3.org/2000/svg";
const mathmlNS = "http://www.w3.org/1998/Math/MathML";
const doc = typeof document !== "undefined" ? document : null;
const templateContainer = doc && /* @__PURE__ */ doc.createElement("template");
const nodeOps = {
  insert: (child, parent2, anchor) => {
    parent2.insertBefore(child, anchor || null);
  },
  remove: (child) => {
    const parent2 = child.parentNode;
    if (parent2) {
      parent2.removeChild(child);
    }
  },
  createElement: (tag, namespace, is2, props) => {
    const el2 = namespace === "svg" ? doc.createElementNS(svgNS, tag) : namespace === "mathml" ? doc.createElementNS(mathmlNS, tag) : is2 ? doc.createElement(tag, { is: is2 }) : doc.createElement(tag);
    if (tag === "select" && props && props.multiple != null) {
      el2.setAttribute("multiple", props.multiple);
    }
    return el2;
  },
  createText: (text) => doc.createTextNode(text),
  createComment: (text) => doc.createComment(text),
  setText: (node, text) => {
    node.nodeValue = text;
  },
  setElementText: (el2, text) => {
    el2.textContent = text;
  },
  parentNode: (node) => node.parentNode,
  nextSibling: (node) => node.nextSibling,
  querySelector: (selector) => doc.querySelector(selector),
  setScopeId(el2, id2) {
    el2.setAttribute(id2, "");
  },
  // __UNSAFE__
  // Reason: innerHTML.
  // Static content here can only come from compiled templates.
  // As long as the user only uses trusted templates, this is safe.
  insertStaticContent(content, parent2, anchor, namespace, start, end) {
    const before = anchor ? anchor.previousSibling : parent2.lastChild;
    if (start && (start === end || start.nextSibling)) {
      while (true) {
        parent2.insertBefore(start.cloneNode(true), anchor);
        if (start === end || !(start = start.nextSibling)) break;
      }
    } else {
      templateContainer.innerHTML = unsafeToTrustedHTML(
        namespace === "svg" ? `<svg>${content}</svg>` : namespace === "mathml" ? `<math>${content}</math>` : content
      );
      const template = templateContainer.content;
      if (namespace === "svg" || namespace === "mathml") {
        const wrapper = template.firstChild;
        while (wrapper.firstChild) {
          template.appendChild(wrapper.firstChild);
        }
        template.removeChild(wrapper);
      }
      parent2.insertBefore(template, anchor);
    }
    return [
      // first
      before ? before.nextSibling : parent2.firstChild,
      // last
      anchor ? anchor.previousSibling : parent2.lastChild
    ];
  }
};
const TRANSITION = "transition";
const ANIMATION = "animation";
const vtcKey = Symbol("_vtc");
const DOMTransitionPropsValidators = {
  name: String,
  type: String,
  css: {
    type: Boolean,
    default: true
  },
  duration: [String, Number, Object],
  enterFromClass: String,
  enterActiveClass: String,
  enterToClass: String,
  appearFromClass: String,
  appearActiveClass: String,
  appearToClass: String,
  leaveFromClass: String,
  leaveActiveClass: String,
  leaveToClass: String
};
const TransitionPropsValidators = /* @__PURE__ */ extend(
  {},
  BaseTransitionPropsValidators,
  DOMTransitionPropsValidators
);
const decorate$1 = (t) => {
  t.displayName = "Transition";
  t.props = TransitionPropsValidators;
  return t;
};
const Transition = /* @__PURE__ */ decorate$1(
  (props, { slots }) => h(BaseTransition, resolveTransitionProps(props), slots)
);
const callHook = (hook, args = []) => {
  if (isArray(hook)) {
    hook.forEach((h2) => h2(...args));
  } else if (hook) {
    hook(...args);
  }
};
const hasExplicitCallback = (hook) => {
  return hook ? isArray(hook) ? hook.some((h2) => h2.length > 1) : hook.length > 1 : false;
};
function resolveTransitionProps(rawProps) {
  const baseProps = {};
  for (const key in rawProps) {
    if (!(key in DOMTransitionPropsValidators)) {
      baseProps[key] = rawProps[key];
    }
  }
  if (rawProps.css === false) {
    return baseProps;
  }
  const {
    name: name2 = "v",
    type,
    duration,
    enterFromClass = `${name2}-enter-from`,
    enterActiveClass = `${name2}-enter-active`,
    enterToClass = `${name2}-enter-to`,
    appearFromClass = enterFromClass,
    appearActiveClass = enterActiveClass,
    appearToClass = enterToClass,
    leaveFromClass = `${name2}-leave-from`,
    leaveActiveClass = `${name2}-leave-active`,
    leaveToClass = `${name2}-leave-to`
  } = rawProps;
  const durations = normalizeDuration(duration);
  const enterDuration = durations && durations[0];
  const leaveDuration = durations && durations[1];
  const {
    onBeforeEnter,
    onEnter,
    onEnterCancelled,
    onLeave,
    onLeaveCancelled,
    onBeforeAppear = onBeforeEnter,
    onAppear = onEnter,
    onAppearCancelled = onEnterCancelled
  } = baseProps;
  const finishEnter = (el2, isAppear, done, isCancelled) => {
    el2._enterCancelled = isCancelled;
    removeTransitionClass(el2, isAppear ? appearToClass : enterToClass);
    removeTransitionClass(el2, isAppear ? appearActiveClass : enterActiveClass);
    done && done();
  };
  const finishLeave = (el2, done) => {
    el2._isLeaving = false;
    removeTransitionClass(el2, leaveFromClass);
    removeTransitionClass(el2, leaveToClass);
    removeTransitionClass(el2, leaveActiveClass);
    done && done();
  };
  const makeEnterHook = (isAppear) => {
    return (el2, done) => {
      const hook = isAppear ? onAppear : onEnter;
      const resolve2 = () => finishEnter(el2, isAppear, done);
      callHook(hook, [el2, resolve2]);
      nextFrame(() => {
        removeTransitionClass(el2, isAppear ? appearFromClass : enterFromClass);
        addTransitionClass(el2, isAppear ? appearToClass : enterToClass);
        if (!hasExplicitCallback(hook)) {
          whenTransitionEnds(el2, type, enterDuration, resolve2);
        }
      });
    };
  };
  return extend(baseProps, {
    onBeforeEnter(el2) {
      callHook(onBeforeEnter, [el2]);
      addTransitionClass(el2, enterFromClass);
      addTransitionClass(el2, enterActiveClass);
    },
    onBeforeAppear(el2) {
      callHook(onBeforeAppear, [el2]);
      addTransitionClass(el2, appearFromClass);
      addTransitionClass(el2, appearActiveClass);
    },
    onEnter: makeEnterHook(false),
    onAppear: makeEnterHook(true),
    onLeave(el2, done) {
      el2._isLeaving = true;
      const resolve2 = () => finishLeave(el2, done);
      addTransitionClass(el2, leaveFromClass);
      if (!el2._enterCancelled) {
        forceReflow(el2);
        addTransitionClass(el2, leaveActiveClass);
      } else {
        addTransitionClass(el2, leaveActiveClass);
        forceReflow(el2);
      }
      nextFrame(() => {
        if (!el2._isLeaving) {
          return;
        }
        removeTransitionClass(el2, leaveFromClass);
        addTransitionClass(el2, leaveToClass);
        if (!hasExplicitCallback(onLeave)) {
          whenTransitionEnds(el2, type, leaveDuration, resolve2);
        }
      });
      callHook(onLeave, [el2, resolve2]);
    },
    onEnterCancelled(el2) {
      finishEnter(el2, false, void 0, true);
      callHook(onEnterCancelled, [el2]);
    },
    onAppearCancelled(el2) {
      finishEnter(el2, true, void 0, true);
      callHook(onAppearCancelled, [el2]);
    },
    onLeaveCancelled(el2) {
      finishLeave(el2);
      callHook(onLeaveCancelled, [el2]);
    }
  });
}
function normalizeDuration(duration) {
  if (duration == null) {
    return null;
  } else if (isObject$4(duration)) {
    return [NumberOf(duration.enter), NumberOf(duration.leave)];
  } else {
    const n = NumberOf(duration);
    return [n, n];
  }
}
function NumberOf(val) {
  const res = toNumber(val);
  return res;
}
function addTransitionClass(el2, cls) {
  cls.split(/\s+/).forEach((c) => c && el2.classList.add(c));
  (el2[vtcKey] || (el2[vtcKey] = /* @__PURE__ */ new Set())).add(cls);
}
function removeTransitionClass(el2, cls) {
  cls.split(/\s+/).forEach((c) => c && el2.classList.remove(c));
  const _vtc = el2[vtcKey];
  if (_vtc) {
    _vtc.delete(cls);
    if (!_vtc.size) {
      el2[vtcKey] = void 0;
    }
  }
}
function nextFrame(cb) {
  requestAnimationFrame(() => {
    requestAnimationFrame(cb);
  });
}
let endId = 0;
function whenTransitionEnds(el2, expectedType, explicitTimeout, resolve2) {
  const id2 = el2._endId = ++endId;
  const resolveIfNotStale = () => {
    if (id2 === el2._endId) {
      resolve2();
    }
  };
  if (explicitTimeout != null) {
    return setTimeout(resolveIfNotStale, explicitTimeout);
  }
  const { type, timeout: timeout2, propCount } = getTransitionInfo(el2, expectedType);
  if (!type) {
    return resolve2();
  }
  const endEvent = type + "end";
  let ended = 0;
  const end = () => {
    el2.removeEventListener(endEvent, onEnd);
    resolveIfNotStale();
  };
  const onEnd = (e) => {
    if (e.target === el2 && ++ended >= propCount) {
      end();
    }
  };
  setTimeout(() => {
    if (ended < propCount) {
      end();
    }
  }, timeout2 + 1);
  el2.addEventListener(endEvent, onEnd);
}
function getTransitionInfo(el2, expectedType) {
  const styles = window.getComputedStyle(el2);
  const getStyleProperties = (key) => (styles[key] || "").split(", ");
  const transitionDelays = getStyleProperties(`${TRANSITION}Delay`);
  const transitionDurations = getStyleProperties(`${TRANSITION}Duration`);
  const transitionTimeout = getTimeout(transitionDelays, transitionDurations);
  const animationDelays = getStyleProperties(`${ANIMATION}Delay`);
  const animationDurations = getStyleProperties(`${ANIMATION}Duration`);
  const animationTimeout = getTimeout(animationDelays, animationDurations);
  let type = null;
  let timeout2 = 0;
  let propCount = 0;
  if (expectedType === TRANSITION) {
    if (transitionTimeout > 0) {
      type = TRANSITION;
      timeout2 = transitionTimeout;
      propCount = transitionDurations.length;
    }
  } else if (expectedType === ANIMATION) {
    if (animationTimeout > 0) {
      type = ANIMATION;
      timeout2 = animationTimeout;
      propCount = animationDurations.length;
    }
  } else {
    timeout2 = Math.max(transitionTimeout, animationTimeout);
    type = timeout2 > 0 ? transitionTimeout > animationTimeout ? TRANSITION : ANIMATION : null;
    propCount = type ? type === TRANSITION ? transitionDurations.length : animationDurations.length : 0;
  }
  const hasTransform = type === TRANSITION && /\b(?:transform|all)(?:,|$)/.test(
    getStyleProperties(`${TRANSITION}Property`).toString()
  );
  return {
    type,
    timeout: timeout2,
    propCount,
    hasTransform
  };
}
function getTimeout(delays, durations) {
  while (delays.length < durations.length) {
    delays = delays.concat(delays);
  }
  return Math.max(...durations.map((d, i) => toMs(d) + toMs(delays[i])));
}
function toMs(s) {
  if (s === "auto") return 0;
  return Number(s.slice(0, -1).replace(",", ".")) * 1e3;
}
function forceReflow(el2) {
  const targetDocument = el2 ? el2.ownerDocument : document;
  return targetDocument.body.offsetHeight;
}
function patchClass(el2, value, isSVG) {
  const transitionClasses = el2[vtcKey];
  if (transitionClasses) {
    value = (value ? [value, ...transitionClasses] : [...transitionClasses]).join(" ");
  }
  if (value == null) {
    el2.removeAttribute("class");
  } else if (isSVG) {
    el2.setAttribute("class", value);
  } else {
    el2.className = value;
  }
}
const vShowOriginalDisplay = Symbol("_vod");
const vShowHidden = Symbol("_vsh");
const vShow = {
  // used for prop mismatch check during hydration
  name: "show",
  beforeMount(el2, { value }, { transition }) {
    el2[vShowOriginalDisplay] = el2.style.display === "none" ? "" : el2.style.display;
    if (transition && value) {
      transition.beforeEnter(el2);
    } else {
      setDisplay(el2, value);
    }
  },
  mounted(el2, { value }, { transition }) {
    if (transition && value) {
      transition.enter(el2);
    }
  },
  updated(el2, { value, oldValue }, { transition }) {
    if (!value === !oldValue) return;
    if (transition) {
      if (value) {
        transition.beforeEnter(el2);
        setDisplay(el2, true);
        transition.enter(el2);
      } else {
        transition.leave(el2, () => {
          setDisplay(el2, false);
        });
      }
    } else {
      setDisplay(el2, value);
    }
  },
  beforeUnmount(el2, { value }) {
    setDisplay(el2, value);
  }
};
function setDisplay(el2, value) {
  el2.style.display = value ? el2[vShowOriginalDisplay] : "none";
  el2[vShowHidden] = !value;
}
const CSS_VAR_TEXT = Symbol("");
const displayRE = /(?:^|;)\s*display\s*:/;
function patchStyle(el2, prev, next) {
  const style = el2.style;
  const isCssString = isString(next);
  let hasControlledDisplay = false;
  if (next && !isCssString) {
    if (prev) {
      if (!isString(prev)) {
        for (const key in prev) {
          if (next[key] == null) {
            setStyle(style, key, "");
          }
        }
      } else {
        for (const prevStyle of prev.split(";")) {
          const key = prevStyle.slice(0, prevStyle.indexOf(":")).trim();
          if (next[key] == null) {
            setStyle(style, key, "");
          }
        }
      }
    }
    for (const key in next) {
      if (key === "display") {
        hasControlledDisplay = true;
      }
      setStyle(style, key, next[key]);
    }
  } else {
    if (isCssString) {
      if (prev !== next) {
        const cssVarText = style[CSS_VAR_TEXT];
        if (cssVarText) {
          next += ";" + cssVarText;
        }
        style.cssText = next;
        hasControlledDisplay = displayRE.test(next);
      }
    } else if (prev) {
      el2.removeAttribute("style");
    }
  }
  if (vShowOriginalDisplay in el2) {
    el2[vShowOriginalDisplay] = hasControlledDisplay ? style.display : "";
    if (el2[vShowHidden]) {
      style.display = "none";
    }
  }
}
const importantRE = /\s*!important$/;
function setStyle(style, name2, val) {
  if (isArray(val)) {
    val.forEach((v) => setStyle(style, name2, v));
  } else {
    if (val == null) val = "";
    if (name2.startsWith("--")) {
      style.setProperty(name2, val);
    } else {
      const prefixed = autoPrefix(style, name2);
      if (importantRE.test(val)) {
        style.setProperty(
          hyphenate(prefixed),
          val.replace(importantRE, ""),
          "important"
        );
      } else {
        style[prefixed] = val;
      }
    }
  }
}
const prefixes = ["Webkit", "Moz", "ms"];
const prefixCache = {};
function autoPrefix(style, rawName) {
  const cached = prefixCache[rawName];
  if (cached) {
    return cached;
  }
  let name2 = camelize(rawName);
  if (name2 !== "filter" && name2 in style) {
    return prefixCache[rawName] = name2;
  }
  name2 = capitalize(name2);
  for (let i = 0; i < prefixes.length; i++) {
    const prefixed = prefixes[i] + name2;
    if (prefixed in style) {
      return prefixCache[rawName] = prefixed;
    }
  }
  return rawName;
}
const xlinkNS = "http://www.w3.org/1999/xlink";
function patchAttr(el2, key, value, isSVG, instance, isBoolean = isSpecialBooleanAttr(key)) {
  if (isSVG && key.startsWith("xlink:")) {
    if (value == null) {
      el2.removeAttributeNS(xlinkNS, key.slice(6, key.length));
    } else {
      el2.setAttributeNS(xlinkNS, key, value);
    }
  } else {
    if (value == null || isBoolean && !includeBooleanAttr(value)) {
      el2.removeAttribute(key);
    } else {
      el2.setAttribute(
        key,
        isBoolean ? "" : isSymbol(value) ? String(value) : value
      );
    }
  }
}
function patchDOMProp(el2, key, value, parentComponent, attrName) {
  if (key === "innerHTML" || key === "textContent") {
    if (value != null) {
      el2[key] = key === "innerHTML" ? unsafeToTrustedHTML(value) : value;
    }
    return;
  }
  const tag = el2.tagName;
  if (key === "value" && tag !== "PROGRESS" && // custom elements may use _value internally
  !tag.includes("-")) {
    const oldValue = tag === "OPTION" ? el2.getAttribute("value") || "" : el2.value;
    const newValue = value == null ? (
      // #11647: value should be set as empty string for null and undefined,
      // but <input type="checkbox"> should be set as 'on'.
      el2.type === "checkbox" ? "on" : ""
    ) : String(value);
    if (oldValue !== newValue || !("_value" in el2)) {
      el2.value = newValue;
    }
    if (value == null) {
      el2.removeAttribute(key);
    }
    el2._value = value;
    return;
  }
  let needRemove = false;
  if (value === "" || value == null) {
    const type = typeof el2[key];
    if (type === "boolean") {
      value = includeBooleanAttr(value);
    } else if (value == null && type === "string") {
      value = "";
      needRemove = true;
    } else if (type === "number") {
      value = 0;
      needRemove = true;
    }
  }
  try {
    el2[key] = value;
  } catch (e) {
  }
  needRemove && el2.removeAttribute(attrName || key);
}
function addEventListener(el2, event, handler, options) {
  el2.addEventListener(event, handler, options);
}
function removeEventListener(el2, event, handler, options) {
  el2.removeEventListener(event, handler, options);
}
const veiKey = Symbol("_vei");
function patchEvent(el2, rawName, prevValue, nextValue, instance = null) {
  const invokers = el2[veiKey] || (el2[veiKey] = {});
  const existingInvoker = invokers[rawName];
  if (nextValue && existingInvoker) {
    existingInvoker.value = nextValue;
  } else {
    const [name2, options] = parseName(rawName);
    if (nextValue) {
      const invoker = invokers[rawName] = createInvoker(
        nextValue,
        instance
      );
      addEventListener(el2, name2, invoker, options);
    } else if (existingInvoker) {
      removeEventListener(el2, name2, existingInvoker, options);
      invokers[rawName] = void 0;
    }
  }
}
const optionsModifierRE = /(?:Once|Passive|Capture)$/;
function parseName(name2) {
  let options;
  if (optionsModifierRE.test(name2)) {
    options = {};
    let m;
    while (m = name2.match(optionsModifierRE)) {
      name2 = name2.slice(0, name2.length - m[0].length);
      options[m[0].toLowerCase()] = true;
    }
  }
  const event = name2[2] === ":" ? name2.slice(3) : hyphenate(name2.slice(2));
  return [event, options];
}
let cachedNow = 0;
const p = /* @__PURE__ */ Promise.resolve();
const getNow = () => cachedNow || (p.then(() => cachedNow = 0), cachedNow = Date.now());
function createInvoker(initialValue, instance) {
  const invoker = (e) => {
    if (!e._vts) {
      e._vts = Date.now();
    } else if (e._vts <= invoker.attached) {
      return;
    }
    callWithAsyncErrorHandling(
      patchStopImmediatePropagation(e, invoker.value),
      instance,
      5,
      [e]
    );
  };
  invoker.value = initialValue;
  invoker.attached = getNow();
  return invoker;
}
function patchStopImmediatePropagation(e, value) {
  if (isArray(value)) {
    const originalStop = e.stopImmediatePropagation;
    e.stopImmediatePropagation = () => {
      originalStop.call(e);
      e._stopped = true;
    };
    return value.map(
      (fn) => (e2) => !e2._stopped && fn && fn(e2)
    );
  } else {
    return value;
  }
}
const isNativeOn = (key) => key.charCodeAt(0) === 111 && key.charCodeAt(1) === 110 && // lowercase letter
key.charCodeAt(2) > 96 && key.charCodeAt(2) < 123;
const patchProp = (el2, key, prevValue, nextValue, namespace, parentComponent) => {
  const isSVG = namespace === "svg";
  if (key === "class") {
    patchClass(el2, nextValue, isSVG);
  } else if (key === "style") {
    patchStyle(el2, prevValue, nextValue);
  } else if (isOn(key)) {
    if (!isModelListener(key)) {
      patchEvent(el2, key, prevValue, nextValue, parentComponent);
    }
  } else if (key[0] === "." ? (key = key.slice(1), true) : key[0] === "^" ? (key = key.slice(1), false) : shouldSetAsProp(el2, key, nextValue, isSVG)) {
    patchDOMProp(el2, key, nextValue);
    if (!el2.tagName.includes("-") && (key === "value" || key === "checked" || key === "selected")) {
      patchAttr(el2, key, nextValue, isSVG, parentComponent, key !== "value");
    }
  } else if (
    // #11081 force set props for possible async custom element
    el2._isVueCE && (/[A-Z]/.test(key) || !isString(nextValue))
  ) {
    patchDOMProp(el2, camelize(key), nextValue, parentComponent, key);
  } else {
    if (key === "true-value") {
      el2._trueValue = nextValue;
    } else if (key === "false-value") {
      el2._falseValue = nextValue;
    }
    patchAttr(el2, key, nextValue, isSVG);
  }
};
function shouldSetAsProp(el2, key, value, isSVG) {
  if (isSVG) {
    if (key === "innerHTML" || key === "textContent") {
      return true;
    }
    if (key in el2 && isNativeOn(key) && isFunction(value)) {
      return true;
    }
    return false;
  }
  if (key === "spellcheck" || key === "draggable" || key === "translate" || key === "autocorrect") {
    return false;
  }
  if (key === "sandbox" && el2.tagName === "IFRAME") {
    return false;
  }
  if (key === "form") {
    return false;
  }
  if (key === "list" && el2.tagName === "INPUT") {
    return false;
  }
  if (key === "type" && el2.tagName === "TEXTAREA") {
    return false;
  }
  if (key === "width" || key === "height") {
    const tag = el2.tagName;
    if (tag === "IMG" || tag === "VIDEO" || tag === "CANVAS" || tag === "SOURCE") {
      return false;
    }
  }
  if (isNativeOn(key) && isString(value)) {
    return false;
  }
  return key in el2;
}
const positionMap = /* @__PURE__ */ new WeakMap();
const newPositionMap = /* @__PURE__ */ new WeakMap();
const moveCbKey = Symbol("_moveCb");
const enterCbKey = Symbol("_enterCb");
const decorate = (t) => {
  delete t.props.mode;
  return t;
};
const TransitionGroupImpl = /* @__PURE__ */ decorate({
  name: "TransitionGroup",
  props: /* @__PURE__ */ extend({}, TransitionPropsValidators, {
    tag: String,
    moveClass: String
  }),
  setup(props, { slots }) {
    const instance = getCurrentInstance();
    const state = useTransitionState();
    let prevChildren;
    let children;
    onUpdated(() => {
      if (!prevChildren.length) {
        return;
      }
      const moveClass = props.moveClass || `${props.name || "v"}-move`;
      if (!hasCSSTransform(
        prevChildren[0].el,
        instance.vnode.el,
        moveClass
      )) {
        prevChildren = [];
        return;
      }
      prevChildren.forEach(callPendingCbs);
      prevChildren.forEach(recordPosition);
      const movedChildren = prevChildren.filter(applyTranslation);
      forceReflow(instance.vnode.el);
      movedChildren.forEach((c) => {
        const el2 = c.el;
        const style = el2.style;
        addTransitionClass(el2, moveClass);
        style.transform = style.webkitTransform = style.transitionDuration = "";
        const cb = el2[moveCbKey] = (e) => {
          if (e && e.target !== el2) {
            return;
          }
          if (!e || e.propertyName.endsWith("transform")) {
            el2.removeEventListener("transitionend", cb);
            el2[moveCbKey] = null;
            removeTransitionClass(el2, moveClass);
          }
        };
        el2.addEventListener("transitionend", cb);
      });
      prevChildren = [];
    });
    return () => {
      const rawProps = toRaw(props);
      const cssTransitionProps = resolveTransitionProps(rawProps);
      let tag = rawProps.tag || Fragment;
      prevChildren = [];
      if (children) {
        for (let i = 0; i < children.length; i++) {
          const child = children[i];
          if (child.el && child.el instanceof Element) {
            prevChildren.push(child);
            setTransitionHooks(
              child,
              resolveTransitionHooks(
                child,
                cssTransitionProps,
                state,
                instance
              )
            );
            positionMap.set(child, {
              left: child.el.offsetLeft,
              top: child.el.offsetTop
            });
          }
        }
      }
      children = slots.default ? getTransitionRawChildren(slots.default()) : [];
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        if (child.key != null) {
          setTransitionHooks(
            child,
            resolveTransitionHooks(child, cssTransitionProps, state, instance)
          );
        }
      }
      return createVNode(tag, null, children);
    };
  }
});
const TransitionGroup = TransitionGroupImpl;
function callPendingCbs(c) {
  const el2 = c.el;
  if (el2[moveCbKey]) {
    el2[moveCbKey]();
  }
  if (el2[enterCbKey]) {
    el2[enterCbKey]();
  }
}
function recordPosition(c) {
  newPositionMap.set(c, {
    left: c.el.offsetLeft,
    top: c.el.offsetTop
  });
}
function applyTranslation(c) {
  const oldPos = positionMap.get(c);
  const newPos = newPositionMap.get(c);
  const dx = oldPos.left - newPos.left;
  const dy = oldPos.top - newPos.top;
  if (dx || dy) {
    const s = c.el.style;
    s.transform = s.webkitTransform = `translate(${dx}px,${dy}px)`;
    s.transitionDuration = "0s";
    return c;
  }
}
function hasCSSTransform(el2, root, moveClass) {
  const clone = el2.cloneNode();
  const _vtc = el2[vtcKey];
  if (_vtc) {
    _vtc.forEach((cls) => {
      cls.split(/\s+/).forEach((c) => c && clone.classList.remove(c));
    });
  }
  moveClass.split(/\s+/).forEach((c) => c && clone.classList.add(c));
  clone.style.display = "none";
  const container = root.nodeType === 1 ? root : root.parentNode;
  container.appendChild(clone);
  const { hasTransform } = getTransitionInfo(clone);
  container.removeChild(clone);
  return hasTransform;
}
const getModelAssigner = (vnode) => {
  const fn = vnode.props["onUpdate:modelValue"] || false;
  return isArray(fn) ? (value) => invokeArrayFns(fn, value) : fn;
};
function onCompositionStart(e) {
  e.target.composing = true;
}
function onCompositionEnd(e) {
  const target = e.target;
  if (target.composing) {
    target.composing = false;
    target.dispatchEvent(new Event("input"));
  }
}
const assignKey = Symbol("_assign");
function castValue(value, trim, number) {
  if (trim) value = value.trim();
  if (number) value = looseToNumber(value);
  return value;
}
const vModelText = {
  created(el2, { modifiers: { lazy, trim, number } }, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    const castToNumber = number || vnode.props && vnode.props.type === "number";
    addEventListener(el2, lazy ? "change" : "input", (e) => {
      if (e.target.composing) return;
      el2[assignKey](castValue(el2.value, trim, castToNumber));
    });
    if (trim || castToNumber) {
      addEventListener(el2, "change", () => {
        el2.value = castValue(el2.value, trim, castToNumber);
      });
    }
    if (!lazy) {
      addEventListener(el2, "compositionstart", onCompositionStart);
      addEventListener(el2, "compositionend", onCompositionEnd);
      addEventListener(el2, "change", onCompositionEnd);
    }
  },
  // set value on mounted so it's after min/max for type="range"
  mounted(el2, { value }) {
    el2.value = value == null ? "" : value;
  },
  beforeUpdate(el2, { value, oldValue, modifiers: { lazy, trim, number } }, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    if (el2.composing) return;
    const elValue = (number || el2.type === "number") && !/^0\d/.test(el2.value) ? looseToNumber(el2.value) : el2.value;
    const newValue = value == null ? "" : value;
    if (elValue === newValue) {
      return;
    }
    if (document.activeElement === el2 && el2.type !== "range") {
      if (lazy && value === oldValue) {
        return;
      }
      if (trim && el2.value.trim() === newValue) {
        return;
      }
    }
    el2.value = newValue;
  }
};
const vModelRadio = {
  created(el2, { value }, vnode) {
    el2.checked = looseEqual(value, vnode.props.value);
    el2[assignKey] = getModelAssigner(vnode);
    addEventListener(el2, "change", () => {
      el2[assignKey](getValue(el2));
    });
  },
  beforeUpdate(el2, { value, oldValue }, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    if (value !== oldValue) {
      el2.checked = looseEqual(value, vnode.props.value);
    }
  }
};
const vModelSelect = {
  // <select multiple> value need to be deep traversed
  deep: true,
  created(el2, { value, modifiers: { number } }, vnode) {
    const isSetModel = isSet(value);
    addEventListener(el2, "change", () => {
      const selectedVal = Array.prototype.filter.call(el2.options, (o) => o.selected).map(
        (o) => number ? looseToNumber(getValue(o)) : getValue(o)
      );
      el2[assignKey](
        el2.multiple ? isSetModel ? new Set(selectedVal) : selectedVal : selectedVal[0]
      );
      el2._assigning = true;
      nextTick(() => {
        el2._assigning = false;
      });
    });
    el2[assignKey] = getModelAssigner(vnode);
  },
  // set value in mounted & updated because <select> relies on its children
  // <option>s.
  mounted(el2, { value }) {
    setSelected(el2, value);
  },
  beforeUpdate(el2, _binding, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
  },
  updated(el2, { value }) {
    if (!el2._assigning) {
      setSelected(el2, value);
    }
  }
};
function setSelected(el2, value) {
  const isMultiple = el2.multiple;
  const isArrayValue = isArray(value);
  if (isMultiple && !isArrayValue && !isSet(value)) {
    return;
  }
  for (let i = 0, l = el2.options.length; i < l; i++) {
    const option = el2.options[i];
    const optionValue = getValue(option);
    if (isMultiple) {
      if (isArrayValue) {
        const optionType = typeof optionValue;
        if (optionType === "string" || optionType === "number") {
          option.selected = value.some((v) => String(v) === String(optionValue));
        } else {
          option.selected = looseIndexOf(value, optionValue) > -1;
        }
      } else {
        option.selected = value.has(optionValue);
      }
    } else if (looseEqual(getValue(option), value)) {
      if (el2.selectedIndex !== i) el2.selectedIndex = i;
      return;
    }
  }
  if (!isMultiple && el2.selectedIndex !== -1) {
    el2.selectedIndex = -1;
  }
}
function getValue(el2) {
  return "_value" in el2 ? el2._value : el2.value;
}
const systemModifiers = ["ctrl", "shift", "alt", "meta"];
const modifierGuards = {
  stop: (e) => e.stopPropagation(),
  prevent: (e) => e.preventDefault(),
  self: (e) => e.target !== e.currentTarget,
  ctrl: (e) => !e.ctrlKey,
  shift: (e) => !e.shiftKey,
  alt: (e) => !e.altKey,
  meta: (e) => !e.metaKey,
  left: (e) => "button" in e && e.button !== 0,
  middle: (e) => "button" in e && e.button !== 1,
  right: (e) => "button" in e && e.button !== 2,
  exact: (e, modifiers) => systemModifiers.some((m) => e[`${m}Key`] && !modifiers.includes(m))
};
const withModifiers = (fn, modifiers) => {
  const cache = fn._withMods || (fn._withMods = {});
  const cacheKey = modifiers.join(".");
  return cache[cacheKey] || (cache[cacheKey] = (event, ...args) => {
    for (let i = 0; i < modifiers.length; i++) {
      const guard = modifierGuards[modifiers[i]];
      if (guard && guard(event, modifiers)) return;
    }
    return fn(event, ...args);
  });
};
const keyNames = {
  esc: "escape",
  space: " ",
  up: "arrow-up",
  left: "arrow-left",
  right: "arrow-right",
  down: "arrow-down",
  delete: "backspace"
};
const withKeys = (fn, modifiers) => {
  const cache = fn._withKeys || (fn._withKeys = {});
  const cacheKey = modifiers.join(".");
  return cache[cacheKey] || (cache[cacheKey] = (event) => {
    if (!("key" in event)) {
      return;
    }
    const eventKey = hyphenate(event.key);
    if (modifiers.some(
      (k) => k === eventKey || keyNames[k] === eventKey
    )) {
      return fn(event);
    }
  });
};
const rendererOptions = /* @__PURE__ */ extend({ patchProp }, nodeOps);
let renderer;
function ensureRenderer() {
  return renderer || (renderer = createRenderer(rendererOptions));
}
const render = (...args) => {
  ensureRenderer().render(...args);
};
const createApp = (...args) => {
  const app = ensureRenderer().createApp(...args);
  const { mount: mount2 } = app;
  app.mount = (containerOrSelector) => {
    const container = normalizeContainer(containerOrSelector);
    if (!container) return;
    const component = app._component;
    if (!isFunction(component) && !component.render && !component.template) {
      component.template = container.innerHTML;
    }
    if (container.nodeType === 1) {
      container.textContent = "";
    }
    const proxy = mount2(container, false, resolveRootNamespace(container));
    if (container instanceof Element) {
      container.removeAttribute("v-cloak");
      container.setAttribute("data-v-app", "");
    }
    return proxy;
  };
  return app;
};
function resolveRootNamespace(container) {
  if (container instanceof SVGElement) {
    return "svg";
  }
  if (typeof MathMLElement === "function" && container instanceof MathMLElement) {
    return "mathml";
  }
}
function normalizeContainer(container) {
  if (isString(container)) {
    const res = document.querySelector(container);
    return res;
  }
  return container;
}
/*!
 * pinia v2.3.1
 * (c) 2025 Eduardo San Martin Morote
 * @license MIT
 */
let activePinia;
const setActivePinia = (pinia) => activePinia = pinia;
const piniaSymbol = (
  /* istanbul ignore next */
  Symbol()
);
function isPlainObject(o) {
  return o && typeof o === "object" && Object.prototype.toString.call(o) === "[object Object]" && typeof o.toJSON !== "function";
}
var MutationType;
(function(MutationType2) {
  MutationType2["direct"] = "direct";
  MutationType2["patchObject"] = "patch object";
  MutationType2["patchFunction"] = "patch function";
})(MutationType || (MutationType = {}));
function createPinia() {
  const scope = effectScope(true);
  const state = scope.run(() => ref({}));
  let _p2 = [];
  let toBeInstalled = [];
  const pinia = markRaw({
    install(app) {
      setActivePinia(pinia);
      {
        pinia._a = app;
        app.provide(piniaSymbol, pinia);
        app.config.globalProperties.$pinia = pinia;
        toBeInstalled.forEach((plugin) => _p2.push(plugin));
        toBeInstalled = [];
      }
    },
    use(plugin) {
      if (!this._a && true) {
        toBeInstalled.push(plugin);
      } else {
        _p2.push(plugin);
      }
      return this;
    },
    _p: _p2,
    // it's actually undefined here
    // @ts-expect-error
    _a: null,
    _e: scope,
    _s: /* @__PURE__ */ new Map(),
    state
  });
  return pinia;
}
const noop$3 = () => {
};
function addSubscription(subscriptions, callback, detached, onCleanup = noop$3) {
  subscriptions.push(callback);
  const removeSubscription = () => {
    const idx = subscriptions.indexOf(callback);
    if (idx > -1) {
      subscriptions.splice(idx, 1);
      onCleanup();
    }
  };
  if (!detached && getCurrentScope()) {
    onScopeDispose(removeSubscription);
  }
  return removeSubscription;
}
function triggerSubscriptions(subscriptions, ...args) {
  subscriptions.slice().forEach((callback) => {
    callback(...args);
  });
}
const fallbackRunWithContext = (fn) => fn();
const ACTION_MARKER = Symbol();
const ACTION_NAME = Symbol();
function mergeReactiveObjects(target, patchToApply) {
  if (target instanceof Map && patchToApply instanceof Map) {
    patchToApply.forEach((value, key) => target.set(key, value));
  } else if (target instanceof Set && patchToApply instanceof Set) {
    patchToApply.forEach(target.add, target);
  }
  for (const key in patchToApply) {
    if (!patchToApply.hasOwnProperty(key))
      continue;
    const subPatch = patchToApply[key];
    const targetValue = target[key];
    if (isPlainObject(targetValue) && isPlainObject(subPatch) && target.hasOwnProperty(key) && !isRef(subPatch) && !isReactive(subPatch)) {
      target[key] = mergeReactiveObjects(targetValue, subPatch);
    } else {
      target[key] = subPatch;
    }
  }
  return target;
}
const skipHydrateSymbol = (
  /* istanbul ignore next */
  Symbol()
);
function shouldHydrate(obj) {
  return !isPlainObject(obj) || !obj.hasOwnProperty(skipHydrateSymbol);
}
const { assign } = Object;
function isComputed(o) {
  return !!(isRef(o) && o.effect);
}
function createOptionsStore(id2, options, pinia, hot) {
  const { state, actions, getters } = options;
  const initialState = pinia.state.value[id2];
  let store;
  function setup() {
    if (!initialState && true) {
      {
        pinia.state.value[id2] = state ? state() : {};
      }
    }
    const localState = toRefs(pinia.state.value[id2]);
    return assign(localState, actions, Object.keys(getters || {}).reduce((computedGetters, name2) => {
      computedGetters[name2] = markRaw(computed(() => {
        setActivePinia(pinia);
        const store2 = pinia._s.get(id2);
        return getters[name2].call(store2, store2);
      }));
      return computedGetters;
    }, {}));
  }
  store = createSetupStore(id2, setup, options, pinia, hot, true);
  return store;
}
function createSetupStore($id, setup, options = {}, pinia, hot, isOptionsStore) {
  let scope;
  const optionsForPlugin = assign({ actions: {} }, options);
  const $subscribeOptions = { deep: true };
  let isListening;
  let isSyncListening;
  let subscriptions = [];
  let actionSubscriptions = [];
  let debuggerEvents;
  const initialState = pinia.state.value[$id];
  if (!isOptionsStore && !initialState && true) {
    {
      pinia.state.value[$id] = {};
    }
  }
  ref({});
  let activeListener;
  function $patch(partialStateOrMutator) {
    let subscriptionMutation;
    isListening = isSyncListening = false;
    if (typeof partialStateOrMutator === "function") {
      partialStateOrMutator(pinia.state.value[$id]);
      subscriptionMutation = {
        type: MutationType.patchFunction,
        storeId: $id,
        events: debuggerEvents
      };
    } else {
      mergeReactiveObjects(pinia.state.value[$id], partialStateOrMutator);
      subscriptionMutation = {
        type: MutationType.patchObject,
        payload: partialStateOrMutator,
        storeId: $id,
        events: debuggerEvents
      };
    }
    const myListenerId = activeListener = Symbol();
    nextTick().then(() => {
      if (activeListener === myListenerId) {
        isListening = true;
      }
    });
    isSyncListening = true;
    triggerSubscriptions(subscriptions, subscriptionMutation, pinia.state.value[$id]);
  }
  const $reset = isOptionsStore ? function $reset2() {
    const { state } = options;
    const newState = state ? state() : {};
    this.$patch(($state) => {
      assign($state, newState);
    });
  } : (
    /* istanbul ignore next */
    noop$3
  );
  function $dispose() {
    scope.stop();
    subscriptions = [];
    actionSubscriptions = [];
    pinia._s.delete($id);
  }
  const action = (fn, name2 = "") => {
    if (ACTION_MARKER in fn) {
      fn[ACTION_NAME] = name2;
      return fn;
    }
    const wrappedAction = function() {
      setActivePinia(pinia);
      const args = Array.from(arguments);
      const afterCallbackList = [];
      const onErrorCallbackList = [];
      function after(callback) {
        afterCallbackList.push(callback);
      }
      function onError(callback) {
        onErrorCallbackList.push(callback);
      }
      triggerSubscriptions(actionSubscriptions, {
        args,
        name: wrappedAction[ACTION_NAME],
        store,
        after,
        onError
      });
      let ret;
      try {
        ret = fn.apply(this && this.$id === $id ? this : store, args);
      } catch (error) {
        triggerSubscriptions(onErrorCallbackList, error);
        throw error;
      }
      if (ret instanceof Promise) {
        return ret.then((value) => {
          triggerSubscriptions(afterCallbackList, value);
          return value;
        }).catch((error) => {
          triggerSubscriptions(onErrorCallbackList, error);
          return Promise.reject(error);
        });
      }
      triggerSubscriptions(afterCallbackList, ret);
      return ret;
    };
    wrappedAction[ACTION_MARKER] = true;
    wrappedAction[ACTION_NAME] = name2;
    return wrappedAction;
  };
  const partialStore = {
    _p: pinia,
    // _s: scope,
    $id,
    $onAction: addSubscription.bind(null, actionSubscriptions),
    $patch,
    $reset,
    $subscribe(callback, options2 = {}) {
      const removeSubscription = addSubscription(subscriptions, callback, options2.detached, () => stopWatcher());
      const stopWatcher = scope.run(() => watch(() => pinia.state.value[$id], (state) => {
        if (options2.flush === "sync" ? isSyncListening : isListening) {
          callback({
            storeId: $id,
            type: MutationType.direct,
            events: debuggerEvents
          }, state);
        }
      }, assign({}, $subscribeOptions, options2)));
      return removeSubscription;
    },
    $dispose
  };
  const store = reactive(partialStore);
  pinia._s.set($id, store);
  const runWithContext = pinia._a && pinia._a.runWithContext || fallbackRunWithContext;
  const setupStore = runWithContext(() => pinia._e.run(() => (scope = effectScope()).run(() => setup({ action }))));
  for (const key in setupStore) {
    const prop = setupStore[key];
    if (isRef(prop) && !isComputed(prop) || isReactive(prop)) {
      if (!isOptionsStore) {
        if (initialState && shouldHydrate(prop)) {
          if (isRef(prop)) {
            prop.value = initialState[key];
          } else {
            mergeReactiveObjects(prop, initialState[key]);
          }
        }
        {
          pinia.state.value[$id][key] = prop;
        }
      }
    } else if (typeof prop === "function") {
      const actionValue = action(prop, key);
      {
        setupStore[key] = actionValue;
      }
      optionsForPlugin.actions[key] = prop;
    } else ;
  }
  {
    assign(store, setupStore);
    assign(toRaw(store), setupStore);
  }
  Object.defineProperty(store, "$state", {
    get: () => pinia.state.value[$id],
    set: (state) => {
      $patch(($state) => {
        assign($state, state);
      });
    }
  });
  pinia._p.forEach((extender) => {
    {
      assign(store, scope.run(() => extender({
        store,
        app: pinia._a,
        pinia,
        options: optionsForPlugin
      })));
    }
  });
  if (initialState && isOptionsStore && options.hydrate) {
    options.hydrate(store.$state, initialState);
  }
  isListening = true;
  isSyncListening = true;
  return store;
}
/*! #__NO_SIDE_EFFECTS__ */
// @__NO_SIDE_EFFECTS__
function defineStore(idOrOptions, setup, setupOptions) {
  let id2;
  let options;
  const isSetupStore = typeof setup === "function";
  if (typeof idOrOptions === "string") {
    id2 = idOrOptions;
    options = isSetupStore ? setupOptions : setup;
  } else {
    options = idOrOptions;
    id2 = idOrOptions.id;
  }
  function useStore(pinia, hot) {
    const hasContext = hasInjectionContext();
    pinia = // in test mode, ignore the argument provided as we can always retrieve a
    // pinia instance with getActivePinia()
    pinia || (hasContext ? inject(piniaSymbol, null) : null);
    if (pinia)
      setActivePinia(pinia);
    pinia = activePinia;
    if (!pinia._s.has(id2)) {
      if (isSetupStore) {
        createSetupStore(id2, setup, options, pinia);
      } else {
        createOptionsStore(id2, options, pinia);
      }
    }
    const store = pinia._s.get(id2);
    return store;
  }
  useStore.$id = id2;
  return useStore;
}
const Tt = {
  props: {
    color: {
      type: String,
      default: () => "default"
    }
  }
}, $e = {
  props: {
    disabled: {
      type: Boolean,
      default: () => false
    }
  }
}, F = (e, n) => {
  const t = e.__vccOpts || e;
  for (const [i, o] of n)
    t[i] = o;
  return t;
}, Io = {
  name: "vu-divider",
  props: {
    vertical: {
      type: Boolean
    }
  }
};
function Co(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("hr", {
    class: normalizeClass(["vu-divider", { vertical: t.vertical }])
  }, null, 2);
}
const Gs = /* @__PURE__ */ F(Io, [["render", Co], ["__scopeId", "data-v-d007942d"]]), Bo = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Gs
}, Symbol.toStringTag, { value: "Module" })), $o = {
  name: "vu-badge",
  mixins: [Tt, $e],
  emits: ["close", "selected", "update:modelValue"],
  components: { VuDivider: Gs },
  props: {
    value: {
      type: Boolean,
      default: () => {
      }
    },
    text: {
      type: String,
      default: () => ""
    },
    rounded: {
      type: Boolean
    },
    alternate: {
      type: Boolean
    },
    badge2: {
      type: String,
      default: () => ""
    },
    badge3: {
      type: String,
      default: () => ""
    },
    icon: {
      type: String,
      default: () => ""
    },
    selectable: {
      type: Boolean,
      default: () => false
    },
    selected: {
      type: Boolean,
      default: () => false
    },
    togglable: {
      type: Boolean,
      default: () => true
    },
    closable: {
      type: Boolean,
      default: () => false
    }
  },
  data() {
    return {
      isSelected: false
    };
  },
  computed: {
    classes() {
      return [
        "vu-badge",
        `badge-root badge badge-${this.color}`,
        {
          "badge-alternate": this.alternate,
          rounded: this.rounded,
          "badge-closable": this.closable,
          "badge-selectable": this.selectable,
          disabled: this.disabled,
          "badge-selected": this.isSelected || this.selected || this.value,
          "badge-icon": this.icon
        }
      ];
    },
    iconClasses() {
      return `fonticon fonticon-${this.icon} badge-icon`;
    },
    showContent() {
      return typeof this.$slots.default == "function" || this.text;
    }
  },
  methods: {
    onClickOutside() {
      this.selectable && this.value === void 0 && this.togglable && (this.isSelected = false);
    },
    selectBadge() {
      this.selectable && (this.value === void 0 && (this.isSelected = this.togglable ? !this.isSelected : true), this.$emit("selected", this.isSelected), this.$emit("update:modelValue", this.isSelected));
    }
  }
}, Oo = {
  key: 1,
  class: "badge-content"
}, To = {
  key: 5,
  class: "badge-content"
};
function xo(e, n, t, i, o, s) {
  const a = resolveComponent("VuDivider"), u = resolveDirective("click-outside");
  return withDirectives((openBlock(), createElementBlock("span", {
    class: normalizeClass(s.classes),
    onClick: n[1] || (n[1] = (c) => s.selectBadge(c))
  }, [
    t.icon ? (openBlock(), createElementBlock("span", {
      key: 0,
      class: normalizeClass(s.iconClasses)
    }, null, 2)) : createCommentVNode("", true),
    s.showContent ? (openBlock(), createElementBlock("span", Oo, [
      renderSlot(e.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(t.text), 1)
      ], true)
    ])) : createCommentVNode("", true),
    t.badge2 ? (openBlock(), createBlock(a, {
      key: 2,
      vertical: ""
    })) : createCommentVNode("", true),
    t.badge2 ? (openBlock(), createElementBlock("span", {
      key: 3,
      class: normalizeClass(["badge-content", { "badge-center": t.badge3 }])
    }, toDisplayString(t.badge2), 3)) : createCommentVNode("", true),
    t.badge3 ? (openBlock(), createBlock(a, {
      key: 4,
      vertical: ""
    })) : createCommentVNode("", true),
    t.badge3 ? (openBlock(), createElementBlock("span", To, toDisplayString(t.badge3), 1)) : createCommentVNode("", true),
    t.closable ? (openBlock(), createElementBlock("span", {
      key: 6,
      class: "fonticon fonticon-cancel",
      onClick: n[0] || (n[0] = (c) => e.$emit("close"))
    })) : createCommentVNode("", true)
  ], 2)), [
    [u, s.onClickOutside]
  ]);
}
const jn = /* @__PURE__ */ F($o, [["render", xo], ["__scopeId", "data-v-867e386a"]]), Mo = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: jn
}, Symbol.toStringTag, { value: "Module" })), Ys = {
  props: {
    size: {
      type: String,
      default: () => ""
    }
  }
}, Vo = /^on[^a-z]/, Po = (e) => Vo.test(e), Ze = (e, n) => {
  const t = {};
  for (const i in e)
    Po(i) && (t[n ? i[2].toLowerCase() + i.slice(3) : i] = e[i]);
  return t;
}, Lo = {
  name: "vu-icon",
  mixins: [Tt, Ys],
  data: () => ({
    getListenersFromAttrs: Ze
  }),
  props: {
    icon: {
      required: true,
      type: String
    },
    withinText: {
      default: true,
      type: Boolean
    }
  }
};
function Do(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("span", mergeProps({
    class: ["vu-icon fonticon", [t.withinText ? "fonticon-within-text" : "", `fonticon-${t.icon}`, `${e.color}`, `${e.size}`]]
  }, toHandlers(e.getListenersFromAttrs(e.$attrs), true)), null, 16);
}
const ue = /* @__PURE__ */ F(Lo, [["render", Do], ["__scopeId", "data-v-68860d4f"]]), Ao = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: ue
}, Symbol.toStringTag, { value: "Module" }));
function Fo(e) {
  return e && e.__esModule && Object.prototype.hasOwnProperty.call(e, "default") ? e.default : e;
}
var Xs = { exports: {} }, ge = Xs.exports = {}, We, qe;
function $n() {
  throw new Error("setTimeout has not been defined");
}
function On() {
  throw new Error("clearTimeout has not been defined");
}
(function() {
  try {
    typeof setTimeout == "function" ? We = setTimeout : We = $n;
  } catch {
    We = $n;
  }
  try {
    typeof clearTimeout == "function" ? qe = clearTimeout : qe = On;
  } catch {
    qe = On;
  }
})();
function Js(e) {
  if (We === setTimeout)
    return setTimeout(e, 0);
  if ((We === $n || !We) && setTimeout)
    return We = setTimeout, setTimeout(e, 0);
  try {
    return We(e, 0);
  } catch {
    try {
      return We.call(null, e, 0);
    } catch {
      return We.call(this, e, 0);
    }
  }
}
function zo(e) {
  if (qe === clearTimeout)
    return clearTimeout(e);
  if ((qe === On || !qe) && clearTimeout)
    return qe = clearTimeout, clearTimeout(e);
  try {
    return qe(e);
  } catch {
    try {
      return qe.call(null, e);
    } catch {
      return qe.call(this, e);
    }
  }
}
var Je = [], St = false, ht, nn = -1;
function Eo() {
  !St || !ht || (St = false, ht.length ? Je = ht.concat(Je) : nn = -1, Je.length && Zs());
}
function Zs() {
  if (!St) {
    var e = Js(Eo);
    St = true;
    for (var n = Je.length; n; ) {
      for (ht = Je, Je = []; ++nn < n; )
        ht && ht[nn].run();
      nn = -1, n = Je.length;
    }
    ht = null, St = false, zo(e);
  }
}
ge.nextTick = function(e) {
  var n = new Array(arguments.length - 1);
  if (arguments.length > 1)
    for (var t = 1; t < arguments.length; t++)
      n[t - 1] = arguments[t];
  Je.push(new Qs(e, n)), Je.length === 1 && !St && Js(Zs);
};
function Qs(e, n) {
  this.fun = e, this.array = n;
}
Qs.prototype.run = function() {
  this.fun.apply(null, this.array);
};
ge.title = "browser";
ge.browser = true;
ge.env = {};
ge.argv = [];
ge.version = "";
ge.versions = {};
function nt() {
}
ge.on = nt;
ge.addListener = nt;
ge.once = nt;
ge.off = nt;
ge.removeListener = nt;
ge.removeAllListeners = nt;
ge.emit = nt;
ge.prependListener = nt;
ge.prependOnceListener = nt;
ge.listeners = function(e) {
  return [];
};
ge.binding = function(e) {
  throw new Error("process.binding is not supported");
};
ge.cwd = function() {
  return "/";
};
ge.chdir = function(e) {
  throw new Error("process.chdir is not supported");
};
ge.umask = function() {
  return 0;
};
var No = Xs.exports;
const Ro = /* @__PURE__ */ Fo(No);
function at(e) {
  return getCurrentScope() ? (onScopeDispose(e), true) : false;
}
function Ho(e, n) {
  if (typeof Symbol < "u") {
    const t = { ...e };
    return Object.defineProperty(t, Symbol.iterator, {
      enumerable: false,
      value() {
        let i = 0;
        return {
          next: () => ({
            value: n[i++],
            done: i > n.length
          })
        };
      }
    }), t;
  } else
    return Object.assign([...n], e);
}
function ie(e) {
  return typeof e == "function" ? e() : unref(e);
}
const un = typeof window < "u" && typeof document < "u";
typeof WorkerGlobalScope < "u" && globalThis instanceof WorkerGlobalScope;
const jo = Object.prototype.toString, ei = (e) => jo.call(e) === "[object Object]", Uo = () => +Date.now(), Be = () => {
}, qt = /* @__PURE__ */ Wo();
function Wo() {
  var e, n;
  return un && ((e = window == null ? void 0 : window.navigator) == null ? void 0 : e.userAgent) && (/iP(ad|hone|od)/.test(window.navigator.userAgent) || ((n = window == null ? void 0 : window.navigator) == null ? void 0 : n.maxTouchPoints) > 2 && /iPad|Macintosh/.test(window == null ? void 0 : window.navigator.userAgent));
}
function Un(e, n) {
  function t(...i) {
    return new Promise((o, s) => {
      Promise.resolve(e(() => n.apply(this, i), { fn: n, thisArg: this, args: i })).then(o).catch(s);
    });
  }
  return t;
}
const qo = (e) => e();
function Ko(e, n = {}) {
  let t, i, o = Be;
  const s = (u) => {
    clearTimeout(u), o(), o = Be;
  };
  return (u) => {
    const c = ie(e), d = ie(n.maxWait);
    return t && s(t), c <= 0 || d !== void 0 && d <= 0 ? (i && (s(i), i = null), Promise.resolve(u())) : new Promise((h2, p2) => {
      o = n.rejectOnCancel ? p2 : h2, d && !i && (i = setTimeout(() => {
        t && s(t), i = null, h2(u());
      }, d)), t = setTimeout(() => {
        i && s(i), i = null, h2(u());
      }, c);
    });
  };
}
function ti(e, n = true, t = true, i = false) {
  let o = 0, s, a = true, u = Be, c;
  const d = () => {
    s && (clearTimeout(s), s = void 0, u(), u = Be);
  };
  return (p2) => {
    const _ = ie(e), v = Date.now() - o, y = () => c = p2();
    return d(), _ <= 0 ? (o = Date.now(), y()) : (v > _ && (t || !a) ? (o = Date.now(), y()) : n && (c = new Promise((L, B) => {
      u = i ? B : L, s = setTimeout(() => {
        o = Date.now(), a = true, L(y()), d();
      }, Math.max(0, _ - v));
    })), !t && !s && (s = setTimeout(() => a = true, _)), a = false, c);
  };
}
const Go = {
  mounted: "mounted"
};
function Yo(e) {
  const n = /* @__PURE__ */ Object.create(null);
  return (t) => n[t] || (n[t] = e(t));
}
const Xo = /-(\w)/g, Jo = Yo((e) => e.replace(Xo, (n, t) => t ? t.toUpperCase() : ""));
function Zo(e) {
  return getCurrentInstance();
}
function Wn(...e) {
  if (e.length !== 1)
    return toRef$3(...e);
  const n = e[0];
  return typeof n == "function" ? readonly(customRef(() => ({ get: n, set: Be }))) : ref(n);
}
function Tn(e, n = 200, t = {}) {
  return Un(
    Ko(n, t),
    e
  );
}
function Qo(e, n = 200, t = false, i = true, o = false) {
  return Un(
    ti(n, t, i, o),
    e
  );
}
function el(e, n, t = {}) {
  const {
    eventFilter: i = qo,
    ...o
  } = t;
  return watch(
    e,
    Un(
      i,
      n
    ),
    o
  );
}
function dn(e, n = true, t) {
  Zo() ? onMounted(e, t) : n ? e() : nextTick(e);
}
function tl(e, n = {}) {
  var t;
  const i = ref((t = n.initialValue) != null ? t : null);
  return watch(
    e,
    () => i.value = Uo(),
    n
  ), i;
}
function nl(e, n, t = {}) {
  const {
    immediate: i = true
  } = t, o = ref(false);
  let s = null;
  function a() {
    s && (clearTimeout(s), s = null);
  }
  function u() {
    o.value = false, a();
  }
  function c(...d) {
    a(), o.value = true, s = setTimeout(() => {
      o.value = false, s = null, e(...d);
    }, ie(n));
  }
  return i && (o.value = true, un && c()), at(u), {
    isPending: readonly(o),
    start: c,
    stop: u
  };
}
function xn(e, n, t = {}) {
  const {
    throttle: i = 0,
    trailing: o = true,
    leading: s = true,
    ...a
  } = t;
  return el(
    e,
    n,
    {
      ...a,
      eventFilter: ti(i, o, s)
    }
  );
}
function Mn(e, n, t) {
  return watch(
    e,
    (i, o, s) => {
      i && n(i, o, s);
    },
    t
  );
}
function on(e = {}) {
  const {
    inheritAttrs: n = true
  } = e, t = shallowRef(), i = /* @__PURE__ */ defineComponent({
    setup(s, { slots: a }) {
      return () => {
        t.value = a.default;
      };
    }
  }), o = /* @__PURE__ */ defineComponent({
    inheritAttrs: n,
    setup(s, { attrs: a, slots: u }) {
      return () => {
        var c;
        if (!t.value && Ro.env.NODE_ENV !== "production")
          throw new Error("[VueUse] Failed to find the definition of reusable template");
        const d = (c = t.value) == null ? void 0 : c.call(t, { ...sl(a), $slots: u });
        return n && (d == null ? void 0 : d.length) === 1 ? d[0] : d;
      };
    }
  });
  return Ho(
    { define: i, reuse: o },
    [i, o]
  );
}
function sl(e) {
  const n = {};
  for (const t in e)
    n[Jo(t)] = e[t];
  return n;
}
function ye(e) {
  var n;
  const t = ie(e);
  return (n = t == null ? void 0 : t.$el) != null ? n : t;
}
const st = un ? window : void 0;
function pe(...e) {
  let n, t, i, o;
  if (typeof e[0] == "string" || Array.isArray(e[0]) ? ([t, i, o] = e, n = st) : [n, t, i, o] = e, !n)
    return Be;
  Array.isArray(t) || (t = [t]), Array.isArray(i) || (i = [i]);
  const s = [], a = () => {
    s.forEach((h2) => h2()), s.length = 0;
  }, u = (h2, p2, _, v) => (h2.addEventListener(p2, _, v), () => h2.removeEventListener(p2, _, v)), c = watch(
    () => [ye(n), ie(o)],
    ([h2, p2]) => {
      if (a(), !h2)
        return;
      const _ = ei(p2) ? { ...p2 } : p2;
      s.push(
        ...t.flatMap((v) => i.map((y) => u(h2, v, y, _)))
      );
    },
    { immediate: true, flush: "post" }
  ), d = () => {
    c(), a();
  };
  return at(d), d;
}
let Cs = false;
function il(e, n, t = {}) {
  const { window: i = st, ignore: o = [], capture: s = true, detectIframe: a = false } = t;
  if (!i)
    return Be;
  qt && !Cs && (Cs = true, Array.from(i.document.body.children).forEach((_) => _.addEventListener("click", Be)), i.document.documentElement.addEventListener("click", Be));
  let u = true;
  const c = (_) => o.some((v) => {
    if (typeof v == "string")
      return Array.from(i.document.querySelectorAll(v)).some((y) => y === _.target || _.composedPath().includes(y));
    {
      const y = ye(v);
      return y && (_.target === y || _.composedPath().includes(y));
    }
  }), h2 = [
    pe(i, "click", (_) => {
      const v = ye(e);
      if (!(!v || v === _.target || _.composedPath().includes(v))) {
        if (_.detail === 0 && (u = !c(_)), !u) {
          u = true;
          return;
        }
        n(_);
      }
    }, { passive: true, capture: s }),
    pe(i, "pointerdown", (_) => {
      const v = ye(e);
      u = !c(_) && !!(v && !_.composedPath().includes(v));
    }, { passive: true }),
    a && pe(i, "blur", (_) => {
      setTimeout(() => {
        var v;
        const y = ye(e);
        ((v = i.document.activeElement) == null ? void 0 : v.tagName) === "IFRAME" && !(y != null && y.contains(i.document.activeElement)) && n(_);
      }, 0);
    })
  ].filter(Boolean);
  return () => h2.forEach((_) => _());
}
function ol(e) {
  return typeof e == "function" ? e : typeof e == "string" ? (n) => n.key === e : Array.isArray(e) ? (n) => e.includes(n.key) : () => true;
}
function Vn(...e) {
  let n, t, i = {};
  e.length === 3 ? (n = e[0], t = e[1], i = e[2]) : e.length === 2 ? typeof e[1] == "object" ? (n = true, t = e[0], i = e[1]) : (n = e[0], t = e[1]) : (n = true, t = e[0]);
  const {
    target: o = st,
    eventName: s = "keydown",
    passive: a = false,
    dedupe: u = false
  } = i, c = ol(n);
  return pe(o, s, (h2) => {
    h2.repeat && ie(u) || c(h2) && t(h2);
  }, a);
}
function Bs(e, n, t = {}) {
  return Vn(e, n, { ...t, eventName: "keydown" });
}
function ll() {
  const e = ref(false);
  return getCurrentInstance() && onMounted(() => {
    e.value = true;
  }), e;
}
function qn(e) {
  const n = ll();
  return computed(() => (n.value, !!e()));
}
function al(e, n = {}) {
  const { window: t = st } = n, i = qn(() => t && "matchMedia" in t && typeof t.matchMedia == "function");
  let o;
  const s = ref(false), a = (d) => {
    s.value = d.matches;
  }, u = () => {
    o && ("removeEventListener" in o ? o.removeEventListener("change", a) : o.removeListener(a));
  }, c = watchEffect(() => {
    i.value && (u(), o = t.matchMedia(ie(e)), "addEventListener" in o ? o.addEventListener("change", a) : o.addListener(a), s.value = o.matches);
  });
  return at(() => {
    c(), u(), o = void 0;
  }), s;
}
function rl(e, n, t = {}) {
  const { window: i = st, ...o } = t;
  let s;
  const a = qn(() => i && "MutationObserver" in i), u = () => {
    s && (s.disconnect(), s = void 0);
  }, c = watch(
    () => ye(e),
    (p2) => {
      u(), a.value && i && p2 && (s = new MutationObserver(n), s.observe(p2, o));
    },
    { immediate: true }
  ), d = () => s == null ? void 0 : s.takeRecords(), h2 = () => {
    u(), c();
  };
  return at(h2), {
    isSupported: a,
    stop: h2,
    takeRecords: d
  };
}
function Kn(e, n, t = {}) {
  const { window: i = st, ...o } = t;
  let s;
  const a = qn(() => i && "ResizeObserver" in i), u = () => {
    s && (s.disconnect(), s = void 0);
  }, c = computed(() => Array.isArray(e) ? e.map((p2) => ye(p2)) : [ye(e)]), d = watch(
    c,
    (p2) => {
      if (u(), a.value && i) {
        s = new ResizeObserver(n);
        for (const _ of p2)
          _ && s.observe(_, o);
      }
    },
    { immediate: true, flush: "post", deep: true }
  ), h2 = () => {
    u(), d();
  };
  return at(h2), {
    isSupported: a,
    stop: h2
  };
}
function ln(e, n = {}) {
  const {
    reset: t = true,
    windowResize: i = true,
    windowScroll: o = true,
    immediate: s = true
  } = n, a = ref(0), u = ref(0), c = ref(0), d = ref(0), h2 = ref(0), p2 = ref(0), _ = ref(0), v = ref(0);
  function y() {
    const L = ye(e);
    if (!L) {
      t && (a.value = 0, u.value = 0, c.value = 0, d.value = 0, h2.value = 0, p2.value = 0, _.value = 0, v.value = 0);
      return;
    }
    const B = L.getBoundingClientRect();
    a.value = B.height, u.value = B.bottom, c.value = B.left, d.value = B.right, h2.value = B.top, p2.value = B.width, _.value = B.x, v.value = B.y;
  }
  return Kn(e, y), watch(() => ye(e), (L) => !L && y()), rl(e, y, {
    attributeFilter: ["style", "class"]
  }), o && pe("scroll", y, { capture: true, passive: true }), i && pe("resize", y, { passive: true }), dn(() => {
    s && y();
  }), {
    height: a,
    bottom: u,
    left: c,
    right: d,
    top: h2,
    width: p2,
    x: _,
    y: v,
    update: y
  };
}
function ni(e, n = { width: 0, height: 0 }, t = {}) {
  const { window: i = st, box: o = "content-box" } = t, s = computed(() => {
    var p2, _;
    return (_ = (p2 = ye(e)) == null ? void 0 : p2.namespaceURI) == null ? void 0 : _.includes("svg");
  }), a = ref(n.width), u = ref(n.height), { stop: c } = Kn(
    e,
    ([p2]) => {
      const _ = o === "border-box" ? p2.borderBoxSize : o === "content-box" ? p2.contentBoxSize : p2.devicePixelContentBoxSize;
      if (i && s.value) {
        const v = ye(e);
        if (v) {
          const y = i.getComputedStyle(v);
          a.value = Number.parseFloat(y.width), u.value = Number.parseFloat(y.height);
        }
      } else if (_) {
        const v = Array.isArray(_) ? _ : [_];
        a.value = v.reduce((y, { inlineSize: L }) => y + L, 0), u.value = v.reduce((y, { blockSize: L }) => y + L, 0);
      } else
        a.value = p2.contentRect.width, u.value = p2.contentRect.height;
    },
    t
  );
  dn(() => {
    const p2 = ye(e);
    p2 && (a.value = "offsetWidth" in p2 ? p2.offsetWidth : n.width, u.value = "offsetHeight" in p2 ? p2.offsetHeight : n.height);
  });
  const d = watch(
    () => ye(e),
    (p2) => {
      a.value = p2 ? n.width : 0, u.value = p2 ? n.height : 0;
    }
  );
  function h2() {
    c(), d();
  }
  return {
    width: a,
    height: u,
    stop: h2
  };
}
function kn(e) {
  return typeof Window < "u" && e instanceof Window ? e.document.documentElement : typeof Document < "u" && e instanceof Document ? e.documentElement : e;
}
const ul = {
  page: (e) => [e.pageX, e.pageY],
  client: (e) => [e.clientX, e.clientY],
  screen: (e) => [e.screenX, e.screenY],
  movement: (e) => e instanceof Touch ? null : [e.movementX, e.movementY]
};
function si(e = {}) {
  const {
    type: n = "page",
    touch: t = true,
    resetOnTouchEnds: i = false,
    initialValue: o = { x: 0, y: 0 },
    window: s = st,
    target: a = s,
    scroll: u = true,
    eventFilter: c
  } = e;
  let d = null;
  const h2 = ref(o.x), p2 = ref(o.y), _ = ref(null), v = typeof n == "function" ? n : ul[n], y = (D) => {
    const P = v(D);
    d = D, P && ([h2.value, p2.value] = P, _.value = "mouse");
  }, L = (D) => {
    if (D.touches.length > 0) {
      const P = v(D.touches[0]);
      P && ([h2.value, p2.value] = P, _.value = "touch");
    }
  }, B = () => {
    if (!d || !s)
      return;
    const D = v(d);
    d instanceof MouseEvent && D && (h2.value = D[0] + s.scrollX, p2.value = D[1] + s.scrollY);
  }, R = () => {
    h2.value = o.x, p2.value = o.y;
  }, ee = c ? (D) => c(() => y(D), {}) : (D) => y(D), ce = c ? (D) => c(() => L(D), {}) : (D) => L(D), Z = c ? () => c(() => B(), {}) : () => B();
  if (a) {
    const D = { passive: true };
    pe(a, ["mousemove", "dragover"], ee, D), t && n !== "movement" && (pe(a, ["touchstart", "touchmove"], ce, D), i && pe(a, "touchend", R, D)), u && n === "page" && pe(s, "scroll", Z, { passive: true });
  }
  return {
    x: h2,
    y: p2,
    sourceType: _
  };
}
function dl(e, n) {
  const t = shallowRef(n);
  return watch(
    Wn(e),
    (i, o) => {
      t.value = o;
    },
    { flush: "sync" }
  ), readonly(t);
}
function ii(e) {
  const n = window.getComputedStyle(e);
  if (n.overflowX === "scroll" || n.overflowY === "scroll" || n.overflowX === "auto" && e.clientWidth < e.scrollWidth || n.overflowY === "auto" && e.clientHeight < e.scrollHeight)
    return true;
  {
    const t = e.parentNode;
    return !t || t.tagName === "BODY" ? false : ii(t);
  }
}
function cl(e) {
  const n = e || window.event, t = n.target;
  return ii(t) ? false : n.touches.length > 1 ? true : (n.preventDefault && n.preventDefault(), false);
}
const Qt = /* @__PURE__ */ new WeakMap();
function hl(e, n = false) {
  const t = ref(n);
  let i = null, o;
  watch(Wn(e), (u) => {
    const c = kn(ie(u));
    if (c) {
      const d = c;
      Qt.get(d) || Qt.set(d, o), t.value && (d.style.overflow = "hidden");
    }
  }, {
    immediate: true
  });
  const s = () => {
    const u = kn(ie(e));
    !u || t.value || (qt && (i = pe(
      u,
      "touchmove",
      (c) => {
        cl(c);
      },
      { passive: false }
    )), u.style.overflow = "hidden", t.value = true);
  }, a = () => {
    var u;
    const c = kn(ie(e));
    !c || !t.value || (qt && (i == null || i()), c.style.overflow = (u = Qt.get(c)) != null ? u : "", Qt.delete(c), t.value = false);
  };
  return at(a), computed({
    get() {
      return t.value;
    },
    set(u) {
      u ? s() : a();
    }
  });
}
function fl(e = {}) {
  const {
    window: n = st,
    initialWidth: t = Number.POSITIVE_INFINITY,
    initialHeight: i = Number.POSITIVE_INFINITY,
    listenOrientation: o = true,
    includeScrollbar: s = true
  } = e, a = ref(t), u = ref(i), c = () => {
    n && (s ? (a.value = n.innerWidth, u.value = n.innerHeight) : (a.value = n.document.documentElement.clientWidth, u.value = n.document.documentElement.clientHeight));
  };
  if (c(), dn(c), pe("resize", c, { passive: true }), o) {
    const d = al("(orientation: portrait)");
    watch(d, () => c());
  }
  return { width: a, height: u };
}
const xt = {
  props: {
    show: { type: [Boolean, Object], default: false }
  },
  emits: ["update:show"],
  data() {
    return {
      innerShow: false
    };
  },
  watch: {
    show: {
      immediate: true,
      handler(e) {
        this.innerShow = !!e;
      }
    },
    innerShow(e) {
      !!e !== this.show && this.$emit("update:show", e);
    }
  }
}, Gn = (e) => {
  const n = typeof e;
  return n === "boolean" || n === "string" ? true : e.nodeType === Node.ELEMENT_NODE;
}, Yn = {
  name: "detachable",
  props: {
    attach: {
      default: () => false,
      validator: Gn
    },
    contentClass: {
      type: [String, Object],
      default: ""
    },
    contentStyle: {
      type: [String, Object],
      default: () => ""
    }
  },
  data: () => ({
    hasDetached: false,
    // the final value of renderTo
    target: null
  }),
  inject: {
    vuDebug: {
      default: true
    }
  },
  watch: {
    attach() {
      this.hasDetached = false, this.initDetach();
    }
  },
  mounted() {
    this.initDetach();
  },
  methods: {
    initDetach() {
      if (this._isDestroyed || this.hasDetached || this.attach === "" || this.attach === true || this.attach === "attach")
        return;
      let e;
      if (this.attach ? typeof this.attach == "string" ? e = document.querySelector(this.attach) : e = this.attach : e = document.body, !e) {
        this.vuDebug && console.warn(`Unable to locate target ${this.attach}`, this);
        return;
      }
      this.vuDebug && e.tagName.toLowerCase() !== "body" && window.getComputedStyle(e).position !== "relative" && console.warn(`target (${e.tagName.toLowerCase()}${e.id && ` #${e.id}`}${e.className && ` .${e.className}`}) element should have a relative position`), this.target = e, this.hasDetached = true;
    }
  }
}, Pn = function(n, t) {
  let i, o;
  return function(...a) {
    const u = this, c = +/* @__PURE__ */ new Date();
    i && c < i + t ? (clearTimeout(o), o = setTimeout(() => {
      i = c, n.apply(u, a);
    }, t)) : (i = c, n.apply(u, a));
  };
}, Xn = (e, n, t, i = { width: 0, x: 0, y: 0 }, { scrollTop: o = 0, scrollLeft: s = 0 } = {}, a = false, u = { left: 2, right: 2, top: 0, bottom: 0 }, c = { x: 0, y: 0 }) => {
  let d = n.y - i.y + o + (c.y || 0), h2 = n.x - i.x + s + (c.x || 0);
  isNaN(n.width) && (n.width = 0), isNaN(n.height) && (n.height = 0), /-right/.test(e) ? h2 += n.width - t.width : /^(top|bottom)$/.test(e) && (h2 += n.width / 2 - t.width / 2), /^bottom/.test(e) ? d += n.height : /^(left|right)(-top|-bottom)?$/.test(e) ? (h2 -= t.width, /^(right|right-\w{3,6})$/.test(e) && (h2 += n.width + t.width), /(-top|-bottom)/.test(e) ? /-bottom/.test(e) && (d += n.height - t.height) : d += n.height / 2 - t.height / 2) : d -= t.height;
  let p2 = 0, _ = 0;
  const v = n.width / 2;
  if (a) {
    const y = u.left, L = i.width - t.width - u.right, B = Math.max(y, Math.min(h2, L));
    p2 = h2 - B, h2 = B;
  }
  return {
    left: h2,
    top: d,
    shiftX: p2,
    shiftY: _,
    offset: v
  };
}, pl = {
  name: "vu-tooltip",
  mixins: [xt],
  data: () => ({
    setPosition: Xn
  }),
  props: {
    type: {
      type: String,
      default: () => "tooltip"
    },
    side: {
      type: String,
      default: () => "top"
    },
    arrow: {
      type: Boolean,
      default: true
    },
    text: {
      type: String,
      default: () => ""
    },
    animated: {
      type: Boolean,
      default: true
    },
    contentClass: {
      type: String,
      required: false,
      default: ""
    },
    prerender: {
      type: Boolean,
      required: false
    }
  }
}, ml = ["innerHTML"];
function vl(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    ref: "content",
    class: normalizeClass([`${t.side} ${t.type} ${t.type}-root`, { "without-arrow": !t.arrow }, { prerender: t.prerender }, t.contentClass])
  }, [
    createVNode(Transition, {
      name: t.animated ? "fade" : ""
    }, {
      default: withCtx(() => [
        e.show ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass([`${t.type}-wrapper`])
        }, [
          renderSlot(e.$slots, "arrow", { side: t.side }, () => [
            t.arrow ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: normalizeClass(`${t.type}-arrow`)
            }, null, 2)) : createCommentVNode("", true)
          ], true),
          renderSlot(e.$slots, "title", { side: t.side }, void 0, true),
          createBaseVNode("div", {
            ref: "body",
            class: normalizeClass(`${t.type}-body`)
          }, [
            t.text ? (openBlock(), createElementBlock("span", {
              key: 0,
              innerHTML: t.text
            }, null, 8, ml)) : renderSlot(e.$slots, "default", {
              key: 1,
              side: t.side
            }, void 0, true)
          ], 2)
        ], 2)) : createCommentVNode("", true)
      ]),
      _: 3
    }, 8, ["name"])
  ], 2);
}
const Jn = /* @__PURE__ */ F(pl, [["render", vl], ["__scopeId", "data-v-d95aa7e4"]]), gl = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Jn
}, Symbol.toStringTag, { value: "Module" })), bl = ["top", "top-right", "right-bottom", "right", "right-top", "bottom-right", "bottom", "bottom-left", "left-top", "left", "left-bottom", "top-left"], yl = (e, n, t, i) => {
  const o = t.indexOf(e), s = t[(o + 1) % t.length];
  return i.includes(s) ? n : s;
}, _l = ({ intersectionRatio: e }) => e < 1, wl = {
  name: "vu-popover",
  mixins: [xt, Yn],
  expose: ["updatePosition", "toggle"],
  emits: ["unpositionable"],
  components: { VuTooltip: Jn },
  props: {
    type: {
      type: String,
      default: "popover"
    },
    side: {
      type: String,
      default: "bottom"
    },
    arrow: {
      type: Boolean,
      default: false
    },
    shift: {
      type: Boolean,
      default: false
    },
    offsets: {
      type: Object,
      default: void 0
    },
    animated: {
      type: Boolean,
      default: true
    },
    overlay: {
      type: Boolean,
      default: false
    },
    click: {
      type: Boolean,
      default: true
    },
    hover: {
      type: Boolean,
      default: false
    },
    hoverImmediate: {
      type: Boolean,
      default: false
    },
    hoverDelay: {
      type: Number,
      default: 500
    },
    title: {
      type: String,
      default: () => ""
    },
    persistent: {
      type: Boolean,
      default: false
    },
    positions: {
      type: Array,
      required: false,
      default: () => bl
    },
    getNextPosition: {
      type: Function,
      required: false,
      default: yl
    },
    checkPosition: {
      type: Function,
      required: false,
      default: _l
    },
    syncWidth: {
      type: Boolean,
      default: false
    },
    ignoreEscapeKey: {
      type: Boolean,
      default: false
    },
    ignoreClickOutside: {
      type: Boolean,
      default: false
    }
  },
  data: () => ({
    open: false,
    width: 0,
    resizeObs: null,
    debounce() {
    },
    useDebounceFn: Tn,
    intersectionObs: null,
    setPositionBound: null,
    shifted: false,
    positioned: false,
    fadeTimeout: void 0,
    positionAttempts: [],
    scrollableAncestors: [],
    // put in positionable
    innerSide: "",
    keyboardListener() {
    }
  }),
  watch: {
    innerShow: {
      immediate: true,
      async handler(e) {
        e ? (this.fadeTimeout && (this.fadeTimeout = void 0), await new Promise((n) => setTimeout(n, 10)), this.positioned = false, this.open = true, this.positionAttempts = [], await this.$nextTick(), this.setPositionBound(), this.intersectionObs.observe(this.$refs.tooltip.$el), this.resizeObs || (this.resizeObs = new ResizeObserver(async () => {
          this.setPositionBound(true);
        })), this.listenScrolls()) : (this.$refs.tooltip && (this.intersectionObs.unobserve(this.$refs.tooltip.$el), this.resizeObs.disconnect()), this.stopScrollListening(), this.animated ? this.fadeTimeout = setTimeout(() => {
          this.open = false;
        }, 500) : this.open = false);
      }
    },
    innerSide: {
      handler() {
        this.updatePosition();
      }
    },
    attach() {
      this.innerShow && this.updatePosition();
    },
    open: {
      handler(e) {
        this.target && (e && !this.ignoreEscapeKey ? this.keyboardListener = pe(this.target, "keydown", (n) => {
          n.code === "Escape" && (this.innerShow = false);
        }) : this.keyboardListener());
      }
    },
    hover: {
      immediate: true,
      handler() {
        this.attachHover();
      }
    },
    hoverImmediate() {
      this.attachHover();
    },
    hoverDelay() {
      this.attachHover();
    }
  },
  created() {
    this.setPositionBound = Pn(this.setPosition.bind(this), 1);
  },
  async mounted() {
    await this.$nextTick();
    let e = 0;
    const n = 5;
    for (; e < n && this.$refs.activator === void 0 && this.$refs.tooltip === void 0; )
      e++, await this.$nextTick();
    const { target: t, positionAttempts: i } = this;
    this.intersectionObs = new IntersectionObserver(([{ boundingClientRect: o, rootBounds: s, intersectionRatio: a, intersectionRect: u }]) => {
      if (this.$refs.tooltip && this.intersectionObs.unobserve(this.$refs.tooltip.$el), this.checkPosition({ intersectionRatio: a, elementRect: o, targetRect: s, intersectionRect: u, positionAttempts: i })) {
        const c = this.getNextPosition(this.innerSide || this.side, this.side, this.positions, this.positionAttempts);
        if (this.positionAttempts.length > this.positions.length) {
          this.$emit("unpositionable"), this.positioned = true, this.positionAttempts = [];
          return;
        }
        this.innerSide = c, this.positionAttempts.push(this.innerSide);
      } else
        this.positioned = true, this.positionAttempts = [], this.resizeObs.observe(this.$refs.tooltip.$el), this.resizeObs.observe(this.target);
    }, { root: t !== document.body ? t : document });
  },
  beforeUnmount() {
    try {
      this.innerShow = false, this.stopScrollListening(), this.intersectionObs.disconnect(), this.resizeObs.disconnect();
    } catch {
    }
  },
  methods: {
    listenScrolls() {
      const e = [];
      let n = this.$refs.activator.parentElement;
      for (; n && (this.target.contains(n) || n === this.target); ) {
        const { overflow: t } = window.getComputedStyle(n), i = t.split(" ");
        ["auto", "scroll"].some((o) => i.includes(o)) && e.push(n), n = n.parentElement;
      }
      this.scrollableAncestors = e, this.scrollableAncestors.forEach((t) => t.addEventListener("scroll", this.setPositionBound));
    },
    stopScrollListening() {
      this.scrollableAncestors.forEach((e) => e.removeEventListener("scroll", this.setPositionBound));
    },
    updatePosition() {
      var e;
      this.setPositionBound(), this.intersectionObs.observe((e = this.$refs.tooltip) == null ? void 0 : e.$el);
    },
    async setPosition(e) {
      var u;
      e && await this.$nextTick();
      let n = this.$refs.activator.getBoundingClientRect();
      const t = (u = this.$refs.tooltip) == null ? void 0 : u.$el;
      if (!t)
        return;
      let i = t.getBoundingClientRect();
      this.syncWidth && i.width !== n.width && (this.width = n.width, await this.$nextTick(), n = this.$refs.activator.getBoundingClientRect(), i = this.$refs.tooltip.$el.getBoundingClientRect());
      const o = this.target.getBoundingClientRect(), s = this.offsets && this.offsets[this.innerSide || this.side] || {};
      this.positionAttempts.push(this.innerSide || this.side);
      const a = Xn(
        this.innerSide || this.side,
        n,
        i,
        o,
        this.target,
        this.shift,
        { left: 0, right: 0 },
        s
      );
      this.shifted = a.shiftX, t.style.top = `${a.top}px`, t.style.left = `${a.left}px`, this.overlay && (this.$refs.overlay.style.top = `${this.target === document.body ? document.scrollingElement.scrollTop : this.target.scrollTop}px`);
    },
    onClickOutside(e, n = false) {
      if (this.ignoreClickOutside || !this.innerShow)
        return;
      const { target: t } = e;
      n && e.preventDefault(), !(this.$refs.tooltip && (t === this.$refs.tooltip.$el || this.$refs.tooltip.$el.contains(t))) && (this.innerShow = false);
    },
    onHover(e) {
      this.debounce(e).then((n) => {
        this.openedByClick || (n === "mouseenter" ? this.innerShow = true : (this.innerShow = false, this.openedByClick = false));
      }).catch(() => {
      });
    },
    attachHover() {
      this.hover && !this.hoverImmediate ? this.debounce = Tn(({ type: e }) => e, this.hoverDelay, { rejectOnCancel: true }) : this.debounce = function() {
      };
    },
    onClick() {
      this.toggle(), this.hover && this.innerShow ? this.openedByClick = true : this.openedByClick = false;
    },
    toggle(e = void 0) {
      e !== void 0 ? this.innerShow = e : this.innerShow = !this.innerShow;
    }
  }
};
function kl(e, n, t, i, o, s) {
  const a = resolveComponent("VuTooltip"), u = resolveDirective("click-outside");
  return openBlock(), createElementBlock(Fragment, null, [
    withDirectives((openBlock(), createElementBlock("span", mergeProps({
      ref: "activator",
      class: "vu-popover__activator"
    }, e.$attrs, {
      onClick: n[0] || (n[0] = (c) => t.click && s.onClick(true)),
      onContextmenu: n[1] || (n[1] = withModifiers(() => {
      }, ["prevent", "stop"])),
      onMouseenter: n[2] || (n[2] = (c) => t.hover && s.onHover(c)),
      onMouseleave: n[3] || (n[3] = (c) => t.hover && s.onHover(c))
    }), [
      renderSlot(e.$slots, "default", {}, void 0, true)
    ], 16)), [
      [u, { handler: s.onClickOutside, innerShow: e.innerShow }]
    ]),
    e.open || t.persistent ? withDirectives((openBlock(), createBlock(Teleport, {
      key: 0,
      to: e.target
    }, [
      createVNode(Transition, {
        name: t.animated ? "fade" : ""
      }, {
        default: withCtx(() => [
          e.innerShow && t.overlay ? (openBlock(), createElementBlock("div", {
            key: 0,
            ref: "overlay",
            class: "mask popover-mask",
            onWheel: n[4] || (n[4] = withModifiers((...c) => s.onClickOutside && s.onClickOutside(...c), ["prevent"])),
            onTouchstart: n[5] || (n[5] = (c) => s.onClickOutside(c, true))
          }, null, 544)) : createCommentVNode("", true)
        ]),
        _: 1
      }, 8, ["name"]),
      createVNode(Transition, {
        appear: "",
        name: t.animated ? "fade" : ""
      }, {
        default: withCtx(() => [
          withDirectives(createVNode(a, {
            ref: "tooltip",
            arrow: t.arrow,
            prerender: !e.positioned,
            type: t.type,
            show: true,
            side: e.innerSide || t.side,
            class: normalizeClass(e.contentClass),
            style: normalizeStyle([e.width ? `width: ${e.width}px` : {}, e.contentStyle]),
            "onUpdate:show": n[6] || (n[6] = (c) => e.open = false),
            onMouseenter: n[7] || (n[7] = (c) => t.hover && s.onHover(c)),
            onMouseleave: n[8] || (n[8] = (c) => t.hover && s.onHover(c))
          }, {
            arrow: withCtx(({ side: c }) => [
              renderSlot(e.$slots, "arrow", {
                side: e.innerSide || c,
                shift: e.shifted
              }, void 0, true)
            ]),
            title: withCtx(({ side: c }) => [
              renderSlot(e.$slots, "title", {
                side: e.innerSide || c
              }, () => [
                t.title ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
                  createTextVNode(toDisplayString(t.title), 1)
                ], 64)) : createCommentVNode("", true)
              ], true)
            ]),
            default: withCtx(() => [
              renderSlot(e.$slots, "body", {}, void 0, true)
            ]),
            _: 3
          }, 8, ["arrow", "prerender", "type", "side", "class", "style"]), [
            [vShow, e.innerShow || e.show]
          ])
        ]),
        _: 3
      }, 8, ["name"])
    ], 8, ["to"])), [
      [vShow, e.open]
    ]) : createCommentVNode("", true)
  ], 64);
}
const Ge = /* @__PURE__ */ F(wl, [["render", kl], ["__scopeId", "data-v-acca35da"]]), Sl = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ge
}, Symbol.toStringTag, { value: "Module" })), Il = {
  name: "vu-status-bar",
  props: {
    items: {
      type: Array,
      default: () => []
    },
    constrained: Boolean
  },
  data() {
    return {
      overflows: false,
      ellipsis: false,
      intObs: null,
      intObs2: null,
      visibleAmount: 0
    };
  },
  mounted() {
    this.watchSize();
  },
  computed: {
    visibleItems() {
      return this.items.slice(0, this.visibleAmount);
    },
    hiddenItems() {
      return this.overflows ? this.items.slice(this.visibleAmount) : [];
    }
  },
  watch: {
    items: {
      immediate: true,
      // eslint-disable-next-line object-shorthand, func-names
      handler: function(e) {
        this.visibleAmount = e.length, this.ellipsis = false, this.overflows = false, this.$el && this.$nextTick(() => this.watchSize());
      }
    }
  },
  methods: {
    watchSize() {
      this.intObs = new IntersectionObserver(this.intersects, {
        root: this.$refs.container,
        threshold: 1
      }), this.intObs.observe(this.$refs.inner), this.intObs2 = new IntersectionObserver(this.intersects2, {
        root: this.$refs.inner,
        threshold: 1
      });
    },
    async intersects() {
      this.intObs.disconnect(), this.ellipsis = true;
      const e = this.$refs.inner.querySelectorAll(".vu-badge");
      await this.$nextTick(), e.forEach((n) => {
        this.intObs2.observe(n);
      });
    },
    intersects2(e) {
      const n = e.filter((i) => i.intersectionRatio < 1);
      let { length: t } = n;
      if (t) {
        const i = this.$refs.inner.getBoundingClientRect(), { right: o } = i, s = n.shift();
        s && o - s.target.getBoundingClientRect().left - 22 < 0 && (t += 1), this.visibleAmount -= t, this.overflows = true;
      }
      this.intObs2.disconnect();
    },
    units(e) {
      return this.ellipsis ? e > 99 ? "99+" : `${e}` : `${e}`;
    },
    destroyed() {
      this.intObs1 && delete this.intObs1, this.intObs2 && delete this.intObs2;
    }
  },
  components: { VuBadge: jn, VuPopover: Ge, VuIcon: ue }
}, Cl = {
  class: "status-bar__inner",
  ref: "inner"
};
function Bl(e, n, t, i, o, s) {
  const a = resolveComponent("VuBadge"), u = resolveComponent("VuIcon"), c = resolveComponent("VuPopover"), d = resolveDirective("tooltip");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-status-bar", { "status-bar--constrained": t.constrained }]),
    ref: "container"
  }, [
    createBaseVNode("div", Cl, [
      (openBlock(true), createElementBlock(Fragment, null, renderList(s.visibleItems, (h2) => withDirectives((openBlock(), createBlock(a, {
        key: h2.id,
        icon: h2.icon,
        text: h2.text || h2.amount && s.units(h2.amount) || "",
        color: h2.color || "copy-grey",
        value: h2.value,
        togglable: false,
        style: normalizeStyle([h2.amount && h2.icon ? "min-width: 45px" : ""])
      }, null, 8, ["icon", "text", "color", "value", "style"])), [
        [
          d,
          h2.tooltip || h2.text || h2.amount || "",
          void 0,
          { hover: true }
        ]
      ])), 128)),
      o.overflows ? (openBlock(), createBlock(c, {
        key: 0,
        type: "tooltip",
        "content-class": "vu-status-bar",
        shift: "",
        arrow: ""
      }, {
        default: withCtx(() => [
          createVNode(u, {
            icon: "menu-dot",
            style: { transform: "rotate(90deg)" }
          })
        ]),
        body: withCtx(() => [
          (openBlock(true), createElementBlock(Fragment, null, renderList(s.hiddenItems, (h2) => (openBlock(), createBlock(a, {
            key: h2.id,
            icon: h2.icon,
            text: h2.text || `${h2.amount}` || "",
            color: h2.color || "copy-grey",
            value: h2.value,
            togglable: false
          }, null, 8, ["icon", "text", "color", "value"]))), 128))
        ]),
        _: 1
      })) : createCommentVNode("", true)
    ], 512)
  ], 2);
}
const Zn = /* @__PURE__ */ F(Il, [["render", Bl], ["__scopeId", "data-v-71ee9873"]]), $l = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Zn
}, Symbol.toStringTag, { value: "Module" })), Ol = {
  name: "vu-lazy",
  props: {
    height: {
      type: [Number, String],
      default: () => "10px"
    },
    options: {
      type: Object,
      default: () => ({})
    }
  },
  emits: ["intersect"],
  data: () => ({
    observer: null,
    intersected: false
  }),
  mounted() {
    "IntersectionObserver" in window ? (this.observer = new IntersectionObserver((e) => {
      const n = e == null ? void 0 : e.pop();
      n != null && n.isIntersecting && (this.intersected = true, this.observer.disconnect(), this.$emit("intersect"));
    }, this.options), this.observer.observe(this.$el)) : (this.intersected = true, this.$emit("intersect"));
  },
  beforeUnmount() {
    "IntersectionObserver" in window && this.observer && this.observer.disconnect(), delete this.observer;
  }
};
function Tl(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    style: normalizeStyle(e.intersected ? "" : `min-height: ${t.height}${typeof t.height == "number" && "px" || ""}`)
  }, [
    e.intersected ? renderSlot(e.$slots, "default", { key: 0 }) : renderSlot(e.$slots, "placeholder", { key: 1 })
  ], 4);
}
const Qn = /* @__PURE__ */ F(Ol, [["render", Tl]]), xl$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Qn
}, Symbol.toStringTag, { value: "Module" })), Ml = {
  name: "vu-image",
  components: { VuLazy: Qn },
  props: {
    lazy: {
      type: Boolean,
      required: false
    },
    src: {
      type: [URL, String],
      required: true
    },
    height: [Number, String],
    maxHeight: [Number, String],
    maxWidth: [Number, String],
    minHeight: [Number, String],
    minWidth: [Number, String],
    width: [Number, String],
    contain: Boolean,
    aspectRatio: String
  },
  emits: ["load", "error"],
  data: () => ({
    image: void 0,
    calculatedAspectRatio: void 0,
    naturalWidth: void 0,
    isLoading: true,
    hasError: false
  }),
  inject: {
    vuDebug: {
      default: false
    }
  },
  computed: {
    computedAspectRatio() {
      return Number(this.aspectRatio || this.calculatedAspectRatio);
    },
    imageSizerStyle() {
      return this.computedAspectRatio ? { paddingBottom: `${1 / this.computedAspectRatio * 100}%` } : void 0;
    },
    imageStyle() {
      return [
        Number.isNaN(this.width) ? "" : { width: `${this.width}px` },
        Number.isNaN(this.height) ? "" : { height: `${this.height}px` },
        Number.isNaN(this.minHeight) ? "" : { minHeight: `${this.minHeight}px` },
        Number.isNaN(this.maxHeight) ? "" : { maxHeight: `${this.maxHeight}px` },
        Number.isNaN(this.minWidth) ? "" : { minWidth: `${this.minWidth}px` },
        Number.isNaN(this.maxWidth) ? "" : { maxWidth: `${this.maxWidth}px` }
      ];
    },
    imageClasses() {
      return `vu-image__image--${this.contain ? "contain" : "cover"}`;
    }
  },
  watch: {
    src() {
      this.isLoading ? this.loadImage() : this.init();
    }
  },
  mounted() {
    this.init();
  },
  methods: {
    init() {
      this.lazy || this.loadImage();
    },
    loadImage() {
      const e = new Image();
      this.image = e, this.isLoading = true, e.onload = () => {
        e.decode ? e.decode().catch((n) => {
          this.vuDebug && console.warn(
            `Failed to decode image, trying to render anyway

src: ${this.src}` + (n.message ? `
Original error: ${n.message}` : ""),
            this
          );
        }).then(this.onLoad) : this.onLoad();
      }, e.onerror = this.onError, e.src = this.src, this.aspectRatio || this.pollForSize(e);
    },
    pollForSize(e, n = 100) {
      const t = () => {
        const { naturalHeight: i, naturalWidth: o } = e;
        i || o ? (this.naturalWidth = o, this.calculatedAspectRatio = o / i, this.image = null) : !e.complete && this.isLoading && !this.hasError && n != null && setTimeout(t, n);
      };
      t();
    },
    onLoad() {
      this.isLoading = false, this.$emit("load", this.src);
    },
    onError() {
      this.hasError = true, this.$emit("error", this.src);
    }
  }
}, Vl = (e) => (pushScopeId("data-v-485690ee"), e = e(), popScopeId(), e), Pl = /* @__PURE__ */ Vl(() => /* @__PURE__ */ createBaseVNode("div", { class: "vu-image__fill" }, null, -1));
function Ll(e, n, t, i, o, s) {
  const a = resolveComponent("VuLazy");
  return openBlock(), createElementBlock("div", {
    class: "vu-image",
    style: normalizeStyle(s.imageStyle)
  }, [
    createBaseVNode("div", {
      class: "vu-image__sizer",
      style: normalizeStyle(s.imageSizerStyle)
    }, null, 4),
    t.lazy ? (openBlock(), createBlock(a, {
      key: 0,
      height: t.height || t.maxHeight || t.minHeight || 10,
      onIntersect: n[0] || (n[0] = (u) => s.loadImage())
    }, {
      default: withCtx(() => [
        createBaseVNode("div", {
          class: normalizeClass(["vu-image__image", s.imageClasses]),
          style: normalizeStyle([[e.isLoading ? "" : { backgroundImage: `url(${t.src})` }], { "background-position": "center center" }])
        }, null, 6)
      ]),
      _: 1
    }, 8, ["height"])) : (openBlock(), createElementBlock("div", {
      key: 1,
      class: normalizeClass(["vu-image__image", s.imageClasses]),
      style: normalizeStyle([[e.isLoading ? "" : { backgroundImage: `url(${t.src})` }], { "background-position": "center center" }])
    }, null, 6)),
    Pl,
    renderSlot(e.$slots, "default", {}, void 0, true)
  ], 4);
}
const it = /* @__PURE__ */ F(Ml, [["render", Ll], ["__scopeId", "data-v-485690ee"]]), Dl = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: it
}, Symbol.toStringTag, { value: "Module" })), Yt = Symbol("vuIsMobileOrTablet"), es = Symbol("vuIsIOS"), ts = Symbol("vuMessageNoProgress"), oi = Symbol("vuAlertDialogConfirmButtonLabel"), li = Symbol("vuAlertDialogCloseButtonLabel"), ai = Symbol("vuAlertDialogRiskyButtonLabel"), ri = Symbol("vuAlertDialogCloseButtonAltLabel"), ui = Symbol("vuDropdownMenuOverlay"), di = Symbol("vuTimelineDividerAncestorDepth"), ci = Symbol("vuTimelineDividerStickyContainer"), hi = Symbol("vuThumbnailListItemActionsActiveClass"), ns = Symbol("vuHasWUX");
function Re() {
  return window ? ("10000000-1000-4000-8000" + -1e11).replace(/[018]/g, (e) => (e ^ (window.crypto || window.msCrypto).getRandomValues(new Uint8Array(1))[0] & 15 >> e / 4).toString(16)) : (void 0)();
}
function ss(e, n = true) {
  let t = n;
  return ie(e).forEach((o) => {
    !o.text && !o.label && (!o.class || !o.class.includes("divider")) && (t = false), o.items && (t = ss(o.items, t));
  }), t;
}
function Al() {
  return window ? navigator.userAgent.toLowerCase().indexOf("firefox") >= 0 : false;
}
const Fl = {
  name: "vu-spinner",
  props: {
    mask: {
      type: Boolean,
      default: () => false
    },
    text: {
      type: String,
      default: () => ""
    }
  }
}, zl = { class: "mask-wrapper" }, El = { class: "mask-content" }, Nl = /* @__PURE__ */ createStaticVNode('<div class="spinner spinning fade in"><span class="spinner-bar"></span><span class="spinner-bar spinner-bar1"></span><span class="spinner-bar spinner-bar2"></span><span class="spinner-bar spinner-bar3"></span></div>', 1), Rl = {
  key: 0,
  class: "text"
};
function Hl(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass({ mask: t.mask })
  }, [
    createBaseVNode("div", zl, [
      createBaseVNode("div", El, [
        Nl,
        t.text.length ? (openBlock(), createElementBlock("span", Rl, toDisplayString(t.text), 1)) : createCommentVNode("", true)
      ])
    ])
  ], 2);
}
const is = /* @__PURE__ */ F(Fl, [["render", Hl]]), jl = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: is
}, Symbol.toStringTag, { value: "Module" })), Ul = {
  name: "vu-scroller",
  exposes: ["stopLoading", "stopLoadingBefore"],
  props: {
    lock: {
      type: Boolean,
      default: false
    },
    reverse: {
      type: Boolean,
      default: false
    },
    infinite: {
      type: Boolean,
      default: false
    },
    showLoading: {
      type: Boolean,
      default: false
    },
    // alias for infinite
    dataAfter: {
      type: Boolean,
      default: false
    },
    dataBefore: {
      type: Boolean,
      default: false
    },
    infiniteMargin: {
      type: Number,
      default: 200
    },
    infiniteHeight: {
      type: String,
      default: "50px"
    },
    infiniteBeforeHeight: {
      type: String,
      default: "50px"
    },
    loadingText: {
      type: String,
      default: ""
    },
    horizontal: {
      type: Boolean,
      default: false
    },
    alwaysShow: {
      type: Boolean,
      default: false
    },
    // Allows to configure timeout for innerScroll to happen.
    // The new content needs to be rerender to not endlessly loop on the intersection.
    updateSleep: {
      type: Number,
      default: 15
    },
    noIntersectionRoot: {
      type: Boolean,
      default: false
    }
  },
  emits: ["loading-before", "loading", "mounted"],
  data() {
    return {
      lazyKeyIndex: 0,
      lazyKeyIndex2: 0,
      wait: false,
      waitBefore: false,
      firefox: false,
      isLocked: void 0
    };
  },
  inject: {
    noCss: {
      default: true,
      from: ns
    }
  },
  computed: {
    rootMargin() {
      return Array(4).fill(`${this.infiniteMargin}px`).join(" ");
    },
    options() {
      const e = {}, { rootMargin: n } = this;
      return this.noIntersectionRoot || (e.root = this.$refs["scroll-container"]), {
        ...e,
        rootMargin: n
      };
    }
  },
  mounted() {
    this.firefox = Al(), this.$emit("mounted"), this.lockHandler(this.lock);
  },
  methods: {
    stopLoading(e) {
      e ? (this.lazyKeyIndex2 += 1, this.sleep()) : (this.lazyKeyIndex += 1, this.sleep());
    },
    async sleep() {
      this.wait = true, this.waitBefore = true, await setTimeout(() => {
      }, this.updateSleep), this.wait = false, this.waitBefore = false;
    },
    lockHandler(e) {
      this.isLocked === void 0 && (this.isLocked = hl(this.$refs["scroll-container"])), this.isLocked = e;
    }
  },
  watch: {
    lock: {
      handler: function(e) {
        this.lockHandler(e);
      }
    }
  },
  components: { VuSpinner: is, VuLazy: Qn }
}, Wl = { class: "vu-scroll-container__inner" };
function ql(e, n, t, i, o, s) {
  const a = resolveComponent("VuSpinner"), u = resolveComponent("VuLazy");
  return openBlock(), createElementBlock("div", {
    ref: "scroll-container",
    class: normalizeClass(["vu-scroll-container", [{
      "vu-scroll-container--reverse": t.reverse,
      "vu-scroll-container--horizontal": t.horizontal,
      "vu-scroll-container--always-show": t.alwaysShow,
      "vu-scroll-container--classic": !s.noCss,
      firefox: o.firefox
    }]])
  }, [
    createBaseVNode("div", Wl, [
      t.dataBefore && !o.waitBefore ? (openBlock(), createBlock(u, {
        key: `lazy-key-${o.lazyKeyIndex2}`,
        onIntersect: n[0] || (n[0] = (c) => {
          e.$emit("loading-before"), e.$emit("loading", true);
        }),
        options: s.options,
        height: t.infiniteBeforeHeight || t.infiniteHeight,
        class: "vu-scroll__lazy vu-scroll__lazy-top"
      }, {
        default: withCtx(() => [
          renderSlot(e.$slots, "loadingBefore", {}, () => [
            createVNode(a, { text: t.loadingText }, null, 8, ["text"])
          ], true)
        ]),
        _: 3
      }, 8, ["options", "height"])) : createCommentVNode("", true),
      renderSlot(e.$slots, "default", {}, void 0, true),
      (t.infinite || t.dataAfter) && !o.wait ? (openBlock(), createBlock(u, {
        key: `lazy-key-${o.lazyKeyIndex}`,
        onIntersect: n[1] || (n[1] = (c) => e.$emit("loading")),
        options: s.options,
        height: t.infiniteHeight,
        style: { "min-width": "30px" },
        class: "vu-scroll__lazy vu-scroll__lazy-bottom"
      }, {
        default: withCtx(() => [
          renderSlot(e.$slots, "loading", {}, () => [
            createVNode(a, { text: t.loadingText }, null, 8, ["text"])
          ], true)
        ]),
        _: 3
      }, 8, ["options", "height"])) : t.showLoading ? renderSlot(e.$slots, "loading", { key: 2 }, () => [
        createVNode(a, { text: t.loadingText }, null, 8, ["text"])
      ], true) : createCommentVNode("", true)
    ])
  ], 2);
}
const Qe = /* @__PURE__ */ F(Ul, [["render", ql], ["__scopeId", "data-v-ffb36e87"]]), Kl = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Qe
}, Symbol.toStringTag, { value: "Module" })), Gl = {
  name: "vu-dropdownmenu-items-scrollable",
  components: { VuIcon: ue, VuImage: it, VuScroller: Qe },
  expose: ["checkHeight"],
  emits: ["update:responsive", "update:position", "click-item", "update:selected"],
  props: {
    target: {
      type: Object,
      required: false
    },
    items: {
      type: Array,
      required: true,
      validator: ss
    },
    selected: {
      type: Array,
      required: true
    },
    scrollable: {
      type: Boolean,
      default: true,
      required: false
    },
    zIndex: {
      type: Number,
      default: 1e3
    },
    itemHeight: {
      type: Number,
      default: 45
    },
    maxItemsBeforeScroll: {
      type: Number,
      default: Number.Infinity
    },
    mainMenu: {
      default: false
    }
  },
  data: () => ({
    uuid: Re,
    root: false,
    height: Number.Infinity,
    displayTop: false
  }),
  watch: {
    items() {
      this.isScrollable && this.checkHeight();
    }
  },
  computed: {
    maxItemsHeight() {
      return this.itemHeight * this.maxItemsBeforeScroll - 1;
    },
    hasNotSubMenus() {
      var e;
      return !((e = this.items) != null && e.some(({ items: n }) => n !== void 0));
    },
    isScrollable() {
      return this.hasNotSubMenus;
    },
    upwardsShift() {
      return !this.mainMenu && this.displayTop ? {
        bottom: 0,
        top: "auto",
        position: "absolute"
      } : null;
    }
  },
  async mounted() {
    await this.$nextTick(), this.isScrollable && this.items && this.checkHeight();
  },
  methods: {
    toggleSelected(e) {
      const n = this.selected.slice(0);
      return e.selected || this.selected.includes(e) ? n.splice(this.selected.indexOf(e), 1) : n.push(e), n;
    },
    onItemClick(e) {
      e.disabled || ((e.selectable || e.selected || this.selected.includes(e)) && this.$emit("update:selected", this.toggleSelected(e)), this.$emit("click-item", e));
    },
    async checkHeight() {
      var e, n, t;
      if (this.scrollable) {
        await this.$nextTick(), this.$el;
        const { top: i } = this.$el.getBoundingClientRect(), {
          bottom: o,
          top: s
        } = (e = this.target) == null ? void 0 : e.getBoundingClientRect(), a = o ? Math.min(window.innerHeight, o) : window.innerHeight, u = i - Math.max(0, s);
        this.height = a - i, await this.$nextTick();
        const c = (t = (n = this.$refs) == null ? void 0 : n.scroller) == null ? void 0 : t.$el;
        c && c.scrollHeight > c.clientHeight && u > this.height && (this.displayTop = true, this.height = u);
      } else
        this.height = Number.Infinity, this.displayTop = false;
    }
  }
}, Yl = {
  class: "dropdown-menu-wrap",
  ref: "items"
}, Xl = ["onClick"], Jl = {
  key: 1,
  class: "p-[5px] w-[44px] float-left h-full"
}, Zl = { class: "item-text" }, Ql = {
  key: 0,
  class: "item-text"
};
function ea(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon"), u = resolveComponent("VuImage"), c = resolveComponent("VuScroller");
  return openBlock(), createElementBlock("div", {
    class: "dropdown-menu dropdown-menu-root dropdown-root",
    style: normalizeStyle([{ zIndex: t.zIndex }, s.upwardsShift])
  }, [
    createVNode(c, {
      style: normalizeStyle(
        t.scrollable && (e.height || t.maxItemsBeforeScroll !== 1 / 0) ? {
          maxHeight: `${Math.min(s.maxItemsHeight, e.height)}px`
        } : {}
      ),
      ref: "scroller"
    }, {
      default: withCtx(() => [
        createBaseVNode("ul", Yl, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(t.items, (d) => (openBlock(), createElementBlock(Fragment, null, [
            !d.class || !d.class.includes("header") && !d.class.includes("divider") ? (openBlock(), createElementBlock("li", {
              key: d.text || d.label,
              class: normalizeClass(["item", [{
                selectable: !d.disabled && d.selectable || d.selected || t.selected.includes(d),
                selected: d.selected || t.selected.includes(d),
                hidden: d.hidden,
                disabled: d.disabled
              }, d.class]]),
              onClick: withModifiers((h2) => s.onItemClick(d), ["stop"])
            }, [
              renderSlot(e.$slots, "default", { item: d }, () => [
                d.fonticon ? (openBlock(), createBlock(a, {
                  key: 0,
                  icon: d.fonticon,
                  withinText: false
                }, null, 8, ["icon"])) : d.imageSource ? (openBlock(), createElementBlock("div", Jl, [
                  createVNode(u, {
                    src: d.imageSource,
                    aspectRatio: "1",
                    width: "34"
                  }, null, 8, ["src"])
                ])) : createCommentVNode("", true),
                createBaseVNode("span", Zl, toDisplayString(d.text || d.label), 1)
              ], true)
            ], 10, Xl)) : (openBlock(), createElementBlock("li", {
              key: d.text || d.label || e.uuid(),
              class: normalizeClass(d.class)
            }, [
              d.class !== "divider" ? (openBlock(), createElementBlock("span", Ql, toDisplayString(d.text || d.label), 1)) : createCommentVNode("", true)
            ], 2))
          ], 64))), 256))
        ], 512)
      ]),
      _: 3
    }, 8, ["style"])
  ], 4);
}
const cn = /* @__PURE__ */ F(Gl, [["render", ea], ["__scopeId", "data-v-48e80993"]]), ta = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: cn
}, Symbol.toStringTag, { value: "Module" })), na = (e) => (pushScopeId("data-v-75e575b0"), e = e(), popScopeId(), e), sa = { class: "dropdown-menu-wrap" }, ia = {
  key: 0,
  class: "item item-back"
}, oa = { class: "item-text" }, la = ["onClick"], aa = {
  key: 1,
  class: "p-[5px] w-[44px] float-left h-full"
}, ra = { class: "item-text" }, ua = ["onClick"], da = /* @__PURE__ */ na(() => /* @__PURE__ */ createBaseVNode("span", { class: "divider" }, null, -1)), ca = {
  key: 0,
  class: "item-text"
}, ha = ref(), fa = {
  name: "vu-dropdownmenu-items",
  components: { VuIcon: ue, VuImage: it, VuDropdownmenuItemsScrollable: cn },
  emits: ["update:responsive", "update:position", "click-item", "update:selected"],
  props: {
    target: {
      type: Object,
      required: false
    },
    items: {
      type: Array,
      required: true,
      validator: ss
    },
    selected: {
      type: Array,
      required: true
    },
    scrollable: {
      type: Boolean
    },
    zIndex: {
      type: Number,
      default: 1e3
    },
    responsive: {
      type: Boolean,
      default: false
    },
    dividedResponsiveItems: {
      type: Boolean,
      default: false
    },
    disableResponsive: {
      type: Boolean,
      default: false
    },
    maxItemsBeforeScroll: {
      required: true
    }
  },
  data: () => ({
    stack: [],
    left: false,
    uuid: Re,
    root: false,
    parent: {}
  }),
  computed: {
    classes() {
      return {
        "open-left": this.left,
        "responsive-menu": this.responsive
      };
    },
    _items() {
      return this.stack[this.stack.length - 1] || this.items;
    },
    _parent() {
      return (this.stack[this.stack.length - 2] || this.items).find((e) => JSON.stringify(e.items) === JSON.stringify(this._items));
    }
  },
  async mounted() {
    var i;
    if (this.disableResponsive)
      return;
    await this.$nextTick();
    const e = {
      root: this.target,
      threshold: 1
    }, n = ((i = this.target) == null ? void 0 : i.getBoundingClientRect()) || { right: window.right, left: 0 }, t = new IntersectionObserver(async ([o]) => {
      t.unobserve(this.$el);
      const s = o.target.getBoundingClientRect();
      n.right < s.right && !this.left ? (this.left = true, await this.$nextTick(), t.observe(this.$el)) : n.left > s.left && this.left && (this.$emit("update:responsive", true), this.$emit("update:position"));
    }, e);
    await this.$nextTick(), t.observe(this.$el);
  },
  methods: {
    toggleSelected(e) {
      const n = this.selected.slice(0);
      return e.selected || this.selected.includes(e) ? n.splice(this.selected.indexOf(e), 1) : n.push(e), n;
    },
    onItemClick(e) {
      e.disabled || ((e.selectable || e.selected || this.selected.includes(e)) && this.$emit("update:selected", this.toggleSelected(e)), this.$emit("click-item", e));
    },
    onNextItemClick(e) {
      this.responsive && this.stack.push(e.items);
    },
    onBackItemClick() {
      this.stack.pop();
    },
    itemHasSubmenus(e) {
      var n;
      return (n = e.items) == null ? void 0 : n.some(({ items: t }) => t !== void 0);
    }
  }
}, pa = /* @__PURE__ */ Object.assign(fa, {
  setup(e) {
    return (n, t) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["dropdown-menu dropdown-menu-root dropdown-root", n.classes]),
      style: normalizeStyle([{ zIndex: e.zIndex }]),
      ref_key: "self",
      ref: ha
    }, [
      createBaseVNode("ul", sa, [
        e.responsive && n.stack.length ? (openBlock(), createElementBlock("li", ia, [
          createVNode(ue, {
            icon: "left-open",
            class: "back-item",
            onClick: withModifiers(n.onBackItemClick, ["stop"])
          }, null, 8, ["onClick"]),
          createBaseVNode("span", oa, toDisplayString(n._parent.text), 1)
        ])) : createCommentVNode("", true),
        (openBlock(true), createElementBlock(Fragment, null, renderList(n._items, (i) => (openBlock(), createElementBlock(Fragment, null, [
          !i.class || !i.class.includes("header") && !i.class.includes("divider") ? (openBlock(), createElementBlock("li", {
            key: i.text || i.label,
            class: normalizeClass(["item", [{
              "item-submenu": i.items,
              selectable: !i.disabled && i.selectable || i.selected || e.selected.includes(i),
              selected: i.selected || e.selected.includes(i),
              hidden: i.hidden,
              disabled: i.disabled,
              "hide-responsive-divider": !e.dividedResponsiveItems
            }, i.class]]),
            onClick: withModifiers((o) => i.items && e.responsive && !e.dividedResponsiveItems ? n.onNextItemClick(i) : n.onItemClick(i), ["stop"])
          }, [
            renderSlot(n.$slots, "default", { item: i }, () => [
              i.fonticon ? (openBlock(), createBlock(ue, {
                key: 0,
                icon: i.fonticon,
                withinText: false
              }, null, 8, ["icon"])) : i.imageSource ? (openBlock(), createElementBlock("div", aa, [
                createVNode(it, {
                  src: i.imageSource,
                  aspectRatio: "1",
                  width: "34"
                }, null, 8, ["src"])
              ])) : createCommentVNode("", true),
              createBaseVNode("span", ra, toDisplayString(i.text || i.label), 1)
            ], true),
            i.items ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: "next-icon",
              onClick: withModifiers((o) => n.onNextItemClick(i), ["stop"])
            }, [
              da,
              createVNode(ue, { icon: "right-open" })
            ], 8, ua)) : createCommentVNode("", true),
            !e.responsive && i.items ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
              e.scrollable && !n.itemHasSubmenus(i) ? (openBlock(), createBlock(cn, mergeProps({ key: 0 }, { maxItemsBeforeScroll: e.maxItemsBeforeScroll, scrollable: e.scrollable, selected: e.selected, target: e.target }, {
                items: i.items,
                "z-index": e.zIndex + 1,
                onClickItem: n.onItemClick,
                "onUpdate:selected": t[0] || (t[0] = (o) => n.$emit("update:selected", o)),
                "onUpdate:responsive": t[1] || (t[1] = (o) => n.$emit("update:responsive", o)),
                "onUpdate:position": t[2] || (t[2] = () => {
                  var a;
                  const { left: o, top: s } = (a = n.$refs.self) == null ? void 0 : a.getBoundingClientRect();
                  n.$emit("update:position", { x: o, y: s });
                })
              }), null, 16, ["items", "z-index", "onClickItem"])) : (openBlock(), createBlock(Xt, mergeProps({ key: 1 }, { maxItemsBeforeScroll: e.maxItemsBeforeScroll, scrollable: e.scrollable, selected: e.selected, target: e.target }, {
                items: i.items,
                onClickItem: n.onItemClick,
                "onUpdate:selected": t[3] || (t[3] = (o) => n.$emit("update:selected", o)),
                "onUpdate:responsive": t[4] || (t[4] = (o) => n.$emit("update:responsive", o)),
                "onUpdate:position": t[5] || (t[5] = () => {
                  var a;
                  const { left: o, top: s } = (a = n.$refs.self) == null ? void 0 : a.getBoundingClientRect();
                  n.$emit("update:position", { x: o, y: s });
                })
              }), null, 16, ["items", "onClickItem"]))
            ], 64)) : createCommentVNode("", true)
          ], 10, la)) : (openBlock(), createElementBlock("li", {
            key: i.text || i.label || unref(Re)(),
            class: normalizeClass(i.class)
          }, [
            i.class !== "divider" ? (openBlock(), createElementBlock("span", ca, toDisplayString(i.text || i.label), 1)) : createCommentVNode("", true)
          ], 2))
        ], 64))), 256))
      ])
    ], 6));
  }
}), Xt = /* @__PURE__ */ F(pa, [["__scopeId", "data-v-75e575b0"]]), ma = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Xt
}, Symbol.toStringTag, { value: "Module" })), fi = ["top", "top-right", "bottom-right", "bottom", "bottom-left", "top-left"], pi = ({ intersectionRatio: e, elementRect: n, targetRect: t }) => e < 1 && (n.top < t.top || n.bottom > t.bottom), mi = (e, n, t, i) => {
  if (i.length === 1) {
    const o = i[0];
    return o.includes("top") ? o.replace("top", "bottom") : o.replace("bottom", "top");
  } else
    i.length > 1 && i.push(...fi);
  return n;
};
function vi(e, n = true) {
  let t = n;
  return e.forEach((i) => {
    !i.text && !i.label && (!i.class || !i.class.includes("divider")) && (t = false), i.items && (t = vi(i.items, t));
  }), t;
}
const va = {
  components: { VuDropdownmenuItems: Xt, VuPopover: Ge },
  name: "vu-dropdownmenu",
  mixins: [xt, Yn],
  emits: ["open", "close", "click-item", "update:modelValue"],
  props: {
    value: {
      type: Array,
      default: () => []
    },
    items: {
      type: Array,
      required: true,
      validator: vi
    },
    dividedResponsiveItems: {
      type: Boolean,
      default: false
    },
    position: {
      type: String,
      required: false,
      default: "bottom-left"
    },
    arrow: {
      type: Boolean,
      default: false
    },
    overlay: {
      type: Boolean,
      default: false
    },
    zIndex: {
      type: Number,
      default: () => 1e3
    },
    responsive: {
      type: Boolean,
      default: false
    },
    shift: {
      type: Boolean,
      default: false
    },
    closeOnClick: {
      type: Boolean,
      default: true
    },
    positions: {
      type: Array,
      required: false,
      default: () => fi
    },
    getNextPosition: {
      type: Function,
      required: false,
      default: mi
    },
    checkPosition: {
      type: Function,
      required: false,
      default: pi
    },
    ignoreEscapeKey: {
      type: Boolean,
      default: false
    },
    ignoreClickOutside: {
      type: Boolean,
      default: false
    },
    contentClass: {
      type: String
    },
    scrollable: {
      type: Boolean,
      default: false
    },
    itemHeight: {
      type: Number,
      default: 45
    },
    maxItemsBeforeScroll: {
      type: Number,
      default: Number.INFINITY
    }
  },
  data: () => ({
    innerResponsive: false
  }),
  computed: {
    isResponsive: {
      get() {
        return this.innerResponsive || this.responsive;
      },
      set(e) {
        this.innerResponsive = e;
      }
    },
    hasNotSubMenus() {
      var e;
      return !((e = this.items) != null && e.some(({ items: n }) => n !== void 0));
    },
    isScrollable() {
      return this.hasNotSubMenus && this.scrollable;
    }
  },
  watch: {
    async items() {
      var e;
      this.innerShow && (await this.$nextTick(), this.isScrollable && ((e = this.$refs.dropdownItems) == null || e.checkHeight()), this.$refs.popover.updatePosition());
    }
  },
  methods: {
    handleClick(e) {
      e.disabled || (e.handler && e.handler(e), this.$emit("click-item", e), this.updateShow(false));
    },
    updateShow(e) {
      e ? (this.isResponsive = false, this.$emit("open")) : this.closeOnClick && (this.innerShow = false, this.$emit("close"));
    }
  }
}, rt = /* @__PURE__ */ Object.assign(va, {
  setup(e) {
    const n = inject(ui, false);
    return (t, i) => (openBlock(), createBlock(Ge, {
      ref: "popover",
      show: t.innerShow,
      "onUpdate:show": [
        i[3] || (i[3] = (o) => t.innerShow = o),
        t.updateShow
      ],
      shift: e.shift || e.responsive,
      type: "dropdownmenu popover",
      attach: t.target,
      side: e.position,
      overlay: e.overlay || unref(n),
      animated: false,
      "check-position": unref(pi),
      "get-next-position": unref(mi),
      contentClass: e.contentClass,
      "ignore-click-outside": e.ignoreClickOutside,
      arrow: false,
      ignoreEscapeKey: e.ignoreEscapeKey
    }, {
      body: withCtx(() => [
        e.scrollable && t.hasNotSubMenus ? (openBlock(), createBlock(cn, mergeProps({ key: 0 }, { scrollable: e.scrollable, maxItemsBeforeScroll: e.maxItemsBeforeScroll }, {
          target: t.target,
          mainMenu: true,
          items: e.items,
          selected: e.value,
          onClickItem: t.handleClick,
          "onUpdate:selected": i[0] || (i[0] = (o) => t.$emit("update:modelValue", o))
        }), null, 16, ["target", "items", "selected", "onClickItem"])) : (openBlock(), createBlock(Xt, mergeProps({ key: 1 }, { scrollable: e.scrollable, maxItemsBeforeScroll: e.maxItemsBeforeScroll }, {
          responsive: t.isResponsive,
          "onUpdate:responsive": i[1] || (i[1] = (o) => t.isResponsive = o),
          "divided-responsive-items": e.dividedResponsiveItems,
          target: t.target,
          items: e.items,
          selected: e.value,
          onClickItem: t.handleClick,
          "onUpdate:selected": i[2] || (i[2] = (o) => t.$emit("update:modelValue", o))
        }), null, 16, ["responsive", "divided-responsive-items", "target", "items", "selected", "onClickItem"]))
      ]),
      default: withCtx(() => [
        renderSlot(t.$slots, "default", { active: t.innerShow })
      ]),
      _: 3
    }, 8, ["show", "shift", "attach", "side", "overlay", "check-position", "get-next-position", "contentClass", "ignore-click-outside", "ignoreEscapeKey", "onUpdate:show"]));
  }
}), ga = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: rt
}, Symbol.toStringTag, { value: "Module" })), os = {
  props: {
    active: {
      type: Boolean,
      default: () => false
    }
  }
}, ba = {
  name: "vu-icon-btn",
  mixins: [os, $e, Tt, Ys],
  components: { VuIcon: ue },
  props: {
    icon: {
      required: true,
      type: String
    },
    disableChevronResize: {
      default: false,
      type: Boolean
    },
    hover: {
      default: false,
      type: Boolean
    },
    noActive: {
      default: false,
      type: Boolean
    },
    noHover: {
      default: false,
      type: Boolean
    }
  }
};
function ya(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-icon-btn", [e.color, e.size, {
      active: e.active && !t.noActive,
      "no-active": t.noActive,
      hovered: !t.noHover && t.hover,
      "no-hover": t.noHover,
      disabled: e.disabled
    }]]),
    onClickCapture: n[0] || (n[0] = (u) => {
      e.disabled && u.stopPropagation();
    })
  }, [
    createVNode(a, {
      icon: t.icon,
      color: e.color,
      class: normalizeClass({ "chevron-menu-icon": t.icon === "chevron-down" && t.disableChevronResize, disabled: e.disabled })
    }, null, 8, ["icon", "color", "class"])
  ], 34);
}
const de = /* @__PURE__ */ F(ba, [["render", ya], ["__scopeId", "data-v-aab558fa"]]), _a$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: de
}, Symbol.toStringTag, { value: "Module" })), wa = {
  name: "vu-tile",
  inject: ["vuCollectionActions", "vuCollectionLazyImages", "lang", "vuTileEmphasizeText", "vuDateFormatWeekday", "vuDateFormatShort"],
  emits: ["click-action"],
  props: {
    /* eslint-disable vue/require-default-prop */
    id: {
      type: String
    },
    src: String,
    type: String,
    title: String,
    text: String,
    author: String,
    date: Date,
    customMetaData: String,
    status: Array,
    active: Boolean,
    actions: Array || String,
    selected: Boolean,
    selectable: Boolean,
    thumbnail: Boolean,
    hideStatusBar: Boolean
  },
  computed: {
    classes() {
      return {
        "tile--selectable": this.selectable || this.selected,
        "tile--selected": this.selected,
        "tile--active": this.active,
        "tile--thumbnail": this.thumbnail
      };
    },
    _actions() {
      return this.actions || this.vuCollectionActions;
    },
    contentClasses() {
      const e = "tile__content";
      return this.thumbnail ? this.meta ? `${e}__title--2rows` : `${e}__title--3rows` : this.meta && this.text ? this.vuTileEmphasizeText ? [
        `${e}__title--1row`,
        `${e}__text--2rows`
      ] : [
        `${e}__title--2row`,
        `${e}__text--1row`
      ] : (this.meta ? !this.text : this.text) ? [`${e}__title--3rows`, `${e}__text--1row`] : `${e}__title--4rows`;
    },
    meta() {
      return this.customMetaData || `${this.author || ""}${this.author && this.date ? " | " : ""}${this.dateFormat}`;
    },
    dateFormatOptions() {
      const e = {
        weekday: this.vuDateFormatShort ? "short" : "long",
        month: this.vuDateFormatShort ? "short" : "long",
        day: "numeric",
        year: "numeric"
      };
      return this.vuDateFormatWeekday || delete e.weekday, e;
    },
    dateFormat() {
      return this.date ? this.date.toLocaleDateString(this.lang, this.dateFormatOptions) : "";
    }
  },
  data() {
    return {
      started: false
    };
  },
  mounted() {
  },
  watch: {},
  methods: {},
  components: { VuImage: it, VuIcon: ue, VuIcon: ue, VuDropdownmenu: rt, VuStatusBar: Zn, VuIconBtn: de }
}, ka = { class: "tile-wrap" }, Sa = {
  key: 0,
  class: "tile__thumb"
}, Ia = {
  key: 1,
  class: "tile__image"
}, Ca = { class: "tile__title" }, Ba = { class: "inner" }, $a = {
  key: 0,
  class: "tile__meta"
}, Oa = { class: "inner" }, Ta = {
  key: 1,
  class: "tile__text"
}, xa = { class: "inner" }, Ma = {
  key: 2,
  class: "tile__action-icon"
};
function Va(e, n, t, i, o, s) {
  const a = resolveComponent("VuImage"), u = resolveComponent("VuIcon"), c = resolveComponent("vuIconBtn"), d = resolveComponent("VuDropdownmenu"), h2 = resolveComponent("VuIconBtn"), p2 = resolveComponent("VuStatusBar");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-tile", s.classes])
  }, [
    createBaseVNode("div", ka, [
      t.active ? (openBlock(), createElementBlock("div", Sa)) : createCommentVNode("", true),
      t.src ? (openBlock(), createElementBlock("div", Ia, [
        createVNode(a, {
          src: t.src,
          width: "80",
          height: "60",
          contain: "",
          "aspect-ratio": "1",
          lazy: s.vuCollectionLazyImages
        }, null, 8, ["src", "lazy"]),
        t.src && (t.selectable || t.selected) ? (openBlock(), createBlock(u, {
          key: 0,
          icon: "check",
          class: "tile__check"
        })) : createCommentVNode("", true)
      ])) : createCommentVNode("", true),
      createBaseVNode("div", {
        class: normalizeClass(["tile__content", s.contentClasses])
      }, [
        createBaseVNode("div", Ca, [
          t.type ? (openBlock(), createBlock(u, {
            key: 0,
            icon: t.type
          }, null, 8, ["icon"])) : createCommentVNode("", true),
          createBaseVNode("span", Ba, toDisplayString(t.title), 1)
        ]),
        s.meta ? (openBlock(), createElementBlock("div", $a, [
          createBaseVNode("span", Oa, toDisplayString(s.meta), 1)
        ])) : createCommentVNode("", true),
        t.text ? (openBlock(), createElementBlock("div", Ta, [
          createBaseVNode("span", xa, toDisplayString(t.text), 1)
        ])) : createCommentVNode("", true)
      ], 2),
      s._actions ? (openBlock(), createElementBlock("div", Ma, [
        s._actions.length > 1 ? (openBlock(), createBlock(d, {
          key: 0,
          items: s._actions,
          onClickItem: n[0] || (n[0] = (_) => e.$emit("click-action", { item: _, id: t.id }))
        }, {
          default: withCtx((_) => [
            createVNode(c, {
              icon: "chevron-down",
              class: normalizeClass(_)
            }, null, 8, ["class"])
          ]),
          _: 1
        }, 8, ["items"])) : (openBlock(), createBlock(h2, {
          key: 1,
          icon: s._actions[0].fonticon,
          onClick: n[1] || (n[1] = (_) => e.$emit("click-action", { item: _, id: t.id }))
        }, null, 8, ["icon"]))
      ])) : createCommentVNode("", true)
    ]),
    t.hideStatusBar ? createCommentVNode("", true) : (openBlock(), createBlock(p2, {
      key: 0,
      status: t.status
    }, null, 8, ["status"]))
  ], 2);
}
const gi = /* @__PURE__ */ F(wa, [["render", Va], ["__scopeId", "data-v-32fc46b9"]]), Pa = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: gi
}, Symbol.toStringTag, { value: "Module" })), La = {
  name: "vu-thumbnail",
  inject: ["vuCollectionLazyImages"],
  props: {
    /* eslint-disable vue/require-default-prop */
    id: {
      type: String,
      required: true
    },
    src: String,
    type: String,
    active: Boolean,
    actions: Array,
    title: String,
    text: String,
    selected: Boolean,
    selectable: Boolean,
    author: String,
    date: Date,
    customMetaData: String,
    status: Array,
    hideStatusBar: Boolean
  },
  data: () => ({
    getListenersFromAttrs: Ze
  }),
  computed: {
    classes() {
      return {
        "thumbnail--selectable": this.selectable || this.selected,
        "thumbnail--selected": this.selected,
        "thumbnail--active": this.active
      };
    }
  },
  components: { VuImage: it, VuIcon: ue, VuTile: gi, VuStatusBar: Zn }
}, Da = {
  class: "thumbnail-wrap",
  style: { position: "relative" }
}, Aa = {
  key: 0,
  class: "thumbnail__thumb"
}, Fa = { class: "thumbnail__content" };
function za(e, n, t, i, o, s) {
  const a = resolveComponent("VuImage"), u = resolveComponent("VuIcon"), c = resolveComponent("VuTile"), d = resolveComponent("VuStatusBar");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-thumbnail item", s.classes])
  }, [
    createBaseVNode("div", Da, [
      createVNode(a, {
        src: t.src,
        lazy: s.vuCollectionLazyImages,
        "aspect-ratio": "200/150",
        contain: ""
      }, null, 8, ["src", "lazy"]),
      t.active ? (openBlock(), createElementBlock("div", Aa)) : createCommentVNode("", true),
      t.selectable || t.selected ? (openBlock(), createBlock(u, {
        key: 1,
        icon: "check",
        class: "thumbnail__check"
      })) : createCommentVNode("", true),
      createVNode(c, {
        thumbnail: "",
        title: t.title,
        type: t.type,
        author: t.author,
        date: t.date,
        actions: t.actions,
        "custom-meta-data": t.customMetaData,
        "hide-status-bar": "",
        onClickAction: e.getListenersFromAttrs(e.$attrs).onClickAction
      }, null, 8, ["title", "type", "author", "date", "actions", "custom-meta-data", "onClickAction"]),
      createBaseVNode("div", Fa, toDisplayString(t.text), 1),
      t.hideStatusBar ? createCommentVNode("", true) : (openBlock(), createBlock(d, {
        key: 2,
        status: t.status
      }, null, 8, ["status"]))
    ])
  ], 2);
}
const Ea = /* @__PURE__ */ F(La, [["render", za], ["__scopeId", "data-v-86120de9"]]), Na = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ea
}, Symbol.toStringTag, { value: "Module" })), vt = {
  props: {
    loading: {
      type: Boolean,
      default: () => false
    }
  }
}, Ra = {
  name: "vu-accordion",
  mixins: [vt],
  props: {
    modelValue: {
      type: Array,
      default: () => []
    },
    items: {
      type: Number,
      default: () => 0
    },
    open: {
      type: Boolean,
      default: () => false
    },
    filled: {
      type: Boolean,
      default: () => false
    },
    divided: {
      type: Boolean,
      default: () => false
    },
    outlined: {
      type: Boolean,
      default: () => false
    },
    separated: {
      type: Boolean,
      default: () => false
    },
    animated: {
      type: Boolean,
      default: () => false
    },
    exclusive: {
      type: Boolean,
      default: () => false
    },
    keepRendered: {
      type: Boolean,
      default: () => false
    }
  },
  emits: ["update:modelValue"],
  data: () => ({
    guid: Re
  }),
  created() {
    if (this.open && !this.exclusive) {
      let e = this.items;
      const n = [];
      for (; e; )
        n.push(e--);
      this.$emit("update:modelValue", n);
    }
  },
  computed: {
    value() {
      return this.modelValue;
    }
  },
  methods: {
    toggle(e) {
      if (this.value.includes(e)) {
        const n = this.value.slice();
        n.splice(n.indexOf(e), 1), this.$emit("update:modelValue", n);
      } else
        this.exclusive ? this.$emit("update:modelValue", [e]) : this.$emit("update:modelValue", [e].concat(this.value || []));
    }
  }
}, Ha = { class: "accordion-container" }, ja = ["onClick"], Ua = /* @__PURE__ */ createBaseVNode("i", { class: "caret-left" }, null, -1), Wa = {
  key: 0,
  class: "content-wrapper"
};
function qa(e, n, t, i, o, s) {
  const a = resolveDirective("mask");
  return withDirectives((openBlock(), createElementBlock("div", Ha, [
    createBaseVNode("div", {
      class: normalizeClass(["accordion accordion-root", {
        filled: t.filled,
        "filled-separate": t.separated,
        divided: t.divided,
        styled: t.outlined,
        animated: t.animated
      }])
    }, [
      (openBlock(true), createElementBlock(Fragment, null, renderList(t.items, (u) => (openBlock(), createElementBlock("div", {
        key: `${e.guid}-accordion-${u}`,
        class: normalizeClass(["accordion-item", { active: s.value.includes(u) }])
      }, [
        createBaseVNode("div", {
          onClick: (c) => s.toggle(u),
          class: "accordion-title"
        }, [
          Ua,
          renderSlot(e.$slots, "title-" + u)
        ], 8, ja),
        t.keepRendered || s.value.includes(u) ? withDirectives((openBlock(), createElementBlock("div", Wa, [
          createBaseVNode("div", {
            class: normalizeClass(["content", { "accordion-animated-content": t.animated }])
          }, [
            renderSlot(e.$slots, "item-" + u)
          ], 2)
        ], 512)), [
          [vShow, s.value.includes(u)]
        ]) : createCommentVNode("", true)
      ], 2))), 128))
    ], 2)
  ])), [
    [a, e.loading]
  ]);
}
const Ka = /* @__PURE__ */ F(Ra, [["render", qa]]), Ga = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ka
}, Symbol.toStringTag, { value: "Module" })), ls = (e, ...n) => Object.fromEntries(
  n.filter((t) => t in e).map((t) => [t, e[t]])
), Ya = (e, ...n) => Object.fromEntries(
  n.filter(({ key: t }) => t in e).map(({ key: t, newName: i = t }) => [i, e[t]])
), Xa = (e) => (pushScopeId("data-v-c7fc0a0c"), e = e(), popScopeId(), e), Ja = { class: "vu-alert-dialog vu-alert-dialog-root" }, Za = { class: "vu-alert-dialog-content" }, Qa = /* @__PURE__ */ Xa(() => /* @__PURE__ */ createBaseVNode("hr", null, null, -1)), er = [
  Qa
], tr = { class: "vu-alert-dialog-body" }, nr = ["src"], sr = {
  key: 3,
  class: "vu-alert-dialog-title"
}, ir = {
  key: 4,
  class: "vu-alert-dialog-text"
}, or = { class: "vu-alert-dialog-buttons" }, lr = {
  name: "vu-alert-dialog"
}, ar = /* @__PURE__ */ defineComponent({
  ...lr,
  props: {
    title: {},
    text: {},
    icon: {},
    svg: {},
    svgUrl: {},
    img: {},
    iconCircle: { type: Boolean },
    iconColor: {},
    animate: { type: Boolean },
    animationDuration: {},
    noOverlay: { type: Boolean },
    emitCancelOnClickOutside: { type: Boolean },
    emitCancelOnCloseButtonClick: { type: Boolean },
    showRiskyButton: { type: Boolean },
    showConfirmButton: { type: Boolean },
    showCloseButton: { type: Boolean },
    riskyButtonLabel: {},
    confirmButtonLabel: {},
    closeButtonLabel: {},
    _show: { type: Boolean },
    lazy: { type: Boolean },
    src: {},
    height: {},
    maxHeight: {},
    maxWidth: {},
    minHeight: {},
    minWidth: {},
    width: {},
    contain: { type: Boolean },
    aspectRatio: {}
  },
  emits: ["close", "confirm", "cancel"],
  setup(e, { emit: n }) {
    const t = e, i = n, o = computed(() => ls(t, "height", "maxHeight", "maxWidth", "minHeight", "minWidth", "width", "contain", "aspectRatio")), s = inject(oi, "Confirm"), a = inject(li, "Close"), u = inject(ri, "Cancel"), c = inject(ai, "Proceed");
    return (d, h2) => {
      const p2 = resolveComponent("vu-icon"), _ = resolveComponent("vu-btn");
      return openBlock(), createElementBlock("div", Ja, [
        createVNode(Transition, { name: "fade" }, {
          default: withCtx(() => [
            !d.noOverlay && !(d.animate && !d._show) ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: "vu-overlay",
              onClick: h2[0] || (h2[0] = (v) => d.emitCancelOnClickOutside ? i("cancel") : i("close"))
            })) : createCommentVNode("", true)
          ]),
          _: 1
        }),
        createBaseVNode("div", {
          class: normalizeClass(["vu-alert-dialog-wrap", { "vu-alert-dialog--disposed": d.animate && !d._show }])
        }, [
          createBaseVNode("div", Za, [
            createBaseVNode("div", {
              class: "vu-alert-dialog-drag-handle",
              onClick: h2[1] || (h2[1] = (v) => d.emitCancelOnClickOutside ? i("cancel") : i("close"))
            }, er),
            createBaseVNode("div", tr, [
              renderSlot(d.$slots, "alert-content", {}, () => [
                d.img || d.src ? (openBlock(), createBlock(it, mergeProps({
                  key: 0,
                  class: "vu-alert-dialog-image"
                }, o.value, {
                  src: d.img || d.src
                }), null, 16, ["src"])) : d.svgUrl ? (openBlock(), createElementBlock("img", {
                  key: 1,
                  src: d.svgUrl,
                  style: { height: "120px !important" }
                }, null, 8, nr)) : d.icon || d.svg ? (openBlock(), createElementBlock("div", {
                  key: 2,
                  class: normalizeClass(["vu-alert-dialog-icon-wrap", [{ "vu-alert-dialog-icon-circle": d.iconCircle }, d.iconColor ? `vu-alert-dialog-icon-${d.iconColor}` : ""]])
                }, [
                  d.svg ? (openBlock(), createBlock(resolveDynamicComponent(d.svg), { key: 1 })) : (openBlock(), createBlock(p2, {
                    key: 0,
                    icon: d.icon,
                    "within-text": false
                  }, null, 8, ["icon"]))
                ], 2)) : createCommentVNode("", true),
                d.title ? (openBlock(), createElementBlock("div", sr, toDisplayString(d.title), 1)) : createCommentVNode("", true),
                d.text ? (openBlock(), createElementBlock("div", ir, toDisplayString(d.text), 1)) : createCommentVNode("", true)
              ], true),
              renderSlot(d.$slots, "alert-buttons", {}, () => [
                createBaseVNode("div", or, [
                  d.showConfirmButton ? (openBlock(), createBlock(_, {
                    key: 0,
                    color: "primary",
                    onClick: h2[2] || (h2[2] = (v) => i("confirm"))
                  }, {
                    default: withCtx(() => [
                      createTextVNode(toDisplayString(d.confirmButtonLabel || unref(s)), 1)
                    ]),
                    _: 1
                  })) : createCommentVNode("", true),
                  d.showRiskyButton ? (openBlock(), createBlock(_, {
                    key: 1,
                    color: "error",
                    onClick: h2[3] || (h2[3] = (v) => i("confirm"))
                  }, {
                    default: withCtx(() => [
                      createTextVNode(toDisplayString(d.riskyButtonLabel || unref(c)), 1)
                    ]),
                    _: 1
                  })) : createCommentVNode("", true),
                  d.showCloseButton ? (openBlock(), createBlock(_, {
                    key: 2,
                    onClick: h2[4] || (h2[4] = (v) => d.emitCancelOnCloseButtonClick ? i("cancel") : i("close"))
                  }, {
                    default: withCtx(() => [
                      createTextVNode(toDisplayString(d.closeButtonLabel || d.showRiskyButton && unref(u) || unref(a)), 1)
                    ]),
                    _: 1
                  })) : createCommentVNode("", true)
                ])
              ], true)
            ])
          ])
        ], 2)
      ]);
    };
  }
}), as = /* @__PURE__ */ F(ar, [["__scopeId", "data-v-c7fc0a0c"]]), rr = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: as
}, Symbol.toStringTag, { value: "Module" })), ur = {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 128 128"
}, dr = /* @__PURE__ */ createBaseVNode("path", { d: "M125.26 34.87 93.13 2.74C91.42 1.03 89.15 0 86.73 0H41.28c-2.42 0-4.69 1.03-6.4 2.74L2.74 34.87C1.03 36.58 0 38.85 0 41.27v45.45c0 2.42 1.03 4.69 2.74 6.4l32.13 32.13c1.71 1.71 3.98 2.74 6.4 2.74h45.45c2.42 0 4.69-1.03 6.4-2.74l32.13-32.13c1.71-1.71 2.74-3.98 2.74-6.4V41.27c0-2.42-1.03-4.69-2.74-6.4Zm-24.3 49.37-16.72 16.72L64 80.58l-20.24 20.38-16.72-16.72L47.42 64 27.04 43.76l16.72-16.72L64 47.42l20.24-20.38 16.72 16.72L80.58 64z" }, null, -1), cr = [
  dr
];
function hr(e, n) {
  return openBlock(), createElementBlock("svg", ur, [...cr]);
}
const fr = { render: hr }, pr = {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 128 128"
}, mr = /* @__PURE__ */ createBaseVNode("path", { d: "M64 0C28.65 0 0 28.65 0 64s28.65 64 64 64 64-28.65 64-64S99.35 0 64 0m9.14 109.71H54.85V47.02h18.29zM64 36.57c-5.05 0-9.14-4.09-9.14-9.14s4.09-9.14 9.14-9.14 9.14 4.09 9.14 9.14-4.09 9.14-9.14 9.14" }, null, -1), vr = [
  mr
];
function gr(e, n) {
  return openBlock(), createElementBlock("svg", pr, [...vr]);
}
const br = { render: gr }, yr = {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 128 128"
}, _r = /* @__PURE__ */ createBaseVNode("path", { d: "M64 0C28.65 0 0 28.65 0 64s28.65 64 64 64 64-28.65 64-64S99.35 0 64 0m9.14 111.02H54.85V92.73h18.29zm13.33-43.89c-5.83 4.34-12.1 7.15-13.32 15.15H54.86c.81-11.79 6.46-17.35 11.89-21.55 5.29-4.2 9.8-7.31 9.8-14.63 0-8.27-4.31-12.15-11.49-12.15-9.76 0-13.84 8.01-13.98 17.63H31.23c.41-19.38 13.12-33.57 32.91-33.57 25.62 0 33.7 15.82 33.7 26.25 0 13.15-5.53 18.38-11.36 22.86Z" }, null, -1), wr = [
  _r
];
function kr(e, n) {
  return openBlock(), createElementBlock("svg", yr, [...wr]);
}
const Sr = { render: kr }, Ir = {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 128 112.85"
}, Cr = /* @__PURE__ */ createBaseVNode("path", { d: "M128 105.8c0-1.18-.26-2.39-.91-3.53L70.14 3.53C68.78 1.18 66.38 0 64 0s-4.78 1.17-6.14 3.53L.91 102.27c-.66 1.14-.91 2.35-.91 3.53 0 3.69 2.93 7.05 7.05 7.05h113.89c4.12 0 7.05-3.36 7.05-7.05Zm-54.86-7.84c0 1.44-1.17 2.61-2.61 2.61H57.47c-1.44 0-2.61-1.17-2.61-2.61V84.9c0-1.44 1.17-2.61 2.61-2.61h13.06c1.44 0 2.61 1.17 2.61 2.61zm-1.3-26.12H56.17l-1.31-37.88c0-3.61 2.92-6.53 6.53-6.53h5.22c3.61 0 6.53 2.92 6.53 6.53l-1.31 37.88Z" }, null, -1), Br = [
  Cr
];
function $r(e, n) {
  return openBlock(), createElementBlock("svg", Ir, [...Br]);
}
const $s = { render: $r };
let bi = {
  _alerts: shallowReactive([])
};
function Or(e) {
  const n = shallowReactive([]), t = markRaw({
    _alerts: n,
    show(i) {
      return this.hide(), new Promise((o, s) => {
        const a = {
          id: Re(),
          component: as,
          bind: reactive({
            height: 120,
            ...i,
            contain: true,
            _show: true
          }),
          on: {
            close: () => {
              this.hide(a), o();
            },
            confirm: () => {
              this.hide(a), o();
            },
            cancel: () => {
              this.hide(a), s();
            }
          }
        };
        this._alerts.push(shallowReactive(a));
      });
    },
    hide(i) {
      if (i) {
        const o = this._alerts.find((s) => s.id === i.id);
        if (!o)
          return;
        o.bind._show = false, setTimeout(() => {
          const s = this._alerts.findIndex((a) => a.id === i.id);
          s > -1 && this._alerts.splice(s, 1);
        }, o.bind.animationDuration);
      } else
        this._alerts.forEach((o) => {
          o._show = false;
        }), this._alerts.splice(0, this._alerts.length);
    },
    information(i) {
      return this.show({
        showCloseButton: true,
        iconColor: "cyan",
        iconCircle: true,
        icon: "info",
        svg: br,
        animate: true,
        animationDuration: 300,
        ...i
      });
    },
    confirm(i) {
      return this.show({
        showCloseButton: true,
        showConfirmButton: true,
        iconColor: "cyan",
        iconCircle: true,
        icon: "help",
        svg: Sr,
        animate: true,
        animationDuration: 300,
        ...i,
        emitCancelOnClickOutside: true,
        emitCancelOnCloseButtonClick: true
      });
    },
    warning(i) {
      return this.show({
        iconColor: "orange",
        icon: "attention",
        svg: $s,
        iconCircle: true,
        showCloseButton: true,
        animate: true,
        animationDuration: 300,
        ...i
      });
    },
    confirmWithRisk(i) {
      return this.show({
        iconColor: "orange",
        icon: "attention",
        svg: $s,
        iconCircle: true,
        showRiskyButton: true,
        showCloseButton: true,
        animate: true,
        animationDuration: 300,
        ...i,
        emitCancelOnClickOutside: true,
        emitCancelOnCloseButtonClick: true
      });
    },
    error(i) {
      return this.show({
        iconColor: "red",
        iconCircle: true,
        icon: "error",
        svg: fr,
        showCloseButton: true,
        animate: true,
        animationDuration: 300,
        ...i
      });
    }
  });
  return bi = t, e.provide("vuAlertDialogAPI", t), e.config.globalProperties.$vuAlertDialog = t, t;
}
const Tr = {
  name: "vu-alert-dialog-container",
  components: {
    VuAlertDialog: as
  },
  data: () => ({
    _alerts: {
      type: Object
    }
  }),
  created() {
    this._alerts = bi._alerts;
  }
};
function xr(e, n, t, i, o, s) {
  return openBlock(true), createElementBlock(Fragment, null, renderList(e._alerts, (a) => (openBlock(), createBlock(resolveDynamicComponent(a.component), mergeProps({
    key: a.id
  }, a.bind, {
    modelValue: a.value,
    "onUpdate:modelValue": (u) => a.value = u
  }, toHandlers(a.on)), null, 16, ["modelValue", "onUpdate:modelValue"]))), 128);
}
const Mr = /* @__PURE__ */ F(Tr, [["render", xr]]), Vr = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Mr
}, Symbol.toStringTag, { value: "Module" })), Fe = {
  props: {
    modelValue: {
      type: [Object, String, Number, Array, Boolean, Date],
      default: () => ""
    },
    label: {
      type: String,
      default: () => ""
    },
    type: {
      type: String,
      default: () => "text"
    },
    helper: {
      type: String,
      default: () => ""
    },
    placeholder: {
      type: String,
      default: () => ""
    },
    options: {
      type: Array,
      default: () => []
    }
  },
  emits: ["update:modelValue"],
  computed: {
    value: {
      get() {
        return this.modelValue;
      },
      set(e) {
        this.$emit("update:modelValue", e);
      }
    }
  }
}, Pr = {
  name: "vu-btn",
  mixins: [vt, os, Tt, Fe, $e],
  props: {
    large: {
      type: Boolean,
      default: () => false
    },
    small: {
      type: Boolean,
      default: () => false
    },
    block: {
      type: Boolean,
      default: () => false
    },
    icon: {
      type: String,
      required: false
    }
  },
  data: () => ({
    getListenersFromAttrs: Ze
    // tooltip: {},
  }),
  components: { VuIcon: ue },
  computed: {
    classes() {
      return [
        `btn btn-${this.color}`,
        {
          "btn-sm": this.small,
          "btn-lg": this.large,
          "btn-block": this.block,
          active: this.active
        }
      ];
    }
  }
}, Lr = ["disabled"];
function Dr(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon"), u = resolveDirective("mask");
  return withDirectives((openBlock(), createElementBlock("button", mergeProps({
    type: "button",
    disabled: e.disabled
  }, toHandlers(e.getListenersFromAttrs(e.$attrs), true), { class: s.classes }), [
    t.icon ? (openBlock(), createBlock(a, {
      key: 0,
      icon: t.icon,
      color: "inherit"
    }, null, 8, ["icon"])) : createCommentVNode("", true),
    renderSlot(e.$slots, "default", {}, void 0, true)
  ], 16, Lr)), [
    [u, e.loading]
  ]);
}
const Ye = /* @__PURE__ */ F(Pr, [["render", Dr], ["__scopeId", "data-v-e48c0881"]]), Ar = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ye
}, Symbol.toStringTag, { value: "Module" })), Fr = { class: "vu-btn-dropdown flex flex-nowrap" }, zr = {
  key: 1,
  class: "caret text-grey-7"
}, Er = {
  name: "vu-btn-dropdown",
  components: { VuDropdownMenu: rt, VuBtn: Ye, VuIcon: ue, VuIconBtn: de }
}, Nr = /* @__PURE__ */ defineComponent({
  ...Er,
  props: {
    value: {},
    attach: {},
    position: {},
    shift: { type: Boolean },
    dividedResponsiveItems: { type: Boolean },
    color: {},
    icon: {},
    label: {},
    options: {},
    chevronDown: { type: Boolean }
  },
  emits: ["click", "click-item"],
  setup(e, { emit: n }) {
    const t = e, i = n;
    return (o, s) => (openBlock(), createElementBlock("div", Fr, [
      createVNode(Ye, {
        icon: t.icon,
        color: t.color,
        class: "flex-basis-auto",
        style: normalizeStyle(t.options && "border-top-right-radius:0;border-bottom-right-radius:0"),
        onClick: s[0] || (s[0] = (a) => i("click", a))
      }, {
        default: withCtx(() => [
          renderSlot(o.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(o.label), 1)
          ], true)
        ]),
        _: 3
      }, 8, ["icon", "color", "style"]),
      t.options ? (openBlock(), createBlock(rt, mergeProps({ key: 0 }, { ...t, items: o.options }, {
        class: "flex-basis-[38px] ml-[2px]",
        style: { display: "flex" },
        onClickItem: s[1] || (s[1] = (a) => i("click-item", a))
      }), {
        default: withCtx(({ active: a }) => [
          createVNode(Ye, {
            color: o.color,
            class: "dropdown_btn",
            active: a
          }, {
            default: withCtx(() => [
              o.chevronDown ? (openBlock(), createBlock(ue, {
                key: 0,
                icon: "chevron-down"
              })) : (openBlock(), createElementBlock("span", zr))
            ]),
            _: 2
          }, 1032, ["color", "active"])
        ]),
        _: 1
      }, 16)) : createCommentVNode("", true)
    ]));
  }
}), Rr = /* @__PURE__ */ F(Nr, [["__scopeId", "data-v-2532dc63"]]), Hr = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Rr
}, Symbol.toStringTag, { value: "Module" })), jr = {
  name: "vu-btn-grp",
  mixins: [vt],
  props: {
    color: {
      type: String,
      default: () => "default"
    }
  }
}, Ur = { class: "btn-grp" };
function Wr(e, n, t, i, o, s) {
  const a = resolveDirective("mask");
  return withDirectives((openBlock(), createElementBlock("div", Ur, [
    renderSlot(e.$slots, "default")
  ])), [
    [a, e.loading]
  ]);
}
const qr = /* @__PURE__ */ F(jr, [["render", Wr]]), Kr = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: qr
}, Symbol.toStringTag, { value: "Module" })), Gr = {
  name: "vu-carousel-slide",
  props: { title: { type: String, default: "" } },
  emits: ["slideclick", "slide-click"],
  data() {
    return {
      width: null,
      id: "",
      carousel: void 0,
      guid: Re
    };
  },
  created() {
    this.id = this.guid(), this.carousel = this.$parent;
  },
  mounted() {
    this.$isServer || this.$el.addEventListener("dragstart", (e) => e.preventDefault()), this.$el.addEventListener(
      this.carousel.isTouch ? "touchend" : "mouseup",
      this.onTouchEnd
    );
  },
  computed: {
    activeSlides() {
      const { currentPage: e = 0, breakpointSlidesPerPage: n, children: t } = this.carousel, i = [], o = t.filter(
        (a) => a.$el && a.$el.className.indexOf("vu-slide") >= 0
      ).map((a) => a._uid || a.id);
      let s = 0;
      for (; s < n; ) {
        const a = o[e * n + s];
        i.push(a), s++;
      }
      return i;
    },
    /**
     * `isActive` describes whether a slide is visible
     * @return {Boolean}
     */
    isActive() {
      return this.activeSlides.indexOf(this._uid) >= 0;
    },
    /**
     * `isCenter` describes whether a slide is in the center of all visible slides
     * if perPage is an even number, we quit
     * @return {Boolean}
     */
    isCenter() {
      const { breakpointSlidesPerPage: e } = this.carousel;
      return e % 2 === 0 || !this.isActive ? false : this.activeSlides.indexOf(this._uid) === Math.floor(e / 2);
    },
    /**
     * `isAdjustableHeight` describes if the carousel adjusts its height to the active slide(s)
     * @return {Boolean}
     */
    isAdjustableHeight() {
      const { adjustableHeight: e } = this.carousel;
      return e;
    }
  },
  methods: {
    onTouchEnd(e) {
      const n = this.carousel.isTouch && e.changedTouches && e.changedTouches.length > 0 ? e.changedTouches[0].clientX : e.clientX, t = this.carousel.dragStartX - n;
      (this.carousel.minSwipeDistance === 0 || Math.abs(t) < this.carousel.minSwipeDistance) && (this.$emit("slideclick", { ...e.currentTarget.dataset }), this.$emit("slide-click", { ...e.currentTarget.dataset }));
    }
  }
}, Yr = ["aria-hidden"];
function Xr(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-slide", {
      "vu-slide-active": s.isActive,
      "vu-slide-center": s.isCenter,
      "vu-slide-adjustableHeight": s.isAdjustableHeight
    }]),
    tabindex: "-1",
    "aria-hidden": !s.isActive,
    role: "tabpanel"
  }, [
    renderSlot(e.$slots, "default")
  ], 10, Yr);
}
const Jr = /* @__PURE__ */ F(Gr, [["render", Xr]]), Zr = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Jr
}, Symbol.toStringTag, { value: "Module" })), Qr = {
  props: {
    /**
     * Flag to enable autoplay
     */
    autoplay: {
      type: Boolean,
      default: false
    },
    /**
     * Time elapsed before advancing slide
     */
    autoplayTimeout: {
      type: Number,
      default: 3e3
    },
    /**
     * Flag to pause autoplay on hover
     */
    autoplayHoverPause: {
      type: Boolean,
      default: true
    },
    /**
     * Autoplay direction. User can insert backward to make autoplay move from right to left
     */
    autoplayDirection: {
      type: String,
      default: "forward"
    }
  },
  data() {
    return {
      autoplayInterval: null
    };
  },
  destroyed() {
    this.$isServer || (this.$el.removeEventListener("mouseenter", this.pauseAutoplay), this.$el.removeEventListener("mouseleave", this.startAutoplay));
  },
  methods: {
    pauseAutoplay() {
      this.autoplayInterval && (this.autoplayInterval = clearInterval(this.autoplayInterval));
    },
    startAutoplay() {
      this.autoplay && (this.autoplayInterval = setInterval(
        this.autoplayAdvancePage,
        this.autoplayTimeout
      ));
    },
    restartAutoplay() {
      this.pauseAutoplay(), this.startAutoplay();
    },
    autoplayAdvancePage() {
      this.advancePage(this.autoplayDirection);
    }
  },
  mounted() {
    !this.$isServer && this.autoplayHoverPause && (this.$el.addEventListener("mouseenter", this.pauseAutoplay), this.$el.addEventListener("mouseleave", this.startAutoplay)), this.startAutoplay();
  }
}, eu = (e, n, t) => {
  let i;
  return () => {
    const s = () => {
      i = null, e.apply(void 0);
    };
    clearTimeout(i), i = setTimeout(s, n);
  };
}, Sn = {
  onwebkittransitionend: "webkitTransitionEnd",
  onmoztransitionend: "transitionend",
  onotransitionend: "oTransitionEnd otransitionend",
  ontransitionend: "transitionend"
}, Os = () => {
  const e = Object.keys(Sn).find((n) => n in window);
  return e ? Sn[e] : Sn.ontransitionend;
}, tu = {
  name: "vu-carousel",
  emits: ["pageChange", "page-change", "update:modelValue", "navigation-click", "pagination-click", "transitionStart", "transition-start", "transitionEnd", "transition-end", "mounted"],
  beforeUpdate() {
    this.computeCarouselWidth();
  },
  data() {
    return {
      browserWidth: null,
      carouselWidth: 0,
      currentPage: 0,
      dragging: false,
      dragMomentum: 0,
      dragOffset: 0,
      dragStartY: 0,
      dragStartX: 0,
      isTouch: typeof window < "u" && "ontouchstart" in window,
      offset: 0,
      refreshRate: 16,
      slideCount: 0,
      transitionstart: "transitionstart",
      transitionend: "transitionend",
      currentHeight: "auto"
    };
  },
  mixins: [Qr],
  // use `provide` to avoid `Slide` being nested with other components
  provide() {
    return {
      carousel: this
    };
  },
  props: {
    /**
       *  Adjust the height of the carousel for the current slide
       */
    adjustableHeight: {
      type: Boolean,
      default: false
    },
    /**
       * Slide transition easing for adjustableHeight
       * Any valid CSS transition easing accepted
       */
    adjustableHeightEasing: {
      type: String,
      default: ""
    },
    /**
       *  Center images when the size is less than the container width
       */
    centerMode: {
      type: Boolean,
      default: false
    },
    /**
       * Slide transition easing
       * Any valid CSS transition easing accepted
       */
    easing: {
      type: String,
      validator(e) {
        return ["ease", "linear", "ease-in", "ease-out", "ease-in-out"].indexOf(e) !== -1 || e.includes("cubic-bezier");
      },
      default: "ease"
    },
    /**
       * Flag to make the carousel loop around when it reaches the end
       */
    loop: {
      type: Boolean,
      default: false
    },
    /**
       * Minimum distance for the swipe to trigger
       * a slide advance
       */
    minSwipeDistance: {
      type: Number,
      default: 8
    },
    /**
       * Flag to toggle mouse dragging
       */
    mouseDrag: {
      type: Boolean,
      default: true
    },
    /**
       * Flag to toggle touch dragging
       */
    touchDrag: {
      type: Boolean,
      default: true
    },
    /**
       * Flag to render pagination component
       */
    pagination: {
      type: Boolean,
      default: true
    },
    /**
       * Maximum number of slides displayed on each page
       */
    perPage: {
      type: Number,
      default: 1
    },
    /**
       * Configure the number of visible slides with a particular browser width.
       * This will be an array of arrays, ex. [[320, 2], [1199, 4]]
       * Formatted as [x, y] where x=browser width, and y=number of slides displayed.
       * ex. [1199, 4] means if (window <= 1199) then show 4 slides per page
       */
    // eslint-disable-next-line vue/require-default-prop
    perPageCustom: {
      type: Array
    },
    /**
       * Resistance coefficient to dragging on the edge of the carousel
       * This dictates the effect of the pull as you move towards the boundaries
       */
    resistanceCoef: {
      type: Number,
      default: 20
    },
    /**
       * Scroll per page, not per item
       */
    scrollPerPage: {
      type: Boolean,
      default: false
    },
    /**
       *  Space padding option adds left and right padding style (in pixels) onto vu-carousel-inner.
       */
    spacePadding: {
      type: Number,
      default: 0
    },
    /**
       *  Specify by how much should the space padding value be multiplied of, to re-arange the final slide padding.
       */
    spacePaddingMaxOffsetFactor: {
      type: Number,
      default: 0
    },
    /**
       * Slide transition speed
       * Number of milliseconds accepted
       */
    speed: {
      type: Number,
      default: 500
    },
    /**
       * Name (tag) of slide component
       * Overwrite when extending slide component
       */
    tagName: {
      type: String,
      default: "slide"
    },
    /**
       * Support for v-model functionality
       */
    modelValue: {
      type: Number,
      default: 0
    },
    /**
       * Support Max pagination dot amount
       */
    maxPaginationDotCount: {
      type: Number,
      default: -1
    }
  },
  watch: {
    value(e) {
      e !== this.currentPage && (this.goToPage(e), this.render());
    },
    currentPage(e) {
      this.$emit("pageChange", e), this.$emit("page-change", e), this.$emit("update:modelValue", e);
    },
    autoplay(e) {
      e === false ? this.pauseAutoplay() : this.restartAutoplay();
    }
  },
  computed: {
    children() {
      return this.$slots && this.$slots.default() && this.$slots.default().filter((e) => e.tag && e.tag.match(
        `^vue-component-\\d+-${this.tagName}$`
      ) !== null) || [];
    },
    /**
       * Given a viewport width, find the number of slides to display
       * @param  {Number} width Current viewport width in pixels
       * @return {Number} Number of slides to display
       */
    breakpointSlidesPerPage() {
      if (!this.perPageCustom)
        return this.perPage;
      const e = this.perPageCustom, n = this.browserWidth, i = e.sort(
        (s, a) => s[0] > a[0] ? -1 : 1
      ).filter((s) => n >= s[0]);
      return i[0] && i[0][1] || this.perPage;
    },
    /**
       * @return {Boolean} Can the slider move forward?
       */
    canAdvanceForward() {
      return this.loop || this.offset < this.maxOffset;
    },
    /**
       * @return {Boolean} Can the slider move backward?
       */
    canAdvanceBackward() {
      return this.loop || this.currentPage > 0;
    },
    /**
       * Number of slides to display per page in the current context.
       * This is constant unless responsive perPage option is set.
       * @return {Number} The number of slides per page to display
       */
    currentPerPage() {
      return !this.perPageCustom || this.$isServer ? this.perPage : this.breakpointSlidesPerPage;
    },
    /**
       * The horizontal distance the inner wrapper is offset while navigating.
       * @return {Number} Pixel value of offset to apply
       */
    currentOffset() {
      return this.isCenterModeEnabled ? 0 : (this.offset + this.dragOffset) * -1;
    },
    isHidden() {
      return this.carouselWidth <= 0;
    },
    /**
       * Maximum offset the carousel can slide
       * Considering the spacePadding
       * @return {Number}
       */
    maxOffset() {
      return Math.max(
        this.slideWidth * (this.slideCount - this.currentPerPage) - this.spacePadding * this.spacePaddingMaxOffsetFactor,
        0
      );
    },
    /**
       * Calculate the number of pages of slides
       * @return {Number} Number of pages
       */
    pageCount() {
      return this.scrollPerPage ? Math.ceil(this.slideCount / this.currentPerPage) : this.slideCount - this.currentPerPage + 1;
    },
    /**
       * Calculate the width of each slide
       * @return {Number} Slide width
       */
    slideWidth() {
      const e = this.carouselWidth - this.spacePadding * 2, n = this.currentPerPage;
      return e / n;
    },
    /**
       * @return {Boolean} Is navigation required?
       */
    isNavigationRequired() {
      return this.slideCount > this.currentPerPage;
    },
    /**
       * @return {Boolean} Center images when have less than min currentPerPage value
       */
    isCenterModeEnabled() {
      return this.centerMode && !this.isNavigationRequired;
    },
    transitionStyle() {
      const e = `${this.speed / 1e3}s`, n = `${e} ${this.easing} transform`;
      return this.adjustableHeight ? `${n}, height ${e} ${this.adjustableHeightEasing || this.easing}` : n;
    },
    padding() {
      const e = this.spacePadding;
      return e > 0 ? e : false;
    }
  },
  methods: {
    /**
       * @return {Number} The index of the next page
       * */
    getNextPage() {
      return this.currentPage < this.pageCount - 1 ? this.currentPage + 1 : this.loop ? 0 : this.currentPage;
    },
    /**
       * @return {Number} The index of the previous page
       * */
    getPreviousPage() {
      return this.currentPage > 0 ? this.currentPage - 1 : this.loop ? this.pageCount - 1 : this.currentPage;
    },
    /**
       * Increase/decrease the current page value
       * @param  {String} direction (Optional) The direction to advance
       */
    advancePage(e) {
      e === "backward" && this.canAdvanceBackward ? this.goToPage(this.getPreviousPage(), "navigation") : (!e || e !== "backward") && this.canAdvanceForward && this.goToPage(this.getNextPage(), "navigation");
    },
    goToLastSlide() {
      this.dragging = true, setTimeout(() => {
        this.dragging = false;
      }, this.refreshRate), this.$nextTick(() => {
        this.goToPage(this.pageCount);
      });
    },
    /**
       * A mutation observer is used to detect changes to the containing node
       * in order to keep the magnet container in sync with the height its reference node.
       */
    attachMutationObserver() {
      const e = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;
      if (e) {
        let n = {
          attributes: true,
          data: true
        };
        if (this.adjustableHeight && (n = {
          ...n,
          childList: true,
          subtree: true,
          characterData: true
        }), this.mutationObserver = new e(() => {
          this.$nextTick(() => {
            this.computeCarouselWidth(), this.computeCarouselHeight();
          });
        }), this.$parent.$el) {
          const t = this.$el.getElementsByClassName(
            "vu-carousel-inner"
          );
          for (let i = 0; i < t.length; i++)
            this.mutationObserver.observe(t[i], n);
        }
      }
    },
    handleNavigation(e) {
      this.advancePage(e), this.pauseAutoplay(), this.$emit("navigation-click", e);
    },
    /**
       * Stop listening to mutation changes
       */
    detachMutationObserver() {
      this.mutationObserver && this.mutationObserver.disconnect();
    },
    /**
       * Get the current browser viewport width
       * @return {Number} Browser"s width in pixels
       */
    getBrowserWidth() {
      return this.browserWidth = window.innerWidth, this.browserWidth;
    },
    /**
       * Get the width of the carousel DOM element
       * @return {Number} Width of the carousel in pixels
       */
    getCarouselWidth() {
      const e = this.$el.getElementsByClassName(
        "vu-carousel-inner"
      );
      for (let n = 0; n < e.length; n++)
        e[n].clientWidth > 0 && (this.carouselWidth = e[n].clientWidth || 0);
      return this.carouselWidth;
    },
    /**
       * Get the maximum height of the carousel active slides
       * @return {String} The carousel height
       */
    getCarouselHeight() {
      if (!this.adjustableHeight)
        return "auto";
      const e = this.currentPerPage * (this.currentPage + 1) - 1, n = [...Array(this.currentPerPage)].map((t, i) => this.getSlide(e + i)).reduce(
        (t, i) => Math.max(t, i && i.$el.clientHeight || 0),
        0
      );
      return this.currentHeight = n === 0 ? "auto" : `${n}px`, this.currentHeight;
    },
    /**
       * Filter slot contents to slide instances and return length
       * @return {Number} The number of slides
       */
    getSlideCount() {
      return this.children.length;
    },
    /**
       * Gets the slide at the specified index
       * @return {Object} The slide at the specified index
       */
    getSlide(e) {
      return this.children[e];
    },
    /**
       * Set the current page to a specific value
       * This function will only apply the change if the value is within the carousel bounds
       * for carousel scrolling per page.
       * @param  {Number} page The value of the new page number
       * @param  {string|undefined} advanceType An optional value describing the type of page advance
       */
    goToPage(e, n) {
      e >= 0 && e <= this.pageCount && (this.offset = this.scrollPerPage ? Math.min(this.slideWidth * this.currentPerPage * e, this.maxOffset) : this.slideWidth * e, this.autoplay && !this.autoplayHoverPause && this.restartAutoplay(), this.currentPage = e, n === "pagination" && (this.pauseAutoplay(), this.$emit("pagination-click", e)));
    },
    /**
       * Trigger actions when mouse is pressed
       * @param  {Object} e The event object
       */
    /* istanbul ignore next */
    onStart(e) {
      e.button !== 2 && (document.addEventListener(this.isTouch ? "touchend" : "mouseup", this.onEnd, true), document.addEventListener(this.isTouch ? "touchmove" : "mousemove", this.onDrag, true), this.startTime = e.timeStamp, this.dragging = true, this.dragStartX = this.isTouch ? e.touches[0].clientX : e.clientX, this.dragStartY = this.isTouch ? e.touches[0].clientY : e.clientY);
    },
    /**
       * Trigger actions when mouse is released
       * @param  {Object} e The event object
       */
    onEnd(e) {
      this.autoplay && !this.autoplayHoverPause && this.restartAutoplay(), this.pauseAutoplay();
      const n = this.isTouch ? e.changedTouches[0].clientX : e.clientX, t = this.dragStartX - n;
      if (this.dragMomentum = t / (e.timeStamp - this.startTime), this.minSwipeDistance !== 0 && Math.abs(t) >= this.minSwipeDistance) {
        const i = this.scrollPerPage ? this.slideWidth * this.currentPerPage : this.slideWidth;
        this.dragOffset += Math.sign(t) * (i / 2);
      }
      this.offset += this.dragOffset, this.dragOffset = 0, this.dragging = false, this.render(), document.removeEventListener(this.isTouch ? "touchend" : "mouseup", this.onEnd, true), document.removeEventListener(this.isTouch ? "touchmove" : "mousemove", this.onDrag, true);
    },
    /**
       * Trigger actions when mouse is pressed and then moved (mouse drag)
       * @param  {Object} e The event object
       */
    onDrag(e) {
      const n = this.isTouch ? e.touches[0].clientX : e.clientX, t = this.isTouch ? e.touches[0].clientY : e.clientY, i = this.dragStartX - n, o = this.dragStartY - t;
      if (this.isTouch && Math.abs(i) < Math.abs(o))
        return;
      e.stopImmediatePropagation(), this.dragOffset = i;
      const s = this.offset + this.dragOffset;
      s < 0 ? this.dragOffset = -Math.sqrt(-this.resistanceCoef * this.dragOffset) : s > this.maxOffset && (this.dragOffset = Math.sqrt(this.resistanceCoef * this.dragOffset));
    },
    onResize() {
      this.computeCarouselWidth(), this.computeCarouselHeight(), this.dragging = true, this.render(), setTimeout(() => {
        this.dragging = false;
      }, this.refreshRate);
    },
    render() {
      this.offset += Math.max(-this.currentPerPage + 1, Math.min(
        Math.round(this.dragMomentum),
        this.currentPerPage - 1
      )) * this.slideWidth;
      const e = this.scrollPerPage ? this.slideWidth * this.currentPerPage : this.slideWidth, n = e * Math.floor(this.slideCount / (this.currentPerPage - 1)), t = n + this.slideWidth * (this.slideCount % this.currentPerPage);
      this.offset > (n + t) / 2 ? this.offset = t : this.offset = e * Math.round(this.offset / e), this.offset = Math.max(0, Math.min(this.offset, this.maxOffset)), this.currentPage = this.scrollPerPage ? Math.round(this.offset / this.slideWidth / this.currentPerPage) : Math.round(this.offset / this.slideWidth);
    },
    /**
       * Re-compute the width of the carousel and its slides
       */
    computeCarouselWidth() {
      this.getSlideCount(), this.getBrowserWidth(), this.getCarouselWidth(), this.setCurrentPageInBounds();
    },
    /**
       * Re-compute the height of the carousel and its slides
       */
    computeCarouselHeight() {
      this.getCarouselHeight();
    },
    /**
       * When the current page exceeds the carousel bounds, reset it to the maximum allowed
       */
    setCurrentPageInBounds() {
      if (!this.canAdvanceForward && this.scrollPerPage) {
        const e = this.pageCount - 1;
        this.currentPage = e >= 0 ? e : 0, this.offset = Math.max(0, Math.min(this.offset, this.maxOffset));
      }
    },
    handleTransitionStart() {
      this.$emit("transitionStart"), this.$emit("transition-start");
    },
    handleTransitionEnd() {
      this.$emit("transitionEnd"), this.$emit("transition-end");
    }
  },
  mounted() {
    window.addEventListener(
      "resize",
      eu(this.onResize, this.refreshRate)
    ), (this.isTouch && this.touchDrag || this.mouseDrag) && this.$refs["vu-carousel-wrapper"].addEventListener(
      this.isTouch ? "touchstart" : "mousedown",
      this.onStart
    ), this.attachMutationObserver(), this.computeCarouselWidth(), this.computeCarouselHeight(), this.transitionstart = Os(), this.$refs["vu-carousel-inner"].addEventListener(this.transitionstart, this.handleTransitionStart), this.transitionend = Os(), this.$refs["vu-carousel-inner"].addEventListener(this.transitionend, this.handleTransitionEnd), this.$emit("mounted"), this.autoplayDirection === "backward" && this.goToLastSlide();
  },
  beforeUnmount() {
    this.detachMutationObserver(), window.removeEventListener("resize", this.getBrowserWidth), this.$refs["vu-carousel-inner"].removeEventListener(
      this.transitionstart,
      this.handleTransitionStart
    ), this.$refs["vu-carousel-inner"].removeEventListener(
      this.transitionend,
      this.handleTransitionEnd
    ), this.$refs["vu-carousel-wrapper"].removeEventListener(
      this.isTouch ? "touchstart" : "mousedown",
      this.onStart
    );
  }
}, nu = { class: "vu-carousel" }, su = {
  class: "vu-carousel-wrapper",
  ref: "vu-carousel-wrapper"
}, iu = {
  key: 0,
  class: "carousel-indicators"
}, ou = ["onClick"];
function lu(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", nu, [
    createBaseVNode("div", su, [
      createBaseVNode("div", {
        ref: "vu-carousel-inner",
        class: normalizeClass([
          "vu-carousel-inner",
          { "vu-carousel-inner--center": s.isCenterModeEnabled }
        ]),
        style: normalizeStyle({
          transform: `translate(${s.currentOffset}px, 0)`,
          transition: o.dragging ? "none" : s.transitionStyle,
          "ms-flex-preferred-size": `${s.slideWidth}px`,
          "webkit-flex-basis": `${s.slideWidth}px`,
          "flex-basis": `${s.slideWidth}px`,
          visibility: s.slideWidth ? "visible" : "hidden",
          height: `${o.currentHeight}`,
          "padding-left": `${s.padding}px`,
          "padding-right": `${s.padding}px`
        })
      }, [
        renderSlot(e.$slots, "default")
      ], 6)
    ], 512),
    t.pagination && s.pageCount > 1 ? (openBlock(), createElementBlock("ol", iu, [
      (openBlock(true), createElementBlock(Fragment, null, renderList(s.pageCount, (a, u) => (openBlock(), createElementBlock("li", {
        key: `carousel-pagination_${u}`,
        class: normalizeClass(["indicator", { active: u === o.currentPage }]),
        onClick: (c) => s.goToPage(u, "pagination")
      }, null, 10, ou))), 128))
    ])) : createCommentVNode("", true)
  ]);
}
const au = /* @__PURE__ */ F(tu, [["render", lu]]), ru = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: au
}, Symbol.toStringTag, { value: "Module" })), ze = {
  exposes: ["validate"],
  props: {
    rules: {
      type: [Array],
      default: () => [() => true]
    },
    required: {
      type: Boolean,
      default: () => false
    },
    success: {
      type: Boolean,
      default: () => false
    },
    lazyValidation: {
      type: Boolean,
      default: () => false
    }
  },
  data: () => ({
    errorBucket: [],
    valid: true,
    localRules: []
  }),
  inject: {
    vuDebug: {
      default: false
    }
  },
  watch: {
    value(e) {
      this.lazyValidation || (this.valid = this.validate(e));
    }
  },
  computed: {
    classes() {
      return {
        "has-error": !this.valid,
        "has-success": this.success && this.valid
      };
    },
    hasError() {
      return this.errorBucket.length > 0;
    },
    hasSuccess() {
      return this.errorBucket.length === 0;
    },
    isValid() {
      if (!this.required)
        return true;
      switch (typeof this.value) {
        case "string":
        case "array":
        case "number":
        case "date":
          return this.value.length !== 0;
        default:
          return true;
      }
    }
  },
  methods: {
    validate(e, n) {
      const t = [];
      let i = 0;
      const o = e || this.value, s = [...this.localRules, ...this.rules];
      for (let a = 0; a < s.length; a++) {
        const u = s[a], c = typeof u == "function" ? u(o) : u;
        typeof c == "string" ? (t.push(c), i += 1) : typeof c == "boolean" && !c ? i += 1 : typeof c != "boolean" && this.vuDebug && console.error(`Rules should return a string or boolean, received '${typeof c}' instead`, this);
      }
      return n || (this.errorBucket = t), this.valid = i === 0 && this.isValid, this.valid;
    }
  }
}, uu = {
  data: () => ({
    inputs: []
  }),
  exposes: ["validate"],
  provide() {
    return {
      inputs: this.inputs
    };
  },
  methods: {
    validate(e) {
      return this.inputs.map((n) => n.validate(void 0, e)).reduce((n, t) => n && t, true);
    }
  }
}, Ee = {
  inject: {
    inputs: {
      default: () => ""
    }
  },
  created() {
    typeof this.inputs == "object" && this.inputs.push(this);
  },
  beforeUnmount() {
    typeof this.inputs == "object" && this.inputs.splice(this.inputs.indexOf(this), 1);
  }
}, Ts = [...Array(256).keys()].map((e) => e.toString(16).padStart(2, "0")), gt = () => {
  const e = crypto.getRandomValues(new Uint8Array(16));
  return e[6] = e[6] & 15 | 64, e[8] = e[8] & 63 | 128, [...e.entries()].map(([n, t]) => [4, 6, 8, 10].includes(n) ? `-${Ts[t]}` : Ts[t]).join("");
}, du = {
  name: "vu-checkbox",
  mixins: [Fe, ze, Ee, $e],
  emits: ["update:modelValue"],
  inheritAttrs: false,
  props: {
    dense: {
      type: Boolean,
      default: () => false
    },
    switch: {
      type: Boolean,
      required: false
    },
    type: {
      type: String,
      default: () => "checkbox"
    }
  },
  data: () => ({ uid: gt() }),
  computed: {
    internalClasses() {
      return {
        "toggle-switch": this.type === "switch",
        "toggle-primary": ["checkbox", "radio", "dense"].includes(this.type)
      };
    }
  },
  methods: {
    input(e) {
      if (this.options.length > 1 && this.type !== "radio") {
        if (e.target.checked)
          return this.$emit("update:modelValue", [e.target.value].concat(this.value));
        const n = JSON.parse(JSON.stringify(this.value));
        return n.splice(this.value.indexOf(e.target.value), 1), this.$emit("update:modelValue", n);
      }
      return this.$emit("update:modelValue", e.target.checked ? e.target.value : null);
    },
    isChecked(e) {
      return Array.isArray(this.value) ? this.value.includes(e) : this.type === "radio" ? this.value === e : !!this.value;
    }
  }
}, cu = {
  key: 0,
  class: "control-label"
}, hu = {
  key: 0,
  class: "label-field-required"
}, fu = ["type", "id", "value", "disabled", "checked"], pu = ["innerHTML", "for"], mu = {
  key: 1,
  class: "form-control-helper-text"
};
function vu(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", { dense: t.dense }])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", cu, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", hu, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.options, (a, u) => (openBlock(), createElementBlock("div", {
      key: `${e.uid}-${a.value}-${u}`,
      class: normalizeClass(["toggle", s.internalClasses])
    }, [
      (openBlock(), createElementBlock("input", {
        type: t.type === "radio" ? "radio" : "checkbox",
        id: `${e.uid}-${a.value}-${u}`,
        value: a.value,
        disabled: e.disabled || a.disabled,
        checked: s.isChecked(a.value),
        key: s.isChecked(a.value),
        onClick: n[0] || (n[0] = withModifiers((...c) => s.input && s.input(...c), ["prevent"]))
      }, null, 8, fu)),
      createBaseVNode("label", {
        class: "control-label",
        innerHTML: a.label,
        for: `${e.uid}-${a.value}-${u}`
      }, null, 8, pu),
      renderSlot(e.$slots, "prepend-icon", { item: a }, void 0, true)
    ], 2))), 128)),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (a, u) => (openBlock(), createElementBlock("span", {
      key: `${u}-error-${a}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(a), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", mu, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const yi = /* @__PURE__ */ F(du, [["render", vu], ["__scopeId", "data-v-689207c6"]]), gu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: yi
}, Symbol.toStringTag, { value: "Module" }));
function bu(e, n = {}) {
  const {
    onVisibleChange: t = Be,
    onShow: i = Be,
    onHide: o = Be,
    attach: s,
    target: a
  } = n, u = computed(() => ye(e)), c = computed(() => ye(s)), d = computed(() => ye(a)), h2 = ref(true), p2 = ref(false), _ = ref({ x: 0, y: 0 }), v = () => setTimeout(() => p2.value = true, 10), y = () => {
    const q = !p2.value;
    p2.value = false, q && o();
  }, L = ni(u, { width: 0, height: 0 }, { box: "border-box" }), B = ln(c), R = computed(() => s !== document.body), ee = fl({
    includeScrollbar: false
  }), ce = computed(() => Math.max(B.top.value, 0)), Z = computed(() => Math.min(B.right.value, ee.width.value)), D = computed(() => {
    let [q, te, he, we] = [
      `${_.value.x}px`,
      `${_.value.y}px`,
      null,
      null
    ];
    const Se = _.value.x + L.width.value > Z.value, Ve = _.value.y + L.height.value > ee.height.value;
    if (Se && (q = `${B.right.value - (R.value ? 0 : _.value.x) - L.width.value}px`), Ve)
      if (B.height.value - _.value.y > 0) {
        const He = ee.height.value - _.value.y;
        _.value.y - ce.value < He ? [te, we] = [`${ce.value}px`, null] : [te, we] = [null, `${ee.height.value - ee.height.value}px`];
      } else
        [te, we] = [null, `${ee.height.value - _.value.y}px`];
    return {
      left: q,
      top: te,
      right: he,
      bottom: we
    };
  });
  function P() {
    var he;
    const q = [];
    let te = (he = ie(d)) == null ? void 0 : he.parentElement;
    for (; te; ) {
      const { overflow: we } = window.getComputedStyle(te), Se = we.split(" ");
      ["auto", "scroll"].some((Ve) => Se.includes(Ve)) && q.push(te), te = te.parentElement;
    }
    return q;
  }
  watch(h2, y), watch(p2, t);
  const z = [watchEffect(
    () => {
      const q = u.value;
      if (q) {
        q.style.position = "fixed", q.style.visibility = p2.value ? "visible" : "hidden";
        for (const [te, he] of Object.entries(D.value))
          q.style.setProperty(te, he);
      }
    },
    { flush: "post" }
  )], se = [], j = [], oe = () => {
    h2.value = false, nextTick(() => {
      z.concat(se, j).forEach((q) => q());
    });
  }, fe = (q) => {
    i(), !(!h2.value || q != null && q._prevent) && (q.preventDefault(), _.value = {
      x: q.clientX,
      y: q.clientY
    }, v(), q._prevent = true);
  }, Ie = watchEffect(() => {
    if (se.forEach((q) => q()), se.splice(0, se.length), p2.value && (se.push(
      pe("scroll", y),
      pe("click", y),
      pe("contextmenu", y, { capture: true })
    ), ie(c) && se.push(pe(ie(c), "scroll", y)), ie(d))) {
      const q = P();
      se.push(...q.map((te) => pe(te, "scroll", y)));
    }
  }), E = watchEffect(() => {
    j.forEach((q) => q()), j.splice(0, j.length), d ? j.push(pe(ie(d) || document.body, "contextmenu", fe)) : j.push(pe("contextmenu", fe));
  });
  return z.push(Ie, E), {
    visible: p2,
    position: _,
    enabled: h2,
    hide: y,
    show: v,
    stop: oe
  };
}
const yu = {
  name: "vu-contextual-dropdown"
}, _u = /* @__PURE__ */ defineComponent({
  ...yu,
  props: {
    /**
     * The area where the right-click will be listened to.
     * @default document.body
     */
    target: {
      type: Object,
      // [Boolean, String, Element],
      default: void 0
    },
    /**
     * Selected items.
     */
    value: {
      type: Array,
      default: () => []
    },
    /**
     * List of items to render.
     */
    items: {
      type: Array,
      required: true
    },
    /**
     * In responsive-mode, separates sub-menu open icon with item text.
     * Useful when an item with a sub-menu is selectable.
     */
    dividedResponsiveItems: {
      type: Boolean,
      default: false
    },
    /**
     * Allows to tweak z-Index value.
     */
    zIndex: {
      type: Number,
      default: () => 1e3
    },
    /**
     * Should the menu close on item click.
     */
    closeOnClick: {
      type: Boolean,
      default: true
    },
    /**
     * Prevents menu to position itself horizontally outside these boundaries.
     * @default document.body
     */
    attach: {
      type: [Boolean, String, Element, Object],
      default: void 0
    }
  },
  emits: ["close", "click-item"],
  setup(e, { expose: n, emit: t }) {
    const i = e, o = t, s = ref(false), a = ref(), u = computed(() => ye(i.target)), c = computed(() => ye(i.attach)), d = computed(() => (c == null ? void 0 : c.value) || document.body);
    function h2() {
      s.value = false;
    }
    function p2() {
      o("close", void 0);
    }
    const { position: _, visible: v, show: y, hide: L, stop: B } = bu(a, {
      attach: d,
      target: u,
      onShow: h2,
      onHide: p2
    });
    function R(ee) {
      ee.handler && ee.handler(ee), o("click-item", ee), i.closeOnClick && (L(), s.value = false);
    }
    return n({
      show: y,
      hide: L,
      stop: B
    }), (ee, ce) => (openBlock(), createBlock(Teleport, {
      to: d.value,
      disabled: !d.value
    }, [
      unref(v) ? (openBlock(), createBlock(Xt, mergeProps({
        key: 0,
        ref_key: "menu",
        ref: a,
        responsive: s.value,
        "onUpdate:responsive": ce[0] || (ce[0] = (Z) => s.value = Z),
        position: unref(_),
        "onUpdate:position": ce[1] || (ce[1] = (Z) => isRef(_) ? _.value = Z : null),
        "divided-responsive-items": e.dividedResponsiveItems
      }, {
        items: e.items,
        zIndex: e.zIndex
      }, {
        target: d.value,
        selected: e.value,
        onClickItem: R
      }), null, 16, ["responsive", "position", "divided-responsive-items", "target", "selected"])) : createCommentVNode("", true)
    ], 8, ["to", "disabled"]));
  }
}), wu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: _u
}, Symbol.toStringTag, { value: "Module" })), rs = (e) => e instanceof Date && !Number.isNaN(e.getTime()), ku = (e) => e % 4 === 0 && e % 100 !== 0 || e % 400 === 0, Su = (e, n) => [31, ku(e) ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][n], xs$1 = (e, n) => e.getTime() === n.getTime(), Iu = (e) => {
  let n;
  if (rs(e))
    n = e;
  else if (e && typeof e == "string")
    try {
      n = new Date(Date.parse(e));
    } catch {
    }
  return n;
}, hn = {
  emits: ["update:modelValue", "boundary-change"],
  props: {
    modelValue: {
      type: [null, Date, Array],
      default: null
    },
    min: {
      type: [Number, Date],
      default: () => -22089888e5
      // 1900-01-01Z00:00:00.000Z
    },
    max: {
      type: [Number, Date],
      default: () => 4102444799999
      // 2099-12-31T23:59:59.999Z
    }
  },
  data: () => ({
    getListenersFromAttrs: Ze
  }),
  watch: {
    min: {
      handler(e) {
        this.checkBoundary(e, "min");
      },
      immediate: true
    },
    max: {
      handler(e) {
        this.checkBoundary(e, "max");
      },
      immediate: true
    }
  },
  methods: {
    setBoundary(e, n) {
      return [
        n === "min" ? this.value[0] < e : this.value[0] > e,
        n === "min" ? this.value[1] < e : this.value[1] > e
      ].map((i, o) => i ? e : this.value[o]);
    },
    anyOutOfRange(e, n) {
      return this.value.some((t) => n === "min" ? t < e : t > e);
    },
    checkBoundary(e, n) {
      if (!this.value)
        return;
      const t = this.getListenersFromAttrs(this.$attrs)["boundary-change"] ? "boundary-change" : "update:modelValue";
      (Array.isArray(this.value) && this.anyOutOfRange(e, n) || ["min"].includes(n) && this.value < e || ["max"].includes(n) && this.value > e) && (rs(e) ? this.$emit(t, t === "update:modelValue" ? new Date(e) : { boundary: n, value: new Date(e) }) : this.$emit(t, t === "update:modelValue" ? this.setBoundary(e, n) : { boundary: n, value: e }));
    }
  }
}, Cu = {
  name: "vu-datepicker-table-date",
  mixins: [hn],
  emits: ["select"],
  props: {
    date: {
      type: Date
    },
    year: {
      type: Number,
      required: true
    },
    month: {
      type: Number,
      required: true
    },
    unselectableDaysOfWeek: {
      type: Array[Number],
      default: () => []
    },
    firstDay: {
      type: Number,
      default: () => 0
    },
    showWeekNumber: {
      type: Boolean,
      required: false
    },
    isRTL: {
      type: Boolean,
      required: false
    },
    // i18n
    weekdaysLabels: {
      type: Array,
      required: true
    },
    weekdaysShortLabels: {
      type: Array,
      required: true
    }
  },
  methods: {
    renderTable(e) {
      return h("table", {
        class: "datepicker-table",
        attrs: { cellspacing: "0", cellpadding: "0" }
      }, [
        this.renderHead(),
        this.renderBody(e)
      ]);
    },
    renderHead() {
      const e = [];
      for (let n = 0; n < 7; n++) {
        const t = h("th", {
          attrs: { scope: "col", cellspacing: "0", cellpadding: "0" }
        }, [
          h("abbr", {
            attrs: {
              title: this.renderDayName(n)
            }
          }, this.renderDayName(n, true))
        ]);
        e.push(t);
      }
      return h("thead", {}, e);
    },
    renderBody(e) {
      return h("tbody", {}, e);
    },
    renderWeek(e, n, t) {
      const i = new Date(t, 0, 1), o = Math.ceil(((new Date(t, n, e) - i) / 864e5 + i.getDay() + 1) / 7), s = `datepicker${this.week}`;
      return h("td", { class: s }, o);
    },
    renderDayName(e, n) {
      let t = e + this.firstDay;
      for (; t >= 7; )
        t -= 7;
      return n ? this.weekdaysShortLabels[t] : this.weekdaysLabels[t];
    },
    renderDay(e, n, t, i, o, s, a) {
      const u = [];
      return a ? h("td", { class: "is-empty" }) : (s && u.push("is-disabled"), o && u.push("is-today"), i && u.push("is-selected"), h("td", {
        class: u.join(" "),
        attrs: {
          "data-day": e
        }
      }, [
        h("button", {
          class: "datepicker-button datepicker-name",
          type: "button",
          "data-year": t,
          "data-month": n,
          "data-day": e,
          onClick: this.onSelect
        }, e)
      ]));
    },
    renderRow(e) {
      return h("tr", {}, e);
    },
    onSelect(e) {
      const n = e.target.getAttribute("data-year"), t = e.target.getAttribute("data-month"), i = e.target.getAttribute("data-day");
      this.$emit("select", new Date(n, t, i));
    }
  },
  render() {
    const e = /* @__PURE__ */ new Date();
    e.setHours(0, 0, 0, 0);
    const n = Su(this.year, this.month);
    let t = new Date(this.year, this.month, 1).getDay();
    const i = [];
    let o = [], s, a;
    for (this.firstDay > 0 && (t -= this.firstDay, t < 0 && (t += 7)), s = n + t, a = s; a > 7; )
      a -= 7;
    s += 7 - a;
    for (let u = 0, c = 0; u < s; u++) {
      const d = new Date(this.year, this.month, 1 + (u - t)), h2 = Date.parse(this.min), p2 = Date.parse(this.max), _ = h2 && d < h2 || p2 && d > p2 || this.unselectableDaysOfWeek && this.unselectableDaysOfWeek.indexOf(d.getDay()) > -1, v = rs(this.date) ? xs$1(d, this.date) : false, y = xs$1(d, e), L = u < t || u >= n + t;
      o.push(this.renderDay(1 + (u - t), this.month, this.year, v, y, _, L)), ++c === 7 && (this.showWeekNumber && o.unshift(this.renderWeek(u - t, this.month, this.year)), i.push(this.renderRow(o, this.isRTL)), o = [], c = 0);
    }
    return this.renderTable(i);
  }
}, Bu = {
  name: "vu-datepicker",
  mixins: [xt, hn],
  components: {
    "vu-datepicker-table-date": Cu
  },
  props: {
    className: { type: String, default: "" },
    modelValue: {
      type: [String, Date],
      default: () => ""
    },
    unselectableDaysOfWeek: {
      type: Array[Number],
      default: () => []
    },
    yearRange: {
      type: Number,
      default: () => 10
    },
    firstDay: {
      type: Number,
      default: () => 1
    },
    // i18n
    previousMonthLabel: {
      type: String,
      default: () => "Next Month"
    },
    nextMonthLabel: {
      type: String,
      default: () => "Previous Month"
    },
    monthsLabels: {
      type: Array,
      default: () => ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]
    },
    weekdaysLabels: {
      type: Array,
      default: () => ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
    },
    weekdaysShortLabels: {
      type: Array,
      default: () => ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"]
    },
    showWeekNumber: {
      type: Boolean,
      required: false
    },
    isRTL: {
      type: Boolean,
      required: false
    }
  },
  emits: ["update:modelValue"],
  data: () => ({
    left: 0,
    top: 38,
    month: 0,
    year: 0
  }),
  computed: {
    date: {
      get() {
        return this.modelValue;
      },
      set(e) {
        return this.$emit("update:modelValue", e);
      }
    },
    isEmpty() {
      return this.value === null || this.value === "" || this.value === void 0;
    },
    currentMonth() {
      return this.monthsLabels[this.month];
    },
    minYear() {
      return new Date(this.min).getFullYear();
    },
    minMonth() {
      return new Date(this.min).getMonth();
    },
    maxYear() {
      return new Date(this.max).getFullYear();
    },
    maxMonth() {
      return new Date(this.max).getMonth();
    },
    hasPrevMonth() {
      return !(this.year === this.minYear && (this.month === 0 || this.minMonth >= this.month));
    },
    hasNextMonth() {
      return !(this.year === this.maxYear && (this.month === 11 || this.maxMonth <= this.month));
    },
    selectableMonths() {
      return this.monthsLabels.map((e, n) => {
        const t = this.year === this.minYear && n < this.minMonth || this.year === this.maxYear && n > this.maxMonth;
        return {
          value: n,
          label: e,
          disabled: t
        };
      });
    },
    selectableYears() {
      const e = Math.max(this.year - this.yearRange, this.minYear), n = Math.min(this.year + 1 + this.yearRange, this.maxYear + 1);
      return Array(n - e).fill({}).map((i, o) => ({ value: e + o }));
    }
  },
  watch: {
    innerShow(e) {
      e && this.setCurrent();
    },
    value() {
      this.innerShow && this.setCurrent();
    },
    month(e) {
      e > 11 ? (this.year++, this.month = 0) : e < 0 && (this.month = 11, this.year--);
    }
  },
  methods: {
    setCurrent() {
      const e = Iu(this.date) || /* @__PURE__ */ new Date();
      this.month = e.getMonth(), this.year = e.getFullYear();
    },
    onSelect(e) {
      this.month = e.getMonth(), this.year = e.getFullYear(), this.date = e;
    }
  }
}, $u = { class: "datepicker-calendar" }, Ou = { class: "datepicker-title" }, Tu = { class: "datepicker-label" }, xu = ["disabled", "value"], Mu = { class: "datepicker-label" }, Vu = ["disabled", "value"];
function Pu(e, n, t, i, o, s) {
  const a = resolveComponent("vu-datepicker-table-date");
  return e.innerShow ? (openBlock(), createElementBlock("div", {
    key: 0,
    class: normalizeClass(["datepicker datepicker-root", t.className])
  }, [
    createBaseVNode("div", $u, [
      createBaseVNode("div", Ou, [
        createBaseVNode("div", Tu, [
          createTextVNode(toDisplayString(s.currentMonth) + " ", 1),
          withDirectives(createBaseVNode("select", {
            class: "datepicker-select datepicker-select-month",
            "onUpdate:modelValue": n[0] || (n[0] = (u) => e.month = u)
          }, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(s.selectableMonths, (u) => (openBlock(), createElementBlock("option", {
              key: u.value,
              disabled: u.disabled,
              value: u.value
            }, toDisplayString(u.label), 9, xu))), 128))
          ], 512), [
            [vModelSelect, e.month]
          ])
        ]),
        createBaseVNode("div", Mu, [
          createTextVNode(toDisplayString(e.year) + " ", 1),
          withDirectives(createBaseVNode("select", {
            class: "datepicker-select datepicker-select-year",
            "onUpdate:modelValue": n[1] || (n[1] = (u) => e.year = u)
          }, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(s.selectableYears, (u) => (openBlock(), createElementBlock("option", {
              key: u.value,
              disabled: u.disabled,
              value: u.value
            }, toDisplayString(u.value), 9, Vu))), 128))
          ], 512), [
            [vModelSelect, e.year]
          ])
        ]),
        createBaseVNode("button", {
          class: normalizeClass(["datepicker-prev", { "is-disabled": !s.hasPrevMonth }]),
          type: "button",
          onClick: n[2] || (n[2] = (u) => s.hasPrevMonth && e.month--)
        }, toDisplayString(t.previousMonthLabel), 3),
        createBaseVNode("button", {
          class: normalizeClass(["datepicker-next", { "is-disabled": !s.hasNextMonth }]),
          type: "button",
          onClick: n[3] || (n[3] = (u) => s.hasNextMonth && e.month++)
        }, toDisplayString(t.nextMonthLabel), 3)
      ]),
      createVNode(a, {
        date: s.date,
        year: e.year,
        month: e.month,
        min: e.min,
        max: e.max,
        "first-day": t.firstDay,
        "unselectable-days-of-week": t.unselectableDaysOfWeek,
        "months-labels": t.monthsLabels,
        "show-week-number": t.showWeekNumber,
        "is-r-t-l": t.isRTL,
        "weekdays-labels": t.weekdaysLabels,
        "weekdays-short-labels": t.weekdaysShortLabels,
        onSelect: n[4] || (n[4] = (u) => s.onSelect(u))
      }, null, 8, ["date", "year", "month", "min", "max", "first-day", "unselectable-days-of-week", "months-labels", "show-week-number", "is-r-t-l", "weekdays-labels", "weekdays-short-labels"])
    ])
  ], 2)) : createCommentVNode("", true);
}
const _i = /* @__PURE__ */ F(Bu, [["render", Pu]]), Lu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: _i
}, Symbol.toStringTag, { value: "Module" })), Du = { class: "vu-dropzone__span" }, Au = {
  name: "vu-dropzone"
}, Fu = /* @__PURE__ */ defineComponent({
  ...Au,
  props: {
    icon: { default: "drag-drop" },
    color: { default: "grey-light" },
    label: {},
    noHover: { type: Boolean },
    dashed: { type: Boolean, default: false },
    centered: { type: Boolean, default: false },
    addHoverClass: { type: Boolean, default: true },
    hoverClassName: {}
  },
  emits: ["drop", "dragover", "dragleave"],
  setup(e, { emit: n }) {
    const t = e, i = n, o = ref(false), s = ref(), a = computed(() => {
      var c;
      return (c = s.value) != null && c.clientHeight ? s.value.clientHeight % 4 : 0;
    }), u = computed(() => Number(a == null ? void 0 : a.value) !== Number.NaN && a.value / 2 || 0);
    return watch(() => t.noHover, (c) => {
      c && (o.value = false);
    }), (c, d) => (openBlock(), createElementBlock("div", {
      ref_key: "container",
      ref: s,
      class: normalizeClass(["vu-dropzone animated fade-in", [
        c.color && `vu-dropzone--${c.color}`,
        c.addHoverClass && o.value && c.hoverClassName,
        { "vu-dropzone--centered": c.centered },
        { "vu-dropzone--hover": o.value && !c.noHover },
        { "no-hover": c.noHover },
        "relative"
      ]]),
      onDragover: d[0] || (d[0] = (h2) => {
        (c.addHoverClass || !c.noHover) && (o.value = true), i("dragover", h2);
      }),
      onDragleave: d[1] || (d[1] = (h2) => {
        const p2 = h2 == null ? void 0 : h2.relatedTarget;
        s.value !== p2 && !s.value.contains(p2) && ((c.addHoverClass || !c.noHover) && (o.value = false), i("dragleave", h2));
      }),
      onDrop: d[2] || (d[2] = (h2) => {
        i("drop", h2), o.value = false;
      })
    }, [
      renderSlot(c.$slots, "default", {}, () => [
        createVNode(ue, {
          class: normalizeClass(["vu-dropzone__icon flex-grow-0", {
            centered: c.centered
          }]),
          icon: c.icon,
          withinText: false
        }, null, 8, ["class", "icon"]),
        createBaseVNode("span", Du, toDisplayString(c.label), 1)
      ], true),
      createBaseVNode("div", {
        class: normalizeClass([
          {
            "vu-dropzone--dashed": c.dashed
          },
          "absolute top-0 bottom-0 left-0 right-0"
        ]),
        style: normalizeStyle(u.value && `margin-top:${u.value}px;margin-bottom:${u.value}px` || "")
      }, null, 6)
    ], 34));
  }
}), kt = /* @__PURE__ */ F(Fu, [["__scopeId", "data-v-2cdef6d6"]]), zu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: kt
}, Symbol.toStringTag, { value: "Module" })), Eu = {
  name: "vu-facets-bar",
  emits: ["update:modelValue"],
  components: { VuDropdownMenu: rt, VuIconBtn: de, VuPopover: Ge, VuBtn: Ye, VuIcon: ue },
  props: {
    modelValue: {
      type: Object,
      default: () => {
      }
    },
    items: {
      type: Array,
      default: () => []
    }
  },
  data: () => ({
    uuidv4: gt,
    throttle: Pn,
    hideLabels: false,
    forceOverflow: false,
    showFromIndex: 0,
    hiddenFacets: 0,
    intersectionObserver: void 0,
    iconsIntersectionObserver: void 0,
    resizeObserver: void 0,
    onResizeThrottled: null,
    paddingForLongestFacet: 0
  }),
  mounted() {
    this.intersectionObserver = new IntersectionObserver(this.onLabelIntersects, {
      root: this.$refs.container,
      threshold: 1
    }), this.labelIntersectionObserver = new IntersectionObserver(this.onPaddingIntersects, {
      root: this.$refs.container
    }), this.iconsIntersectionObserver = new IntersectionObserver(this.onIconIntersects, {
      root: this.$refs.container,
      threshold: [0, 1],
      rootMargin: "0px -32px 0px 0px"
    }), this.onResizeThrottled = Pn(this.onResize.bind(this), 200), this.resizeObserver = new ResizeObserver(this.onResizeThrottled), this.intersectionObserver.observe(this.$refs.inner), this.resizeObserver.observe(this.$refs.container);
  },
  beforeUnmount() {
    this.intersectionObserver && this.intersectionObserver.disconnect(), this.labelIntersectionObserver && this.iconsIntersectionObserver.disconnect(), this.iconsIntersectionObserver && this.iconsIntersectionObserver.disconnect(), this.resizeObserver && this.resizeObserver.disconnect(), delete this.intersectionObserver, delete this.iconsIntersectionObserver;
  },
  computed: {
    visibleItems() {
      return this.hiddenFacets ? this.items.slice(this.showFromIndex, this.showFromIndex + this.visibleFacets) : this.items;
    },
    overflowMenuItems() {
      return this.items.map((e) => ({ ...e, fonticon: e.icon, item: e, selected: this.modelValue === e }));
    },
    visibleFacets() {
      return this.items.length - this.hiddenFacets;
    }
  },
  watch: {
    modelValue(e) {
      (this.hiddenFacets || this.forceOverflow) && this.showActiveFacet(e);
    },
    items(e, n) {
      ((e == null ? void 0 : e.length) !== (n == null ? void 0 : n.length) || e.some((t, i) => t.text !== n[i].text)) && this.onResize();
    }
  },
  methods: {
    // First check if all label display is possible
    async onLabelIntersects(e) {
      var n, t;
      this.intersectionObserver.unobserve(this.$refs.inner), ((n = e == null ? void 0 : e[0]) == null ? void 0 : n.intersectionRatio) < 1 && !this.hideLabels && (this.calcPaddingForLongestFacet(), this.hideLabels = true, await this.$nextTick(), (t = this.$refs) != null && t.labelPadding && this.labelIntersectionObserver.observe(this.$refs.labelPadding));
    },
    // Then check if the longest label will not lead to overflow
    async onPaddingIntersects(e) {
      const n = this.$refs.labelPadding;
      e != null && e[0] && !this.forceOverflow && e[0] && (this.labelIntersectionObserver.unobserve(n), e[0].intersectionRatio < 1 && (this.forceOverflow = true, this.paddingForLongestFacet = 0, await this.$nextTick(), this.$refs.facets.map(({ $el: i }) => i).forEach((i) => this.iconsIntersectionObserver.observe(i))));
    },
    // Finally check if all icons fit
    async onIconIntersects(e) {
      if (e != null && e.length) {
        const n = this.$refs.facets.map(({ $el: i }) => i), t = e.filter(({ target: i }) => i.parentNode).filter((i) => i.intersectionRatio < 1);
        this.hiddenFacets += t.length, n.forEach((i) => {
          try {
            this.iconsIntersectionObserver.unobserve(i);
          } catch {
          }
        });
      }
    },
    onResize() {
      this.showFromIndex = 0, this.hiddenFacets = 0, this.hideLabels = false, this.forceOverflow = false, this.paddingForLongestFacet = 0, this.intersectionObserver.observe(this.$refs.inner);
    },
    calcPaddingForLongestFacet() {
      const e = this.$refs.facets.map(({ $el: i }) => i), n = e.reduce((i, o) => Math.max(o.clientWidth, i), 0), t = e.find((i) => i.classList.contains("facet--selected"));
      this.paddingForLongestFacet = n - t.clientWidth + 38;
    },
    showActiveFacet(e) {
      const n = this.items.indexOf(e);
      let t = n;
      n > this.visibleFacets - 1 && (t = n - this.visibleFacets + 2), this.showFromIndex = Math.min(t, this.items.length - this.visibleFacets);
    }
  }
}, Nu = {
  class: "vu-facets-bar",
  ref: "container"
}, Ru = {
  class: "facets-bar__inner",
  ref: "inner"
};
function Hu(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon"), u = resolveComponent("VuPopover"), c = resolveComponent("VuBtn"), d = resolveComponent("VuIconBtn"), h2 = resolveComponent("VuDropdownMenu");
  return openBlock(), createElementBlock("div", Nu, [
    createBaseVNode("div", Ru, [
      (openBlock(true), createElementBlock(Fragment, null, renderList(s.visibleItems, (p2) => (openBlock(), createBlock(c, {
        key: `${e.uuidv4()}`,
        ref_for: true,
        ref: "facets",
        class: normalizeClass([
          "facet",
          {
            default: p2 !== t.modelValue,
            "facet--selected": p2 === t.modelValue,
            "facet--unselected": p2 !== t.modelValue
          }
        ]),
        style: normalizeStyle(e.hiddenFacets ? "max-width: calc(100% - 38px)" : ""),
        onClick: (_) => e.$emit("update:modelValue", p2)
      }, {
        default: withCtx(() => [
          (!e.hideLabels || p2 === t.modelValue) && !e.forceOverflow ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
            p2.icon ? (openBlock(), createBlock(a, {
              key: 0,
              icon: p2.icon,
              active: p2 === t.modelValue
            }, null, 8, ["icon", "active"])) : createCommentVNode("", true),
            createBaseVNode("span", null, toDisplayString(p2.text), 1)
          ], 64)) : (openBlock(), createBlock(u, {
            key: 1,
            type: "tooltip",
            hover: "",
            arrow: ""
          }, {
            default: withCtx(() => [
              p2.icon ? (openBlock(), createBlock(a, {
                key: 0,
                icon: p2.icon
              }, null, 8, ["icon"])) : createCommentVNode("", true)
            ]),
            body: withCtx(() => [
              createTextVNode(toDisplayString(p2.text), 1)
            ]),
            _: 2
          }, 1024))
        ]),
        _: 2
      }, 1032, ["class", "style", "onClick"]))), 128)),
      e.hiddenFacets || e.forceOverflow ? (openBlock(), createBlock(h2, {
        key: 0,
        shift: true,
        class: "vu-facets-bar__dropdownmenu",
        items: s.overflowMenuItems,
        model: t.modelValue,
        "onUpdate:modelValue": n[0] || (n[0] = (p2) => e.$emit("update:modelValue", p2.item)),
        onClickItem: n[1] || (n[1] = (p2) => e.$emit("update:modelValue", p2.item))
      }, {
        default: withCtx(() => [
          createVNode(d, { icon: "menu-dot" })
        ]),
        _: 1
      }, 8, ["items", "model"])) : createCommentVNode("", true),
      !e.hiddenFacets && e.hideLabels && !e.forceOverflow ? (openBlock(), createElementBlock("span", {
        key: 1,
        style: normalizeStyle([[{ width: `${e.paddingForLongestFacet}px` }], { display: "inline-block", height: "10px" }]),
        ref: "labelPadding"
      }, null, 4)) : createCommentVNode("", true)
    ], 512)
  ], 512);
}
const ju = /* @__PURE__ */ F(Eu, [["render", Hu], ["__scopeId", "data-v-4eaed4b2"]]), Uu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: ju
}, Symbol.toStringTag, { value: "Module" })), Wu = {
  name: "vu-form",
  mixins: [uu]
};
function qu(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("form", {
    novalidate: "novalidate",
    class: "form form-root",
    onSubmit: withModifiers(() => {
    }, ["prevent"])
  }, [
    renderSlot(e.$slots, "default")
  ], 32);
}
const wi = /* @__PURE__ */ F(Wu, [["render", qu]]), Ku = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: wi
}, Symbol.toStringTag, { value: "Module" })), Gu = {
  props: {
    elevated: {
      type: Boolean,
      default: false
    }
  }
}, ki = {
  props: {
    clearable: {
      type: Boolean,
      default: () => false
    }
  }
}, In = {
  offline: "status-empty",
  online: "status-ok",
  busy: "status-noway",
  away: "status-clock"
}, Yu = {
  name: "vu-user-picture",
  inject: [
    "vuUserPictureSrcUrl"
  ],
  props: {
    size: {
      type: String,
      default: "medium",
      validator: (e) => ["tiny", "small", "medium", "medium-1", "big", "bigger", "large", "extra-large"].includes(e)
    },
    circle: {
      type: Boolean,
      default: true
    },
    clickable: {
      type: Boolean,
      default: false
    },
    gutter: {
      type: Boolean,
      default: false
    },
    hoverable: {
      type: Boolean,
      default: false
    },
    inheritBackground: {
      type: Boolean,
      default: true
    },
    // eslint-disable-next-line vue/require-default-prop
    presence: {
      type: String,
      required: false,
      validator: (e) => e ? In[e] !== void 0 : true
    },
    src: {
      type: String,
      required: false,
      default: void 0
    },
    id: {
      type: String,
      required: false,
      default: void 0
    }
  },
  data: () => ({
    presenceStates: In,
    hovered: false
  }),
  watch: {
    hoverable: {
      // eslint-disable-next-line object-shorthand, func-names
      handler: function(e) {
        !e && this.hovered && (this.hovered = false);
      }
    }
  },
  computed: {
    fonticon() {
      return this.presence && In[this.presence];
    },
    _src() {
      return this.vuUserPictureSrcUrl && this.id && !this.src ? `${this.vuUserPictureSrcUrl}/${this.id}` : this.src;
    }
  }
}, Xu = {
  key: 0,
  class: "vu-user-picture__hover-mask"
}, Ju = {
  key: 1,
  class: "vu-presence"
};
function Zu(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-user-picture", [t.size ? `vu-user-picture--${t.size}` : "", {
      "vu-user-picture--gutter": t.gutter,
      "vu-user-picture--circle": t.circle,
      "vu-user-picture--clickable": t.clickable,
      "vu-user-picture--bg-inherit": t.inheritBackground
    }]]),
    onMouseover: n[0] || (n[0] = () => {
      t.hoverable && (e.hovered = true);
    }),
    onMouseleave: n[1] || (n[1] = () => {
      t.hoverable && (e.hovered = false);
    })
  }, [
    createBaseVNode("div", {
      class: "vu-user-picture-wrap",
      style: normalizeStyle([t.presence ? { background: "inherit" } : ""])
    }, [
      createBaseVNode("div", {
        class: "vu-user-picture__image",
        style: normalizeStyle({ "background-image": `url(${s._src})` })
      }, null, 4),
      e.hovered ? (openBlock(), createElementBlock("div", Xu)) : createCommentVNode("", true),
      t.size !== "tiny" ? (openBlock(), createElementBlock("div", Ju, [
        createBaseVNode("div", {
          class: normalizeClass(`vu-presence__indicator vu-presence__indicator--${t.presence}`)
        }, null, 2)
      ])) : createCommentVNode("", true)
    ], 4)
  ], 34);
}
const $t = /* @__PURE__ */ F(Yu, [["render", Zu], ["__scopeId", "data-v-a9791afa"]]), Qu = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $t
}, Symbol.toStringTag, { value: "Module" })), ed = {
  name: "vu-select-options",
  props: {
    options: {
      type: Array,
      required: true
    },
    multiple: {
      type: Boolean,
      required: false
    },
    user: {
      type: Boolean,
      required: false
    },
    selected: {
      type: Array,
      default: () => []
    },
    placeholder: {
      type: String,
      default: () => ""
    },
    keyIndex: {
      type: Number,
      default: () => -1
    },
    forceHasIcon: {
      type: Boolean
    },
    searchField: {
      type: Boolean
    }
  },
  expose: ["focus", "liRefs"],
  emits: ["click-item", "select-keydown", "change"],
  data: () => ({
    uid: gt,
    liRefs: {}
  }),
  computed: {
    anyHasIcon() {
      return this.forceHasIcon || this.options.some((e) => e.icon || e.fonticon);
    }
  },
  methods: {
    focus() {
      var e;
      (e = this.$refs.nativeSelect) == null || e.focus();
    }
  },
  components: { VuIcon: ue, VuIconBtn: de, VuUserPicture: $t }
}, td = ["label", "selected"], nd = ["value", "selected", "disabled"], sd = { class: "option__text" }, id = ["disabled", "onMouseenter", "onMouseleave", "onClick"], od = {
  key: 0,
  class: "flex"
}, ld = { class: "option__text" }, ad = { class: "option__text" };
function rd(e, n, t, i, o, s) {
  const a = resolveComponent("VuUserPicture"), u = resolveComponent("VuIcon"), c = resolveComponent("VuIconBtn"), d = resolveDirective("tooltip");
  return openBlock(), createElementBlock("ul", {
    class: normalizeClass(["vu-select-options", { "select-options--multiple": t.multiple, "select-options--single": !t.multiple, "select-options--user": t.user, "options--has-icon": s.anyHasIcon }])
  }, [
    createBaseVNode("select", {
      ref: "nativeSelect",
      class: "select-hidden",
      onKeydown: n[0] || (n[0] = (h2) => e.$emit("select-keydown", h2)),
      onChange: n[1] || (n[1] = () => {
        const h2 = e.$refs.nativeSelect.value;
        h2 === "__placeholder__" ? e.$emit("change", void 0) : e.$emit("change", h2);
      })
    }, [
      createBaseVNode("option", {
        value: "__placeholder__",
        label: t.placeholder,
        selected: t.selected[0] === void 0 || t.selected === ""
      }, null, 8, td),
      (openBlock(true), createElementBlock(Fragment, null, renderList(t.options, (h2) => (openBlock(), createElementBlock("option", {
        key: `${e.uid}-${h2.value || h2.label}`,
        value: h2.value || h2.label,
        selected: h2.selected || t.selected.includes(h2),
        disabled: h2.disabled
      }, toDisplayString(h2.label), 9, nd))), 128))
    ], 544),
    !t.multiple && t.placeholder ? (openBlock(), createElementBlock("li", {
      key: 0,
      class: normalizeClass([{ "option--selected": t.selected[0].value === void 0 }, "option__placeholder"]),
      onClick: n[2] || (n[2] = (h2) => e.$emit("click-item", { value: "" }))
    }, [
      createBaseVNode("span", sd, toDisplayString(t.placeholder), 1)
    ], 2)) : createCommentVNode("", true),
    (openBlock(true), createElementBlock(Fragment, null, renderList(t.options, (h2, p2, _) => (openBlock(), createElementBlock("li", {
      key: `${h2.id || e.uid()}`,
      ref_for: true,
      ref: (v) => {
        e.liRefs[h2.value] = v;
      },
      class: normalizeClass({
        "option--selected": h2.selected || t.selected.includes(h2),
        "option--keyboard": p2 === t.keyIndex,
        "option--hovered": _,
        "option--no-icon": s.anyHasIcon && !(h2.icon || h2.fonticon)
      }),
      disabled: h2.disabled,
      onMouseenter: (v) => _ = true,
      onMouseleave: (v) => _ = false,
      onClick: (v) => !h2.disabled && e.$emit("click-item", h2)
    }, [
      t.user ? (openBlock(), createElementBlock("div", od, [
        createVNode(a, {
          id: h2.value,
          class: "pt-6px",
          size: "small",
          src: h2.src
        }, null, 8, ["id", "src"]),
        createBaseVNode("span", ld, toDisplayString(h2.text || h2.label), 1)
      ])) : renderSlot(e.$slots, "default", {
        key: 1,
        item: h2
      }, () => [
        h2.fonticon || h2.icon ? (openBlock(), createBlock(u, {
          key: 0,
          icon: h2.fonticon || h2.icon,
          color: _ && "secondary" || "",
          size: "button"
        }, null, 8, ["icon", "color"])) : createCommentVNode("", true),
        createBaseVNode("span", ad, toDisplayString(h2.text || h2.label), 1),
        h2.action ? withDirectives((openBlock(), createBlock(c, {
          key: 1,
          icon: h2.action,
          color: _ && "secondary"
        }, null, 8, ["icon", "color"])), [
          [d, h2.actionTooltip]
        ]) : createCommentVNode("", true)
      ], true)
    ], 42, id))), 128))
  ], 2);
}
const us = /* @__PURE__ */ F(ed, [["render", rd], ["__scopeId", "data-v-b5de3623"]]), ud = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: us
}, Symbol.toStringTag, { value: "Module" })), dd = {
  name: "vu-select",
  inheritAttrs: false,
  mixins: [Fe, ki, $e, ze, Ee],
  props: {
    autocomplete: {
      type: Boolean,
      default: () => false
    },
    hidePlaceholderOption: {
      type: Boolean,
      default: () => false
    },
    grouped: {
      type: Boolean,
      default: () => false
    },
    maxVisible: {
      type: Number,
      default: () => 5
    },
    dropdownZIndex: {
      type: Number,
      default: 1020
    },
    // detachable props
    attach: {
      default: () => true,
      validator: Gn
    },
    contentClass: {
      type: [String, Object],
      default: ""
    },
    contentStyle: {
      type: [String, Object],
      default: () => ""
    }
    // end detachable
  },
  emits: ["update:modelValue"],
  inject: {
    isIos: {
      from: Yt
    }
  },
  data: () => ({
    open: false,
    focused: false,
    search: "",
    uid: gt()
  }),
  watch: {
    value() {
      this.search = this.selected.label;
    },
    open(e) {
      e && this.focus();
    }
  },
  created() {
    this.search = this.value && this.selected.label || this.value;
  },
  computed: {
    hasSomeEnabledOptions() {
      return this.enabledOptions.length > 0;
    },
    firstEnabledOption() {
      return this.enabledOptions.slice(0)[0];
    },
    lastEnabledOption() {
      return this.enabledOptions.slice(-1)[0];
    },
    enabledOptions() {
      return (this.autocomplete && this.search ? this.options : this.innerOptions).filter((n) => !n.disabled);
    },
    innerOptions() {
      return this.autocomplete ? this.options.filter((e) => e.label.toLowerCase().includes(this.search.toLowerCase()) || e.value.toLowerCase().includes(this.search.toLowerCase())) : this.options;
    },
    selected() {
      return this.options.find((e) => e.value === this.value) || {
        label: this.placeholder
      };
    },
    willDetach() {
      return this.attach === false || this.attach !== "" && typeof this.attach == "string";
    },
    groupedOptions() {
      return this.grouped ? this.options.reduce((e, n) => (e[n.group] || (e[n.group] = []), e[n.group].push(n), e), {}) : null;
    },
    internMaxVisible() {
      return this.maxVisible > this.options.length ? this.options.length : this.maxVisible;
    }
  },
  methods: {
    stop(e) {
      e.preventDefault(), e.stopPropagation();
    },
    innerSelectKeydown(e) {
      switch (e.code) {
        case "Space":
        case "Enter":
        case "NumpadEnter":
          this.open = !this.open, this.stop(e);
          break;
        case "Escape":
          this.open = false, this.stop(e);
          break;
        case "ArrowUp":
          this.browse(void 0, e);
          break;
        case "ArrowDown":
          this.open ? this.browse(true, e) : (this.open = true, this.stop(e));
          break;
      }
    },
    focus() {
      var e, n;
      this.focused = true, !(this.autocomplete || this.isIos) && (this.willDetach ? setTimeout(() => {
        var t, i;
        (i = (t = this.$refs) == null ? void 0 : t.selectOptions) == null || i.focus();
      }, 50) : (n = (e = this.$refs) == null ? void 0 : e.nativeSelect) == null || n.focus());
    },
    blur() {
      this.focused = false;
    },
    async browse(e, n) {
      this.grouped || (!e && this.selected === this.firstEnabledOption ? (this.value = this.hidePlaceholderOption ? this.lastEnabledOption.value : void 0, this.stop(n), this.scrollIntoView()) : e && this.selected === this.lastEnabledOption ? (this.value = this.hidePlaceholderOption ? this.firstEnabledOption.value : void 0, this.stop(n), this.scrollIntoView()) : this.modelValue || (this.value = e ? this.firstEnabledOption.value : this.lastEnabledOption.value, this.stop(n), this.scrollIntoView()));
    },
    scrollIntoView() {
      this.$nextTick(() => {
        var t;
        const e = this.$refs && this.$refs.dropdown;
        let n;
        if (e && (n = (t = this.$refs) == null ? void 0 : t.dropdown.querySelector("ul li.result-option-selected")), n) {
          const i = n.offsetTop + n.clientHeight;
          (i > e.scrollTop + e.clientHeight || i < e.scrollTop) && this.$refs.dropdown.scrollTo({ top: n.offsetTop });
        }
      });
    }
  },
  components: { VuIconBtn: de, VuPopover: Ge, VuSelectOptions: us, VuScroller: Qe }
}, cd = {
  key: 0,
  class: "control-label"
}, hd = {
  key: 0,
  class: "label-field-required"
}, fd = ["disabled", "placeholder"], pd = {
  key: 2,
  class: "select-handle"
}, md$1 = ["disabled"], vd = ["label"], gd = ["value", "selected", "disabled"], bd = {
  key: 4,
  class: "select-handle"
}, yd = {
  key: 5,
  class: "select-choices form-control"
}, _d = { class: "select-choice" }, wd = { class: "select-results" }, kd = ["onClick"], Sd = { class: "result-group-label" }, Id = { class: "result-group-sub" }, Cd = ["onClick"], Bd = {
  key: 1,
  class: "form-control-helper-text"
};
function $d(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconBtn"), u = resolveComponent("VuSelectOptions"), c = resolveComponent("VuScroller"), d = resolveComponent("VuPopover"), h2 = resolveDirective("click-outside");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", e.classes])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", cd, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", hd, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    withDirectives((openBlock(), createElementBlock("div", {
      onClick: n[10] || (n[10] = (p2) => {
        e.open = !e.open && !e.disabled, e.search = e.value && s.selected.label || e.value;
      }),
      class: normalizeClass([
        "vu-select",
        "select",
        {
          "select-placeholder": !t.autocomplete,
          "select-no-placeholder-option": t.hidePlaceholderOption,
          "select-not-chosen": !t.autocomplete && !e.value,
          "dropdown-visible": e.open,
          "select-disabled": e.disabled,
          "select-autocomplete": t.autocomplete,
          "select-clearable": e.clearable,
          "select-focus": e.focused && !e.disabled
        }
      ])
    }, [
      t.autocomplete ? withDirectives((openBlock(), createElementBlock("input", {
        key: 0,
        ref: "innerInput",
        disabled: e.disabled,
        placeholder: s.selected.label,
        class: "form-control",
        "onUpdate:modelValue": n[0] || (n[0] = (p2) => e.search = p2)
      }, null, 8, fd)), [
        [vModelText, e.search]
      ]) : createCommentVNode("", true),
      e.value && (t.autocomplete || e.clearable) ? (openBlock(), createBlock(a, {
        key: 1,
        icon: "clear",
        class: normalizeClass(["select__clear-icon", { "select--has-handle": t.autocomplete }]),
        onClick: n[1] || (n[1] = (p2) => {
          var _, v;
          e.$emit("update:modelValue", ""), (v = (_ = e.$refs) == null ? void 0 : _.innerInput) == null || v.focus(), e.search = "";
        })
      }, null, 8, ["class"])) : createCommentVNode("", true),
      t.autocomplete ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", pd)),
      !t.autocomplete && !s.willDetach ? (openBlock(), createElementBlock("select", {
        key: 3,
        class: "form-control select-hidden",
        disabled: e.disabled,
        ref: "nativeSelect",
        onFocus: n[2] || (n[2] = (p2) => e.focused = true),
        onBlur: n[3] || (n[3] = (p2) => s.blur()),
        onChange: n[4] || (n[4] = () => {
          const p2 = e.$refs.nativeSelect.value;
          p2 === "__placeholder__" ? e.value = void 0 : e.value = p2, s.scrollIntoView();
        }),
        onKeydown: n[5] || (n[5] = (p2) => s.innerSelectKeydown(p2))
      }, [
        createBaseVNode("option", {
          value: "__placeholder__",
          label: e.placeholder
        }, null, 8, vd),
        (openBlock(true), createElementBlock(Fragment, null, renderList(s.innerOptions, (p2) => (openBlock(), createElementBlock("option", {
          key: `${e.uid}-${p2.value || p2.label}`,
          value: p2.value || p2.label,
          selected: p2.value === e.value,
          disabled: p2.disabled
        }, toDisplayString(p2.label), 9, gd))), 128))
      ], 40, md$1)) : createCommentVNode("", true),
      t.autocomplete ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", bd)),
      t.autocomplete ? createCommentVNode("", true) : (openBlock(), createElementBlock("ul", yd, [
        createBaseVNode("li", _d, toDisplayString(s.selected.label), 1)
      ])),
      t.attach && e.open ? (openBlock(), createElementBlock("div", {
        key: 6,
        class: "select-dropdown",
        ref: "dropdown",
        style: normalizeStyle(`height: ${38 * (s.innerOptions.length + (!t.autocomplete && !t.hidePlaceholderOption ? 1 : 0))}px; max-height: ${38 * (s.internMaxVisible + 1)}px;`)
      }, [
        createBaseVNode("ul", wd, [
          !t.autocomplete && !t.hidePlaceholderOption ? (openBlock(), createElementBlock("li", {
            key: 0,
            class: normalizeClass(["result-option result-option-placeholder", { "result-option-selected": !e.modelValue }]),
            onClick: n[6] || (n[6] = (p2) => {
              e.$emit("update:modelValue", ""), e.search = "";
            })
          }, toDisplayString(e.placeholder), 3)) : createCommentVNode("", true),
          t.grouped ? (openBlock(true), createElementBlock(Fragment, { key: 2 }, renderList(s.groupedOptions, (p2, _) => (openBlock(), createElementBlock("li", {
            key: `${e.uid}-${p2.group}`,
            class: "result-group"
          }, [
            createBaseVNode("span", Sd, toDisplayString(_), 1),
            createBaseVNode("ul", Id, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(p2, (v) => (openBlock(), createElementBlock("li", {
                key: `${e.uid}-${v.value}`,
                class: normalizeClass([{
                  "result-option-disabled": v.disabled,
                  "result-option-selected": v.value === e.value
                }, "result-option"]),
                onClick: (y) => v.disabled ? null : e.$emit("update:modelValue", v.value)
              }, toDisplayString(v.label), 11, Cd))), 128))
            ])
          ]))), 128)) : (openBlock(true), createElementBlock(Fragment, { key: 1 }, renderList(s.innerOptions, (p2) => (openBlock(), createElementBlock("li", {
            key: `${e.uid}-${p2.value || p2.label}`,
            class: normalizeClass([{
              "result-option-disabled": p2.disabled,
              "result-option-selected": p2.value === e.value
            }, "result-option"]),
            onClick: (_) => {
              p2.disabled || e.$emit("update:modelValue", p2.value), e.search = p2.label;
            }
          }, toDisplayString(p2.label), 11, kd))), 128))
        ])
      ], 4)) : s.willDetach && e.open ? (openBlock(), createBlock(d, {
        key: 7,
        attach: t.attach,
        type: "vu-select-dropdown",
        show: e.open,
        positions: ["bottom-left", "top-left"],
        side: "bottom-left",
        "sync-width": true,
        animated: false,
        "content-class": t.contentClass,
        offsets: { "bottom-left": { y: 3 }, "top-left": { y: -43 } },
        "content-style": [{ zIndex: t.dropdownZIndex }, "position: absolute;", t.contentStyle],
        "onUpdate:show": n[9] || (n[9] = (p2) => {
          e.open = p2;
        })
      }, {
        body: withCtx(() => [
          createVNode(c, { "always-show": "" }, {
            default: withCtx(() => [
              createVNode(u, mergeProps({ ref: "selectOptions" }, { options: s.innerOptions, selected: [s.selected], placeholder: e.placeholder }, {
                onChange: n[7] || (n[7] = (p2) => e.value = p2),
                onSelectKeydown: s.innerSelectKeydown,
                onClickItem: n[8] || (n[8] = (p2) => {
                  this.focus(), e.$emit("update:modelValue", p2.value);
                })
              }), null, 16, ["onSelectKeydown"])
            ]),
            _: 1
          })
        ]),
        _: 1
      }, 8, ["attach", "show", "content-class", "content-style"])) : createCommentVNode("", true)
    ], 2)), [
      [h2, {
        events: ["click", "contextmenu"],
        handler: function() {
          e.open = false, e.search = e.value && s.selected.label || e.value;
        }
      }]
    ]),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (p2, _) => (openBlock(), createElementBlock("span", {
      key: `${_}-error-${p2}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(p2), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", Bd, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const Si = /* @__PURE__ */ F(dd, [["render", $d], ["__scopeId", "data-v-e780f452"]]), Od = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Si
}, Symbol.toStringTag, { value: "Module" })), Td = {
  name: "vu-grid-view",
  mixins: [vt, Gu],
  props: {
    modelValue: {
      type: [Object, Array],
      default: () => []
    },
    items: {
      type: Array,
      required: true
    },
    headers: {
      type: Array,
      required: true
    },
    dense: {
      type: Boolean,
      default: false
    },
    rich: {
      type: Boolean,
      default: true
    },
    selectable: {
      type: Boolean,
      default: false
    },
    allSelectable: {
      type: Boolean,
      default: true
    },
    serverItemsLength: {
      type: Number,
      default: 0
    },
    rowsPerPage: {
      type: Number,
      default: 5
    },
    topPagination: {
      type: Boolean,
      default: false
    },
    hideRowsPerPageSelect: {
      type: Boolean,
      default: false
    },
    whiteBackground: {
      type: Boolean,
      default: false
    },
    sort: {
      type: Function,
      default(e, n) {
        return this.isAscending ? e[this.sortKey] < n[this.sortKey] ? -1 : e[this.sortKey] > n[this.sortKey] ? 1 : 0 : e[this.sortKey] > n[this.sortKey] ? -1 : e[this.sortKey] < n[this.sortKey] ? 1 : 0;
      }
    },
    itemPerPageOptions: {
      type: Array,
      default: () => [10, 20, 50]
    },
    labels: {
      type: Object,
      default: () => ({
        previousLabel: "Previous",
        nextLabel: "Next"
      })
    }
  },
  emits: ["cellClick", "update:modelValue", "update:rowsPerPage", "pageUp", "pageDown"],
  data() {
    return {
      sortKey: "",
      isAscending: void 0,
      startRow: 0,
      selectedCellItem: "",
      selectedCellProperty: "",
      size: null
    };
  },
  computed: {
    hasSelected() {
      return this.value.length > 0;
    },
    sortedItems() {
      const e = this.startRow + this.rowsPerPage;
      return this.sortKey ? [...this.items].sort(this.sort.bind(this)).slice(this.startRow, e) : this.items.slice(this.startRow, e);
    },
    itemMax() {
      const e = this.startRow + this.rowsPerPage;
      return e > this.items.length ? this.items.length : e;
    },
    containerHeight() {
      return (this.dense ? 24 : 38) + (this.dense ? 24 : 38) * (this.sortedItems.length < this.rowsPerPage ? this.sortedItems.length : this.rowsPerPage);
    },
    value: {
      get() {
        return this.modelValue;
      },
      set(e) {
        this.$emit("update:modelValue", e);
      }
    },
    isNarrow() {
      var e;
      return ((e = this.size) == null ? void 0 : e.width) < 420;
    },
    isTiny() {
      var e;
      return ((e = this.size) == null ? void 0 : e.width) < 250;
    }
  },
  mounted() {
    this.size = ni(this.$refs.container);
  },
  methods: {
    isEqual(e, n) {
      return e === n;
    },
    selectAll() {
      this.value.length === this.items.length ? this.value = [] : this.value = [...this.items];
    },
    selectItem(e) {
      const n = this.value.includes(e), t = [...this.value];
      if (n) {
        const i = t.indexOf(e);
        this.value.splice(i, 1);
      } else
        this.value.push(e);
    },
    updateRows(e) {
      this.$emit("update:rowsPerPage", e);
    },
    sortBy(e) {
      this.sortKey === e ? this.isAscending = !this.isAscending : (this.sortKey = e, this.isAscending = true);
    },
    pageUp() {
      this.startRow += this.rowsPerPage, this.$emit("pageUp");
    },
    pageDown() {
      this.startRow -= this.rowsPerPage, this.$emit("pageDown");
    }
  },
  components: { VuCheckbox: yi, VuIconBtn: de, VuSelect: Si, VuBtn: Ye }
}, Ii = (e) => (pushScopeId("data-v-c2902439"), e = e(), popScopeId(), e), xd = {
  key: 0,
  class: "grid-view__table__header-intersection"
}, Md = { class: "grid-view__table__body" }, Vd = ["onClick"], Pd = {
  key: 0,
  class: "grid-view__table__row__header"
}, Ld = ["onClick"], Dd = { style: { "margin-right": "5px" } }, Ad = /* @__PURE__ */ Ii(() => /* @__PURE__ */ createBaseVNode("span", { class: "icon-left fonticon fonticon-chevron-left" }, null, -1)), Fd = { class: "inner-text" }, zd = { class: "inner-text" }, Ed = /* @__PURE__ */ Ii(() => /* @__PURE__ */ createBaseVNode("span", { class: "icon-left fonticon fonticon-chevron-right" }, null, -1));
function Nd(e, n, t, i, o, s) {
  const a = resolveComponent("VuCheckbox"), u = resolveComponent("VuIconBtn"), c = resolveComponent("VuSelect"), d = resolveComponent("VuBtn"), h2 = resolveDirective("tooltip"), p2 = resolveDirective("mask");
  return withDirectives((openBlock(), createElementBlock("div", {
    ref: "container",
    class: normalizeClass(["vu-grid-view", [{ elevated: e.elevated, "vu-grid-view--rich": t.rich }]])
  }, [
    createBaseVNode("div", {
      class: "grid-view__container",
      style: normalizeStyle(`height: ${s.containerHeight}px;`)
    }, [
      createBaseVNode("table", {
        class: normalizeClass(["grid-view__table", [
          { dense: t.dense, "grid-view__table--has-selection": s.hasSelected }
        ]])
      }, [
        createBaseVNode("thead", null, [
          createBaseVNode("tr", null, [
            t.selectable ? (openBlock(), createElementBlock("th", xd, [
              t.allSelectable ? (openBlock(), createBlock(a, {
                key: 0,
                dense: "",
                class: "grid-view__table__checkbox",
                "model-value": s.value.length === t.items.length && t.items.length,
                options: [{}],
                "onUpdate:modelValue": s.selectAll
              }, null, 8, ["model-value", "onUpdate:modelValue"])) : createCommentVNode("", true)
            ])) : createCommentVNode("", true),
            (openBlock(true), createElementBlock(Fragment, null, renderList(t.headers, (_, v) => (openBlock(), createElementBlock("th", {
              key: `header_${_.property}_${v}`
            }, [
              withDirectives((openBlock(), createElementBlock("span", null, [
                createTextVNode(toDisplayString(_.label), 1)
              ])), [
                [
                  h2,
                  _.label,
                  void 0,
                  {
                    ellipsis: true,
                    bottom: true
                  }
                ]
              ]),
              _.sortable !== false ? (openBlock(), createBlock(u, {
                key: 0,
                class: "icon-smaller",
                icon: _.property === o.sortKey && o.isAscending ? "expand-up" : "expand-down",
                active: _.property === o.sortKey,
                onClick: (y) => s.sortBy(_.property)
              }, null, 8, ["icon", "active", "onClick"])) : createCommentVNode("", true)
            ]))), 128))
          ])
        ]),
        createBaseVNode("tbody", Md, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(s.sortedItems, (_, v) => (openBlock(), createElementBlock("tr", {
            key: `line_${v}`,
            class: normalizeClass({ dense: t.dense, selected: s.value.includes(_) }),
            onClick: (y) => s.selectItem(_)
          }, [
            t.selectable ? (openBlock(), createElementBlock("td", Pd, [
              createVNode(a, {
                dense: "",
                class: "grid-view__table__body__checkbox",
                "model-value": s.value.includes(_),
                options: [{}],
                "onUpdate:modelValue": (y) => s.selectItem(_)
              }, null, 8, ["model-value", "onUpdate:modelValue"])
            ])) : createCommentVNode("", true),
            (openBlock(true), createElementBlock(Fragment, null, renderList(t.headers, (y) => (openBlock(), createElementBlock("td", {
              key: `${y.property}_${_[y.property]}`,
              class: normalizeClass([
                s.isEqual(_, o.selectedCellItem) && s.isEqual(y.property, o.selectedCellProperty) ? "selected" : ""
              ]),
              onClick: () => {
                o.selectedCellItem = _, o.selectedCellProperty = y.property, e.$emit("cellClick", { item: _, header: y, property: e.property });
              }
            }, [
              renderSlot(e.$slots, y.property, normalizeProps(guardReactiveProps(_)), () => [
                createTextVNode(toDisplayString(_[y.property]), 1)
              ], true)
            ], 10, Ld))), 128))
          ], 10, Vd))), 128))
        ])
      ], 2)
    ], 4),
    createBaseVNode("div", {
      class: normalizeClass(["grid-view__pagination", { "grid-view__pagination--top": t.topPagination, "is-narrow": s.isNarrow }])
    }, [
      renderSlot(e.$slots, "pagination", {}, () => [
        t.hideRowsPerPageSelect ? createCommentVNode("", true) : withDirectives((openBlock(), createBlock(c, {
          key: 0,
          options: t.itemPerPageOptions.map((_) => ({ value: _, label: _ })),
          "hide-placeholder-option": true,
          "model-value": t.rowsPerPage,
          "onUpdate:modelValue": s.updateRows
        }, null, 8, ["options", "model-value", "onUpdate:modelValue"])), [
          [vShow, !s.isTiny]
        ]),
        createBaseVNode("div", Dd, toDisplayString(o.startRow + 1) + "-" + toDisplayString(s.itemMax) + " / " + toDisplayString(t.serverItemsLength || t.items.length), 1),
        createVNode(d, {
          disabled: o.startRow === 0,
          class: "pagination-previous-button",
          onClick: s.pageDown
        }, {
          default: withCtx(() => [
            Ad,
            createBaseVNode("span", Fd, toDisplayString(t.labels.previousLabel), 1)
          ]),
          _: 1
        }, 8, ["disabled", "onClick"]),
        createVNode(d, {
          disabled: o.startRow + t.rowsPerPage >= (t.serverItemsLength || t.items.length),
          class: "pagination-next-button",
          onClick: s.pageUp
        }, {
          default: withCtx(() => [
            createBaseVNode("span", zd, toDisplayString(t.labels.nextLabel), 1),
            Ed
          ]),
          _: 1
        }, 8, ["disabled", "onClick"])
      ], true)
    ], 2)
  ], 2)), [
    [p2, e.loading]
  ]);
}
const Rd = /* @__PURE__ */ F(Td, [["render", Nd], ["__scopeId", "data-v-c2902439"]]), Hd = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Rd
}, Symbol.toStringTag, { value: "Module" })), jd = {
  name: "vu-icon-link",
  components: { VuIcon: ue },
  mixins: [os],
  props: {
    label: {
      type: String,
      default: () => ""
    },
    icon: {
      type: String,
      default: () => ""
    }
  },
  data: () => ({
    pressed: false
  })
}, Ud = { class: "icon-link__link" };
function Wd(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon");
  return openBlock(), createElementBlock("a", {
    class: normalizeClass(["vu-icon-link", { active: e.active }])
  }, [
    t.icon ? (openBlock(), createBlock(a, {
      key: 0,
      icon: t.icon,
      active: e.active
    }, null, 8, ["icon", "active"])) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
      createTextVNode(" ")
    ], 64)),
    createBaseVNode("span", Ud, [
      renderSlot(e.$slots, "default", {}, () => [
        createTextVNode(toDisplayString(t.label), 1)
      ], true)
    ])
  ], 2);
}
const ds = /* @__PURE__ */ F(jd, [["render", Wd], ["__scopeId", "data-v-fc65c3e7"]]), qd = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: ds
}, Symbol.toStringTag, { value: "Module" })), Kd = {
  name: "vu-input-date",
  mixins: [Fe, hn, ki, ze, Ee, $e],
  emits: ["update:modelValue"],
  components: { VuDatepicker: _i },
  props: {
    modelValue: {
      type: Date,
      default: () => null
    },
    contentClass: {
      type: String,
      default: () => ""
    },
    contentStyle: {
      type: [String, Object],
      default: () => ""
    },
    unselectableDaysOfWeek: {
      type: Array[Number],
      default: () => []
    },
    yearRange: {
      type: Number,
      default: () => 10
    },
    firstDay: {
      type: Number,
      default: () => 1
    },
    // input
    placeholder: {
      type: String,
      default: () => "Select a value"
    },
    // i18n
    dateFormatLocale: {
      type: String,
      default: () => "en"
    },
    dateFormatOptions: {
      type: Object,
      default: () => ({
        weekday: "short",
        year: "numeric",
        month: "short",
        day: "2-digit"
      })
    },
    hideOnSelect: {
      type: Boolean,
      default: () => true
    },
    previousMonthLabel: {
      type: String,
      required: false,
      default: void 0
    },
    nextMonthLabel: {
      type: String,
      required: false,
      default: void 0
    },
    monthsLabels: {
      type: Array,
      required: false,
      default: () => {
      }
    },
    weekdaysLabels: {
      type: Array,
      required: false,
      default: () => {
      }
    },
    weekdaysShortLabels: {
      type: Array,
      required: false,
      default: () => {
      }
    },
    showWeekNumber: {
      type: Boolean,
      required: false
    },
    isRTL: {
      type: Boolean,
      required: false
    }
  },
  data: () => ({
    open: false,
    stringifedValue: ""
  }),
  computed: {
    date: {
      get() {
        return this.modelValue;
      },
      set(e) {
        this.$emit("update:modelValue", e);
      }
    },
    isEmpty() {
      return this.value === null || this.value === "" || this.value === void 0;
    }
  },
  watch: {
    modelValue: {
      immediate: true,
      handler() {
        this.date ? this.stringifedValue = new Intl.DateTimeFormat(this.dateFormatLocale, this.dateFormatOptions).format(this.date) : this.stringifedValue = "";
      }
    }
  },
  methods: {
    click() {
      this.date = "";
    },
    handleSelect(e) {
      this.date = e, this.hideOnSelect && (this.open = false);
    }
  }
}, Gd = {
  key: 0,
  class: "control-label"
}, Yd = {
  key: 0,
  class: "label-field-required"
}, Xd = {
  ref: "activator",
  class: "input-date"
}, Jd = ["value", "placeholder", "disabled"], Zd = {
  key: 1,
  class: "form-control-helper-text"
};
function Qd(e, n, t, i, o, s) {
  const a = resolveComponent("VuDatepicker"), u = resolveDirective("click-outside");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", e.classes])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", Gd, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", Yd, " * ")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    withDirectives((openBlock(), createElementBlock("div", Xd, [
      createBaseVNode("input", {
        ref: "input",
        value: e.stringifedValue,
        placeholder: t.placeholder,
        disabled: e.disabled,
        readonly: "",
        type: "text",
        class: normalizeClass(["form-control input-date", { filled: !s.isEmpty }]),
        onClick: n[0] || (n[0] = (c) => {
          e.open = true;
        })
      }, null, 10, Jd),
      e.clearable ? (openBlock(), createElementBlock("span", {
        key: 0,
        class: "input-date-reset fonticon fonticon-clear",
        onClick: n[1] || (n[1] = (c) => s.click())
      })) : createCommentVNode("", true),
      createVNode(a, {
        style: normalizeStyle([{ position: "absolute", top: "38px" }, t.contentStyle]),
        class: normalizeClass(t.contentClass),
        modelValue: e.value,
        "onUpdate:modelValue": [
          n[2] || (n[2] = (c) => e.value = c),
          s.handleSelect
        ],
        show: e.open,
        min: e.min,
        max: e.max,
        "unselectable-days-of-week": t.unselectableDaysOfWeek,
        "year-range": t.yearRange,
        "first-day": t.firstDay,
        "show-week-number": t.showWeekNumber,
        "is-r-t-l": t.isRTL,
        "previous-month-label": t.previousMonthLabel,
        "next-month-label": t.nextMonthLabel,
        "months-labels": t.monthsLabels,
        "weekdays-labels": t.weekdaysLabels,
        "weekdays-short-labels": t.weekdaysShortLabels,
        onBoundaryChange: n[3] || (n[3] = (c) => s.date = c.value)
      }, null, 8, ["style", "class", "modelValue", "show", "min", "max", "unselectable-days-of-week", "year-range", "first-day", "show-week-number", "is-r-t-l", "previous-month-label", "next-month-label", "months-labels", "weekdays-labels", "weekdays-short-labels", "onUpdate:modelValue"])
    ])), [
      [u, function() {
        e.open = false;
      }]
    ]),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (c, d) => (openBlock(), createElementBlock("span", {
      key: `${d}-error-${c}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(c), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", Zd, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const ec = /* @__PURE__ */ F(Kd, [["render", Qd], ["__scopeId", "data-v-bcad46d4"]]), tc = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: ec
}, Symbol.toStringTag, { value: "Module" })), nc = {
  name: "vu-input-number",
  inheritAttrs: false,
  mixins: [Fe, ze, Ee, $e],
  props: {
    step: {
      type: Number,
      default: () => 0.1
    },
    decimal: {
      type: Number,
      default: () => 2
    },
    min: {
      type: Number,
      default: 0
    },
    max: {
      type: Number,
      default: Number.MAX_SAFE_INTEGER
    },
    showButtons: {
      type: Boolean,
      default: true
    }
  },
  emits: ["update:modelValue"],
  methods: {
    input(e, n) {
      if (n && e === "" && this.value !== "") {
        this.$refs.input.value = this.value;
        return;
      }
      if (e === "" && n === "-" || n === "." || n === ",")
        return;
      let t = e !== "" ? this.parseValue(this.fixed(e)) : void 0;
      this.$emit("update:modelValue", t), this.$refs.input.value = this.value;
    },
    decrement() {
      let e = parseFloat(this.value);
      e = Number.isNaN(e) ? this.max : e, this.input(e - this.step);
    },
    increment() {
      let e = parseFloat(this.value);
      e = Number.isNaN(e) ? this.min : e, this.input(e + this.step);
    },
    parseValue(e) {
      const n = parseFloat(e);
      return n > this.max ? this.max : n < this.min ? this.min : n;
    },
    fixed(e) {
      return Math.round(e * 10 ** this.decimal) / 10 ** this.decimal;
    }
  }
}, sc = {
  key: 0,
  class: "control-label"
}, ic = {
  key: 0,
  class: "label-field-required"
}, oc = { class: "input-number" }, lc = ["disabled"], ac = ["value", "placeholder", "disabled", "min", "max", "step"], rc = ["disabled"], uc = {
  key: 1,
  class: "form-control-helper-text"
};
function dc(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-number form-group", { ...e.classes, "vu-number--no-buttons": !t.showButtons }])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", sc, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", ic, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    createBaseVNode("div", oc, [
      t.showButtons ? (openBlock(), createElementBlock("button", {
        key: 0,
        type: "button",
        disabled: e.disabled,
        class: "input-number-button input-number-button-left btn btn-default",
        onClick: n[0] || (n[0] = (...a) => s.decrement && s.decrement(...a))
      }, null, 8, lc)) : createCommentVNode("", true),
      createBaseVNode("input", mergeProps(e.$attrs, {
        ref: "input",
        value: e.value,
        placeholder: e.placeholder,
        disabled: e.disabled,
        min: t.min,
        max: t.max,
        step: t.step,
        type: "number",
        class: "form-control",
        onKeypress: [
          n[1] || (n[1] = withKeys((...a) => s.increment && s.increment(...a), ["up"])),
          n[2] || (n[2] = withKeys((...a) => s.decrement && s.decrement(...a), ["down"]))
        ],
        onInput: n[3] || (n[3] = (a) => s.input(a.target.value, a.data))
      }), null, 16, ac),
      t.showButtons ? (openBlock(), createElementBlock("button", {
        key: 1,
        type: "button",
        disabled: e.disabled,
        class: "input-number-button input-number-button-right btn btn-default",
        onClick: n[4] || (n[4] = (...a) => s.increment && s.increment(...a))
      }, null, 8, rc)) : createCommentVNode("", true)
    ]),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (a, u) => (openBlock(), createElementBlock("span", {
      key: `${u}-error-${a}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(a), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", uc, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const cc = /* @__PURE__ */ F(nc, [["render", dc], ["__scopeId", "data-v-9ec5f27e"]]), hc = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: cc
}, Symbol.toStringTag, { value: "Module" })), fc = {
  name: "vu-input",
  inheritAttrs: false,
  inject: {
    vuInputComposition: {
      default: false
    }
  },
  expose: ["validate", "focus"],
  mixins: [Fe, ze, $e, Ee],
  emits: ["update:modelValue"],
  methods: {
    focus() {
      var e, n;
      (n = (e = this.$refs) == null ? void 0 : e.input) == null || n.focus();
    }
  }
}, pc = {
  key: 0,
  class: "control-label"
}, mc = {
  key: 0,
  class: "label-field-required"
}, vc = ["value", "placeholder", "disabled", "type"], gc = {
  key: 1,
  class: "form-control-helper-text"
};
function bc(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", e.classes])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", pc, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", mc, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    createBaseVNode("input", mergeProps(e.$attrs, {
      ref: "input",
      value: e.value,
      placeholder: e.placeholder,
      disabled: e.disabled,
      type: e.type,
      class: "form-control",
      onInput: n[0] || (n[0] = ({ target: a }) => {
        s.vuInputComposition || (a.composing = false), e.$emit("update:modelValue", a.value);
      })
    }), null, 16, vc),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (a, u) => (openBlock(), createElementBlock("span", {
      key: `${u}-error-${a}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(a), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", gc, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const Ci = /* @__PURE__ */ F(fc, [["render", bc], ["__scopeId", "data-v-0798aae9"]]), yc = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ci
}, Symbol.toStringTag, { value: "Module" })), dt = (e) => typeof e != "string" ? "" : e.charAt(0).toUpperCase() + e.slice(1), _c = {
  name: "vu-lightbox-bar",
  emits: ["close", "click-comment", "click-download", "click-information", "click-share", "media-type-drag-start", "media-type-drag", "media-type-drag-end", "click-compass"],
  props: {
    // eslint-disable-next-line vue/require-prop-types
    showCloseIcon: { default: () => true },
    // eslint-disable-next-line vue/require-prop-types
    showCompass: { default: () => true },
    label: {
      type: String,
      default: () => ""
    },
    type: {
      type: Object,
      default: () => {
      }
    },
    items: {
      type: Array,
      default: () => []
    },
    customItems: {
      type: Array,
      default: () => []
    },
    subItems: {
      type: Array,
      default: () => []
    },
    rightItems: {
      type: Array,
      default: () => []
    },
    responsive: {
      type: Boolean,
      default: () => false
    },
    widget: {
      type: Boolean,
      default: () => false
    },
    moreActionsLabel: {
      type: String,
      default: () => "More"
    },
    disableCompass: {
      type: Boolean,
      required: true
    },
    closeLabel: {
      type: String,
      default: () => "Close"
    },
    dropdownOverlay: Boolean,
    /* eslint-disable vue/require-default-prop */
    onMediaTypeDragStart: Function,
    onMediaTypeDrag: Function,
    onMediaTypeDragEnd: Function
  },
  data: () => ({
    getListenersFromAttrs: Ze,
    capitalize: dt,
    uid: gt()
  }),
  computed: {
    menuIcon() {
      return this.responsive ? "menu-dot" : "chevron-down";
    },
    hasLeftToDividerContent() {
      return this.items.length > 0 && this.items.some((e) => !e.hidden) || this._dropdownMenuItems.length > 0 || this.$slots["lightbox-bar__special-actions"];
    },
    hasRightToDividerContent() {
      return this.showCloseIcon || this.rightItems && this.rightItems.length > 0 && this.rightItems.some((e) => !e.hidden);
    },
    hasDragEvent() {
      return this.onMediaTypeDragStart || this.onMediaTypeDrag || this.onMediaTypeDragEnd;
    },
    _items() {
      return this.actionsMergeSubs(this.items, this.customItems);
    },
    dropdownMenuListeners() {
      const e = this.getListenersFromAttrs(this.$attrs);
      if (e.close) {
        const n = { ...e };
        return delete n.close, n;
      }
      return e;
    },
    _dropdownMenuItems() {
      if (this.responsive) {
        const e = this._items.filter(({ nonResponsive: n, hidden: t }) => !n && !t);
        return this.subItems && this.subItems.length > 0 && e.push({
          name: "more-actions",
          label: this.moreActionsLabel,
          items: this.subItems
        }), e;
      }
      return this.subItems;
    }
  },
  methods: {
    icon(e) {
      return e.icon ? `${e.icon}` : `${e.fonticon}`;
    },
    actionClick(e, n = "primary-action") {
      e.disabled || (e.handler && e.handler(e), this.$emit(`click-${e.name.toLowerCase()}`, e, { type: n }));
    },
    actionsMergeSubs(e, n) {
      const t = n.filter(({ name: s }) => e.find(({ name: a }) => s === a)), i = n.filter(({ name: s }) => !t.find(({ name: a }) => s === a));
      e.forEach(({ name: s, items: a }) => {
        const u = t.find(({ name: c }) => c === s);
        if (u) {
          const { items: c } = u;
          c && (Array.isArray(a) || (a = []), a.push(...c));
        }
      });
      let o = [...e, ...i];
      return o = o.map((s) => {
        if (s.text === void 0) {
          const a = this.capitalize(s.name);
          s.text = a;
        }
        return s;
      }), o;
    },
    selectedItemsArray(e) {
      return this.customItems ? this.getSelectedItems(e) : [];
    },
    getSelectedItems(e) {
      let n = [];
      return Array.isArray(e) && e.forEach((t) => {
        if (t.items) {
          const i = this.getSelectedItems(t);
          n = [n, ...i];
        }
      }), n.filter((t) => t.selected);
    }
  },
  components: { VuIconBtn: de, VuDropdownMenu: rt }
}, Bi = (e) => (pushScopeId("data-v-5e131de1"), e = e(), popScopeId(), e), wc = { class: "lightbox-bar__left" }, kc = /* @__PURE__ */ Bi(() => /* @__PURE__ */ createBaseVNode("div", { class: "lightbox-bar__compass-active" }, null, -1)), Sc = [
  kc
], Ic = { class: "lightbox-bar-menu-item lightbox-bar-menu-item--no-cursor" }, Cc = ["draggable"], Bc = { class: "lightbox-bar__title" }, $c = { class: "lightbox-bar__right" }, Oc = { class: "lightbox-bar__menu" }, Tc = {
  key: 2,
  class: "lightbox-bar__divider"
}, xc = /* @__PURE__ */ Bi(() => /* @__PURE__ */ createBaseVNode("hr", { class: "divider divider--vertical" }, null, -1)), Mc = [
  xc
];
function Vc(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconBtn"), u = resolveComponent("VuDropdownMenu"), c = resolveDirective("tooltip");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-lightbox-bar", {
      "lightbox-bar--responsive": t.responsive,
      "lightbox-bar--widget-header": t.widget
    }])
  }, [
    createBaseVNode("div", wc, [
      t.showCompass && !t.widget ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["lightbox-bar__compass", { "lightbox-bar__compass--disabled": t.disableCompass }]),
        onClick: n[0] || (n[0] = (d) => e.$emit("click-compass"))
      }, Sc, 2)) : createCommentVNode("", true),
      renderSlot(e.$slots, "lightbox-bar__object-type", {}, () => [
        createBaseVNode("div", Ic, [
          createBaseVNode("div", {
            class: "lightbox-bar__media-type",
            style: normalizeStyle({ "background-color": t.type.backgroundColor }),
            onDragstart: n[1] || (n[1] = (d) => e.$emit("media-type-drag-start", d)),
            onDrag: n[2] || (n[2] = (d) => e.$emit("media-type-drag", d)),
            onDragend: n[3] || (n[3] = (d) => e.$emit("media-type-drag-end", d)),
            draggable: s.hasDragEvent ? "true" : "false"
          }, [
            createBaseVNode("span", {
              class: normalizeClass(`fonticon fonticon-${t.type.icon}`)
            }, null, 2)
          ], 44, Cc)
        ])
      ], true),
      createBaseVNode("div", Bc, [
        renderSlot(e.$slots, "lightbox-bar__title", {}, () => [
          createBaseVNode("span", null, toDisplayString(t.label), 1)
        ], true)
      ])
    ]),
    createBaseVNode("div", $c, [
      createBaseVNode("div", Oc, [
        t.responsive ? createCommentVNode("", true) : (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(s._items, (d, h2) => (openBlock(), createElementBlock(Fragment, {
          key: `${e.uid}-${h2}-rm`
        }, [
          d.items && !d.hidden ? (openBlock(), createBlock(u, mergeProps({
            "v-model": s.selectedItemsArray(s._items),
            key: `lightbox-dropdownmenu_${e.uid}-${h2}`,
            items: d.items,
            shift: true,
            disabled: d.disabled
          }, { overlay: t.dropdownOverlay }, { class: "lightbox-bar-dropdown-wrap" }, toHandlers(s.dropdownMenuListeners)), {
            default: withCtx(({ active: p2 }) => [
              withDirectives(createVNode(a, {
                icon: s.icon(d),
                active: d.selected || p2,
                disabled: d.disabled,
                color: t.widget ? "default" : "secondary",
                class: "lightbox-bar-menu-item",
                onClick: () => s.actionClick(d)
              }, null, 8, ["icon", "active", "disabled", "color", "onClick"]), [
                [
                  c,
                  `${d.label || e.capitalize(d.name)}`,
                  void 0,
                  {
                    body: true,
                    bottom: true
                  }
                ]
              ])
            ]),
            _: 2
          }, 1040, ["v-model", "items", "disabled"])) : d.hidden ? createCommentVNode("", true) : withDirectives((openBlock(), createBlock(a, {
            key: 1,
            icon: s.icon(d),
            active: d.selected,
            disabled: d.disabled,
            color: t.widget ? "default" : "secondary",
            class: "lightbox-bar-menu-item",
            onClick: () => s.actionClick(d)
          }, null, 8, ["icon", "active", "disabled", "color", "onClick"])), [
            [
              c,
              `${d.label || e.capitalize(d.name)}`,
              void 0,
              {
                body: true,
                bottom: true
              }
            ]
          ])
        ], 64))), 128)),
        s._dropdownMenuItems.length > 0 ? (openBlock(), createBlock(u, mergeProps({
          key: 1,
          "v-model": s.selectedItemsArray(s._dropdownMenuItems),
          class: "lightbox-bar-dropdown-wrap",
          "prevent-dropup": true,
          items: s._dropdownMenuItems,
          position: "bottom-left",
          shift: true
        }, { overlay: t.dropdownOverlay }, toHandlers(s.dropdownMenuListeners)), {
          default: withCtx(({ active: d }) => [
            withDirectives(createVNode(a, {
              icon: s.menuIcon,
              active: d,
              color: t.widget ? "default" : "secondary",
              class: normalizeClass(["lightbox-bar-menu-item", t.responsive ? "" : "chevron-menu-icon"])
            }, null, 8, ["icon", "active", "color", "class"]), [
              [
                c,
                `${t.moreActionsLabel}`,
                void 0,
                {
                  body: true,
                  bottom: true
                }
              ]
            ])
          ]),
          _: 1
        }, 16, ["v-model", "items"])) : createCommentVNode("", true),
        renderSlot(e.$slots, "lightbox-bar__special-actions", {}, void 0, true),
        s.hasLeftToDividerContent && s.hasRightToDividerContent ? (openBlock(), createElementBlock("div", Tc, Mc)) : createCommentVNode("", true),
        (openBlock(true), createElementBlock(Fragment, null, renderList(t.rightItems, (d, h2) => (openBlock(), createElementBlock(Fragment, null, [
          d.hidden ? createCommentVNode("", true) : withDirectives((openBlock(), createBlock(a, {
            key: `${e.uid}-sa-${h2}`,
            class: "lightbox-bar-menu-item",
            color: t.widget ? "default" : "secondary",
            icon: s.icon(d),
            active: d.selected,
            disabled: d.disabled,
            onClick: (p2) => s.actionClick(d, "side-action")
          }, null, 8, ["color", "icon", "active", "disabled", "onClick"])), [
            [
              c,
              `${d.label || e.capitalize(d.name)}`,
              void 0,
              {
                body: true,
                bottom: true
              }
            ]
          ])
        ], 64))), 256)),
        t.showCloseIcon ? withDirectives((openBlock(), createBlock(a, {
          key: 3,
          class: "lightbox-bar-menu-item",
          color: t.widget ? "default" : "secondary",
          icon: "close",
          onClick: n[4] || (n[4] = (d) => e.$emit("close", false))
        }, null, 8, ["color"])), [
          [
            c,
            t.closeLabel,
            void 0,
            {
              body: true,
              bottom: true
            }
          ]
        ]) : createCommentVNode("", true)
      ])
    ])
  ], 2);
}
const $i = /* @__PURE__ */ F(_c, [["render", Vc], ["__scopeId", "data-v-5e131de1"]]), Pc = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $i
}, Symbol.toStringTag, { value: "Module" })), Lc = { class: "relative full-width h-100%" }, Dc = {
  key: 2,
  class: "panel__header"
}, Ac = { class: "panel__title__text" }, Fc = {
  name: "vu-side-panel"
}, Oi = /* @__PURE__ */ defineComponent({
  ...Fc,
  props: /* @__PURE__ */ mergeModels({
    side: { default: "left" },
    topbar: { type: Boolean },
    animated: { type: Boolean, default: true },
    closeIcon: { type: Boolean, default: true },
    closeIconClasses: {},
    closeIconTooltip: {},
    pinIcon: { type: Boolean },
    pinIconClasses: {},
    pinIconTooltip: {},
    pinOnScroll: { type: [Boolean, String] },
    pinOnClick: { type: [Boolean, String], default: true },
    pinOnContextmenu: { type: [Boolean, String] },
    transient: { type: [Boolean, String] },
    transientTarget: {},
    title: {},
    panelTitleClasses: {},
    smallCloseIcon: { type: Boolean },
    showEdit: { type: Boolean }
  }, {
    modelValue: {},
    modelModifiers: {}
  }),
  emits: /* @__PURE__ */ mergeModels(["panel-edit"], ["update:modelValue"]),
  setup(e, { expose: n, emit: t }) {
    const i = e, o = t, s = useModel(e, "modelValue"), a = ref(false), u = computed(() => ye(i.transientTarget)), c = computed(() => `slide-in-${i.side}`), d = computed(() => `slide-out-${i.side}`), h2 = function() {
    };
    let p2 = h2;
    watchEffect(() => {
      p2(), i.transient ? p2 = pe(u.value, "mouseenter", () => a.value = true) : p2 = h2;
    }), watch(s, (B) => {
      B && (a.value = false);
    });
    function _() {
      a.value = true;
    }
    function v() {
      a.value = false;
    }
    function y() {
      s.value = false;
    }
    function L() {
      s.value = true;
    }
    return n({ close: y, open: L, showTransient: _, hideTransient: v }), (B, R) => {
      const ee = resolveDirective("tooltip");
      return openBlock(), createBlock(Transition, {
        appear: "",
        "enter-active-class": c.value,
        "leave-active-class": d.value
      }, {
        default: withCtx(() => [
          withDirectives(createBaseVNode("aside", {
            class: normalizeClass(["vu-side-panel animated bottom-0 absolute", [
              {
                "vu-side-panel--topbar": B.topbar,
                "top-0": !B.topbar
              },
              `${B.side}-0`,
              `vu-side-panel--${B.side}`
            ]]),
            onMouseleave: R[3] || (R[3] = () => {
              s.value || (a.value = false);
            }),
            onClick: R[4] || (R[4] = () => {
              B.pinOnClick && !s.value && (s.value = true);
            }),
            onContextmenu: R[5] || (R[5] = () => {
              B.pinOnContextmenu && !s.value && (s.value = true);
            }),
            onScroll: R[6] || (R[6] = () => {
              B.pinOnScroll && !s.value && (s.value = true);
            })
          }, [
            createBaseVNode("div", Lc, [
              B.pinIcon && !s.value && a.value ? withDirectives((openBlock(), createBlock(de, {
                key: 0,
                icon: "pin",
                class: normalizeClass(["absolute! top-2px", [B.pinIconClasses]]),
                onClick: R[0] || (R[0] = () => {
                  s.value = true, a.value = false;
                })
              }, null, 8, ["class"])), [
                [
                  ee,
                  B.pinIconTooltip,
                  void 0,
                  { bottom: true }
                ]
              ]) : createCommentVNode("", true),
              B.closeIcon && s.value ? withDirectives((openBlock(), createBlock(de, {
                key: 1,
                icon: "close",
                class: normalizeClass(["absolute! top-2px right-0", [B.closeIconClasses, { "font-size-[13px]!": B.smallCloseIcon }]]),
                onClick: R[1] || (R[1] = () => {
                  s.value = false, a.value = false;
                })
              }, null, 8, ["class"])), [
                [
                  ee,
                  B.closeIconTooltip,
                  void 0,
                  { bottom: true }
                ]
              ]) : createCommentVNode("", true),
              B.title ? (openBlock(), createElementBlock("div", Dc, [
                createBaseVNode("span", {
                  class: normalizeClass([
                    "panel__title",
                    [{
                      "mr-38px": B.closeIcon && s.value && !B.closeIconClasses,
                      "ml-28px": B.pinIcon && a.value && !B.pinIconClasses
                    }, B.panelTitleClasses]
                  ])
                }, [
                  renderSlot(B.$slots, "title", {}, () => [
                    createBaseVNode("span", Ac, toDisplayString(B.title), 1)
                  ]),
                  renderSlot(B.$slots, "title_icons", {}, () => [
                    B.showEdit ? (openBlock(), createBlock(de, {
                      key: 0,
                      class: "panel__edit__icon",
                      icon: "pencil",
                      onClick: R[2] || (R[2] = (ce) => o("panel-edit"))
                    })) : createCommentVNode("", true)
                  ])
                ], 2)
              ])) : createCommentVNode("", true),
              renderSlot(B.$slots, "default")
            ])
          ], 34), [
            [vShow, a.value || s.value]
          ])
        ]),
        _: 3
      }, 8, ["enter-active-class", "leave-active-class"]);
    };
  }
}), zc = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Oi
}, Symbol.toStringTag, { value: "Module" })), Ms = [
  {
    name: "previous",
    fonticon: "chevron-left",
    selected: false,
    disabled: false,
    hidden: false
  },
  {
    name: "next",
    fonticon: "chevron-right",
    selected: false,
    disabled: false,
    hidden: false
  }
], Vs = [
  {
    name: "comment",
    fonticon: "topbar-comment",
    disabled: false,
    hidden: false
  },
  {
    name: "share",
    fonticon: "share-alt",
    disabled: false,
    hidden: false
  },
  {
    name: "download",
    fonticon: "download",
    disabled: false,
    hidden: false
  },
  {
    name: "information",
    fonticon: "topbar-info",
    disabled: false,
    hidden: false
  }
], Ps = {
  picture: {
    id: 1,
    icon: "picture",
    backgroundColor: "#70036b"
    // $violet-dv-1
  },
  audio: {
    id: 2,
    icon: "sound",
    backgroundColor: "#70036b"
    // $violet-dv-1
  },
  video: {
    id: 3,
    icon: "video",
    backgroundColor: "#70036b"
    // $violet-dv-1
  },
  "3dmodel": {
    id: 4,
    icon: "3d-object",
    backgroundColor: "#70036b"
    // $violet-dv-1
  },
  document: {
    id: 5,
    icon: "doc",
    backgroundColor: "#70036b"
    // $violet-dv-1
  }
}, Ec = {
  name: "vu-lightbox",
  components: { VuLightboxBar: $i, VuIconBtn: de, VuIconBtn: de, VuSidePanel: Oi },
  data() {
    return {
      panelStates: [],
      panelsRefs: {},
      openCompass: false,
      compassAlreadyOpened: false,
      compassPath: "webapps/i3DXCompassStandalone/i3DXCompassStandalone.html",
      resizeObserver: null,
      transforms: {
        responsive: false,
        left: {},
        center: {},
        right: {}
      },
      capitalize: dt,
      customItems: [],
      getListenersFromAttrs: Ze,
      uid: crypto.randomUUID(),
      inAnimate: false,
      outAnimate: false,
      rightPanelResizeObserver: null
    };
  },
  emits: ["close", "click-comment", "click-information", "click-share", "click-download", "media-type-drag-start", "media-type-drag", "media-type-drag-end", "click-compass"],
  props: {
    /* eslint-disable vue/prop-name-casing, style/quote-props */
    title: {
      type: String,
      default: () => ""
    },
    animatedPanels: {
      type: Boolean,
      default: true
    },
    userId: {
      type: String,
      required: false
    },
    panels: {
      type: Array,
      required: false,
      default: () => [{}]
    },
    widget: {
      type: Boolean,
      default: () => false
    },
    objectType: {
      type: [String, Object],
      default: () => "picture",
      validator: (e) => !!Ps[e] || e && e.icon && e.backgroundColor
    },
    primaryActions: {
      type: [Array, String],
      default: () => Vs
    },
    customActions: {
      type: Boolean,
      default: () => false
    },
    menuActions: {
      type: Array,
      required: false,
      default: () => []
    },
    sideActions: {
      type: Array,
      default: () => Ms
    },
    customSideActions: {
      type: Boolean,
      default: () => false
    },
    noObjectType: {
      type: Boolean,
      default: () => false
    },
    disableCompass: {
      type: Boolean,
      default: () => false
    },
    zIndex: {
      type: Number,
      default: () => 100
    },
    moreActionsLabel: {
      type: String,
      default: () => "More"
    },
    closeLabel: {
      type: String,
      default: () => "Close"
    },
    noAnimation: {
      type: Boolean,
      default: () => false
    },
    fasterAnimation: {
      type: Boolean,
      default: () => false
    },
    hideCloseIcon: {
      type: Boolean,
      default: () => false
    },
    dropdownOverlay: Boolean,
    onClose: Function,
    "onClick-comment": Function,
    "onClick-download": Function,
    "onClick-information": Function,
    "onClose-panel-information": Function,
    "onClose-panel-comment": Function,
    "onClick-share": Function,
    "onMedia-type-drag-start": Function,
    "onMedia-type-drag": Function,
    "onMedia-type-drag-end": Function
  },
  created() {
    this.panels.find(({ show: e }) => e !== void 0) || (this.panelStates = this.panels.map((e) => ({ ...e, show: false })));
  },
  computed: {
    typeInfo() {
      return typeof this.objectType == "object" ? this.objectType : Ps[this.objectType];
    },
    compassIframeUrl() {
      return `${this.serviceUrl || ""}/${this.compassPath}${this.userId ? `#userId:${this.userId}` : ""}`;
    },
    listeners() {
      return Ze(this.$attrs, true);
    },
    listenersFromProps() {
      return this.getListenersFromAttrs(this.$props, true);
    },
    _panels() {
      return this.panelStates.length > 0 ? this.panelStates : this.panels;
    },
    shownRightPanel() {
      return this._panels.find(({ show: e }) => typeof e == "object" ? e.value === true : e);
    },
    noCompass() {
      return this.widget;
    },
    _primaryActions() {
      const e = this.primaryActions, n = Vs;
      if (this.widget) {
        const t = e.find(({ name: o }) => o === "information"), i = e.find(({ name: o }) => o === "comment");
        t && !t.fonticon && (n.find(({ name: o }) => o === "information").fonticon = "info"), i && !i.fonticon && (n.find(({ name: o }) => o === "comment").fonticon = "comment");
      }
      return this.actionsMerge(e, n, this.customActions);
    },
    _sideActions() {
      return this.actionsMerge(this.sideActions, Ms, this.customSideActions);
    }
  },
  mounted() {
    this.onResize();
    const e = new ResizeObserver(() => {
      (!this.animatedPanels || !this.shownRightPanel || this.transforms.responsive) && this.onResize();
    });
    e.observe(this.$refs.lightbox), this.resizeObserver = e, this.shownRightPanel && this.updateRightPanelsObservers(this.shownRightPanel);
    const n = this;
    !this.noCompass && window && window.require && window.require(["DS/UWPClientCode/Data/Utils", "DS/UWPClientCode/PublicAPI"], (t, i) => {
      this.getCompassUrl = () => {
        t.getServiceUrl({
          serviceName: "3DCompass",
          onComplete: (o) => {
            n.serviceUrl = o;
          },
          onFailure: () => {
            UWA && UWA.debug && console.error("Lightbox Compass failed to retrieve 3DCompass service url");
          },
          scope: n
        });
      }, this.userId ? this.getCompassUrl() : i.getCurrentUser().then(
        ({ login: o }) => {
          n.userId = o, this.getCompassUrl();
        },
        () => this.getCompassUrl()
      );
    });
  },
  watch: {
    openCompass() {
      this.onResize();
    },
    shownRightPanel(e) {
      this.updateRightPanelsObservers(e);
    },
    outAnimate(e) {
      e && this.onResize(), e && setTimeout(() => {
        this.outAnimate = false;
      }, 500);
    }
  },
  methods: {
    addCustomAction(e) {
      const n = this.customItems.find(({ name: t }) => t === e.name);
      n ? this.customItems[this.customItems.indexOf(n)] = e : this.customItems.push(e);
    },
    clearCustomActions() {
      this.customItems = [];
    },
    showPanel(e, n = true) {
      if (!this.panelStates.length)
        return;
      n && this.hideAllPanels(e, false);
      const t = this.panelStates.find(({ name: i }) => e === i);
      this.showRightPanel || (this.inAnimate = true), t.show = true;
    },
    hidePanel(e) {
      if (!this.panelStates.length)
        return;
      const n = this.panelStates.find(({ name: t }) => e === t);
      n.show = false;
    },
    hideAllPanels(e = "", n = true) {
      this.panelStates.length && (this.panelStates.filter(({ name: t }) => t !== e).forEach((t) => {
        t.show = false;
      }), n && (this.outAnimate = true));
    },
    actionsMerge(e, n, t) {
      let i = e;
      return t || (i = e.slice(0, n.length).filter(({ name: o }) => n.find(({ name: s }) => o === s)), i = i.map((o) => ({
        // If component user messes up order \o/
        ...n.find(({ name: s }) => o.name === s),
        ...o
      }))), i = i.map((o) => {
        if (o.text === void 0) {
          const s = this.capitalize(o.name);
          o.text = s;
        }
        return o;
      }), i;
    },
    onResize(e) {
      const { clientWidth: n } = this.$refs.lightbox;
      let t;
      if (n > 639) {
        const i = Math.min(n * 0.125 + 240, 480);
        t = {
          responsive: false,
          left: {
            width: `${i}px`
          },
          center: {
            "margin-left": this.openCompass ? `${i}px` : 0,
            "margin-right": this.animatedPanels && e ? `${(e == null ? void 0 : e.clientWidth) || 0}px` : this.shownRightPanel ? `${i}px` : 0
          },
          right: {
            width: `${i}px`
          }
        };
      } else
        t = { responsive: true, center: {}, right: {} };
      this.transforms = t;
    },
    updateRightPanelsObservers(e) {
      if (this.rightPanelResizeObserver && (this.rightPanelResizeObserver.stop(), delete this.rightPanelResizeObserver), e) {
        const n = this.panelsRefs[e == null ? void 0 : e.name];
        this.rightPanelResizeObserver = Kn(n, () => {
          this.onResize(n.$el);
        });
      } else
        this.onResize();
    }
  },
  beforeUnmount() {
    this.resizeObserver && this.resizeObserver.disconnect(), delete this.resizeObserver;
  }
}, Nc = (e) => (pushScopeId("data-v-93b8f28b"), e = e(), popScopeId(), e), Rc = ["data-id"], Hc = /* @__PURE__ */ Nc(() => /* @__PURE__ */ createBaseVNode("div", { class: "lightbox__overlay" }, null, -1)), jc = ["src"], Uc = {
  key: 0,
  class: "panel__header"
}, Wc = { class: "panel__title" }, qc = { class: "panel__title__text" };
function Kc(e, n, t, i, o, s) {
  const a = resolveComponent("VuLightboxBar"), u = resolveComponent("VuIconBtn");
  return openBlock(), createElementBlock("div", null, [
    renderSlot(e.$slots, "lightbox-activator", {}, void 0, true),
    createBaseVNode("div", {
      ref: "lightbox",
      class: normalizeClass(["vu-lightbox", {
        "lightbox--responsive": o.transforms.responsive,
        "lightbox--widget-header": t.widget,
        "vu-lightbox--appear-faster": !t.widget && !t.noAnimation && t.fasterAnimation,
        "vu-lightbox--appear-fast": !t.widget && !t.noAnimation && !t.fasterAnimation,
        "overflow-hidden": t.animatedPanels
      }]),
      style: normalizeStyle({
        zIndex: t.zIndex
      }),
      "data-id": o.uid
    }, [
      createVNode(a, mergeProps({
        label: t.title,
        "show-compass": !s.noCompass,
        class: { "lightbox-bar--compass-open": o.openCompass },
        type: s.typeInfo,
        items: s._primaryActions,
        "sub-items": t.menuActions,
        "right-items": s._sideActions,
        responsive: o.transforms.responsive,
        "show-close-icon": !t.hideCloseIcon
      }, { disableCompass: t.disableCompass, customItems: o.customItems, dropdownOverlay: t.dropdownOverlay, widget: t.widget, moreActionsLabel: t.moreActionsLabel, closeLabel: t.closeLabel }, toHandlers({ ...s.listeners, ...s.listenersFromProps }), {
        onClickCompass: n[0] || (n[0] = () => {
          t.disableCompass || (o.openCompass = !o.openCompass, o.compassAlreadyOpened = true), e.$emit("click-compass", o.openCompass);
        })
      }), {
        "lightbox-bar__object-type": withCtx((c) => [
          renderSlot(e.$slots, "lightbox-bar__object-type", normalizeProps(guardReactiveProps(c)), void 0, true)
        ]),
        "lightbox-bar__title": withCtx((c) => [
          renderSlot(e.$slots, "lightbox-bar__title", normalizeProps(guardReactiveProps(c)), void 0, true)
        ]),
        "lightbox-bar__special-actions": withCtx(() => [
          renderSlot(e.$slots, "lightbox-bar__special-actions", {}, void 0, true)
        ]),
        _: 3
      }, 16, ["label", "show-compass", "class", "type", "items", "sub-items", "right-items", "responsive", "show-close-icon"]),
      Hc,
      createBaseVNode("div", {
        ref: "content",
        class: "lightbox__content",
        style: normalizeStyle(o.transforms.center || {})
      }, [
        renderSlot(e.$slots, "lightbox-content", {}, void 0, true)
      ], 4),
      !s.noCompass && o.compassAlreadyOpened ? withDirectives((openBlock(), createBlock(resolveDynamicComponent(t.animatedPanels ? "vu-side-panel" : "div"), {
        key: 0,
        modelValue: o.openCompass,
        side: e.left,
        class: normalizeClass(t.animatedPanels ? "" : "column"),
        topbar: true,
        animate: true,
        style: normalizeStyle(o.transforms.left || {})
      }, {
        default: withCtx(() => [
          createBaseVNode("iframe", {
            class: "compass",
            src: s.compassIframeUrl
          }, null, 8, jc),
          o.transforms.responsive ? (openBlock(), createBlock(u, {
            key: 0,
            icon: "close",
            style: { position: "absolute", right: "0", top: "0", zindex: "21" },
            onClick: n[1] || (n[1] = (c) => o.openCompass = false)
          })) : createCommentVNode("", true)
        ]),
        _: 1
      }, 8, ["modelValue", "side", "class", "style"])), [
        [vShow, o.openCompass]
      ]) : createCommentVNode("", true),
      (openBlock(true), createElementBlock(Fragment, null, renderList(s._panels, ({ name: c, show: d, showClose: h2 = false, showEdit: p2, classes: _ = [], title: v }, y) => withDirectives((openBlock(), createBlock(resolveDynamicComponent(t.animatedPanels ? "vu-side-panel" : "div"), mergeProps({
        key: `${o.uid}-${y}`,
        modelValue: typeof d == "object" ? d.value : d,
        class: ["vu-panel", [
          ..._,
          {
            "panel--responsive": o.transforms.responsive,
            "slide-in-right": o.inAnimate,
            "slide-out-right": o.outAnimate
          },
          c,
          t.animatedPanels ? "full-height flex-col side-panel--column" : ["column", "lightbox__panel", "lightbox__panel--right"]
        ]],
        animate: true,
        side: "right",
        ref_for: true,
        ref: (L) => {
          c && (o.panelsRefs[c] = L);
        },
        style: [
          t.animatedPanels ? "" : o.transforms.right
        ]
      }, t.animatedPanels ? {
        smallCloseIcon: true,
        title: v,
        showEdit: p2,
        showClose: h2,
        topbar: !o.transforms.responsive,
        vOnPanelEdit: () => e.$emit(`panel-edit-${c}`)
      } : {}, {
        "onUpdate:modelValue": (L) => {
          L || e.$emit(`close-panel-${c}`);
        }
      }), {
        default: withCtx(() => [
          !t.animatedPanels && v ? (openBlock(), createElementBlock("div", Uc, [
            createBaseVNode("span", Wc, [
              createBaseVNode("span", qc, toDisplayString(v), 1),
              p2 ? (openBlock(), createBlock(u, {
                key: 0,
                class: "panel__edit__icon",
                icon: "pencil",
                onClick: (L) => e.$emit(`panel-edit-${c}`)
              }, null, 8, ["onClick"])) : createCommentVNode("", true)
            ]),
            h2 ? (openBlock(), createBlock(u, {
              key: 0,
              class: "panel__close_icon",
              icon: "close",
              onClick: (L) => e.$emit(`close-panel-${c}`)
            }, null, 8, ["onClick"])) : createCommentVNode("", true)
          ])) : !t.animatedPanels && (o.transforms.responsive || h2) ? (openBlock(), createBlock(u, {
            key: 1,
            class: "panel__close_icon",
            icon: "close",
            onClick: (L) => e.$emit(`close-panel-${c}`)
          }, null, 8, ["onClick"])) : createCommentVNode("", true),
          createBaseVNode("div", {
            class: normalizeClass([`vu-dynamic-panel-wrap-${c}`, "panel__content"])
          }, [
            renderSlot(e.$slots, `lightbox-panel-${c}`, {}, void 0, true)
          ], 2)
        ]),
        _: 2
      }, 1040, ["modelValue", "class", "style", "onUpdate:modelValue"])), [
        [vShow, t.animatedPanels ? true : typeof d == "object" ? d.value : d]
      ])), 128))
    ], 14, Rc)
  ]);
}
const Gc = /* @__PURE__ */ F(Ec, [["render", Kc], ["__scopeId", "data-v-93b8f28b"]]), Yc = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Gc
}, Symbol.toStringTag, { value: "Module" })), Xc = {
  name: "vu-media-upload-droppable",
  props: {
    isOver: {
      type: Boolean
    },
    validDrop: {
      type: Boolean
    }
  },
  emits: ["drop"],
  inject: {
    vuMediaUploadDropText: {
      default: "Drop your files to upload"
    }
  },
  computed: {
    classes() {
      return {
        "vu-media-upload-droppable--valid": this.validDrop
      };
    }
  },
  mounted() {
  },
  beforeUnmount() {
  },
  methods: {},
  components: { VuIcon: ue }
}, Jc = { class: "vu-media-upload-droppable__icon" }, Zc = { class: "vu-media-upload-droppable__label" };
function Qc(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-media-upload-droppable", s.classes]),
    onDrop: n[0] || (n[0] = withModifiers((u) => e.$emit("drop", u), ["prevent", "stop"]))
  }, [
    renderSlot(e.$slots, "drop-main", {}, () => [
      createBaseVNode("div", Jc, [
        createVNode(a, {
          icon: "up",
          color: "none"
        })
      ])
    ]),
    renderSlot(e.$slots, "drop-alt", {}, () => [
      createBaseVNode("span", Zc, toDisplayString(s.vuMediaUploadDropText), 1)
    ])
  ], 34);
}
const Ti = /* @__PURE__ */ F(Xc, [["render", Qc]]), eh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ti
}, Symbol.toStringTag, { value: "Module" })), th = {
  name: "vu-media-upload-empty",
  components: { VuIcon: ue, VuBtn: Ye, VuIconLink: ds },
  props: {
    rich: {
      // default: true,
      type: Boolean
    }
  },
  emits: ["browse"],
  inject: {
    vuMediaUploadPlaceholderLong: {
      default: "Drag & Drop files here"
    },
    vuMediaUploadPlaceholder: {
      default: "Drag & Drop or"
    },
    vuMediaUploadOR: {
      default: "or"
    },
    vuMediaUploadBrowse: {
      default: "Browse Files"
    }
  }
}, nh = { class: "vu-media-upload-empty" }, sh = { class: "vu-media-upload-empty__OR" }, ih = { key: 1 };
function oh(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon"), u = resolveComponent("VuBtn"), c = resolveComponent("VuIconLink");
  return openBlock(), createElementBlock("div", nh, [
    createVNode(a, { icon: "drag-drop" }),
    t.rich ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
      createBaseVNode("span", null, toDisplayString(s.vuMediaUploadPlaceholderLong), 1),
      createBaseVNode("span", sh, toDisplayString(s.vuMediaUploadOR), 1),
      createVNode(u, {
        onClick: n[0] || (n[0] = (d) => e.$emit("browse")),
        color: "primary"
      }, {
        default: withCtx(() => [
          createTextVNode(toDisplayString(s.vuMediaUploadBrowse), 1)
        ]),
        _: 1
      })
    ], 64)) : (openBlock(), createElementBlock("div", ih, [
      createTextVNode(toDisplayString(s.vuMediaUploadPlaceholder), 1),
      createVNode(c, {
        onClick: n[1] || (n[1] = (d) => e.$emit("browse"))
      }, {
        default: withCtx(() => [
          createTextVNode(toDisplayString(s.vuMediaUploadBrowse), 1)
        ]),
        _: 1
      })
    ]))
  ]);
}
const xi = /* @__PURE__ */ F(th, [["render", oh], ["__scopeId", "data-v-4e3fec0a"]]), lh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: xi
}, Symbol.toStringTag, { value: "Module" })), ah = {
  name: "vu-media-upload-error",
  inject: {
    vuMediaUploadRetry: {
      default: "Retry"
    }
  },
  emits: ["retry"],
  props: {
    icon: {
      type: String,
      default: "attention"
    },
    // eslint-disable-next-line vue/require-prop-types
    errorBucket: {
      default: () => []
    }
  },
  components: { VuIconBtn: de, VuBtn: Ye }
}, rh = { class: "vu-media-upload-error" };
function uh(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconBtn"), u = resolveComponent("VuBtn");
  return openBlock(), createElementBlock("div", rh, [
    createVNode(a, {
      icon: t.icon,
      class: "vu-media-upload-error__icon"
    }, null, 8, ["icon"]),
    (openBlock(true), createElementBlock(Fragment, null, renderList(t.errorBucket, (c, d) => (openBlock(), createElementBlock("span", {
      class: "vu-media-upload-error__error_label",
      key: d
    }, toDisplayString(c), 1))), 128)),
    createVNode(u, {
      onClick: n[0] || (n[0] = (c) => e.$emit("retry")),
      class: "vu-media-upload-error__retry",
      small: ""
    }, {
      default: withCtx(() => [
        createTextVNode(toDisplayString(s.vuMediaUploadRetry), 1)
      ]),
      _: 1
    })
  ]);
}
const Mi = /* @__PURE__ */ F(ah, [["render", uh], ["__scopeId", "data-v-ba65e06d"]]), dh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Mi
}, Symbol.toStringTag, { value: "Module" })), ch = {
  name: "vu-progress-circular",
  mixins: [Tt],
  data() {
    return {
      progressAngle: this.value / this.total * 100 * 3.6,
      intervalId: null,
      completedView: this.value >= this.total
    };
  },
  props: {
    value: {
      default: 0,
      type: Number
    },
    total: {
      default: 100,
      type: Number
    },
    radius: {
      default: 60,
      type: Number
    },
    noHatch: {
      default: false,
      type: Boolean
    },
    unfilledColor: {
      type: String,
      default: "#d1d4d4"
      // $grey-4
    },
    color: {
      type: String,
      default: () => "default",
      validator(e) {
        return ["default", "success", "warning", "error"].includes(e);
      }
    },
    hexColor: {
      type: String,
      required: false,
      default: ""
    },
    speedModifier: {
      type: Number,
      default: 1
    }
  },
  watch: {
    total() {
      this.animateProgress();
    },
    value() {
      this.animateProgress();
    }
  },
  computed: {
    radiusPx() {
      return `${this.radius}px`;
    },
    formattedCompletedCount() {
      return this.value < this.total ? this.value : this.total;
    },
    progressPercentage() {
      return this.value / this.total * 100;
    },
    renderHatch() {
      return !this.noHatch && this.value < this.total;
    }
  },
  methods: {
    updateAngle(e) {
      this.completedView = false;
      const n = Math.abs(this.progressAngle - e);
      Math.round(this.progressAngle) < Math.round(e) ? n <= this.speedModifier ? this.progressAngle = e : this.progressAngle += this.speedModifier : Math.round(this.progressAngle) > Math.round(e) ? n <= this.speedModifier ? this.progressAngle = e : this.progressAngle -= this.speedModifier : (clearInterval(this.intervalId), this.value >= this.total && (this.completedView = true));
    },
    animateProgress() {
      this.intervalId && clearInterval(this.intervalId);
      const e = this.progressPercentage * 3.6;
      this.intervalId = setInterval(this.updateAngle.bind(this, e), 5);
    }
  },
  beforeUnmount() {
    this.intervalId && clearInterval(this.intervalId);
  }
}, hh = { class: "vu-progress-circular" }, fh = { class: "vu-progress-circular__content" };
function ph(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", hh, [
    createBaseVNode("div", {
      class: normalizeClass(["vu-progress-circular__circle", t.hexColor ? "" : `vu-progress-circular--${t.color}`]),
      style: normalizeStyle({
        background: `conic-gradient( currentcolor ${o.progressAngle}deg, ${t.unfilledColor} ${o.progressAngle}deg)`,
        width: s.radiusPx,
        height: s.radiusPx,
        color: t.hexColor !== void 0 && t.hexColor,
        "-webkit-mask": `radial-gradient(${t.radius * (2 / 5)}px, #0000 98%, #000)`
      })
    }, [
      s.renderHatch ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["vu-progress-circular__hatch-container", { "vu-progress-circular__hatch-clip": o.progressAngle < 180 }])
      }, [
        createBaseVNode("div", {
          class: "vu-progress-circular__hatch",
          style: normalizeStyle(`transform: rotate(${o.progressAngle}deg)`)
        }, null, 4)
      ], 2)) : createCommentVNode("", true)
    ], 6),
    createBaseVNode("div", fh, [
      o.completedView && this.$slots.complete ? renderSlot(e.$slots, "complete", { key: 0 }, void 0, true) : renderSlot(e.$slots, "default", { key: 1 }, () => [
        createVNode(Transition, {
          name: "fade",
          mode: "out-in"
        }, {
          default: withCtx(() => [
            createBaseVNode("div", {
              key: "uncomplete-view",
              style: normalizeStyle({ fontSize: `${t.radius / 5}px` })
            }, toDisplayString(Math.round(o.progressAngle / 360 * 100)) + "% ", 5)
          ]),
          _: 1
        })
      ], true)
    ])
  ]);
}
const Vi = /* @__PURE__ */ F(ch, [["render", ph], ["__scopeId", "data-v-f8e9018e"]]), mh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Vi
}, Symbol.toStringTag, { value: "Module" })), vh = {
  name: "vu-media-upload-loading",
  props: {
    progress: {
      type: Number,
      default: 0
    }
  },
  inject: {
    vuMediaUploadAbortButton: {
      default: "Abort"
    }
  },
  emits: ["upload-abort"],
  components: { VuProgressCircular: Vi, VuBtn: Ye }
}, gh = { class: "vu-media-upload-loading" };
function bh(e, n, t, i, o, s) {
  const a = resolveComponent("VuProgressCircular"), u = resolveComponent("VuBtn");
  return openBlock(), createElementBlock("div", gh, [
    createVNode(a, { value: t.progress }, null, 8, ["value"]),
    createVNode(u, {
      color: "default",
      onClick: n[0] || (n[0] = (c) => e.$emit("upload-abort")),
      small: "",
      class: "vu-media-upload-loading__abort"
    }, {
      default: withCtx(() => [
        createTextVNode(toDisplayString(s.vuMediaUploadAbortButton), 1)
      ]),
      _: 1
    })
  ]);
}
const Pi = /* @__PURE__ */ F(vh, [["render", bh], ["__scopeId", "data-v-beb3b627"]]), yh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Pi
}, Symbol.toStringTag, { value: "Module" })), _h = {
  name: "vu-media-upload-preview",
  computed: {
    videoSizer() {
      var i;
      const [e, n] = (i = this.displayRatio) == null ? void 0 : i.replace(",", "").split("/"), t = Number(e) / Number(n);
      return t ? { paddingBottom: `${1 / t * 100}%` } : void 0;
    }
  },
  props: {
    deleteIcon: {
      type: String,
      default: () => "trash"
    },
    src: {
      type: String,
      required: true
    },
    isVideo: {
      type: Boolean
    },
    videoControls: {
      type: Boolean,
      required: false
    },
    displayRatio: {
      type: String,
      default: () => "16 / 9"
    }
  },
  emits: ["delete"],
  components: { VuImage: it, VuIconBtn: de }
}, wh = ["src", "controls"];
function kh(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconBtn"), u = resolveComponent("VuImage"), c = resolveComponent("vu-spinner");
  return openBlock(), createElementBlock(Fragment, null, [
    t.isVideo ? (openBlock(), createElementBlock("div", {
      key: 0,
      class: "vu-media-upload-preview__video-container",
      style: normalizeStyle(s.videoSizer)
    }, [
      createBaseVNode("video", {
        class: "vu-media-upload-preview",
        src: t.src,
        controls: t.videoControls
      }, null, 8, wh)
    ], 4)) : t.isVideo ? e.loading ? (openBlock(), createBlock(c, { key: 2 })) : createCommentVNode("", true) : (openBlock(), createBlock(u, {
      key: 1,
      class: "vu-media-upload-preview",
      "aspect-ratio": t.displayRatio,
      src: t.src,
      contain: "",
      style: { height: "100%" }
    }, {
      default: withCtx(() => [
        createBaseVNode("div", {
          class: "vu-media-upload-preview__delete-icon",
          onClick: n[0] || (n[0] = (d) => e.$emit("delete"))
        }, [
          createVNode(a, { icon: t.deleteIcon }, null, 8, ["icon"])
        ])
      ]),
      _: 1
    }, 8, ["aspect-ratio", "src"])),
    t.isVideo ? (openBlock(), createElementBlock("div", {
      key: 3,
      class: "vu-media-upload-preview__delete-icon",
      onClick: n[1] || (n[1] = (d) => e.$emit("delete"))
    }, [
      createVNode(a, { icon: t.deleteIcon }, null, 8, ["icon"])
    ])) : createCommentVNode("", true)
  ], 64);
}
const Li = /* @__PURE__ */ F(_h, [["render", kh], ["__scopeId", "data-v-8449cad3"]]), Sh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Li
}, Symbol.toStringTag, { value: "Module" })), Ih = {
  empty: "empty",
  loading: "loading",
  error: "error",
  complete: "complete"
}, Ch = {
  name: "vu-media-upload",
  mixins: [Fe, $e, vt, ze, Ee],
  props: {
    icon: {
      type: String,
      default: () => ""
    },
    mediaUrl: {
      type: String,
      default: () => ""
    },
    video: {
      type: Boolean,
      default: false
    },
    videoControls: {
      type: Boolean,
      default: true
    },
    uploadProgress: {
      type: Number,
      required: false,
      default: void 0
    },
    fileMaxSize: {
      type: Number,
      default: () => 1 / 0
    },
    displayRatio: {
      type: String,
      default: () => "16 / 9"
    },
    showLabel: {
      type: Boolean
    },
    multiple: {
      type: Boolean
    },
    allowLoadingDrop: {
      type: Boolean
    },
    allowErrorDrop: {
      type: Boolean
    },
    skipTypeCheck: {
      type: Boolean,
      required: false
    },
    noDragNDrop: {
      type: Boolean,
      required: false
    },
    acceptVideo: Boolean,
    acceptImage: {
      type: Boolean,
      default: true
    },
    state: {
      type: String,
      default: ""
    }
  },
  inject: {
    vuMediaUploadSizeExcess: {
      default: "File exceeds maximum size."
    },
    vuMediaUploadShouldBeImage: {
      default: "Please select an image."
    },
    vuMediaUploadShouldBeVideo: {
      default: "Please select a video."
    }
  },
  data() {
    return {
      states: Ih,
      innerState: "empty",
      innerVideo: false,
      allowDrop: false,
      dragged: false,
      error: ""
    };
  },
  created() {
    this.localRules = [this.checkVideoType, this.checkImgType, this.checkFileSize];
  },
  emits: ["update:state", "upload-abort", "select", "delete", "retry"],
  computed: {
    preview() {
      return {
        src: this.mediaUrl,
        isVideo: this.video || this.innerVideo,
        displayRatio: this.displayRatio,
        videoControls: this.videoControls
      };
    },
    hasLabel() {
      return this.showLabel && !this.multiple;
    },
    wrapStyle() {
      return {
        "aspect-ratio": this.displayRatio
      };
    },
    status: {
      get() {
        return this.state || this.innerState;
      },
      set(e) {
        this.$emit("update:state", e), this.innerState = e;
      }
    }
  },
  watch: {
    hasError(e) {
      e && (this.status = this.states.error);
    }
  },
  methods: {
    selectFiles(e) {
      this.multiple && e.length > 1 ? (this.status = this.states.loading, this.$emit("select", e)) : this.skipTypeCheck ? (this.status = this.states.loading, this.$emit("select", e)) : this.validate(e[0]) && (this.status = this.states.loading, this.$emit("select", e));
    },
    dragOver() {
      this.noDragNDrop || this.state !== this.states.complete && (this.state === this.states.loading && !this.allowLoadingDrop || this.state === this.states.error && !this.allowErrorDrop || (this.allowDrop = true, this.dragged = true));
    },
    dragLeave(e) {
      e.currentTarget.contains(e.relatedTarget) || (this.dragged = false, this.allowDrop = false);
    },
    onFileDrop(e) {
      this.dragged = false, this.allowDrop = false, this.status = this.states.loading, this.selectFiles(e.dataTransfer.files);
    },
    checkFileSize({ size: e }) {
      return this.fileMaxSize && e / 1024 / 1024 >= this.fileMaxSize ? this.vuMediaUploadSizeExcess : true;
    },
    /* 3 checks disablable with skipTypeCheck */
    checkImgType({ type: e }) {
      if (this.acceptImage) {
        const n = /image\/(jpg|jpeg|png|webp)$/i.test(e);
        if (n && (this.innerVideo = false), !this.acceptVideo)
          return n || this.vuMediaUploadShouldBeImage;
      }
      return true;
    },
    checkVideoType({ type: e }) {
      if (this.acceptVideo) {
        const n = /video\/(mp4|avi)$/i.test(e);
        if (this.innerVideo = n, !this.acceptImage)
          return n || this.vuMediaUploadShouldBeVideo;
      }
      return true;
    },
    checkVideoAndImgType({ type: e }) {
      return this.acceptVideo && this.acceptImage ? /video\/(mp4|avi)$/i.test(e) && /image\/(jpg|jpeg|png|webp)$/i.test(e) || this.vuMediaUploadTypeUnexpected : true;
    },
    onRetry() {
      this.errorBucket = [], this.status = this.states.empty, this.$emit("retry", this.$refs["upload-input"].value);
    }
  },
  components: { VuIcon: ue, VuMediaUploadDroppable: Ti, VuMediaUploadLoading: Pi, VuMediaUploadError: Mi, VuMediaUploadEmpty: xi, VuMediaUploadPreview: Li }
}, Bh = {
  key: 0,
  class: "control-label"
}, $h = {
  key: 0,
  class: "label-field-required"
}, Oh = ["multiple"];
function Th(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon"), u = resolveComponent("VuMediaUploadDroppable"), c = resolveComponent("VuMediaUploadEmpty"), d = resolveComponent("VuMediaUploadLoading"), h2 = resolveComponent("VuMediaUploadError"), p2 = resolveComponent("vuMediaUploadPreview");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-media-upload", [{ "has-error": o.error, "vu-media-upload--border": !s.hasLabel, "vu-media-upload--inner-flex": s.hasLabel }]]),
    style: normalizeStyle(s.hasLabel ? {} : s.wrapStyle)
  }, [
    s.hasLabel ? (openBlock(), createElementBlock("label", Bh, [
      t.icon ? (openBlock(), createBlock(a, {
        key: 0,
        icon: t.icon
      }, null, 8, ["icon"])) : createCommentVNode("", true),
      renderSlot(e.$slots, "label", {}, () => [
        createTextVNode(toDisplayString(e.label), 1),
        e.required ? (openBlock(), createElementBlock("span", $h, " *")) : createCommentVNode("", true)
      ], true)
    ])) : createCommentVNode("", true),
    createBaseVNode("input", {
      ref: "upload-input",
      type: "file",
      name: "upload",
      style: { display: "none" },
      onChange: n[0] || (n[0] = (_) => s.selectFiles(e.$refs["upload-input"].files)),
      multiple: t.multiple
    }, null, 40, Oh),
    createBaseVNode("div", {
      class: normalizeClass(["vu-media-upload__inner", { "vu-media-upload--border": s.hasLabel, "full-height": !s.hasLabel }]),
      ref: "inner",
      style: normalizeStyle(s.hasLabel ? s.wrapStyle : ""),
      onDragover: n[4] || (n[4] = withModifiers((_) => s.dragOver(), ["prevent"])),
      onDragenter: n[5] || (n[5] = withModifiers((_) => s.dragOver(), ["prevent"])),
      onDragleave: n[6] || (n[6] = (..._) => s.dragLeave && s.dragLeave(..._)),
      onDragend: n[7] || (n[7] = (..._) => s.dragLeave && s.dragLeave(..._))
    }, [
      o.dragged ? (openBlock(), createBlock(u, {
        key: 0,
        "valid-drop": o.allowDrop,
        onDrop: s.onFileDrop
      }, {
        "drop-icon": withCtx(() => [
          renderSlot(e.$slots, "drop-icon", {}, void 0, true)
        ]),
        "drop-label": withCtx(() => [
          renderSlot(e.$slots, "drop-label", {}, void 0, true)
        ]),
        _: 3
      }, 8, ["valid-drop", "onDrop"])) : createCommentVNode("", true),
      s.status === o.states.empty ? renderSlot(e.$slots, "empty", {
        key: 1,
        input: e.$refs["upload-input"]
      }, () => [
        createVNode(c, {
          onBrowse: n[1] || (n[1] = (_) => {
            e.$refs["upload-input"].value = "", e.$refs["upload-input"].click();
          })
        })
      ], true) : s.status === o.states.loading ? renderSlot(e.$slots, "loading", { key: 2 }, () => [
        createVNode(d, {
          progress: t.uploadProgress,
          onUploadAbort: n[2] || (n[2] = (_) => e.$emit("upload-abort"))
        }, null, 8, ["progress"])
      ], true) : s.status === o.states.error ? renderSlot(e.$slots, "error", { key: 3 }, () => [
        createVNode(h2, mergeProps({ onRetry: s.onRetry }, { errorBucket: e.errorBucket }), null, 16, ["onRetry"])
      ], true) : s.status === o.states.complete ? renderSlot(e.$slots, "preview", { key: 4 }, () => [
        createVNode(p2, mergeProps(s.preview, {
          onDelete: n[3] || (n[3] = (_) => {
            e.$emit("delete"), s.status = o.states.empty;
          })
        }), null, 16)
      ], true) : createCommentVNode("", true)
    ], 38)
  ], 6);
}
const xh = /* @__PURE__ */ F(Ch, [["render", Th], ["__scopeId", "data-v-bc0ccd8d"]]), Mh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: xh
}, Symbol.toStringTag, { value: "Module" }));
var Ln = /* @__PURE__ */ ((e) => (e[e.TIMEOUT = 0] = "TIMEOUT", e[e.CLOSE = 1] = "CLOSE", e[e.PROGRAMMATICALLY = 2] = "PROGRAMMATICALLY", e))(Ln || {});
let ct = {
  create: () => {
  },
  hide: () => {
  },
  _exists: () => false,
  _register: () => {
  },
  _installed: false
};
Object.defineProperty(ct, "_installed", {
  value: false,
  writable: false
});
function Vh(e, n) {
  const t = reactive([]), i = reactive({}), o = e.runWithContext(() => inject("vuDebug", { default: false }));
  if (n && ct._installed) {
    o && console.warn("VUEKIT - Message API install was called more than once. Using namespace on message container will help.");
    return;
  }
  ct._installed && o && console.warn("VUEKIT - Message API mutliple installation might affect other default container namespaces.");
  const s = markRaw({
    _messages: i,
    namespaces: t,
    _installed: true,
    create(a, u) {
      const { target: c = "main" } = a;
      if (!this._exists(c))
        throw new Error("Target namespace is unknown");
      const d = reactive({
        id: crypto.randomUUID(),
        bind: {
          target: c,
          ...a,
          disposed: false
        },
        onDispose: u,
        dispose(h2) {
          s.hide(
            d,
            h2 ?? 2
            /* PROGRAMMATICALLY */
          );
        }
      });
      return this._messages[c].push(d), d;
    },
    hide(a, u) {
      var h2;
      if (!a)
        return;
      const { target: c = "main" } = a.bind, d = this._messages[c].findIndex((p2) => p2.id === a.id);
      if (d !== -1) {
        const p2 = this._messages[c][d];
        this._messages[c].splice(d, 1), p2.bind.disposed = true, (h2 = p2.onDispose) == null || h2.call(p2, u);
      }
    },
    _exists(a) {
      return t.includes(a);
    },
    _register(a) {
      t.push(a), this._messages[a] = shallowReactive([]);
    }
  });
  ct = s, e.provide("vuMessageAPI", ct), e.config.globalProperties.$vuMessage = ct;
}
const Ph = {
  name: "vu-message",
  mixins: [xt, Tt],
  components: { VuIconLink: ds },
  props: {
    text: {
      type: String,
      default: () => ""
    },
    closable: {
      type: Boolean,
      default: () => true
    },
    color: {
      type: String,
      default: () => "primary"
    },
    animate: {
      type: Boolean,
      default: () => true
    },
    showProgress: {
      type: Boolean,
      default: () => false
    },
    link: {
      type: String,
      required: false
    },
    linkHandler: {
      type: Function,
      required: false,
      default: () => () => {
      }
    },
    timeout: {
      type: Number,
      default: () => 0
    },
    animationDuration: {
      type: Number,
      default: 500
    },
    target: String
  },
  emits: ["update:show", "click-link"],
  data: () => ({
    DISPOSE_REASON: Ln,
    activeTimeout: 0,
    activeTimer: 0,
    in: true,
    timerStart: 0,
    timer: 0
  }),
  inject: {
    noProgress: {
      from: ts
    }
  },
  computed: {
    colored() {
      return !!this.color;
    },
    classes() {
      return [`alert-${this.color}`, {
        "alert-closable": this.closable
      }];
    }
  },
  watch: {
    timeout() {
      this.setTimeout();
    },
    text() {
      this.setTimeout();
    },
    show(e) {
      e && this.setTimeout();
    }
  },
  beforeUnmount() {
    window.clearInterval(this.activeTimer), window.clearTimeout(this.activeTimeout);
  },
  mounted() {
    this.setTimeout();
  },
  methods: {
    dispose(e) {
      this.$emit("update:show", false, e);
    },
    clearTimers() {
      window.clearInterval(this.activeTimer), window.clearTimeout(this.activeTimeout), this.timerStart = 0, this.timer = 0;
    },
    setTimeout() {
      this.clearTimers(), this.show && this.timeout && (this.timerStart = Date.now(), this.activeTimeout = window.setTimeout(() => {
        this.dispose(Ln.TIMEOUT);
      }, this.timeout + 100), this.showProgress && (this.activeTimer = window.setInterval(() => {
        this.timer = Date.now() - this.timerStart;
      }, 1e3 / 30)));
    }
  }
}, Lh = {
  key: 0,
  class: "icon fonticon"
}, Dh = { class: "alert-message-wrap" }, Ah = ["innerHTML"], Fh = ["max", "value"];
function zh(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconLink");
  return e.show ? (openBlock(), createElementBlock("div", {
    key: 0,
    class: normalizeClass(["vu-message relative alert-has-icon", s.classes])
  }, [
    s.colored ? (openBlock(), createElementBlock("span", Lh)) : createCommentVNode("", true),
    createBaseVNode("span", Dh, [
      renderSlot(e.$slots, "default", {}, () => [
        createBaseVNode("div", { innerHTML: t.text }, null, 8, Ah)
      ], true)
    ]),
    t.link ? (openBlock(), createBlock(a, {
      key: 1,
      label: t.link,
      class: "vu-message_link",
      onClick: n[0] || (n[0] = () => {
        e.$emit("click-link", e.linkData), t.linkHandler();
      })
    }, null, 8, ["label"])) : createCommentVNode("", true),
    t.closable ? (openBlock(), createElementBlock("span", {
      key: 2,
      class: "close fonticon fonticon-cancel",
      onClick: n[1] || (n[1] = (u) => s.dispose(e.DISPOSE_REASON.CLOSE))
    })) : createCommentVNode("", true),
    t.showProgress && t.timeout && !s.noProgress ? (openBlock(), createElementBlock("progress", {
      key: 3,
      class: normalizeClass(`progress animated bottom-0 absolute h-1! progress-${t.color}`),
      max: t.timeout,
      value: Math.min(e.timer, t.timeout)
    }, null, 10, Fh)) : createCommentVNode("", true)
  ], 2)) : createCommentVNode("", true);
}
const Di = /* @__PURE__ */ F(Ph, [["render", zh], ["__scopeId", "data-v-551eda6a"]]), Eh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Di
}, Symbol.toStringTag, { value: "Module" })), Nh = {
  name: "vu-message-container",
  props: {
    namespace: {
      type: String,
      default: "main"
    }
  },
  created() {
    this.api = ct, this.api._exists(this.namespace) ? this.disabled = true : this.api._register(this.namespace);
  },
  data: () => ({
    api: {},
    disabled: false
  }),
  components: { VuMessage: Di }
}, Rh = {
  key: 0,
  class: "alert alert-root",
  style: { visibility: "visible" }
};
function Hh(e, n, t, i, o, s) {
  const a = resolveComponent("VuMessage");
  return e.disabled ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", Rh, [
    createVNode(TransitionGroup, { name: "fade" }, {
      default: withCtx(() => {
        var u;
        return [
          (openBlock(true), createElementBlock(Fragment, null, renderList((u = e.api._messages) == null ? void 0 : u[t.namespace], (c) => (openBlock(), createBlock(a, mergeProps(c.bind, {
            show: "",
            key: `${c.id}`,
            "onUpdate:show": (d, h2) => e.api.hide(c, h2)
          }), null, 16, ["onUpdate:show"]))), 128))
        ];
      }),
      _: 1
    })
  ]));
}
const jh = /* @__PURE__ */ F(Nh, [["render", Hh], ["__scopeId", "data-v-d65705bb"]]), Uh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: jh
}, Symbol.toStringTag, { value: "Module" })), Wh = {
  name: "vu-mobile-dialog",
  emits: ["close", "confirm"],
  components: { VuScroller: Qe, VuIconBtn: de },
  props: {
    title: {
      type: String,
      default: ""
    },
    backIcon: {
      type: String,
      default: "close"
    },
    backIconTooltip: {
      type: String,
      default: "Close"
    },
    nextIcon: {
      type: String,
      default: "check"
    },
    nextIconTooltip: {
      type: String,
      default: "OK"
    },
    scrollable: {
      type: Boolean,
      default: true
    },
    customNextIcon: {
      type: Boolean
    },
    customBackIcon: {
      type: Boolean
    },
    nextIconDisabled: {
      type: Boolean
    }
  },
  computed: {
    _backIcon() {
      return this.customBackIcon ? this.backIcon : ["chevron-left", "close"].includes(this.backIcon) ? this.backIcon : "-";
    },
    _icon() {
      return this.customNextIcon ? this.nextIcon : ["chevron-right", "send", "check"].includes(this.nextIcon) ? this.nextIcon : "-";
    },
    backClasses() {
      return [this._backIcon === "chevron-left" ? "chevron" : ""];
    },
    nextClasses() {
      return [this._icon === "chevron-right" ? "chevron" : ""];
    }
  }
}, qh = { class: "vu-mobile-dialog" }, Kh = { class: "vu-mobile-dialog__header" }, Gh = { class: "vu-mobile-dialog__header__default" }, Yh = {
  class: "vu-label-wrap",
  style: { overflow: "hidden" }
};
function Xh(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconBtn"), u = resolveComponent("VuScroller"), c = resolveDirective("tooltip");
  return openBlock(), createElementBlock("div", qh, [
    createBaseVNode("div", Kh, [
      renderSlot(e.$slots, "mobile-dialog-header", {}, () => [
        createBaseVNode("div", Gh, [
          withDirectives(createVNode(a, {
            icon: s._backIcon,
            class: normalizeClass([s.backClasses, "vu-mobile-dialog__header_back topbar"]),
            onClick: n[0] || (n[0] = (d) => e.$emit("close"))
          }, null, 8, ["icon", "class"]), [
            [
              c,
              t.backIconTooltip,
              void 0,
              { bottom: true }
            ]
          ]),
          createBaseVNode("div", Yh, [
            withDirectives((openBlock(), createElementBlock("label", null, [
              createTextVNode(toDisplayString(t.title), 1)
            ])), [
              [
                c,
                t.title,
                void 0,
                { bottom: true }
              ]
            ])
          ]),
          withDirectives(createVNode(a, {
            icon: s._icon,
            class: normalizeClass([s.nextClasses, "vu-mobile-dialog__header_next topbar"]),
            disabled: t.nextIconDisabled,
            onClick: n[1] || (n[1] = (d) => e.$emit("confirm"))
          }, null, 8, ["icon", "class", "disabled"]), [
            [
              c,
              t.nextIconTooltip,
              void 0,
              { bottom: true }
            ]
          ])
        ])
      ], true)
    ]),
    createBaseVNode("div", {
      class: normalizeClass(["vu-mobile-dialog__content", `vu-mobile-dialog__content--${t.scrollable ? "" : "non-"}scrollable`])
    }, [
      t.scrollable ? (openBlock(), createBlock(u, { key: 0 }, {
        default: withCtx(() => [
          renderSlot(e.$slots, "default", {}, void 0, true)
        ]),
        _: 3
      })) : renderSlot(e.$slots, "default", { key: 1 }, void 0, true)
    ], 2)
  ]);
}
const Ai = /* @__PURE__ */ F(Wh, [["render", Xh], ["__scopeId", "data-v-e2c44982"]]), Jh = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ai
}, Symbol.toStringTag, { value: "Module" })), Zh = {
  name: "vu-modal",
  data: () => ({
    model: "",
    mobileWidth: false,
    resizeObs: {},
    pick: ls,
    pickNRename: Ya
  }),
  emits: ["close", "cancel", "confirm"],
  mixins: [xt],
  props: {
    show: {
      type: Boolean,
      required: false,
      default: () => false
    },
    keepRendered: {
      type: Boolean,
      default: () => false
    },
    title: {
      type: String,
      default: () => ""
    },
    message: {
      type: String,
      default: () => ""
    },
    rawContent: {
      type: String,
      default: ""
    },
    keyboard: {
      type: Boolean,
      default: () => true
    },
    showCancelIcon: {
      type: Boolean,
      default: () => true
    },
    showCancelButton: {
      type: Boolean,
      default: () => false
    },
    showFooter: {
      type: Boolean,
      default: () => true
    },
    showInput: {
      type: Boolean,
      default: () => false
    },
    /* input props */
    label: {
      type: String,
      default: () => ""
    },
    helper: {
      type: String,
      default: () => ""
    },
    placeholder: {
      type: String,
      default: () => ""
    },
    rules: {
      type: Array,
      default: () => []
    },
    required: {
      type: Boolean,
      default: () => true
    },
    success: {
      type: Boolean,
      default: () => false
    },
    disableKeyboardConfirm: {
      type: Boolean,
      default: false
    },
    autofocus: {
      type: Boolean,
      required: false
    },
    autofocusRef: {
      type: Object,
      required: false
    },
    /* input props */
    cancelLabel: {
      type: String,
      default: () => "Cancel"
    },
    okLabel: {
      type: String,
      default: () => "OK"
    },
    formRef: {
      type: Object,
      required: false
    },
    /* mobile specific props */
    noMobile: {
      type: Boolean
    },
    mobileNextIcon: {
      type: String
    },
    mobileNextIconTooltip: {
      type: String
    },
    mobileCustomNextIcon: {
      type: Boolean
    },
    mobileNextIconDisabled: {
      type: Boolean
    },
    mobileBackIcon: {
      type: String
    },
    mobileBackIconTooltip: {
      type: String
    },
    mobileCustomBackIcon: {
      type: Boolean
    },
    mobileScrollable: {
      type: Boolean
    },
    /* cancel */
    // eslint-disable-next-line vue/prop-name-casing
    _cancel: Boolean
  },
  inject: {
    vuMobileBreakpoint: {
      default: () => "640"
    }
  },
  watch: {
    _cancel(e) {
      e && this.cancel();
    },
    show: {
      immediate: true,
      handler(e) {
        this.innerShow = !!e;
      }
    }
  },
  beforeMount() {
    this.noMobile || (this.checkWidth(), window.addEventListener("resize", this.checkWidth));
  },
  mounted() {
    this.show && this.setFocus();
  },
  beforeUnmount() {
    window.removeEventListener("resize", this.checkWidth);
  },
  updated() {
    this.show && this.setFocus();
  },
  methods: {
    cancel(e = false, n = false) {
      this.innerShow = false, e ? this.$emit(e ? "close" : "cancel") : this.$emit(n && this.showCancelButton ? "cancel" : "close"), this.showInput && this.clear();
    },
    confirm() {
      !this.showInput && !this.formRef ? (this.$emit("confirm", true), this.innerShow = false) : this.validate() && (this.$emit("confirm", this.model), this.innerShow = false, this.clear());
    },
    validate(e) {
      var n;
      return typeof ((n = this.formRef) == null ? void 0 : n.validate) == "function" ? this.formRef.validate() : this.$refs.form.validate(e);
    },
    clear() {
      this.model = "";
    },
    checkWidth() {
      window.document.documentElement.clientWidth < this.vuMobileBreakpoint ? this.mobileWidth = true : this.mobileWidth = false;
    },
    setFocus() {
      var e;
      if (this.autofocus || this.autofocusRef) {
        const n = (e = this.$refs) == null ? void 0 : e.input;
        this.autofocusRef ? this.autofocusRef.focus() : n && n.focus();
      }
    }
  },
  components: { VuMobileDialog: Ai, VuInput: Ci, VuForm: wi, VuBtn: Ye }
}, Qh = { key: 0 }, ef = ["innerHTML"], tf = { key: 1 }, nf = {
  class: "vu-modal modal modal-root",
  style: { display: "block" }
}, sf = { class: "modal-wrap" }, of = { class: "modal-header" }, lf = { class: "modal-body" }, af = ["innerHTML"], rf = { key: 1 }, uf = {
  key: 0,
  class: "modal-footer"
}, df = /* @__PURE__ */ createBaseVNode("div", { class: "modal-overlay in" }, null, -1);
function cf(e, n, t, i, o, s) {
  const a = resolveComponent("VuInput"), u = resolveComponent("VuForm"), c = resolveComponent("VuMobileDialog"), d = resolveComponent("VuBtn");
  return t.keepRendered || e.innerShow ? withDirectives((openBlock(), createElementBlock("div", Qh, [
    !t.noMobile && e.mobileWidth ? (openBlock(), createBlock(c, mergeProps({ key: 0 }, {
      ...e.pick(e.$props, "title"),
      ...e.pickNRename(
        e.$props,
        { key: "mobileBackIcon", newName: "backIcon" },
        { key: "mobileBackIconTooltip", newName: "backIconTooltip" },
        { key: "mobileCustomBackIcon", newName: "customBackIcon" },
        { key: "mobileNextIcon", newName: "nextIcon" },
        { key: "mobileNextIconTooltip", newName: "nextIconTooltip" },
        { key: "mobileNextIconDisabled", newName: "nextIconDisabled" },
        { key: "mobileCustomNextIcon", newName: "customNextIcon" },
        { key: "mobileScrollable", newName: "scrollable" }
      ),
      disabled: e.valid
    }, {
      onClose: n[1] || (n[1] = (h2) => s.cancel()),
      onConfirm: n[2] || (n[2] = (h2) => s.confirm())
    }), {
      "mobile-dialog-header": withCtx(() => [
        renderSlot(e.$slots, "mobile-header")
      ]),
      default: withCtx(() => [
        renderSlot(e.$slots, "modal-body", {}, () => [
          t.rawContent ? (openBlock(), createElementBlock("div", {
            key: 0,
            innerHTML: t.rawContent
          }, null, 8, ef)) : t.message ? (openBlock(), createElementBlock("p", tf, toDisplayString(t.message), 1)) : createCommentVNode("", true),
          t.showInput ? (openBlock(), createBlock(u, {
            key: 2,
            ref: "form"
          }, {
            default: withCtx(() => [
              createVNode(a, {
                modelValue: e.model,
                "onUpdate:modelValue": n[0] || (n[0] = (h2) => e.model = h2),
                ref: "input",
                label: t.label,
                required: t.required,
                helper: t.helper,
                success: t.success,
                placeholder: t.placeholder,
                rules: t.rules
              }, null, 8, ["modelValue", "label", "required", "helper", "success", "placeholder", "rules"])
            ]),
            _: 1
          }, 512)) : createCommentVNode("", true)
        ])
      ]),
      _: 3
    }, 16)) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
      createBaseVNode("div", nf, [
        createBaseVNode("div", sf, [
          createBaseVNode("div", {
            class: "modal-content",
            onKeyup: [
              n[6] || (n[6] = withKeys(() => {
                t.keyboard && !t.disableKeyboardConfirm && s.confirm();
              }, ["enter"])),
              n[7] || (n[7] = withKeys(() => {
                t.keyboard && s.cancel();
              }, ["escape"]))
            ]
          }, [
            createBaseVNode("div", of, [
              renderSlot(e.$slots, "modal-header", {}, () => [
                t.showCancelIcon ? (openBlock(), createElementBlock("span", {
                  key: 0,
                  class: "close fonticon fonticon-cancel",
                  title: "",
                  onClick: n[3] || (n[3] = (h2) => s.cancel(true))
                })) : createCommentVNode("", true),
                createBaseVNode("h4", null, toDisplayString(t.title), 1)
              ])
            ]),
            createBaseVNode("div", lf, [
              renderSlot(e.$slots, "modal-body", {}, () => [
                t.rawContent ? (openBlock(), createElementBlock("div", {
                  key: 0,
                  innerHTML: t.rawContent
                }, null, 8, af)) : t.message ? (openBlock(), createElementBlock("p", rf, toDisplayString(t.message), 1)) : createCommentVNode("", true),
                t.showInput ? (openBlock(), createBlock(u, {
                  key: 2,
                  ref: "form"
                }, {
                  default: withCtx(() => [
                    createVNode(a, {
                      modelValue: e.model,
                      "onUpdate:modelValue": n[4] || (n[4] = (h2) => e.model = h2),
                      ref: "input",
                      label: t.label,
                      required: t.required,
                      helper: t.helper,
                      success: t.success,
                      placeholder: t.placeholder,
                      rules: t.rules
                    }, null, 8, ["modelValue", "label", "required", "helper", "success", "placeholder", "rules"])
                  ]),
                  _: 1
                }, 512)) : createCommentVNode("", true)
              ])
            ]),
            t.showFooter ? (openBlock(), createElementBlock("div", uf, [
              renderSlot(e.$slots, "modal-footer", {}, () => [
                createVNode(d, {
                  color: "primary",
                  onClick: s.confirm
                }, {
                  default: withCtx(() => [
                    createTextVNode(toDisplayString(t.okLabel), 1)
                  ]),
                  _: 1
                }, 8, ["onClick"]),
                t.showCancelButton ? (openBlock(), createBlock(d, {
                  key: 0,
                  color: "default",
                  onClick: n[5] || (n[5] = (h2) => s.cancel())
                }, {
                  default: withCtx(() => [
                    createTextVNode(toDisplayString(t.cancelLabel), 1)
                  ]),
                  _: 1
                })) : createCommentVNode("", true)
              ])
            ])) : createCommentVNode("", true)
          ], 32)
        ])
      ]),
      df
    ], 64))
  ], 512)), [
    [vShow, e.innerShow]
  ]) : createCommentVNode("", true);
}
const cs = /* @__PURE__ */ F(Zh, [["render", cf]]), hf = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: cs
}, Symbol.toStringTag, { value: "Module" }));
let Ht = {
  show: () => {
  },
  hide: () => {
  },
  alert: () => {
  },
  confirm: () => {
  },
  prompt: () => {
  },
  _modals: shallowReactive([])
};
function ff(e) {
  const n = shallowReactive([]);
  return Ht = markRaw({
    _modals: n,
    show(i) {
      return this.hide(), new Promise((o, s) => {
        const a = {
          id: Re(),
          component: cs,
          bind: reactive({ ...i, show: true }),
          on: {
            close: () => {
              this.hide(a), s();
            },
            confirm: (u) => {
              this.hide(a), o(u);
            },
            cancel: () => {
              this.hide(a), s();
            }
          }
        };
        this._modals.push(shallowReactive(a));
      });
    },
    hide(i) {
      if (i) {
        const o = this._modals.find((s) => s.id === i.id);
        if (!o)
          return;
        o.bind.show = false, setTimeout(() => {
          const s = this._modals.findIndex((a) => a.id === i.id);
          s > -1 && this._modals.splice(s, 1);
        }, 1e3);
      } else
        this._modals.forEach((o) => {
          o._cancel = true;
        }), this._modals.splice(0, this._modals.length);
    },
    alert(i) {
      return this.show(i);
    },
    confirm(i) {
      return this.show({
        showCancelIcon: true,
        showCancelButton: true,
        ...i
      });
    },
    prompt(i) {
      return this.show({
        showCancelIcon: true,
        showCancelButton: true,
        showInput: true,
        ...i
      });
    }
  }), e.provide("vuModalAPI", Ht), e.config.globalProperties.$vuModal = Ht, Ht;
}
const pf = {
  name: "vu-modal-container",
  components: {
    VuModal: cs
  },
  data: () => ({
    // eslint-disable-next-line vue/no-reserved-keys
    _modals: {
      type: Object
    }
  }),
  created() {
    this._modals = Ht._modals;
  }
};
function mf(e, n, t, i, o, s) {
  return openBlock(true), createElementBlock(Fragment, null, renderList(e._modals, (a) => (openBlock(), createBlock(resolveDynamicComponent(a.component), mergeProps({
    key: a.id
  }, a.bind, {
    modelValue: a.value,
    "onUpdate:modelValue": (u) => a.value = u
  }, toHandlers(a.on)), null, 16, ["modelValue", "onUpdate:modelValue"]))), 128);
}
const vf = /* @__PURE__ */ F(pf, [["render", mf]]), gf = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: vf
}, Symbol.toStringTag, { value: "Module" }));
function Ls(e, n) {
  var t;
  e.value > -1 ? e.value -= 1 : e.value = (((t = n.value) == null ? void 0 : t.length) || 0) - 1;
}
function Ds(e, n) {
  var t;
  e.value > (((t = n.value) == null ? void 0 : t.length) || 0) - 2 ? e.value = -1 : e.value += 1;
}
function en(e, n, t, i, o) {
  const { target: s = false } = t;
  if (s instanceof HTMLInputElement) {
    const { selectionStart: a } = s;
    return e && s.value || n && a !== 0;
  }
  return false;
}
function As(e, n) {
  const {
    target: t,
    items: i,
    debug: o = false,
    disabled: s = false
  } = e || {}, {
    direction: a = "vertical",
    discardWhenValue: u = false,
    discardWhenCaretInBetween: c = false,
    preserveIndexOnRemoval: d = false
  } = n || {};
  if (!t) {
    o && console.warn("VUEKIT - Warning Keyboard Navigation cannot be applied. Please use onMount hook and check target element is mounted.");
    return;
  }
  const h2 = a === "vertical", p2 = ref(-1);
  watch(i, (v, y) => {
    d && (v == null ? void 0 : v.length) < (y == null ? void 0 : y.length) ? p2.value === y.length - 1 && (p2.value = v.length - 1) : p2.value = -1;
  });
  const _ = tl(p2, { initialValue: -1 });
  return !h2 && Bs("ArrowLeft", (v) => {
    s || en(u, c, v) || Ls(p2, i);
  }, { target: t, dedupe: true }), !h2 && Bs("ArrowRight", (v) => {
    if (!(s || en(u, c, v))) {
      if (c)
        if (p2.value !== -1)
          v.preventDefault();
        else
          return;
      Ds(p2, i);
    }
  }, { target: t, dedupe: true }), h2 && Vn("ArrowUp", (v) => {
    s || en(u, c, v) || Ls(p2, i);
  }, { target: t }), h2 && Vn("ArrowDown", (v) => {
    s || en(u, c, v) || Ds(p2, i);
  }, { target: t }), { currentIndex: p2, last: _ };
}
const bf = {
  name: "vu-multiple-select",
  inject: {
    vuMultipleSelectLabels: {
      default: () => ({
        noResults: "No results."
      })
    },
    vuDebug: {
      default: false
    },
    vuInputComposition: {
      default: false
    }
  },
  mixins: [Fe, $e, vt, ze, Yn, Ee],
  props: {
    modelValue: {
      type: Array,
      default: () => []
    },
    search: {
      type: String,
      default: () => null
    },
    itemHeight: {
      type: Number,
      default: () => 38
    },
    minSearchLength: {
      type: Number,
      default: () => 0
    },
    shortPlaceholder: {
      type: String,
      required: false,
      default: () => ""
    },
    user: {
      type: [Boolean, String],
      required: false
    },
    searchField: {
      type: [Boolean, String],
      required: false
    },
    searchIcon: {
      type: String,
      required: false,
      default: () => "search"
    },
    customBadgeGroups: {
      type: [void 0, Object],
      default: () => {
      }
    },
    userBigBadges: {
      type: [Boolean, String],
      required: false
    },
    maxVisible: {
      type: Number,
      default: () => 5
    },
    maxSelectable: {
      type: Number,
      default: () => Number.POSITIVE_INFINITY
    },
    caseSensitive: {
      type: [Boolean, String],
      default: false
    },
    preserveSearchOnBlur: {
      type: [Boolean, String],
      default: false
    },
    preserveSearchOnItemClick: {
      type: [Boolean, String],
      default: false
    },
    preserveSearchOnItemKeyboard: {
      type: [Boolean, void 0],
      default: void 0
    },
    noLocalFiltering: {
      type: Boolean,
      default: false
    },
    disableUnselectionWithinOptions: {
      type: Boolean,
      default: false
    },
    keepFocusOnInputOnItemClick: {
      type: Boolean,
      default: false
    },
    showClearIconWhenText: {
      type: [Boolean, String],
      default: false
    },
    keepFocusOnInputOnItemKeyboard: {
      type: [Boolean, void 0],
      default: void 0
    },
    rounded: {
      type: [Boolean, String],
      default: false
    },
    singleLine: {
      type: [Boolean, String],
      default: false
    },
    minInputWidth: {
      type: [Boolean, Number],
      default: 200
    },
    roundedBadges: {
      default: false
    },
    alternateBadges: {
      default: false
    },
    badgesColor: {
      required: false,
      default: () => {
      }
    },
    hideNoResults: {
      required: false,
      type: [Boolean, String]
    },
    ignoreEscapeKey: {
      type: [Boolean, String],
      default: false
    },
    allowKeyboardBadgesNavWhenText: {
      type: [Boolean, String],
      default: false
    },
    openDropdownOnFocus: {
      type: [Boolean, String],
      default: true
    }
  },
  expose: ["focus", "toggle", "close"],
  emits: ["search", "update:modelValue", "notify:already-selected", "update:search"],
  data: () => ({
    open: false,
    pick: ls,
    inputInFocus: false,
    positioned: true,
    intxObs: null,
    localSearch: "",
    keyIndexItems: -1,
    lastItemChange: -1,
    keyIndexBadges: -1,
    lastBadgeChange: -1,
    bottom: 40,
    top: false,
    resizeObs: null,
    uid: gt(),
    scrollbarVisible: false,
    badgesRefs: {},
    inputWidth: null,
    minimizeInput: false,
    stopInputMinWidth: () => {
    }
  }),
  created() {
    this.resizeObs = new ResizeObserver((e) => {
      this.bottom = e[0].contentRect.height + 4;
    });
  },
  mounted() {
    this.$refs.searchfield && this.resizeObs.observe(this.$refs.searchfield);
    const { width: e, left: n } = ln(this.$refs.searchbox), { left: t } = ln(this.$refs.input), i = computed(() => this.hasClearIcon);
    this.minInputWidth !== false && (this.stopInputMinWidth = xn([i, t], ([a]) => {
      const u = t.value - n.value;
      let c = e.value - u - ((a || this.searchField) && 38 || 0) - 1;
      c < this.minInputWidth && (c = e - 4), this.inputWidth = c, this.minimizeInput = false;
    }, { throttle: 10 })), this.target && (this.intxObs = new IntersectionObserver(() => {
      this.intxObs.unobserve(this.$refs.dropdown);
      const a = this.target.getBoundingClientRect(), u = this.$refs.dropdown.getBoundingClientRect();
      a.bottom < u.bottom && (this.top = true), this.positioned = true;
    }, {
      root: this.target,
      threshold: 1
    }));
    const o = As({
      disabled: this.disabled,
      items: computed(() => this.innerOptions),
      target: this.$refs.input,
      debug: this.vuDebug
    });
    this.lastItemChange = o == null ? void 0 : o.last, this.keyIndexItems = o == null ? void 0 : o.currentIndex;
    const s = As({
      disabled: this.disabled,
      items: computed(() => {
        var a;
        return this.searchField ? ((a = this.customBadgeGroups) == null ? void 0 : a.length) && this.customBadgeGroups || this.badgeGroups : this.modelValue;
      }),
      target: this.$refs.input,
      debug: this.vuDebug
    }, {
      direction: "horizontal",
      discardWhenValue: !this.allowKeyboardBadgesNavWhenText,
      discardWhenCaretInBetween: true,
      preserveIndexOnRemoval: true
    });
    this.keyIndexBadges = s == null ? void 0 : s.currentIndex, this.lastBadgeChange = s == null ? void 0 : s.last;
  },
  watch: {
    getSearch(e) {
      this.executeSearch(e);
    },
    keyIndexItems(e) {
      var s, a, u, c;
      const n = ((a = (s = this.$refs) == null ? void 0 : s.selectOptions) == null ? void 0 : a.liRefs) || [], t = ((u = this.innerOptions[e]) == null ? void 0 : u.value) || -1, i = n[t], o = (c = this.$refs.scroller) == null ? void 0 : c.$el;
      if (o)
        if (i) {
          const d = i.offsetTop + i.clientHeight;
          (d > o.scrollTop + o.clientHeight || d < o.scrollTop) && (o == null || o.scrollTo({ top: i.offsetTop }));
        } else
          t === -1 && o.scrollTo({ top: 0 });
    },
    open: {
      handler(e) {
        var n;
        e && !this.ignoreEscapeKey ? this.keyboardListener = pe((n = this.$refs) == null ? void 0 : n.input, "keyup", (t) => {
          t.code === "Escape" && (this.close(), t.preventDefault(), t.stopPropagation(), this.keyIndexItems = -1);
        }) : this.keyboardListener();
      }
    },
    isActive(e) {
      !e && this.singleLine && this.scrollbarVisible && this.$refs && this.$refs.searchfield && this.$refs.autocomplete.scrollTo({ left: 0, behavior: "instant" });
    },
    keyIndexBadges(e) {
      var n;
      if (this.singleLine && this.scrollbarVisible && e >= -1)
        if (e === -1) {
          const { offsetLeft: t } = (n = this.$refs) == null ? void 0 : n.input;
          this.$refs.autocomplete.scrollTo({ left: t, behavior: "instant" });
        } else {
          const t = this.keyboardNavigationItems[e], i = this.badgesRefs[typeof t == "string" ? t : t.value], { scrollLeft: o } = i;
          this.$refs.autocomplete.scrollTo({ left: o, behavior: "smooth" });
        }
    },
    modelLength: {
      // eslint-disable-next-line object-shorthand
      handler: function() {
        this.minInputWidth !== false && (this.minimizeInput = true, this.inputWidth = false);
      },
      immediate: true
    },
    minInputWidth(e) {
      e === false && (this.stopInputMinWidth(), this.minimizeInput = false, this.inputWidth = false);
    }
  },
  computed: {
    hasClearIcon() {
      var e;
      return this.maxSelectable === 1 && !this.user && ((e = this.value) == null ? void 0 : e.length) || this.showClearIconWhenText && this.search !== "";
    },
    getSearch() {
      return this.search !== null ? this.search : this.localSearch;
    },
    searchLengthMet() {
      return this.getSearch.length >= this.minSearchLength;
    },
    innerOptions() {
      return this.searchLengthMet ? this.noLocalFiltering ? this.options : this.caseSensitive ? this.options.filter((e) => e.label.includes(this.getSearch) || e.value.includes(this.getSearch)) : this.options.filter((e) => e.label.toLowerCase().includes(this.getSearch.toLowerCase()) || e.value.toLowerCase().includes(this.getSearch.toLowerCase())) : [];
    },
    keyboardNavigationItems() {
      var e;
      return this.searchField ? ((e = this.customBadgeGroups) == null ? void 0 : e.length) && this.customBadgeGroups || this.badgeGroups : this.modelValue;
    },
    innerOptionsLength() {
      return this.innerOptions.length;
    },
    noResults() {
      return this.options && this.innerOptions.length === 0 && this.searchLengthMet && !this.$slots.optionsHeader;
    },
    values() {
      return (this.value || []).map((e) => e.value);
    },
    dropdownHeight() {
      return this.$slots.optionsHeader ? "auto" : this.noResults ? this.$slots.noResults || this.hideNoResults ? "auto" : this.itemHeight : this.innerOptionsLength > this.maxVisible ? this.itemHeight * ((this.innerOptionsLength === this.maxVisible ? 0 : 0.5) + this.maxVisible) : this.itemHeight * this.innerOptionsLength;
    },
    hideOverflow() {
      return this.itemHeight * this.innerOptionsLength === this.dropdownHeight;
    },
    isActive() {
      return this.inputInFocus || this.open;
    },
    keepFocusKeyboard() {
      return this.keepFocusOnInputOnItemKeyboard !== void 0 ? this.keepFocusOnInputOnItemKeyboard : this.keepFocusOnInputOnItemClick;
    },
    preserveSearchKeyboard() {
      return this.preserveSearchOnItemKeyboard !== void 0 ? this.preserveSearchOnItemKeyboard : this.preserveSearchOnItemClick;
    },
    badgeGroups() {
    },
    modelLength() {
      var e;
      return (e = this.modelValue) == null ? void 0 : e.length;
    }
  },
  methods: {
    checkScrollbar() {
      var t;
      const { scrollHeight: e = 0, clientHeight: n = 0 } = (t = this.$refs) == null ? void 0 : t.searchfield;
      this.scrollbarVisible = e > n;
    },
    executeSearch(e) {
      this.$emit("search", e), e && !this.open && this.openAndIntersect();
    },
    toggle(e, { fromOptionsClick: n = false, fromOptionsKeyboard: t = false } = { fromOptionsClick: false, fromOptionsKeyboard: false }) {
      if (this.disabled || e.disabled)
        return;
      const i = this.value || [];
      let o = i.findIndex((s) => s.value === e.value);
      if (this.values.includes(e.value))
        if (this.maxSelectable === 1)
          this.$emit("update:modelValue", []);
        else if ((n || t) && this.disableUnselectionWithinOptions)
          this.$emit("notify:already-selected", e);
        else {
          const s = i.slice();
          if (this.badgeGroups) {
            const { valueValue: a } = e;
            s.splice(i.findIndex(({ value: u }) => u === a), 1), o = i.findIndex((u) => u.value === e.value);
          }
          s.splice(o, 1), this.$emit("update:modelValue", s);
        }
      else
        this.maxSelectable === 1 ? (this.$emit("update:modelValue", [e]), this.$emit("update:search", ""), this.localSearch = "", this.close()) : this.$emit("update:modelValue", i.concat([e]));
      (n || t) && ((n && this.keepFocusOnInputOnItemClick || t && this.keepFocusKeyboard) && this.$refs.input.focus(), (n && !this.preserveSearchOnItemClick || t && !this.preserveSearchKeyboard) && (this.$emit("update:search", ""), this.localSearch = ""));
    },
    getOption(e) {
      return this.options.find((n) => n.value === e) || {};
    },
    close() {
      this.open = false, this.top = false, this.positioned = true, this.inputInFocus = false, this.preserveSearchOnBlur || this.$emit("update:search", "");
    },
    async openAndIntersect() {
      if (this.searchLengthMet && !this.open && !this.disabled)
        if (this.target && ["scroll", "auto", "visible"].includes(window.getComputedStyle(this.target).overflowY)) {
          const e = this.target.getBoundingClientRect(), n = this.$refs.searchfield.getBoundingClientRect();
          !this.top && (this.maxVisible + 0.5) * this.itemHeight > e.bottom - n.bottom && (this.top = true), this.open = true;
        } else
          this.open = true, this.positioned = false, await new Promise((e) => setTimeout(e, 10)), await this.$nextTick(), this.intxObs.observe(this.$refs.dropdown);
    },
    beforeUnmount() {
      this.intxObs.disconnect(), delete this.intxObs;
    },
    onDelete(e) {
      var n;
      if (this.open && this.lastItemChange > this.lastBadgeChange) {
        if (this.keyIndexItems > -1) {
          const t = this.innerOptions[this.keyIndexItems];
          !(t != null && t.disabled) && this.values.includes(t.value) && (this.toggle(t, { fromOptionsKeyboard: true }), e.preventDefault());
        }
      } else
        this.keyIndexBadges > -1 && !((n = this.value[this.keyIndexBadges]) != null && n.disabled) && this.toggle(this.keyboardNavigationItems[this.keyIndexBadges]);
    },
    onEnter() {
      var e;
      this.open && this.lastItemChange > this.lastBadgeChange && this.keyIndexItems > -1 ? !((e = this.value[this.keyIndexBadges]) != null && e.disabled) && this.toggle(this.innerOptions[this.keyIndexItems], { fromOptionsKeyboard: true }) : this.open || (this.open = true);
    },
    onInput(e) {
      const { target: n } = e;
      this.keyIndexBadges > -1 && (this.keyIndexBadges = -1), this.vuInputComposition || (n.composing = false), this.$emit("update:search", n.value), this.localSearch = n.value;
    },
    focus() {
      var e, n;
      (n = (e = this.$refs) == null ? void 0 : e.input) == null || n.focus(), this.openDropdownOnFocus && this.openAndIntersect();
    }
  },
  components: { VuUserPicture: $t, VuBadge: jn, VuIconBtn: de, VuScroller: Qe, VuSelectOptions: us }
}, yf = {
  key: 0,
  class: "control-label"
}, _f = {
  key: 0,
  class: "label-field-required"
}, wf = {
  key: 1,
  style: { "line-height": "30px" }
}, kf = ["value", "disabled", "placeholder"], Sf = { style: { "padding-top": "15px" } }, If = { class: "message" }, Cf = {
  key: 0,
  class: "multiple-select__no-results"
}, Bf = {
  key: 1,
  class: "form-control-helper-text"
};
function $f(e, n, t, i, o, s) {
  const a = resolveComponent("VuUserPicture"), u = resolveComponent("VuIconBtn"), c = resolveComponent("VuBadge"), d = resolveComponent("VuSelectOptions"), h2 = resolveComponent("vu-spinner"), p2 = resolveComponent("VuScroller"), _ = resolveDirective("click-outside");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-multiple-select form-group", [
      e.classes,
      {
        "vu-multiple-select--rounded": t.rounded,
        "vu-multiple-select--single-line": t.singleLine,
        "vu-multiple-select--searchIcon": t.searchField
      }
    ]])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", yf, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", _f, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    withDirectives((openBlock(), createElementBlock("div", {
      ref: "searchfield",
      class: normalizeClass(["select select-autocomplete", [{
        "dropdown-visible": e.open,
        "select-disabled": e.disabled,
        "single-select": t.maxSelectable === 1,
        "has-clear-icon": s.hasClearIcon,
        "has-search-icon": !s.hasClearIcon && t.searchField,
        "select--single-line": t.singleLine && s.isActive
      }]])
    }, [
      createBaseVNode("div", {
        ref: "searchbox",
        class: normalizeClass(["autocomplete-searchbox", {
          "autocomplete-searchbox-active": s.isActive,
          disabled: e.disabled,
          "autocomplete-searchbox--user": t.user,
          "autocomplete-searchbox--user-big-badges": t.user && t.userBigBadges
        }]),
        onClick: n[9] || (n[9] = (v) => {
          t.maxSelectable === 1 && s.values.length || e.$refs.input.focus(), s.openAndIntersect(), s.checkScrollbar();
        })
      }, [
        t.user ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(e.value, (v, y) => (openBlock(), createElementBlock("div", {
          key: `${e.uid}-tag-${v}`,
          class: normalizeClass(["vu-user-badge", {
            "vu-user-badge--hovered": y === e.keyIndexBadges
          }])
        }, [
          createVNode(a, {
            id: v.value,
            src: v.src,
            size: "tiny"
          }, null, 8, ["id", "src"]),
          createBaseVNode("span", null, toDisplayString(v.label), 1),
          createVNode(u, {
            class: "vu-user-badge__close",
            icon: "close",
            size: "icon-smaller",
            onClick: (L) => s.toggle(v)
          }, null, 8, ["onClick"])
        ], 2))), 128)) : (openBlock(true), createElementBlock(Fragment, { key: 1 }, renderList(s.badgeGroups || t.customBadgeGroups || e.value, (v, y) => (openBlock(), createElementBlock("span", {
          key: `${e.uid}-tag-${typeof v == "string" ? v : v.value}`,
          ref_for: true,
          ref: (L) => e.badgesRefs[typeof v == "string" ? v : v.value] = L,
          onClick: n[1] || (n[1] = (...L) => s.toggle && s.toggle(...L))
        }, [
          renderSlot(e.$slots, "badge", { value: v }, () => [
            t.maxSelectable !== 1 ? (openBlock(), createBlock(c, mergeProps({ key: 0 }, e.pick(v, ["disabled", "icon", "selectable", "selected", "togglable"]), {
              badge2: v.valueLabel,
              value: y === e.keyIndexBadges,
              closable: "",
              icon: v.icon,
              rounded: v.rounded || t.roundedBadges,
              alternate: v.alternate || t.alternateBadges,
              color: v.badgeClass || t.badgesColor,
              text: t.searchField ? "" : v.label || v.text,
              onClick: n[0] || (n[0] = withModifiers(() => {
              }, ["stop"])),
              onClose: (L) => s.toggle(v)
            }), null, 16, ["badge2", "value", "icon", "rounded", "alternate", "color", "text", "onClose"])) : (openBlock(), createElementBlock("span", wf, toDisplayString(v.label), 1))
          ], true)
        ]))), 128)),
        s.values.length < t.maxSelectable ? (openBlock(), createElementBlock("input", {
          key: 2,
          value: s.getSearch,
          ref: "input",
          type: "text",
          class: normalizeClass(["autocomplete-input", [e.minimizeInput && "minimized", t.minInputWidth === false && "var"]]),
          disabled: e.disabled,
          style: normalizeStyle([e.inputWidth && { width: `${e.inputWidth}px` }]),
          placeholder: s.values.length && t.shortPlaceholder ? t.shortPlaceholder : e.placeholder,
          onInput: n[2] || (n[2] = (...v) => s.onInput && s.onInput(...v)),
          onBlur: n[3] || (n[3] = () => {
            t.searchField || e.open || (e.inputInFocus = false);
          }),
          onFocus: n[4] || (n[4] = (v) => e.inputInFocus = true),
          onKeydown: n[5] || (n[5] = withKeys((...v) => s.onDelete && s.onDelete(...v), ["delete", "backspace"])),
          onKeyup: n[6] || (n[6] = withKeys(withModifiers((...v) => s.onEnter && s.onEnter(...v), ["stop"]), ["enter"])),
          onClick: n[7] || (n[7] = (v) => {
            s.openAndIntersect();
          })
        }, null, 46, kf)) : createCommentVNode("", true),
        !s.hasClearIcon && t.searchField ? (openBlock(), createBlock(u, {
          key: 3,
          icon: t.searchIcon,
          class: "absolute! top-0 right-0 multiple-select__search-icon",
          onClick: n[8] || (n[8] = withModifiers((v) => s.executeSearch(), ["stop"]))
        }, null, 8, ["icon"])) : createCommentVNode("", true)
      ], 2),
      s.hasClearIcon ? (openBlock(), createBlock(u, {
        key: 0,
        icon: "clear",
        class: "select__clear-icon",
        onClick: n[10] || (n[10] = withModifiers((v) => {
          t.searchField ? e.$emit("update:modelValue", []) : s.toggle(e.value[0]), e.$emit("update:search", "");
        }, ["stop"]))
      })) : createCommentVNode("", true),
      e.open && s.searchLengthMet ? (openBlock(), createElementBlock("div", {
        key: 1,
        ref: "dropdown",
        class: normalizeClass(["select-dropdown", [{ "select-dropdown--no-results": !t.hideNoResults && s.noResults, "select-dropdown--dropup": e.top, "rounded-lg": t.rounded }, e.contentClass]]),
        style: normalizeStyle([
          `height: ${s.dropdownHeight}${s.dropdownHeight !== "auto" ? "px" : ""}`,
          e.top ? `bottom: ${e.bottom}px` : "",
          e.positioned ? "" : "opacity: 0",
          e.contentStyle
        ])
      }, [
        createVNode(p2, {
          ref: "scroller",
          class: normalizeClass({ "hide-scroller": s.hideOverflow }),
          "always-show": ""
        }, {
          default: withCtx(() => {
            var v;
            return [
              renderSlot(e.$slots, "optionsHeader", {}, void 0, true),
              withDirectives(createVNode(d, {
                ref: "selectOptions",
                multiple: "",
                user: t.user || t.searchField && ((v = s.innerOptions) == null ? void 0 : v.some(({ login: y, userImgUrl: L }) => y || L)),
                selected: e.value,
                options: s.innerOptions,
                "key-index": e.keyIndexItems,
                onClickItem: n[11] || (n[11] = (y) => s.toggle(y, { fromOptionsClick: true }))
              }, {
                default: withCtx(({ item: y }) => [
                  renderSlot(e.$slots, "default", { item: y }, void 0, true)
                ]),
                _: 3
              }, 8, ["user", "selected", "options", "key-index"]), [
                [vShow, s.searchLengthMet && !e.loading]
              ]),
              e.loading ? renderSlot(e.$slots, "loading", { key: 0 }, () => [
                createBaseVNode("ul", Sf, [
                  createBaseVNode("li", If, [
                    createVNode(h2, { show: "" })
                  ])
                ])
              ], true) : createCommentVNode("", true),
              !e.loading && s.noResults ? renderSlot(e.$slots, "noResults", { key: 1 }, () => [
                t.hideNoResults ? createCommentVNode("", true) : (openBlock(), createElementBlock("ul", Cf, [
                  createBaseVNode("li", null, toDisplayString(s.vuMultipleSelectLabels.noResults), 1)
                ]))
              ], true) : createCommentVNode("", true)
            ];
          }),
          _: 3
        }, 8, ["class"])
      ], 6)) : createCommentVNode("", true)
    ], 2)), [
      [_, function() {
        s.close();
      }]
    ]),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (v, y) => (openBlock(), createElementBlock("span", {
      key: `${y}-error-${v}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(v), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", Bf, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const Of = /* @__PURE__ */ F(bf, [["render", $f], ["__scopeId", "data-v-52ddcb37"]]), Tf = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Of
}, Symbol.toStringTag, { value: "Module" })), xf = {
  name: "vu-range",
  mixins: [Fe, hn, $e, ze, Ee],
  props: {
    step: {
      type: Number,
      default: 1
    },
    showLabels: {
      type: Boolean,
      default: true
    },
    customLabels: {
      type: Array,
      required: false,
      default: void 0
    }
  },
  emits: ["update:modelValue", "mouseup"],
  data() {
    return {
      lowervalue: 0,
      uppervalue: 1
    };
  },
  watch: {
    value: {
      immediate: true,
      handler() {
        this.lowervalue = Math.min(...this.value), this.uppervalue = Math.max(...this.value);
      }
    }
  },
  computed: {
    value() {
      return this.modelValue || [];
    },
    minLabel() {
      return this.customLabels && this.customLabels.length ? this.customLabels[0] : this.min;
    },
    maxLabel() {
      return this.customLabels && this.customLabels.length ? this.customLabels[(this.max + this.max % this.step) / this.step - this.min] : this.max;
    },
    lowerLabel() {
      return this.customLabels && this.customLabels.length ? this.customLabels[(this.lowervalue - this.min) / this.step] : this.lowervalue;
    },
    upperLabel() {
      return this.customLabels && this.customLabels.length ? this.customLabels[(this.uppervalue - this.min) / this.step] : this.uppervalue;
    },
    computedStyles() {
      const e = (this.lowervalue - this.min) / (this.max - this.min) * 100;
      return {
        width: `${(this.uppervalue - this.min - (this.lowervalue - this.min)) / (this.max - this.min || 1) * 100}%`,
        left: `${e}%`
      };
    }
  },
  methods: {
    commit() {
      this.disabled || this.$emit("mouseup", [this.lowervalue, this.uppervalue]);
    },
    update(e, n) {
      if (this.disabled)
        return;
      let t, i;
      e === "lower" ? (i = Math.min(n, this.uppervalue), t = Math.max(n, this.uppervalue), i > t && (t = Math.min(t + this.step, this.max))) : (i = Math.min(n, this.lowervalue), t = Math.max(n, this.lowervalue), i > t && (i = Math.max(i - this.step, this.min))), this.lowervalue = i, this.uppervalue = t, this.$emit("update:modelValue", [this.lowervalue, this.uppervalue]);
    }
  }
}, Mf = {
  key: 0,
  class: "control-label"
}, Vf = {
  key: 0,
  class: "label-field-required"
}, Pf = ["disabled", "value", "min", "max", "step"], Lf = ["disabled", "value", "min", "max", "step"], Df = { class: "vu-range__grey-bar" }, Af = {
  key: 0,
  class: "vu-range__labels-container"
}, Ff = { class: "vu-range__left vu-range__left-label" }, zf = { class: "vu-range__right vu-range__right-label" }, Ef = {
  key: 1,
  class: "form-control-helper-text"
};
function Nf(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", e.classes])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", Mf, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", Vf, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    createBaseVNode("div", {
      class: normalizeClass(["vu-range", { disabled: e.disabled }])
    }, [
      createBaseVNode("div", {
        onMouseup: n[2] || (n[2] = (...a) => s.commit && s.commit(...a)),
        class: "vu-range__inputs-container"
      }, [
        createBaseVNode("input", {
          disabled: e.disabled,
          onInput: n[0] || (n[0] = (a) => s.update("lower", parseFloat(a.target.value))),
          value: o.lowervalue,
          min: e.min,
          max: e.max,
          step: t.step,
          class: "slider vu-range__left",
          type: "range"
        }, null, 40, Pf),
        createBaseVNode("input", {
          disabled: e.disabled,
          onInput: n[1] || (n[1] = (a) => s.update("upper", parseFloat(a.target.value))),
          value: o.uppervalue,
          min: e.min,
          max: e.max,
          step: t.step,
          class: "slider vu-range__right",
          type: "range"
        }, null, 40, Lf),
        createBaseVNode("div", Df, [
          createBaseVNode("div", {
            class: "vu-range__blue-bar",
            style: normalizeStyle(s.computedStyles)
          }, null, 4)
        ])
      ], 32),
      t.showLabels ? (openBlock(), createElementBlock("div", Af, [
        createBaseVNode("div", Ff, toDisplayString(s.minLabel), 1),
        createBaseVNode("div", zf, toDisplayString(s.maxLabel), 1),
        o.lowervalue !== e.min && o.uppervalue !== o.lowervalue ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: "vu-range__lower-label",
          style: normalizeStyle("left: " + (o.lowervalue - e.min) / (e.max - e.min) * 100 + "%")
        }, toDisplayString(s.lowerLabel), 5)) : createCommentVNode("", true),
        o.uppervalue !== e.max ? (openBlock(), createElementBlock("div", {
          key: 1,
          class: "vu-range__upper-label",
          style: normalizeStyle("left: " + (o.uppervalue - e.min) / (e.max - e.min) * 100 + "%")
        }, toDisplayString(s.upperLabel), 5)) : createCommentVNode("", true)
      ])) : createCommentVNode("", true)
    ], 2),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (a, u) => (openBlock(), createElementBlock("span", {
      key: `${u}-error-${a}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(a), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", Ef, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const Rf = /* @__PURE__ */ F(xf, [["render", Nf], ["__scopeId", "data-v-7de621bd"]]), Hf = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Rf
}, Symbol.toStringTag, { value: "Module" }));
function Fi() {
  return window ? !!(/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent) || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1) : false;
}
const jf = ["draggable"], Uf = {
  key: 0,
  class: "section-header_indicator"
}, Wf = {
  name: "vu-section-header"
}, qf = /* @__PURE__ */ defineComponent({
  ...Wf,
  props: /* @__PURE__ */ mergeModels({
    name: {},
    title: {},
    actions: {},
    dragged: { type: Boolean },
    dashed: { type: Boolean },
    draggable: { type: Boolean },
    sticked: { type: Boolean },
    disabled: { type: Boolean },
    noMargin: { type: Boolean },
    tabbed: { type: Boolean },
    selected: { type: Boolean },
    expandable: { type: Boolean },
    expanded: { type: Boolean },
    focused: { type: Boolean },
    reorderable: { type: Boolean },
    indicator: {}
  }, {
    expanded: { type: Boolean, default: true },
    expandedModifiers: {}
  }),
  emits: /* @__PURE__ */ mergeModels(["click", "click-title", "click-expander", "click-action", "dragstart", "dragleave", "dragover", "dragend"], ["update:expanded"]),
  setup(e, { emit: n }) {
    const t = e, i = n, o = useModel(e, "expanded"), s = ref(false), a = ref(false), u = ref(false), c = Fi(), d = inject(Yt, c), h2 = computed(() => t.expanded !== void 0 ? t.expanded : o.value);
    function p2() {
      o.value = !o.value;
    }
    function _(v, y = false) {
      const { target: L = {}, relatedTarget: B = {} } = v, { previousSibling: R, nextSibling: ee } = L;
      y && B === R || B === ee || (a.value = false);
    }
    return (v, y) => {
      const L = resolveDirective("tooltip");
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(["section-header", {
          "section-header--expandable": t.expandable,
          "section-header--active": t.selected,
          "section-header--focused": t.focused,
          "section-header--hover": s.value || a.value || u.value,
          "section-header--sticky": t.sticked,
          "section-header--reorder": t.reorderable,
          "section-header--disabled": t.disabled,
          "section-header--dashed": t.dashed,
          "is-target": t.noMargin,
          "section-header--dragged": t.dragged
        }]),
        draggable: t.draggable,
        onDragstart: y[8] || (y[8] = (B) => i("dragstart", B)),
        onDragleave: y[9] || (y[9] = (B) => i("dragleave", B)),
        onDragover: y[10] || (y[10] = (B) => i("dragover", B)),
        onDragend: y[11] || (y[11] = (B) => i("dragend", B)),
        onMouseenter: y[12] || (y[12] = (B) => s.value = true),
        onMouseleave: y[13] || (y[13] = (B) => s.value = false)
      }, [
        t.tabbed ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: "section-header_tab",
          onMouseenter: y[0] || (y[0] = (B) => a.value = true),
          onMouseleave: _
        }, null, 32)) : createCommentVNode("", true),
        t.expandable ? (openBlock(), createElementBlock("div", {
          key: 1,
          class: normalizeClass(["section-header_expander justify-center h-full", {
            "section-header_expander--open": h2.value,
            "section-header_expander--hover": a.value
          }]),
          onClick: y[1] || (y[1] = (B) => {
            t.expandable && p2(), i("click", B), i("click-expander", B);
          }),
          onMouseenter: y[2] || (y[2] = (B) => a.value = true),
          onMouseleave: _
        }, [
          createVNode(ue, {
            icon: "expand-right",
            class: "block vertical-middle h-full cursor-pointer",
            style: { "line-height": "38px", "font-size": "8px" },
            "within-text": false
          })
        ], 34)) : createCommentVNode("", true),
        withDirectives((openBlock(), createElementBlock("h5", {
          class: "section-header_title flex flex-grow",
          onClick: y[3] || (y[3] = (B) => {
            t.expandable && p2(), i("click", B), i("click-title", B);
          }),
          onMouseenter: y[4] || (y[4] = (B) => a.value = true),
          onMouseleave: y[5] || (y[5] = (B) => _(B, true))
        }, [
          createTextVNode(toDisplayString(t.title), 1)
        ], 32)), [
          [
            L,
            t.title,
            void 0,
            { ellipsis: true }
          ]
        ]),
        t.reorderable ? createCommentVNode("", true) : (openBlock(), createElementBlock(Fragment, { key: 2 }, [
          t.actions ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(t.actions, ({ icon: B, type: R, contentClass: ee, position: ce, items: Z, tooltip: D }, P) => (openBlock(), createElementBlock(Fragment, { key: P }, [
            R !== "dropdownmenu" ? withDirectives((openBlock(), createBlock(de, {
              key: 0,
              class: "flex-grow-0 flex-basis-[38px]",
              icon: B,
              color: "default-lean",
              onClick: (Q) => {
                var z;
                return i("click-action", (z = t.actions) == null ? void 0 : z[P]);
              }
            }, null, 8, ["icon", "onClick"])), [
              [vShow, !t.reorderable && unref(d)],
              [L, D]
            ]) : (openBlock(), createBlock(rt, {
              key: 1,
              items: Z,
              "content-class": ee,
              position: ce,
              class: "flex-grow-0 flex-basis-38px",
              onOpen: y[6] || (y[6] = (Q) => u.value = true),
              onClose: y[7] || (y[7] = (Q) => u.value = false),
              onClickItem: (Q) => {
                i(
                  "click-action",
                  // @ts-expect-error props. required
                  t.actions[P],
                  Q
                );
              }
            }, {
              default: withCtx(({ active: Q }) => [
                withDirectives(createVNode(de, mergeProps(
                  { icon: "chevron-down" },
                  t.selected && !t.expanded ? {
                    noActive: true,
                    noHover: true,
                    color: "secondary"
                  } : {
                    color: "default-lean"
                  },
                  { active: Q }
                ), null, 16, ["active"]), [
                  [vShow, !t.reorderable && (s.value || unref(d)) || Q]
                ])
              ]),
              _: 2
            }, 1032, ["items", "content-class", "position", "onClickItem"]))
          ], 64))), 128)) : createCommentVNode("", true)
        ], 64)),
        t.indicator ? renderSlot(v.$slots, "indicator", { key: 3 }, () => [
          t.indicator && !unref(d) && !t.reorderable ? (openBlock(), createElementBlock("div", Uf, [
            createBaseVNode("span", null, toDisplayString(t.indicator), 1)
          ])) : createCommentVNode("", true)
        ], true) : createCommentVNode("", true),
        t.reorderable ? renderSlot(v.$slots, "default", { key: 4 }, () => [
          t.reorderable ? (openBlock(), createBlock(de, {
            key: 0,
            class: "flex section-header_reorder-handle",
            icon: "drag-grip",
            color: "default-lean",
            hover: s.value || a.value,
            noHover: t.focused
          }, null, 8, ["hover", "noHover"])) : createCommentVNode("", true)
        ], true) : createCommentVNode("", true)
      ], 42, jf);
    };
  }
}), zi = /* @__PURE__ */ F(qf, [["__scopeId", "data-v-de35b382"]]), Kf = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: zi
}, Symbol.toStringTag, { value: "Module" })), Gf = {
  name: "vu-single-checkbox",
  mixins: [ze, Ee, $e],
  inheritAttrs: false,
  props: {
    // String for Radio, Boolean for Switch/Default
    modelValue: {
      type: [String, Boolean],
      default: () => ""
    },
    label: {
      type: String,
      default: ""
    },
    // Removes slot and label
    standalone: {
      type: Boolean,
      default: false
    },
    // Optional
    // eslint-disable-next-line vue/require-default-prop
    icon: {
      type: String,
      required: false
    },
    // Exclusive with Switch
    radio: {
      type: Boolean,
      required: false
    },
    // Required by radio.
    // eslint-disable-next-line vue/require-default-prop
    group: {
      type: String,
      required: false
    },
    // Required by radio
    // eslint-disable-next-line vue/require-default-prop
    value: {
      type: String,
      required: false
    },
    // Excludes radio
    switch: {
      type: Boolean,
      required: false
    },
    // eslint-disable-next-line vue/require-default-prop
    id: {
      type: [String, Number],
      required: false
    }
  },
  emits: ["update:modelValue"],
  data: () => ({ uid: gt() }),
  computed: {
    topClasses() {
      return {
        "vu-single-checkbox--switch": this.switch,
        "vu-single-checkbox--standalone": this.standalone,
        "vu-single-checkbox--checkbox": !this.switch && !this.radio,
        "vu-single-checkbox--radio": this.radio,
        "vu-single-checkbox--extra-content": this.hasExtraContent
      };
    },
    internalClasses() {
      return {
        "toggle-icon": this.icon,
        "toggle-switch": this.switch,
        "toggle-primary": !this.switch
      };
    },
    hasExtraContent() {
      return this.$slots.default && !this.standalone;
    }
  },
  methods: {
    input(e) {
      return this.radio ? this.$emit("update:modelValue", e.target.value) : this.$emit("update:modelValue", e.target.checked);
    }
  },
  components: { VuIcon: ue }
}, Yf = ["type", "checked", "name", "value", "id", "disabled"], Xf = ["for"], Jf = { class: "vu-single-checkbox__inner-span" }, Zf = {
  key: 0,
  class: "vu-single-checkbox__extra-content"
};
function Qf(e, n, t, i, o, s) {
  const a = resolveComponent("VuIcon");
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-single-checkbox", s.topClasses])
  }, [
    createBaseVNode("div", {
      class: normalizeClass(["toggle", s.internalClasses])
    }, [
      createBaseVNode("input", mergeProps({
        class: "vu-single-checkbox__input",
        type: t.radio ? "radio" : "checkbox",
        checked: t.radio ? t.group === t.modelValue : t.modelValue
      }, e.$attrs, {
        name: t.radio ? t.group : void 0,
        value: t.radio ? t.value : void 0,
        id: e.$attrs[t.id] || `${e.uid}`,
        disabled: e.disabled,
        onClick: n[0] || (n[0] = (...u) => s.input && s.input(...u))
      }), null, 16, Yf),
      t.standalone ? createCommentVNode("", true) : (openBlock(), createElementBlock(Fragment, { key: 0 }, [
        createBaseVNode("label", {
          class: "control-label vu-single-checkbox__label",
          for: e.$attrs[t.id] || `${e.uid}`
        }, [
          t.icon ? (openBlock(), createBlock(a, {
            key: 0,
            icon: t.icon
          }, null, 8, ["icon"])) : createCommentVNode("", true),
          createBaseVNode("span", Jf, toDisplayString(t.label), 1)
        ], 8, Xf),
        renderSlot(e.$slots, "label-prepend", {}, void 0, true)
      ], 64))
    ], 2),
    s.hasExtraContent ? (openBlock(), createElementBlock("div", Zf, [
      renderSlot(e.$slots, "default", {}, void 0, true)
    ])) : createCommentVNode("", true)
  ], 2);
}
const ep = /* @__PURE__ */ F(Gf, [["render", Qf], ["__scopeId", "data-v-573b078e"]]), tp = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: ep
}, Symbol.toStringTag, { value: "Module" })), np = {
  name: "vu-slider",
  mixins: [Fe, $e, ze, Ee],
  props: {
    labels: {
      required: false,
      type: Object,
      default: () => ({
        min: "Min",
        max: "Max"
      })
    },
    min: {
      type: Number,
      default: 0
    },
    max: {
      type: Number,
      default: 10
    },
    step: {
      type: Number,
      default: 1
    },
    stepped: {
      type: Boolean,
      default: false
    },
    showLabels: {
      type: Boolean,
      default: false
    },
    labelsBeneath: {
      type: Boolean,
      default: false
    }
  },
  emits: ["mouseUp", "input"],
  data: () => ({
    labelsWidth: 0,
    innerValue: 0
  }),
  created() {
    this.innerValue = this.value;
  },
  mounted() {
    const { leftLabel: { offsetWidth: e = 0 } = {}, rightLabel: { offsetWidth: n = 0 } = {} } = this.$refs;
    this.labelsWidth = Math.max(e, n);
  },
  computed: {
    steps() {
      return [];
    },
    labelsMargin() {
      return this.labelsBeneath ? "" : `${this.labelsWidth}px`;
    },
    computedStyle() {
      return {
        left: this.labelsMargin,
        right: this.labelsMargin,
        width: `calc(100% - ${2 * this.labelsWidth}px + 14px)`
      };
    },
    innerBlueBarStyle() {
      return {
        // right: `calc(${percent}%${ left ? (` + ${ left }`) : ''})`,
        width: `${(this.innerValue - this.min) / (this.max - this.min) * 100}%`
      };
    }
  },
  methods: {
    commit() {
      this.disabled || this.$emit("mouseUp", this.value);
    },
    update(e) {
      this.disabled || (this.innerValue = e, this.$emit("input", this.innerValue));
    }
  }
}, sp = {
  key: 0,
  class: "control-label"
}, ip = {
  key: 0,
  class: "label-field-required"
}, op = ["disabled", "value", "min", "max", "step"], lp = {
  key: 0,
  class: "vu-slider__steps"
}, ap = {
  key: 1,
  class: "form-control-helper-text"
};
function rp(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", e.classes])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", sp, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", ip, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    createBaseVNode("div", {
      class: normalizeClass(["vu-slider", { disabled: e.disabled }])
    }, [
      createBaseVNode("div", {
        onMouseup: n[1] || (n[1] = (...a) => s.commit && s.commit(...a)),
        class: "vu-slider__container"
      }, [
        createBaseVNode("div", {
          ref: "leftLabel",
          class: "vu-slider__left vu-slider__label"
        }, toDisplayString(t.showLabels ? t.labels.min : t.min), 513),
        createBaseVNode("div", {
          ref: "rightLabel",
          class: "vu-slider__right vu-slider__label"
        }, toDisplayString(t.showLabels ? t.labels.max : t.max), 513),
        createBaseVNode("input", {
          class: "slider vu-slider__left",
          type: "range",
          disabled: e.disabled,
          value: e.innerValue,
          min: t.min,
          max: t.max,
          step: t.step,
          style: normalizeStyle(t.labelsBeneath ? {} : s.computedStyle),
          onInput: n[0] || (n[0] = (a) => s.update(parseFloat(a.target.value)))
        }, null, 44, op),
        createBaseVNode("div", {
          class: "vu-slider__grey-bar",
          style: normalizeStyle({ left: s.labelsMargin, right: s.labelsMargin })
        }, [
          createBaseVNode("div", {
            class: "vu-slider__blue-bar vu-slider__blue-bar--left",
            style: normalizeStyle(s.innerBlueBarStyle)
          }, null, 4)
        ], 4)
      ], 32),
      t.stepped ? (openBlock(), createElementBlock("div", lp, [
        (openBlock(true), createElementBlock(Fragment, null, renderList(s.steps, (a, u) => (openBlock(), createElementBlock("div", {
          key: u,
          class: "vu-slider__step",
          style: normalizeStyle(a.style)
        }, null, 4))), 128))
      ])) : createCommentVNode("", true)
    ], 2),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (a, u) => (openBlock(), createElementBlock("span", {
      key: `${u}-error-${a}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(a), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", ap, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const up = /* @__PURE__ */ F(np, [["render", rp], ["__scopeId", "data-v-9b4ebc74"]]), dp = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: up
}, Symbol.toStringTag, { value: "Module" })), cp = {
  name: "vu-textarea",
  mixins: [Fe, $e, ze, Ee],
  expose: "focus",
  props: {
    rows: {
      type: [Number, String],
      default: () => 2
    },
    name: {
      type: [String],
      required: false
    },
    minlength: {
      type: Number,
      required: false
    },
    maxlength: {
      type: Number,
      required: false
    },
    readonly: {
      type: [Boolean, String, void 0],
      required: false,
      default: void 0
    },
    spellcheck: {
      type: [Boolean, String, void 0],
      required: false,
      default: void 0
    },
    wrap: {
      type: String,
      required: false
    },
    autocomplete: {
      type: [Boolean, String, void 0],
      required: false,
      default: void 0
    },
    autocorrect: {
      type: [Boolean, String, void 0],
      required: false,
      default: void 0
    },
    autofocus: {
      type: [Boolean, String, void 0],
      required: false,
      default: void 0
    }
  },
  emits: ["update:modelValue"],
  inject: {
    isIos: {
      from: es
    }
  },
  methods: {
    focus() {
      var e, n;
      (n = (e = this.$refs) == null ? void 0 : e.textarea) == null || n.focus();
    }
  }
}, hp = {
  key: 0,
  class: "control-label"
}, fp = {
  key: 0,
  class: "label-field-required"
}, pp = ["value", "placeholder", "disabled", "name", "minlength", "maxlength", "readonly", "spellcheck", "rows", "wrap", "autocomplete", "autocorrect", "autofocus", "required"], mp = {
  key: 1,
  class: "form-control-helper-text"
};
function vp(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["form-group", [e.classes, { ios: s.isIos }]])
  }, [
    e.label.length ? (openBlock(), createElementBlock("label", hp, [
      createTextVNode(toDisplayString(e.label), 1),
      e.required ? (openBlock(), createElementBlock("span", fp, " *")) : createCommentVNode("", true)
    ])) : createCommentVNode("", true),
    createBaseVNode("textarea", {
      ref: "textarea",
      value: e.value,
      placeholder: e.placeholder,
      disabled: e.disabled,
      name: t.name,
      minlength: t.minlength,
      maxlength: t.maxlength,
      readonly: t.readonly,
      spellcheck: t.spellcheck,
      rows: t.rows,
      wrap: t.wrap,
      autocomplete: t.autocomplete,
      autocorrect: t.autocorrect,
      autofocus: t.autofocus,
      required: e.required,
      class: "form-control",
      onInput: n[0] || (n[0] = (a) => e.$emit("update:modelValue", a.target.value))
    }, null, 40, pp),
    (openBlock(true), createElementBlock(Fragment, null, renderList(e.errorBucket, (a, u) => (openBlock(), createElementBlock("p", {
      key: `${u}-error-${a}`,
      style: { display: "block" },
      class: "form-control-error-text"
    }, toDisplayString(a), 1))), 128)),
    e.helper.length ? (openBlock(), createElementBlock("span", mp, toDisplayString(e.helper), 1)) : createCommentVNode("", true)
  ], 2);
}
const gp = /* @__PURE__ */ F(cp, [["render", vp], ["__scopeId", "data-v-5258aea1"]]), bp = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: gp
}, Symbol.toStringTag, { value: "Module" })), yp = { class: "list-item__thumbnail relative" }, _p = {
  key: 0,
  class: "absolute w-full h-full top-0"
}, wp = { class: "list-item__body" }, kp = { key: 0 }, Sp = ["innerHTML"], Ip = {
  key: 0,
  class: "body__description"
}, Cp = {
  key: 0,
  class: "list-item__additional_element"
}, Bp = {
  key: 1,
  class: "list-item__action-menu"
}, $p = {
  name: "vu-thumbnail-list-item"
}, Op = /* @__PURE__ */ defineComponent({
  ...$p,
  props: {
    icon: { default: "" },
    iconColor: { default: "default" },
    iconSelectedColor: { default: "secondary" },
    scrollIntoView: { type: Boolean, default: true },
    scrollableMenu: { type: Boolean },
    attachMenu: { type: [String, Boolean] },
    forceScrollIntoView: { type: Boolean },
    title: { default: "" },
    rawTitle: {},
    imgUrl: {},
    unread: { type: Boolean, default: false },
    selected: { type: Boolean, default: false },
    description: {},
    actions: { default: () => [] },
    iconFill: { type: Boolean, default: false },
    value: { default: void 0 },
    lazyImage: { type: Boolean, default: true },
    actionsContentClass: {}
  },
  emits: ["click", "click-action", "actions-close", "actions-open"],
  setup(e, { expose: n, emit: t }) {
    const i = e, o = t;
    n({
      openMenu: _
    });
    const s = inject(hi, "active"), a = ref(null), u = ref(null), c = ref(null), d = ref(false);
    function h2() {
      var v;
      a.value && ((v = a.value) != null && v.scrollIntoViewIfNeeded ? a.value.scrollIntoViewIfNeeded({ behavior: "smooth" }) : a.value.scrollIntoView({ block: "nearest" }));
    }
    Mn(() => i.selected, () => {
      i.scrollIntoView && h2();
    }), Mn(() => i.forceScrollIntoView, h2);
    function p2({ target: v }) {
      var y, L, B, R;
      !((L = (y = u.value) == null ? void 0 : y.$el) != null && L.contains(v)) && !((R = (B = c.value) == null ? void 0 : B.$el) != null && R.contains(v)) && o("click", i.value);
    }
    function _() {
      if (!u.value)
        throw Error("Item has no menu");
      d.value = true;
    }
    return onMounted(() => {
      (i.selected && i.scrollIntoView || i.forceScrollIntoView) && h2();
    }), (v, y) => {
      var B;
      const L = resolveDirective("tooltip");
      return openBlock(), createElementBlock("div", {
        ref_key: "container",
        ref: a,
        class: normalizeClass(["vu-thumbnail-list-item", [{
          "menu-is-open": d.value,
          selected: v.selected,
          "with-unread-content": v.unread
        }]]),
        onClick: p2
      }, [
        createBaseVNode("div", yp, [
          renderSlot(v.$slots, "thumbnail", {}, () => [
            v.icon ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: normalizeClass(["thumbnail__container", [{ "bg-grey-0": v.iconFill }]])
            }, [
              createVNode(ue, {
                class: "thumbnail__icon",
                color: v.selected ? i.iconSelectedColor : i.iconColor,
                icon: v.icon
              }, null, 8, ["color", "icon"])
            ], 2)) : v.imgUrl ? (openBlock(), createBlock(it, {
              key: 1,
              src: v.imgUrl || "",
              lazy: v.lazyImage
            }, null, 8, ["src", "lazy"])) : createCommentVNode("", true)
          ], true),
          v.$slots.thumbnail__extra ? (openBlock(), createElementBlock("div", _p, [
            renderSlot(v.$slots, "thumbnail__extra", {}, void 0, true)
          ])) : createCommentVNode("", true)
        ]),
        createBaseVNode("div", wp, [
          renderSlot(v.$slots, "title", {
            isMenuOpen: d.value,
            listItemRef: a.value
          }, () => [
            v.title ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: normalizeClass(["body__title", [{
                "font-bold": v.unread,
                "show-1-line": !!v.$slots.description || v.description
              }]])
            }, [
              v.rawTitle ? (openBlock(), createElementBlock("span", {
                key: 1,
                innerHTML: v.rawTitle
              }, null, 8, Sp)) : (openBlock(), createElementBlock("span", kp, toDisplayString(v.title), 1))
            ], 2)) : createCommentVNode("", true)
          ], true),
          renderSlot(v.$slots, "description", {}, () => [
            v.description ? (openBlock(), createElementBlock("div", Ip, toDisplayString(v.description), 1)) : createCommentVNode("", true)
          ], true)
        ]),
        (B = v.$slots) != null && B.additional__element ? (openBlock(), createElementBlock("div", Cp, [
          renderSlot(v.$slots, "additional__element", {}, void 0, true)
        ])) : createCommentVNode("", true),
        v.unread || v.actions.length ? (openBlock(), createElementBlock("div", Bp, [
          v.unread ? (openBlock(), createBlock(de, {
            key: 0,
            class: "action-menu__unread-icon",
            "no-active": true,
            "no-hover": "",
            icon: "record"
          })) : createCommentVNode("", true),
          v.actions.length > 1 ? (openBlock(), createBlock(rt, {
            key: 1,
            ref_key: "actionMenu",
            ref: u,
            show: d.value,
            "onUpdate:show": y[0] || (y[0] = (R) => d.value = R),
            items: v.actions,
            side: "bottom-right",
            scrollable: v.scrollableMenu,
            maxItemsBeforeScroll: Number.POSITIVE_INFINITY,
            attach: v.attachMenu,
            contentClass: v.actionsContentClass,
            onClickItem: y[1] || (y[1] = (R) => o("click-action", R)),
            onClose: y[2] || (y[2] = (R) => o("actions-close")),
            onOpen: y[3] || (y[3] = (R) => o("actions-open"))
          }, {
            default: withCtx(() => [
              createVNode(de, {
                clickable: "",
                ref_key: "actionMenuActivator",
                ref: c,
                color: v.selected && "white" || "default-lean",
                class: normalizeClass(["action-menu__action", d.value && unref(s)]),
                icon: "chevron-down",
                active: d.value,
                "within-text": false
              }, null, 8, ["color", "class", "active"])
            ]),
            _: 1
          }, 8, ["show", "items", "scrollable", "maxItemsBeforeScroll", "attach", "contentClass"])) : v.actions.length === 1 ? withDirectives((openBlock(), createBlock(de, {
            key: 2,
            ref_key: "actionMenu",
            ref: u,
            clickable: "",
            color: v.selected && "white" || void 0,
            class: "action-menu__action",
            icon: v.actions[0].fonticon,
            active: d.value,
            "within-text": false,
            onClick: y[4] || (y[4] = (R) => o("click-action", v.actions[0]))
          }, null, 8, ["color", "icon", "active"])), [
            [L, v.actions[0].text || v.actions[0].label]
          ]) : createCommentVNode("", true)
        ])) : createCommentVNode("", true),
        renderSlot(v.$slots, "hidden-content", {}, void 0, true)
      ], 2);
    };
  }
}), hs = /* @__PURE__ */ F(Op, [["__scopeId", "data-v-cb2f8cc2"]]), Tp = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: hs
}, Symbol.toStringTag, { value: "Module" })), xp = ["name"], Mp = { class: "section-header_title flex" }, Vp = ["onClick"], Pp = ["data-key"], Lp = ["data-key"], Dp = ["data-key"], Ap = {
  key: 0,
  class: "h-2px mt-2px border-drag-dash-5 border-blue-3 full-width"
}, Fp = ["data-key"], zp = { key: 0 }, Ep = ["name"], Np = { class: "section-header_title flex" }, Rp = ["onClick"], Hp = {
  name: "vu-thumbnail-grid"
}, jp = /* @__PURE__ */ defineComponent({
  ...Hp,
  props: {
    items: {},
    sections: {},
    pinSection: {},
    pinHeaders: { default: true },
    draggable: { default: true },
    labels: { default: () => ({
      dragHereToReorder: "Drag here to reorder"
    }) },
    dragBrowserShadow: {},
    dragShadowZIndex: { default: 1100 }
  },
  emits: ["click-header-action"],
  setup(e, { expose: n, emit: t }) {
    const i = e, o = t, s = ref(null), a = (j) => {
      if (console.log(j), j === void 0)
        debugger;
      if (j === window)
        debugger;
    }, u = ref(), c = ref(), d = ref(), h2 = ref(), p2 = ref(), _ = ref({ x: 0, y: 0 }), v = ref(), { x: y, y: L } = si(), B = computed(() => i.sections.map(({ name: j }) => i.items.filter(({ sectionId: oe }) => oe === j).length).reduce((j, oe) => oe > j ? oe : j)), R = computed(() => i.sections.filter(({ name: j }) => i.pinSection === j).pop()), ee = computed(() => R.value ? i.sections.filter((j) => j !== R.value) : i.sections);
    async function ce(j, oe, fe) {
      var we, Se;
      await nextTick(), d.value = true, h2.value = { id: fe.id };
      const { top: Ie, bottom: E, left: q, width: te, height: he } = (we = j == null ? void 0 : j.target) == null ? void 0 : we.getBoundingClientRect();
      _.value.x = -(y.value - q), _.value.y = 2, v.value = te, (Se = i.dragBrowserShadow) != null && Se.value || j.dataTransfer.setDragImage(new Image(), 0, 0);
    }
    const Z = computed(() => {
      var fe, Ie, E, q, te;
      if (!d.value && ((fe = p2.value) == null ? void 0 : fe.length) === 0)
        return;
      const j = L.value + ((E = (Ie = u.value) == null ? void 0 : Ie.$el) == null ? void 0 : E.scrollTop), oe = (q = p2.value) == null ? void 0 : q.map(({ offsetTop: he }) => he).reduce((he, we) => Math.abs(we - j) < Math.abs(he - j) ? we : he, 0);
      return (te = p2.value) == null ? void 0 : te.filter((he) => he.offsetTop === oe)[0];
    });
    function D(j) {
      d.value = false, h2.value = void 0, _.value = { x: 0, y: 0 }, v.value = 0;
    }
    n(
      { log: a }
    );
    const [P, Q] = on(), [z, se] = on();
    return onMounted(() => {
      c.value = true;
    }), (j, oe) => {
      const fe = resolveComponent("vu-icon-btn"), Ie = resolveDirective("tooltip");
      return openBlock(), createElementBlock(Fragment, null, [
        createVNode(unref(z), null, {
          default: withCtx(({ item: E }) => [
            renderSlot(j.$slots, "section-header", { item: E }, () => [
              createBaseVNode("div", {
                class: "section-header",
                name: E.name
              }, [
                createBaseVNode("h5", Mp, toDisplayString(E.title), 1),
                createBaseVNode("a", {
                  class: "section-header_link flex cursor-pointer",
                  onClick: (q) => j.emits("click-header-link", q, E, E.linkTarget)
                }, toDisplayString(E.link), 9, Vp),
                E.actions ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(E.actions, ({ icon: q, tooltip: te }) => withDirectives((openBlock(), createBlock(fe, {
                  key: unref(Re)(),
                  icon: q,
                  onClick: (he) => o("click-header-action", he, E, q)
                }, null, 8, ["icon", "onClick"])), [
                  [Ie, te]
                ])), 128)) : createCommentVNode("", true)
              ], 8, xp)
            ], true)
          ]),
          _: 3
        }),
        createVNode(unref(P), null, {
          default: withCtx(({ item: E, index: q, items: te }) => {
            var he, we, Se, Ve, He, bt;
            return [
              createBaseVNode("div", {
                style: normalizeStyle({ "grid-column": q + 1, "grid-row": 1 })
              }, [
                j.pinHeaders ? createCommentVNode("", true) : (openBlock(), createBlock(unref(se), {
                  key: 0,
                  section: E
                }, null, 8, ["section"]))
              ], 4),
              d.value ? (openBlock(), createElementBlock("div", {
                key: 0,
                ref_key: "dragTargets",
                ref: p2,
                "data-key": `__drag-header-${E.name}`,
                style: normalizeStyle({ "grid-column": q, "grid-row": 1 })
              }, null, 12, Pp)) : createCommentVNode("", true),
              renderSlot(j.$slots, `section-drag-header-${E.name}`, normalizeProps(guardReactiveProps({ section: E })), void 0, true),
              E.reorderable && d.value ? (openBlock(), createElementBlock("div", {
                key: 1,
                ref_key: "dragTargets",
                ref: p2,
                "data-key": `__first-${E.name}`
              }, null, 8, Lp)) : createCommentVNode("", true),
              d.value ? renderSlot(j.$slots, "item-drop-placeholder", mergeProps({
                key: 2,
                dragData: h2.value,
                show: ((Se = (we = (he = Z.value) == null ? void 0 : he.attributes) == null ? void 0 : we["data-key"]) == null ? void 0 : Se.value) === `__first-${E.name}`
              }, { section: E }), () => {
                var Ce, me, Te;
                return [
                  ((Te = (me = (Ce = Z.value) == null ? void 0 : Ce.attributes) == null ? void 0 : me["data-key"]) == null ? void 0 : Te.value) === `__first-${E.name}` ? (openBlock(), createElementBlock("div", {
                    key: 0,
                    class: "h-2px mt-2px border-drag-dash-5 border-blue-3 full-width",
                    style: normalizeStyle({ "grid-column": q, "grid-row": 1 })
                  }, null, 4)) : createCommentVNode("", true)
                ];
              }, true) : createCommentVNode("", true),
              (openBlock(true), createElementBlock(Fragment, null, renderList(te.filter((Ce) => Ce.sectionId === E.name), (Ce, me) => {
                var Te, yt, Mt, Jt;
                return openBlock(), createElementBlock(Fragment, {
                  key: Ce.id
                }, [
                  createVNode(hs, mergeProps({ ...Ce }, {
                    draggable: i.draggable,
                    onDragstart: (Pe) => ce(Pe, E, Ce),
                    onDragend: oe[0] || (oe[0] = (Pe) => D()),
                    style: [{ "grid-column": q, "grid-row": me + 2 }, Ce.id === ((Te = h2.value) == null ? void 0 : Te.id) && "opacity: 0.6"]
                  }), null, 16, ["draggable", "onDragstart", "style"]),
                  E.reorderable && d.value && Ce.id !== h2.value.id ? (openBlock(), createElementBlock("div", {
                    key: 0,
                    ref_for: true,
                    ref_key: "dragTargets",
                    ref: p2,
                    "data-key": Ce.id
                  }, null, 8, Dp)) : createCommentVNode("", true),
                  d.value ? renderSlot(j.$slots, "item-drop-placeholder", {
                    key: 1,
                    dragData: h2.value,
                    show: ((Jt = (Mt = (yt = Z.value) == null ? void 0 : yt.attributes) == null ? void 0 : Mt["data-key"]) == null ? void 0 : Jt.value) === Ce.id
                  }, () => {
                    var Pe, ut, Vt;
                    return [
                      ((Vt = (ut = (Pe = Z.value) == null ? void 0 : Pe.attributes) == null ? void 0 : ut["data-key"]) == null ? void 0 : Vt.value) === Ce.id ? (openBlock(), createElementBlock("div", Ap)) : createCommentVNode("", true)
                    ];
                  }, true) : createCommentVNode("", true)
                ], 64);
              }), 128)),
              d.value ? (openBlock(), createElementBlock("div", {
                key: 3,
                ref_key: "dragTargets",
                ref: p2,
                "data-key": `__drag-footer-${E.name}`
              }, null, 8, Fp)) : createCommentVNode("", true),
              renderSlot(j.$slots, `section-drag-footer-${E.name}`, normalizeProps(guardReactiveProps({
                show: ((bt = (He = (Ve = Z.value) == null ? void 0 : Ve.attributes) == null ? void 0 : He["data-key"]) == null ? void 0 : bt.value) === `drag-footer-${E.name}`,
                section: E,
                dragging: d.value,
                dragData: h2.value
              })), void 0, true),
              renderSlot(j.$slots, `section-footer-${E.name}`, {}, void 0, true)
            ];
          }),
          _: 3
        }),
        createBaseVNode("div", {
          ref_key: "container",
          ref: s,
          class: "vu-thumbnail-list flex items-stretch full-width h-full flex-nowrap'"
        }, [
          j.pinSection ? (openBlock(), createElementBlock("div", zp, [
            createVNode(Qe, null, {
              default: withCtx(() => [
                createVNode(unref(se), { item: R.value }, null, 8, ["item"]),
                createVNode(unref(Q), {
                  item: R.value,
                  index: 1,
                  items: j.items
                }, null, 8, ["item", "items"])
              ]),
              _: 1
            })
          ])) : createCommentVNode("", true),
          createBaseVNode("div", null, [
            j.pinHeaders ? (openBlock(), createElementBlock("div", {
              key: 0,
              class: "grid",
              style: normalizeStyle({ "grid-template-columns": `repeat(${ee.value.length}, minmax(0, 1fr))` })
            }, [
              (openBlock(true), createElementBlock(Fragment, null, renderList(ee.value, (E, q) => renderSlot(j.$slots, "section-header", {
                key: E.name,
                section: E
              }, () => [
                createBaseVNode("div", {
                  class: "section-header",
                  name: E.name
                }, [
                  createBaseVNode("h5", Np, toDisplayString(E.title), 1),
                  createBaseVNode("a", {
                    class: "section-header_link flex cursor-pointer",
                    onClick: (te) => j.emits("click-header-link", te, E, E.linkTarget)
                  }, toDisplayString(E.link), 9, Rp),
                  E.actions ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(E.actions, ({ icon: te, tooltip: he }) => withDirectives((openBlock(), createBlock(fe, {
                    key: unref(Re)(),
                    icon: te,
                    onClick: (we) => o("click-header-action", we, E, te)
                  }, null, 8, ["icon", "onClick"])), [
                    [Ie, he]
                  ])), 128)) : createCommentVNode("", true)
                ], 8, Ep)
              ], true)), 128))
            ], 4)) : createCommentVNode("", true),
            createVNode(Qe, {
              ref_key: "scroller",
              ref: u,
              class: normalizeClass(["flex-grow", j.pinSection && "flex"])
            }, {
              default: withCtx(() => [
                createBaseVNode("div", {
                  class: "vu=thumbnail-list__inner grid grid-gap-col-2",
                  style: normalizeStyle({
                    "grid-template-columns": `repeat(${ee.value.length}minmax(0, 1fr))`,
                    "grid-template-rows": `repeat(${B.value.value + 1}, minmax(0, 1fr))`
                  })
                }, [
                  (openBlock(true), createElementBlock(Fragment, null, renderList(ee.value, (E, q) => (openBlock(), createBlock(unref(Q), {
                    key: E.name,
                    item: E,
                    index: q,
                    items: j.items
                  }, null, 8, ["item", "index", "items"]))), 128))
                ], 4)
              ]),
              _: 1
            }, 8, ["class"])
          ])
        ], 512)
      ], 64);
    };
  }
}), Up = /* @__PURE__ */ F(jp, [["__scopeId", "data-v-5bda3095"]]), Wp = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Up
}, Symbol.toStringTag, { value: "Module" }));
function qp(e) {
  var n;
  const t = ie(e);
  return (n = t == null ? void 0 : t.$el) != null ? n : t;
}
const Ei = un ? window : void 0;
function Dn(...e) {
  let n, t, i, o;
  if (typeof e[0] == "string" || Array.isArray(e[0]) ? ([t, i, o] = e, n = Ei) : [n, t, i, o] = e, !n)
    return Be;
  Array.isArray(t) || (t = [t]), Array.isArray(i) || (i = [i]);
  const s = [], a = () => {
    s.forEach((h2) => h2()), s.length = 0;
  }, u = (h2, p2, _, v) => (h2.addEventListener(p2, _, v), () => h2.removeEventListener(p2, _, v)), c = watch(
    () => [qp(n), ie(o)],
    ([h2, p2]) => {
      if (a(), !h2)
        return;
      const _ = ei(p2) ? { ...p2 } : p2;
      s.push(
        ...t.flatMap((v) => i.map((y) => u(h2, v, y, _)))
      );
    },
    { immediate: true, flush: "post" }
  ), d = () => {
    c(), a();
  };
  return at(d), d;
}
const Fs = 1;
function zs(e, n = {}) {
  const {
    throttle: t = 0,
    idle: i = 200,
    onStop: o = Be,
    onScroll: s = Be,
    offset: a = {
      left: 0,
      right: 0,
      top: 0,
      bottom: 0
    },
    eventListenerOptions: u = {
      capture: false,
      passive: true
    },
    behavior: c = "auto",
    window: d = Ei
  } = n, h2 = ref(0), p2 = ref(0), _ = computed({
    get() {
      return h2.value;
    },
    set(P) {
      y(P, void 0);
    }
  }), v = computed({
    get() {
      return p2.value;
    },
    set(P) {
      y(void 0, P);
    }
  });
  function y(P, Q) {
    var z, se, j;
    if (!d)
      return;
    const oe = ie(e);
    oe && ((j = oe instanceof Document ? d.document.body : oe) == null || j.scrollTo({
      top: (z = ie(Q)) != null ? z : v.value,
      left: (se = ie(P)) != null ? se : _.value,
      behavior: ie(c)
    }));
  }
  const L = ref(false), B = reactive({
    left: true,
    right: false,
    top: true,
    bottom: false
  }), R = reactive({
    left: false,
    right: false,
    top: false,
    bottom: false
  }), ee = (P) => {
    L.value && (L.value = false, R.left = false, R.right = false, R.top = false, R.bottom = false, o(P));
  }, ce = Tn(ee, t + i), Z = (P) => {
    var Q;
    if (!d)
      return;
    const z = P.document ? P.document.documentElement : (Q = P.documentElement) != null ? Q : P, { display: se, flexDirection: j } = getComputedStyle(z), oe = z.scrollLeft;
    R.left = oe < h2.value, R.right = oe > h2.value;
    const fe = Math.abs(oe) <= 0 + (a.left || 0), Ie = Math.abs(oe) + z.clientWidth >= z.scrollWidth - (a.right || 0) - Fs;
    se === "flex" && j === "row-reverse" ? (B.left = Ie, B.right = fe) : (B.left = fe, B.right = Ie), h2.value = oe;
    let E = z.scrollTop;
    P === d.document && !E && (E = d.document.body.scrollTop), R.top = E < p2.value, R.bottom = E > p2.value;
    const q = Math.abs(E) <= 0 + (a.top || 0), te = Math.abs(E) + z.clientHeight >= z.scrollHeight - (a.bottom || 0) - Fs;
    se === "flex" && j === "column-reverse" ? (B.top = te, B.bottom = q) : (B.top = q, B.bottom = te), p2.value = E;
  }, D = (P) => {
    var Q;
    if (!d)
      return;
    const z = (Q = P.target.documentElement) != null ? Q : P.target;
    Z(z), L.value = true, ce(P), s(P);
  };
  return Dn(
    e,
    "scroll",
    t ? Qo(D, t, true, false) : D,
    u
  ), dn(() => {
    const P = ie(e);
    P && Z(P);
  }), Dn(
    e,
    "scrollend",
    ee,
    u
  ), {
    x: _,
    y: v,
    isScrolling: L,
    arrivedState: B,
    directions: R,
    measure() {
      const P = ie(e);
      d && P && Z(P);
    }
  };
}
function Cn(e) {
  return typeof Window < "u" && e instanceof Window ? e.document.documentElement : typeof Document < "u" && e instanceof Document ? e.documentElement : e;
}
const Kp = {
  [Go.mounted](e, n) {
    if (typeof n.value == "function") {
      const t = n.value, i = zs(e, {
        onScroll() {
          t(i);
        },
        onStop() {
          t(i);
        }
      });
    } else {
      const [t, i] = n.value, o = zs(e, {
        ...i,
        onScroll(s) {
          var a;
          (a = i.onScroll) == null || a.call(i, s), t(o);
        },
        onStop(s) {
          var a;
          (a = i.onStop) == null || a.call(i, s), t(o);
        }
      });
    }
  }
};
function Ni(e) {
  const n = window.getComputedStyle(e);
  if (n.overflowX === "scroll" || n.overflowY === "scroll" || n.overflowX === "auto" && e.clientWidth < e.scrollWidth || n.overflowY === "auto" && e.clientHeight < e.scrollHeight)
    return true;
  {
    const t = e.parentNode;
    return !t || t.tagName === "BODY" ? false : Ni(t);
  }
}
function Gp(e) {
  const n = e || window.event, t = n.target;
  return Ni(t) ? false : n.touches.length > 1 ? true : (n.preventDefault && n.preventDefault(), false);
}
const tn = /* @__PURE__ */ new WeakMap();
function Yp(e, n = false) {
  const t = ref(n);
  let i = null, o;
  watch(Wn(e), (u) => {
    const c = Cn(ie(u));
    if (c) {
      const d = c;
      tn.get(d) || tn.set(d, o), t.value && (d.style.overflow = "hidden");
    }
  }, {
    immediate: true
  });
  const s = () => {
    const u = Cn(ie(e));
    !u || t.value || (qt && (i = Dn(
      u,
      "touchmove",
      (c) => {
        Gp(c);
      },
      { passive: false }
    )), u.style.overflow = "hidden", t.value = true);
  }, a = () => {
    var u;
    const c = Cn(ie(e));
    !c || !t.value || (qt && (i == null || i()), c.style.overflow = (u = tn.get(c)) != null ? u : "", tn.delete(c), t.value = false);
  };
  return at(a), computed({
    get() {
      return t.value;
    },
    set(u) {
      u ? s() : a();
    }
  });
}
function Xp() {
  let e = false;
  const n = ref(false);
  return (t, i) => {
    if (n.value = i.value, e)
      return;
    e = true;
    const o = Yp(t, i.value);
    watch(n, (s) => o.value = s);
  };
}
Xp();
function Jp(e, n) {
  const t = e.findIndex((i) => !n(i));
  return t >= 0 ? e.slice(0, t) : e;
}
function Zp(e, n) {
  const t = e.toReversed(), i = t.findIndex((o) => !n(o));
  return i >= 0 ? t.slice(0, i).reverse() : e;
}
const Qp = ["data-number", "data-id"], em = {
  key: 1,
  class: "v-spacer"
}, tm = {
  key: 0,
  class: "h-drag-dash-2 blue-2 flex flex-grow w-full"
}, nm = ["data-key"], sm$1 = {
  key: 0,
  class: "h-drag-dash-2 blue-2 flex flex-grow w-full"
}, im = ["data-id"], om = ["data-id"], lm = {
  key: 1,
  class: "h-drag-dash-2 item-drop blue-2 flex flex-grow w-full"
}, am = ["data-id"], rm = ["data-id"], um = {
  name: "vu-thumbnail-list"
}, dm = /* @__PURE__ */ defineComponent({
  ...um,
  props: /* @__PURE__ */ mergeModels({
    items: {},
    sections: {},
    selectedSections: {},
    labels: {},
    draggable: { type: Boolean, default: true },
    reorderableSections: { type: Boolean, default: false },
    stickyHeaders: { type: Boolean },
    expandableHeaders: { type: Boolean, default: true },
    itemHeight: { default: 44 },
    headerItemHeight: { default: 44 },
    dropZoneHeight: { default: 60 },
    dropZoneMargin: { default: 10 },
    dragOffset: { type: [Function, Boolean], default: true },
    restoreCollapsedOnReorderExit: { type: Boolean, default: true },
    options: { default: () => ({
      thumbnailDropMinDistance: 80,
      thumbnailFullscreenDropMinDistance: 220,
      sectionDropMinDistance: 80
    }) },
    showDelete: { default: [] },
    showDeleteOnAll: { type: Boolean, default: false },
    fullscreen: { type: Boolean, default: false },
    showEmptySectionDrop: { type: Boolean },
    showEmptySectionDropExcept: { default: [] },
    showItemDropzone: { type: Boolean },
    scrollToFirst: { type: Boolean }
  }, {
    modelValue: {
      default: []
    },
    modelModifiers: {},
    dragData: {
      default: {}
    },
    dragDataModifiers: {}
  }),
  emits: /* @__PURE__ */ mergeModels(["click-header-action", "drag-start-item", "drag-start", "drag-cancel", "drop-section", "drop-item", "drop-item-remove", "section-actions-menu-open", "section-actions-menu-close", "section-collapse", "section-expand", "item-click", "item-click-action", "item-actions-menu-open", "item-actions-menu-close", "item-mouseenter", "item-mouseleave", "item-contextmenu", "item-thumbnail-doubleclick"], ["update:modelValue", "update:dragData"]),
  setup(e, { expose: n, emit: t }) {
    let i;
    ((m) => {
      m[m.USER = 0] = "USER", m[m.PROGRAMMATICALLY = 1] = "PROGRAMMATICALLY";
    })(i || (i = {}));
    const o = e, s = t, a = "ANY", u = ref({
      moveHere: "Move here",
      moveSectionHere: "Move here",
      removeFrom: "Remove"
    }), c = useSlots(), d = useModel(e, "modelValue"), h2 = useModel(e, "dragData"), p2 = ref(null), _ = ref({}), v = ref({}), y = ref([]), L = ref([]), B = ref(), { top: R, left: ee } = ln(B), ce = ref(false);
    function Z(m) {
      const { isScrolling: I } = m;
      ce.value = I.value;
    }
    const D = computed(() => {
      var m;
      return ((m = o.sections) == null ? void 0 : m.map(({ name: I }) => I)) || {};
    }), P = ref(false), Q = ref(false), z = ref({}), se = ref(), { x: j, y: oe } = si({ scroll: false }), fe = ref(), Ie = ref();
    function E(m) {
      var W, ne, A;
      const I = m.target.cloneNode(true);
      I.classList += " dragged", I.classList.remove("selected"), (W = I.classList) != null && W.contains("section-header") && I.classList.remove(".section-header--dragged");
      const { width: g, left: O } = m.target.getBoundingClientRect();
      I.style.width = `${g}px`, I.style.opacity = "0.6", I.style.position = "absolute", I.style.left = "-5000px", I.style.top = "-1000px", I.style.zIndex = "10000", gs.value = I, document.body.appendChild(I);
      let N = 0;
      typeof o.dragOffset == "function" ? N = o.dragOffset(m) : o.dragOffset === true && (N = (m == null ? void 0 : m.clientX) - O, (ne = m.dataTransfer) == null || ne.setDragImage(I, N, 0)), (A = m.dataTransfer) == null || A.setData("text/plain", " ");
    }
    function q(m) {
      m != null && m.dataTransfer && (m.dataTransfer.dropEffect = "none"), m.preventDefault(), m.stopPropagation();
    }
    function te(m) {
      m != null && m.dataTransfer && (m.dataTransfer.dropEffect = "move"), m.preventDefault();
    }
    function he(m, I, g) {
      o.draggable && (P.value = true, z.value = { id: I.id, section: g, thumbnail: I }, m.dataTransfer.effectAllowed = "move", m.dataTransfer.dropEffect = "move", E(m), s("drag-start"), s("drag-start-item", z.value));
    }
    function we(m, I) {
      Q.value = true, z.value = { section: I, thumbnail: void 0 }, m.dataTransfer.effectAllowed = "move", m.dataTransfer.dropEffect = "move", E(m), s("drag-start");
    }
    const Se = ref(false);
    function Ve(m, I) {
      var g, O, N, W;
      if (P.value) {
        if (I && !(!I.accept || ((g = I == null ? void 0 : I.accept) == null ? void 0 : g.includes(a)) || ((N = (O = z.value) == null ? void 0 : O.thumbnail) == null ? void 0 : N.type) && ((W = I.accept) == null ? void 0 : W.includes(z.value.thumbnail.type)))) {
          m != null && m.dataTransfer && (m.dataTransfer.dropEffect = "none");
          return;
        }
        Se.value = true, m != null && m.dataTransfer && (m.dataTransfer.dropEffect = "move");
      }
    }
    function He(m) {
      Se.value = false, fe.value && (m != null && m.dataTransfer) && (m.dataTransfer.dropEffect = "move");
    }
    function bt({ x: m, y: I }, g, O = Number.POSITIVE_INFINITY, N = "y") {
      const { key: W, id: ne } = (g == null ? void 0 : g.map((A) => {
        var Ft, ke, Oe, Ne;
        A = (A == null ? void 0 : A.$el) || A;
        const { offsetLeft: ae, offsetHeight: ve, offsetTop: be, attributes: { "data-id": Le, "data-key": ot, clientWidth: yn } = {} } = A;
        let Lt = be, Dt = ae;
        (ke = (Ft = A == null ? void 0 : A.parentElement) == null ? void 0 : Ft.classList) != null && ke.contains("vu-thumbnail-list-item") && (Dt += A.parentElement.offsetLeft, Lt += A.parentElement.offsetTop);
        let wt;
        const Zt = (Ne = (Oe = B.value) == null ? void 0 : Oe.$el) == null ? void 0 : Ne.scrollTop, At = Lt - Zt;
        switch (N) {
          case "y":
            wt = Math.abs(At - I);
            break;
          case "both":
            wt = Math.sqrt((At - I) ** 2 + (Dt - m) ** 2);
        }
        return {
          distance: wt,
          key: ot == null ? void 0 : ot.value,
          id: Le == null ? void 0 : Le.value
        };
      }).filter(({ distance: A }) => A < O).reduce((A, ae) => ae.distance < A.distance ? ae : A, { distance: Number.POSITIVE_INFINITY, key: void 0, id: void 0 })) || {};
      return W || ne ? g == null ? void 0 : g.find((A) => {
        A = (A == null ? void 0 : A.$el) || A;
        const { attributes: ae } = A, { "data-key": ve = { value: "_noValue" }, "data-id": be = { value: "_noId" } } = ae;
        return W === (ve == null ? void 0 : ve.value) || ne === (be == null ? void 0 : be.value);
      }) : void 0;
    }
    xn([P, se, oe, j, Se], () => {
      if (!P.value) {
        fe.value = void 0;
        return;
      }
      if (ce.value)
        return;
      if (Se.value) {
        fe.value = void 0;
        return;
      }
      const m = oe.value - R.value, I = j.value - ee.value;
      fe.value = bt({ y: m, x: I }, se.value, Ce.value, o.fullscreen ? "both" : "y");
    }, { throttle: 120 }), xn([Q, se, oe], () => {
      if (!Q.value) {
        Ie.value = void 0;
        return;
      }
      if (ce.value)
        return;
      const m = oe.value - R.value;
      Ie.value = bt({ x: 0, y: m }, se.value, o.options.sectionDropMinDistance);
    }, { throttle: 120 });
    const Ce = computed(() => o.fullscreen ? o.options.thumbnailFullscreenDropMinDistance : o.options.thumbnailDropMinDistance), me = computed(() => {
      var g, O, N, W, ne, A;
      const m = (N = (O = (g = fe.value) == null ? void 0 : g.attributes) == null ? void 0 : O["data-id"]) == null ? void 0 : N.value, I = (A = (ne = (W = fe.value) == null ? void 0 : W.attributes) == null ? void 0 : ne["data-number"]) == null ? void 0 : A.value;
      return m && (I ? Number(m) : m) || void 0;
    }), Te = computed(() => {
      var m, I, g;
      return (g = (I = (m = Ie.value) == null ? void 0 : m.attributes) == null ? void 0 : I["data-key"]) == null ? void 0 : g.value;
    }), yt = computed(() => {
      var m;
      return (m = o.sections) == null ? void 0 : m.filter(({ reorderable: I }) => I === false);
    }), Mt = computed(() => Jp(o.sections, ({ reorderable: m }) => m === false)), Jt = computed(() => Zp(o.sections, ({ reorderable: m }) => m === false)), Pe = ref();
    watch(me, (m, I) => {
      var O;
      if (I === m)
        return;
      const g = (O = m == null ? void 0 : m.toString().match(/(__collapsed-)(\w+)/)) == null ? void 0 : O[2];
      if (g) {
        const N = o.sections.find(({ name: W }) => W === g);
        y.value.includes(N) && (Pe.value = N, co());
      } else
        Pe.value = void 0, ho();
    });
    const ut = computed(() => {
      var m;
      if (P.value)
        return (m = o.items) == null ? void 0 : m.filter((I) => I.sectionId === z.value.thumbnail.sectionId);
    }), Vt = computed(() => {
      var m, I;
      return ((I = ut == null ? void 0 : ut.value) == null ? void 0 : I.indexOf((m = z.value) == null ? void 0 : m.thumbnail)) - 1 || 0;
    });
    function Pt(m) {
      var I;
      if (Vt.value !== -1)
        return ((I = ut.value) == null ? void 0 : I.indexOf(m)) === Vt.value;
    }
    function Qi(m) {
      return o.items.findLast((g) => m.sectionId === g.sectionId) === m;
    }
    function ps(m) {
      var I;
      if (Mt.value.includes(m) && ((I = Mt.value.slice(-1)) == null ? void 0 : I[0]) !== m)
        return false;
      if (!Jt.value.includes(m))
        return true;
    }
    function eo(m) {
      const I = Te.value;
      if (!I)
        return;
      const g = [...o.sections], O = g.findIndex(({ name: W }) => W === I);
      return g.findIndex(({ name: W }) => W === m) === O + 1;
    }
    function ms(m) {
      var N, W, ne, A, ae, ve, be, Le;
      P.value = false;
      const I = ((ne = (W = (N = m == null ? void 0 : m.target) == null ? void 0 : N.attributes) == null ? void 0 : W["data-id"]) == null ? void 0 : ne.value) || ((be = (ve = (ae = (A = m == null ? void 0 : m.target) == null ? void 0 : A.parentElement) == null ? void 0 : ae.attributes) == null ? void 0 : ve["data-id"]) == null ? void 0 : be.value);
      if (!I) {
        z.value = void 0;
        return;
      }
      const g = I.split("__remove-from-").pop(), O = o.items.find(({ sectionId: ot }) => ot === name);
      s("drop-item-remove", m, {
        item: (Le = z.value) == null ? void 0 : Le.thumbnail,
        section: O,
        sectionId: g
      }), z.value = void 0;
    }
    function to2() {
      io.value = void 0, vs.value = void 0, P.value = false, Q.value = false, z.value = void 0, gs.value = void 0, Se.value = false, s("drag-cancel");
    }
    function fn(m) {
      var N, W, ne;
      let I = me.value;
      if (!P.value)
        return;
      if (((N = z.value) == null ? void 0 : N.id) === I || !I) {
        to2();
        return;
      }
      I.toString().startsWith("__collapsed-") && (I = I.replace("__collapsed-", "__first-"));
      const g = o.items.find(({ id: A }) => A === I);
      P.value = false, Se.value = false;
      let O;
      if (g)
        O = {
          item: g,
          index: ((W = o.items) == null ? void 0 : W.indexOf(g)) + 1
        };
      else {
        const A = I.split("__first-")[1];
        O = {
          index: o.items.findIndex(({ sectionId: ve }) => ve === name) + 1 || 0,
          name: A
        };
      }
      s("drop-item", m, {
        // The item after which the element must be placed.
        // If empty, will be first element of section
        from: (ne = z.value) == null ? void 0 : ne.thumbnail,
        to: O
      }), z.value = void 0;
    }
    function no(m) {
      var O, N, W, ne, A, ae, ve, be;
      P.value = false, Se.value = false;
      const I = ((W = (N = (O = m == null ? void 0 : m.target) == null ? void 0 : O.attributes) == null ? void 0 : N["data-id"]) == null ? void 0 : W.value) || ((ve = (ae = (A = (ne = m == null ? void 0 : m.target) == null ? void 0 : ne.parentElement) == null ? void 0 : A.attributes) == null ? void 0 : ae["data-id"]) == null ? void 0 : ve.value);
      if (!I) {
        z.value = void 0;
        return;
      }
      const g = I.split("__empty-section-").pop();
      s(
        "drop-item",
        m,
        {
          from: (be = z.value) == null ? void 0 : be.thumbnail,
          to: {
            index: 0,
            name: g
          }
        }
      ), z.value = void 0;
    }
    function so(m) {
      var g, O;
      const I = Te.value;
      if (Q.value = false, I) {
        const N = (g = o.sections) == null ? void 0 : g.findIndex(({ name: ne }) => ne === I), W = N !== -1 ? o.sections[N] : void 0;
        s("drop-section", m, {
          from: (O = z.value) == null ? void 0 : O.section,
          // The item after which the element must be placed.
          // If empty, will be first element.
          to: {
            section: W,
            index: N
          }
        });
      }
      z.value = void 0;
    }
    const io = ref(), vs = ref(), gs = ref();
    onBeforeMount(() => {
      h2.value = {
        dragData: z,
        dropThumbnailElement: fe,
        dropSectionElement: Ie,
        dropThumbnailId: me,
        dropSectionId: Te
      };
    });
    const pn = ref();
    ref(), n({
      expandHeader: vn,
      expand: gn,
      collapse: _s
    });
    const [oo, bs] = on(), [lo, ao] = on();
    onMounted(() => {
      mn.value = true, o.sections.map((m) => ps(m));
    });
    const je = computed(() => ({
      ...u.value,
      ...o.labels
    })), mn = ref(false);
    watch(mn, () => {
      setTimeout(() => {
        mn.value = false;
      }, 1);
    });
    function vn(m, { smooth: I = true } = {}) {
      const O = _.value[m].offsetTop;
      let N;
      I && (N = { behavior: "smooth" }), B.value.$el.scrollTo({ top: O, ...N });
    }
    function ys(m) {
      o.reorderableSections || (y.value.includes(m) ? y.value.includes(m) && (gn(m.name, true), o.stickyHeaders && vn(m.name)) : _s(m.name, true));
    }
    function gn(m, I = false) {
      if (o.reorderableSections)
        return;
      const g = o.sections.find(({ name: W }) => W === m);
      if (!g)
        return;
      const N = Object.entries(y.value).findIndex(([W, ne]) => ne.name === g.name);
      if (N !== -1) {
        y.value.splice(N, 1);
        const { name: W } = g;
        s(
          "section-expand",
          g,
          W,
          I ? 0 : 1
          /* PROGRAMMATICALLY */
        );
      }
    }
    function _s(m, I = false) {
      const g = o.sections.find(({ name: ne }) => ne === m);
      if (!g || Object.entries(y.value).findIndex(([ne, A]) => A.name === g.name) !== -1)
        return;
      o.stickyHeaders && (v.value[m] = false), y.value.push(g);
      const { name: W } = g;
      s(
        "section-collapse",
        g,
        W,
        I ? 0 : 1
        /* PROGRAMMATICALLY */
      );
    }
    watch(() => o.reorderableSections, (m) => {
      o.restoreCollapsedOnReorderExit && (m ? L.value = [...y.value] : y.value = L.value), m && y.value.splice(0, (y == null ? void 0 : y.value.length) || 0, ...o.sections);
    }, { immediate: true });
    const _t = dl(D);
    function ws(m) {
      const I = m.map((O) => o.sections.find(({ name: N }) => N === O)), g = I.filter(({ collapsed: O }) => O === true);
      o.reorderableSections ? (o.restoreCollapsedOnReorderExit && g.length && L.value.push(...g), y.value.push(...I)) : y.value.push(...g);
    }
    watch(() => o.sections, () => {
      var I;
      const m = (I = o.sections) == null ? void 0 : I.map(({ name: g }) => g);
      ws(m);
    }, { immediate: true, once: true }), watch([() => o.sections.length], () => {
      const m = o.sections, I = (m == null ? void 0 : m.map(({ name: W }) => W)) || [], g = _t != null && _t.value ? Array.from(_t == null ? void 0 : _t.value) : [], O = g == null ? void 0 : g.filter((W) => !(I != null && I.includes(W))), N = I == null ? void 0 : I.filter((W) => !(g != null && g.includes(W)));
      O.map((W) => {
        o.restoreCollapsedOnReorderExit && delete L.value[W];
      }), N != null && N.length && ws(N);
    }, { deep: true });
    const bn = computed(() => {
      var m;
      return !o.selectedSections || ((m = o.selectedSections) == null ? void 0 : m.length) === 0;
    }), ro = computed(() => {
      var m, I;
      if (o.scrollToFirst)
        return (I = (m = o == null ? void 0 : o.sections) == null ? void 0 : m.map(({ name: g }) => {
          var N;
          const O = (N = o == null ? void 0 : o.items) == null ? void 0 : N.findIndex(({ sectionId: W }) => W === g);
          return [g, O > -1 ? O : void 0];
        }).find(([, g]) => g > -1)) == null ? void 0 : I.map((g) => o.items[g]).pop();
    });
    Mn(() => o.selectedSections, () => {
      var m;
      if (!bn.value && ((m = o.selectedSections) != null && m[0])) {
        const I = () => {
          var O, N;
          let g = (N = (O = _ == null ? void 0 : _.value) == null ? void 0 : O[o.selectedSections[0].name]) == null ? void 0 : N.$el;
          return g = (g == null ? void 0 : g.nextElementSibling) || g, g ? (g != null && g.scrollIntoViewIfNeeded ? g.scrollIntoViewIfNeeded() : g.scrollIntoView({
            block: "nearest"
          }), true) : false;
        };
        I() || nextTick(I);
      }
    }, { immediate: true });
    function uo(m) {
      return o.items.find(({ sectionId: I }) => I === m);
    }
    const { start: co, stop: ho, isPending: ks } = nl(() => {
      var m;
      (m = Pe.value) != null && m.name && (gn(Pe.value.name), Pe.value = void 0);
    }, 1e3);
    return (m, I) => (openBlock(), createElementBlock(Fragment, null, [
      createVNode(unref(oo), null, {
        default: withCtx(({ item: g }) => [
          (openBlock(true), createElementBlock(Fragment, null, renderList(m.items.filter((O) => O.sectionId === g.name), (O) => {
            var N, W, ne;
            return openBlock(), createElementBlock(Fragment, {
              key: O.id
            }, [
              withDirectives(createVNode(hs, mergeProps({ class: "relative" }, {
                ...O,
                selected: d.value.includes(O),
                scrollIntoView: bn.value,
                forceScrollIntoView: m.scrollToFirst && O === ro.value || O.forceScrollIntoView && !bn.value
              }, {
                draggable: o.draggable,
                class: {
                  "max-w-[280px]": m.fullscreen,
                  "m-t-[2px]": m.fullscreen,
                  dragging: P.value
                },
                style: [
                  O.id === ((N = z.value) == null ? void 0 : N.id) && "opacity: 0.6",
                  !m.fullscreen && Qi(O) && "margin-bottom: 2px",
                  !m.fullscreen && me.value === O.id && "margin-bottom: 0"
                ],
                onClick: (A) => s("item-click", A, O),
                onClickAction: (A) => s("item-click-action", A, O.value),
                onDragstart: (A) => he(A, O, g),
                onDragover: (A) => {
                  var ae;
                  P.value && me.value === O.id && O.id === ((ae = z.value) == null ? void 0 : ae.id) || !g.reorderableItems ? q(A) : P.value && !pn.value && me.value && te(A);
                },
                onActionsOpen: (A) => s("item-actions-menu-open", O),
                onActionsClose: (A) => s("item-actions-menu-close", O),
                onMouseenter: (A) => s("item-mouseenter", A, O),
                onMouseleave: (A) => s("item-mouseleave", A, O),
                onContextmenu: (A) => s("item-contextmenu", A, O)
              }), createSlots({
                "hidden-content": withCtx(() => {
                  var A, ae, ve, be;
                  return [
                    g.reorderableItems && P.value && !Pt(O) && !y.value.includes(g) && (!g.accept || (A = g == null ? void 0 : g.accept) != null && A.includes(a) || (ve = (ae = z.value) == null ? void 0 : ae.thumbnail) != null && ve.type && ((be = g.accept) != null && be.includes(z.value.thumbnail.type))) ? (openBlock(), createElementBlock("div", {
                      key: 0,
                      ref_for: true,
                      ref_key: "dragTargets",
                      ref: se,
                      class: "drag-target absolute w-[1px] h-[1px] top-[50%] left-[50%]",
                      style: normalizeStyle(o.fullscreen && "left:100%; right:0;"),
                      "data-number": typeof O.id == "number" || "",
                      "data-id": O.id
                    }, null, 12, Qp)) : createCommentVNode("", true)
                  ];
                }),
                _: 2
              }, [
                O.extraThumbnailIcon ? {
                  name: "thumbnail__extra",
                  fn: withCtx(() => [
                    createVNode(unref(ao), normalizeProps(guardReactiveProps({ extraIcon: O.extraThumbnailIcon, thumbnail: O, section: g })), null, 16)
                  ]),
                  key: "0"
                } : void 0
              ]), 1040, ["draggable", "class", "style", "onClick", "onClickAction", "onDragstart", "onDragover", "onActionsOpen", "onActionsClose", "onMouseenter", "onMouseleave", "onContextmenu"]), [
                [vShow, !((W = y.value) != null && W.includes(g))]
              ]),
              renderSlot(m.$slots, "item-drop-zone", mergeProps({
                dragData: z.value,
                show: me.value && me.value === O.id && me.value !== ((ne = z.value) == null ? void 0 : ne.id)
              }, { section: g, thumbnail: O, fullscreen: m.fullscreen }), () => {
                var A, ae, ve, be, Le;
                return [
                  m.fullscreen && !y.value.includes(g) ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
                    g.reorderableItems && P.value && !Pt(O) && O.id !== ((A = z.value) == null ? void 0 : A.id) ? (openBlock(), createElementBlock("div", {
                      key: 0,
                      class: normalizeClass(["v-drag-dash-2 blue-2 flex-basis-[2px] flex-grow-0 flex-shrink-0 h-[50px] self-stretch", {
                        invisible: me.value !== O.id,
                        "mt-[2px]": m.fullscreen
                      }])
                    }, null, 2)) : g.reorderableItems && P.value && (!Pt(O) || O.id !== ((ae = z.value) == null ? void 0 : ae.id)) ? (openBlock(), createElementBlock("div", em)) : createCommentVNode("", true)
                  ], 64)) : !m.fullscreen && m.showItemDropzone && me.value === O.id && !Pt(O) ? (openBlock(), createBlock(kt, {
                    key: 1,
                    icon: "drag-drop",
                    dashed: "",
                    centered: "",
                    color: "blue",
                    "no-hover": "",
                    label: ((ve = je.value) == null ? void 0 : ve[`moveTo${unref(dt)(g.name)}`]) || ((be = je.value) == null ? void 0 : be.moveHere),
                    onDrop: fn,
                    onDragover: Ve,
                    onDragleave: He
                  }, null, 8, ["label"])) : !m.fullscreen && !m.showItemDropzone && me.value === O.id && !Pt(O) && !unref(ks) ? (openBlock(), createElementBlock("div", {
                    key: 2,
                    class: normalizeClass(["h-drag-dash-2 blue-2 flex flex-grow w-full", { invisible: O.id === ((Le = z.value) == null ? void 0 : Le.id) }])
                  }, null, 2)) : createCommentVNode("", true)
                ];
              }, true)
            ], 64);
          }), 128))
        ]),
        _: 3
      }),
      createVNode(unref(lo), null, {
        default: withCtx(({ extraIcon: g, thumbnail: O, section: N }) => [
          createVNode(ue, {
            icon: g,
            onClick: I[0] || (I[0] = withModifiers(() => {
            }, ["stop"])),
            onDblclick: (W) => s("item-thumbnail-doubleclick", g, O, N),
            style: { "z-index": "20", "font-size": "130%", "line-height": "40px", "vertical-align": "middle", height: "100%", width: "100%", margin: "0" }
          }, null, 8, ["icon", "onDblclick"])
        ]),
        _: 1
      }),
      createBaseVNode("div", mergeProps({
        ref_key: "container",
        ref: p2,
        class: ["vu-thumbnail-list", { fullscreen: m.fullscreen }],
        onDragend: I[2] || (I[2] = (g) => {
          P.value ? fn(g) : Q.value && so(g);
        })
      }, m.$attrs), [
        withDirectives((openBlock(), createBlock(Qe, {
          ref_key: "scroller",
          ref: B
        }, {
          default: withCtx(() => [
            (openBlock(true), createElementBlock(Fragment, null, renderList(m.sections, (g, O) => {
              var N, W, ne, A, ae, ve, be, Le, ot, yn, Lt, Dt, wt, Zt, At, Ft;
              return openBlock(), createElementBlock(Fragment, {
                key: g.name
              }, [
                renderSlot(m.$slots, "section-header", { section: g }, () => {
                  var ke, Oe, Ne, zt, Et, Nt, Rt;
                  return [
                    Q.value && O === 0 && g.reorderable !== false ? (openBlock(), createElementBlock("div", {
                      key: 0,
                      ref_for: true,
                      ref_key: "dragTargets",
                      ref: se,
                      class: "drag-section",
                      "data-key": "__first"
                    }, null, 512)) : createCommentVNode("", true),
                    O === 0 ? renderSlot(m.$slots, "section-drop-placeholder", {
                      key: 1,
                      dragData: z.value,
                      show: Te.value === "__first",
                      vBind: { section: g }
                    }, () => [
                      Te.value === "__first" ? (openBlock(), createElementBlock("div", tm)) : createCommentVNode("", true)
                    ], true) : createCommentVNode("", true),
                    createVNode(zi, mergeProps({
                      ref_for: true,
                      ref: (De) => _.value[g.name] = De,
                      name: g.name,
                      class: "section-header"
                    }, {
                      expandable: m.expandableHeaders,
                      expanded: !((ke = y.value) != null && ke.includes(g)),
                      tabbed: !m.reorderableSections && ((Oe = y.value) == null ? void 0 : Oe.includes(g)) && d.value.some(({ sectionId: De }) => De === g.name),
                      title: g.title,
                      actions: g.actions,
                      indicator: g.indicator,
                      sticked: m.stickyHeaders,
                      reorderable: m.reorderableSections && !yt.value.includes(g),
                      disabled: m.reorderableSections && (((Ne = yt.value) == null ? void 0 : Ne.includes(g)) || Q.value && g.name === ((Et = (zt = z.value) == null ? void 0 : zt.section) == null ? void 0 : Et.name)),
                      dragged: Q.value && g.name === ((Rt = (Nt = z.value) == null ? void 0 : Nt.section) == null ? void 0 : Rt.name),
                      draggable: m.reorderableSections && !yt.value.includes(g),
                      dashed: Pe.value === g,
                      focused: vs.value === g
                    }, {
                      onDragover: withModifiers(() => {
                      }, ["prevent"]),
                      class: {
                        reorderable: m.reorderableSections,
                        "lesser-margin": Q.value && eo(g.name)
                      },
                      onClickExpander: (De) => ys(g),
                      onClickTitle: (De) => m.expandableHeaders ? ys(g) : m.stickyHeaders && vn(g.name),
                      onDragstart: (De) => we(De, g),
                      onMouseenter: (De) => pn.value = g,
                      onMouseleave: I[1] || (I[1] = (De) => pn.value = void 0)
                    }), null, 16, ["name", "class", "onClickExpander", "onClickTitle", "onDragstart", "onMouseenter"]),
                    Q.value && ps(g) ? (openBlock(), createElementBlock("div", {
                      key: 2,
                      ref_for: true,
                      ref_key: "dragTargets",
                      ref: se,
                      class: "drag-section",
                      "data-key": g.name
                    }, null, 8, nm)) : createCommentVNode("", true),
                    renderSlot(m.$slots, "section-drop-placeholder", {
                      dragData: z.value,
                      show: Q.value && Te.value === g.name,
                      vBind: { section: g }
                    }, () => [
                      Te.value === g.name ? (openBlock(), createElementBlock("div", sm$1)) : createCommentVNode("", true)
                    ], true)
                  ];
                }, true),
                P.value && y.value.includes(g) && !m.reorderableSections ? (openBlock(), createElementBlock("div", {
                  key: 0,
                  ref_for: true,
                  ref_key: "dragTargets",
                  ref: se,
                  "data-id": `__collapsed-${g.name}`
                }, null, 8, im)) : createCommentVNode("", true),
                renderSlot(m.$slots, `item-drop-zone-empty-section-${g.name}`, normalizeProps(guardReactiveProps({ section: g })), () => {
                  var ke, Oe, Ne, zt, Et, Nt, Rt, De;
                  return [
                    !o.reorderableSections && o.showEmptySectionDrop && !((ke = m.showEmptySectionDropExcept) != null && ke.includes(g.name)) && !((Oe = y.value) != null && Oe.includes(g)) && !m.items.filter(({ sectionId: _n }) => _n === g.name).length ? (openBlock(), createBlock(kt, {
                      key: 0,
                      class: normalizeClass([{ "w-[280px]": m.fullscreen }, "empty-dropzone"]),
                      ref_for: true,
                      ref_key: "dragTargets",
                      ref: se,
                      icon: "drag-drop",
                      dashed: "",
                      centered: "",
                      color: "grey-light",
                      hoverClassName: "vu-dropzone--blue",
                      "no-hover": "",
                      "add-hover-class": !g.accept || ((Ne = g == null ? void 0 : g.accept) == null ? void 0 : Ne[0]) === a || P.value && ((Nt = g.accept) == null ? void 0 : Nt.includes((Et = (zt = z.value) == null ? void 0 : zt.thumbnail) == null ? void 0 : Et.type)),
                      "data-id": `__empty-section-${g.name}`,
                      label: ((Rt = je.value) == null ? void 0 : Rt[`moveToEmpty${unref(dt)(g.name)}`]) || ((De = je.value) == null ? void 0 : De.moveHere),
                      onDrop: no,
                      onDragover: withModifiers((_n) => Ve(_n, g), ["prevent", "stop"]),
                      onDragleave: withModifiers(He, ["prevent", "stop"])
                    }, null, 8, ["class", "add-hover-class", "data-id", "label", "onDragover"])) : createCommentVNode("", true)
                  ];
                }, true),
                (!o.showEmptySectionDrop || o.showEmptySectionDrop && !((N = m.showEmptySectionDropExcept) != null && N.includes(g.name)) && m.items.filter(({ sectionId: ke }) => ke === g.name).length) && (!g.accept || ((W = g == null ? void 0 : g.accept) == null ? void 0 : W[0]) === a || (A = (ne = z.value) == null ? void 0 : ne.thumbnail) != null && A.type && ((ae = g.accept) != null && ae.includes(z.value.thumbnail.type))) && (g.reorderableItems || g.droppable) && P.value && z.value.thumbnail !== uo(g.name) && !y.value.includes(g) ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
                  o.fullscreen ? createCommentVNode("", true) : (openBlock(), createElementBlock("div", {
                    key: 0,
                    ref_for: true,
                    ref_key: "dragTargets",
                    ref: se,
                    class: "drag-target",
                    "data-id": `__first-${g.name}`
                  }, null, 8, om))
                ], 64)) : createCommentVNode("", true),
                renderSlot(m.$slots, "item-drop-zone", mergeProps({ dragData: z.value }, { section: g }, {
                  show: P.value && me.value === `__first-${g.name}`,
                  vBind: { section: g }
                }), () => {
                  var ke, Oe;
                  return [
                    !o.fullscreen && m.showItemDropzone && P.value && me.value === `__first-${g.name}` && !y.value.includes(g) ? (openBlock(), createBlock(kt, {
                      key: 0,
                      icon: "drag-drop",
                      dashed: "",
                      centered: "",
                      "no-hover": "",
                      color: "blue",
                      label: ((ke = je.value) == null ? void 0 : ke[`moveTo${unref(dt)(g.name)}`]) || ((Oe = je.value) == null ? void 0 : Oe.moveHere),
                      onDragover: withModifiers(Ve, ["prevent"]),
                      onDragleave: withModifiers(He, ["prevent"]),
                      onDrop: fn
                    }, null, 8, ["label"])) : !o.fullscreen && !m.showItemDropzone && P.value && me.value === `__first-${g.name}` && !y.value.includes(g) && !unref(ks) ? (openBlock(), createElementBlock("div", lm)) : createCommentVNode("", true)
                  ];
                }, true),
                m.fullscreen ? (openBlock(), createElementBlock("div", {
                  key: 2,
                  class: normalizeClass(["flex flex-row flex-wrap gap-y-[6px] gap-x-[10px] m-l-[12px]", { "gap-x-[4px]": P.value && g.reorderableItems }])
                }, [
                  P.value && (g.reorderableItems || g.droppable) && (!g.accept || ((ve = g == null ? void 0 : g.accept) == null ? void 0 : ve[0]) === a || P.value && ((ot = g.accept) != null && ot.includes((Le = (be = z.value) == null ? void 0 : be.thumbnail) == null ? void 0 : Le.type))) && !y.value.includes(g) && (!o.showEmptySectionDrop || o.showEmptySectionDrop && !((yn = m.showEmptySectionDropExcept) != null && yn.includes(g.name)) && m.items.filter(({ sectionId: ke }) => ke === g.name).length) ? (openBlock(), createElementBlock("div", {
                    key: 0,
                    class: normalizeClass(["mx-[1px] m-t-[2px] v-drag-dash-2 blue-2 flex-basis-[2px] flex-grow-0 flex-shrink-0 h-[50px] absolute left-[4px] self-stretch", {
                      invisible: !P.value || me.value !== `__first-${g.name}`
                    }]),
                    ref_for: true,
                    ref_key: "dragTargets",
                    ref: se,
                    "data-id": `__first-${g.name}`
                  }, null, 10, am)) : createCommentVNode("", true),
                  createVNode(unref(bs), normalizeProps(guardReactiveProps({ item: g })), null, 16),
                  P.value && g.reorderableItems && z.value.section === g && (m.showDeleteOnAll || (Lt = m.showDelete) != null && Lt.includes(g.name)) ? (openBlock(), createBlock(kt, {
                    key: 1,
                    class: normalizeClass(m.fullscreen && "m-y-0 w-[280px]"),
                    color: "red",
                    icon: "trash",
                    dashed: "",
                    centered: "",
                    noHover: !P.value,
                    "data-id": `__remove-from-${g.name}`,
                    label: ((Dt = je.value) == null ? void 0 : Dt[`removeFrom${unref(dt)(g.name)}`]) || ((wt = je.value) == null ? void 0 : wt.removeFrom),
                    onDrop: ms,
                    onDragover: withModifiers(Ve, ["prevent", "stop"]),
                    onDragleave: withModifiers(He, ["prevent", "stop"])
                  }, null, 8, ["class", "noHover", "data-id", "label"])) : createCommentVNode("", true)
                ], 2)) : (openBlock(), createBlock(unref(bs), normalizeProps(mergeProps({ key: 3 }, { item: g })), null, 16)),
                unref(c)[`item-drop-zone-section-footer-${g.name}`] && P.value ? (openBlock(), createElementBlock("div", {
                  key: 4,
                  ref_for: true,
                  ref_key: "dragTargets",
                  ref: se,
                  "data-id": `__item-section-footer-${g.name}`
                }, null, 8, rm)) : createCommentVNode("", true),
                renderSlot(m.$slots, `item-drop-zone-section-footer-${g.name}`, normalizeProps(guardReactiveProps({
                  show: ((Ft = (At = (Zt = fe.value) == null ? void 0 : Zt.attributes) == null ? void 0 : At["data-id"]) == null ? void 0 : Ft.value) === `drag-footer-${g.name}`,
                  section: g,
                  dragging: P.value,
                  dragData: z.value
                })), () => {
                  var ke, Oe, Ne;
                  return [
                    P.value && !m.fullscreen && z.value.section === g && (m.showDeleteOnAll || (ke = m.showDelete) != null && ke.includes(g.name)) ? (openBlock(), createBlock(kt, {
                      key: 0,
                      color: "red",
                      icon: "trash",
                      dashed: "",
                      centered: "",
                      noHover: !P.value,
                      "data-id": `__remove-from-${g.name}`,
                      label: ((Oe = je.value) == null ? void 0 : Oe[`removeFrom${unref(dt)(g.name)}`]) || ((Ne = je.value) == null ? void 0 : Ne.removeFrom),
                      onDrop: ms,
                      onDragover: withModifiers(Ve, ["prevent", "stop"]),
                      onDragleave: withModifiers(He, ["prevent", "stop"])
                    }, null, 8, ["noHover", "data-id", "label"])) : createCommentVNode("", true)
                  ];
                }, true),
                renderSlot(m.$slots, `section-footer-${g.name}`, {}, void 0, true)
              ], 64);
            }), 128))
          ]),
          _: 3
        })), [
          [unref(Kp), [Z, { throttle: 50 }]]
        ])
      ], 16)
    ], 64));
  }
}), cm = /* @__PURE__ */ F(dm, [["__scopeId", "data-v-11bcbea2"]]), hm = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: cm
}, Symbol.toStringTag, { value: "Module" })), fm = {
  key: 0,
  class: "control-label"
}, pm = {
  key: 0,
  class: "label-field-required"
}, mm = { key: 1 }, vm = ["value", "placeholder", "disabled"], gm = { class: "vu-time-picker__display form-control" }, bm = { class: "vu-time-picker__body" }, ym = { class: "vu-time-picker__hours" }, _m = ["value"], wm = { class: "vu-time-picker__minutes" }, km = ["value"], Sm = {
  key: 3,
  class: "form-control vu-time-picker__display",
  disabled: ""
}, Im = {
  key: 4,
  class: "form-control-helper-text"
}, Cm = {
  name: "vu-time-picker",
  inheritAttrs: false,
  mixins: [Fe, ze, $e, Ee],
  props: {
    useNativeInput: {
      type: Boolean,
      required: false,
      default: false
    }
  },
  emits: ["update:modelValue"],
  data() {
    return {
      minutes: "00",
      hours: "00",
      isPopoverOpen: false
    };
  },
  watch: {
    modelValue(e) {
      const [n, t] = this.splitTime(e);
      this.hours = n, this.minutes = t;
    },
    minutes(e) {
      this.$emit("update:modelValue", `${this.hours}:${e}`);
    },
    hours(e) {
      this.$emit("update:modelValue", `${e}:${this.minutes}`);
    }
  },
  beforeMount() {
    const [e, n] = this.splitTime(this.modelValue);
    this.hours = e, this.minutes = n;
  },
  methods: {
    splitTime(e) {
      return e.split(":");
    },
    formatNumberForTime(e) {
      return e < 10 ? `0${e}` : `${e}`;
    }
  },
  components: { VuPopover: Ge, VuPopover: Ge }
}, Bm = /* @__PURE__ */ Object.assign(Cm, {
  setup(e) {
    const n = inject("vuInputComposition", false), t = inject(Yt, false);
    return (i, o) => (openBlock(), createElementBlock("div", {
      class: normalizeClass(["vu-time-picker form-group", i.classes])
    }, [
      i.label.length ? (openBlock(), createElementBlock("label", fm, [
        createTextVNode(toDisplayString(i.label) + " ", 1),
        i.required ? (openBlock(), createElementBlock("span", pm, " *")) : createCommentVNode("", true)
      ])) : createCommentVNode("", true),
      e.useNativeInput || unref(t) ? (openBlock(), createElementBlock("div", mm, [
        createBaseVNode("input", mergeProps(i.$attrs, {
          value: i.value,
          placeholder: i.placeholder,
          disabled: i.disabled,
          type: "time",
          class: "vu-time-picker__display-native form-control",
          style: { width: "fit-content" },
          onInput: o[0] || (o[0] = ({ target: s }) => {
            unref(n) || (s.composing = false), i.$emit("update:modelValue", s.value);
          })
        }), null, 16, vm)
      ])) : i.disabled ? (openBlock(), createElementBlock("div", Sm, [
        createBaseVNode("span", null, toDisplayString(i.hours), 1),
        createTextVNode(":"),
        createBaseVNode("span", null, toDisplayString(i.minutes), 1)
      ])) : (openBlock(), createBlock(Ge, {
        key: 2,
        class: "vu-time-picker__popover",
        style: { width: "fit-content" },
        show: i.isPopoverOpen
      }, {
        body: withCtx(() => [
          createBaseVNode("div", bm, [
            createBaseVNode("div", ym, [
              (openBlock(true), createElementBlock(Fragment, null, renderList([...Array(24).keys()], (s) => (openBlock(), createElementBlock("label", {
                key: s,
                class: normalizeClass({ "vu-time-picker__hours--selected": i.hours === i.formatNumberForTime(s) })
              }, [
                createBaseVNode("span", null, toDisplayString(i.formatNumberForTime(s)), 1),
                withDirectives(createBaseVNode("input", {
                  "onUpdate:modelValue": o[1] || (o[1] = (a) => i.hours = a),
                  type: "radio",
                  name: "hours",
                  value: i.formatNumberForTime(s)
                }, null, 8, _m), [
                  [vModelRadio, i.hours]
                ])
              ], 2))), 128))
            ]),
            createBaseVNode("div", wm, [
              (openBlock(true), createElementBlock(Fragment, null, renderList([...Array(60).keys()], (s) => (openBlock(), createElementBlock("label", {
                key: s,
                class: normalizeClass({ "vu-time-picker__minutes--selected": i.minutes === i.formatNumberForTime(s) })
              }, [
                createBaseVNode("span", null, toDisplayString(i.formatNumberForTime(s)), 1),
                withDirectives(createBaseVNode("input", {
                  "onUpdate:modelValue": o[2] || (o[2] = (a) => i.minutes = a),
                  type: "radio",
                  name: "minutes",
                  value: i.formatNumberForTime(s)
                }, null, 8, km), [
                  [vModelRadio, i.minutes]
                ])
              ], 2))), 128))
            ])
          ])
        ]),
        default: withCtx(() => [
          createBaseVNode("div", gm, [
            createBaseVNode("span", null, toDisplayString(i.hours), 1),
            createTextVNode(":"),
            createBaseVNode("span", null, toDisplayString(i.minutes), 1)
          ])
        ]),
        _: 1
      }, 8, ["show"])),
      (openBlock(true), createElementBlock(Fragment, null, renderList(i.errorBucket, (s, a) => (openBlock(), createElementBlock("span", {
        key: `${a}-error-${s}`,
        style: { display: "block" },
        class: "form-control-error-text"
      }, toDisplayString(s), 1))), 128)),
      i.helper.length ? (openBlock(), createElementBlock("span", Im, toDisplayString(i.helper), 1)) : createCommentVNode("", true)
    ], 2));
  }
}), $m = /* @__PURE__ */ F(Bm, [["__scopeId", "data-v-2ece058d"]]), Om = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $m
}, Symbol.toStringTag, { value: "Module" }));
function Tm(e, n) {
  let t;
  for (let i = 0; i < n; i++)
    t = (e == null ? void 0 : e.parentElement) || t;
  return t ?? document.documentElement;
}
function xm(e) {
  const {
    enabled: n = true,
    el: t,
    placeholder: i,
    class: o = "",
    ancestor: s = 0
  } = e || {}, a = ref(false), u = ref(), c = ref();
  let d = ref([]), h2 = ref(/* @__PURE__ */ new WeakMap());
  const p2 = ref(0), _ = computed(() => d.value.map((D) => h2.value.get(D)).reduce((D, P) => D || P, false)), v = computed(
    () => a.value === false && _.value
  );
  watch(v, (Z, D) => {
    s && y();
  }, { flush: "sync" }), watch(v, (Z, D) => {
    L(!!s);
  }, { flush: "post" });
  const y = () => {
    if (u.value) {
      const Z = toValue$3(i);
      Z && u.value.unobserve(Z);
    }
  }, L = async (Z) => {
    if (u.value) {
      Z && await nextTick();
      const D = toValue$3(i);
      D && u.value.observe(D);
    }
  }, B = (Z) => {
    let D = Z[0].intersectionRatio;
    s ? v.value ? (a.value = D > p2.value, p2.value = D) : a.value = D === 1 : a.value = v.value ? D > 0 : D === 1;
  }, R = () => {
    if (n) {
      const Z = toValue$3(t);
      if (d.value = [], !Z)
        return;
      c.value = new IntersectionObserver(
        (P) => {
          P.forEach(({ intersectionRatio: Q, target: z }) => {
            h2.value.set(z, Q > 0);
          });
        }
      );
      let { nextElementSibling: D } = s ? Tm(Z, s) : Z;
      if (s === 0)
        for (; D && (D == null ? void 0 : D.className.indexOf(o)) === -1; )
          d.value.push(D), c.value.observe(D), D = D == null ? void 0 : D.nextElementSibling;
      else if (D)
        for (; D && D.querySelectorAll(`.${o}`).length === 0; )
          d.value.push(D), c.value.observe(D), D = D == null ? void 0 : D.nextElementSibling;
      u.value = new IntersectionObserver(
        B,
        {
          threshold: 1
        }
      ), L(v.value);
    }
  }, ee = async () => {
    ce(), R();
  }, ce = () => {
    c.value && c.value.disconnect(), u.value && u.value.disconnect(), delete c.value, delete u.value;
  };
  return onMounted(() => {
    R();
  }), onUnmounted(() => {
    ce();
  }), { stick: v, refresh: ee };
}
const Ri = (e) => (pushScopeId("data-v-1de1d51d"), e = e(), popScopeId(), e), Mm = /* @__PURE__ */ Ri(() => /* @__PURE__ */ createBaseVNode("hr", null, null, -1)), Vm = { class: "vu-timeline-divider-date__date" }, Pm = /* @__PURE__ */ Ri(() => /* @__PURE__ */ createBaseVNode("hr", null, null, -1)), Lm = {
  name: "vu-timeline-divider"
}, Dm = /* @__PURE__ */ defineComponent({
  ...Lm,
  props: {
    date: {},
    label: {},
    sticky: { type: Boolean },
    forceStick: { type: Boolean }
  },
  setup(e) {
    const n = inject("lang"), t = inject(di, 0), i = inject(ci, void 0), o = ref(null), s = ref(null), a = e, { stick: u } = xm({ enabled: a.sticky, el: o, placeholder: s, class: "vu-timeline-divider-date", ancestor: t }), d = (h2) => {
      const p2 = new Date(h2), _ = p2.getFullYear(), v = (/* @__PURE__ */ new Date()).getFullYear(), y = _ === v ? { weekday: "long", month: "long", day: "numeric" } : {
        weekday: "long",
        month: "long",
        day: "numeric",
        year: "numeric"
      };
      return p2.toLocaleDateString(n, y);
    };
    return (h2, p2) => (openBlock(), createElementBlock(Fragment, null, [
      createBaseVNode("div", {
        class: normalizeClass(["vu-timeline-divider__placeholder", { "vu-timeline-divider__detached": unref(u) && unref(t), "vu-timeline-divider--hidden": unref(u) && unref(t) }]),
        ref_key: "placeholder",
        ref: s
      }, null, 2),
      (openBlock(), createBlock(Teleport, {
        to: unref(i),
        disabled: !unref(i) || !unref(u)
      }, [
        createBaseVNode("div", {
          class: normalizeClass(["vu-timeline-divider-date", [
            { "vu-timeline-divider-date--top": unref(u) || a.forceStick },
            unref(t) && (unref(u) || a.forceStick) && unref(i) && "absolute" || (unref(u) || a.forceStick) && "sticky"
          ]]),
          ref_key: "el",
          ref: o
        }, [
          Mm,
          createBaseVNode("div", Vm, toDisplayString(h2.label || d(h2.date)), 1),
          Pm
        ], 2)
      ], 8, ["to", "disabled"]))
    ], 64));
  }
}), Am = /* @__PURE__ */ F(Dm, [["__scopeId", "data-v-1de1d51d"]]), Fm = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Am
}, Symbol.toStringTag, { value: "Module" })), zm = (e) => {
  try {
    const { label: n, id: t } = e;
    if (n && t)
      return true;
  } catch {
  }
  return false;
}, Em = {
  name: "vu-tree-view-item",
  mixins: [vt],
  emits: ["load-complete", "click", "expand", "select"],
  props: {
    selected: {
      type: Array,
      default: () => []
    },
    expanded: {
      type: Array,
      default: () => []
    },
    loading: {
      type: Array,
      default: () => []
    },
    depth: {
      type: Number,
      default: () => 0
    },
    hover: {
      type: Boolean,
      default: false
    },
    siblingsHaveNoType: {
      type: Boolean,
      default: false
    },
    item: {
      type: Object,
      validator: zm,
      required: true
    },
    main: {
      type: Boolean,
      default: false
    },
    leftPadding: {
      type: Number,
      default: 0
    }
  },
  inject: {
    vuTreeViewLazy: {
      default: false
    },
    vuTreeViewLeftPadBase: {
      default: 38
    },
    vuTreeViewLeftPadFunc: {
      type: Function,
      default: void 0
    },
    vuTreeViewLeftPadReduce: {
      type: Boolean,
      default: false
    },
    vuTreeIcon: {
      type: String,
      default: "expand"
    }
  },
  data: () => ({
    guid: Re
  }),
  watch: {
    item: {
      deep: true,
      handler(e) {
        this.isLoading && this.$emit("load-complete", e);
      }
    }
  },
  created() {
    this.item.expanded && !this.isExpanded && this.$emit("expand", this.item), this.item.selected && !this.isSelected && this.$emit("select", this.item);
  },
  computed: {
    otherSlots() {
      return Object.fromEntries(this.$slots.filter((e) => e.startsWith("item-")));
    },
    showTreeIcon() {
      return (
        // eslint-disable-next-line operator-linebreak
        this.hasItems || this.vuTreeViewLazy && !this.item.leaf && this.item.items === void 0 && !this.isLoading
      );
    },
    hasItems() {
      return this.item.items && this.item.items.length > 0;
    },
    isSelected() {
      return this.selected.includes(this.item);
    },
    isExpanded() {
      return this.expanded.includes(this.item);
    },
    isLoading() {
      return this.vuTreeViewLazy && this.loading.includes(this.item);
    },
    anyChildrenHasIcon() {
      return this.hasItems && this.item.items.some((e) => e.icon !== void 0);
    },
    getTreeIconClass() {
      return this.isExpanded ? `${this.vuTreeIcon}-down` : `${this.vuTreeIcon}-right`;
    },
    calcLeftPadding() {
      return this.vuTreeViewLeftPadFunc ? this.vuTreeViewLeftPadFunc(this.depth, this.leftPadding) : this.depth ? this.vuTreeViewLeftPadReduce ? Math.max(this.leftPadding + this.vuTreeViewLeftPadBase - 6 * this.depth, this.leftPadding + 6) : this.leftPadding + this.vuTreeViewLeftPadBase : 0;
    }
  },
  methods: {
    onClick(e) {
      var t, i;
      [(t = this.$refs.loadingSpinner) == null ? void 0 : t.$el, (i = this.$refs.treeIcon) == null ? void 0 : i.$el].filter((o) => o).every((o) => !o.contains(e.target)) && this.$emit("select", this.item);
    }
  },
  components: { VuIconBtn: de }
}, Nm = (e) => (pushScopeId("data-v-222dea3e"), e = e(), popScopeId(), e), Rm = {
  key: 1,
  class: "vu-tree-view-item__tree-icon-loading",
  ref: "loadingSpinner"
}, Hm = /* @__PURE__ */ Nm(() => /* @__PURE__ */ createBaseVNode("svg", {
  class: "vu-spin",
  viewBox: "25 25 50 50"
}, [
  /* @__PURE__ */ createBaseVNode("circle", {
    class: "path",
    cx: "50",
    cy: "50",
    r: "20",
    fill: "none",
    stroke: "currentColor",
    "stroke-width": "5",
    "stroke-miterlimit": "10"
  })
], -1)), jm = {
  key: 2,
  class: "vu-tree-view-item__tree-icon-placeholder"
}, Um = {
  key: 4,
  class: "vu-tree-view-item__type-icon-placeholder"
}, Wm = { class: "vu-tree-view-item__label" };
function qm(e, n, t, i, o, s) {
  const a = resolveComponent("VuIconBtn"), u = resolveComponent("VuTreeViewItem", true), c = resolveDirective("tooltip");
  return openBlock(), createElementBlock(Fragment, null, [
    createBaseVNode("div", {
      class: normalizeClass(["vu-tree-view-item", {
        "vu-tree-view-item--selected": s.isSelected,
        "vu-tree-view-item--unselected": !s.isSelected,
        "vu-tree-view-item--main": t.main,
        "vu-tree-view-item--child": !t.main,
        "vu-tree-view-item--chevron-icon": s.vuTreeIcon === "chevron"
      }]),
      style: normalizeStyle({
        "padding-left": `${s.calcLeftPadding}px`
      }),
      onClick: n[1] || (n[1] = (...d) => s.onClick && s.onClick(...d))
    }, [
      s.showTreeIcon ? (openBlock(), createBlock(a, {
        key: 0,
        icon: s.getTreeIconClass,
        class: "vu-tree-view-item__tree-icon",
        onClick: n[0] || (n[0] = (d) => e.$emit("expand", t.item)),
        ref: "treeIcon"
      }, null, 8, ["icon"])) : s.isLoading ? (openBlock(), createElementBlock("div", Rm, [
        renderSlot(e.$slots, "itemLoading", {}, () => [
          Hm
        ], true)
      ], 512)) : (openBlock(), createElementBlock("div", jm)),
      t.item.icon ? (openBlock(), createBlock(a, {
        key: 3,
        class: "vu-tree-view-item__type-icon",
        color: "default-inactive",
        icon: t.item.icon
      }, null, 8, ["icon"])) : t.siblingsHaveNoType ? (openBlock(), createElementBlock("div", Um)) : createCommentVNode("", true),
      renderSlot(e.$slots, "item-" + t.item.type || "default", {}, () => [
        withDirectives((openBlock(), createElementBlock("div", Wm, [
          createTextVNode(toDisplayString(t.item.label), 1)
        ])), [
          [
            c,
            t.item.label,
            void 0,
            { ellipsis: true }
          ]
        ])
      ], true)
    ], 6),
    s.hasItems && s.isExpanded ? (openBlock(true), createElementBlock(Fragment, { key: 0 }, renderList(t.item.items, (d) => (openBlock(), createBlock(u, {
      key: `${d.id}`,
      item: d,
      depth: t.depth + 1,
      "left-padding": s.calcLeftPadding,
      selected: t.selected,
      loading: t.loading,
      expanded: t.expanded,
      "siblings-have-no-type": s.anyChildrenHasIcon,
      onLoadComplete: n[2] || (n[2] = (h2) => e.$emit("load-complete", h2)),
      onExpand: n[3] || (n[3] = (h2) => e.$emit("expand", h2)),
      onSelect: n[4] || (n[4] = (h2) => e.$emit("select", h2))
    }, null, 8, ["item", "depth", "left-padding", "selected", "loading", "expanded", "siblings-have-no-type"]))), 128)) : createCommentVNode("", true)
  ], 64);
}
const An = /* @__PURE__ */ F(Em, [["render", qm], ["__scopeId", "data-v-222dea3e"]]), Km = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: An
}, Symbol.toStringTag, { value: "Module" })), Gm = {
  name: "vu-tree-view",
  emits: ["update:selected", "update:expanded", "fetch", "item-click", "update:loading"],
  props: {
    selected: {
      type: Array,
      default: () => []
    },
    expanded: {
      type: Array,
      default: () => []
    },
    loading: {
      type: Array,
      required: false,
      default: void 0
    },
    items: {
      type: Array,
      required: true
    },
    exclusive: {
      type: Boolean,
      default: true
    },
    firstLevelBigger: {
      type: Boolean,
      default: false
    }
  },
  data: () => ({
    innerLoading: []
  }),
  methods: {
    toggleSelect(e) {
      if (this.selected.includes(e)) {
        const n = this.expanded.slice();
        n.splice(n.indexOf(e), 1), this.$emit("update:selected", n);
      } else
        this.exclusive ? this.$emit("update:selected", [e]) : this.$emit("update:selected", [e].concat(this.expanded || []));
    },
    toggleExpand(e) {
      const n = this.expanded.slice();
      this.expanded.includes(e) ? (n.splice(n.indexOf(e), 1), this.$emit("update:expanded", n)) : e.items === void 0 ? (this.$emit("fetch", e), this.loading === void 0 ? this.innerLoading.push(e) : this.$emit("update:loading", [e].concat(this.loading || []))) : (n.push(e), this.$emit("update:expanded", n));
    },
    onLoad(e) {
      this.loading === void 0 && this.innerLoading.splice(this.innerLoading.indexOf(e)), e.items && e.items.length > 0 && !e.leaf && this.$emit("update:expanded", [e].concat(this.expanded || []));
    }
  },
  components: { VuTreeViewItem: An, VuScroller: Qe, VuTreeViewItem: An }
}, Ym = { class: "vu-tree-view" };
function Xm(e, n, t, i, o, s) {
  const a = resolveComponent("VuTreeViewItem"), u = resolveComponent("VuScroller");
  return openBlock(), createElementBlock("div", Ym, [
    createVNode(u, null, {
      default: withCtx(() => [
        (openBlock(true), createElementBlock(Fragment, null, renderList(t.items, (c) => (openBlock(), createBlock(a, {
          key: `${c.id}`,
          item: c,
          loading: t.loading || e.innerLoading,
          expanded: t.expanded,
          selected: t.selected,
          main: t.firstLevelBigger,
          onExpand: s.toggleExpand,
          onSelect: s.toggleSelect,
          onLoadComplete: s.onLoad
        }, null, 8, ["item", "loading", "expanded", "selected", "main", "onExpand", "onSelect", "onLoadComplete"]))), 128))
      ]),
      _: 1
    })
  ]);
}
const Jm = /* @__PURE__ */ F(Gm, [["render", Xm]]), Zm = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Jm
}, Symbol.toStringTag, { value: "Module" })), Ut = "__v-click-outside", Hi = typeof window < "u", Qm = typeof navigator < "u", ev = Hi && ("ontouchstart" in window || Qm && navigator.msMaxTouchPoints > 0), tv = ev ? ["touchstart"] : ["click", "contextmenu"];
function nv(e) {
  const n = typeof e == "function";
  if (!n && typeof e != "object")
    throw new Error("v-click-outside: Binding value must be a function or an object");
  return {
    handler: n ? e : e.handler,
    middleware: e.middleware || ((t) => t),
    events: e.events || tv,
    innerShow: e.innerShow !== false
  };
}
function sv({
  el: e,
  event: n,
  handler: t,
  middleware: i
}) {
  const o = n.path || n.composedPath && n.composedPath(), s = o ? !o.includes(e) : !e.contains(n.target);
  n.target !== e && s && i(n) && t(n);
}
function ji(e, { value: n }) {
  const {
    events: t,
    handler: i,
    middleware: o,
    innerShow: s
  } = nv(n);
  s && (e[Ut] = t.map((a) => ({
    event: a,
    handler: (u) => sv({
      event: u,
      el: e,
      handler: i,
      middleware: o
    })
  })), e[Ut].forEach(({ event: a, handler: u }) => setTimeout(() => {
    e[Ut] && document.documentElement.addEventListener(a, u, false);
  }, 0)));
}
function Ui(e) {
  (e[Ut] || []).forEach(({ event: t, handler: i }) => document.documentElement.removeEventListener(t, i, false)), delete e[Ut];
}
function iv(e, { value: n, oldValue: t }) {
  JSON.stringify(n) !== JSON.stringify(t) && (Ui(e), ji(e, { value: n }));
}
const ov = {
  beforeMount: ji,
  updated: iv,
  beforeUnmount: Ui
}, Fn = Hi ? ov : {}, lv = {
  viewAll: "View all",
  contactsInCommon: "### contact$(s) in common",
  profile: "See full profile",
  message: "Start conversation",
  network: "Add user to my network",
  audio: "Add audio",
  conferencing: "Add video",
  screenshare: "Share screen",
  FR: "France",
  BR: "Brazil",
  CN: "China",
  DE: "Germany",
  ES: "Spain",
  GB: "United-Kingdom",
  HU: "Hungary",
  IT: "Italy",
  JP: "Japan",
  PL: "Poland",
  PT: "Portugal",
  RU: "Russia",
  SE: "Sweden",
  TR: "Turkey"
}, av = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#73AF00;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<path style="fill:#FFE15A;" d="M251.41,135.209L65.354,248.46c-5.651,3.439-5.651,11.641,0,15.081L251.41,376.793  c2.819,1.716,6.36,1.716,9.18,0l186.057-113.251c5.651-3.439,5.651-11.641,0-15.081L260.59,135.209  C257.771,133.493,254.229,133.493,251.41,135.209z"/>\r
<circle style="fill:#41479B;" cx="256" cy="256.001" r="70.62"/>\r
<g>\r
	<path style="fill:#F5F5F5;" d="M195.401,219.874c-3.332,5.578-5.905,11.64-7.605,18.077c39.149-2.946,97.062,8.006,133.922,43.773   c2.406-6.141,3.994-12.683,4.59-19.522C288.247,230.169,235.628,218.778,195.401,219.874z"/>\r
	<path style="fill:#F5F5F5;" d="M258.925,280.1l1.88,5.638l5.943,0.046c0.769,0.006,1.088,0.988,0.47,1.445l-4.781,3.531   l1.793,5.666c0.232,0.734-0.604,1.341-1.229,0.893l-4.835-3.456l-4.835,3.456c-0.626,0.448-1.461-0.159-1.229-0.893l1.793-5.666   l-4.781-3.531c-0.619-0.457-0.3-1.439,0.469-1.445l5.943-0.046l1.88-5.638C257.649,279.37,258.681,279.37,258.925,280.1z"/>\r
	<path style="fill:#F5F5F5;" d="M282.024,294.685l0.809,2.426l2.558,0.02c0.331,0.002,0.469,0.425,0.202,0.622l-2.058,1.519   l0.771,2.439c0.1,0.316-0.259,0.577-0.529,0.384l-2.081-1.487l-2.081,1.487c-0.269,0.193-0.629-0.068-0.529-0.384l0.771-2.439   l-2.058-1.519c-0.266-0.196-0.129-0.619,0.202-0.622l2.558-0.02l0.809-2.426C281.474,294.37,281.919,294.37,282.024,294.685z"/>\r
	<path style="fill:#F5F5F5;" d="M248.938,269.39l0.809,2.426l2.558,0.02c0.331,0.002,0.469,0.425,0.202,0.622l-2.058,1.519   l0.771,2.439c0.1,0.316-0.259,0.577-0.529,0.384l-2.081-1.487l-2.081,1.487c-0.269,0.193-0.629-0.068-0.529-0.384l0.771-2.439   l-2.058-1.519c-0.266-0.196-0.129-0.619,0.202-0.622l2.558-0.02l0.809-2.426C248.388,269.076,248.833,269.076,248.938,269.39z"/>\r
	<path style="fill:#F5F5F5;" d="M204.13,266.448l0.809,2.426l2.558,0.02c0.331,0.002,0.469,0.425,0.202,0.622l-2.058,1.519   l0.771,2.439c0.1,0.316-0.259,0.577-0.529,0.384l-2.081-1.487l-2.081,1.487c-0.269,0.193-0.629-0.068-0.529-0.384l0.771-2.439   l-2.058-1.519c-0.266-0.196-0.129-0.619,0.202-0.622l2.558-0.02l0.809-2.426C203.581,266.134,204.025,266.134,204.13,266.448z"/>\r
	<path style="fill:#F5F5F5;" d="M241.614,293.847l0.809,2.426l2.558,0.02c0.331,0.002,0.469,0.425,0.202,0.622l-2.058,1.519   l0.771,2.439c0.1,0.316-0.259,0.577-0.529,0.384l-2.081-1.487l-2.081,1.487c-0.269,0.193-0.629-0.068-0.529-0.384l0.771-2.439   l-2.058-1.519c-0.266-0.196-0.129-0.619,0.202-0.622l2.558-0.02l0.809-2.426C241.065,293.534,241.51,293.534,241.614,293.847z"/>\r
	<path style="fill:#F5F5F5;" d="M220.99,264.755l0.662,1.984l2.092,0.017c0.27,0.002,0.383,0.348,0.166,0.509l-1.683,1.242   l0.631,1.994c0.082,0.258-0.212,0.472-0.433,0.314l-1.702-1.216l-1.702,1.216c-0.221,0.158-0.514-0.056-0.433-0.314l0.631-1.994   l-1.683-1.242c-0.217-0.161-0.106-0.507,0.166-0.509l2.092-0.017l0.662-1.984C220.541,264.498,220.904,264.498,220.99,264.755z"/>\r
	<path style="fill:#F5F5F5;" d="M283.819,223.794l0.828,2.482l2.616,0.02c0.339,0.002,0.479,0.435,0.206,0.636l-2.104,1.554   l0.789,2.495c0.103,0.323-0.266,0.59-0.541,0.393l-2.129-1.522l-2.129,1.522c-0.276,0.198-0.643-0.071-0.541-0.393l0.789-2.495   l-2.104-1.554c-0.273-0.201-0.132-0.633,0.206-0.636l2.616-0.02l0.828-2.482C283.257,223.472,283.712,223.472,283.819,223.794z"/>\r
	<path style="fill:#F5F5F5;" d="M207.012,252.617l0.662,1.984l2.092,0.017c0.27,0.002,0.383,0.348,0.166,0.509l-1.683,1.242   l0.631,1.994c0.082,0.258-0.212,0.472-0.433,0.314l-1.702-1.216l-1.702,1.216c-0.221,0.158-0.514-0.056-0.433-0.314l0.631-1.994   l-1.683-1.242c-0.217-0.161-0.106-0.506,0.166-0.509l2.092-0.017l0.662-1.984C206.563,252.36,206.926,252.36,207.012,252.617z"/>\r
	<path style="fill:#F5F5F5;" d="M217.112,280.581l1.002,3.006l3.168,0.024c0.41,0.003,0.58,0.526,0.25,0.77l-2.549,1.882l0.956,3.02   c0.124,0.391-0.321,0.715-0.655,0.476l-2.578-1.842l-2.578,1.842c-0.333,0.238-0.779-0.085-0.655-0.476l0.956-3.02l-2.549-1.882   c-0.33-0.244-0.16-0.767,0.25-0.77l3.168-0.024l1.002-3.006C216.433,280.193,216.983,280.193,217.112,280.581z"/>\r
	<path style="fill:#F5F5F5;" d="M294.903,295.315l0.63,1.891l1.993,0.015c0.258,0.002,0.365,0.331,0.158,0.484l-1.603,1.184   l0.601,1.9c0.078,0.246-0.202,0.449-0.413,0.299l-1.621-1.159l-1.622,1.159c-0.21,0.15-0.49-0.053-0.413-0.299l0.601-1.9   l-1.603-1.184c-0.207-0.153-0.1-0.482,0.158-0.484l1.993-0.015l0.63-1.891C294.475,295.07,294.822,295.07,294.903,295.315z"/>\r
	<path style="fill:#F5F5F5;" d="M301.877,280.885l0.809,2.426l2.558,0.02c0.331,0.002,0.469,0.425,0.202,0.622l-2.058,1.519   l0.771,2.439c0.1,0.316-0.259,0.577-0.529,0.384l-2.081-1.487l-2.081,1.487c-0.269,0.193-0.629-0.068-0.529-0.384l0.771-2.439   l-2.058-1.519c-0.266-0.196-0.129-0.619,0.202-0.622l2.558-0.02l0.809-2.426C301.327,280.57,301.772,280.57,301.877,280.885z"/>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, rv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#FF4B55;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<g>\r
	<path style="fill:#FFE15A;" d="M85.007,140.733l8.416,25.234l26.6,0.206c3.444,0.026,4.872,4.422,2.101,6.467l-21.398,15.801   l8.023,25.362c1.038,3.284-2.7,5.999-5.502,3.997l-21.64-15.469l-21.64,15.468c-2.802,2.003-6.54-0.714-5.502-3.997l8.023-25.362   l-21.398-15.8c-2.771-2.046-1.343-6.441,2.101-6.467l26.6-0.206l8.416-25.234C79.297,137.465,83.918,137.465,85.007,140.733z"/>\r
	<path style="fill:#FFE15A;" d="M181.599,146.951l6.035,8.23l9.739-3.046c1.261-0.394,2.298,1.044,1.526,2.115l-5.962,8.281   l5.906,8.321c0.765,1.077-0.282,2.508-1.54,2.105l-9.719-3.111l-6.089,8.189c-0.788,1.06-2.473,0.506-2.478-0.814l-0.045-10.205   l-9.67-3.261c-1.251-0.423-1.246-2.195,0.009-2.609l9.69-3.196l0.114-10.204C179.129,146.427,180.818,145.886,181.599,146.951z"/>\r
	<path style="fill:#FFE15A;" d="M144.857,122.421l10.145,1.102l4.328-9.241c0.561-1.196,2.321-0.991,2.591,0.302l2.086,9.988   l10.126,1.26c1.311,0.163,1.66,1.901,0.513,2.558l-8.855,5.07l1.931,10.02c0.25,1.298-1.295,2.166-2.274,1.279l-7.559-6.855   l-8.932,4.932c-1.156,0.639-2.461-0.563-1.919-1.768l4.183-9.308l-7.452-6.972C142.805,123.89,143.544,122.279,144.857,122.421z"/>\r
	<path style="fill:#FFE15A;" d="M160.895,221.314l-6.035,8.23l-9.739-3.046c-1.261-0.394-2.298,1.044-1.526,2.115l5.962,8.281   l-5.906,8.321c-0.765,1.077,0.282,2.508,1.54,2.105l9.719-3.111l6.089,8.189c0.788,1.06,2.473,0.506,2.478-0.814l0.045-10.205   l9.67-3.261c1.252-0.423,1.246-2.195-0.009-2.609l-9.69-3.196l-0.114-10.204C163.363,220.791,161.676,220.248,160.895,221.314z"/>\r
	<path style="fill:#FFE15A;" d="M197.635,198.263l-10.145,1.102l-4.328-9.241c-0.561-1.196-2.321-0.991-2.591,0.302l-2.087,9.988   l-10.126,1.26c-1.311,0.163-1.66,1.901-0.513,2.558l8.855,5.07l-1.931,10.02c-0.25,1.298,1.295,2.166,2.274,1.279l7.559-6.855   l8.932,4.932c1.156,0.639,2.461-0.563,1.919-1.768l-4.183-9.308l7.452-6.972C199.689,199.732,198.95,198.121,197.635,198.263z"/>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, uv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#464655;" d="M512,200.093H0V97.104c0-4.875,3.953-8.828,8.828-8.828h494.345c4.875,0,8.828,3.953,8.828,8.828  L512,200.093L512,200.093z"/>\r
<path style="fill:#FFE15A;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V311.909h512v102.988  C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<rect y="200.091" style="fill:#FF4B55;" width="512" height="111.81"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, dv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#C8414B;" d="M8.828,423.725h494.345c4.875,0,8.828-3.953,8.828-8.828V97.104c0-4.875-3.953-8.828-8.828-8.828  H8.828C3.953,88.277,0,92.229,0,97.104v317.793C0,419.773,3.953,423.725,8.828,423.725z"/>\r
<rect y="158.901" style="fill:#FFD250;" width="512" height="194.21"/>\r
<path style="fill:#C8414B;" d="M216.276,256.001l7.485-33.681c0.69-3.102-1.671-6.044-4.849-6.044h-5.272  c-3.177,0-5.537,2.942-4.849,6.044L216.276,256.001z"/>\r
<rect x="207.45" y="238.341" style="fill:#F5F5F5;" width="17.655" height="75.03"/>\r
<rect x="203.03" y="229.521" style="fill:#FAB446;" width="26.483" height="8.828"/>\r
<g>\r
	<rect x="185.38" y="256.001" style="fill:#C8414B;" width="44.14" height="8.828"/>\r
	<polygon style="fill:#C8414B;" points="229.517,291.311 203.034,282.484 203.034,273.656 229.517,282.484  "/>\r
	<path style="fill:#C8414B;" d="M83.862,256.001l7.485-33.681c0.69-3.102-1.671-6.044-4.849-6.044h-5.272   c-3.177,0-5.537,2.942-4.849,6.044L83.862,256.001z"/>\r
</g>\r
<path style="fill:#F5F5F5;" d="M114.759,229.518c-4.875,0-8.828,3.953-8.828,8.828v57.379c0,10.725,10.01,30.897,44.138,30.897  s44.138-20.171,44.138-30.897v-57.379c0-4.875-3.953-8.828-8.828-8.828H114.759z"/>\r
<g>\r
	<path style="fill:#C8414B;" d="M150.069,273.656h-44.138v-35.31c0-4.875,3.953-8.828,8.828-8.828h35.31V273.656z"/>\r
	<path style="fill:#C8414B;" d="M150.069,273.656h44.138v22.069c0,12.189-9.88,22.069-22.069,22.069l0,0   c-12.189,0-22.069-9.88-22.069-22.069V273.656z"/>\r
</g>\r
<path style="fill:#FAB446;" d="M105.931,273.656h44.138v22.069c0,12.189-9.88,22.069-22.069,22.069l0,0  c-12.189,0-22.069-9.88-22.069-22.069V273.656z"/>\r
<g>\r
	<path style="fill:#C8414B;" d="M141.241,313.281v-39.625h-8.828v43.693C135.697,316.683,138.664,315.229,141.241,313.281z"/>\r
	<path style="fill:#C8414B;" d="M123.586,317.349v-43.693h-8.828v39.625C117.336,315.229,120.303,316.683,123.586,317.349z"/>\r
</g>\r
<rect x="114.76" y="256.001" style="fill:#FFB441;" width="26.483" height="8.828"/>\r
<g>\r
	<rect x="114.76" y="238.341" style="fill:#FAB446;" width="26.483" height="8.828"/>\r
	<rect x="119.17" y="243.591" style="fill:#FAB446;" width="17.655" height="15.992"/>\r
</g>\r
<rect x="75.03" y="238.341" style="fill:#F5F5F5;" width="17.655" height="75.03"/>\r
<g>\r
	<rect x="70.62" y="308.971" style="fill:#FAB446;" width="26.483" height="8.828"/>\r
	<rect x="70.62" y="229.521" style="fill:#FAB446;" width="26.483" height="8.828"/>\r
</g>\r
<rect x="66.21" y="317.791" style="fill:#5064AA;" width="35.31" height="8.828"/>\r
<rect x="207.45" y="308.971" style="fill:#FAB446;" width="26.483" height="8.828"/>\r
<rect x="198.62" y="317.791" style="fill:#5064AA;" width="35.31" height="8.828"/>\r
<rect x="123.59" y="220.691" style="fill:#FAB446;" width="52.966" height="8.828"/>\r
<rect x="145.66" y="194.211" style="fill:#FFB441;" width="8.828" height="26.483"/>\r
<g>\r
	<path style="fill:#F5F5F5;" d="M141.241,207.449c-7.302,0-13.241-5.94-13.241-13.241c0-7.302,5.94-13.241,13.241-13.241   c7.302,0,13.241,5.94,13.241,13.241C154.483,201.509,148.543,207.449,141.241,207.449z M141.241,189.794   c-2.435,0-4.414,1.978-4.414,4.414c0,2.435,1.978,4.414,4.414,4.414s4.414-1.978,4.414-4.414   C145.655,191.773,143.677,189.794,141.241,189.794z"/>\r
	<path style="fill:#F5F5F5;" d="M158.897,207.449c-7.302,0-13.241-5.94-13.241-13.241c0-7.302,5.94-13.241,13.241-13.241   c7.302,0,13.241,5.94,13.241,13.241S166.198,207.449,158.897,207.449z M158.897,189.794c-2.435,0-4.414,1.978-4.414,4.414   c0,2.435,1.978,4.414,4.414,4.414c2.435,0,4.414-1.978,4.414-4.414C163.31,191.773,161.332,189.794,158.897,189.794z"/>\r
	<path style="fill:#F5F5F5;" d="M176.552,216.277c-7.302,0-13.241-5.94-13.241-13.241c0-7.302,5.94-13.241,13.241-13.241   c7.302,0,13.241,5.94,13.241,13.241S183.853,216.277,176.552,216.277z M176.552,198.622c-2.435,0-4.414,1.978-4.414,4.414   c0,2.435,1.978,4.414,4.414,4.414c2.435,0,4.414-1.978,4.414-4.414S178.987,198.622,176.552,198.622z"/>\r
	<path style="fill:#F5F5F5;" d="M123.586,216.277c-7.302,0-13.241-5.94-13.241-13.241c0-7.302,5.94-13.241,13.241-13.241   c7.302,0,13.241,5.94,13.241,13.241C136.828,210.337,130.888,216.277,123.586,216.277z M123.586,198.622   c-2.435,0-4.414,1.978-4.414,4.414c0,2.435,1.978,4.414,4.414,4.414s4.414-1.979,4.414-4.415   C128,200.6,126.022,198.622,123.586,198.622z"/>\r
</g>\r
<path style="fill:#FAB446;" d="M176.552,291.311v4.414c0,2.434-1.98,4.414-4.414,4.414s-4.414-1.98-4.414-4.414v-4.414H176.552   M185.379,282.484h-26.483v13.241c0,7.302,5.94,13.241,13.241,13.241c7.302,0,13.241-5.94,13.241-13.241v-13.241H185.379z"/>\r
<path style="fill:#FFA0D2;" d="M172.138,264.829L172.138,264.829c-4.875,0-8.828-3.953-8.828-8.828v-8.828  c0-4.875,3.953-8.828,8.828-8.828l0,0c4.875,0,8.828,3.953,8.828,8.828v8.828C180.966,260.876,177.013,264.829,172.138,264.829z"/>\r
<circle style="fill:#5064AA;" cx="150.07" cy="273.651" r="13.241"/>\r
<rect x="145.66" y="176.551" style="fill:#FAB446;" width="8.828" height="26.483"/>\r
<path style="fill:#C8414B;" d="M123.586,220.691l-8.828-8.828l5.171-5.171c7.993-7.993,18.835-12.484,30.14-12.484l0,0  c11.305,0,22.146,4.491,30.14,12.484l5.171,5.171l-8.828,8.828H123.586z"/>\r
<g>\r
	<circle style="fill:#FFD250;" cx="150.07" cy="211.861" r="4.414"/>\r
	<circle style="fill:#FFD250;" cx="132.41" cy="211.861" r="4.414"/>\r
	<circle style="fill:#FFD250;" cx="167.72" cy="211.861" r="4.414"/>\r
</g>\r
<g>\r
	<rect x="70.62" y="256.001" style="fill:#C8414B;" width="44.14" height="8.828"/>\r
	<polygon style="fill:#C8414B;" points="70.621,291.311 97.103,282.484 97.103,273.656 70.621,282.484  "/>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, cv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512 512" style="enable-background:new 0 0 512 512;" xml:space="preserve">\r
<path style="fill:#41479B;" d="M170.667,423.721H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.1c0-4.875,3.953-8.828,8.828-8.828  h161.839V423.721z"/>\r
<rect x="170.67" y="88.277" style="fill:#F5F5F5;" width="170.67" height="335.45"/>\r
<path style="fill:#FF4B55;" d="M503.172,423.721H341.333V88.273h161.839c4.875,0,8.828,3.953,8.828,8.828v317.793  C512,419.77,508.047,423.721,503.172,423.721z"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, hv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.002 512.002" style="enable-background:new 0 0 512.002 512.002;" xml:space="preserve">\r
<path style="fill:#41479B;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.772,508.047,423.725,503.172,423.725z"/>\r
<path style="fill:#F5F5F5;" d="M512,97.104c0-4.875-3.953-8.828-8.828-8.828h-39.495l-163.54,107.147V88.276h-88.276v107.147  L48.322,88.276H8.828C3.953,88.276,0,92.229,0,97.104v22.831l140.309,91.927H0v88.276h140.309L0,392.066v22.831  c0,4.875,3.953,8.828,8.828,8.828h39.495l163.54-107.147v107.147h88.276V316.578l163.54,107.147h39.495  c4.875,0,8.828-3.953,8.828-8.828v-22.831l-140.309-91.927H512v-88.276H371.691L512,119.935V97.104z"/>\r
<g>\r
	<polygon style="fill:#FF4B55;" points="512,229.518 282.483,229.518 282.483,88.276 229.517,88.276 229.517,229.518 0,229.518    0,282.483 229.517,282.483 229.517,423.725 282.483,423.725 282.483,282.483 512,282.483  "/>\r
	<path style="fill:#FF4B55;" d="M178.948,300.138L0.25,416.135c0.625,4.263,4.14,7.59,8.577,7.59h12.159l190.39-123.586h-32.428   V300.138z"/>\r
	<path style="fill:#FF4B55;" d="M346.388,300.138H313.96l190.113,123.404c4.431-0.472,7.928-4.09,7.928-8.646v-7.258   L346.388,300.138z"/>\r
	<path style="fill:#FF4B55;" d="M0,106.849l161.779,105.014h32.428L5.143,89.137C2.123,90.54,0,93.555,0,97.104V106.849z"/>\r
	<path style="fill:#FF4B55;" d="M332.566,211.863L511.693,95.586c-0.744-4.122-4.184-7.309-8.521-7.309h-12.647L300.138,211.863   H332.566z"/>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, fv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#FF4B55;" d="M512,200.093H0V97.104c0-4.875,3.953-8.828,8.828-8.828h494.345c4.875,0,8.828,3.953,8.828,8.828  L512,200.093L512,200.093z"/>\r
<path style="fill:#73AF00;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V311.909h512v102.988  C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<rect y="200.091" style="fill:#F5F5F5;" width="512" height="111.81"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, pv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512 512" style="enable-background:new 0 0 512 512;" xml:space="preserve">\r
<path style="fill:#73AF00;" d="M170.667,423.721H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.1c0-4.875,3.953-8.828,8.828-8.828  h161.839V423.721z"/>\r
<rect x="170.67" y="88.277" style="fill:#F5F5F5;" width="170.67" height="335.45"/>\r
<path style="fill:#FF4B55;" d="M503.172,423.721H341.333V88.273h161.839c4.875,0,8.828,3.953,8.828,8.828v317.793  C512,419.77,508.047,423.721,503.172,423.721z"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, mv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#F5F5F5;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<circle style="fill:#FF4B55;" cx="256" cy="256.001" r="97.1"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, vv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#FF4B55;" d="M0,256h512v158.897c0,4.875-3.953,8.828-8.828,8.828H8.828c-4.875,0-8.828-3.953-8.828-8.828V256z"/>\r
<path style="fill:#F5F5F5;" d="M512,256H0V97.103c0-4.875,3.953-8.828,8.828-8.828h494.345c4.875,0,8.828,3.953,8.828,8.828L512,256  L512,256z"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, gv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#FF4B55;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<path style="fill:#73AF00;" d="M185.379,88.277H8.828C3.953,88.277,0,92.229,0,97.104v317.793c0,4.875,3.953,8.828,8.828,8.828  H185.38V88.277H185.379z"/>\r
<circle style="fill:#FFE15A;" cx="185.45" cy="256.001" r="79.38"/>\r
<path style="fill:#FF4B55;" d="M211.932,229.518v35.31c0,14.603-11.88,26.483-26.483,26.483s-26.483-11.88-26.483-26.483v-35.31  H211.932 M220.759,211.863h-70.621c-4.875,0-8.828,3.953-8.828,8.828v44.138c0,24.376,19.762,44.138,44.138,44.138  s44.138-19.762,44.138-44.138v-44.138C229.587,215.816,225.634,211.863,220.759,211.863L220.759,211.863z"/>\r
<path style="fill:#F5F5F5;" d="M211.932,229.518v35.31c0,14.603-11.88,26.483-26.483,26.483s-26.483-11.88-26.483-26.483v-35.31  H211.932"/>\r
<g>\r
	<circle style="fill:#FFE15A;" cx="150.07" cy="220.691" r="4.414"/>\r
	<circle style="fill:#FFE15A;" cx="220.69" cy="220.691" r="4.414"/>\r
	<circle style="fill:#FFE15A;" cx="150.07" cy="256.001" r="4.414"/>\r
	<circle style="fill:#FFE15A;" cx="220.69" cy="256.001" r="4.414"/>\r
	<circle style="fill:#FFE15A;" cx="185.38" cy="220.691" r="4.414"/>\r
	<circle style="fill:#FFE15A;" cx="211.88" cy="288.551" r="4.414"/>\r
	<circle style="fill:#FFE15A;" cx="159.4" cy="288.551" r="4.414"/>\r
</g>\r
<g>\r
	<path style="fill:#41479B;" d="M191.149,253.763v7.602c0,3.144-2.558,5.702-5.702,5.702s-5.702-2.558-5.702-5.702v-7.602   L191.149,253.763"/>\r
	<path style="fill:#41479B;" d="M191.149,235.741v7.602c0,3.144-2.558,5.702-5.702,5.702s-5.702-2.558-5.702-5.702v-7.602H191.149"/>\r
	<path style="fill:#41479B;" d="M191.149,271.97v7.602c0,3.144-2.558,5.702-5.702,5.702s-5.702-2.558-5.702-5.702v-7.602H191.149"/>\r
	<path style="fill:#41479B;" d="M206.506,253.763v7.602c0,3.144-2.558,5.702-5.702,5.702s-5.702-2.558-5.702-5.702v-7.602   L206.506,253.763"/>\r
	<path style="fill:#41479B;" d="M175.794,253.763v7.602c0,3.144-2.558,5.702-5.702,5.702s-5.702-2.558-5.702-5.702v-7.602   L175.794,253.763"/>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, bv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#F5F5F5;" d="M512,200.093H0V97.104c0-4.875,3.953-8.828,8.828-8.828h494.345c4.875,0,8.828,3.953,8.828,8.828  L512,200.093L512,200.093z"/>\r
<path style="fill:#FF4B55;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V311.909h512v102.988  C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<rect y="200.091" style="fill:#41479B;" width="512" height="111.81"/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, yv = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#4173CD;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<polygon style="fill:#FFE15A;" points="512,229.518 211.862,229.518 211.862,88.277 158.897,88.277 158.897,229.518 0,229.518   0,282.484 158.897,282.484 158.897,423.725 211.862,423.725 211.862,282.484 512,282.484 "/>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, _v = `<?xml version="1.0" encoding="iso-8859-1"?>\r
<!-- Generator: Adobe Illustrator 19.0.0, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->\r
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" id="Layer_1" x="0px" y="0px" viewBox="0 0 512.001 512.001" style="enable-background:new 0 0 512.001 512.001;" xml:space="preserve">\r
<path style="fill:#FF4B55;" d="M503.172,423.725H8.828c-4.875,0-8.828-3.953-8.828-8.828V97.104c0-4.875,3.953-8.828,8.828-8.828  h494.345c4.875,0,8.828,3.953,8.828,8.828v317.793C512,419.773,508.047,423.725,503.172,423.725z"/>\r
<g>\r
	<path style="fill:#F5F5F5;" d="M253.474,225.753l13.837,18.101l21.606-7.232c1.208-0.404,2.236,0.962,1.512,2.01l-12.939,18.753   l13.555,18.314c0.758,1.024-0.224,2.423-1.444,2.059l-21.834-6.511l-13.228,18.55c-0.739,1.037-2.375,0.536-2.406-0.737   l-0.555-22.777l-21.73-6.849c-1.215-0.383-1.244-2.092-0.042-2.515l21.491-7.566l-0.202-22.783   C251.083,225.296,252.701,224.741,253.474,225.753z"/>\r
	<path style="fill:#F5F5F5;" d="M176.956,326.662c-38.995,0-70.627-31.633-70.627-70.663c0-38.958,31.633-70.662,70.627-70.662   c14.508,0,27.887,4.462,39.037,12.014c1.707,1.156,3.656-1.087,2.227-2.573c-16.664-17.325-40.248-27.894-66.398-27.001   c-44.926,1.533-82.118,37.553-84.989,82.413c-3.287,51.383,37.399,94.086,88.055,94.086c24.953,0,47.379-10.432,63.393-27.112   c1.415-1.473-0.538-3.683-2.229-2.537C204.89,322.196,191.489,326.662,176.956,326.662z"/>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
<g>\r
</g>\r
</svg>\r
`, Es = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  BR: av,
  CN: rv,
  DE: uv,
  ES: dv,
  FR: cv,
  GB: hv,
  HU: fv,
  IT: pv,
  JP: mv,
  PL: vv,
  PT: gv,
  RU: bv,
  SE: yv,
  TR: _v
}, Symbol.toStringTag, { value: "Module" })), Wi = (e) => {
  if (e && e.id)
    return true;
}, wv = (e) => {
  try {
    const { firstName: n } = e;
    if (n)
      return true;
  } catch {
  }
  return false;
}, kv = (e) => `${e.firstName}${e.lastName ? ` ${e.lastName}` : ""}`, ft = {
  message: {
    name: "message",
    icon: "chat-alt"
  },
  network: {
    name: "network",
    icon: "user-add",
    left: true
  },
  audio: {
    name: "audio",
    icon: "phone"
  },
  conferencing: {
    name: "conferencing",
    icon: "videocamera"
  },
  screenshare: {
    name: "screenshare",
    icon: "monitor"
  }
};
ft.network;
const Sv = [ft.message, ft.audio, ft.conferencing, ft.screenshare], an = (e) => {
  const n = Object.keys(ft);
  return !(e.length > 0 && e.filter((t) => n.indexOf(t)) === -1);
}, Iv = {
  name: "vu-rich-user-tooltip",
  emits: ["network", "message", "audio", "conferencing", "screenshare", "see-profile"],
  directives: {
    "click-outside": Fn
  },
  inject: {
    vuUserLabels: {
      default: () => lv
    },
    vuDebug: {
      default: false
    }
  },
  props: {
    show: {
      type: Boolean,
      required: false
    },
    user: {
      type: Object,
      validator: Wi,
      required: true
    },
    disabledActions: {
      type: Array,
      validator: an,
      required: false,
      default: () => []
    },
    hiddenActions: {
      type: Array,
      validator: an,
      required: false,
      default: () => []
    },
    side: {
      type: String,
      default: "bottom"
    },
    // eslint-disable-next-line vue/require-prop-types
    attach: {
      default: false
    },
    activator: {
      type: Object,
      default: void 0
    }
  },
  watch: {
    show(e) {
      this.innerShow = e;
    },
    contacts: {
      immediate: true,
      handler() {
        this.parseContactsInCommonLabel();
      }
    }
  },
  /* eslint-disable no-unused-vars */
  data: () => ({
    overflowHover: false,
    actions: ft,
    RHSactions: Sv,
    uuid: Re,
    getFullname: kv,
    validateName: wv,
    contactsLabelPart2: "",
    contactsLabelPart1: "",
    visibleAmount: 7
  }),
  /* eslint-enable no-unused-vars */
  computed: {
    hasInfo() {
      return this.user.company || this.user.country;
    },
    hasContacts() {
      return Array.isArray(this.user.contacts) && this.user.contacts.length > 0;
    },
    contacts() {
      return this.hasContacts ? this.user.contacts : [];
    },
    countryImg() {
      return !this.user.countryCode || !Es[this.user.countryCode.toUpperCase()] ? false : Es[this.user.countryCode.toUpperCase()];
    },
    countryLabel() {
      return this.user.countryCode && this.vuUserLabels[this.user.countryCode];
    },
    overflows() {
      return this.user.contacts && this.user.contacts.length > 7;
    },
    visibleContacts() {
      return this.hasContacts && this.overflows ? this.contacts.slice(0, this.visibleAmount) : this.contacts;
    },
    overflowContact() {
      return this.hasContacts && this.overflows ? this.contacts[this.visibleAmount] : null;
    },
    numberOfOverflowingContactsCssVariable() {
      return `"${this.contacts.length - this.visibleAmount}"`;
    }
  },
  methods: {
    parseContactsInCommonLabel() {
      if (!this.vuUserLabels.contactsInCommon && this.vuDebug) {
        console.warn("contactsInCommon nls is missing");
        return;
      }
      let { contactsInCommon: e } = this.vuUserLabels;
      const n = e.match(/\$\(.*\)/).length > 0;
      this.contacts.length > 1 && n ? e = e.replace("$(", "").replace(")", "") : e = e.replace(/\$\(.*\)/, ""), e = e.split("###"), this.contactsLabelPart1 = e[0], this.contactsLabelPart2 = e[1];
    },
    isDisabled(e) {
      return this.disabledActions.length > 0 && this.disabledActions.includes(e);
    }
  },
  components: { VuPopover: Ge, VuUserPicture: $t, VuIconBtn: de }
}, Cv = (e) => (pushScopeId("data-v-8bf3cebb"), e = e(), popScopeId(), e), Bv = { class: "rich-user-tooltip__header__wrap-name" }, $v = /* @__PURE__ */ Cv(() => /* @__PURE__ */ createBaseVNode("div", { class: "rich-user-tooltip__header__topbar" }, null, -1)), Ov = { class: "rich-user-tooltip__avatar-wrap" }, Tv = {
  key: 0,
  class: "rich-user-tooltip__info"
}, xv = {
  key: 0,
  class: "rich-user-tooltip__info__company"
}, Mv = {
  key: 1,
  class: "rich-user-tooltip__info__locale"
}, Vv = ["src"], Pv = {
  key: 1,
  class: "rich-user-tooltip__info__country"
}, Lv = { class: "rich-user-tooltip__contacts__label" }, Dv = { class: "rich-user-tooltip__contacts__list" }, Av = { class: "rich-user-tooltip__footer" }, Fv = { class: "rich-user-tooltip__footer__left" };
function zv(e, n, t, i, o, s) {
  const a = resolveComponent("VuUserPicture"), u = resolveComponent("VuIconBtn"), c = resolveComponent("VuPopover"), d = resolveDirective("tooltip");
  return openBlock(), createBlock(c, {
    side: t.side,
    show: t.show,
    arrow: "",
    shift: "",
    positions: ["bottom", "top"],
    attach: "body",
    "content-class": "vu-rich-user-tooltip",
    activator: t.activator
  }, createSlots({
    default: withCtx(() => [
      renderSlot(e.$slots, "default", {}, () => [
        withDirectives(createVNode(a, {
          id: t.user.id,
          clickable: "",
          src: t.user.imgSrc,
          presence: t.user.presence,
          class: "rich-user-tooltip__default-content"
        }, null, 8, ["id", "src", "presence"]), [
          [
            d,
            e.getFullname(t.user),
            void 0,
            { top: true }
          ]
        ])
      ], true)
    ]),
    arrow: withCtx(({ side: h2, shift: p2 }) => [
      withDirectives(createBaseVNode("div", {
        class: normalizeClass(["rich-user-tooltip__arrow popover-arrow", `rich-user-tooltip__arrow--${h2}`])
      }, null, 2), [
        [vShow, !p2]
      ])
    ]),
    title: withCtx(({ side: h2 }) => [
      createBaseVNode("div", {
        class: normalizeClass(["rich-user-tooltip__header", `rich-user-tooltip__header--${h2}`])
      }, [
        createBaseVNode("div", Bv, [
          withDirectives((openBlock(), createElementBlock("label", {
            class: "rich-user-tooltip__header__name",
            onClick: n[0] || (n[0] = (p2) => e.$emit("see-profile", t.user.id))
          }, [
            createTextVNode(toDisplayString(e.getFullname(t.user)), 1)
          ])), [
            [d, e.getFullname(t.user)]
          ])
        ]),
        $v,
        withDirectives((openBlock(), createElementBlock("div", Ov, [
          createVNode(a, {
            class: "rich-user-tooltip__header__avatar",
            size: "big",
            clickable: true,
            id: t.user && t.user.id,
            gutter: true,
            presence: t.user.presence,
            onClick: n[1] || (n[1] = (p2) => e.$emit("see-profile", t.user.id))
          }, null, 8, ["id", "presence"])
        ])), [
          [
            d,
            e.getFullname(t.user),
            void 0,
            { bottom: true }
          ]
        ])
      ], 2)
    ]),
    _: 2
  }, [
    (s.hasInfo || s.hasContacts, {
      name: "body",
      fn: withCtx(() => [
        s.hasInfo ? (openBlock(), createElementBlock("div", Tv, [
          t.user.company ? (openBlock(), createElementBlock("label", xv, toDisplayString(t.user.company), 1)) : createCommentVNode("", true),
          s.countryImg || s.countryLabel ? (openBlock(), createElementBlock("label", Mv, [
            s.countryImg ? (openBlock(), createElementBlock("img", {
              key: 0,
              class: "rich-user-tooltip__info__flag",
              src: s.countryImg
            }, null, 8, Vv)) : createCommentVNode("", true),
            s.countryLabel ? (openBlock(), createElementBlock("span", Pv, toDisplayString(s.countryLabel), 1)) : createCommentVNode("", true)
          ])) : createCommentVNode("", true)
        ])) : createCommentVNode("", true),
        renderSlot(e.$slots, "content", {}, void 0, true),
        s.hasContacts ? (openBlock(), createElementBlock(Fragment, { key: 1 }, [
          createBaseVNode("label", Lv, [
            createTextVNode(toDisplayString(e.contactsLabelPart1), 1),
            withDirectives((openBlock(), createElementBlock("span", {
              class: "rich-user-tooltip__contacts__amount",
              onClick: n[2] || (n[2] = (h2) => e.$emit("see-profile", t.user.id))
            }, [
              createTextVNode(toDisplayString(s.contacts.length), 1)
            ])), [
              [
                d,
                s.vuUserLabels.profile,
                void 0,
                { bottom: true }
              ]
            ]),
            e.contactsLabelPart2 ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
              createTextVNode(toDisplayString(e.contactsLabelPart2), 1)
            ], 64)) : createCommentVNode("", true)
          ]),
          createBaseVNode("div", Dv, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(s.visibleContacts, (h2) => withDirectives((openBlock(), createBlock(a, {
              key: h2.id || e.uuid(),
              id: h2.id || e.uuid(),
              clickable: true,
              onClick: (p2) => e.$emit("see-profile", h2.id)
            }, null, 8, ["id", "onClick"])), [
              [
                d,
                e.getFullname(h2),
                void 0,
                { bottom: true }
              ]
            ])), 128)),
            s.overflowContact ? withDirectives((openBlock(), createBlock(a, {
              key: 0,
              class: "rich-user-tooltip__overflow_contact",
              style: normalizeStyle({
                "--numberOfOverflowingContacts": s.numberOfOverflowingContactsCssVariable
              }),
              clickable: true,
              hoverable: "",
              id: s.overflowContact.id || e.uuid(),
              onClick: n[3] || (n[3] = (h2) => e.$emit("see-profile", s.overflowContact.id))
            }, null, 8, ["style", "id"])), [
              [
                d,
                s.vuUserLabels.profile,
                void 0,
                { bottom: true }
              ]
            ]) : createCommentVNode("", true)
          ])
        ], 64)) : createCommentVNode("", true),
        createBaseVNode("div", Av, [
          createBaseVNode("div", Fv, [
            renderSlot(e.$slots, "footer-left", {}, () => [
              t.hiddenActions.length && t.hiddenActions.includes("network") ? createCommentVNode("", true) : withDirectives((openBlock(), createBlock(a, {
                key: 0,
                icon: e.actions.network.icon,
                class: "add-network",
                disabled: t.disabledActions.length > 0 && t.disabledActions.includes("network"),
                onClick: n[4] || (n[4] = (h2) => {
                  s.isDisabled("network") || e.$emit("network", t.user.id);
                })
              }, null, 8, ["icon", "disabled"])), [
                [
                  d,
                  s.vuUserLabels.network,
                  void 0,
                  { bottom: true }
                ]
              ])
            ], true)
          ]),
          renderSlot(e.$slots, "footer-right", {}, () => [
            (openBlock(true), createElementBlock(Fragment, null, renderList(e.RHSactions, (h2) => (openBlock(), createElementBlock(Fragment, {
              key: h2.name
            }, [
              t.hiddenActions.length && t.hiddenActions.includes(h2.name) ? createCommentVNode("", true) : withDirectives((openBlock(), createBlock(u, {
                key: 0,
                class: "right-btn",
                icon: h2.icon,
                disabled: s.isDisabled(h2.name),
                onClick: (p2) => {
                  s.isDisabled(h2.name) || e.$emit(h2.name, t.user.id);
                }
              }, null, 8, ["icon", "disabled", "onClick"])), [
                [
                  d,
                  s.vuUserLabels[h2.name],
                  void 0,
                  { bottom: true }
                ]
              ])
            ], 64))), 128))
          ], true)
        ])
      ]),
      key: "0"
    })
  ]), 1032, ["side", "show", "activator"]);
}
const qi = /* @__PURE__ */ F(Iv, [["render", zv], ["__scopeId", "data-v-8bf3cebb"]]), Ev = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: qi
}, Symbol.toStringTag, { value: "Module" })), Nv = {
  name: "vu-user-name",
  props: {
    // eslint-disable-next-line vue/require-default-prop
    firstName: String,
    // eslint-disable-next-line vue/require-default-prop
    lastName: String,
    toUpper: {
      type: Boolean,
      required: false,
      default: true
    },
    shift: Boolean,
    clickable: Boolean
  },
  emits: ["click"],
  computed: {
    _lastName() {
      return this.toUpper ? this.lastName.toUpperCase() : this.lastName;
    }
  }
};
function Rv(e, n, t, i, o, s) {
  return openBlock(), createElementBlock("div", {
    class: normalizeClass(["vu-user-name", [
      "vu-user-name--default-color",
      "vu-user-name--default-size",
      { "vu-user-name--with-avatar": t.shift },
      { "vu-user-name--clickable": t.clickable }
    ]])
  }, [
    renderSlot(e.$slots, "default", {}, () => [
      createBaseVNode("span", {
        class: "content",
        onClick: n[0] || (n[0] = (a) => e.$emit("click"))
      }, toDisplayString(t.firstName + " " + s._lastName), 1)
    ], true)
  ], 2);
}
const Ki = /* @__PURE__ */ F(Nv, [["render", Rv], ["__scopeId", "data-v-5bae4c6d"]]), Hv = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: Ki
}, Symbol.toStringTag, { value: "Module" })), jv = {
  name: "vu-user",
  emits: ["click-other-user", "click-user"],
  props: {
    user: {
      type: Object,
      required: true,
      validator: Wi
    },
    disabledActions: {
      type: Array,
      required: false,
      default: () => [],
      validator: an
    },
    hiddenActions: {
      type: Array,
      required: false,
      default: () => [],
      validator: an
    },
    showPicture: {
      type: Boolean,
      required: false,
      default: true
    },
    showName: {
      type: Boolean,
      required: false,
      default: false
    },
    showUserTooltip: {
      type: Boolean,
      required: false,
      default: true
    },
    clickable: {
      type: Boolean,
      required: false,
      default: true
    },
    pictureBackground: {
      type: String,
      required: false,
      default: "#fff"
    },
    attach: {
      default: () => false,
      validator: Gn
    }
  },
  computed: {
    listeners() {
      return Ze(this.$attrs, true);
    }
  },
  data: () => ({
    getListenersFromAttrs: Ze
  }),
  components: { VuRichUserTooltip: qi, VuUserPicture: $t, VuUserName: Ki, VuUserPicture: $t }
}, Uv = { class: "vu-user" };
function Wv(e, n, t, i, o, s) {
  const a = resolveComponent("VuUserPicture"), u = resolveComponent("VuUserName"), c = resolveComponent("VuRichUserTooltip");
  return openBlock(), createElementBlock("div", Uv, [
    t.showUserTooltip ? (openBlock(), createBlock(c, mergeProps({
      key: 0,
      user: t.user,
      "disabled-actions": t.disabledActions,
      "hidden-actions": t.hiddenActions,
      attach: t.attach
    }, toHandlers(s.listeners.vOn || {})), {
      default: withCtx(() => [
        t.showPicture ? (openBlock(), createBlock(a, {
          key: 0,
          id: t.user.id,
          src: t.user.imgSrc,
          presence: t.user.presence,
          clickable: t.clickable,
          style: normalizeStyle({ background: t.pictureBackground }),
          onClick: n[0] || (n[0] = (d) => e.$emit("click-user", e.value))
        }, null, 8, ["id", "src", "presence", "clickable", "style"])) : createCommentVNode("", true),
        t.showName ? (openBlock(), createBlock(u, {
          key: 1,
          "first-name": t.user.firstName,
          "last-name": t.user.lastName,
          clickable: t.clickable,
          shift: t.showPicture,
          onClick: n[1] || (n[1] = (d) => e.$emit("click-user", d))
        }, {
          default: withCtx(() => [
            renderSlot(e.$slots, "userName", {}, void 0, true)
          ]),
          _: 3
        }, 8, ["first-name", "last-name", "clickable", "shift"])) : createCommentVNode("", true)
      ]),
      _: 3
    }, 16, ["user", "disabled-actions", "hidden-actions", "attach"])) : (openBlock(), createElementBlock(Fragment, { key: 1 }, [
      t.showPicture ? (openBlock(), createBlock(a, {
        key: 0,
        id: t.user.id,
        src: t.user.imgSrc,
        presence: t.user.presence,
        clickable: t.clickable,
        style: normalizeStyle({ background: t.pictureBackground }),
        onClick: n[2] || (n[2] = (d) => e.$emit("click-user", d))
      }, null, 8, ["id", "src", "presence", "clickable", "style"])) : createCommentVNode("", true),
      t.showName ? (openBlock(), createBlock(u, {
        key: 1,
        "first-name": t.user.firstName,
        "last-name": t.user.lastName,
        clickable: t.clickable,
        shift: t.showPicture,
        onClick: n[3] || (n[3] = (d) => e.$emit("click-user", d))
      }, {
        default: withCtx(() => [
          renderSlot(e.$slots, "userName", {}, void 0, true)
        ]),
        _: 3
      }, 8, ["first-name", "last-name", "clickable", "shift"])) : createCommentVNode("", true)
    ], 64))
  ]);
}
const qv = /* @__PURE__ */ F(jv, [["render", Wv], ["__scopeId", "data-v-32241cd5"]]), Kv = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: qv
}, Symbol.toStringTag, { value: "Module" }));
function Kt() {
  if (!window)
    return false;
  const e = navigator.userAgent.toLowerCase();
  return !!(/iPhone|iPad/i.test(e) || /safari/.test(e) && !/chrome/.test(e) && ("ontouchstart" in window || navigator.maxTouchPoints > 0 || navigator.msMaxTouchPoints > 0) || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
}
const rn = ({ userAgent: e }) => e.match(/android/i);
let Gi = null, fs = null;
function Gv({ disableTooltipsOnDevices: e, deferTooltips: n }) {
  Gi = e, fs = n;
}
function Yi(e, n, t, i) {
  const o = n.getBoundingClientRect(), {
    left: s,
    top: a,
    shiftX: u
  } = Xn(e, o, t.getBoundingClientRect(), i.getBoundingClientRect(), {}, true);
  t.style.top = `${a}px`, t.style.left = `${s}px`;
  const c = t.querySelector(".tooltip-arrow") || { style: {} };
  return u > 0 ? (c.style.right = `${u - 5}px`, c.style.left = "initial") : u < 0 && (c.style.left = `${o.left - s + n.clientWidth / 2}px`, c.style.right = "initial"), true;
}
function Xi(e) {
  switch (true) {
    case e.left:
      return "left";
    case e.right:
      return "right";
    case e.bottom:
      return "bottom";
    default:
      return "top";
  }
}
function Ji(e, n, t) {
  const { text: i = "" } = t || {}, o = createVNode({ ...Jn }, {
    type: n ? "popover" : "tooltip",
    text: typeof t == "string" ? t : i
  });
  e.tooltip = o, fs && (e.deferedTooltip = true);
}
async function Ns(e, n = {}) {
  const { value: t, modifiers: i } = n;
  if (i.ellipsis && e.offsetWidth >= e.scrollWidth)
    return;
  const { attach: o = document.body } = t, s = Xi(i), { tooltip: { component: a = false } = {} } = e;
  a || Ji(e, i.popover, t), render(e.tooltip, o), e.tooltip.component.props.show = true, e.tooltip.component.props.side = s, await new Promise((u) => setTimeout(u, 1)), Yi(s, e, e.tooltip.el, o), (rn(navigator) || Kt()) && (e.stopClickOutside = il(e, () => zn(e), { detectIframe: true }));
}
function zn(e) {
  const { tooltip: n } = e, { component: t } = n || {};
  !n || !t || (t.props.show = false, (rn(navigator) || Kt()) && e.stopClickOutside && (e.stopClickOutside(), delete e.stopClickOutside), Zi(e));
}
async function Yv(e, n, t) {
  var u;
  const { tooltip: i } = e, { modifiers: o, value: s = {} } = n, { text: a } = s;
  if (i) {
    const { component: c } = i, d = typeof s == "string" ? s : a;
    if (i.props.text = d, c && (c.props.text = d), (u = c == null ? void 0 : c.props) != null && u.show) {
      const h2 = Xi(o);
      await new Promise((p2) => setTimeout(p2, 1)), Yi(h2, t.el, i.el, document.body);
    }
  }
}
function Zi(e) {
  var n, t, i, o, s;
  if (e.tooltip) {
    const { tooltip: a } = e;
    a && ((t = (n = a == null ? void 0 : a.component) == null ? void 0 : n.el) == null || t.remove(), (s = (o = (i = a == null ? void 0 : a.component) == null ? void 0 : i.vnode) == null ? void 0 : o.el) == null || s.remove()), delete e.tooltip;
  }
}
function Rs(e, n, t) {
  n.modifiers.click || t ? (e.clickListener = () => {
    var i, o, s;
    (s = (o = (i = e == null ? void 0 : e.tooltip) == null ? void 0 : i.component) == null ? void 0 : o.props) != null && s.show ? zn(e) : Ns(e, n);
  }, e.addEventListener("click", e.clickListener)) : (e.showListener = Ns.bind(null, e, n), e.addEventListener("mouseenter", e.showListener), e.hideListener = zn.bind(null, e), e.addEventListener("mouseleave", e.hideListener));
}
function Xv(e) {
  e.clickListener && e.removeEventListener("click", e.clickListener), e.showListener && e.removeEventListener("mouseenter", e.showListener), e.hideListener && e.removeEventListener("mouseleave", e.hideListener);
}
const Bn = {
  setConfig: Gv,
  mounted(e, n) {
    const { modifiers: t, value: i } = n, { forceOnDevices: o = false, popover: s = false } = t, a = rn(navigator) || Kt();
    Gi && !o && a || n.disabled || (!fs && i ? Ji(e, s, i) : e.deferedTooltip = true, i && Rs(e, n, a));
  },
  updated(e, n, t) {
    const i = typeof n.value == "function", o = typeof n.oldValue == "function", s = i ? n.value() : n.value, a = o ? n.oldValue() : n.oldValue;
    if (s !== a && (Yv(e, n, t), Xv(e), s)) {
      const u = rn(navigator) || Kt();
      Rs(e, n, u);
    }
  },
  beforeUnmount: Zi
}, Hs = (e, n, t) => {
  const i = createVNode(is, { mask: true });
  if (render(i, t.el), e.spinner = i, i && typeof n.value == "string") {
    const { component: o } = i;
    i.props.text = n.value, o && (o.props.text = n.value);
  }
  e.classList.add("masked");
}, js = (e, n, t) => {
  e.spinner && (render(null, t.el), e.spinner = null, e.classList.remove("masked"));
}, Us = {
  mounted(e, n, t) {
    n.value && Hs(e, n, t);
  },
  updated(e, n, t) {
    n.value !== n.oldValue && (n.value ? Hs : js)(e, n, t);
  },
  unmounted(e, n, t) {
    js(e, n, t);
  }
}, Jv = {
  install(e, n = { disableTooltipsOnDevices: true }) {
    e.directive("click-outside", Fn), e.directive("mask", Us), Bn.setConfig(n), e.directive("tooltip", Bn);
  },
  clickOutside: Fn,
  tooltip: Bn,
  mask: Us
};
function Zv(e, n = {}) {
  const {
    lang: t = "en",
    country: i = "US",
    isMobile: o,
    isIOS: s,
    globalRegister: a = true,
    disableVuMessageProgressBar: u,
    sharedMessageContainer: c = true
  } = n;
  if (ff(e), Vh(e, c), Or(e), a) {
    const d = /* @__PURE__ */ Object.assign({ "./components/layouts/vu-status-bar.vue": $l, "./components/layouts/vu-thumbnail.vue": Na, "./components/layouts/vu-tile.vue": Pa, "./components/vu-accordion.vue": Ga, "./components/vu-alert-dialog/vu-alert-dialog-container.vue": Vr, "./components/vu-alert-dialog/vu-alert-dialog.vue": rr, "./components/vu-badge.vue": Mo, "./components/vu-btn-dropdown.vue": Hr, "./components/vu-btn-group.vue": Kr, "./components/vu-btn.vue": Ar, "./components/vu-carousel-slide.vue": Zr, "./components/vu-carousel.vue": ru, "./components/vu-checkbox.vue": gu, "./components/vu-contextual-dropdown.vue": wu, "./components/vu-datepicker.vue": Lu, "./components/vu-divider.vue": Bo, "./components/vu-dropdown/vu-dropdownmenu-items-scrollable.vue": ta, "./components/vu-dropdownmenu-items.vue": ma, "./components/vu-dropdownmenu.vue": ga, "./components/vu-dropzone.vue": zu, "./components/vu-facets-bar.vue": Uu, "./components/vu-form.vue": Ku, "./components/vu-grid-view.vue": Hd, "./components/vu-icon-btn.vue": _a$1, "./components/vu-icon-link.vue": qd, "./components/vu-icon.vue": Ao, "./components/vu-image.vue": Dl, "./components/vu-input-date.vue": tc, "./components/vu-input-number.vue": hc, "./components/vu-input.vue": yc, "./components/vu-lazy.vue": xl$1, "./components/vu-lightbox/vu-lightbox-bar.vue": Pc, "./components/vu-lightbox/vu-lightbox.vue": Yc, "./components/vu-media-upload-droppable.vue": eh, "./components/vu-media-upload-empty.vue": lh, "./components/vu-media-upload-error.vue": dh, "./components/vu-media-upload-loading.vue": yh, "./components/vu-media-upload-preview.vue": Sh, "./components/vu-media-upload.vue": Mh, "./components/vu-message/vu-message-container.vue": Uh, "./components/vu-message/vu-message.vue": Eh, "./components/vu-modal/vu-mobile-dialog.vue": Jh, "./components/vu-modal/vu-modal-container.vue": gf, "./components/vu-modal/vu-modal.vue": hf, "./components/vu-multiple-select.vue": Tf, "./components/vu-popover.vue": Sl, "./components/vu-progress-circular.vue": mh, "./components/vu-range.vue": Hf, "./components/vu-scroller.vue": Kl, "./components/vu-section-header.vue": Kf, "./components/vu-select-options.vue": ud, "./components/vu-select.vue": Od, "./components/vu-side-panel.vue": zc, "./components/vu-single-checkbox.vue": tp, "./components/vu-slider.vue": dp, "./components/vu-spinner.vue": jl, "./components/vu-textarea.vue": bp, "./components/vu-thumbnail-grid.vue": Wp, "./components/vu-thumbnail-list-item.vue": Tp, "./components/vu-thumbnail-list.vue": hm, "./components/vu-time-picker.vue": Om, "./components/vu-timeline-divider.vue": Fm, "./components/vu-tooltip.vue": gl, "./components/vu-tree-view-item.vue": Km, "./components/vu-tree-view.vue": Zm, "./components/vu-user/vu-rich-user-tooltip.vue": Ev, "./components/vu-user/vu-user-name.vue": Hv, "./components/vu-user/vu-user-picture.vue": Qu, "./components/vu-user/vu-user.vue": Kv });
    for (const h2 in d) {
      const p2 = d[h2];
      e.component(p2.default.name, p2.default);
    }
  }
  t && i ? e.provide("lang", `${t}-${i}`) : e.provide("lang", "en-US"), e.provide(Yt, o !== void 0 ? o : Fi()), e.provide(es, s !== void 0 ? s : Kt()), e.provide("vuCollectionActions", null), e.provide("vuCollectionLazyImages", true), e.provide("vuTileEmphasizeText", false), e.provide("vuDateFormatWeekday", true), e.provide("vuDateFormatShort", false), e.provide("vuTreeViewLazy", true), e.provide("vuTreeViewIcon", "chevron"), e.provide(ns, true), e.provide(ts, u), Jv.install(e, n);
}
function tryOnScopeDispose$2(fn) {
  if (getCurrentScope()) {
    onScopeDispose(fn);
    return true;
  }
  return false;
}
function toValue$2(r) {
  return typeof r === "function" ? r() : unref(r);
}
const isClient$2 = typeof window !== "undefined" && typeof document !== "undefined";
typeof WorkerGlobalScope !== "undefined" && globalThis instanceof WorkerGlobalScope;
const notNullish$1 = (val) => val != null;
const toString$1 = Object.prototype.toString;
const isObject$3 = (val) => toString$1.call(val) === "[object Object]";
const noop$2 = () => {
};
function createFilterWrapper(filter2, fn) {
  function wrapper(...args) {
    return new Promise((resolve2, reject) => {
      Promise.resolve(filter2(() => fn.apply(this, args), { fn, thisArg: this, args })).then(resolve2).catch(reject);
    });
  }
  return wrapper;
}
const bypassFilter = (invoke2) => {
  return invoke2();
};
function debounceFilter(ms, options = {}) {
  let timer;
  let maxTimer;
  let lastRejector = noop$2;
  const _clearTimeout = (timer2) => {
    clearTimeout(timer2);
    lastRejector();
    lastRejector = noop$2;
  };
  const filter2 = (invoke2) => {
    const duration = toValue$2(ms);
    const maxDuration = toValue$2(options.maxWait);
    if (timer)
      _clearTimeout(timer);
    if (duration <= 0 || maxDuration !== void 0 && maxDuration <= 0) {
      if (maxTimer) {
        _clearTimeout(maxTimer);
        maxTimer = null;
      }
      return Promise.resolve(invoke2());
    }
    return new Promise((resolve2, reject) => {
      lastRejector = options.rejectOnCancel ? reject : resolve2;
      if (maxDuration && !maxTimer) {
        maxTimer = setTimeout(() => {
          if (timer)
            _clearTimeout(timer);
          maxTimer = null;
          resolve2(invoke2());
        }, maxDuration);
      }
      timer = setTimeout(() => {
        if (maxTimer)
          _clearTimeout(maxTimer);
        maxTimer = null;
        resolve2(invoke2());
      }, duration);
    });
  };
  return filter2;
}
function throttleFilter(...args) {
  let lastExec = 0;
  let timer;
  let isLeading = true;
  let lastRejector = noop$2;
  let lastValue;
  let ms;
  let trailing;
  let leading;
  let rejectOnCancel;
  if (!isRef(args[0]) && typeof args[0] === "object")
    ({ delay: ms, trailing = true, leading = true, rejectOnCancel = false } = args[0]);
  else
    [ms, trailing = true, leading = true, rejectOnCancel = false] = args;
  const clear = () => {
    if (timer) {
      clearTimeout(timer);
      timer = void 0;
      lastRejector();
      lastRejector = noop$2;
    }
  };
  const filter2 = (_invoke) => {
    const duration = toValue$2(ms);
    const elapsed = Date.now() - lastExec;
    const invoke2 = () => {
      return lastValue = _invoke();
    };
    clear();
    if (duration <= 0) {
      lastExec = Date.now();
      return invoke2();
    }
    if (elapsed > duration && (leading || !isLeading)) {
      lastExec = Date.now();
      invoke2();
    } else if (trailing) {
      lastValue = new Promise((resolve2, reject) => {
        lastRejector = rejectOnCancel ? reject : resolve2;
        timer = setTimeout(() => {
          lastExec = Date.now();
          isLeading = true;
          resolve2(invoke2());
          clear();
        }, Math.max(0, duration - elapsed));
      });
    }
    if (!leading && !timer)
      timer = setTimeout(() => isLeading = true, duration);
    isLeading = false;
    return lastValue;
  };
  return filter2;
}
function getLifeCycleTarget$1(target) {
  return getCurrentInstance();
}
function toRef$2(...args) {
  if (args.length !== 1)
    return toRef$3(...args);
  const r = args[0];
  return typeof r === "function" ? readonly(customRef(() => ({ get: r, set: noop$2 }))) : ref(r);
}
function useDebounceFn(fn, ms = 200, options = {}) {
  return createFilterWrapper(
    debounceFilter(ms, options),
    fn
  );
}
function useThrottleFn(fn, ms = 200, trailing = false, leading = true, rejectOnCancel = false) {
  return createFilterWrapper(
    throttleFilter(ms, trailing, leading, rejectOnCancel),
    fn
  );
}
function watchWithFilter(source, cb, options = {}) {
  const {
    eventFilter = bypassFilter,
    ...watchOptions
  } = options;
  return watch(
    source,
    createFilterWrapper(
      eventFilter,
      cb
    ),
    watchOptions
  );
}
function tryOnMounted(fn, sync = true, target) {
  const instance = getLifeCycleTarget$1();
  if (instance)
    onMounted(fn, target);
  else if (sync)
    fn();
  else
    nextTick(fn);
}
function useIntervalFn(cb, interval = 1e3, options = {}) {
  const {
    immediate = true,
    immediateCallback = false
  } = options;
  let timer = null;
  const isActive = ref(false);
  function clean() {
    if (timer) {
      clearInterval(timer);
      timer = null;
    }
  }
  function pause() {
    isActive.value = false;
    clean();
  }
  function resume() {
    const intervalValue = toValue$2(interval);
    if (intervalValue <= 0)
      return;
    isActive.value = true;
    if (immediateCallback)
      cb();
    clean();
    timer = setInterval(cb, intervalValue);
  }
  if (immediate && isClient$2)
    resume();
  if (isRef(interval) || typeof interval === "function") {
    const stopWatch = watch(interval, () => {
      if (isActive.value && isClient$2)
        resume();
    });
    tryOnScopeDispose$2(stopWatch);
  }
  tryOnScopeDispose$2(pause);
  return {
    isActive,
    pause,
    resume
  };
}
function useTimeoutFn(cb, interval, options = {}) {
  const {
    immediate = true
  } = options;
  const isPending = ref(false);
  let timer = null;
  function clear() {
    if (timer) {
      clearTimeout(timer);
      timer = null;
    }
  }
  function stop2() {
    isPending.value = false;
    clear();
  }
  function start(...args) {
    clear();
    isPending.value = true;
    timer = setTimeout(() => {
      isPending.value = false;
      timer = null;
      cb(...args);
    }, toValue$2(interval));
  }
  if (immediate) {
    isPending.value = true;
    if (isClient$2)
      start();
  }
  tryOnScopeDispose$2(stop2);
  return {
    isPending: readonly(isPending),
    start,
    stop: stop2
  };
}
function watchDebounced(source, cb, options = {}) {
  const {
    debounce = 0,
    maxWait = void 0,
    ...watchOptions
  } = options;
  return watchWithFilter(
    source,
    cb,
    {
      ...watchOptions,
      eventFilter: debounceFilter(debounce, { maxWait })
    }
  );
}
function watchOnce(source, cb, options) {
  const stop2 = watch(source, (...args) => {
    nextTick(() => stop2());
    return cb(...args);
  }, options);
  return stop2;
}
function unrefElement$2(elRef) {
  var _a3;
  const plain = toValue$2(elRef);
  return (_a3 = plain == null ? void 0 : plain.$el) != null ? _a3 : plain;
}
const defaultWindow$2 = isClient$2 ? window : void 0;
function useEventListener$1(...args) {
  let target;
  let events2;
  let listeners;
  let options;
  if (typeof args[0] === "string" || Array.isArray(args[0])) {
    [events2, listeners, options] = args;
    target = defaultWindow$2;
  } else {
    [target, events2, listeners, options] = args;
  }
  if (!target)
    return noop$2;
  if (!Array.isArray(events2))
    events2 = [events2];
  if (!Array.isArray(listeners))
    listeners = [listeners];
  const cleanups = [];
  const cleanup = () => {
    cleanups.forEach((fn) => fn());
    cleanups.length = 0;
  };
  const register = (el2, event, listener, options2) => {
    el2.addEventListener(event, listener, options2);
    return () => el2.removeEventListener(event, listener, options2);
  };
  const stopWatch = watch(
    () => [unrefElement$2(target), toValue$2(options)],
    ([el2, options2]) => {
      cleanup();
      if (!el2)
        return;
      const optionsClone = isObject$3(options2) ? { ...options2 } : options2;
      cleanups.push(
        ...events2.flatMap((event) => {
          return listeners.map((listener) => register(el2, event, listener, optionsClone));
        })
      );
    },
    { immediate: true, flush: "post" }
  );
  const stop2 = () => {
    stopWatch();
    cleanup();
  };
  tryOnScopeDispose$2(stop2);
  return stop2;
}
function useMounted$1() {
  const isMounted = ref(false);
  const instance = getCurrentInstance();
  if (instance) {
    onMounted(() => {
      isMounted.value = true;
    }, instance);
  }
  return isMounted;
}
function useSupported$1(callback) {
  const isMounted = useMounted$1();
  return computed(() => {
    isMounted.value;
    return Boolean(callback());
  });
}
function useRafFn(fn, options = {}) {
  const {
    immediate = true,
    fpsLimit = void 0,
    window: window2 = defaultWindow$2
  } = options;
  const isActive = ref(false);
  const intervalLimit = fpsLimit ? 1e3 / fpsLimit : null;
  let previousFrameTimestamp = 0;
  let rafId = null;
  function loop(timestamp2) {
    if (!isActive.value || !window2)
      return;
    if (!previousFrameTimestamp)
      previousFrameTimestamp = timestamp2;
    const delta = timestamp2 - previousFrameTimestamp;
    if (intervalLimit && delta < intervalLimit) {
      rafId = window2.requestAnimationFrame(loop);
      return;
    }
    previousFrameTimestamp = timestamp2;
    fn({ delta, timestamp: timestamp2 });
    rafId = window2.requestAnimationFrame(loop);
  }
  function resume() {
    if (!isActive.value && window2) {
      isActive.value = true;
      previousFrameTimestamp = 0;
      rafId = window2.requestAnimationFrame(loop);
    }
  }
  function pause() {
    isActive.value = false;
    if (rafId != null && window2) {
      window2.cancelAnimationFrame(rafId);
      rafId = null;
    }
  }
  if (immediate)
    resume();
  tryOnScopeDispose$2(pause);
  return {
    isActive: readonly(isActive),
    pause,
    resume
  };
}
function useIntersectionObserver(target, callback, options = {}) {
  const {
    root,
    rootMargin = "0px",
    threshold = 0.1,
    window: window2 = defaultWindow$2,
    immediate = true
  } = options;
  const isSupported = useSupported$1(() => window2 && "IntersectionObserver" in window2);
  const targets = computed(() => {
    const _target = toValue$2(target);
    return (Array.isArray(_target) ? _target : [_target]).map(unrefElement$2).filter(notNullish$1);
  });
  let cleanup = noop$2;
  const isActive = ref(immediate);
  const stopWatch = isSupported.value ? watch(
    () => [targets.value, unrefElement$2(root), isActive.value],
    ([targets2, root2]) => {
      cleanup();
      if (!isActive.value)
        return;
      if (!targets2.length)
        return;
      const observer = new IntersectionObserver(
        callback,
        {
          root: unrefElement$2(root2),
          rootMargin,
          threshold
        }
      );
      targets2.forEach((el2) => el2 && observer.observe(el2));
      cleanup = () => {
        observer.disconnect();
        cleanup = noop$2;
      };
    },
    { immediate, flush: "post" }
  ) : noop$2;
  const stop2 = () => {
    cleanup();
    stopWatch();
    isActive.value = false;
  };
  tryOnScopeDispose$2(stop2);
  return {
    isSupported,
    isActive,
    pause() {
      cleanup();
      isActive.value = false;
    },
    resume() {
      isActive.value = true;
    },
    stop: stop2
  };
}
function useElementVisibility(element, options = {}) {
  const { window: window2 = defaultWindow$2, scrollTarget, threshold = 0 } = options;
  const elementIsVisible = ref(false);
  useIntersectionObserver(
    element,
    (intersectionObserverEntries) => {
      let isIntersecting = elementIsVisible.value;
      let latestTime = 0;
      for (const entry of intersectionObserverEntries) {
        if (entry.time >= latestTime) {
          latestTime = entry.time;
          isIntersecting = entry.isIntersecting;
        }
      }
      elementIsVisible.value = isIntersecting;
    },
    {
      root: scrollTarget,
      window: window2,
      threshold
    }
  );
  return elementIsVisible;
}
const ARRIVED_STATE_THRESHOLD_PIXELS = 1;
function useScroll(element, options = {}) {
  const {
    throttle = 0,
    idle = 200,
    onStop = noop$2,
    onScroll = noop$2,
    offset = {
      left: 0,
      right: 0,
      top: 0,
      bottom: 0
    },
    eventListenerOptions = {
      capture: false,
      passive: true
    },
    behavior = "auto",
    window: window2 = defaultWindow$2,
    onError = (e) => {
      console.error(e);
    }
  } = options;
  const internalX = ref(0);
  const internalY = ref(0);
  const x = computed({
    get() {
      return internalX.value;
    },
    set(x2) {
      scrollTo2(x2, void 0);
    }
  });
  const y = computed({
    get() {
      return internalY.value;
    },
    set(y2) {
      scrollTo2(void 0, y2);
    }
  });
  function scrollTo2(_x, _y) {
    var _a3, _b2, _c3, _d3;
    if (!window2)
      return;
    const _element = toValue$2(element);
    if (!_element)
      return;
    (_c3 = _element instanceof Document ? window2.document.body : _element) == null ? void 0 : _c3.scrollTo({
      top: (_a3 = toValue$2(_y)) != null ? _a3 : y.value,
      left: (_b2 = toValue$2(_x)) != null ? _b2 : x.value,
      behavior: toValue$2(behavior)
    });
    const scrollContainer = ((_d3 = _element == null ? void 0 : _element.document) == null ? void 0 : _d3.documentElement) || (_element == null ? void 0 : _element.documentElement) || _element;
    if (x != null)
      internalX.value = scrollContainer.scrollLeft;
    if (y != null)
      internalY.value = scrollContainer.scrollTop;
  }
  const isScrolling = ref(false);
  const arrivedState = reactive({
    left: true,
    right: false,
    top: true,
    bottom: false
  });
  const directions = reactive({
    left: false,
    right: false,
    top: false,
    bottom: false
  });
  const onScrollEnd = (e) => {
    if (!isScrolling.value)
      return;
    isScrolling.value = false;
    directions.left = false;
    directions.right = false;
    directions.top = false;
    directions.bottom = false;
    onStop(e);
  };
  const onScrollEndDebounced = useDebounceFn(onScrollEnd, throttle + idle);
  const setArrivedState = (target) => {
    var _a3;
    if (!window2)
      return;
    const el2 = ((_a3 = target == null ? void 0 : target.document) == null ? void 0 : _a3.documentElement) || (target == null ? void 0 : target.documentElement) || unrefElement$2(target);
    const { display, flexDirection } = getComputedStyle(el2);
    const scrollLeft = el2.scrollLeft;
    directions.left = scrollLeft < internalX.value;
    directions.right = scrollLeft > internalX.value;
    const left = Math.abs(scrollLeft) <= (offset.left || 0);
    const right = Math.abs(scrollLeft) + el2.clientWidth >= el2.scrollWidth - (offset.right || 0) - ARRIVED_STATE_THRESHOLD_PIXELS;
    if (display === "flex" && flexDirection === "row-reverse") {
      arrivedState.left = right;
      arrivedState.right = left;
    } else {
      arrivedState.left = left;
      arrivedState.right = right;
    }
    internalX.value = scrollLeft;
    let scrollTop = el2.scrollTop;
    if (target === window2.document && !scrollTop)
      scrollTop = window2.document.body.scrollTop;
    directions.top = scrollTop < internalY.value;
    directions.bottom = scrollTop > internalY.value;
    const top2 = Math.abs(scrollTop) <= (offset.top || 0);
    const bottom = Math.abs(scrollTop) + el2.clientHeight >= el2.scrollHeight - (offset.bottom || 0) - ARRIVED_STATE_THRESHOLD_PIXELS;
    if (display === "flex" && flexDirection === "column-reverse") {
      arrivedState.top = bottom;
      arrivedState.bottom = top2;
    } else {
      arrivedState.top = top2;
      arrivedState.bottom = bottom;
    }
    internalY.value = scrollTop;
  };
  const onScrollHandler = (e) => {
    var _a3;
    if (!window2)
      return;
    const eventTarget = (_a3 = e.target.documentElement) != null ? _a3 : e.target;
    setArrivedState(eventTarget);
    isScrolling.value = true;
    onScrollEndDebounced(e);
    onScroll(e);
  };
  useEventListener$1(
    element,
    "scroll",
    throttle ? useThrottleFn(onScrollHandler, throttle, true, false) : onScrollHandler,
    eventListenerOptions
  );
  tryOnMounted(() => {
    try {
      const _element = toValue$2(element);
      if (!_element)
        return;
      setArrivedState(_element);
    } catch (e) {
      onError(e);
    }
  });
  useEventListener$1(
    element,
    "scrollend",
    onScrollEnd,
    eventListenerOptions
  );
  return {
    x,
    y,
    isScrolling,
    arrivedState,
    directions,
    measure() {
      const _element = toValue$2(element);
      if (window2 && _element)
        setArrivedState(_element);
    }
  };
}
function resolveElement$1(el2) {
  if (typeof Window !== "undefined" && el2 instanceof Window)
    return el2.document.documentElement;
  if (typeof Document !== "undefined" && el2 instanceof Document)
    return el2.documentElement;
  return el2;
}
function useInfiniteScroll(element, onLoadMore, options = {}) {
  var _a3;
  const {
    direction = "bottom",
    interval = 100,
    canLoadMore = () => true
  } = options;
  const state = reactive(useScroll(
    element,
    {
      ...options,
      offset: {
        [direction]: (_a3 = options.distance) != null ? _a3 : 0,
        ...options.offset
      }
    }
  ));
  const promise = ref();
  const isLoading = computed(() => !!promise.value);
  const observedElement = computed(() => {
    return resolveElement$1(toValue$2(element));
  });
  const isElementVisible = useElementVisibility(observedElement);
  function checkAndLoad() {
    state.measure();
    if (!observedElement.value || !isElementVisible.value || !canLoadMore(observedElement.value))
      return;
    const { scrollHeight, clientHeight, scrollWidth, clientWidth } = observedElement.value;
    const isNarrower = direction === "bottom" || direction === "top" ? scrollHeight <= clientHeight : scrollWidth <= clientWidth;
    if (state.arrivedState[direction] || isNarrower) {
      if (!promise.value) {
        promise.value = Promise.all([
          onLoadMore(state),
          new Promise((resolve2) => setTimeout(resolve2, interval))
        ]).finally(() => {
          promise.value = null;
          nextTick(() => checkAndLoad());
        });
      }
    }
  }
  watch(
    () => [state.arrivedState[direction], isElementVisible.value],
    checkAndLoad,
    { immediate: true }
  );
  return {
    isLoading
  };
}
function useNow(options = {}) {
  const {
    controls: exposeControls = false,
    interval = "requestAnimationFrame"
  } = options;
  const now = ref(/* @__PURE__ */ new Date());
  const update = () => now.value = /* @__PURE__ */ new Date();
  const controls = interval === "requestAnimationFrame" ? useRafFn(update, { immediate: true }) : useIntervalFn(update, interval, { immediate: true });
  if (exposeControls) {
    return {
      now,
      ...controls
    };
  } else {
    return now;
  }
}
function usePrevious(value, initialValue) {
  const previous = shallowRef(initialValue);
  watch(
    toRef$2(value),
    (_, oldValue) => {
      previous.value = oldValue;
    },
    { flush: "sync" }
  );
  return readonly(previous);
}
function useUWA() {
  let UWA2 = window.UWA;
  return {
    UWA: UWA2
  };
}
const DS_BASE_URL = ((_a2 = window.dsDefaultWebappsBaseUrl) == null ? void 0 : _a2.replace("webapps/", "")) || `${window.location.origin}/`;
const DS_WEBAPPS_URL = window.dsDefaultWebappsBaseUrl || `${DS_BASE_URL}webapps/`;
const DS_BASE_URL_IFRAME = DS_BASE_URL || `${window.location.origin}/`;
const DS_WEBAPPS_URL_IFRAME = DS_WEBAPPS_URL || `${DS_BASE_URL_IFRAME}webapps/`;
const AMDLOADER_SCRIPT = "AmdLoader/AmdLoader.js";
let initAsync$1;
async function setupGlobalEnvVar() {
  var _a3;
  const path = JSON.parse(JSON.stringify(DS_WEBAPPS_URL_IFRAME.replace("webapps/", "webapps")));
  const paths = {
    "DS": path,
    // 'DS/UWPClientCode/UWA': 'UWA2/js',
    // 'UWA': 'UWA2/js',
    "vuejs": `${path}/vuejs/2.6.10/vue.min`,
    "vu-kit": `${path}/vuekit/vu-kit.umd`
    // have to call it "vu-kit"
  };
  (_a3 = window.require) == null ? void 0 : _a3.config({ paths });
}
async function initRequire$1() {
  if (initAsync$1)
    return;
  await setupGlobalEnvVar();
  const script = document.createElement("script");
  const loadingAsync = new Promise((resolve2, reject) => {
    script.onload = resolve2;
    script.onerror = reject;
  });
  script.src = `${DS_WEBAPPS_URL_IFRAME}${AMDLOADER_SCRIPT}`;
  document.head.appendChild(script);
  await loadingAsync;
}
async function requirejs$1(modules) {
  if (!initAsync$1)
    initAsync$1 = initRequire$1();
  await initAsync$1;
  return new Promise((resolve2, reject) => {
    window.require(modules, (...m) => resolve2(m), reject);
  });
}
async function getPlatformAPI() {
  const [PlatformAPI] = await requirejs$1(["DS/PlatformAPI/PlatformAPI"]);
  return PlatformAPI;
}
async function getI3DXCompassPlatformServices() {
  const [i3DXCompassPlatformServices] = await requirejs$1(["DS/i3DXCompassPlatformServices/i3DXCompassPlatformServices"]);
  return i3DXCompassPlatformServices;
}
async function getTransientWidget() {
  const [TransientWidget] = await requirejs$1(["DS/TransientWidget/TransientWidget"]);
  return TransientWidget;
}
async function getWAFData() {
  const [WAFData] = await requirejs$1(["DS/WAFData/WAFData"]);
  return WAFData;
}
async function getOpenWith() {
  const [OpenWith] = await requirejs$1(["DS/i3DXCompassPlatformServices/OpenWith"]);
  return OpenWith;
}
async function getTrackerAPI() {
  const [TrackerAPI] = await requirejs$1(["DS/Usage/TrackerAPI"]);
  return TrackerAPI;
}
async function getCompassData() {
  const [CompassData] = await requirejs$1(["DS/i3DXCompass/Data"]);
  return CompassData;
}
async function getI18n() {
  const [I18n] = await requirejs$1(["DS/UWPClientCode/I18n"]);
  return I18n;
}
const useNotificationsStore = /* @__PURE__ */ defineStore("notifications", () => {
  const clusterId = ref("");
  const setClusterId = (id2) => {
    if (typeof id2 === "string")
      clusterId.value = id2;
  };
  const isCurrentCluster = (id2) => {
    return clusterId.value === id2;
  };
  const centerFrameOpened = ref(false);
  const setCenterFrameOpened = (opened) => {
    centerFrameOpened.value = opened;
  };
  const isLoading = ref(true);
  const dispose = ref(null);
  const loadingMore = ref(false);
  const filterQuery = ref(false);
  const canLoadMoreHistory = ref(true);
  const renderToDoc = ref(null);
  const hasRenderToDoc = computed(() => renderToDoc.value !== null);
  const setRenderToDoc = (doc2) => {
    renderToDoc.value = doc2;
  };
  const resetRenderToDoc = () => {
    renderToDoc.value = null;
  };
  const canLoadMoreFromArchive = ref(false);
  const unreadTotal = ref(0);
  const setUnreadTotal2 = (nbUnread) => {
    let counter = 0;
    if (typeof nbUnread === "number") {
      counter = nbUnread;
    } else {
      counter = Number.parseInt(nbUnread);
    }
    unreadTotal.value = counter;
  };
  const unreadTotalFederation = ref([]);
  const setUnreadTotalFederation = ({ unread: unread2, clusterId: clusterId2 }) => {
    const find = unreadTotalFederation.value.find((n) => n.clusterId === clusterId2);
    if (!find) {
      unreadTotalFederation.value.push({ unread: unread2, clusterId: clusterId2 });
    } else {
      find.unread = unread2;
    }
  };
  const getUnreadTotalFederation = computed(() => {
    return unreadTotalFederation.value.map((n) => n.unread).reduce((a, b) => a + b, 0);
  });
  watch(
    () => getUnreadTotalFederation.value,
    (value) => {
      setUnreadTotal2(value);
    },
    { immediate: true, deep: true }
  );
  const filterTotal = ref(0);
  const oldID = ref(-1);
  const oldIDs = ref([]);
  const clusterInOldIDs = (clusterId2) => {
    return oldIDs.value.find((item) => item.CLUSTER_ID === clusterId2);
  };
  const addOldID = (OLD_ID, CREATION_DATE, CLUSTER_ID) => {
    const old_id_data = clusterInOldIDs(CLUSTER_ID);
    if (!old_id_data) {
      oldIDs.value.push({ OLD_ID, CREATION_DATE, CLUSTER_ID });
    } else {
      if (old_id_data.CREATION_DATE > CREATION_DATE) {
        old_id_data.OLD_ID = OLD_ID;
        old_id_data.CREATION_DATE = CREATION_DATE;
      }
    }
  };
  const groupIDs = ref([]);
  const filterNotifIds = ref([]);
  const notifications = reactive(/* @__PURE__ */ new Map());
  const groups = reactive(/* @__PURE__ */ new Map());
  const merges = reactive(/* @__PURE__ */ new Map());
  const sectionListIds = ref([]);
  const scroller = ref(null);
  const sectionList = reactive(/* @__PURE__ */ new Map());
  const selectionMode = ref(false);
  const selected2 = ref([]);
  const error = ref(false);
  const isEmpty = computed(() => {
    return notifications.size === 0;
  });
  const getNotificationById = (id2) => {
    return notifications.get(id2) ? notifications.get(id2) : null;
  };
  const getNotificationByGroupId = (groupId) => {
    for (const [, notification] of notifications) {
      if (notification.GROUPID === groupId) {
        return notification;
      }
    }
    return null;
  };
  const getMergeById = (id2) => {
    return merges.get(id2) ? merges.get(id2) : null;
  };
  const getMergeByGroupId = (groupId) => {
    for (const [, notification] of merges) {
      if (notification.GROUPID === groupId)
        return notification;
    }
    return null;
  };
  const getAllMergesByGroupId = (groupID) => {
    const merges2 = [];
    for (const [, merge] of merges2) {
      if (merge.GROUPID === groupID)
        merges2.push(merge);
    }
    return merges2;
  };
  const addGroupId = (groupId) => {
    if (!groupIDs.value.includes(groupId)) {
      groupIDs.value.push(groupId);
    }
  };
  const setScrollerBody = (el2) => {
    scroller.value = el2;
  };
  const setOldID = (id2) => {
    oldID.value = id2;
  };
  const setFilterQuery = (fetch2) => {
    filterQuery.value = fetch2;
  };
  const setFilterTotal = (count) => {
    if (typeof count === "number")
      filterTotal.value = count;
  };
  const addFilterNotifIds = (notifIds, clusterId2, archive) => {
    for (const id2 of notifIds) {
      const find = filterNotifIds.value.find((n) => n.id === id2);
      if (!find)
        filterNotifIds.value.push({ id: id2, archive, clusterId: clusterId2 });
    }
  };
  const removeFilterNotifId = (notifIds) => {
    filterNotifIds.value = filterNotifIds.value.filter((filterId) => !notifIds.includes(filterId.id));
  };
  const setIsLoading = (loading) => {
    isLoading.value = loading;
  };
  const setCanLoadMoreHistory = (load) => {
    canLoadMoreHistory.value = load;
  };
  const setCanLoadMoreFromArchive = (load) => {
    canLoadMoreFromArchive.value = load;
  };
  const setLoadingMore = (load) => {
    loadingMore.value = load;
  };
  const addUnreadTotal = (nbUnread) => {
    unreadTotal.value += nbUnread;
  };
  const isUnreadTotalEmpty = () => {
    return unreadTotal.value === 0;
  };
  const setMerge2 = ({ notification, options }) => {
    const { addMerge } = useNotificationManagement();
    addMerge(notification, options);
  };
  const getGroup = (groupId) => {
    return groups.get(groupId) ? groups.get(groupId) : null;
  };
  const setNotification = (notification, options) => {
    const { addNotification: addNotification2 } = useNotificationManagement();
    addNotification2({ notification, options });
  };
  const setNotifAppInfos = (data) => {
    const { isMerge, isNormalNotification, isGroup } = useNotificationManagement();
    let notification;
    if (isMerge(data.notifID)) {
      notification = getMergeById(data.notifID);
      notification.MESSAGE.icon.src = data.src;
    }
    if (isNormalNotification(data.notifID) || isGroup(data.notifID)) {
      notification = getNotificationById(data.notifID);
      notification.MESSAGE.icon.src = data.src;
    }
  };
  const resetStates = () => {
    oldID.value = -1;
    filterTotal.value = 0;
    notifications.clear();
    sectionList.clear();
    oldIDs.value.length = 0;
    sectionListIds.value.length = 0;
    unreadTotal.value = 0;
    groupIDs.value.length = 0;
    filterNotifIds.value.length = 0;
    selectionMode.value = false;
    selected2.value.length = 0;
    canLoadMoreHistory.value = true;
    canLoadMoreFromArchive.value = false;
    loadingMore.value = false;
    error.value = false;
    groups.clear();
    merges.clear();
    scroller.value = null;
    filterQuery.value = false;
    dispose.value = null;
    unreadTotalFederation.value.length = 0;
  };
  return {
    // STATE
    notifications,
    sectionList,
    sectionListIds,
    unreadTotal,
    getUnreadTotalFederation,
    filterTotal,
    oldID,
    oldIDs,
    groupIDs,
    filterNotifIds,
    selectionMode,
    selected: selected2,
    canLoadMoreHistory,
    canLoadMoreFromArchive,
    archive: canLoadMoreFromArchive,
    loadingMore,
    isLoading,
    error,
    groups,
    merges,
    scroller,
    filterQuery,
    dispose,
    renderToDoc,
    centerFrameOpened,
    clusterId,
    unreadTotalFederation,
    // COMPUTED GETTERS
    isEmpty,
    hasRenderToDoc,
    // NOT COMPUTED GETTERS
    getNotificationById,
    getNotificationByGroupId,
    getMergeById,
    getMergeByGroupId,
    getAllMergesByGroupId,
    getGroup,
    getSelected: () => selected2.value,
    getGroupIDs: () => groupIDs.value,
    getSectionListIds: () => sectionListIds,
    getSectionList: () => sectionList,
    // ACTIONS
    setClusterId,
    isCurrentCluster,
    setCenterFrameOpened,
    setNotifAppInfos,
    setIsLoading,
    setUnreadTotal: setUnreadTotal2,
    addUnreadTotal,
    setUnreadTotalFederation,
    isUnreadTotalEmpty,
    setNotification,
    setMerge: setMerge2,
    setScrollerBody,
    setLoadingMore,
    setCanLoadMoreHistory,
    setCanLoadMoreFromArchive,
    setOldID,
    addFilterNotifIds,
    removeFilterNotifId,
    resetStates,
    addGroupId,
    setFilterTotal,
    setFilterQuery,
    setRenderToDoc,
    resetRenderToDoc,
    addOldID,
    clusterInOldIDs
  };
});
const noNotifications = "No Notifications";
const unreadNotifications = "unread notifications";
const oneUnreadNotification = "unread notification";
const foundNotifications = "notifications found";
const oneFoundNotification = "notification found";
const viewAll = "View All";
const hideAll = "Hide All";
const viewMore = "View more";
const markAllAsRead = "Mark All as Read";
const markAllAsUnread = "Mark All as Unread";
const markAllFilterAsRead = "Mark All Filtered as Read";
const markAllFilterAsUnread = "Mark All Filtered as Unread";
const deleteAll = "Delete All";
const deleteFilteredNotif = "Delete Filtered Notifications";
const deleteSelectedTitle = "Deletion of selected notifications";
const deleteSelectedMsg = "All selected notifications will be deleted. Are you sure?";
const deleteFilteredTitle = "Deletion of filtered notifications";
const deleteFilteredMsg = "All filtered notifications will be deleted. Are you sure?";
const notificationsDisplay = "Notifications Display";
const currentPlatform = "Current Platform";
const allPlatforms = "All Platforms";
const preferencesMenu = "Preferences";
const today = "today";
const yesterday = "Yesterday";
const notificationDeleted = "Notification will be deleted";
const cancel = "Cancel";
const archiveAlert = "Notice: Archived notifications have been added to the timeline";
const persons = "people";
const Persons = "People";
const actionLabel = "action performed";
const actionFailed = "action failed";
const groupActionLabel = "all actions performed";
const accept = "Accept";
const deny = "Deny";
const openWith = "Open With";
const openInNewTab = "Open in New Tab";
const markAsRead = "Mark as Read";
const markAsUnread = "Mark as Unread";
const markGroupAsRead = "Mark Group as read";
const markGroupAsUnRead = "Mark Group as unread";
const markAsImportant = "Mark as Important";
const removeImportant = "Remove from Important";
const removeGroupImportant = "Remove Group from Important";
const markGroupAsImportant = "Mark Group as Important";
const unsubscribe$1 = "Unsubscribe";
const unsubscribeIndefinitely = "Unsubscribe Indefinitely";
const unsubscribeForHour = "Unsubscribe for 1 Hour";
const deleteGroup = "Delete Group";
const filter$1 = "Filter";
const reset = "Reset";
const close = "Close";
const selectStartDate = "Start date";
const to = "to";
const selectEndDate = "End date";
const searches = "Search notifications";
const read = "Read";
const unread = "Unread";
const starred = "Important";
const unstarred = "Not Important";
const platformsSelection = "Platforms Selection";
const multiSelection = "Multi-selection";
const selected = "selected";
const selectAll = "Select All";
const unselectAll = "Unselect All";
const markSelectedAsRead$1 = "Mark Selected as Read";
const markSelectedAsUnread = "Mark Selected as Unread";
const markSelectedAsStarred$1 = "Mark Selected as Important";
const markSelectedAsUnstarred = "Remove Selected from Important";
const unsubscribeSelected$1 = "Unsubscribe Selected";
const deleteSelected$1 = "Delete Selected";
const deleteAllTitle = "Deletion of all notifications";
const deleteAllMsg = "All notifications will be deleted. Are you sure?";
const settings = "Notifications Settings";
const preferences = "Notifications Preferences";
const globalSetting = "Global Setting";
const forcedPreference = "Preference forced by administrator";
const centerIconText = "Notification Center";
const alertIconText = "Alert";
const browserIconText = "Browser";
const settingOff = "Off";
const settingOffForAnHour = "Off for 1 Hour";
const minLeft = "min left";
const hourLeft = "hour left";
const centerNotificationTooltipEnabled = "Notification Center notifications enabled";
const alertNotificationTooltipEnabled = "Alert notifications enabled";
const browserNotificationTooltipEnabled = "Browser notifications enabled";
const alertNotificationTooltipDisabled = "Alert notifications disabled";
const browserNotificationTooltipDisabled = "Browser notifications disabled";
const overrideByGlobal = "but override by global setting";
const browserSupport = "Notification Support";
const browserNotSupported = "Your browser is not supported. Please use the latest version of Chrome, Firefox, Safari or Edge.";
const notificationPermission = "Notification Permission";
const deniedPermission = "You have denied the permission to show notifications. Please allow the permission to receive notifications or reload the page to apply the changes.";
const notificationPopupActivationTitle = "Notification Activation";
const notificationPopupActivationMsgPopupForBlock = "Browser notifications are blocked for 3DExperience platform. To unblock notifications : click on the icon  place on the left of the URL and turn on browser notifications for 3DExperience platform.";
const browserNotifDeactivateMsgForPopup = "Browser notifications will be de-activated for this service.";
const mailNotifDeactivateMsgForPopup = "Mail notifications will be de-activated for this service.";
const alertNotifDeactivateMsgForPopup = "Alert notifications will be de-activated for this service.";
const notificationPopupDeactivationTitle = "Notification Deactivation";
const confirmButtonLabel = "Confirm";
const dndAlertMsg = "Browser and alert notifications are hidden as your status is DoNotDisturb";
const rollbackPopupTitleFor3DDashboard = "3DNotification: Purged Notifications Rollback";
const rollbackPopupTitleForNotifCenter = "Purged Notification Rollback";
const rollbackPopupMsg = "Purged notifications, older than ninety days, will be rolled back";
const platformPreferencesMenu = "Platform Preferences";
const emailNotificationTooltipEnabled = "Mail notifications enabled";
const emailNotificationTooltipDisabled = "Mail notifications disabled";
const emailIconText = "Mail";
const forcedPreferenceOption = "Enforced for all tenant users";
const notForcedPreferenceOption = "Not enforced";
const filterDateDefaultValue = "No date selected";
const messagePreventplatformpreferences = "These settings apply to all users of the platform";
const platformpreferences = "Notifications Platform Preferences";
const warningTitlePlatformPreferences = "Platform Preference Update";
const settingTurnedOnAndForced = "This setting will be locked ON for all users of your platform";
const settingTurnedOnNotForced = "This setting will be set by default as ON for all users of your platform";
const settingTurnedOffAndForced = "This setting will be locked OFF for all users of your platform";
const settingTurnedOffNotForced = "This setting will be set by default as OFF for all users of your platform";
const settingTurnedUnlocked = "This setting will be unlocked for all users of your platform";
const nls = {
  noNotifications,
  unreadNotifications,
  oneUnreadNotification,
  foundNotifications,
  oneFoundNotification,
  viewAll,
  hideAll,
  viewMore,
  markAllAsRead,
  markAllAsUnread,
  markAllFilterAsRead,
  markAllFilterAsUnread,
  deleteAll,
  deleteFilteredNotif,
  deleteSelectedTitle,
  deleteSelectedMsg,
  deleteFilteredTitle,
  deleteFilteredMsg,
  notificationsDisplay,
  currentPlatform,
  allPlatforms,
  preferencesMenu,
  today,
  yesterday,
  notificationDeleted,
  cancel,
  archiveAlert,
  persons,
  Persons,
  actionLabel,
  actionFailed,
  groupActionLabel,
  accept,
  deny,
  openWith,
  openInNewTab,
  markAsRead,
  markAsUnread,
  markGroupAsRead,
  markGroupAsUnRead,
  markAsImportant,
  removeImportant,
  removeGroupImportant,
  markGroupAsImportant,
  unsubscribe: unsubscribe$1,
  unsubscribeIndefinitely,
  unsubscribeForHour,
  "delete": "Delete",
  deleteGroup,
  filter: filter$1,
  reset,
  close,
  selectStartDate,
  to,
  selectEndDate,
  searches,
  read,
  unread,
  starred,
  unstarred,
  platformsSelection,
  multiSelection,
  selected,
  selectAll,
  unselectAll,
  markSelectedAsRead: markSelectedAsRead$1,
  markSelectedAsUnread,
  markSelectedAsStarred: markSelectedAsStarred$1,
  markSelectedAsUnstarred,
  unsubscribeSelected: unsubscribeSelected$1,
  deleteSelected: deleteSelected$1,
  deleteAllTitle,
  deleteAllMsg,
  settings,
  preferences,
  globalSetting,
  forcedPreference,
  centerIconText,
  alertIconText,
  browserIconText,
  settingOff,
  settingOffForAnHour,
  minLeft,
  hourLeft,
  centerNotificationTooltipEnabled,
  alertNotificationTooltipEnabled,
  browserNotificationTooltipEnabled,
  alertNotificationTooltipDisabled,
  browserNotificationTooltipDisabled,
  overrideByGlobal,
  browserSupport,
  browserNotSupported,
  notificationPermission,
  deniedPermission,
  notificationPopupActivationTitle,
  notificationPopupActivationMsgPopupForBlock,
  browserNotifDeactivateMsgForPopup,
  mailNotifDeactivateMsgForPopup,
  alertNotifDeactivateMsgForPopup,
  notificationPopupDeactivationTitle,
  confirmButtonLabel,
  dndAlertMsg,
  rollbackPopupTitleFor3DDashboard,
  rollbackPopupTitleForNotifCenter,
  rollbackPopupMsg,
  platformPreferencesMenu,
  emailNotificationTooltipEnabled,
  emailNotificationTooltipDisabled,
  emailIconText,
  forcedPreferenceOption,
  notForcedPreferenceOption,
  filterDateDefaultValue,
  messagePreventplatformpreferences,
  platformpreferences,
  warningTitlePlatformPreferences,
  settingTurnedOnAndForced,
  settingTurnedOnNotForced,
  settingTurnedOffAndForced,
  settingTurnedOffNotForced,
  settingTurnedUnlocked
};
const i18n = ref({});
async function loadTranslations(filename = "feed", path = "i18n!DS/NotificationsCenterVue/assets/nls/") {
  try {
    const [result] = await requirejs$1([`${path}${filename}`]);
    i18n.value = Object.assign(unref(i18n), result);
  } catch (error) {
    console.error(`Error loading nls ${path}${filename}, using local nls.`);
    i18n.value = Object.assign(unref(i18n), nls);
  }
  return i18n.value;
}
const translationsPromiseSetup = loadTranslations();
function useTranslations$1() {
  const $i18n2 = (key, values = {}) => {
    let value = unref(i18n)[key] || "";
    Object.entries(values).forEach(([key2, val]) => {
      value = value.replace(`{${key2}}`, val);
    });
    return value;
  };
  return { i18n, $i18n: $i18n2 };
}
const { $i18n: $i18n$a } = useTranslations$1();
function usePreferencesManagement() {
  const store = useSettingsStore();
  const notifStore = useNotificationsStore();
  const now = useNow();
  let stopSettingUnsubDateWatcher = null;
  const resetSettingUnsubDate = (id2) => {
    resetSettingUnsubscriptionDate$1({ id: id2 });
    store.removeUnsubscribe_dateSetting(id2);
  };
  const watchSettingWithUnsubDate = (debounce = 6e4, maxWait = 6e4) => {
    watch(
      () => store.unsubscribe_dateSettingList,
      (list) => {
        if (stopSettingUnsubDateWatcher)
          stopSettingUnsubDateWatcher();
        if (list.length) {
          stopSettingUnsubDateWatcher = watchDebounced(
            () => now.value,
            (realTimeCurrentDate) => {
              for (let i = 0; i < list.length; i++) {
                const setting = store.getSetting(list[i]);
                if (new Date(setting.unsubscribe_date).getTime() < new Date(realTimeCurrentDate).getTime()) {
                  resetSettingUnsubDate(setting.id);
                }
              }
            },
            { debounce, maxWait }
          );
        }
      },
      { immediate: true, deep: true }
    );
  };
  const loadSettings = () => {
    const settings2 = store.isPlatformPreferences ? { method: getServicesSettingsOfTenant, name: "servicesSettings" } : { method: getSettings, name: "settings" };
    if (!store.settings.size) {
      store.setSettingLoadingState(settings2.name, true);
      settings2.method();
    } else {
      store.setSettingLoadState(settings2.name, true);
      store.setSettingLoadingState(settings2.name, false);
    }
    if (!store.isDNDRefreshed)
      getDNDStatus();
  };
  const alertNotice = (title, msg) => {
    Ht.show({
      title,
      message: msg,
      noMobile: true
    });
  };
  const messageNotice = (msg, timeout2) => {
    try {
      if (notifStore.dispose) {
        if (!notifStore.dispose.bind._dispose) {
          notifStore.dispose.bind.show = false;
        }
        notifStore.dispose.dispose();
      }
    } catch (error) {
      console.log("error trying trying to hide the message", error);
    }
    if (typeof timeout2 === "number" && typeof msg === "string") {
      notifStore.dispose = ct.create({ text: msg, timeout: timeout2 });
    } else {
      console.log("Wrong parameters for message");
    }
  };
  const alertConfirmNotice = async ({ title, msg, okLabel, cancelLabel }) => {
    try {
      await Ht.confirm({
        title,
        message: msg,
        ...!!okLabel && { okLabel },
        ...!!cancelLabel && { cancelLabel },
        noMobile: true,
        keyboard: false
      });
      return true;
    } catch {
      return false;
    }
  };
  const alertPromptNotice = ({ title, msg, okLabel, cancelLabel, placeholder, required }) => {
    return new Promise((resolve2, reject) => {
      Ht.prompt({
        title,
        rawContent: msg,
        // for html tags
        okLabel,
        ...!!cancelLabel && { cancelLabel },
        placeholder,
        required,
        keyboard: false,
        noMobile: true
      }).then((onConfirmVal) => {
        resolve2(onConfirmVal);
      }).catch((onCloseVal) => {
        reject(onCloseVal);
      });
    });
  };
  const requestNotificationPermission = () => {
    let granted = false;
    return new Promise((resolve2) => {
      if (!("Notification" in window)) {
        granted = false;
        alertNotice($i18n$a("browserSupport"), $i18n$a("browserNotSupported"));
        resolve2(granted);
      } else if (Notification.permission === "granted") {
        granted = true;
        resolve2(granted);
      } else if (Notification.permission !== "denied") {
        Notification.requestPermission().then((permission) => {
          if (permission === "granted") {
            granted = true;
          } else {
            granted = false;
            alertNotice($i18n$a("notificationPermission"), $i18n$a("deniedPermission"));
          }
          resolve2(granted);
        });
      } else {
        alertNotice(
          $i18n$a("notificationPopupActivationTitle"),
          $i18n$a("notificationPopupActivationMsgPopupForBlock")
        );
        resolve2(granted);
      }
    });
  };
  const settingUpdateVerification = async (setting, attribut) => {
    const needConfirm = setting.getNotifOptionEnabled(attribut);
    const needBrowserPermission = attribut === "browser" && !setting.getNotifOptionEnabled(attribut);
    if (needBrowserPermission) {
      const granted = await requestNotificationPermission();
      return granted;
    } else if (needConfirm) {
      const message = attribut === "browser" ? $i18n$a("browserNotifDeactivateMsgForPopup") : attribut === "email" ? $i18n$a("mailNotifDeactivateMsgForPopup") : $i18n$a("alertNotifDeactivateMsgForPopup");
      return alertConfirmNotice({
        title: $i18n$a("notificationPopupDeactivationTitle"),
        msg: message,
        okLabel: $i18n$a("confirmButtonLabel"),
        cancelLabel: $i18n$a("cancel")
      });
    } else {
      return true;
    }
  };
  return {
    loadSettings,
    settingUpdateVerification,
    alertConfirmNotice,
    messageNotice,
    alertNotice,
    alertPromptNotice,
    resetSettingUnsubDate,
    watchSettingWithUnsubDate
  };
}
const { $i18n: $i18n$9 } = useTranslations$1();
function formatOptions(value) {
  if (typeof value !== "object") {
    return { enable: +value, readOnly: 0 };
  } else {
    return value;
  }
}
class Setting {
  /**
   * Constructs a new Setting object.
   * @param {object} setting - The setting object.
   */
  constructor(setting) {
    /**
     * Updates the settings with the given value.
     *
     * @param {boolean} updateSubscribe - true if the udpateSetting is call to update subscribe.
     */
    __publicField(this, "updateTheSettings", (updateSubscribe) => {
      const { isPlatformPreferences } = useSettingsStore();
      if (isPlatformPreferences) {
        updateServiceSettings({
          subscribe: this.subscribe,
          id: this.id,
          notif_by_email: this.notif_by_email
        });
      } else if (this.readOnly) {
        const { messageNotice } = usePreferencesManagement();
        messageNotice($i18n$9("forcedPreference"), 3e3);
      } else {
        if (updateSubscribe) {
          updateSettings({
            id: this.id,
            subscribe: this.subscribe
          });
        } else {
          updateSettings({
            id: this.id,
            notif_by_browser: this.notif_by_browser.enable,
            notif_by_email: this.notif_by_email.enable,
            notif_by_ui: this.notif_by_ui.enable
          });
        }
      }
    });
    const keyToMap = ["notif_by_ui", "notif_by_email", "notif_by_browser", "subscribe"];
    for (const [key, value] of Object.entries(setting)) {
      if (keyToMap.includes(key.toLowerCase())) {
        this[key.toLowerCase()] = formatOptions(value);
      } else {
        this[key.toLowerCase()] = value;
      }
    }
    this.groupList = [];
  }
  /**
   * Returns whether the setting is a group.
   * @returns {boolean} True if the setting is a group, false otherwise.
   */
  get isGroup() {
    return this.name === "GLOBAL";
  }
  /**
   * Returns whether the setting is read-only.
   * @returns {boolean} True if the setting is read-only, false otherwise.
   */
  get readOnly() {
    return !!this.subscribe.readOnly;
  }
  /**
   * Change the value readOnly of subscribe and call updateTheSettings.
   * @param {number | boolean} readOnly
   */
  setReadOnly(readOnly) {
    this.subscribe.readOnly = +readOnly;
    this.updateTheSettings();
  }
  /**
   * Returns whether the notification is enabled.
   * @returns {boolean} True if the notification is enabled, false otherwise.
   */
  get enable() {
    return (this == null ? void 0 : this.subscribe.enable) === 1 && (this.unsubscribe_date === null || new Date(this.unsubscribe_date) < new Date(Date.now()));
  }
  /**
   * Change the value of enabled and call updateTheSettings.
   * @param {number | boolean} value if the notification is enabled, false otherwise.
   */
  setEnable(value) {
    this.subscribe.enable = +value;
    this.updateTheSettings(true);
  }
  /**
   * Returns the value of notif_by_uiEnabled.
   * @returns {boolean} The value of notif_by_uiEnabled.
   */
  get notif_by_uiEnabled() {
    return !!(this == null ? void 0 : this.notif_by_ui.enable);
  }
  /**
   * Change the value of notif_by_uiEnabled and call updateTheSettings.
   * @param {number | boolean} value if the notification is notif_by_uiEnabled, false otherwise.
   */
  set_notif_by_uiEnabled(value) {
    this.notif_by_ui.enable = +value;
    this.updateTheSettings(false);
  }
  /**
   * Returns the value of notif_by_uiReadOnly.
   * @returns {boolean} The value of notif_by_uiReadOnly.
   */
  get notif_by_uiReadOnly() {
    return !!(this == null ? void 0 : this.notif_by_ui.readOnly);
  }
  /**
   * Change the value of notif_by_uiReadOnly and call updateTheSettings.
   * @param {number | boolean} value if the notification is notif_by_uiReadOnly, false otherwise.
   */
  set_notif_by_uiReadOnly(value) {
    this.notif_by_ui.readOnly = +value;
    this.updateTheSettings();
  }
  /**
   * Returns the value of notif_by_emailEnabled.
   * @returns {boolean} The value of notif_by_emailEnabled.
   */
  get notif_by_emailEnabled() {
    return !!(this == null ? void 0 : this.notif_by_email.enable);
  }
  /**
   * Change the value of notif_by_emailEnabled and call updateTheSettings.
   * @param {number | boolean} value if the notification is notif_by_emailEnabled, false otherwise.
   */
  set_notif_by_emailEnabled(value) {
    this.notif_by_email.enable = +value;
    this.updateTheSettings(false);
  }
  /**
   * Returns the value of notif_by_emailReadOnly.
   * @returns {boolean} The value of notif_by_emailReadOnly.
   */
  get notif_by_emailReadOnly() {
    return !!(this == null ? void 0 : this.notif_by_email.readOnly);
  }
  /**
   * Change the value of notif_by_emailReadOnly and call updateTheSettings.
   * @param {number | boolean} value if the notification is notif_by_emailReadOnly, false otherwise.
   */
  set_notif_by_emailReadOnly(value) {
    this.notif_by_email.readOnly = +value;
    this.updateTheSettings();
  }
  /**
   * Returns the value of notif_by_browserEnabled.
   * @returns {boolean} The value of notif_by_browserEnabled.
   */
  get notif_by_browserEnabled() {
    return !!(this == null ? void 0 : this.notif_by_browser.enable);
  }
  /**
   * Change the value of notif_by_browserEnabled and call updateTheSettings.
   * @param {number | boolean} value if the notification is notif_by_browserEnabled, false otherwise.
   */
  set_notif_by_browserEnabled(value) {
    this.notif_by_browser.enable = value;
    this.updateTheSettings(false);
  }
  /**
   * Returns the value of notif_by_browserReadOnly.
   * @returns {boolean} The value of notif_by_browserReadOnly.
   */
  get notif_by_browserReadOnly() {
    return !!(this == null ? void 0 : this.notif_by_browser.readOnly);
  }
  /**
   * Change the value of notif_by_browserReadOnly and call updateTheSettings.
   * @param {number | boolean} value if the notification is notif_by_browserReadOnly, false otherwise.
   */
  set_notif_by_browserReadOnly(value) {
    this.notif_by_browser.readOnly = +value;
    this.updateTheSettings();
  }
  /**
   * Returns true if at least one notification option or subscribe is readOnly.
   * @returns {boolean} return true if one options of the setting is readOnly or subscribe is readOnly.
   */
  get hasAtLeastOneReadonly() {
    return this.hasAtLeastOneNotifOptionReadonly || this.readOnly;
  }
  /**
   * Returns true if at least one notification option is readOnly.
   * @returns {boolean} return true if one options of the setting is readOnly
   */
  get hasAtLeastOneNotifOptionReadonly() {
    return this.notif_by_emailReadOnly || this.notif_by_browserReadOnly || this.notif_by_uiReadOnly;
  }
  /**
   * Returns the value of enable dependig of the option.
   * @param {string} option email | ui | browser
   * @returns {boolean} The value of enable depending of the option.
   */
  getNotifOptionEnabled(option) {
    switch (option) {
      case "email":
        return this.notif_by_emailEnabled;
      case "ui":
      case "alert":
        return this.notif_by_uiEnabled;
      case "browser":
        return this.notif_by_browserEnabled;
      case "center":
        return this.enable;
      default:
        throw new Error(`Option ${option} didn't exist. Only 'email', 'ui', 'alert' and 'browser' are available.`);
    }
  }
  /**
   * Change the value of enable dependig of the option and call updateTheSettings.
   * @param {string} option email | ui | browser
   * @param {number | boolean} value if the notification is notif_by_browserReadOnly, false otherwise.
   */
  setNotifOptionEnabled(option, value) {
    switch (option) {
      case "email":
        this.set_notif_by_emailEnabled(value);
        break;
      case "ui":
      case "alert":
        this.set_notif_by_uiEnabled(value);
        break;
      case "browser":
        this.set_notif_by_browserEnabled(value);
        break;
      default:
        throw new Error(`Option ${option} didn't exist. Only 'email', 'ui', 'alert' and 'browser' are available.`);
    }
  }
  /**
   * Returns the value of readOnly dependig of the option.
   * @param {string} option email | ui | browser
   * @returns {boolean} The value of ReadOnly depending of the option.
   */
  getNotifOptionReadOnly(option) {
    switch (option) {
      case "email":
        return this.notif_by_emailReadOnly;
      case "ui":
      case "alert":
        return this.notif_by_uiReadOnly;
      case "browser":
        return this.notif_by_browserReadOnly;
      case "center":
        return this.readOnly;
      default:
        throw new Error(`Option ${option} didn't exist. Only 'email', 'ui', 'alert' and 'browser' are available.`);
    }
  }
  /**
   * Change the value of ReadOnly dependig of the option and call updateTheSettings.
   * @param {string} option email | ui | browser
   * @param {number | boolean} value if the notification is notif_by_browserReadOnly, false otherwise.
   */
  setNotifOptionReadOnly(option, value) {
    switch (option) {
      case "email":
        this.set_notif_by_emailReadOnly(value);
        break;
      case "ui":
      case "alert":
        this.set_notif_by_uiReadOnly(value);
        break;
      case "browser":
        this.set_notif_by_browserReadOnly(value);
        break;
      default:
        throw new Error(`Option ${option} didn't exist. Only 'email', 'ui', 'alert' and 'browser' are available.`);
    }
  }
  /**
   * Resets the unsubscription date.
   */
  resetUnsubscriptionDate() {
    this.unsubscribe_date = null;
  }
  /**
   * Checks if the given ID is present in the group list.
   *
   * @param {any} id - The ID to check.
   * @returns {boolean} - Returns true if the ID is present in the group list, otherwise returns false.
   */
  isInGroupList(id2) {
    return this.groupList.includes(id2);
  }
  /**
   * Sets the group list.
   *
   * @param {Array} groupList - The new group list.
   * @returns {void}
   */
  setGroupList(groupList) {
    this.groupList = groupList;
  }
  /**
   * Adds an ID to the group list.
   *
   * @param {any} id - The ID to be added to the group list.
   * @returns {void}
   */
  addIdToGroupList(id2) {
    if (!this.groupList.includes(id2))
      this.groupList.push(id2);
  }
}
const useSettingsStore = /* @__PURE__ */ defineStore("settings", () => {
  const settingState = reactive({
    loaded: {
      settings: false
    },
    isLoading: {
      settings: false
    }
  });
  const setSettingLoadState = (key, value) => {
    settingState.loaded[key] = value;
  };
  const setSettingLoadingState = (key, value) => {
    settingState.isLoading[key] = value;
  };
  const windowShow = ref("CENTER");
  const isDND = ref(false);
  const isDNDRefreshed = ref(false);
  const appSettingOpened = ref(false);
  const tenants = reactive({
    tenants: []
  });
  const currentSetting = ref(null);
  const setCurrentSetting = (id2) => {
    currentSetting.value = id2;
  };
  const resetCurrentSetting = () => {
    currentSetting.value = null;
  };
  const currentTenant = ref(null);
  const userSettings = reactive(/* @__PURE__ */ new Map());
  const ServicesSettings = reactive(/* @__PURE__ */ new Map());
  const settings2 = ref(userSettings);
  const getSetting = (id2) => {
    return settings2.value.get(id2) ?? null;
  };
  const resetSettingUnsubDate = (id2) => {
    const s = getSetting(id2);
    if (s)
      s.unsubscribe_date = null;
  };
  watch(
    () => windowShow.value,
    (newValue) => {
      settings2.value = newValue === "PLATFORM_PREFERENCES" ? ServicesSettings : userSettings;
    },
    { immediate: true }
  );
  const unsubscribe_dateSettingList = ref([]);
  const isInUnsubscribe_dateSettingList = (id2) => {
    return unsubscribe_dateSettingList.value.includes(id2);
  };
  const addUnsubscribe_dateSetting = (id2) => {
    if (!isInUnsubscribe_dateSettingList(id2)) {
      unsubscribe_dateSettingList.value.push(id2);
    }
  };
  const removeUnsubscribe_dateSetting = (id2) => {
    const index = unsubscribe_dateSettingList.value.indexOf(id2);
    if (index !== -1) {
      unsubscribe_dateSettingList.value.splice(index, 1);
    }
  };
  const settingsList = reactive(/* @__PURE__ */ new Map());
  const listOfService = ref([]);
  const platformPreferencesAccess = ref(false);
  const refreshNotificationCenter = ref(false);
  const nameofUpdatedSetting = ref(null);
  const hidePlatformSelection = ref(true);
  const isTenantAgnostic = ref(0);
  const setPlatformPreferencesAccess = (userRole) => {
    platformPreferencesAccess.value = !!userRole;
  };
  const setWindoShow = (value) => {
    const allowedWindow = ["CENTER", "PREFERENCES", "PLATFORM_PREFERENCES"];
    if (!allowedWindow.includes(value)) {
      return;
    }
    windowShow.value = value;
  };
  const isPlatformPreferences = computed(() => windowShow.value === "PLATFORM_PREFERENCES");
  const setDNDStatus2 = (value) => {
    isDND.value = value.isDND;
    isDNDRefreshed.value = true;
  };
  const setAppSettingOpened = (value) => {
    appSettingOpened.value = value;
  };
  const setCurrentTenant = (tenant) => {
    currentTenant.value = tenant;
  };
  const getCurentTenantName = () => {
    return tenants.tenants.find((tenant) => tenant.PLATFORMID.toLowerCase() === currentTenant.value.toLowerCase()).NAME;
  };
  const setTenantAgnosticData = (data) => {
    hidePlatformSelection.value = data.hidePlatformSelection;
    isTenantAgnostic.value = data.isTenantAgnostic;
  };
  const setListOfService = (data) => {
    listOfService.value = data.services;
    setCurrentTenant(data.currentTenant);
  };
  const setNameofUpdatedSetting = (data) => {
    nameofUpdatedSetting.value = data.setting;
  };
  const setTenantsData = (data) => {
    tenants.tenants = data;
  };
  const getIndividualSettingIds = (id2) => {
    const setting = getSetting(id2);
    return setting ? setting.groupList : [];
  };
  const getSettingGroup = (setting, settingsMap = null) => {
    for (const [, value] of settingsMap ? settingsMap.entries() : settings2.value.entries()) {
      if (value.isGroup && value.service === setting.service) {
        return value;
      }
    }
    return null;
  };
  const isSettingGroup = (setting) => {
    const thatSetting = getSetting(setting.id);
    return thatSetting && thatSetting.isGroup;
  };
  const addSettingIdToGroupList = (group, settingId) => {
    group.addIdToGroupList(settingId);
  };
  const getAllGroupSettings = computed(() => {
    const groupSettings = [];
    settings2.value.forEach((setting) => {
      if (setting.isGroup) {
        groupSettings.push(setting);
      }
    });
    return groupSettings;
  });
  const getGroupIndividualSettingsId = (group) => {
    const groupSettingsId = [];
    group.groupList.forEach((id2) => {
      const setting = getSetting(id2);
      if (setting && !setting.isGroup) {
        groupSettingsId.push(id2);
      }
    });
    return groupSettingsId;
  };
  const addSetting = (setting, settingsMap) => {
    var _a3;
    const newSetting = new Setting(setting);
    const existingSetting = settingsMap.get(setting.ID) ?? null;
    if (existingSetting) {
      for (const key in setting) {
        if (key.toLowerCase() !== "servicename" && key.toLowerCase() !== "id" && key.toLowerCase() !== "name" && !((_a3 = existingSetting[key.toLowerCase()]) == null ? void 0 : _a3.readOnly)) {
          existingSetting[key.toLowerCase()] = newSetting[key.toLowerCase()];
        }
      }
    } else {
      settingsMap.set(newSetting.id, newSetting);
      if (newSetting.unsubscribe_date !== null) {
        addUnsubscribe_dateSetting(newSetting.id);
      }
    }
  };
  const setSettingsData = (data) => {
    for (let i = 0; i < data.settings.length; i++) {
      if (data.settings[i].service !== "X3DNTFC_AP") {
        addSetting(data.settings[i], userSettings);
      }
    }
    userSettings.forEach((setting) => {
      const group = getSettingGroup(setting, userSettings);
      if (group) {
        addSettingIdToGroupList(group, setting.id);
      }
    });
  };
  const setServicesSettingsData = (data) => {
    for (let i = 0; i < data.settings.length; i++) {
      if (data.settings[i].service !== "X3DNTFC_AP") {
        addSetting(data.settings[i], ServicesSettings);
      }
    }
    ServicesSettings.forEach((setting) => {
      const group = getSettingGroup(setting, ServicesSettings);
      if (group) {
        addSettingIdToGroupList(group, setting.id);
      }
    });
  };
  const setSettingData = (data) => {
    addSetting(data, userSettings);
  };
  return {
    refreshNotificationCenter,
    unsubscribe_dateSettingList,
    listOfService,
    nameofUpdatedSetting,
    tenants,
    settings: settings2,
    settingsList,
    isDND,
    isDNDRefreshed,
    currentTenant,
    appSettingOpened,
    getAllGroupSettings,
    settingState,
    currentSetting,
    hidePlatformSelection,
    isTenantAgnostic,
    getPlatformPreferencesAccess: () => platformPreferencesAccess.value,
    getDNDStatus: () => isDND.value,
    getDNDRefreshedStatus: () => isDNDRefreshed.value,
    getCurrentTenant: () => currentTenant.value,
    getTenants: () => tenants.tenants,
    getListOfService: () => listOfService.value,
    getWindowShow: () => windowShow.value,
    setPlatformPreferencesAccess,
    setCurrentSetting,
    resetCurrentSetting,
    getGroupIndividualSettingsId,
    getSettingGroup,
    getSetting,
    isSettingGroup,
    isPlatformPreferences,
    getIndividualSettingIds,
    addSettingIdToGroupList,
    addSetting,
    setTenantAgnosticData,
    setSettingLoadState,
    setSettingLoadingState,
    setListOfService,
    setCurrentTenant,
    getCurentTenantName,
    setTenantsData,
    setNameofUpdatedSetting,
    setSettingsData,
    setServicesSettingsData,
    setSettingData,
    setWindoShow,
    setDNDStatus: setDNDStatus2,
    setAppSettingOpened,
    addUnsubscribe_dateSetting,
    removeUnsubscribe_dateSetting,
    resetSettingUnsubDate
  };
});
function NotificationTrackerUsage() {
  const { UWA: UWA2 } = useUWA();
  const notificationsStore = useNotificationsStore();
  const report = {
    appID: "X3DNTFC_AP"
  };
  const centerActionTracker = () => {
    const openNotifWith = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "openNotifWith";
      report.eventLabel = "Open notification with";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Open notification with Analytics request failed : ${error}`);
      }
    };
    const openNotifInNewTab = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "openNotifInNewTab";
      report.eventLabel = "Open notification in new tab";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Open notification in new tab Analytics request failed : ${error}`);
      }
    };
    const notifClick = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "notifClick";
      report.eventLabel = "Click on a notification";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notification click Analytics request failed : ${error}`);
      }
    };
    const profileClick = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "profileClick";
      report.eventLabel = "Click on a profile";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Profile click Analytics request failed : ${error}`);
      }
    };
    const viewAllMergeNotif = async (data = {}) => {
      const { nbMerge, persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "viewAllMergeNotif";
      report.eventLabel = "View all merge Notifications";
      report.eventValue = nbMerge;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`View all merge Notifications Analytics request failed : ${error}`);
      }
    };
    const hideAllMergeNotif = async (data = {}) => {
      const { nbMerge, persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "hideAllMergeNotif";
      report.eventLabel = "Hide all merge Notifications";
      report.eventValue = nbMerge;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Hide all merge Notifications Analytics request failed : ${error}`);
      }
    };
    const notificationStar = async (data = {}) => {
      const { persDim, persVal, starred: starred2 } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = starred2 ? "notificationStarred" : "notificationUnstarred";
      report.eventLabel = `${starred2 ? "Sta" : "Unsta"}r a notification`;
      report.eventValue = starred2 ? 1 : 0;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notification starred Analytics request failed : ${error}`);
      }
    };
    const notificationRead2 = async (data = {}) => {
      const { persDim, persVal, read: read2 } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = read2 ? "notificationRead" : "notificationUnread";
      report.eventLabel = `${read2 ? "R" : "Unr"}ead a notification`;
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notification read Analytics request failed : ${error}`);
      }
    };
    const cancelNotificationDeletion = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "cancelNotificationDeletion";
      report.eventLabel = "Cancel notification deletion";
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Cancel notification deletion Analytics request failed : ${error}`);
      }
    };
    const notificationDelete2 = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "notificationDelete";
      report.eventLabel = "Delete a notification";
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notification delete Analytics request failed : ${error}`);
      }
    };
    return {
      openNotifWith,
      viewAllMergeNotif,
      hideAllMergeNotif,
      notificationRead: notificationRead2,
      notificationDelete: notificationDelete2,
      notificationStar,
      openNotifInNewTab,
      notifClick,
      profileClick,
      cancelNotificationDeletion
    };
  };
  const multipleSelectionActionTracker = () => {
    const selectionAction = async (data = {}) => {
      const { persDim, persVal, action, length } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = action;
      report.eventLabel = `Multiple selection action: ${action}`;
      report.eventValue = length;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Multiple selection action Analytics request failed : ${error}`);
      }
    };
    const enableSelection = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = "enableSelection";
      report.eventLabel = "Enable multiple selection";
      report.eventValue = 1;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Enable multiple selection Analytics request failed : ${error}`);
      }
    };
    const disableSelection = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = "disableSelection";
      report.eventLabel = "Disable multiple selection";
      report.eventValue = 0;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Disable multiple selection Analytics request failed : ${error}`);
      }
    };
    const readSelectedNotifications = async (data = {}) => {
      const { persDim, persVal, read: read2, length } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = read2 ? "readSelectedNotifications" : "unreadSelectedNotifications";
      report.eventLabel = `${read2 ? "R" : "Unr"}ead selected notification(s)`;
      report.eventValue = length;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Read/Unred selected notifications Analytics request failed  : ${error}`);
      }
    };
    const deleteSelectedNotifications = async (data = {}) => {
      const { persDim, persVal, length } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = "deleteSelectedNotifications";
      report.eventLabel = "Delete selected notification(s)";
      report.eventValue = length;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Delete selected notifications Analytics request failed : ${error}`);
      }
    };
    const starSelectedNotifications = async (data = {}) => {
      const { persDim, persVal, starred: starred2, length } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = starred2 ? "starSelectedNotifications" : "unstarSelectedNotifications";
      report.eventLabel = `${starred2 ? "Sta" : "Unsta"}r selected notification(s)`;
      report.eventValue = length;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Star/Unstar selected notifications Analytics request failed : ${error}`);
      }
    };
    const unsubscribeSelectedNotifications = async (data = {}) => {
      const { persDim, persVal, length } = data;
      report.eventCategory = "notification.selection";
      report.eventAction = "unsubscribeSelectedNotifications";
      report.eventLabel = "Unsubscribe selected notification(s)";
      report.eventValue = length;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Unsubscribe selected notifications Analytics request failed : ${error}`);
      }
    };
    return {
      readSelectedNotifications,
      deleteSelectedNotifications,
      starSelectedNotifications,
      unsubscribeSelectedNotifications,
      enableSelection,
      disableSelection,
      selectionAction
    };
  };
  const settingIconActionTracker = () => {
    const invokenotifSettingView = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "invokenotifSettingView";
      report.eventLabel = "Open settings view";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`invokenotifSettingView Analytics request failed : ${error}`);
      }
    };
    const deleteAllNotifications = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "deleteAllNotifications";
      report.eventLabel = "Delete all notifications";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Delete all notifications Analytics request failed : ${error}`);
      }
    };
    const deleteAllFilteredNotifications = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "deleteAllFilteredNotifications";
      report.eventLabel = "Delete all filtered notifications";
      report.eventValue = notificationsStore.filterNotifIds.length;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Delete all filtered notifications Analytics request failed : ${error}`);
      }
    };
    const readAllNotifications2 = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "readAllNotifications";
      report.eventLabel = "Read all notifications";
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Read all notifications Analytics request failed : ${error}`);
      }
    };
    const unreadAllNotifications = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "unreadAllNotifications";
      report.eventLabel = "Unread all notifications";
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Unread all notifications Analytics request failed : ${error}`);
      }
    };
    const readAllFilteredNotifications = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "readAllFilteredNotifications";
      report.eventLabel = "Read all filtered notifications";
      report.eventValue = notificationsStore.filterTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Read all filtered notifications Analytics request failed : ${error}`);
      }
    };
    const unreadAllFilteredNotifications = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "unreadAllFilteredNotifications";
      report.eventLabel = "Unread all filtered notifications";
      report.eventValue = notificationsStore.filterTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Unread all filtered notifications Analytics request failed : ${error}`);
      }
    };
    const switchTenantNotifications = async (data = {}) => {
      const { persDim, persVal, tenant } = data;
      report.eventCategory = "notification.settingsicon.v2";
      report.eventAction = "switchTenantNotifications";
      report.eventLabel = `Switch to ${tenant === "all" ? "all" : `current (${tenant})`} tenant notifications`;
      report.eventValue = tenant === "all" ? 1 : 0;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Switch tenant notifications Analytics request failed : ${error}`);
      }
    };
    return {
      invokenotifSettingView,
      deleteAllNotifications,
      deleteAllFilteredNotifications,
      readAllNotifications: readAllNotifications2,
      readAllFilteredNotifications,
      unreadAllNotifications,
      unreadAllFilteredNotifications,
      switchTenantNotifications
    };
  };
  const notificationSettingViewTracker = () => {
    const notifServiceOrSettings = async (data = {}) => {
      const { getSetting } = useSettingsStore();
      const { persVal, setting } = data;
      report.eventCategory = "notification.settingsview.v2";
      report.eventValue = setting.SUBSCRIBE;
      report.persVal = persVal;
      const defautSetting = getSetting(setting.ID);
      if (defautSetting) {
        report.persDim = {
          ppd1: defautSetting.name,
          pd2: defautSetting.servicename,
          pd3: defautSetting.service
        };
        if (defautSetting.subscribe !== setting.SUBSCRIBE) {
          if (setting.SUBSCRIBE === 0) {
            report.eventAction = "disableNotifServiceOrSettings";
            report.eventLabel = "Disable notification service or settings";
          } else {
            report.eventAction = "enableNotifServiceOrSettings";
            report.eventLabel = "Enable notification service or settings";
          }
          try {
            const trackerAPI = await getTrackerAPI();
            trackerAPI.trackPageEvent(report);
          } catch (error) {
            UWA2.log(`Notification service or settings Analytics request failed : ${error}`);
          }
        }
      }
    };
    const unsubscribeNotification = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsview.v2";
      report.eventAction = "unsubscribeNotification";
      report.eventLabel = "Unsubscribe notification";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Unsubscribe notification Analytics request failed : ${error}`);
      }
    };
    const resetUnsubscribeNotificationDate = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsview.v2";
      report.eventAction = "resetUnsubscribeNotificationDate";
      report.eventLabel = "Reset unsubscribe notification date";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Reset unsubscribe notification date Analytics request failed : ${error}`);
      }
    };
    const notifAlertSetting = async (data = {}) => {
      const { persVal, setting } = data;
      const { getSetting } = useSettingsStore();
      report.eventCategory = "notification.settingsview.v2";
      report.persVal = persVal;
      const defautSetting = getSetting(setting.ID);
      if (defautSetting) {
        report.persDim = {
          pd1: defautSetting.name,
          pd2: defautSetting.servicename,
          pd3: defautSetting.service
        };
        if (setting.NOTIF_BY_UI === 0 && defautSetting.notif_by_ui !== setting.NOTIF_BY_UI) {
          report.eventAction = "disableNotifAlert";
          report.eventLabel = "Disable notification alert setting";
          report.eventValue = setting.NOTIF_BY_UI;
        } else if (setting.NOTIF_BY_UI === 1 && defautSetting.notif_by_ui !== setting.NOTIF_BY_UI) {
          report.eventAction = "enableNotifAlert";
          report.eventLabel = "Enable notification alert setting";
          report.eventValue = setting.NOTIF_BY_UI;
        }
        if (setting.NOTIF_BY_EMAIL === 0 && defautSetting.notif_by_email !== setting.NOTIF_BY_EMAIL) {
          report.eventAction = "disableNotifMail";
          report.eventLabel = "Disable notification mail setting";
          report.eventValue = setting.NOTIF_BY_EMAIL;
        } else if (setting.NOTIF_BY_EMAIL === 1 && defautSetting.notif_by_email !== setting.NOTIF_BY_EMAIL) {
          report.eventAction = "enableNotifMail";
          report.eventLabel = "Enable notification mail setting";
          report.eventValue = setting.NOTIF_BY_EMAIL;
        }
        if (setting.NOTIF_BY_BROWSER === 0 && defautSetting.notif_by_browser !== setting.NOTIF_BY_BROWSER) {
          report.eventAction = "disableNotifBrowser";
          report.eventLabel = "Disable notification browser setting";
          report.eventValue = setting.NOTIF_BY_BROWSER;
        } else if (setting.NOTIF_BY_BROWSER === 1 && defautSetting.notif_by_browser !== setting.NOTIF_BY_BROWSER) {
          report.eventAction = "enableNotifBrowser";
          report.eventLabel = "Enable notification browser setting";
          report.eventValue = setting.NOTIF_BY_BROWSER;
        }
        try {
          const trackerAPI = await getTrackerAPI();
          trackerAPI.trackPageEvent(report);
        } catch (error) {
          UWA2.log(`Notification alert settings Analytics request failed : ${error}`);
        }
      }
    };
    const serviceSettingsView = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.settingsview.v2";
      report.eventAction = "serviceSettingsView";
      report.eventLabel = "Specific service settings";
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Service settings view Analytics request failed : ${error}`);
      }
    };
    const backtoNotificationSettingsView = async () => {
      report.eventCategory = "notification.settingsview.v2";
      report.eventAction = "backtoNotificationSettingsView";
      report.eventLabel = "Backword navigation from service view";
      report.persDim = {
        pd1: "showSettings"
      };
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Back to notification settings view Analytics request failed : ${error}`);
      }
    };
    const backtoNotificationCenterView = async () => {
      report.eventCategory = "notification.settingsview.v2";
      report.eventAction = "backtoNotificationCenterView";
      report.eventLabel = "Backword navigation from setting view";
      report.persDim = {
        pd1: "showCenter"
      };
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Back to notification center view Analytics request failed : ${error}`);
      }
    };
    return {
      notifServiceOrSettings,
      unsubscribeNotification,
      resetUnsubscribeNotificationDate,
      notifAlertSetting,
      serviceSettingsView,
      backtoNotificationSettingsView,
      backtoNotificationCenterView
    };
  };
  const notificationFilterViewTracker = () => {
    const clicknotifFilterIconToShow = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.filterview.v2";
      report.eventAction = "clicknotifFilterIconToShow";
      report.eventLabel = "Click on notification filter icon to show filter";
      report.eventValue = 1;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Click on notification filter icon to show filter Analytics request failed : ${error}`);
      }
    };
    const clicknotifFilterIconToHide = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.filterview.v2";
      report.eventAction = "clicknotifFilterIconToHide";
      report.eventLabel = "Click on notification filter icon to hide filter";
      report.eventValue = 0;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Click on notification filter icon to hide filter Analytics request failed : ${error}`);
      }
    };
    const clickFilterButton = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.filterview.v2";
      report.eventAction = "clickFilterButton";
      report.eventLabel = "Filter button click";
      report.eventValue = notificationsStore.filterTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Click on filter button Analytics request failed : ${error}`);
      }
    };
    const cancelFilterView = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.filterview.v2";
      report.eventAction = "cancelFilterView";
      report.eventLabel = "Filter panel closed";
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Filter panel closed view Analytics request failed : ${error}`);
      }
    };
    const resetFilterFields = async (data = {}) => {
      const { persDim, persVal } = data;
      report.eventCategory = "notification.filterview.v2";
      report.eventAction = "resetFilterFields";
      report.eventLabel = "Reset filter panel fields";
      report.eventValue = notificationsStore.unreadTotal;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Reset filter fields Analytics request failed : ${error}`);
      }
    };
    return {
      clicknotifFilterIconToShow,
      clicknotifFilterIconToHide,
      clickFilterButton,
      cancelFilterView,
      resetFilterFields
    };
  };
  const notificationsLoadTimeTracker = () => {
    const notificationLoaded = async (data = {}) => {
      const { persDim, persVal, time } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "notificationLoaded";
      report.eventLabel = "Loading time in Notification Center in milliseconds";
      report.eventValue = time;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notifications load time Analytics request failed : ${error}`);
      }
    };
    const notificationFilterLoaded = async (data = {}) => {
      const { persDim, persVal, time } = data;
      report.eventCategory = "notification.center.v2";
      report.eventAction = "notificationFilterLoaded";
      report.eventLabel = "Loading time in Notification center Filter in milliseconds";
      report.eventValue = time;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notification filter load time Analytics request failed : ${error}`);
      }
    };
    const notificationSettingsLoaded = async (data = {}) => {
      const { persDim, persVal, time } = data;
      report.eventCategory = "notification.settingsview.v2";
      report.eventAction = "notificationSettingsLoaded";
      report.eventLabel = "Loading time of Settings in milliseconds";
      report.eventValue = time;
      report.persDim = persDim;
      report.persVal = persVal;
      try {
        const trackerAPI = await getTrackerAPI();
        trackerAPI.trackPageEvent(report);
      } catch (error) {
        UWA2.log(`Notification settings load time Analytics request failed : ${error}`);
      }
    };
    return {
      notificationLoaded,
      notificationFilterLoaded,
      notificationSettingsLoaded
    };
  };
  return {
    centerActionTracker,
    settingIconActionTracker,
    notificationSettingViewTracker,
    notificationFilterViewTracker,
    notificationsLoadTimeTracker,
    multipleSelectionActionTracker
  };
}
const mobileDevice = window.ds && window.ds.env === "MOBILE" || window.top && window.top.ds && window.top.ds.env === "MOBILE";
const useFilterStore = /* @__PURE__ */ defineStore("filter", () => {
  const notifStore = useNotificationsStore();
  const applied = ref(true);
  const filterNotifId = ref([]);
  const userTenants = ref([]);
  const opened = ref(false);
  const filter2 = reactive({
    first_date: null,
    // Calendar is empty
    last_date: null,
    // Calendar is empty
    searches: [],
    read: false,
    unread: true,
    // Default unread checkbox is checked
    starred: false,
    unstarred: false,
    // TODO federation
    tenants: []
  });
  const isDefaultUnread = () => {
    const noBoxes = filter2.read === false && filter2.unread === true && filter2.starred === false && filter2.unstarred === false;
    const noSearches = filter2.searches.length === 0;
    const noDates = filter2.first_date == null && filter2.last_date == null;
    return noBoxes && noSearches && noDates;
  };
  const getFilter = () => {
    const base = {
      ...filter2,
      oldID: notifStore.oldID,
      groupIDs: notifStore.groupIDs,
      archive: notifStore.archive,
      clusterId: notifStore.clusterId
    };
    if (isDefaultUnread()) {
      base.unread = true;
    }
    return base;
  };
  const resetFilterFields = () => {
    filter2.first_date = null;
    filter2.last_date = null;
    filter2.searches = [];
    filter2.read = false;
    filter2.unread = true;
    filter2.starred = false;
    filter2.unstarred = false;
    filter2.tenants = [];
    applied.value = true;
  };
  const needToBeSaved = () => {
    return filter2.first_date !== null || filter2.last_date !== null || filter2.searches.length || filter2.read !== false || filter2.unread !== true || filter2.starred !== false || filter2.unstarred !== false || filter2.tenants.length;
  };
  const setOpened = (o) => {
    opened.value = o;
  };
  const saveToLocalStorage = async () => {
    applied.value = true;
    localStorage.removeItem("notification-center-filter");
    if (needToBeSaved()) {
      if (filter2.first_date !== null && filter2.first_date !== "") {
        filter2.first_date.setHours(0, 0, 0, 0);
      }
      if (filter2.last_date !== null && filter2.last_date !== "") {
        filter2.last_date.setHours(23, 59, 59, 999);
      }
      localStorage.setItem("notification-center-filter", JSON.stringify(filter2));
      if (!filter2.last_date && !filter2.first_date && !filter2.read && !filter2.unread && !filter2.starred && !filter2.unstarred && filter2.searches.length === 0) {
        applied.value = false;
      } else {
        applied.value = true;
      }
    }
  };
  const resetFilter = () => {
    resetFilterFields();
    saveToLocalStorage();
  };
  const getFilterInStorage = () => {
    if (localStorage.getItem("notification-center-filter")) {
      const filterStorage = JSON.parse(localStorage.getItem("notification-center-filter"));
      applied.value = true;
      if (filterStorage.first_date) {
        filter2.first_date = new Date(filterStorage.first_date);
      } else {
        filter2.first_date = null;
      }
      if (filterStorage.last_date) {
        filter2.last_date = new Date(filterStorage.last_date);
      } else {
        filter2.last_date = null;
      }
      filter2.searches = filterStorage.searches;
      filter2.read = filterStorage.read;
      filter2.unread = filterStorage.unread;
      filter2.starred = filterStorage.starred;
      filter2.unstarred = filterStorage.unstarred;
      filter2.tenants = filterStorage.tenants;
      if (!filter2.last_date && !filter2.first_date && !filter2.read && !filter2.unread && !filter2.starred && !filter2.unstarred && filter2.searches.length === 0) {
        applied.value = false;
      } else {
        applied.value = true;
      }
    }
  };
  const loadFilter = () => {
    getFilterInStorage();
  };
  const addSearch = (search) => {
    if (search.value.trim().length) {
      if (!filter2.searches.includes(search.value.trim())) {
        if (filter2.searches.length < 6) {
          filter2.searches.push(search.value.trim());
        }
        search.value = "";
      }
    }
  };
  watch(
    isDefaultUnread,
    (defaultunread) => {
      if (defaultunread) {
        applied.value = true;
      }
    },
    { immediate: true }
  );
  return {
    applied,
    userTenants,
    filterNotifId,
    filter: filter2,
    opened,
    isFilterApplied: () => applied.value || isDefaultUnread(),
    getFilter,
    resetFilter,
    loadFilter,
    setOpened,
    saveToLocalStorage,
    resetFilterFields,
    addSearch
  };
});
const { $i18n: $i18n$8 } = useTranslations$1();
const apps = [];
const myWindowParent = ((_b = window.dsDashboardWindow) == null ? void 0 : _b.parent) || window.parent;
const myWindowTop = ((_c2 = window.dsDashboardWindow) == null ? void 0 : _c2.top) || window.top;
const myTop = ((_d2 = window.dsDashboardWindow) == null ? void 0 : _d2.top) || top;
async function isWebInWinContext() {
  const CompassData = await getCompassData();
  const addinmode = CompassData && CompassData.addinMode && CompassData.addinMode.value || (typeof window.widget !== "undefined" && typeof window.widget.getValue === "function" ? window.widget.getValue("addinmode") : window.addinmode) || "";
  return addinmode === "solidworks" || addinmode === "catiav5" || myWindowParent.document.location.search.includes("addinmode=solidworks") || myWindowParent.document.location.search.includes("addinmode=catiav5");
}
function useNotificationActions() {
  const { UWA: UWA2 } = useUWA();
  const openIconUrl = async (icon) => {
    const WAFData = await getWAFData();
    const imgUrl = icon && decodeURIComponent(icon);
    const loginExists = imgUrl.includes("login/");
    let loginId, baseSwymUrl, baseUrl;
    if (loginExists) {
      try {
        loginId = imgUrl.split("login/")[1].split("/")[0];
      } catch (e) {
        console.error(e);
      }
    }
    if (!loginId)
      return;
    let login;
    let tenant;
    const matches = window.location.href.match(
      /https:\/\/([A-Za-z0-9]+)-([A-Za-z0-9]+)-[A-Za-z0-9]+(-|\.)/
    );
    if (matches && matches.length === 4 && (matches[3] === "-" || matches[1] && matches[1].length > 4)) {
      tenant = matches[1];
      tenant = tenant.toUpperCase();
    }
    if (myWindowParent.ds.env === "3DSwym" || myWindowParent.ds.env === "MOBILE") {
      const notifAvatarSrc = decodeURIComponent(icon);
      const notifIconLogin = notifAvatarSrc.includes("login");
      if (notifIconLogin) {
        login = notifAvatarSrc.split("login/")[1].split("/")[0];
      }
      const i3DXCompassPlatformServices = await getI3DXCompassPlatformServices();
      i3DXCompassPlatformServices.getServiceUrl({
        serviceName: "3DSwym",
        platformId: tenant,
        onComplete(dataUrl) {
          if (typeof dataUrl === "object") {
            baseSwymUrl = dataUrl[0].url;
          } else if (typeof dataUrl === "string") {
            baseSwymUrl = dataUrl;
          }
          try {
            baseSwymUrl = WAFData.proxifyUrl(baseSwymUrl, { proxy: "passport" });
          } catch (e) {
            console.error(e);
          }
          baseUrl = `${baseSwymUrl}/#people:${login}/activities`;
          myWindowParent.location.href = baseUrl;
        }
      });
    } else {
      const appKey = "X3DPRFL_AP";
      const appTitle = "My Profile";
      const appData = {
        login: loginId,
        url: baseUrl,
        x3dPlatformId: tenant
      };
      const TransientWidget = await getTransientWidget();
      const Transient = window !== myTop && myTop.require ? myWindowTop.require("DS/TransientWidget/TransientWidget") : TransientWidget;
      Transient.showWidget(appKey, appTitle, appData);
    }
  };
  const getLocation = (href) => {
    if (!href)
      return;
    try {
      const url2 = new URL(href);
      return {
        protocol: url2.protocol,
        // includes ":" at the end
        host: url2.host,
        // hostname + port
        hostname: url2.hostname,
        // just hostname
        port: url2.port,
        // port as string
        pathname: url2.pathname,
        // path
        search: url2.search,
        // query string, includes "?"
        hash: url2.hash
        // hash, includes "#"
      };
    } catch (e) {
      return void 0;
    }
  };
  const getServiceForNotification = (urlInfos) => {
    if (!urlInfos)
      return;
    let regExpMatch;
    if (urlInfos && urlInfos.search) {
      regExpMatch = urlInfos.search.match(/.*&serviceForNotification=([^&]*)/);
    }
    if (regExpMatch && regExpMatch[1]) {
      return regExpMatch[1].toLowerCase();
    }
  };
  const openInNewTab2 = (url2) => {
    if (!url2)
      return;
    return window.open(url2, "_blank");
  };
  const isSameService = async (url1, url2) => {
    return UWA2.Utils.matchUrl(url1, url2);
  };
  const openInSameTab = async (url2, notifOptions, hash) => {
    const PlatformAPI = await getPlatformAPI();
    const urlInfos = getLocation(url2);
    if (urlInfos.hash === hash) {
      if (url2.contains("ifweloop") || url2.contains("innovate") || url2.contains("engineering")) {
        myWindowParent.document.location.href = url2;
      } else {
        PlatformAPI.publish("common.3DNotification.topic", {
          action: "refresh",
          data: {
            url: url2
          }
        });
      }
    } else {
      const platformId = notifOptions.platformId;
      if (notifOptions.provider === "X3DOPMR_AP" && platformId) {
        if (myWindowParent.document.location.hash.contains("app:X3DOPMR_AP")) {
          const urlPlatform = UWA2.Utils.getQueryString(myWindowParent.document.location.hash, "content:x3dPlatformId");
          if (urlPlatform) {
            myWindowParent.document.location.hash = myWindowParent.document.location.hash.replace(
              urlPlatform,
              platformId.trim()
            );
          } else {
            myWindowParent.document.location.hash = `${myWindowParent.document.location.hash}/content:x3dPlatformId=${platformId.trim()}`;
          }
        } else {
          myWindowParent.document.location.hash = `${urlInfos.hash}/content:x3dPlatformId=${platformId.trim()}`;
        }
      } else {
        if (notifOptions.provider === "USASWYM_AP") {
          PlatformAPI.publish("common.3DNotification.topic", {
            action: "refresh",
            data: {
              url: url2
            }
          });
        } else {
          if (urlInfos.hash && urlInfos.hash !== "")
            myWindowParent.document.location.hash = urlInfos.hash;
          else if (urlInfos.search && urlInfos.search !== "")
            myWindowParent.document.location.hash = `app:${notifOptions.provider}/content:${urlInfos.search}`;
          else myWindowParent.document.location.hash = urlInfos.hash;
        }
      }
    }
  };
  const openNotification = async (url2, notifOptions, _openInNewTab = false) => {
    const PlatformAPI = await getPlatformAPI();
    if (!url2)
      return console.error("[NotificationCenter openNotification] No url to navigate.");
    if (_openInNewTab || !notifOptions.platformId) {
      return openInNewTab2(url2);
    }
    let currentPlatformId = notifOptions.currentTenant || PlatformAPI.getTenant();
    if (!currentPlatformId) {
      const getTenant = (href) => {
        const match = href.match(/^([A-Z0-9]+)([\-.])(.*)$/i);
        return match && {
          platformId: match[1]
        };
      };
      const hostInfos = getTenant(myWindowParent.location.host);
      if (hostInfos && hostInfos.platformId) {
        currentPlatformId = hostInfos.platformId;
      } else {
        console.warn("[NotificationCenter openNotification] Unknown current tenant.");
        return openInNewTab2(url2);
      }
    }
    const urlInfos = getLocation(url2);
    if (notifOptions.platformId.toLowerCase() === (currentPlatformId == null ? void 0 : currentPlatformId.toLowerCase())) {
      const currentApp2 = notifOptions.appName && notifOptions.appName.toLowerCase();
      const serviceForNotification = getServiceForNotification(urlInfos);
      const sameService = await isSameService(url2, myWindowParent.location);
      if (sameService || serviceForNotification === currentApp2) {
        const isDashboard = currentApp2 === "3ddashboard" || urlInfos.host && urlInfos.host.contains("ifwe") && !urlInfos.host.contains("ifweloop");
        const TransientWidget = await getTransientWidget();
        const Transient = window !== myTop && myTop.require ? myWindowTop.require("DS/TransientWidget/TransientWidget") : TransientWidget;
        const hasTransientApp = Transient.isWidgetOpen();
        const isTransientOpenable = urlInfos && urlInfos.hash && urlInfos.hash.contains("app") || serviceForNotification === currentApp2;
        if (!isDashboard || isDashboard && !hasTransientApp && isTransientOpenable) {
          return openInSameTab(url2, notifOptions, myWindowParent.location.hash);
        }
      } else {
        const isNotifFromSwym = notifOptions.provider === "USASWYM_AP";
        const isMy3DExperience = currentApp2 === "my3dexperience" || urlInfos.host && urlInfos.host.contains("my3dexperience");
        const isNativeApp = mobileDevice || ["localhost", "127.0.0.1", "", "::1"].includes(window.location.hostname);
        if (isNotifFromSwym && (isMy3DExperience || isNativeApp)) {
          return openInSameTab(url2, notifOptions, myWindowParent.location.hash);
        }
      }
    }
    if (notifOptions.provider === "X3DOPMR_AP" && notifOptions.platformId) {
      url2 = `${url2}/content:x3dPlatformId=${notifOptions.platformId.trim()}`;
    }
    return openInNewTab2(url2);
  };
  const updateActionedLabel = async (notification, label, flag) => {
    if (notification.isGroup) {
      label = $i18n$8("groupActionLabel");
    } else if (label && label.toString().includes("NLS")) {
      if (label === "NLS.actionLabel") {
        label = $i18n$8("actionLabel");
      } else {
        label = $i18n$8("actionFailed");
      }
    } else if (flag && Number.parseInt(notification.SHARED) === 1) {
      const PlatformAPI = await getPlatformAPI();
      const actor = ` (by ${PlatformAPI.getUser().login})`;
      if (label)
        label = label + actor;
    } else {
      if (notification.ACTOR_DATA)
        label = `${label} (by ${notification.ACTOR_DATA})`;
    }
    notification.ACTION = label || $i18n$8("actionLabel");
  };
  const checkAction = (action) => {
    if (action.options && action.options.event && action.options.event.type)
      return true;
    if (action.type)
      return true;
    else return false;
  };
  const post = async (notification, options, action_label) => {
    const WAFData = await getWAFData();
    if (!options || !options.url) {
      return UWA2.log("Notification Post Failed some parameters are missing !");
    }
    WAFData.authenticatedRequest(options.url, {
      method: "POST",
      data: options.params,
      headers: options.headers,
      onComplete(res) {
        UWA2.log("Notification Post Action Completed !");
        let temp;
        if (res) {
          try {
            temp = JSON.parse(res);
          } catch (e) {
            UWA2.log(`Notification post Action Error for json parse${e}`);
            updateActionedLabel(notification, $i18n$8("actionFailed"), false);
          }
        }
        action_label = temp && temp.result && temp.result.success ? action_label || "NLS.actionLabel" : "NLS.actionFailed";
        updateActionedLabel(notification, action_label, false);
        if (!notification.isRead) {
          if (notification.isGroup) {
            notificationRead({
              action: "notificationRead",
              id: notification.ID,
              groupID: notification.GROUPID,
              hiddenMerged: true,
              read: true,
              clusterId: notification.CLUSTER_ID
            });
          } else {
            notificationRead({
              action: "notificationRead",
              id: notification.ID,
              read: true,
              clusterId: notification.CLUSTER_ID
            });
          }
        }
        notificationRead({
          action: "actioned",
          clusterId: notification.CLUSTER_ID,
          params: { isMerge: notification.isMerge, id: notification.ID, actioned: true }
        });
        notificationRead({
          action: "notificationRead",
          updateLabel: "updateLabel",
          clusterId: notification.CLUSTER_ID,
          params: {
            isMerge: notification.isMerge,
            flag: false,
            id: notification.ID,
            label: temp && temp.result && temp.result.success ? "NLS.actionLabel" : "NLS.actionFailed"
          }
        });
      },
      onFailure() {
        UWA2.log("Notification Post Action Failed !");
        updateActionedLabel(notification, $i18n$8("actionFailed"), false);
      }
    });
  };
  const csrf = async (notification, options, action_label) => {
    const WAFData = await getWAFData();
    if (options && options.csrf) {
      WAFData.authenticatedRequest(options.csrf, {
        method: "GET",
        data: options.params,
        onComplete(evt) {
          UWA2.log("Notification csrf Action Completed !");
          let rep;
          try {
            rep = JSON.parse(evt);
          } catch (e) {
            UWA2.log(`Notification Csrf Action Error for json parse${e}`);
            updateActionedLabel(notification, $i18n$8("actionFailed"), false);
          }
          if (rep) {
            options.headers = { "X-DS-SWYM-CSRFTOKEN": rep.result.ServerToken };
            post(notification, options, action_label);
          }
        }
      });
    } else {
      UWA2.log("Notification Csrf Action Failed some parameters are missing !");
    }
  };
  const getAppInfo = async (appId, platformId) => {
    var _a3, _b2;
    if (!appId || !platformId)
      return;
    const appInfo = (_a3 = apps[appId]) == null ? void 0 : _a3.platforms[platformId];
    if (appInfo) {
      return appInfo;
    } else {
      const i3DXCompassPlatformServices = await getI3DXCompassPlatformServices();
      await new Promise((resolve2, reject) => i3DXCompassPlatformServices.getAppInfo({
        appId,
        onComplete(app) {
          UWA2.log(app);
          if (!app)
            return;
          apps[appId] = {};
          apps[appId].platforms = [];
          if (!app.platforms)
            return;
          if (app.platforms.length === 0 && app.platformId) {
            apps[appId].platforms[app.platformId] = {
              serviceId: app.serviceId,
              url: app.launchUrl
            };
          } else {
            for (const platform of app.platforms) {
              apps[appId].platforms[platform.id] = {
                serviceId: app.serviceId,
                url: platform.launchUrl
              };
            }
          }
          resolve2();
        },
        onError(error) {
          reject(error);
        }
      }));
    }
    return (_b2 = apps[appId]) == null ? void 0 : _b2.platforms[platformId];
  };
  const activateGetAction = async (notification, eventOptions, _openInNewTab = false, notifOptions) => {
    if (!eventOptions || !eventOptions.url && !eventOptions.uri)
      return;
    let url2 = eventOptions.url;
    try {
      const addinmode = await isWebInWinContext();
      if (addinmode && !_openInNewTab) {
        if (eventOptions.service === "3DPlan" && myWindowParent.document.location.href.includes("addinmode=solidworks")) {
          const planURL = new URL(url2);
          const planSearch = planURL.search.replace(
            "?",
            "?addinmode=solidworks&integratedSearch=false&add_taxonomies_in_select_clause=true&"
          );
          return myWindowParent.document.location.href = planURL.origin + planURL.pathname + planSearch + planURL.hash;
        }
        let hash = eventOptions.uri.charAt(0) === "/" ? eventOptions.uri.slice(1) : eventOptions.uri;
        if (!hash && url2) {
          const urlInfos2 = getLocation(url2);
          if (urlInfos2.hash && urlInfos2.hash !== "")
            hash = urlInfos2.hash;
          else if (urlInfos2.search && urlInfos2.search !== "")
            hash = `app:${notification.APPID}/content:${urlInfos2.search}`;
        }
        if (hash && hash !== "")
          return myWindowParent.document.location.hash = hash;
      }
    } catch (error) {
      console.error(`[NotificationCenter activateGetAction] Failed to get addinmode value: ${error}`);
    }
    const urlInfos = getLocation(url2);
    const isNotifFromSwym = notification.APPID === "USASWYM_AP";
    const isNativeApp = mobileDevice || ["localhost", "127.0.0.1", "", "::1"].includes(window.location.hostname);
    if (isNativeApp) {
      if (isNotifFromSwym && !_openInNewTab) {
        const uri = eventOptions.uri ?? (urlInfos && urlInfos.hash);
        if (uri) {
          url2 = `${window.location.origin}/${uri}`;
          openNotification(url2, notifOptions, _openInNewTab);
        }
      }
      _openInNewTab = true;
    }
    if (isNotifFromSwym) {
      if (!urlInfos || !urlInfos.host || !urlInfos.host.includes("3dswym") && !urlInfos.host.includes("my3dexperience")) {
        const platformId = notification.PLATFORMID;
        if (!platformId) {
          return console.error(
            "[NotificationCenter activateGetAction] Navigation aborted - Missing tenant and url in notification."
          );
        }
        const appInfo = await getAppInfo(notification.APPID, platformId);
        if (appInfo && appInfo.url) {
          url2 = appInfo.url + eventOptions.uri;
        } else {
          const i3DXCompassPlatformServices = await getI3DXCompassPlatformServices();
          await new Promise((resolve2) => i3DXCompassPlatformServices.getServiceUrl({
            serviceName: "3DSwym",
            platformId,
            onComplete(dataUrl) {
              if (typeof dataUrl === "object")
                url2 = dataUrl[0].url + eventOptions.uri;
              else if (typeof dataUrl === "string")
                url2 = dataUrl + eventOptions.uri;
              resolve2();
            }
          })).catch((reason) => console.warn(`[NotificationCenter activateGetAction] Failed to get 3DSwym url: ${reason}`));
        }
      }
      if (!url2)
        return console.error("[NotificationCenter activateGetAction] Navigation aborted - Empty url.");
      openNotification(url2, notifOptions, _openInNewTab);
    } else {
      if (!url2) {
        const platformId = notification.PLATFORMID;
        if (!platformId) {
          return console.error(
            "[NotificationCenter activateGetAction] Navigation aborted - Missing tenant and url in notification."
          );
        }
        const appInfo = await getAppInfo(notification.APPID, platformId);
        if (!appInfo || !appInfo.url)
          return console.error("[NotificationCenter activateGetAction] Navigation aborted - Failed to build url.");
        url2 = appInfo.url + eventOptions.uri;
      }
      if (!url2)
        return console.error("[NotificationCenter activateGetAction] Navigation aborted - Empty url.");
      openNotification(url2, notifOptions, _openInNewTab);
    }
    if (!notification.isRead) {
      if (notification.isGroup) {
        notificationRead({
          action: "notificationRead",
          id: notification.ID,
          groupID: notification.GROUPID,
          hiddenMerged: true,
          read: true,
          clusterId: notification.CLUSTER_ID
        });
      } else {
        notificationRead({
          action: "notificationRead",
          id: notification.ID,
          read: true,
          clusterId: notification.CLUSTER_ID
        });
      }
      notificationRead({
        action: "actioned",
        clusterId: notification.CLUSTER_ID,
        params: { id: notification.ID, actioned: true, isMerge: notification.isMerge }
      });
    }
  };
  const activatePostAction = (notification, eventOptions, action_label) => {
    if (!eventOptions || !eventOptions.url)
      return;
    if (eventOptions.csrf) {
      csrf(
        notification,
        {
          csrf: eventOptions.csrf,
          url: eventOptions.url,
          params: eventOptions.options && eventOptions.options.data ? eventOptions.options.data : void 0,
          headers: eventOptions.options && eventOptions.options.headers ? eventOptions.options.headers : void 0
        },
        action_label
      );
    } else {
      post(
        notification,
        {
          url: eventOptions.url,
          params: eventOptions.options && eventOptions.options.data ? eventOptions.options.data : void 0,
          headers: eventOptions.options && eventOptions.options.headers ? eventOptions.options.headers : void 0
        },
        action_label
      );
    }
  };
  const checkUrlAction = (options) => {
    if (options.event.options && options.event.options.type)
      return true;
    else return false;
  };
  const activateLinkAction = (notification, notifOptions, _openInNewTab) => {
    if (!checkUrlAction(notifOptions.options))
      return;
    const eventOptions = notifOptions.options.event.options;
    const action_label = notifOptions.options.actioned_label;
    eventOptions.appID = notifOptions.provider;
    if (eventOptions.type === "GET") {
      activateGetAction(notification, eventOptions, _openInNewTab, notifOptions);
    } else if (eventOptions.type === "POST") {
      activatePostAction(notification, eventOptions, action_label);
    }
  };
  const linkClickAction = (notification, notifOptions, _openInNewTab) => {
    activateLinkAction(notification, notifOptions, _openInNewTab);
  };
  const url = (notification, notifOptions) => {
    if (notifOptions.type === "POST") {
      activatePostAction(notification, notifOptions, notifOptions.actioned_label);
    } else {
      if (!notifOptions || !notifOptions.type || !notifOptions.options)
        return;
      if (notifOptions.type === "link") {
        let _openInNewTab = false;
        if (notifOptions.appName) {
          _openInNewTab = !(notifOptions.appName === "3ddashboard" || notifOptions.appName === "3dswym" || notifOptions.appName === "my3dexperience" || notifOptions.appName === "ifweloop");
        }
        linkClickAction(notification, notifOptions, _openInNewTab);
        return;
      }
      if (notifOptions.type === "button") {
        if (notifOptions.isActioned)
          return;
        if (notifOptions.options.event.type === "UI" && notifOptions.options.event.options) {
          const options = notifOptions.options.event.options;
          options.attachments.forEach((attachment) => {
            if (attachment.type === "button") {
              if (attachment.options && attachment.options.event && attachment.options.event.options) {
                attachment.options.event.options.options = {};
                const denyReasons = { value: notifOptions.reason.reason, reference: notifOptions.reason.reference };
                attachment.options.event.options.options.data = [denyReasons];
                !attachment ? activateLinkAction(notification, notifOptions) : activateLinkAction(notification, attachment);
              }
            }
          });
        } else {
          activateLinkAction(notification, notifOptions);
        }
      }
    }
  };
  const getEventOptions = (option) => {
    if (option && option.event && option.event.options) {
      const options = option.event.options;
      if (options.topic && options.data) {
        return {
          topic: options.topic,
          data: options.data
        };
      } else {
        UWA2.log("3DNotification Error eventOptions missing topic or data");
      }
    } else {
      UWA2.log("3DNotification Error eventOptions");
    }
  };
  const bus = (notification, notifOptions) => {
    if (!notifOptions || !notifOptions.type || !notifOptions.options)
      return;
    const eventOptions = getEventOptions(notifOptions.options);
    if (!eventOptions)
      return;
    if (notifOptions.type === "link") {
      publish({ topic: eventOptions.topic, data: eventOptions.data });
      if (!notification.isRead) {
        if (notification.isGroup) {
          notificationRead({
            action: "notificationRead",
            id: notification.ID,
            groupID: notification.GROUPID,
            hiddenMerged: true,
            read: true,
            clusterId: notification.CLUSTER_ID
          });
        } else {
          notificationRead({
            action: "notificationRead",
            id: notification.ID,
            read: true,
            clusterId: notification.CLUSTER_ID
          });
        }
      }
    } else if (notifOptions.type === "button") {
      if (eventOptions.data && eventOptions.data.action === "rollback") {
        eventOptions.data.id = notification.ID;
      }
      if (!notification.isActioned) {
        publish({ topic: eventOptions.topic, data: eventOptions.data });
        if (!notification.isRead) {
          if (notification.isGroup) {
            notificationRead({
              action: "notificationRead",
              id: notification.ID,
              groupID: notification.GROUPID,
              hiddenMerged: true,
              read: true,
              clusterId: notification.CLUSTER_ID
            });
          } else {
            notificationRead({
              action: "notificationRead",
              id: notification.ID,
              read: true,
              clusterId: notification.CLUSTER_ID
            });
          }
        }
        notificationRead({
          action: "actioned",
          clusterId: notification.CLUSTER_ID,
          params: { id: notification.ID, actioned: true, isMerge: notification.isMerge }
        });
      }
    }
  };
  const callEvent = (notification, options) => {
    options.action.isActioned = notification.isActioned;
    if (checkAction(options.action) || options.subAction) {
      if (options.type === "UI" || options.type === "url") {
        url(notification, options.action);
      } else {
        bus(notification, options.action);
      }
    }
  };
  const noticationMessageIsLink = (actions) => {
    if (!actions)
      return false;
    const link = actions.find((action) => action.type === "link");
    const isLink = !!(link && link.options && link.options.event);
    return { option: isLink && link || null, isLink };
  };
  const executeActionSelect = (notification, i) => {
    const { getGroup, getMergeById } = useNotificationsStore();
    const mergesIds = getGroup(notification.GROUPID);
    mergesIds.forEach((m) => {
      const merge = getMergeById(m.id);
      const findAction = merge.MESSAGE.actions[0];
      const action = {
        type: findAction.options.event.type,
        subAction: true,
        action: Array.isArray(findAction.options.event.options) ? findAction.options.event.options[i] : findAction.options.event.options
      };
      if (!merge.isActioned)
        callEvent(merge, action);
      if (!merge.isRead) {
        notificationRead({
          action: "notificationRead",
          id: merge.ID,
          read: true,
          clusterId: notification.CLUSTER_ID
        });
      }
    });
    updateActionedLabel(notification, "do not matter", false);
    notification.actionNotification();
  };
  const executeDeny = (notification, reason) => {
    const { getGroup, getMergeById } = useNotificationsStore();
    const mergesIds = getGroup(notification.GROUPID);
    mergesIds.forEach((item) => {
      const merge = getMergeById(item.id);
      const findAction = merge.MESSAGE.actions[1];
      const action = {
        type: findAction.options.event.type,
        action: findAction
      };
      action.action.reason = reason;
      callEvent(merge, action);
      if (!merge.isRead) {
        notificationRead({
          action: "notificationRead",
          id: merge.ID,
          read: true,
          clusterId: notification.CLUSTER_ID
        });
      }
    });
  };
  const getOpenWithMenu = (notification, openWith2) => {
    const apps2 = [];
    return new Promise((resolve2, reject) => {
      openWith2.retrieveCompatibleApps((appsList) => {
        if (!appsList || appsList.length === 0) {
          reject(new Error("No compatible apps found"));
        } else {
          appsList.forEach((app) => {
            apps2.push({
              text: app.text,
              icon: app.icon,
              fonticon: app.fonticon,
              className: app.className,
              name: `openWithApp-${app.text}`,
              handler() {
                if (!notification.isRead) {
                  if (notification.isGroup) {
                    notificationRead({
                      action: "notificationRead",
                      id: notification.ID,
                      groupID: notification.GROUPID,
                      hiddenMerged: true,
                      read: true,
                      clusterId: notification.CLUSTER_ID
                    });
                  } else {
                    notificationRead({
                      action: "notificationRead",
                      id: notification.ID,
                      read: true,
                      clusterId: notification.CLUSTER_ID
                    });
                  }
                }
                app.handler();
                const { centerActionTracker } = NotificationTrackerUsage();
                centerActionTracker().openNotifWith({
                  persDim: {
                    pd1: `${app.text}`
                  }
                });
              }
            });
          });
          resolve2(apps2);
        }
      });
    });
  };
  const getCsrfUrl = () => {
    const { getListOfService } = useSettingsStore();
    const csrf2 = "/api/index/tk/";
    return getListOfService()[0]["3dswym"] + csrf2;
  };
  const getOpenWithActions = async (notification, options) => {
    try {
      const OpenWith = await getOpenWith();
      const encodedUri = options[0].options.event.options.uri;
      if (encodedUri) {
        const decodedUri = decodeURIComponent(encodedUri).replace(/\+/g, " ");
        const x3dcontentUri = decodedUri.match(/X3DContentId=.+\}/);
        if (x3dcontentUri && x3dcontentUri[0] && x3dcontentUri[0].split("=") && x3dcontentUri[0].split("=")[1]) {
          const content = JSON.parse(x3dcontentUri[0].split("=")[1]);
          UWA2.log(`Content:  ${content}`);
          const openwith = new OpenWith();
          openwith.set3DXContent(content);
          const items = await getOpenWithMenu(notification, openwith);
          const data = {
            text: $i18n$8("openWith"),
            fonticon: "fonticon fonticon-open-menu-dot ",
            items
          };
          return data;
        }
      }
      return null;
    } catch (err) {
      UWA2.log("Warning: Content not received in the uri");
      return null;
    }
  };
  const denyAction = async ({ notification, options, reasonValues, denyDisabled, actionAfterFetch }) => {
    const { alertPromptNotice } = usePreferencesManagement();
    if (options.type !== "url") {
      let reason = "";
      let message = options.action.options.event.options.attachments[0].text;
      const placeholder = options.action.options.event.options.attachments[1].placeholder;
      const okLabel = options.action.options.event.options.attachments[2].options.label;
      const title = options.action.options.event.options.title;
      const reference = options.action.options.event.options.attachments[1].reference;
      if (notification.isGroup) {
        if (message.includes("##MERGE_COUNT##")) {
          message = message.replace("##MERGE_COUNT##", notification.COUNT);
        } else {
          const data = options.action.options.event.options.attachments[0].data;
          if (message.indexOf(data.COMMUNITY_TITLE) < message.length - 1) {
            const substr = message.substr(0, message.indexOf(data.COMMUNITY_TITLE) + data.COMMUNITY_TITLE.length);
            message = `${substr}, ${notification.COUNT} ${$i18n$8("persons")}`;
          }
        }
      }
      try {
        const onConfirmVal = await alertPromptNotice({
          title,
          msg: message,
          okLabel,
          placeholder,
          required: false
        });
        reason = onConfirmVal;
        reasonValues.value = {
          reason,
          reference
        };
      } catch (onCloseVal) {
        denyDisabled.value = false;
        reason = onCloseVal;
        return;
      }
    }
    denyDisabled.value = true;
    if (notification.isGroup) {
      const { getNotificationById } = useNotificationsStore();
      const { getFilter } = useFilterStore();
      const group = getNotificationById(notification.ID);
      if (!group.mergesFetched) {
        getMerge({
          parentNotif: {
            id: notification.ID,
            groupID: notification.GROUPID,
            clusterId: notification.CLUSTERID,
            fromArchive: notification.FROM_ARCHIVE,
            ignoreSearchesForChildren: !!notification.ignoreSearchesForChildren
          },
          filterData: getFilter()
        });
        actionAfterFetch.value = true;
      } else {
        executeDeny(notification, reasonValues);
      }
    } else {
      options.action.reason = reasonValues.value;
      callEvent(notification, options);
    }
  };
  const acceptAction = ({ notification, options, i, selectDisabled, actionAfterFetch, iAfterFetch }) => {
    selectDisabled.value = true;
    if (notification.isGroup) {
      const { getNotificationById } = useNotificationsStore();
      const { getFilter } = useFilterStore();
      const group = getNotificationById(notification.ID);
      if (!group.mergesFetched) {
        getMerge({
          parentNotif: {
            id: notification.ID,
            groupID: notification.GROUPID,
            clusterId: notification.CLUSTERID,
            fromArchive: notification.FROM_ARCHIVE,
            ignoreSearchesForChildren: !!notification.ignoreSearchesForChildren
          },
          filterData: getFilter()
        });
        actionAfterFetch.value = true;
        iAfterFetch.value = i;
      } else {
        executeActionSelect(notification, i);
      }
    } else {
      callEvent(notification, options);
    }
  };
  const NavigateToLink = (notification, action) => {
    const notifOptions = {
      platformId: notification.PLATFORMID,
      currentTenant: notification.currentTenant,
      appName: notification.appName,
      provider: notification.APPID,
      options: action.value.options
    };
    linkClickAction(notification, notifOptions, false);
  };
  return {
    openIconUrl,
    openInNewTab: openInNewTab2,
    openInSameTab,
    openNotification,
    updateActionedLabel,
    activatePostAction,
    callEvent,
    linkClickAction,
    activateLinkAction,
    activateGetAction,
    checkAction,
    checkUrlAction,
    noticationMessageIsLink,
    executeActionSelect,
    executeDeny,
    getOpenWithMenu,
    getOpenWithActions,
    denyAction,
    acceptAction,
    NavigateToLink,
    getCsrfUrl
  };
}
function useNotificationCleaning() {
  const cleanNotificationData = (data) => {
    try {
      const hasCount = Object.hasOwn(data, "COUNT");
      data.TYPE = decodeURIComponent(escape(atob(data.TYPE)));
      data.MESSAGE = decodeURIComponent(escape(atob(data.MESSAGE)));
      if (hasCount) {
        data.MESSAGE = data.MESSAGE.replace("##MERGE_COUNT##", data.COUNT);
      }
      data.MESSAGE = JSON.parse(data.MESSAGE);
      if (hasCount) {
        data.MESSAGE.nls.msg = data.MESSAGE.nls.msg.replace(`(${data.COUNT})`, "");
      }
    } catch (error) {
      console.warn("No panic, it just means that the cleaning may have already been done.");
    }
  };
  return {
    cleanNotificationData
  };
}
const { $i18n: $i18n$7 } = useTranslations$1();
function useNotificationMenu() {
  const setNotificationRead = (notification) => {
    const data = {
      action: "notificationRead",
      id: notification.ID,
      archive: !!notification.FROM_ARCHIVE,
      read: !notification.isRead,
      clusterId: notification.CLUSTER_ID
    };
    if (notification.isGroup) {
      data.groupID = notification.GROUPID;
      data.hiddenMerged = true;
    }
    notificationRead(data);
  };
  const setNotificationMenu = async (notification) => {
    const { centerActionTracker, notificationSettingViewTracker } = NotificationTrackerUsage();
    const theCenterActionTracker = centerActionTracker();
    const settingsViewTracker = notificationSettingViewTracker();
    const { tryNotificationDeletion } = useNotificationManagement();
    const { noticationMessageIsLink, getOpenWithActions } = useNotificationActions();
    const settingStore = useSettingsStore();
    const isLink = noticationMessageIsLink(notification.MESSAGE.actions).isLink;
    const setting = settingStore.getSetting(notification.SERVICE_ID);
    let openApps = null;
    openApps = await getOpenWithActions(notification, notification.MESSAGE.actions);
    const menuOptions = [
      ...isLink ? [
        {
          fonticon: "fonticon fonticon-window",
          name: "newTab",
          text: $i18n$7("openInNewTab"),
          handler: () => {
            const { noticationMessageIsLink: noticationMessageIsLink2, linkClickAction } = useNotificationActions();
            const { option } = noticationMessageIsLink2(notification.MESSAGE.actions);
            const notifOptions = {
              platformId: notification.PLATFORMID,
              currentTenant: notification.currentTenant,
              appName: notification.appName,
              provider: notification.APPID,
              options: option.options,
              _appID: []
            };
            linkClickAction(notification, notifOptions, true);
            theCenterActionTracker.openNotifInNewTab();
          }
        }
      ] : [],
      ...openApps !== null ? [openApps] : [],
      ...isLink || openApps !== null ? [
        {
          class: "divider"
        }
      ] : [],
      {
        fonticon: notification.isRead ? "fonticon fonticon-bell" : "fonticon fonticon-bell-alt",
        name: "read",
        text: notification.isGroup ? notification.isRead ? $i18n$7("markGroupAsUnRead") : $i18n$7("markGroupAsRead") : notification.isRead ? $i18n$7("markAsUnread") : $i18n$7("markAsRead"),
        handler: () => {
          setNotificationRead(notification);
          theCenterActionTracker.notificationRead({ read: !notification.isRead });
        }
      },
      ...!notification.isMerge ? [
        {
          fonticon: "fonticon fonticon-star",
          name: "important",
          text: notification.isGroup ? notification.isStarred ? $i18n$7("removeGroupImportant") : $i18n$7("markGroupAsImportant") : notification.isStarred ? $i18n$7("removeImportant") : $i18n$7("markAsImportant"),
          handler: () => {
            const data = {
              action: "notificationRead",
              id: notification.ID,
              archive: !!notification.FROM_ARCHIVE,
              starred: "starred",
              param: notification.isStarred ? 0 : 1,
              clusterId: notification.CLUSTER_ID
            };
            if (notification.isGroup) {
              data.groupID = notification.GROUPID;
              data.hiddenMerged = true;
            }
            notificationRead(data);
            theCenterActionTracker.notificationStar({ starred: data.param });
          }
        }
      ] : [],
      {
        class: "divider"
      },
      ...!setting || setting.readOnly ? [] : [
        {
          fonticon: "fonticon fonticon-list-delete",
          name: "unsubscribe",
          text: $i18n$7("unsubscribe"),
          items: [
            {
              fonticon: "fonticon fonticon-list-delete",
              text: $i18n$7("unsubscribeForHour"),
              name: "anHour",
              handler: () => {
                unsubscribe({
                  id: notification.ID,
                  subscribe: 0,
                  forHour: true
                });
                settingsViewTracker.unsubscribeNotification({
                  persDim: {
                    pd1: setting.name,
                    pd2: setting.servicename,
                    pd3: setting.service,
                    pd4: "for an hour",
                    pd5: "notification menu"
                  }
                });
              }
            },
            {
              fonticon: "fonticon fonticon-list-delete",
              text: $i18n$7("unsubscribeIndefinitely"),
              name: "indefinetly",
              handler: () => {
                unsubscribe({
                  id: notification.ID,
                  subscribe: 0
                });
                settingsViewTracker.unsubscribeNotification({
                  persDim: {
                    pd1: setting.name,
                    pd2: setting.servicename,
                    pd3: setting.service,
                    pd4: "indefinitely",
                    pd5: "notification menu"
                  }
                });
              }
            }
          ]
        }
      ],
      {
        fonticon: "fonticon fonticon-trash",
        text: notification.isGroup ? $i18n$7("deleteGroup") : $i18n$7("delete"),
        name: "delete",
        handler: () => {
          tryNotificationDeletion({
            id: notification.ID,
            merge: notification.isMerge,
            group: notification.isGroup,
            archive: !!notification.FROM_ARCHIVE,
            withCancelation: false
          });
        }
      }
    ];
    notification.OPTIONS = menuOptions;
  };
  return {
    setNotificationMenu,
    setNotificationRead
  };
}
const { $i18n: $i18n$6 } = useTranslations$1();
function checkIfIsUrl(str) {
  try {
    const url = new URL(str);
    return Boolean(url);
  } catch {
    return false;
  }
}
function addAndSortNotifsDateDesc(array, el2) {
  if (array.find((item) => item.id === el2.id))
    return;
  let index = 0;
  for (const item of array) {
    if (el2.date >= item.date)
      break;
    index++;
  }
  array.splice(index, 0, el2);
}
function addAndSortIdsDesc(array, id2) {
  if (array.find((idItem) => idItem === id2))
    return;
  let index = 0;
  for (const idItem of array) {
    if (id2 >= idItem && idItem !== -1)
      break;
    index++;
  }
  array.splice(index, 0, id2);
}
function timeDiffByDays(date) {
  const today2 = new Date(Date.now());
  const diff = today2 - date;
  return Math.floor(diff / (1e3 * 60 * 60 * 24));
}
function replaceParenthesisStrong(input, replaceWith) {
  if (input.includes(`)`)) {
    const partToReplace = input.slice(0, input.indexOf(`)`) + `)`.length);
    return input.replace(partToReplace, replaceWith);
  } else if (input.includes(`</strong>`)) {
    const partToReplace = input.slice(0, input.indexOf(`</strong>`) + `</strong>`.length);
    return input.replace(partToReplace, replaceWith);
  }
  return input;
}
async function getDateMergeFormat(date) {
  if (timeDiffByDays(date) === 0) {
    return $i18n$6("today");
  } else if (timeDiffByDays(date) === 1) {
    return $i18n$6("yesterday");
  } else {
    const I18n = await getI18n();
    return date.toLocaleDateString(I18n.getCurrentLanguage(), {
      day: "numeric",
      month: "short",
      year: "numeric"
    });
  }
}
async function getDateSectionFormat(date) {
  if (timeDiffByDays(date) === 0) {
    return $i18n$6("today").toUpperCase();
  } else if (timeDiffByDays(date) === 1) {
    return $i18n$6("yesterday").toUpperCase();
  } else {
    const format = {
      weekday: "short",
      year: "numeric",
      month: "short",
      day: "numeric"
    };
    const I18n = await getI18n();
    return date.toLocaleDateString(I18n.getCurrentLanguage(), format).toUpperCase();
  }
}
function checkIfIdHasHyphen(id2) {
  try {
    if (id2.includes("-")) {
      return {
        id: id2.split("-")[0],
        clusterId: id2.split("-")[1]
      };
    }
    return false;
  } catch (error) {
    return false;
  }
}
function linkIds(id2, clusterId) {
  return `${id2}-${clusterId}`;
}
function useNotificationResolution() {
  const { getListOfService } = useSettingsStore();
  const getServiceUrl2 = (service) => {
    for (let i = 0; i < getListOfService().length; i++) {
      if (service.platformId) {
        if (getListOfService()[i].platformid.toLowerCase() === service.platformId.toLowerCase()) {
          if (getListOfService()[i][service.name.toLowerCase()]) {
            return getListOfService()[i][service.name.toLowerCase()];
          } else {
            if (!getListOfService()[i][service.name.toLowerCase()] && service.name.toLowerCase() === "3ddashboard") {
              for (let j = 0; j < getListOfService().length; j++) {
                if (getListOfService()[j][service.platformId.toLowerCase()])
                  return getListOfService()[j][service.name.toLowerCase()];
              }
            }
          }
        }
      } else {
        return getListOfService()[i][service.name.toLowerCase()] ? getListOfService()[i][service.name.toLowerCase()] : getListOfService()[i + 1] ? getListOfService()[i + 1][service.name.toLowerCase()] : void 0;
      }
    }
    if (getListOfService().length > 0) {
      return getListOfService()[0][service.name.toLowerCase()];
    }
    return void 0;
  };
  const getIconUrl = (data) => {
    let url;
    switch (data.icon.type) {
      case "url":
        if (typeof data.icon.data === "object") {
          if (data.platformId && data.platformId !== "null") {
            url = getServiceUrl2({ name: data.icon.data.service, platformId: data.platformId });
          } else {
            if (!data.icon.data.service) {
              if (checkIfIsUrl(data.icon.data)) {
                return data.icon.data;
              } else {
                return "./../../resources/en/1/webapps/NotifAlert/assets/icons/notificon.png";
              }
            } else {
              for (const letiable in getListOfService()[0]) {
                if (data.icon.data.service.toLowerCase() === letiable.toLowerCase()) {
                  return getListOfService()[0][letiable] + data.icon.data.uri;
                }
              }
            }
          }
        } else {
          return data.icon.data;
        }
        if (url)
          return url + data.icon.data.uri;
        return void 0;
      case "login":
        url = getServiceUrl2({ name: "3dswym", platformId: data.platformId });
        let swymApi;
        if (url === "/")
          swymApi = "api/user/getpicture/login/";
        else swymApi = "/api/user/getpicture/login/";
        return url + swymApi + data.icon.data;
      case "app":
        getAppInfos({ id: data.icon.data });
        return void 0;
    }
  };
  const resolveNotificationIcon = (notification) => {
    const message = notification.MESSAGE;
    if (message.icon) {
      switch (message.icon.type) {
        case "appID":
          getAppInfos({ appID: message.icon.data, notifID: notification.ID, clusterId: notification.CLUSTER_ID });
          message.icon.isLink = false;
          break;
        case "login":
        case "url":
          message.icon.src = getIconUrl({ icon: message.icon, platformId: notification.PLATFORMID });
          message.icon.isLink = true;
          break;
        default:
          message.icon.src = "./../../resources/en/1/webapps/NotifAlert/assets/icons/notificon.png";
          message.icon.isLink = false;
          break;
      }
    } else {
      message.icon = {
        src: "./../../resources/en/1/webapps/NotifAlert/assets/icons/notificon.png",
        isLink: false
      };
    }
    notification.MESSAGE = message;
  };
  const resolveNotificationAction = (notification) => {
    const message = notification.MESSAGE;
    if (message.actions) {
      for (let i = 0; i < message.actions.length; i++) {
        const action = message.actions[i];
        if (action.options && action.options.event && (action.options.event.type === "url" || action.options.event.type === "UI")) {
          let event = action.options.event;
          if (action.options && action.options.event && action.options.event.type === "UI") {
            const eventUI = action.options.event.options;
            if (eventUI.attachments && eventUI.attachments.length > 0) {
              for (let y = 0; y < eventUI.attachments.length; y++) {
                if (eventUI.attachments[y].type === "button") {
                  event = eventUI.attachments[y].options.event;
                }
              }
            }
          }
          if (event.options && event.options.service && event.options.uri) {
            const url = getServiceUrl2({ name: event.options.service, platformId: notification.PLATFORMID });
            event.options.url = url + event.options.uri;
            if (notification.APPID === "USASWYM_AP") {
              event.options.csrf = `${url}/api/index/tk/`;
            }
          } else if (action.type === "select" && event.options && event.options.length > 0) {
            for (let k = 0; k < event.options.length; k++) {
              const url = getServiceUrl2({ name: event.options[k].service, platformId: notification.PLATFORMID });
              event.options[k].url = url + event.options[k].uri;
              if (notification.APPID === "USASWYM_AP") {
                event.options[k].csrf = `${url}/api/index/tk/`;
              }
            }
          }
        }
      }
    }
    notification.MESSAGE = message;
  };
  return {
    resolveNotificationAction,
    resolveNotificationIcon,
    getIconUrl,
    getServiceUrl: getServiceUrl2
  };
}
let Notification$1 = class Notification2 {
  /**
   * Build a notification object.
   * @param {object} notification - A notification object.
   */
  constructor(notification, options, merge = false) {
    for (const [key, value] of Object.entries(notification)) {
      if (key === "CREATION_DATE" || key === "READ_DATE" || key === "ACTION_DATE") {
        this[key] = value !== null ? new Date(value) : null;
      } else {
        this[key] = value;
      }
    }
    this.CLUSTER_ID = options.clusterId || this.CLUSTER_ID;
    const idWithHyphen = checkIfIdHasHyphen(this.ID);
    if (!idWithHyphen) {
      this.NOTIF_ID = JSON.parse(JSON.stringify(this.ID));
      this.ID = linkIds(this.NOTIF_ID, this.CLUSTER_ID);
    } else {
      this.NOTIF_ID = Number.parseInt(idWithHyphen.id);
    }
    this.deleted = false;
    this.deleteTimeoutId = null;
    this.currentTenant = options.currentTenant;
    this.appName = options.appName;
    if (!merge) {
      if (Object.hasOwn(this, "COUNT") && this.COUNT > 1) {
        this.hasMerges = true;
        this.mergesFetched = false;
        this.mergesOpened = false;
        this.mergesRead = this.READ_DATE !== null ? this.COUNT : 0;
      }
    }
  }
  /**
   * Return notification options.
   */
  get getOptions() {
    return this.OPTIONS;
  }
  /**
   * Get the creation date to a format with options.
   * @param {string} local - The local string.
   * @param {object} option - The format option.
   * @returns {string} - The format outcome.
   */
  toLocaleDateString(local = void 0, option = {}) {
    return this.CREATION_DATE.toLocaleDateString(local, option);
  }
  /**
   * Returns the hour and minutes of the notification's creation date.
  //  * @param {string} format - The format for the date and time. (Optional).
   * @returns {string} The formatted hour and minutes.
   */
  getHourMinutes() {
    return new Intl.DateTimeFormat("fr", { hour: "2-digit", minute: "2-digit" }).format(this.CREATION_DATE);
  }
  /**
   * Get the timestamp of the creation date.
   */
  get getCreationDateToTimestamp() {
    return this.CREATION_DATE.getTime();
  }
  /**
   * Return true if icon is a link.
   */
  get isIconLink() {
    if (this.MESSAGE.icon) {
      if (this.MESSAGE.icon.isLink)
        return true;
    }
    return false;
  }
  /**
   * Todo: this is not reactive yet.
   */
  get imageSrc() {
    if (this.MESSAGE.icon) {
      if (this.MESSAGE.icon.src)
        return this.MESSAGE.icon.src;
    }
    return "./../../resources/en/1/webapps/NotifAlert/assets/icons/notificon.png";
  }
  /**
   * Return notification section id.
   */
  get getSection() {
    const date = new Date(this.CREATION_DATE.getTime());
    date.setHours(0, 0, 0, 0);
    return date.getTime();
  }
  /**
   * Return true if READ_DATE is null.
   */
  get isRead() {
    return this.READ_DATE !== null;
  }
  /**
   * Return true if ACTION is null.
   */
  get isActioned() {
    return !!(this.ACTION_DATE !== null && this.ACTION_DATE !== "0000-00-00 00:00:00");
  }
  /**
   * Return true if is starred.
   */
  get isStarred() {
    return this.STARRED === 1 || this.STARRED === "1";
  }
  /**
   * Return true if is Count > 1.
   */
  get isGroup() {
    return Object.hasOwn(this, "COUNT") && this.COUNT > 1;
  }
  /**
   * Return true if is Merge.
   */
  get isMerge() {
    return !Object.hasOwn(this, "COUNT");
  }
  /**
   * Performs an action for the notification.
   */
  actionNotification() {
    this.ACTION_DATE = (/* @__PURE__ */ new Date()).toISOString();
  }
};
class Section {
  /**
   * The Section constructor to create a new section with an id.
   * @param {number} id - The id is the notification date timestamp without the time.
   */
  constructor(id2) {
    __publicField(this, "id");
    __publicField(this, "title");
    __publicField(this, "notifs");
    this.notifs = [];
    if (typeof id2 === "number") {
      this.id = id2;
      this.title = ref(" ");
      const date = new Date(id2);
      getDateSectionFormat(date).then((resolvedTitle) => {
        this.title.value = resolvedTitle;
      });
    }
  }
  /**
   * Add an notification to notifs then sort it.
   * @param {object} item - { id, date }.
   */
  add(item) {
    const itemExists = this.notifs.filter((o) => o.id === item.id).length > 0;
    if (!itemExists) {
      addAndSortNotifsDateDesc(this.notifs, item);
    }
  }
  /**
   * Removes a notification with the specified ID from the section.
   * @param {number} id - The ID of the notification to remove.
   */
  remove(id2) {
    this.notifs = this.notifs.filter((o) => o.id !== id2);
  }
}
const { $i18n: $i18n$5 } = useTranslations$1();
function useNotificationManagement() {
  const store = useNotificationsStore();
  const settingStore = useSettingsStore();
  const refreshCenterView2 = (withSettings = true, withServicesSettings = false) => {
    const { isFilterApplied, getFilter } = useFilterStore();
    store.resetStates();
    store.setIsLoading(true);
    if (withSettings)
      getSettings();
    if (withServicesSettings)
      getServicesSettingsOfTenant();
    let payload;
    if (isFilterApplied()) {
      payload = getFilter();
    } else {
      payload = { ...getFilter(), unread: true };
      getUnreadTotal();
    }
    filter(payload);
  };
  const addNotificationToSection = (notification) => {
    const sectionId = notification.getSection;
    let section = store.sectionList.get(sectionId);
    if (!section) {
      section = new Section(sectionId);
      if (!store.sectionListIds.includes(sectionId)) {
        addAndSortIdsDesc(store.sectionListIds, sectionId);
      }
    }
    const notifItem = {
      id: notification.ID,
      groupID: Object.hasOwn(notification, "GROUPID") ? notification.GROUPID : null,
      date: notification.getCreationDateToTimestamp
    };
    section.add(notifItem);
    if (!store.sectionList.has(sectionId))
      store.sectionList.set(sectionId, section);
  };
  const refreshWithoutReset = (withSettings = true, groupIdToExclude = null) => {
    const { isFilterApplied, getFilter } = useFilterStore();
    if (withSettings)
      getSettings();
    let payload;
    if (isFilterApplied()) {
      payload = getFilter();
    } else {
      payload = { ...getFilter(), unread: true };
      getUnreadTotal();
    }
    if (groupIdToExclude) {
      if (payload.groupIDs && payload.groupIDs.length > 0 && payload.groupIDs.includes(groupIdToExclude)) {
        payload.groupIDs = payload.groupIDs.filter((groupId) => groupId !== groupIdToExclude);
      }
      payload.oldID = -1;
      payload.archive = false;
    }
    filter(payload);
  };
  const setNotif = (notif) => {
    if (Object.hasOwn(notif, "COUNT") && notif.COUNT > 1) {
      const groupSection = [];
      store.groups.set(notif.GROUPID, groupSection);
    }
    store.notifications.set(notif.ID, notif);
    addNotificationToSection(notif);
  };
  const isGroup = (id2) => {
    const notif = store.getNotificationById(id2);
    return notif !== null && notif.isGroup;
  };
  const fetchMoreNotificationsIfNecessary = () => {
    if (store.canLoadMoreHistory || store.canLoadMoreFromArchive) {
      const { isFilterApplied, getFilter } = useFilterStore();
      let hasFetched = false;
      if (isFilterApplied()) {
        if (store.notifications.size < 19) {
          if (store.oldID !== -1) {
            const data = {
              ...getFilter(),
              oldID: store.oldID,
              groupIDs: store.groupIDs,
              ...store.canLoadMoreFromArchive && {
                archive: true
              }
            };
            if (!store.isLoading)
              store.setLoadingMore(true);
            filter(data);
            hasFetched = true;
          }
        }
      } else {
        if (store.notifications.size < 40) {
          if (store.oldID !== -1) {
            const data = {
              type: "all",
              oldID: store.oldID,
              groupIDs: store.groupIDs,
              ...store.canLoadMoreFromArchive && {
                archive: true
              }
            };
            if (!store.isLoading)
              store.setLoadingMore(true);
            getHistory(data);
            getUnreadTotal();
            hasFetched = true;
          }
        }
      }
      if (!hasFetched) {
        store.setLoadingMore(false);
        store.setIsLoading(false);
      }
    } else {
      store.setLoadingMore(false);
      store.setIsLoading(false);
    }
  };
  const deleteNotification2 = (id2, editTotal = true, fetchMore = true) => {
    const { isFilterApplied } = useFilterStore();
    const notification = store.getNotificationById(id2);
    if (editTotal) {
      if (!notification.isRead && !notification.FROM_ARCHIVE)
        store.setUnreadTotal(store.unreadTotal - 1);
      if (isFilterApplied())
        store.setFilterTotal(store.filterTotal - 1);
    }
    store.selected.splice(store.selected.indexOf(id2), 1);
    const sectionId = store.getNotificationById(id2).getSection;
    const section = store.sectionList.get(sectionId);
    section == null ? void 0 : section.remove(id2);
    if ((section == null ? void 0 : section.notifs.length) === 0) {
      store.sectionList.delete(section.id);
      store.sectionListIds = store.sectionListIds.filter((item) => item !== section.id);
    }
    store.notifications.delete(id2);
    store.removeFilterNotifId(id2);
    if (isGroup(id2)) {
      const group = store.getNotificationById(id2);
      const merges = store.groups.get(group.GROUPID);
      const idsToDelete = [];
      merges.forEach((item) => {
        idsToDelete.push(item.id);
      });
      idsToDelete.forEach((id3) => {
        store.merges.delete(id3);
      });
      store.groups.delete(group.GROUPID);
    }
    if (store.notifications.size === 0) {
      store.setUnreadTotal(0);
      store.setFilterTotal(0);
    }
    if (fetchMore)
      fetchMoreNotificationsIfNecessary();
  };
  const addNotification2 = ({ notification, options, group = false, oldSectionId, oldNotifId }) => {
    let oldSection;
    if (group) {
      store.notifications.delete(oldNotifId);
      oldSection = store.sectionList.get(oldSectionId);
      oldSection.remove(oldNotifId);
      if (oldSection.notifs.length === 0) {
        store.sectionList.delete(oldSectionId);
        store.sectionListIds = store.sectionListIds.filter((item) => item !== oldSectionId);
      }
    }
    const notif = new Notification$1(notification, options);
    const notifToReplace = store.getNotificationById(notif.ID) ?? (notif.GROUPID !== null ? store.getNotificationByGroupId(notif.GROUPID) : null);
    if (notifToReplace && (notifToReplace.getSection !== notif.getSection || notifToReplace.CREATION_DATE !== notif.CREATION_DATE)) {
      deleteNotification2(notifToReplace.ID, false, false);
      setNotif(notif);
    } else if (!notifToReplace) {
      setNotif(notif);
    }
  };
  const setArchiveState = () => {
    const { isFilterApplied } = useFilterStore();
    if (store.canLoadMoreHistory) {
      store.setCanLoadMoreHistory(false);
      if (!store.canLoadMoreFromArchive) {
        store.setCanLoadMoreFromArchive(true);
        if (store.notifications.size < 40 && !isFilterApplied() || store.notifications.size < 19 && isFilterApplied()) {
          store.setLoadingMore(true);
        } else {
          store.setOldID(-1);
        }
      }
    } else {
      if (store.canLoadMoreFromArchive) {
        store.setCanLoadMoreFromArchive(false);
      }
    }
  };
  const setArchiveSectionMessage = () => {
    if (store.canLoadMoreFromArchive) {
      const { messageNotice } = usePreferencesManagement();
      messageNotice($i18n$5("archiveAlert"), 1e4);
    }
  };
  const addHistory = (data) => {
    var _a3, _b2, _c3, _d3, _e, _f2;
    const { resolveNotificationAction, resolveNotificationIcon } = useNotificationResolution();
    const { cleanNotificationData } = useNotificationCleaning();
    const { setNotificationMenu } = useNotificationMenu();
    const { hasArchiveNotifications: hasArchiveNotifications2 } = useNotificationManagement();
    const { isFilterApplied } = useFilterStore();
    const { addFilterNotifIds, oldID } = useNotificationsStore();
    const options = {
      appName: data.appName,
      currentTenant: data.currentTenant,
      clusterId: data.clusterId
    };
    let oldId = -1;
    const previousOldID = oldID;
    (_a3 = data.notifications) == null ? void 0 : _a3.forEach((notification) => {
      store.addOldID(notification.ID, notification.CREATION_DATE, data.clusterId);
      cleanNotificationData(notification);
      addNotification2({ notification, options });
      const notif = store.getNotificationById(`${notification.ID}-${data.clusterId}`);
      resolveNotificationIcon(notif);
      resolveNotificationAction(notif);
      setNotificationMenu(notif);
      if (notif.COUNT > 1) {
        store.addGroupId(notif.GROUPID);
      }
    });
    if (((_b2 = data.notifications) == null ? void 0 : _b2.length) >= 19) {
      oldId = ((_c3 = store.clusterInOldIDs(data.clusterId)) == null ? void 0 : _c3.OLD_ID) ?? oldId;
    } else {
      setArchiveState();
    }
    store.setOldID(oldId);
    if (isFilterApplied()) {
      if (data.count) {
        let total;
        if (data.archive && store.loadingMore) {
          total = store.filterTotal + (((_d3 = data.notifications) == null ? void 0 : _d3.length) ?? 0);
        } else {
          total = previousOldID === -1 ? data.count : store.filterTotal;
        }
        store.setFilterTotal(total);
        addFilterNotifIds(
          (_e = data.notifications) == null ? void 0 : _e.map((n) => n.ID),
          data.clusterId,
          data.archive
        );
      }
    } else {
      if (data.archive)
        store.setUnreadTotal(store.unreadTotal + (((_f2 = data.notifications) == null ? void 0 : _f2.length) ?? 0));
    }
    if (store.isLoading)
      store.setIsLoading(false);
    if (store.loadingMore)
      store.setLoadingMore(false);
    if (hasArchiveNotifications2(data.notifications))
      setArchiveSectionMessage();
  };
  const addMerge = (notification, options) => {
    const groupSection = store.groups.get(notification.GROUPID);
    const merge = new Notification$1(notification, options, true);
    if (groupSection !== void 0) {
      const item = {
        id: merge.ID,
        groupID: merge.GROUPID,
        date: merge.getCreationDateToTimestamp
      };
      addAndSortNotifsDateDesc(groupSection, item);
      store.groups.set(merge.GROUPID, groupSection);
      store.merges.set(merge.ID, merge);
    }
  };
  const setNotifMerges = (data) => {
    const { setNotificationMenu } = useNotificationMenu();
    const { cleanNotificationData } = useNotificationCleaning();
    const { resolveNotificationAction, resolveNotificationIcon } = useNotificationResolution();
    data.id = linkIds(data.id, data.clusterId);
    const main = store.getNotificationById(data.id);
    main.mergesRead = 0;
    const options = {
      appName: data.appName,
      currentTenant: data.currentTenant,
      clusterId: data.clusterId
    };
    data.notifications.forEach((notification) => {
      cleanNotificationData(notification);
      addMerge(notification, options);
      let id2 = checkIfIdHasHyphen(notification.ID);
      if (!id2) {
        id2 = `${notification.ID}-${data.clusterId}`;
      } else {
        id2 = notification.ID;
      }
      const merge = store.getMergeById(id2);
      resolveNotificationIcon(merge);
      resolveNotificationAction(merge);
      setNotificationMenu(merge);
      if (merge.READ_DATE !== null)
        main.mergesRead += 1;
    });
    if (main.mergesRead === main.COUNT) {
      if (!main.isRead) {
        main.READ_DATE = (/* @__PURE__ */ new Date()).toISOString();
      }
    } else if (main.isRead) {
      main.READ_DATE = null;
    }
    setNotificationMenu(main);
    main.mergesFetched = true;
  };
  const canBeAddedToGroup = (notification) => {
    if (notification.GROUPID === null)
      return false;
    const group = store.getNotificationByGroupId(notification.GROUPID);
    return group !== null;
  };
  const updateGroupNotification = (group, notification, merge = false, last = false) => {
    if (!merge)
      group.COUNT = group.COUNT + 1;
    else if (last)
      group.COUNT = 1;
    for (const [key, value] of Object.entries(notification)) {
      if (key === "CREATION_DATE" || key === "READ_DATE" || key === "ACTION_DATE") {
        group[key] = value !== null ? new Date(value) : null;
      } else if (key !== "COUNT") {
        group[key] = value;
      }
    }
  };
  const replaceGroupMessage = (notification) => {
    let newGroup = store.getNotificationById(notification.ID);
    if (!newGroup)
      newGroup = store.getNotificationByGroupId(notification.GROUPID);
    if (newGroup) {
      let currMsg = newGroup.MESSAGE.nls.msg;
      const newMessage = notification.APPID === "X3DDRIV_AP" ? `<strong>${newGroup.COUNT}</strong>` : `<strong>${newGroup.COUNT} ${$i18n$5("Persons")}</strong>`;
      currMsg = replaceParenthesisStrong(currMsg, newMessage);
      if (newGroup.MESSAGE.nls.data.NOTIFIER_MESSAGE) {
        currMsg = currMsg.replace(`. ${newGroup.MESSAGE.nls.data.NOTIFIER_MESSAGE}`, "");
      }
      newGroup.MESSAGE.nls.msg = currMsg;
    }
  };
  const addNotificationToGroup = (notification, merge = false, last = false) => {
    const group = store.getNotificationByGroupId(notification.GROUPID);
    const oldSectionId = group.getSection;
    const oldID = group.ID;
    const options = {
      currentTenant: group.currentTenant,
      appName: group.appName,
      clusterId: group.CLUSTER_ID
    };
    updateGroupNotification(group, notification, merge, last);
    addNotification2({ notification: group, options, group: true, oldSectionId, oldNotifId: oldID });
    if (!last)
      replaceGroupMessage(notification);
    if (notification.APPID === "X3DDRIV_AP") {
      refreshWithoutReset(false);
    }
  };
  const DeleteNotificationGroup = (id2) => {
    console.log("function not implement", id2);
  };
  const isMerge = (id2) => {
    const merge = store.getMergeById(id2);
    return merge !== null && merge.isMerge;
  };
  const deleteMerge = (id2) => {
    const merge = store.getMergeById(id2);
    if (merge !== void 0) {
      const groupSection = store.groups.get(merge.GROUPID);
      groupSection.splice(
        groupSection.findIndex((item) => item.id === id2),
        1
      );
      store.groups.set(merge.GROUPID, groupSection);
      store.merges.delete(id2);
    }
    const group = store.getNotificationByGroupId(merge.GROUPID);
    group.COUNT = group.COUNT - 1;
  };
  const mergesNumber = (groupId) => {
    return store.getNotificationByGroupId(groupId).COUNT;
  };
  const isIdMainId = (id2) => {
    const notification = store.getNotificationById(id2);
    return notification !== null;
  };
  const removeMerge = (id2) => {
    const { setNotificationMenu } = useNotificationMenu();
    const groupID = store.getMergeById(id2).GROUPID;
    deleteMerge(id2);
    const nbMerges = mergesNumber(groupID);
    if (nbMerges === 1) {
      const lastMerge = store.getMergeByGroupId(groupID);
      addNotificationToGroup(lastMerge, true, true);
      store.groups.delete(groupID);
      store.merges.delete(lastMerge.ID);
      const normalNotif = store.getNotificationByGroupId(groupID);
      setNotificationMenu(normalNotif);
    } else {
      if (isIdMainId(id2)) {
        const nextMerge = store.getMergeByGroupId(groupID);
        addNotificationToGroup(nextMerge, true, false);
        setNotificationMenu(nextMerge);
      } else {
        replaceGroupMessage(store.getNotificationByGroupId(groupID));
      }
    }
  };
  const removeNotification = (id2) => {
    const _isMerge = isMerge(id2);
    if (_isMerge) {
      removeMerge(id2);
    } else {
      deleteNotification2(id2);
    }
  };
  const removeNotificationGroup = (id2) => {
    deleteNotification2(id2);
  };
  const isNotificationGroup = (notification) => {
    return notification.isGroup || Object.hasOwn(notification, "COUNT") && notification.COUNT > 1;
  };
  const isNormalNotification = (id2) => {
    const notif = store.getNotificationById(id2);
    return notif !== null && !notif.isGroup && !notif.isMerge;
  };
  const mergeIsMain = (id2) => {
    const merge = store.getMergeById(id2);
    return merge !== null && store.notifications.has(merge.ID);
  };
  const mainIsMerge = (id2) => {
    const notif = store.getNotificationById(id2);
    return notif !== null && store.merges.has(notif.ID);
  };
  const tryNotificationDeletion = ({ id: id2, merge = false, group = false, archive = false, withCancelation = true }) => {
    const { centerActionTracker } = NotificationTrackerUsage();
    const theCenterActionTracker = centerActionTracker();
    let notification;
    if (group || !merge)
      notification = store.getNotificationById(id2);
    else notification = store.getMergeById(id2);
    if (withCancelation) {
      notification.deleted = true;
      clearTimeout(notification.deleteTimeoutId);
      notification.deleteTimeoutId = setTimeout(() => {
        if (notification.deleted) {
          if (group) {
            notificationDelete({
              id: id2,
              archive,
              clusterId: notification.CLUSTER_ID,
              groupID: notification.GROUPID,
              read: notification.isRead
            });
          } else {
            notificationDelete({
              id: id2,
              archive,
              clusterId: notification.CLUSTER_ID,
              read: notification.isRead
            });
          }
          theCenterActionTracker.notificationDelete({
            persDim: {
              pd1: notification.isMerge ? "merge" : notification.isGroup ? "group" : "normal"
            }
          });
        }
        clearTimeout(notification.deleteTimeoutId);
      }, 5e3);
    } else {
      if (group) {
        notificationDelete({
          id: id2,
          groupID: notification.GROUPID,
          read: notification.isRead,
          clusterId: notification.CLUSTER_ID,
          archive
        });
      } else {
        notificationDelete({
          id: id2,
          clusterId: notification.CLUSTER_ID,
          read: notification.isRead,
          archive
        });
      }
      theCenterActionTracker.notificationDelete({
        persDim: {
          pd1: notification.isMerge ? "merge" : notification.isGroup ? "group" : "normal",
          pd4: `clusterId=${notification.CLUSTER_ID}`
        }
      });
    }
  };
  const notificationType = (id2) => {
    return isMerge(id2) ? "merge" : isGroup(id2) ? "group" : "normal";
  };
  const setNotificationReadState = (notifData, data) => {
    const { setNotificationMenu } = useNotificationMenu();
    if (data.read !== void 0) {
      if (!Object.hasOwn(notifData, "hasMerges")) {
        notifData.READ_DATE = data.read ? (/* @__PURE__ */ new Date()).toISOString() : null;
        store.setUnreadTotal(data.read ? store.unreadTotal - 1 : store.unreadTotal + 1);
      } else {
        notifData.READ_DATE = notifData.mergesRead === notifData.COUNT ? (/* @__PURE__ */ new Date()).toISOString() : null;
      }
    } else if (data.starred !== void 0) {
      notifData.STARRED = data.param;
    }
    setNotificationMenu(notifData);
  };
  const setMergeReadState = (mergeData, data) => {
    const { setNotificationMenu } = useNotificationMenu();
    const mainNotification = store.getNotificationByGroupId(mergeData.GROUPID);
    if (data.read !== void 0) {
      if (data.read) {
        if (!mergeData.isRead)
          mainNotification.mergesRead = mainNotification.mergesRead + 1;
      } else {
        if (mergeData.isRead)
          mainNotification.mergesRead = mainNotification.mergesRead - 1;
      }
      mergeData.READ_DATE = data.read ? (/* @__PURE__ */ new Date()).toISOString() : null;
    }
    setNotificationMenu(mergeData);
    if (mainNotification.mergesRead === mainNotification.COUNT) {
      if (!mainNotification.isRead) {
        mainNotification.READ_DATE = (/* @__PURE__ */ new Date()).toISOString();
        if (!mainNotification.FROM_ARCHIVE)
          store.setUnreadTotal(store.unreadTotal - 1);
      }
    } else if (!data.read && mainNotification.isRead) {
      mainNotification.READ_DATE = null;
      if (!mainNotification.FROM_ARCHIVE)
        store.setUnreadTotal(store.unreadTotal + 1);
    }
    setNotificationMenu(mainNotification);
  };
  const setGroupReadState = (id2, data) => {
    const { setNotificationMenu } = useNotificationMenu();
    const group = store.getNotificationById(id2);
    if (data.read !== void 0) {
      if (data.read) {
        group.READ_DATE = (/* @__PURE__ */ new Date()).toISOString();
        if (group.mergesFetched) {
          const merges = store.groups.get(group.GROUPID);
          merges.forEach((merge) => {
            const mergeItem = store.getMergeById(merge.id);
            mergeItem.READ_DATE = (/* @__PURE__ */ new Date()).toISOString();
            setNotificationMenu(mergeItem);
          });
        }
        group.mergesRead = group.COUNT;
        if (!group.FROM_ARCHIVE)
          store.setUnreadTotal(store.unreadTotal - 1);
      } else {
        group.READ_DATE = null;
        if (group.mergesFetched) {
          const merges = store.groups.get(group.GROUPID);
          merges.forEach((merge) => {
            const mergeItem = store.getMergeById(merge.id);
            mergeItem.READ_DATE = null;
            setNotificationMenu(mergeItem);
          });
        }
        group.mergesRead = 0;
        if (!group.FROM_ARCHIVE)
          store.setUnreadTotal(store.unreadTotal + 1);
      }
    } else if (data.starred !== void 0) {
      group.STARRED = data.param;
    }
    setNotificationMenu(group);
  };
  const setAllReadState = (data) => {
    const { setNotificationMenu } = useNotificationMenu();
    const { isFilterApplied } = useFilterStore();
    store.notifications.forEach((notification) => {
      if (data.read) {
        notification.READ_DATE = (/* @__PURE__ */ new Date()).toISOString();
      } else {
        notification.READ_DATE = null;
      }
      if (isGroup(notification.ID)) {
        if (notification.mergesFetched) {
          const merges = store.groups.get(notification.GROUPID);
          merges.forEach((merge) => {
            const mergeItem = store.getMergeById(merge.id);
            if (data.read) {
              mergeItem.READ_DATE = (/* @__PURE__ */ new Date()).toISOString();
            } else {
              mergeItem.READ_DATE = null;
            }
            setNotificationMenu(mergeItem);
          });
          notification.mergesRead = notification.COUNT;
        }
      }
      setNotificationMenu(notification);
    });
    if (!isFilterApplied()) {
      if (data.read)
        store.setUnreadTotal(0);
      else getUnreadTotal();
    }
  };
  const unsubscribeNotification = ({ setting }) => {
    if (setting.SUBSCRIBE === 0 || setting.UNSUBSCRIBE_DATE !== null) {
      const idsToDelete = [];
      store.notifications.forEach((notification) => {
        if (notification.SERVICE_ID === setting.ID) {
          idsToDelete.push(notification.ID);
        }
      });
      idsToDelete.forEach((id2) => {
        deleteNotification2(id2, true, false);
      });
      if (setting.UNSUBSCRIBE_DATE !== null) {
        settingStore.addUnsubscribe_dateSetting(setting.ID);
      }
    }
  };
  const updateLabel = (params) => {
    const { updateActionedLabel } = useNotificationActions();
    let notification;
    if (params.isMerge)
      notification = store.getMergeById(params.id);
    else notification = store.getNotificationById(params.id);
    updateActionedLabel(notification, params.label, false);
  };
  const setActioned = (params) => {
    const { updateActionedLabel } = useNotificationActions();
    let notification;
    let group;
    if (params.isMerge) {
      notification = store.getMergeById(params.id);
      group = store.getNotificationByGroupId(notification.GROUPID);
    } else {
      notification = store.getNotificationById(params.id);
    }
    if (!notification.isActioned)
      notification.actionNotification();
    let allActioned = true;
    if (group) {
      const merges = store.groups.get(group.GROUPID);
      merges.forEach((item) => {
        const merge = store.getMergeById(item.id);
        if (!merge.isActioned) {
          allActioned = false;
        }
      });
      if (allActioned) {
        updateActionedLabel(group, "do not matter", false);
        group.actionNotification();
      }
    }
  };
  const hasArchiveNotifications = (notifications) => {
    return notifications.some((notification) => notification.FROM_ARCHIVE);
  };
  const notificationInFilterRange = (notification, filter2) => {
    const creationDate = new Date(notification.CREATION_DATE);
    let inDateRange = true;
    if (filter2.first_date && filter2.last_date) {
      filter2.first_date.setHours(0, 0, 0, 0);
      filter2.last_date.setHours(23, 59, 59, 999);
      inDateRange = creationDate >= filter2.first_date && creationDate <= filter2.last_date;
    } else if (filter2.first_date) {
      filter2.first_date.setHours(0, 0, 0, 0);
      inDateRange = creationDate >= filter2.first_date;
    } else if (filter2.last_date) {
      filter2.last_date.setHours(23, 59, 59, 999);
      inDateRange = creationDate <= filter2.last_date;
    }
    const inSearchesRange = filter2.searches.length === 0 || filter2.searches.some((search) => {
      return notification.MESSAGE.nls.msg.toLowerCase().includes(search.toLowerCase());
    });
    const inReadRange = filter2.read === true && notification.READ_DATE !== null;
    const inUnreadRange = filter2.unread === true && notification.READ_DATE === null;
    const inStarredRange = filter2.starred === true && notification.STARRED === 1;
    const inUnstarredRange = filter2.unstarred === true && (!notification.STARRED || notification.STARRED === 0);
    const bothReadAndUnread = filter2.read === true && filter2.unread === true || filter2.read === false && filter2.unread === false;
    const bothStarredAndUnstarred = filter2.starred === true && filter2.unstarred === true || filter2.starred === false && filter2.unstarred === false;
    const defaultRange = bothReadAndUnread && bothStarredAndUnstarred;
    const inTenantRange = filter2.tenants.length === 0 || filter2.tenants.some((tenant) => tenant.value === notification.PLATFORMID);
    return inDateRange && inSearchesRange && (defaultRange || (bothReadAndUnread || inReadRange || inUnreadRange) && (bothStarredAndUnstarred || inStarredRange || inUnstarredRange)) && inTenantRange;
  };
  const cancelDeletion = (id2, merge = false) => {
    const { centerActionTracker } = NotificationTrackerUsage();
    let notif;
    if (merge) {
      if (isMerge(id2)) {
        notif = store.getMergeById(id2);
      }
    } else {
      notif = store.getNotificationById(id2);
    }
    if (notif) {
      notif.deleted = false;
      clearTimeout(notif.deleteTimeoutId);
      centerActionTracker().cancelNotificationDeletion({
        persDim: {
          pd1: notif.isMerge ? "merge" : notif.isGroup ? "group" : "normal"
        }
      });
    }
  };
  const loadMoreNotifications = () => {
    if (store.loadingMore || store.isLoading)
      return;
    if (store.canLoadMoreHistory === true || store.canLoadMoreFromArchive === true) {
      store.setLoadingMore(true);
      const timeout2 = setTimeout(() => {
        const { isFilterApplied, getFilter } = useFilterStore();
        const data = {
          ...!isFilterApplied() && {
            type: "all"
          },
          ...isFilterApplied() && {
            ...getFilter()
          },
          ...store.oldID !== -1 && {
            oldID: store.oldID
          },
          groupIDs: store.groupIDs,
          ...store.canLoadMoreFromArchive && {
            archive: true
          }
        };
        if (isFilterApplied()) {
          filter(data);
        } else {
          getHistory(data);
        }
        clearTimeout(timeout2);
      }, 500);
    }
  };
  const rollbackPopupAction = async (data) => {
    if (Object.keys(data.params).length) {
      const { alertConfirmNotice } = usePreferencesManagement();
      let renderToDoc, title;
      if (data.params.isInNotifCenter) {
        renderToDoc = document.body;
        title = $i18n$5("rollbackPopupTitleForNotifCenter");
      } else {
        renderToDoc = parent.document.body;
        title = $i18n$5("rollbackPopupTitleFor3DDashboard");
      }
      store.setRenderToDoc(renderToDoc);
      alertConfirmNotice({ title, msg: $i18n$5("rollbackPopupMsg") }).then((result) => {
        rollbackPopupconfirmation({ file: data.params.file, id: data.params.id, confirmPopupResult: result });
        store.resetRenderToDoc();
      });
    }
  };
  return {
    refreshCenterView: refreshCenterView2,
    addHistory,
    addMerge,
    setNotifMerges,
    updateLabel,
    addNotificationToSection,
    addNotification: addNotification2,
    removeNotification,
    removeMerge,
    isMerge,
    setActioned,
    isGroup,
    isNotificationGroup,
    mergeIsMain,
    mainIsMerge,
    tryNotificationDeletion,
    isNormalNotification,
    notificationType,
    canBeAddedToGroup,
    addNotificationToGroup,
    updateGroupNotification,
    replaceGroupMessage,
    DeleteNotificationGroup,
    deleteNotification: deleteNotification2,
    deleteMerge,
    mergesNumber,
    isIdMainId,
    removeNotificationGroup,
    setNotificationReadState,
    setMergeReadState,
    setGroupReadState,
    setAllReadState,
    unsubscribeNotification,
    setArchiveState,
    setArchiveSectionMessage,
    hasArchiveNotifications,
    notificationInFilterRange,
    fetchMoreNotificationsIfNecessary,
    cancelDeletion,
    loadMoreNotifications,
    refreshWithoutReset,
    rollbackPopupAction
  };
}
const useConnectionStore = /* @__PURE__ */ defineStore("connection", () => {
  const socketStatus = ref(null);
  const isSocketConnected = computed(() => socketStatus.value === "connected");
  const isSocketDisconnected = computed(() => socketStatus.value === "disconnected");
  const previousSocketStatus = usePrevious(socketStatus);
  const isPreviousSocketConnected = computed(() => previousSocketStatus.value === "connected");
  const isPreviousSocketDisconnected = computed(() => previousSocketStatus.value === "disconnected");
  const checkingSocketStatus = ref(false);
  const start = ref(null);
  const stop2 = ref(null);
  const setSocketStatusVariables = ({ _stop, _start }) => {
    stop2.value = _stop;
    start.value = _start;
  };
  const setCheckingSocketStatus = (status) => {
    if (typeof status === "boolean") {
      checkingSocketStatus.value = status;
    }
  };
  const needCenterRefresh = ref(false);
  const setNeedCenterRefresh = (status) => {
    if (typeof status === "boolean") {
      needCenterRefresh.value = status;
    }
  };
  const driverSenderQueue = ref([]);
  const addToDriverSenderQueue = (action) => {
    driverSenderQueue.value.push(action);
  };
  const setSocketStatus2 = (status) => {
    const statusMap = ["connected", "disconnected"];
    if (typeof status === "string" && statusMap.includes(status)) {
      socketStatus.value = status;
    }
  };
  const resetSocketStatus = () => {
    socketStatus.value = null;
  };
  return {
    socketStatus,
    previousSocketStatus,
    isSocketConnected,
    isSocketDisconnected,
    isPreviousSocketConnected,
    isPreviousSocketDisconnected,
    checkingSocketStatus,
    driverSenderQueue,
    needCenterRefresh,
    start,
    stop: stop2,
    addToDriverSenderQueue,
    setSocketStatus: setSocketStatus2,
    resetSocketStatus,
    setCheckingSocketStatus,
    setNeedCenterRefresh,
    setSocketStatusVariables
  };
});
const channelToServer$1 = "3dnotifinterne";
const clientToServerEvent$1 = "toINTF";
function useConnection() {
  const connectionStore = useConnectionStore();
  const notifStore = useNotificationsStore();
  const setSocketStatusTimeout = (time) => {
    const { stop: stop2, start } = useTimeoutFn(() => {
      if (connectionStore.checkingSocketStatus) {
        connectionStore.setCheckingSocketStatus(false);
      }
    }, time);
    connectionStore.setSocketStatusVariables({ _stop: stop2, _start: start });
  };
  const timeoutSocketStatusCheck = () => {
    connectionStore.stop();
    connectionStore.start();
  };
  const triggerDriverSenderQueue = async () => {
    const PlatformAPI = await getPlatformAPI();
    if (PlatformAPI) {
      const datas = [...connectionStore.driverSenderQueue];
      for (const data of datas) {
        if (connectionStore.isSocketConnected) {
          PlatformAPI.publish(channelToServer$1, { action: clientToServerEvent$1, data });
          connectionStore.driverSenderQueue.shift();
        } else {
          return;
        }
      }
    }
  };
  const reliveSocketIfKO = () => {
    watch(
      () => connectionStore.socketStatus,
      () => {
        if (connectionStore.isSocketConnected) {
          if (connectionStore.isPreviousSocketDisconnected) {
            if (notifStore.centerFrameOpened) {
              const { refreshWithoutReset } = useNotificationManagement();
              refreshWithoutReset();
            } else {
              connectionStore.setNeedCenterRefresh(true);
            }
          }
          if (connectionStore.driverSenderQueue.length) {
            triggerDriverSenderQueue();
          }
        }
      }
    );
    watch(
      () => notifStore.centerFrameOpened,
      (value) => {
        if (value) {
          if (connectionStore.isSocketDisconnected && !connectionStore.checkingSocketStatus) {
            connectionStore.setCheckingSocketStatus(true);
            timeoutSocketStatusCheck();
            checkSocketStatus();
          }
          if (connectionStore.needCenterRefresh) {
            const { refreshWithoutReset } = useNotificationManagement();
            refreshWithoutReset();
            connectionStore.setNeedCenterRefresh(false);
          }
        }
      }
    );
  };
  return {
    reliveSocketIfKO,
    triggerDriverSenderQueue,
    setSocketStatusTimeout,
    timeoutSocketStatusCheck
  };
}
const channelManagerInit = "3dNotifManagerInit";
const channelCenterInit = "3dNotifCenterInit";
const channelToWidget = "3dnotifinterne";
const channelToServer = "3dnotifinterne";
const clientToServerEvent = "toINTF";
const serverToClientEvent = "notification";
const PAPIDriver = {
  /**
   * Generates a TxID.
   * @param {string} action - The event action name to generate the TxID for.
   * @returns A random transaction id for the given action.
   */
  _generateTxID: (action) => `${Date.now()}-r${Math.floor(Math.random() * 1e3)}-${action || "noAction"}`,
  /**
   * Initializes Center to Manager connection.
   * @param {UWA.Widget} widget - Notifications Center widget.
   */
  async init(widget) {
    const PlatformAPI = await getPlatformAPI();
    PlatformAPI.subscribe(channelManagerInit, async (data) => {
      if (data) {
        PlatformAPI.unsubscribe(channelManagerInit);
      } else {
        widget.counterRequest++;
        widget.timerId = window.setTimeout(() => {
          widget.initFlag = true;
          if (widget.counterRequest < widget.maxRequest)
            PlatformAPI.publish(channelCenterInit, true);
          else window.clearTimeout(widget.timerId);
        }, 2e3);
      }
    });
    PlatformAPI.publish(channelCenterInit, true);
  },
  /**
   * Adds callback to call on reception of event.
   * @param {Function} callback - The function to call on reception of event.
   */
  async addCallback(callback) {
    const PlatformAPI = await getPlatformAPI();
    const { clusterId, setClusterId } = useNotificationsStore();
    PlatformAPI.subscribe(channelToWidget, (response) => {
      if (!response)
        return console.debug("[PAPIDriver addCallback] Missing response");
      if (!response.action || response.action !== serverToClientEvent) {
        if (response.action === clientToServerEvent)
          return;
        return console.debug(`[PAPIDriver addCallback] Received non-${serverToClientEvent} event`);
      }
      if (!response.data)
        return console.debug("[PAPIDriver addCallback] Missing response.data");
      response.data.clusterId = response.data.clusterId || response.clusterId;
      if (response.data.clusterId && !clusterId)
        setClusterId(response.data.clusterId);
      callback(response.data);
    });
  },
  /**
   * Relive the socket if it is disconnected.
   * @param {*} data
   */
  async reliveSocket(data) {
    const connectionStore = useConnectionStore();
    const { timeoutSocketStatusCheck } = useConnection();
    if (connectionStore.isSocketDisconnected && !managerEvents[data.action] && !connectionStore.checkingSocketStatus) {
      console.log("[PAPIDriver throttleSocketRelive] Socket is disconnected. Trying to connect...");
      const d = { action: "checkSocketStatus", data: {} };
      const PlatformAPI = await getPlatformAPI();
      connectionStore.setCheckingSocketStatus(true);
      timeoutSocketStatusCheck();
      PlatformAPI.publish(channelToServer, { action: clientToServerEvent, data: d });
      connectionStore.addToDriverSenderQueue(data);
      return;
    }
    if (connectionStore.checkingSocketStatus && !managerEvents[data.action]) {
      connectionStore.addToDriverSenderQueue(data);
    }
  },
  /**
   * Sends the given data to server.
   * @param {{action: string, data: object}} data - The data to send.
   */
  async send(data) {
    if (!data)
      return;
    this.reliveSocket(data);
    if (data.action)
      data.TxID = this._generateTxID(data.action);
    const PlatformAPI = await getPlatformAPI();
    PlatformAPI.publish(channelToServer, { action: clientToServerEvent, data });
  }
};
const commonDriver = PAPIDriver;
const useAnalyticsStore = /* @__PURE__ */ defineStore("analytics", () => {
  const startTime = ref(0);
  const label = ref("");
  const setStartTime = (time) => {
    startTime.value = time;
  };
  const resetAnalytics = () => {
    startTime.value = 0;
    label.value = "";
  };
  const setLabel = (labelValue) => {
    label.value = labelValue;
  };
  const setAnalytics = (time, labelValue) => {
    setStartTime(time);
    setLabel(labelValue);
  };
  return {
    startTime,
    label,
    // endTime,
    getStartTime: () => startTime.value,
    durationTime: () => {
      return startTime.value === 0 ? -1 : Math.abs(performance.now() - startTime.value);
    },
    getLabel: () => label.value,
    setLabel,
    // setEndTime,
    setStartTime,
    setAnalytics,
    resetAnalytics
  };
});
function setAnalyticsStartTime() {
  const { setStartTime } = useAnalyticsStore();
  setStartTime(performance.now());
}
function setFilterOptionsPayload({
  first_date,
  last_date,
  searches: searches2,
  read: read2,
  unread: unread2,
  starred: starred2,
  unstarred: unstarred2,
  tenants,
  oldID,
  archive,
  groupIDs,
  clusterId
}) {
  const tenantsValues = [];
  tenants.forEach((tenant) => {
    tenantsValues.push(tenant.value);
  });
  tenants = tenantsValues;
  const clusters = {};
  clusters[clusterId] = {
    oldId: oldID,
    groupIds: groupIDs,
    archive: archive ?? false,
    tenants: tenants.map((t) => (t == null ? void 0 : t.LABEL) || t)
  };
  const firstDate = first_date ? first_date.toLocaleDateString("en-CA") : null;
  let lastDate = new Date(last_date == null ? void 0 : last_date.valueOf());
  lastDate = lastDate == null ? void 0 : lastDate.setDate((lastDate == null ? void 0 : lastDate.getDate()) + 1);
  lastDate = lastDate ? new Date(lastDate).toLocaleDateString("en-CA") : null;
  return {
    filterOptions: {
      firstDate,
      lastDate,
      searches: searches2,
      read: read2,
      unread: unread2,
      starred: starred2,
      unstarred: unstarred2
    },
    clusters
  };
}
function getUnreadTotal() {
  commonDriver.send({ action: "getUnreadTotal", data: {} });
}
function getTenants() {
  commonDriver.send({ action: "getTenants", data: {} });
}
function resetBadge({ notifCenterOpened: notifCenterOpened2 }) {
  commonDriver.send({ action: "resetBadge", data: { notifCenterOpened: notifCenterOpened2 } });
}
function getHistory({ type, oldID, groupIDs, archive }) {
  setAnalyticsStartTime();
  commonDriver.send({ action: "getHistory", data: { type, oldID, groupIDs, archive } });
}
function notificationRead({
  action,
  id: id2,
  read: read2,
  actioned,
  archive,
  scope,
  filter: filter2,
  notificationID,
  starred: starred2,
  param,
  params,
  updateLabel,
  groupID,
  hiddenMerged,
  clusterId,
  filterData
}) {
  const notifId = checkIfIdHasHyphen(id2);
  if (notifId) {
    id2 = Number.parseInt(notifId.id);
  }
  if (params && !!params.id) {
    const paramId = checkIfIdHasHyphen(params.id);
    if (paramId) {
      params.id = Number.parseInt(paramId.id);
    }
  }
  const { filterOptions, clusters } = filterData ? setFilterOptionsPayload(filterData) : {};
  commonDriver.send({
    action: "notificationRead",
    data: {
      action,
      id: id2,
      read: read2,
      archive,
      scope,
      filter: filter2,
      notificationID,
      actioned,
      starred: starred2,
      param,
      params,
      updateLabel,
      groupID,
      hiddenMerged,
      clusterId,
      filterOptions,
      clusters
    }
  });
}
function deleteAllNotification$1() {
  commonDriver.send({ action: "deleteAllNotification", data: {} });
}
function notificationDelete({ groupID, id: id2, scope, archive, clusterId }) {
  const notifId = checkIfIdHasHyphen(id2);
  if (notifId) {
    id2 = Number.parseInt(notifId.id);
  }
  commonDriver.send({
    action: "notificationDelete",
    data: { id: id2, groupID, scope, archive, clusterId }
  });
}
function getAppInfos({ appID, notifID, clusterId }) {
  const notifId = checkIfIdHasHyphen(notifID);
  if (notifId) {
    notifID = Number.parseInt(notifId.id);
  }
  commonDriver.send({ action: "getAppInfos", data: { appID, notifID, clusterId } });
}
function getServices() {
  commonDriver.send({ action: "getServices", data: {} });
}
function getMerge({ parentNotif, filterData }) {
  const notifId = checkIfIdHasHyphen(parentNotif.id);
  if (notifId) {
    parentNotif.id = Number.parseInt(notifId.id);
  }
  const { filterOptions, clusters } = filterData ? setFilterOptionsPayload(filterData) : {};
  commonDriver.send({ action: "getMerge", data: { parentNotif, filterOptions, clusters } });
}
function unsubscribe({ id: id2, subscribe, forHour }) {
  const notifId = checkIfIdHasHyphen(id2);
  if (notifId) {
    id2 = Number.parseInt(notifId.id);
  }
  commonDriver.send({ action: "unsubscribe", data: { id: id2, subscribe, forHour } });
}
function getTenantAgnosticMode() {
  commonDriver.send({ action: "getTenantAgnosticMode", data: {} });
}
function updateTenantAgnosticMode({ isTenantAgnostic, hidePlatformSelection }) {
  commonDriver.send({ action: "updateTenantAgnosticMode", data: { isTenantAgnostic, hidePlatformSelection } });
}
function publish({ topic, data }) {
  commonDriver.send({ action: "publish", data: { topic, data } });
}
function filter({
  first_date,
  last_date,
  searches: searches2,
  read: read2,
  unread: unread2,
  starred: starred2,
  unstarred: unstarred2,
  tenants,
  oldID,
  archive,
  groupIDs,
  clusterId
}) {
  setAnalyticsStartTime();
  commonDriver.send({
    action: "filter",
    data: setFilterOptionsPayload({
      first_date,
      last_date,
      searches: searches2,
      read: read2,
      unread: unread2,
      starred: starred2,
      unstarred: unstarred2,
      tenants,
      oldID,
      archive,
      groupIDs,
      clusterId
    })
  });
}
function deleteAllFilteredNotification$1({ notificationID, filterData }) {
  const { filterOptions, clusters } = filterData ? setFilterOptionsPayload(filterData) : {};
  commonDriver.send({ action: "deleteAllFilteredNotification", data: { notificationID, filterOptions, clusters } });
}
function updateSettings({ subscribe, id: id2, notif_by_browser, notif_by_email, notif_by_ui, tenantAware }) {
  commonDriver.send({
    action: "updateSettings",
    data: { subscribe, id: id2, notif_by_browser, notif_by_email, notif_by_ui, tenantAware }
  });
}
function updateServiceSettings({ subscribe, id: id2, notif_by_email, tenantAware }) {
  commonDriver.send({
    action: "updateServiceSettings",
    data: { subscribe, id: id2, notif_by_email, tenantAware }
  });
}
function resetSettingUnsubscriptionDate$1({ id: id2 }) {
  commonDriver.send({
    action: "resetSettingUnsubscriptionDate",
    data: { id: id2 }
  });
}
function getSettings() {
  commonDriver.send({ action: "getSettings", data: {} });
}
function getServicesSettingsOfTenant() {
  commonDriver.send({ action: "getServicesSettingsOfTenant", data: {} });
}
function getDNDStatus() {
  commonDriver.send({ action: "getDNDStatus", data: {} });
}
function rollbackPopupconfirmation({ file, id: id2, confirmPopupResult }) {
  commonDriver.send({ action: "rollbackPopupconfirmation", data: { id: id2, file, confirmPopupResult } });
}
function getSocketStatus() {
  commonDriver.send({ action: "getSocketStatus", data: { centerOpened: true } });
}
function checkSocketStatus() {
  commonDriver.send({ action: "checkSocketStatus", data: {} });
}
const managerEvents = {
  getSocketStatus,
  checkSocketStatus,
  getAppInfos,
  getServices,
  publish
};
const _hoisted_1$A = { class: "INTFCenter-zero-notification-container" };
const _hoisted_2$o = { class: "INTFCenter-zero-notification-content" };
const _hoisted_3$g = { class: "INTFCenter-no-notif-text" };
const _sfc_main$G = {
  __name: "NoNotification",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const store = useNotificationsStore();
    onMounted(async () => {
      store.selectionMode = false;
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$A, [
        createBaseVNode("div", _hoisted_2$o, [
          _cache[0] || (_cache[0] = createBaseVNode("span", { class: "fonticon fonticon-bell block INTFCenter-no-notif-icon" }, null, -1)),
          createBaseVNode("p", _hoisted_3$g, toDisplayString(unref($i18n2)("noNotifications")), 1)
        ])
      ]);
    };
  }
};
const _hoisted_1$z = { class: "INTFCenter-notification-is-deleted" };
const _hoisted_2$n = { class: "INTFCenter-deleted-text INTFCenter-color-brown" };
const _hoisted_3$f = { class: "INTFCenter-deletion-msg" };
const _sfc_main$F = {
  __name: "NotificationDeleted",
  props: {
    id: String
  },
  emits: ["cancelDeletion"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit2 = __emit;
    const { $i18n: $i18n2 } = useTranslations$1();
    function cancelDeletion() {
      emit2("cancelDeletion", props.id);
    }
    return (_ctx, _cache) => {
      const _component_vu_btn = resolveComponent("vu-btn");
      return openBlock(), createElementBlock("div", _hoisted_1$z, [
        createBaseVNode("p", _hoisted_2$n, toDisplayString(unref($i18n2)("notificationDeleted")), 1),
        createVNode(_component_vu_btn, {
          class: "INTFCenter-cancel-btn",
          color: "primary",
          small: "",
          onClick: cancelDeletion
        }, {
          default: withCtx(() => [
            _cache[0] || (_cache[0] = createBaseVNode("span", { class: "fonticon fonticon-reload" }, null, -1)),
            createBaseVNode("span", _hoisted_3$f, toDisplayString(unref($i18n2)("cancel")), 1)
          ]),
          _: 1
        })
      ]);
    };
  }
};
const _hoisted_1$y = ["icon-src"];
const _hoisted_2$m = ["src"];
const _sfc_main$E = {
  __name: "NotificationIcon",
  props: {
    id: String,
    group: {
      type: Boolean,
      default: false
    },
    merge: {
      type: Boolean,
      default: false
    },
    type: {
      type: String,
      default: "login"
    },
    isLink: {
      type: Boolean,
      default: false
    },
    icon: String
  },
  setup(__props) {
    const props = __props;
    const tries = ref(0);
    const src = ref("");
    const iconUrlBackup = ref("./../../resources/en/1/webapps/NotifAlert/assets/icons/notificon.png");
    function goToLink() {
      if (props.isLink) {
        const { openIconUrl } = useNotificationActions();
        const { centerActionTracker } = NotificationTrackerUsage();
        openIconUrl(props.icon);
        centerActionTracker().profileClick();
      }
    }
    watch(
      () => props.icon,
      () => {
        src.value = props.icon;
        tries.value = 0;
      }
    );
    function loadDefault(event) {
      tries.value += 1;
      if (tries.value > 3) {
        event.target.src = iconUrlBackup.value;
      } else {
        src.value = `${props.icon}?retry=${tries.value}`;
      }
    }
    onBeforeMount(async () => {
      try {
        const WAFData = await getWAFData();
        if (WAFData) {
          src.value = WAFData.proxifyUrl(props.icon, { proxy: "passport" }) || props.icon;
        }
      } catch (error) {
        console.error("Error loading icon", error);
      }
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(["INTFCenter-notification-notificon", { "cursor-pointer": __props.isLink }]),
        "icon-src": __props.icon,
        onClick: goToLink
      }, [
        createBaseVNode("img", {
          class: normalizeClass(["INTFCenter-bodyIcon", { "INTFCenter-IconRadius": __props.type === "login" || "url", "INTFCenter-mergeIcon": __props.merge }]),
          src: src.value.length > 0 ? src.value : iconUrlBackup.value,
          onError: loadDefault
        }, null, 42, _hoisted_2$m)
      ], 10, _hoisted_1$y);
    };
  }
};
const _hoisted_1$x = { class: "INTFCenter-dropdown-menu-div" };
const _sfc_main$D = {
  __name: "NotificationMenu",
  props: {
    options: Object,
    isRead: {
      type: Boolean,
      default: false
    }
  },
  setup(__props) {
    const store = useNotificationsStore();
    const scroller = computed(() => store.scrollerBody);
    const menuAction = ref(false);
    const selectedAction = ref([]);
    function openMenu() {
      menuAction.value = true;
    }
    return (_ctx, _cache) => {
      const _component_vu_btn = resolveComponent("vu-btn");
      const _component_vu_dropdownmenu = resolveComponent("vu-dropdownmenu");
      return openBlock(), createElementBlock("div", _hoisted_1$x, [
        createVNode(_component_vu_dropdownmenu, {
          modelValue: selectedAction.value,
          "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => selectedAction.value = $event),
          attach: scroller.value,
          items: __props.options,
          position: "bottom-right",
          onClose: _cache[1] || (_cache[1] = ($event) => menuAction.value = false)
        }, {
          default: withCtx(() => [
            createVNode(_component_vu_btn, {
              class: normalizeClass(["INTFCenter-btn-transparent INTFCenter-menu-btn", {
                "INTFCenter-btn-active": menuAction.value,
                "INTFCenter-btn-hover": !menuAction.value,
                "INTFCenter-is-read": __props.isRead
              }]),
              large: "",
              onClick: openMenu
            }, {
              default: withCtx(() => [..._cache[2] || (_cache[2] = [
                createBaseVNode("span", { class: "fonticon fonticon-chevron-down INTFCenter-menu-icon" }, null, -1)
              ])]),
              _: 1
            }, 8, ["class"])
          ]),
          _: 1
        }, 8, ["modelValue", "attach", "items"])
      ]);
    };
  }
};
const _hoisted_1$w = { class: "INTFCenter-actions-container" };
const _hoisted_2$l = { key: 0 };
const _sfc_main$C = {
  __name: "NotificationActions",
  props: {
    isActioned: {
      type: Boolean,
      default: false
    },
    actionedLabel: {
      type: String,
      default: ""
    },
    actions: Object,
    isRead: {
      type: Boolean,
      default: false
    },
    denyDisabled: {
      type: Boolean,
      default: false
    },
    selectDisabled: {
      type: Boolean,
      default: false
    },
    isSelect: {
      type: Boolean,
      default: false
    },
    isButton: {
      type: Boolean,
      default: false
    }
  },
  emits: ["deny", "accept"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit2 = __emit;
    const { $i18n: $i18n2 } = useTranslations$1();
    const target = ref(null);
    const requestLabel = computed(() => {
      if (props.actionedLabel && typeof props.actionedLabel === "string") {
        if (!props.actionedLabel.includes("NLS")) {
          return props.actionedLabel;
        }
        const dotIndex = props.actionedLabel.indexOf(".");
        return $i18n2(props.actionedLabel.substring(dotIndex + 1));
      }
      return $i18n2(props.actionedLabel);
    });
    const acceptLabel = computed(() => {
      var _a3, _b2, _c3, _d3;
      const findOptions = props.isSelect ? (_c3 = (_b2 = (_a3 = props.actions.find((action) => action.type === "select")) == null ? void 0 : _a3.options) == null ? void 0 : _b2.event) == null ? void 0 : _c3.options[0] : (_d3 = props.actions[0]) == null ? void 0 : _d3.options;
      return (findOptions == null ? void 0 : findOptions.label) || $i18n2("accept");
    });
    const denyLabel = computed(() => {
      var _a3, _b2;
      const findOptions = props.isSelect ? (_a3 = props.actions.find((action) => action.type === "button")) == null ? void 0 : _a3.options : (_b2 = props.actions[1]) == null ? void 0 : _b2.options;
      return (findOptions == null ? void 0 : findOptions.label) || $i18n2("deny");
    });
    const options = computed(() => {
      const items = [];
      const findAction = props.actions.find((action) => action.type === "select");
      if (findAction) {
        const length = findAction.options.event.options.length;
        for (let i = 1; i < length; i++) {
          items.push({
            text: findAction.options.event.options[i].label,
            fonticon: "",
            label: findAction.options.event.options[i].label,
            handler: () => {
              const action = {
                type: findAction.options.event.type,
                subAction: true,
                action: findAction.options.event.options[i]
              };
              acceptAction({ options: action, i });
            }
          });
        }
      }
      return items;
    });
    function acceptAction({ options: options2, i }) {
      emit2("accept", { options: options2, i });
    }
    function denyAction() {
      const findAction = props.isSelect ? props.actions.find((action) => action.type === "button") : props.actions[1];
      if (findAction) {
        const action = {
          type: findAction.options.event.type,
          action: findAction
        };
        emit2("deny", action);
      }
    }
    function btnDrop() {
      acceptAction({
        options: {
          type: props.actions[0].options.event.type,
          subAction: true,
          action: props.actions[0].options.event.options[0]
        },
        i: 0
      });
    }
    function btnAccept() {
      acceptAction({
        options: {
          type: props.actions[0].options.event.type,
          subAction: false,
          action: props.actions[0].options.event.options
        },
        i: 0
      });
    }
    return (_ctx, _cache) => {
      const _component_vu_btn_dropdown = resolveComponent("vu-btn-dropdown");
      const _component_vu_btn = resolveComponent("vu-btn");
      return openBlock(), createElementBlock("div", _hoisted_1$w, [
        __props.isActioned ? (openBlock(), createElementBlock("div", _hoisted_2$l, [
          createBaseVNode("strong", null, toDisplayString(requestLabel.value), 1)
        ])) : (openBlock(), createElementBlock("div", {
          key: 1,
          class: normalizeClass(["INTFCenter-notification-request-action", { disabled: __props.selectDisabled || __props.denyDisabled }])
        }, [
          createBaseVNode("div", {
            ref_key: "target",
            ref: target,
            class: "INTFCenter-action-request"
          }, [
            __props.isSelect ? (openBlock(), createBlock(_component_vu_btn_dropdown, {
              key: 0,
              attach: target.value,
              class: "INTFCenter-request-action-btn-vuekit",
              color: "primary",
              options: options.value,
              position: "bottom-right",
              responsive: true,
              onClick: btnDrop
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(acceptLabel.value), 1)
              ]),
              _: 1
            }, 8, ["attach", "options"])) : __props.isButton ? (openBlock(), createBlock(_component_vu_btn, {
              key: 1,
              attach: target.value,
              class: "INTFCenter-request-action-btn-vuekit",
              color: "primary",
              position: "bottom-right",
              responsive: true,
              small: "",
              onClick: btnAccept
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(acceptLabel.value), 1)
              ]),
              _: 1
            }, 8, ["attach"])) : createCommentVNode("", true)
          ], 512),
          createVNode(_component_vu_btn, {
            class: normalizeClass(["INTFCenter-deny-btn", { "INTFCenter-is-read": __props.isRead }]),
            small: "",
            onClick: denyAction
          }, {
            default: withCtx(() => [
              createTextVNode(toDisplayString(denyLabel.value), 1)
            ]),
            _: 1
          }, 8, ["class"])
        ], 2))
      ]);
    };
  }
};
const _sfc_main$B = {
  __name: "NotificationDate",
  props: {
    date: String,
    time: String,
    isRead: {
      type: Boolean,
      default: false
    },
    isMerge: {
      type: Boolean,
      default: false
    }
  },
  setup(__props) {
    return (_ctx, _cache) => {
      return __props.isMerge ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["font-semibold INTFCenter-color-brown INTFCenter-merge-date-component", { "INTFCenter-is-read": __props.isRead }])
      }, [
        createBaseVNode("span", {
          class: normalizeClass(["fonticon fonticon-clock INTFCenter-merge-time", { "INTFCenter-is-read": __props.isRead }])
        }, null, 2),
        createTextVNode(" " + toDisplayString(__props.date) + " - " + toDisplayString(__props.time), 1)
      ], 2)) : (openBlock(), createElementBlock("div", {
        key: 1,
        class: normalizeClass(["INTFCenter-notif-times text-xs INTFCenter-color-brown", { "INTFCenter-is-read": __props.isRead }])
      }, toDisplayString(__props.date), 3));
    };
  }
};
const _hoisted_1$v = { class: "INTFCenter-notification-message-container" };
const _hoisted_2$k = { class: "INTFCenter-message-merge-date" };
const _hoisted_3$e = ["innerHTML"];
const _sfc_main$A = {
  __name: "NotificationMessage",
  props: {
    notification: Notification$1,
    merge: {
      type: Boolean,
      default: false
    }
  },
  setup(__props) {
    const props = __props;
    const isExpanded = ref(false);
    const formattedDate = ref("");
    const selectDisabled = ref(false);
    const denyDisabled = ref(false);
    const reasonValues = ref(null);
    const actionAfterFetch = ref(false);
    const iAfterFetch = ref(-1);
    const messageLink = ref("");
    const action = ref(null);
    const isMergesFetched = computed(() => {
      return props.notification.isGroup === true && props.notification.mergesFetched === true;
    });
    watch(isMergesFetched, (newVal) => {
      if (newVal) {
        if (actionAfterFetch.value) {
          if (iAfterFetch.value !== -1) {
            const { executeActionSelect } = useNotificationActions();
            executeActionSelect(props.notification, iAfterFetch.value);
          } else {
            const { executeDeny } = useNotificationActions();
            executeDeny(props.notification, reasonValues.value);
          }
          iAfterFetch.value = -1;
          actionAfterFetch.value = false;
        }
      }
    });
    const isFirstActionSelect = computed(() => {
      var _a3, _b2;
      if (((_b2 = (_a3 = props == null ? void 0 : props.notification) == null ? void 0 : _a3.MESSAGE) == null ? void 0 : _b2.actions) && props.notification.MESSAGE.actions.length > 0) {
        return props.notification.MESSAGE.actions[0].type === "select";
      }
      return false;
    });
    const isFirstActionButton = computed(() => {
      var _a3, _b2;
      if (((_b2 = (_a3 = props == null ? void 0 : props.notification) == null ? void 0 : _a3.MESSAGE) == null ? void 0 : _b2.actions) && props.notification.MESSAGE.actions.length > 0) {
        return props.notification.MESSAGE.actions[0].type === "button";
      }
      return false;
    });
    async function fetchMergeDate() {
      const date = new Date(props.notification.CREATION_DATE.getTime());
      date.setHours(0, 0, 0, 0);
      const formatted = await getDateMergeFormat(new Date(date.getTime()));
      formattedDate.value = formatted;
    }
    onMounted(() => {
      fetchMergeDate();
    });
    function navigateToLink() {
      if (isMessageLink()) {
        const { NavigateToLink } = useNotificationActions();
        const { centerActionTracker } = NotificationTrackerUsage();
        NavigateToLink(props.notification, action);
        centerActionTracker().notifClick();
      }
    }
    function handleContainerClick(event) {
      if (event.target.classList.contains("toggle-link")) {
        event.stopPropagation();
        isExpanded.value = !isExpanded.value;
      } else {
        navigateToLink();
      }
    }
    const formattedMessage = computed(() => {
      const message = props.notification.MESSAGE.nls.msg;
      const maxLength = 256;
      let result = isExpanded.value ? message : message.length > maxLength ? `${message.slice(0, maxLength)}...` : message;
      if (message.length > maxLength) {
        result += `<a href='#' class='toggle-link'>${isExpanded.value ? " View Less" : " View More"}</a>`;
      }
      return result;
    });
    function isMessageLink() {
      const { noticationMessageIsLink } = useNotificationActions();
      const { option, isLink } = noticationMessageIsLink(props.notification.MESSAGE.actions);
      if (isLink) {
        messageLink.value = option.options.event.options.url;
        action.value = option;
        return true;
      }
      return false;
    }
    function _acceptAction({ options, i }) {
      const { acceptAction } = useNotificationActions();
      acceptAction({ notification: props.notification, options, i, selectDisabled, actionAfterFetch, iAfterFetch });
    }
    function _denyAction(options) {
      const { denyAction } = useNotificationActions();
      denyAction({ notification: props.notification, options, reasonValues, denyDisabled, actionAfterFetch });
    }
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$v, [
        createBaseVNode("div", _hoisted_2$k, [
          createBaseVNode("div", {
            class: normalizeClass(["INTFCenter-color-brown INTFCenter-notification-message", { "cursor-pointer": isMessageLink() }]),
            onClick: handleContainerClick,
            innerHTML: formattedMessage.value
          }, null, 10, _hoisted_3$e),
          __props.merge ? (openBlock(), createBlock(_sfc_main$B, {
            key: 0,
            date: formattedDate.value,
            "is-merge": true,
            "is-read": __props.notification.isRead,
            time: __props.notification.getHourMinutes()
          }, null, 8, ["date", "is-read", "time"])) : createCommentVNode("", true)
        ]),
        isFirstActionSelect.value || isFirstActionButton.value ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass({ "self-end": !__props.notification.isActioned })
        }, [
          createVNode(_sfc_main$C, {
            "actioned-label": __props.notification.ACTION,
            actions: __props.notification.MESSAGE.actions,
            "deny-disabled": denyDisabled.value,
            "is-actioned": __props.notification.isActioned,
            "is-button": isFirstActionButton.value,
            "is-read": __props.notification.isRead,
            "is-select": isFirstActionSelect.value,
            "select-disabled": selectDisabled.value,
            onAccept: _acceptAction,
            onDeny: _denyAction
          }, null, 8, ["actioned-label", "actions", "deny-disabled", "is-actioned", "is-button", "is-read", "is-select", "select-disabled"])
        ], 2)) : createCommentVNode("", true)
      ]);
    };
  }
};
const _hoisted_1$u = { class: "INTFCenter-notifBody-section" };
const _sfc_main$z = {
  __name: "NotificationBody",
  props: {
    notification: Notification$1,
    // In notification center or outside of it.
    center: {
      type: Boolean,
      default: true
    },
    // Is being shown in merge group
    merge: {
      type: Boolean,
      default: false
    },
    // to show or not show menu button
    menu: { type: Boolean, default: true }
    // whitespace: { type: Boolean, default: false },
  },
  setup(__props) {
    const props = __props;
    const store = useNotificationsStore();
    const notifBody = ref(null);
    const icon = computed(() => {
      var _a3;
      return ((_a3 = props.notification.MESSAGE.icon) == null ? void 0 : _a3.src) ? props.notification.MESSAGE.icon.src : "./../../resources/en/1/webapps/NotifAlert/assets/icons/notificon.png";
    });
    onMounted(() => {
      store.setScrollerBody(notifBody.value);
      if (props.notification.isActioned) {
        const { updateActionedLabel } = useNotificationActions();
        updateActionedLabel(props.notification, props.notification.ACTION, false);
      }
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        ref_key: "notifBody",
        ref: notifBody,
        class: "INTFCenter-notifBody"
      }, [
        createBaseVNode("div", _hoisted_1$u, [
          createBaseVNode("div", {
            class: normalizeClass(["INTFCenter-notifBody-container", { "INTFCenter-is-read": __props.notification.isRead }])
          }, [
            createVNode(_sfc_main$E, {
              id: __props.notification.ID,
              group: __props.notification.isGroup,
              icon: icon.value,
              "is-link": __props.notification.isIconLink,
              merge: __props.merge,
              type: __props.notification.MESSAGE.icon.type
            }, null, 8, ["id", "group", "icon", "is-link", "merge", "type"]),
            createVNode(_sfc_main$A, {
              merge: __props.merge,
              notification: __props.notification
            }, null, 8, ["merge", "notification"])
          ], 2),
          __props.menu ? (openBlock(), createBlock(_sfc_main$D, {
            key: 0,
            "is-read": __props.notification.isRead,
            options: __props.notification.getOptions
          }, null, 8, ["is-read", "options"])) : createCommentVNode("", true)
        ])
      ], 512);
    };
  }
};
const _hoisted_1$t = { class: "INTFCenter-notif-status" };
const _hoisted_2$j = {
  key: 2,
  class: "INTFCenter-notification-important fonticon fonticon-favorite-off"
};
const _hoisted_3$d = {
  key: 3,
  class: "INTFCenter-notification-important fonticon fonticon-favorite-on font-semibold"
};
const _sfc_main$y = {
  __name: "NotificationStatus",
  props: {
    isRead: {
      type: Boolean,
      default: false
    },
    isStarred: {
      type: Boolean,
      default: false
    },
    notification: {
      type: Notification$1
    }
  },
  setup(__props) {
    const props = __props;
    const notificationMenu = useNotificationMenu();
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$t, [
        __props.isRead && !__props.isStarred ? (openBlock(), createElementBlock("span", {
          key: 0,
          class: "INTFCenter-read-unread INTFCenter-notification-read cursor-pointer",
          onClick: _cache[0] || (_cache[0] = ($event) => unref(notificationMenu).setNotificationRead(props.notification))
        })) : createCommentVNode("", true),
        !__props.isRead && !__props.isStarred ? (openBlock(), createElementBlock("span", {
          key: 1,
          class: "INTFCenter-read-unread INTFCenter-notification-unread cursor-pointer",
          onClick: _cache[1] || (_cache[1] = ($event) => unref(notificationMenu).setNotificationRead(props.notification))
        })) : createCommentVNode("", true),
        __props.isRead && __props.isStarred ? (openBlock(), createElementBlock("span", _hoisted_2$j)) : createCommentVNode("", true),
        !__props.isRead && __props.isStarred ? (openBlock(), createElementBlock("span", _hoisted_3$d)) : createCommentVNode("", true)
      ]);
    };
  }
};
const _hoisted_1$s = {
  key: 0,
  class: "INTFCenter-notification-group"
};
const _hoisted_2$i = { class: "INTFCenter-view-merge-div" };
const _hoisted_3$c = { class: "INTFCenter-show-hide-merges" };
const _hoisted_4$a = {
  key: 0,
  class: "INTFCenter-color-3ds INTFCenter-view-group"
};
const _hoisted_5$6 = {
  key: 1,
  class: "INTFCenter-color-3ds INTFCenter-view-group"
};
const _hoisted_6$5 = {
  key: 0,
  class: "INTFCenter-open-spinner"
};
const _hoisted_7$2 = {
  key: 0,
  class: "INTFCenter-group-div"
};
const _sfc_main$x = {
  __name: "NotificationMergesList",
  props: {
    mainId: String,
    groupId: String,
    isRead: {
      type: Boolean,
      default: false
    }
  },
  setup(__props) {
    const props = __props;
    const { $i18n: $i18n2 } = useTranslations$1();
    const { cancelDeletion } = useNotificationManagement();
    const { centerActionTracker } = NotificationTrackerUsage();
    const theCenterActionTracker = centerActionTracker();
    const store = useNotificationsStore();
    const { getFilter } = useFilterStore();
    const components = {
      NotificationBody: _sfc_main$z,
      NotificationDeleted: _sfc_main$F
    };
    const opened = ref(false);
    const mergeIndex = ref(0);
    const openRequested = ref(false);
    const getMainNotification = computed(() => {
      return store.getNotificationById(props.mainId);
    });
    const canOpenMerges = computed(() => {
      return getMainNotification.value.mergesFetched === true;
    });
    const allMerges = computed(() => {
      const groupSection = store.groups.get(props.groupId);
      return groupSection;
    });
    const groupMerges = computed(() => {
      return allMerges.value.slice(0, mergeIndex.value + 10);
    });
    watch(canOpenMerges, (newVal) => {
      if (newVal && openRequested.value) {
        opened.value = true;
        openRequested.value = false;
      }
    });
    watch(opened, () => {
      if (!opened.value) {
        mergeIndex.value = 0;
      }
    });
    function getMerges() {
      if (!getMainNotification.value.mergesFetched) {
        getMerge({
          parentNotif: {
            id: getMainNotification.value.ID,
            groupID: getMainNotification.value.GROUPID,
            clusterId: getMainNotification.value.CLUSTERID,
            fromArchive: getMainNotification.value.FROM_ARCHIVE,
            ignoreSearchesForChildren: !!getMainNotification.value.ignoreSearchesForChildren
          },
          filterData: getFilter()
        });
        openRequested.value = true;
        theCenterActionTracker.viewAllMergeNotif({ nbMerge: getMainNotification.value.COUNT });
      } else {
        opened.value = !opened.value;
        if (opened.value) {
          theCenterActionTracker.viewAllMergeNotif({ nbMerge: getMainNotification.value.COUNT });
        } else {
          theCenterActionTracker.hideAllMergeNotif({ nbMerge: getMainNotification.value.COUNT });
        }
      }
    }
    function seeMoreMerge() {
      mergeIndex.value += 10;
    }
    return (_ctx, _cache) => {
      const _component_vu_spinner = resolveComponent("vu-spinner");
      const _component_vu_btn = resolveComponent("vu-btn");
      return getMainNotification.value ? (openBlock(), createElementBlock("div", _hoisted_1$s, [
        createVNode(_component_vu_btn, {
          class: normalizeClass(["INTFCenter-view-btn", { "INTFCenter-is-read": __props.isRead }]),
          small: "",
          onClick: getMerges
        }, {
          default: withCtx(() => [
            createBaseVNode("div", _hoisted_2$i, [
              createBaseVNode("span", {
                class: normalizeClass(["fonticon INTFCenter-color-brown fonticon-expand-right", {
                  "INTFCenter-merges-opened": opened.value,
                  "INTFCenter-merges-closed": !opened.value
                }])
              }, null, 2),
              createBaseVNode("div", _hoisted_3$c, [
                !opened.value ? (openBlock(), createElementBlock("span", _hoisted_4$a, toDisplayString(unref($i18n2)("viewAll")), 1)) : (openBlock(), createElementBlock("span", _hoisted_5$6, toDisplayString(unref($i18n2)("hideAll")), 1))
              ]),
              openRequested.value ? (openBlock(), createElementBlock("span", _hoisted_6$5, [
                createVNode(_component_vu_spinner)
              ])) : createCommentVNode("", true)
            ])
          ]),
          _: 1
        }, 8, ["class"]),
        createVNode(Transition, { name: "merge-list" }, {
          default: withCtx(() => [
            opened.value ? (openBlock(), createElementBlock("div", _hoisted_7$2, [
              createVNode(TransitionGroup, { name: "list" }, {
                default: withCtx(() => [
                  (openBlock(true), createElementBlock(Fragment, null, renderList(groupMerges.value, (merge) => {
                    return openBlock(), createElementBlock("div", {
                      key: merge.id
                    }, [
                      (openBlock(), createBlock(resolveDynamicComponent(components[unref(store).getMergeById(merge.id).deleted ? "NotificationDeleted" : "NotificationBody"]), {
                        id: merge.id,
                        key: `${merge.id} - merge`,
                        class: "INTFCenter-group-body",
                        merge: true,
                        notification: unref(store).getMergeById(merge.id),
                        onCancelDeletion: _cache[0] || (_cache[0] = (id2) => unref(cancelDeletion)(id2, true))
                      }, null, 40, ["id", "notification"]))
                    ]);
                  }), 128))
                ]),
                _: 1
              }),
              groupMerges.value.length < allMerges.value.length ? (openBlock(), createElementBlock("div", {
                key: 0,
                class: normalizeClass({ "INTFCenter-is-read": __props.isRead })
              }, [
                createBaseVNode("span", {
                  class: "INTFCenter-color-3ds INTFCenter-view-group cursor-pointer",
                  onClick: seeMoreMerge
                }, toDisplayString(unref($i18n2)("viewMore")), 1)
              ], 2)) : createCommentVNode("", true)
            ])) : createCommentVNode("", true)
          ]),
          _: 1
        })
      ])) : createCommentVNode("", true);
    };
  }
};
const _sfc_main$w = {
  __name: "SelectWrapper",
  props: {
    id: String
  },
  emits: ["select"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit2 = __emit;
    const store = useNotificationsStore();
    const select = ref(false);
    function applySelect() {
      if (select.value) {
        select.value = false;
        store.selected.splice(store.selected.indexOf(props.id), 1);
      } else {
        select.value = true;
        if (!store.selected.includes(props.id))
          store.selected.push(props.id);
      }
      emit2("select", select.value);
    }
    watch(
      () => store.selected.length,
      () => {
        if (store.selected.includes(props.id))
          select.value = true;
        else select.value = false;
        emit2("select", select.value);
      }
    );
    onBeforeUnmount(() => {
      emit2("select", false);
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(["INTFCenter-selected-icon", { "INTFCenter-color-3ds": select.value, "INTFCenter-color-brown": !select.value }]),
        onClick: applySelect
      }, [..._cache[0] || (_cache[0] = [
        createBaseVNode("span", { class: "fonticon fonticon-status-ok text-2xl" }, null, -1)
      ])], 2);
    };
  }
};
const _hoisted_1$r = { class: "INTFCenter-notification-item" };
const _hoisted_2$h = { class: "INTFCenter-notif-body-ui" };
const _sfc_main$v = {
  __name: "NotificationItem",
  props: {
    notification: {
      type: Notification$1
    }
  },
  setup(__props) {
    const props = __props;
    const store = useNotificationsStore();
    const select = ref(false);
    const selectionMode = computed(() => {
      return store.selectionMode === true;
    });
    const selected2 = computed(() => {
      return store.selected.includes(props.notification.ID);
    });
    function applySelect(value) {
      select.value = value;
    }
    return (_ctx, _cache) => {
      return __props.notification ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["INTFCenter-notification-item-container", { "INTFCenter-notif-is-selected": selected2.value, "INTFCenter-selection-mode-on": selectionMode.value }])
      }, [
        createVNode(TransitionGroup, { name: "wrapper" }, {
          default: withCtx(() => [
            selectionMode.value ? (openBlock(), createBlock(_sfc_main$w, {
              key: 0,
              id: __props.notification.ID,
              onSelect: applySelect
            }, null, 8, ["id"])) : createCommentVNode("", true)
          ]),
          _: 1
        }),
        createBaseVNode("div", _hoisted_1$r, [
          createVNode(_sfc_main$B, {
            date: __props.notification.getHourMinutes(),
            "is-read": __props.notification.isRead
          }, null, 8, ["date", "is-read"]),
          createBaseVNode("div", {
            class: normalizeClass([`INTFCenter-notif-body ${__props.notification.APPID}`])
          }, [
            createBaseVNode("div", _hoisted_2$h, [
              createVNode(_sfc_main$y, {
                "is-read": __props.notification.isRead,
                "is-starred": __props.notification.isStarred,
                notification: __props.notification
              }, null, 8, ["is-read", "is-starred", "notification"]),
              createVNode(_sfc_main$z, {
                class: "INTFCenter-notif-body-component",
                notification: __props.notification
              }, null, 8, ["notification"])
            ]),
            __props.notification.isGroup ? (openBlock(), createBlock(_sfc_main$x, {
              key: 0,
              "group-id": __props.notification.GROUPID,
              "is-read": __props.notification.isRead,
              "main-id": __props.notification.ID
            }, null, 8, ["group-id", "is-read", "main-id"])) : createCommentVNode("", true)
          ], 2)
        ])
      ], 2)) : createCommentVNode("", true);
    };
  }
};
const _hoisted_1$q = {
  key: 0,
  class: "INTFCenter-notif-center-timeline-section"
};
const _hoisted_2$g = { class: "INTFCenter-timeline-section-title INTFCenter-color-brown" };
const _sfc_main$u = {
  __name: "SectionItem",
  props: {
    section: Section
  },
  setup(__props) {
    const { getNotificationById } = useNotificationsStore();
    const { cancelDeletion } = useNotificationManagement();
    const components = {
      NotificationItem: _sfc_main$v,
      NotificationDeleted: _sfc_main$F
    };
    return (_ctx, _cache) => {
      return __props.section ? (openBlock(), createElementBlock("div", _hoisted_1$q, [
        createBaseVNode("div", _hoisted_2$g, toDisplayString(__props.section.title), 1),
        createVNode(TransitionGroup, { name: "list" }, {
          default: withCtx(() => [
            (openBlock(true), createElementBlock(Fragment, null, renderList(__props.section.notifs, (item) => {
              return openBlock(), createElementBlock("div", {
                key: item.id,
                class: "INTFCenter-section-notification-container"
              }, [
                createVNode(Transition, { name: "notification" }, {
                  default: withCtx(() => [
                    (openBlock(), createBlock(resolveDynamicComponent(components[unref(getNotificationById)(item.id).deleted ? "NotificationDeleted" : "NotificationItem"]), {
                      id: item.id,
                      key: item.id,
                      notification: unref(getNotificationById)(item.id),
                      onCancelDeletion: _cache[0] || (_cache[0] = (id2) => unref(cancelDeletion)(id2))
                    }, null, 40, ["id", "notification"]))
                  ]),
                  _: 2
                }, 1024)
              ]);
            }), 128))
          ]),
          _: 1
        })
      ])) : createCommentVNode("", true);
    };
  }
};
const _sfc_main$t = {
  __name: "SectionList",
  setup(__props) {
    const store = useNotificationsStore();
    return (_ctx, _cache) => {
      return openBlock(), createBlock(TransitionGroup, { name: "list" }, {
        default: withCtx(() => [
          (openBlock(true), createElementBlock(Fragment, null, renderList(unref(store).sectionListIds, (id2) => {
            return openBlock(), createBlock(_sfc_main$u, {
              key: id2,
              section: unref(store).sectionList.get(id2)
            }, null, 8, ["section"]);
          }), 128))
        ]),
        _: 1
      });
    };
  }
};
const _hoisted_1$p = { class: "INTFCenter-filter-footer" };
const _hoisted_2$f = { class: "INTFCenter-filter-bottom-container" };
const _hoisted_3$b = { class: "INTFCenter-bottom-filter" };
const _sfc_main$s = {
  __name: "FilterButtons",
  emits: ["close", "apply", "reset"],
  setup(__props, { emit: __emit }) {
    const emit2 = __emit;
    const { $i18n: $i18n2 } = useTranslations$1();
    function closeFilter() {
      emit2("close");
    }
    function resetFilter() {
      emit2("reset");
    }
    function applyFilter() {
      emit2("apply");
    }
    return (_ctx, _cache) => {
      const _component_vu_btn = resolveComponent("vu-btn");
      return openBlock(), createElementBlock("div", _hoisted_1$p, [
        createBaseVNode("div", _hoisted_2$f, [
          createBaseVNode("div", _hoisted_3$b, [
            createVNode(_component_vu_btn, {
              class: "INTFCenter-filter-btn-filter",
              color: "primary",
              onClick: applyFilter
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(unref($i18n2)("filter")), 1)
              ]),
              _: 1
            }),
            createVNode(_component_vu_btn, {
              class: "INTFCenter-filter-btn-reset",
              onClick: resetFilter
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(unref($i18n2)("reset")), 1)
              ]),
              _: 1
            }),
            createVNode(_component_vu_btn, {
              class: "INTFCenter-filter-btn-close",
              onClick: closeFilter
            }, {
              default: withCtx(() => [
                createTextVNode(toDisplayString(unref($i18n2)("close")), 1)
              ]),
              _: 1
            })
          ])
        ])
      ]);
    };
  }
};
var __defProp2 = Object.defineProperty;
var __defNormalProp2 = (obj, key, value) => key in obj ? __defProp2(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField2 = (obj, key, value) => __defNormalProp2(obj, typeof key !== "symbol" ? key + "" : key, value);
const LOG_LEVELS = ["trace", "debug", "log", "info", "warn", "error"];
function getTSMLogLevel() {
  if (typeof window === "undefined")
    return false;
  return window.sessionStorage.getItem("DSY_SWYM_DEBUG") || false;
}
function getLocalStorageLogLevel() {
  if (typeof window === "undefined")
    return false;
  return window.localStorage.getItem("logger-debug") || false;
}
function getQueryStringLogLevel() {
  if (typeof window === "undefined")
    return false;
  return new URL(window.location.href).searchParams.has("debug");
}
function parseLogLevel(value) {
  if (typeof value === "string") {
    if (LOG_LEVELS.includes(value))
      return value;
    if (value === "false")
      return false;
    return true;
  }
  return value;
}
function getLogLevelIndex(value) {
  if (typeof value === "boolean") {
    if (value)
      return 0;
    else
      return LOG_LEVELS.length;
  }
  return LOG_LEVELS.indexOf(value);
}
class Logger {
  constructor() {
    __publicField2(this, "logLevelIndex", getLogLevelIndex(false));
    __publicField2(this, "_log", (level, ...message) => {
      const scope = this._getScope(level);
      const emoji = this.emojis[level];
      const style = this.styles[level];
      this._shouldLog(level) && console[level](`${emoji} %c[${scope}]`, style, ...message);
    });
    __publicField2(this, "emojis", {
      trace: "🧵",
      debug: "🛠️",
      log: "📋",
      info: "📢",
      warn: "🚧",
      error: "🔥"
    });
    __publicField2(this, "styles", {
      trace: "",
      debug: "color: white; background: #3d3d3d;",
      log: "color: black; background: #D5E8F2;",
      info: "color: black; background: #EDF6EB;",
      warn: "background: transparent;",
      error: "background: transparent;"
    });
    __publicField2(this, "_getScope", (level) => {
      return `LOGGER ${level.toUpperCase().padEnd(5, " ")}`;
    });
    __publicField2(this, "log", this._log.bind(this, "log"));
    __publicField2(this, "info", this._log.bind(this, "info"));
    __publicField2(this, "warn", this._log.bind(this, "warn"));
    __publicField2(this, "debug", this._log.bind(this, "debug"));
    __publicField2(this, "error", this._log.bind(this, "error"));
    __publicField2(this, "trace", this._log.bind(this, "trace"));
    const currentLogLevel = parseLogLevel(getQueryStringLogLevel()) || parseLogLevel(getLocalStorageLogLevel()) || parseLogLevel(getTSMLogLevel());
    this.logLevelIndex = getLogLevelIndex(currentLogLevel);
  }
  _shouldLog(level) {
    const levelIndex = LOG_LEVELS.indexOf(level);
    if (levelIndex >= this.logLevelIndex)
      return true;
    return false;
  }
}
const logger = new Logger();
const AMDLOADER_PATH = "AmdLoader/AmdLoader.js";
const CACHE_BUST_ARG = `bust=NO_CACHE_ID`;
let initAsync;
const hooksConfig = [];
const hooksAfterRequireSetup = [];
async function getRequireConfig() {
  const defaultPath = { DS: "." };
  let config = { paths: { ...defaultPath } };
  let hooksPromise = Promise.resolve({});
  while (hooksConfig.length)
    hooksPromise = hooksPromise.then(hooksConfig.pop());
  try {
    config = await hooksPromise;
  } catch (error) {
    logger.error("[RequireJS] Failed to load config:", error);
  }
  config.paths = { ...config.paths, ...defaultPath };
  if (!config.urlArgs)
    config.urlArgs = CACHE_BUST_ARG;
  return config;
}
async function runAfterRequireSetupHook() {
  let hooksPromise = Promise.resolve(void 0);
  while (hooksAfterRequireSetup.length)
    hooksPromise = hooksPromise.then(hooksAfterRequireSetup.pop());
  try {
    await hooksPromise;
  } catch (error) {
    logger.error('[RequireJS] Failed to run "after require setup" hooks:', error);
  }
}
function setupRequireConfig(config) {
  if (!window.require)
    throw new Error("window.require is not initialiazed");
  window.require.config(config);
  logger.debug("[RequireJS] Set config:", config);
}
async function addAMDLoaderScript(config) {
  if (typeof window.require === "function")
    return;
  const script = document.createElement("script");
  const loadingAsync = new Promise((resolve2, reject) => {
    script.onload = resolve2;
    script.onerror = reject;
  });
  script.src = `${config.baseUrl ?? ""}${AMDLOADER_PATH}?${config.urlArgs}`;
  logger.debug("[RequireJS] Download AMDLoader script: ", script.src);
  document.head.appendChild(script);
  await loadingAsync;
}
async function initRequire() {
  if (initAsync)
    return initAsync;
  let resolve2;
  let reject;
  initAsync = new Promise((res, rej) => {
    resolve2 = res;
    reject = rej;
  });
  logger.debug("[RequireJS] init");
  try {
    const config = await getRequireConfig();
    await addAMDLoaderScript(config);
    await setupRequireConfig(config);
    await runAfterRequireSetupHook();
    resolve2 == null ? void 0 : resolve2();
  } catch (error) {
    reject == null ? void 0 : reject();
  }
  return initAsync;
}
async function requirejs(modules, useTopWindow = false) {
  await initRequire();
  return new Promise((resolve2, reject) => {
    var _a3, _b2;
    (_b2 = (_a3 = useTopWindow && window.top ? window.top : window).require) == null ? void 0 : _b2.call(
      _a3,
      modules,
      (...m) => resolve2(m),
      reject
    );
  });
}
async function requirejsOne(module, useTopWindow = false) {
  return (await requirejs([module], useTopWindow))[0];
}
function useTranslations(filename, path = "i18n!DS/SwymVueComponents/assets/nls/", modules) {
  const i18n2 = ref({});
  const promise = new Promise(async (resolve2) => {
    try {
      const result = await requirejsOne(`${path}${filename}`);
      i18n2.value = Object.assign(unref(i18n2), result);
    } catch {
      logger.error(`[TRANSLATION] Error loading nls ${path}${filename}`);
    }
    resolve2(i18n2.value);
  });
  function $i18n2(key, variables = {}) {
    if (!key)
      return "";
    let value = unref(i18n2)[key];
    Object.entries(variables).forEach(([key2, val]) => {
      value = value == null ? void 0 : value.replace(`{${key2}}`, val);
    });
    return value || key;
  }
  return { i18n: i18n2, $i18n: $i18n2, promise };
}
function tryOnScopeDispose$1(fn) {
  if (getCurrentScope()) {
    onScopeDispose(fn);
    return true;
  }
  return false;
}
function toValue$1(r) {
  return typeof r === "function" ? r() : unref(r);
}
const isClient$1 = typeof window !== "undefined" && typeof document !== "undefined";
typeof WorkerGlobalScope !== "undefined" && globalThis instanceof WorkerGlobalScope;
const notNullish = (val) => val != null;
const noop$1 = () => {
};
function increaseWithUnit(target, delta) {
  var _a3;
  if (typeof target === "number")
    return target + delta;
  const value = ((_a3 = target.match(/^-?\d+\.?\d*/)) == null ? void 0 : _a3[0]) || "";
  const unit = target.slice(value.length);
  const result = Number.parseFloat(value) + delta;
  if (Number.isNaN(result))
    return target;
  return result + unit;
}
function getLifeCycleTarget(target) {
  return getCurrentInstance();
}
function toRef$1(...args) {
  if (args.length !== 1)
    return toRef$3(...args);
  const r = args[0];
  return typeof r === "function" ? readonly(customRef(() => ({ get: r, set: noop$1 }))) : ref(r);
}
function tryOnBeforeUnmount(fn, target) {
  const instance = getLifeCycleTarget();
  if (instance)
    onBeforeUnmount(fn, target);
}
function computedAsync(evaluationCallback, initialState, optionsOrRef) {
  let options;
  if (isRef(optionsOrRef)) {
    options = {
      evaluating: optionsOrRef
    };
  } else {
    options = {};
  }
  const {
    lazy = false,
    evaluating = void 0,
    shallow = true,
    onError = noop$1
  } = options;
  const started = ref(!lazy);
  const current = shallow ? shallowRef(initialState) : ref(initialState);
  let counter = 0;
  watchEffect(async (onInvalidate) => {
    if (!started.value)
      return;
    counter++;
    const counterAtBeginning = counter;
    let hasFinished = false;
    if (evaluating) {
      Promise.resolve().then(() => {
        evaluating.value = true;
      });
    }
    try {
      const result = await evaluationCallback((cancelCallback) => {
        onInvalidate(() => {
          if (evaluating)
            evaluating.value = false;
          if (!hasFinished)
            cancelCallback();
        });
      });
      if (counterAtBeginning === counter)
        current.value = result;
    } catch (e) {
      onError(e);
    } finally {
      if (evaluating && counterAtBeginning === counter)
        evaluating.value = false;
      hasFinished = true;
    }
  });
  if (lazy) {
    return computed(() => {
      started.value = true;
      return current.value;
    });
  } else {
    return current;
  }
}
const defaultWindow$1 = isClient$1 ? window : void 0;
const defaultDocument = isClient$1 ? window.document : void 0;
function unrefElement$1(elRef) {
  var _a3;
  const plain = toValue$1(elRef);
  return (_a3 = plain == null ? void 0 : plain.$el) != null ? _a3 : plain;
}
function useMounted() {
  const isMounted = ref(false);
  const instance = getCurrentInstance();
  if (instance) {
    onMounted(() => {
      isMounted.value = true;
    }, instance);
  }
  return isMounted;
}
function useSupported(callback) {
  const isMounted = useMounted();
  return computed(() => {
    isMounted.value;
    return Boolean(callback());
  });
}
function useMutationObserver(target, callback, options = {}) {
  const { window: window2 = defaultWindow$1, ...mutationOptions } = options;
  let observer;
  const isSupported = useSupported(() => window2 && "MutationObserver" in window2);
  const cleanup = () => {
    if (observer) {
      observer.disconnect();
      observer = void 0;
    }
  };
  const targets = computed(() => {
    const value = toValue$1(target);
    const items = (Array.isArray(value) ? value : [value]).map(unrefElement$1).filter(notNullish);
    return new Set(items);
  });
  const stopWatch = watch(
    () => targets.value,
    (targets2) => {
      cleanup();
      if (isSupported.value && targets2.size) {
        observer = new MutationObserver(callback);
        targets2.forEach((el2) => observer.observe(el2, mutationOptions));
      }
    },
    { immediate: true, flush: "post" }
  );
  const takeRecords = () => {
    return observer == null ? void 0 : observer.takeRecords();
  };
  const stop2 = () => {
    stopWatch();
    cleanup();
  };
  tryOnScopeDispose$1(stop2);
  return {
    isSupported,
    stop: stop2,
    takeRecords
  };
}
function useMediaQuery(query, options = {}) {
  const { window: window2 = defaultWindow$1 } = options;
  const isSupported = useSupported(() => window2 && "matchMedia" in window2 && typeof window2.matchMedia === "function");
  let mediaQuery;
  const matches = ref(false);
  const handler = (event) => {
    matches.value = event.matches;
  };
  const cleanup = () => {
    if (!mediaQuery)
      return;
    if ("removeEventListener" in mediaQuery)
      mediaQuery.removeEventListener("change", handler);
    else
      mediaQuery.removeListener(handler);
  };
  const stopWatch = watchEffect(() => {
    if (!isSupported.value)
      return;
    cleanup();
    mediaQuery = window2.matchMedia(toValue$1(query));
    if ("addEventListener" in mediaQuery)
      mediaQuery.addEventListener("change", handler);
    else
      mediaQuery.addListener(handler);
    matches.value = mediaQuery.matches;
  });
  tryOnScopeDispose$1(() => {
    stopWatch();
    cleanup();
    mediaQuery = void 0;
  });
  return matches;
}
function useBreakpoints(breakpoints2, options = {}) {
  function getValue2(k, delta) {
    let v = toValue$1(breakpoints2[toValue$1(k)]);
    if (delta != null)
      v = increaseWithUnit(v, delta);
    if (typeof v === "number")
      v = `${v}px`;
    return v;
  }
  const { window: window2 = defaultWindow$1, strategy = "min-width" } = options;
  function match(query) {
    if (!window2)
      return false;
    return window2.matchMedia(query).matches;
  }
  const greaterOrEqual = (k) => {
    return useMediaQuery(() => `(min-width: ${getValue2(k)})`, options);
  };
  const smallerOrEqual = (k) => {
    return useMediaQuery(() => `(max-width: ${getValue2(k)})`, options);
  };
  const shortcutMethods = Object.keys(breakpoints2).reduce((shortcuts, k) => {
    Object.defineProperty(shortcuts, k, {
      get: () => strategy === "min-width" ? greaterOrEqual(k) : smallerOrEqual(k),
      enumerable: true,
      configurable: true
    });
    return shortcuts;
  }, {});
  function current() {
    const points = Object.keys(breakpoints2).map((i) => [i, greaterOrEqual(i)]);
    return computed(() => points.filter(([, v]) => v.value).map(([k]) => k));
  }
  return Object.assign(shortcutMethods, {
    greaterOrEqual,
    smallerOrEqual,
    greater(k) {
      return useMediaQuery(() => `(min-width: ${getValue2(k, 0.1)})`, options);
    },
    smaller(k) {
      return useMediaQuery(() => `(max-width: ${getValue2(k, -0.1)})`, options);
    },
    between(a, b) {
      return useMediaQuery(() => `(min-width: ${getValue2(a)}) and (max-width: ${getValue2(b, -0.1)})`, options);
    },
    isGreater(k) {
      return match(`(min-width: ${getValue2(k, 0.1)})`);
    },
    isGreaterOrEqual(k) {
      return match(`(min-width: ${getValue2(k)})`);
    },
    isSmaller(k) {
      return match(`(max-width: ${getValue2(k, -0.1)})`);
    },
    isSmallerOrEqual(k) {
      return match(`(max-width: ${getValue2(k)})`);
    },
    isInBetween(a, b) {
      return match(`(min-width: ${getValue2(a)}) and (max-width: ${getValue2(b, -0.1)})`);
    },
    current,
    active() {
      const bps = current();
      return computed(() => bps.value.length === 0 ? "" : bps.value.at(-1));
    }
  });
}
function useTitle(newTitle = null, options = {}) {
  var _a3, _b2, _c3;
  const {
    document: document2 = defaultDocument,
    restoreOnUnmount = (t) => t
  } = options;
  const originalTitle = (_a3 = document2 == null ? void 0 : document2.title) != null ? _a3 : "";
  const title = toRef$1((_b2 = newTitle != null ? newTitle : document2 == null ? void 0 : document2.title) != null ? _b2 : null);
  const isReadonly2 = newTitle && typeof newTitle === "function";
  function format(t) {
    if (!("titleTemplate" in options))
      return t;
    const template = options.titleTemplate || "%s";
    return typeof template === "function" ? template(t) : toValue$1(template).replace(/%s/g, t);
  }
  watch(
    title,
    (t, o) => {
      if (t !== o && document2)
        document2.title = format(typeof t === "string" ? t : "");
    },
    { immediate: true }
  );
  if (options.observe && !options.titleTemplate && document2 && !isReadonly2) {
    useMutationObserver(
      (_c3 = document2.head) == null ? void 0 : _c3.querySelector("title"),
      () => {
        if (document2 && document2.title !== title.value)
          title.value = format(document2.title);
      },
      { childList: true }
    );
  }
  tryOnBeforeUnmount(() => {
    if (restoreOnUnmount) {
      const restoredTitle = restoreOnUnmount(originalTitle, title.value || "");
      if (restoredTitle != null && document2)
        document2.title = restoredTitle;
    }
  });
  return title;
}
const xs = "380px";
const tiny = "465px";
const sm = "640px";
const md = "768px";
const lg = "1024px";
const xl = "1280px";
const breakpoints = {
  "2xs": "300px",
  xs,
  tiny,
  sm,
  md,
  "2md": "960px",
  lg,
  xl,
  "2xl": "1536px",
  "3xl": "1920px"
};
const BREAKPOINTS = (() => {
  const breakPts = {};
  for (const [key, value] of Object.entries(breakpoints))
    breakPts[key] = Number.parseInt(value, 10);
  return Object.freeze(breakPts);
})();
useBreakpoints(BREAKPOINTS).smaller("sm");
const _export_sfc$1 = (sfc, props) => {
  const target = sfc.__vccOpts || sfc;
  for (const [key, val] of props) {
    target[key] = val;
  }
  return target;
};
const _export_sfc = (sfc, props) => {
  const target = sfc.__vccOpts || sfc;
  for (const [key, val] of props) {
    target[key] = val;
  }
  return target;
};
const _sfc_main$r = /* @__PURE__ */ defineComponent({
  __name: "vu-icon",
  props: {
    color: { default: "default" },
    icon: {},
    isHorizontalMargins: { type: Boolean },
    size: {}
  },
  setup(__props) {
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("span", {
        class: normalizeClass(["vu-icon fonticon", [`fonticon-${__props.icon}`, __props.color, __props.size, {
          "fonticon-within-text": __props.isHorizontalMargins
        }]])
      }, null, 2);
    };
  }
});
const VuIcon = /* @__PURE__ */ _export_sfc(_sfc_main$r, [["__scopeId", "data-v-acfd7af8"]]);
function useRegistrableInput(input) {
  const inputs = inject("inputs", []);
  onMounted(() => {
    if (typeof inputs === "object")
      inputs == null ? void 0 : inputs.push(input);
  });
  onBeforeMount(() => {
    const i = inputs == null ? void 0 : inputs.indexOf(input);
    if (i !== -1 && Array.isArray(inputs))
      inputs.splice(i, 1);
  });
}
function useValidatable(inputValue, props) {
  const errorBucketRef = ref([]);
  const validRef = ref(true);
  const localRules = ref([]);
  const classes = computed(() => {
    return {
      "has-error": !validRef.value,
      "has-success": props.success && validRef.value
    };
  });
  const hasError = computed(() => {
    return errorBucketRef.value.length > 0;
  });
  const hasSuccess = computed(() => {
    return errorBucketRef.value.length === 0;
  });
  const isValid = computed(() => {
    if (!props.required)
      return true;
    const val = inputValue.value;
    if (typeof val === "string" || Array.isArray(val))
      return val.length !== 0;
    if (val instanceof Date)
      return !Number.isNaN(val.getTime());
    if (typeof val === "number")
      return val !== 0;
    return true;
  });
  watch(
    () => inputValue.value,
    (newValue) => {
      if (!props.lazyValidation)
        validRef.value = validate(newValue);
    }
  );
  function validate(value, blank = false) {
    const errorBucket = [];
    let errorCount = 0;
    const v = value || inputValue.value;
    const rules = [...localRules.value, ...props.rules ?? []];
    for (let index = 0; index < rules.length; index++) {
      const rule = rules[index];
      const valid = typeof rule === "function" ? rule(v) : rule;
      if (typeof valid === "string") {
        errorBucket == null ? void 0 : errorBucket.push(valid);
        errorCount += 1;
      } else if (typeof valid === "boolean" && !valid) {
        errorCount += 1;
      }
    }
    if (!blank)
      errorBucketRef.value = errorBucket;
    validRef.value = errorCount === 0 && isValid.value;
    return validRef.value;
  }
  return {
    classes,
    errorBucketRef,
    hasError,
    hasSuccess,
    validate
  };
}
const _hoisted_1$o = ["id", "checked", "disabled", "name", "type", "value"];
const _hoisted_2$e = ["for"];
const _hoisted_3$a = { class: "vu-single-checkbox__inner-span" };
const _hoisted_4$9 = {
  key: 0,
  class: "vu-single-checkbox__extra-content"
};
const _sfc_main$q = /* @__PURE__ */ defineComponent({
  __name: "vu-single-checkbox",
  props: /* @__PURE__ */ mergeModels({
    disabled: { type: Boolean },
    group: {},
    icon: {},
    label: {},
    radio: { type: Boolean },
    standalone: { type: Boolean },
    switch: { type: Boolean },
    value: {},
    lazyValidation: { type: Boolean }
  }, {
    "modelValue": { type: [Boolean, String] },
    "modelModifiers": {}
  }),
  emits: ["update:modelValue"],
  setup(__props) {
    const props = __props;
    const slots = useSlots();
    const inputValue = useModel(__props, "modelValue");
    const validatable = useValidatable(inputValue, props);
    useRegistrableInput({
      validate: () => validatable.validate(inputValue.value)
    });
    const uuid = crypto.randomUUID();
    const mode = computed(() => {
      if (props.radio && props.switch || !props.radio && !props.switch) {
        return {
          checkbox: true,
          checked: inputValue.value,
          name: "",
          radio: false,
          switch: false,
          type: "checkbox",
          value: ""
        };
      }
      if (props.radio) {
        return {
          checkbox: false,
          checked: props.group === inputValue.value,
          name: props.group,
          radio: true,
          type: "radio",
          value: props.value
        };
      }
      return {
        checkbox: false,
        checked: inputValue.value,
        switch: true,
        type: "checkbox"
      };
    });
    const hasExtraContent = computed(() => !!slots.default && !props.standalone);
    const canAddExtraLabel = computed(
      () => (props.label || props.icon) && !props.standalone
    );
    const internalClasses = computed(() => {
      return {
        "toggle-icon": props.icon,
        "toggle-switch": mode.value.switch,
        "toggle-primary": !mode.value.switch
      };
    });
    const topClasses = computed(() => {
      return {
        "vu-single-checkbox--switch": mode.value.switch,
        "vu-single-checkbox--standalone": props.standalone,
        "vu-single-checkbox--checkbox": mode.value.checkbox,
        "vu-single-checkbox--radio": mode.value.radio,
        "vu-single-checkbox--extra-content": hasExtraContent.value
      };
    });
    function handleInput(event) {
      const input = event.target;
      if (!mode.value.radio)
        inputValue.value = input.checked;
      else inputValue.value = input.value;
    }
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(["vu-single-checkbox", topClasses.value])
      }, [
        createBaseVNode("div", {
          class: normalizeClass(["toggle", internalClasses.value])
        }, [
          createBaseVNode("input", mergeProps(_ctx.$attrs, {
            id: unref(uuid),
            checked: mode.value.checked,
            class: "vu-single-checkbox__input",
            disabled: __props.disabled,
            name: mode.value.name,
            type: mode.value.type,
            value: mode.value.value,
            onClick: handleInput
          }), null, 16, _hoisted_1$o),
          createBaseVNode("label", {
            class: "control-label vu-single-checkbox__label",
            for: unref(uuid)
          }, [
            !__props.standalone ? (openBlock(), createElementBlock(Fragment, { key: 0 }, [
              __props.icon ? (openBlock(), createBlock(VuIcon, {
                key: 0,
                icon: __props.icon,
                "is-horizontal-margins": ""
              }, null, 8, ["icon"])) : createCommentVNode("", true),
              createBaseVNode("span", _hoisted_3$a, toDisplayString(__props.label), 1)
            ], 64)) : createCommentVNode("", true)
          ], 8, _hoisted_2$e),
          canAddExtraLabel.value ? renderSlot(_ctx.$slots, "labelAfter", { key: 0 }, void 0, true) : createCommentVNode("", true)
        ], 2),
        hasExtraContent.value ? (openBlock(), createElementBlock("div", _hoisted_4$9, [
          renderSlot(_ctx.$slots, "default", {}, void 0, true)
        ])) : createCommentVNode("", true)
      ], 2);
    };
  }
});
const VuSingleCheckbox = /* @__PURE__ */ _export_sfc(_sfc_main$q, [["__scopeId", "data-v-1af514cd"]]);
function checkIsTouchDevice() {
  return "ontouchstart" in window || navigator.maxTouchPoints > 0 || navigator.msMaxTouchPoints > 0;
}
function checkIsIphoneOrIpadDevice() {
  const agent = navigator.userAgent.toLowerCase();
  return /iphone|ipad/i.test(agent) || /safari/.test(agent) && !/chrome/.test(agent) && checkIsTouchDevice();
}
class AppBehavior {
  constructor() {
    this.appSettings = null;
    return this;
  }
  setAppPreferences(appSettings) {
    this.appSettings = appSettings;
  }
  allowODECreationFeature() {
    if (this.appSettings && this.appSettings.allowODECreationFeature)
      return this.appSettings.allowODECreationFeature;
    return false;
  }
  allowODEViewerFeature() {
    if (this.appSettings && this.appSettings.allowODEViewerFeature)
      return this.appSettings.allowODEViewerFeature;
    return false;
  }
  allowODECollabFeature() {
    if (this.appSettings && this.appSettings.allowODECollabFeature)
      return this.appSettings.allowODECollabFeature;
    return false;
  }
  activateNativeWindowBar() {
    if (this.appSettings && this.appSettings.activateNativeWindowBar)
      return this.appSettings.activateNativeWindowBar;
    return false;
  }
  hasMultiWindowsSupport() {
    if (this.appSettings && this.appSettings.hasMultiWindowsSupport)
      return this.appSettings.hasMultiWindowsSupport;
    return false;
  }
  disableTopbarLogout() {
    if (this.appSettings && this.appSettings.disableTopbarLogout)
      return this.appSettings.disableTopbarLogout;
    return false;
  }
  hideMePreferenceLanguage() {
    if (this.appSettings && this.appSettings.hideMePreferenceLanguage)
      return this.appSettings.hideMePreferenceLanguage;
    return false;
  }
}
const AppFeaturePreference = new AppBehavior();
const DEFAULT_TIMEOUT = 5 * 1e3;
class MyTimeout {
  constructor() {
    this.set = (delay2, reason) => new Promise((resolve2, reject) => {
      const id2 = setTimeout(() => {
        if (reason === void 0)
          resolve2(null);
        else
          reject(reason);
        this.clear(id2);
      }, delay2);
      this.ids.push(id2);
    });
    this.wrap = (promise, delay2, reason) => Promise.race([promise, this.set(delay2, reason)]);
    this.clear = (...ids) => {
      this.ids = this.ids.filter((id2) => {
        if (ids.includes(id2)) {
          clearTimeout(id2);
          return false;
        }
        return true;
      });
    };
    this.ids = [];
  }
}
const fetch$1 = (emitEvent, receiveEvent, args) => new Promise((resolve2, reject) => {
  if (!window || !window.nsWebViewInterface)
    reject("No webview interface found!");
  window.nsWebViewInterface.emit(emitEvent, args);
  window.nsWebViewInterface.once(receiveEvent, (data) => {
    resolve2(data);
  });
});
class AppNativeBridge {
  constructor() {
    return this;
  }
  static emitEventToNativeApp(channel, data) {
    try {
      if (window && window.electron && window.electron.ipcRenderer) {
        window.electron.ipcRenderer.send(channel, data);
      } else if (window && window.nsWebViewInterface) {
        window.nsWebViewInterface.emit(channel, data);
      }
    } catch (error) {
      console.log(`Failed with reason: ${error}`);
    }
  }
  static async invokeEventToNativeApp(channel, data) {
    if (window && window.electron && window.electron.ipcRenderer) {
      return await window.electron.ipcRenderer.invoke(channel, data);
    } else {
      throw new Error("Method not supported!");
    }
  }
  static listenEventFromNativeApp(channel, callback) {
    try {
      if (window && window.electron && window.electron.ipcRenderer) {
        window.electron.ipcRenderer.on(channel, (event, data) => callback(data));
      } else if (window && window.nsWebViewInterface) {
        window.nsWebViewInterface.on(channel, (data) => callback(data));
      }
    } catch (error) {
      console.log(`Failed with reason: ${error}`);
    }
  }
  static listenOnceEventFromNativeApp(channel, callback) {
    try {
      if (window && window.electron && window.electron.ipcRenderer) {
        window.electron.ipcRenderer.once(channel, (event, data) => callback(data));
      } else if (window && window.nsWebViewInterface) {
        window.nsWebViewInterface.once(channel, (data) => callback(data));
      }
    } catch (error) {
      console.log(`Failed with reason: ${error}`);
    }
  }
  static async getAppPreferences() {
    let appPref = null;
    try {
      if (window && window.electron) {
        appPref = await window.electron.getAppPreferences();
      } else if (window && window.nsWebViewInterface) {
        const timeout2 = new MyTimeout();
        try {
          const data = await timeout2.wrap(fetch$1("PreferencesReq:getAppPreferences", "PreferencesRes:getAppPreferences", {}), DEFAULT_TIMEOUT, "Fetch timeout!");
          return Promise.resolve(data);
        } catch (error) {
          console.log(`Failed with reason: ${error}`);
          timeout2.clear(...timeout2.ids);
          return Promise.reject(error);
        }
      }
    } catch (error) {
      return Promise.reject(error);
    }
    return Promise.resolve(appPref);
  }
  // Send request to the native app to get the pairing platform
  static async getSelectedPlatform() {
    let selected2 = null;
    try {
      if (window && window.electron) {
        selected2 = await window.electron.getPairedPlatform();
      } else if (window && window.nsWebViewInterface) {
        const timeout2 = new MyTimeout();
        try {
          const data = await timeout2.wrap(fetch$1("PreferencesReq:getSelectedPlatform", "PreferencesRes:getSelectedPlatform", {}), DEFAULT_TIMEOUT, "Fetch timeout!");
          return Promise.resolve(data);
        } catch (error) {
          console.log(`Failed with reason: ${error}`);
          timeout2.clear(...timeout2.ids);
          return Promise.reject(error);
        }
      }
    } catch (error) {
      return Promise.reject(error);
    }
    return Promise.resolve(selected2);
  }
  static async getTrackerData() {
    let data = {};
    try {
      if (window && window.electron) {
        data = await window.electron.getTrackerData();
      } else if (window && window.nsWebViewInterface) {
        const timeout2 = new MyTimeout();
        try {
          const data2 = await timeout2.wrap(fetch$1("PreferencesReq:getTrackerData", "PreferencesRes:getTrackerData", {}), DEFAULT_TIMEOUT, "Fetch timeout!");
          return Promise.resolve(data2);
        } catch (error) {
          console.log(`Failed with reason: ${error}`);
          timeout2.clear(...timeout2.ids);
          return Promise.reject(error);
        }
      }
    } catch (error) {
      return Promise.reject(error);
    }
    return Promise.resolve(data);
  }
  static async getGDPRPreferences() {
    let user = {};
    try {
      if (window && window.electron) {
        user = await window.electron.getGDPRPreferences();
      } else if (window && window.nsWebViewInterface) {
        const timeout2 = new MyTimeout();
        try {
          const data = await timeout2.wrap(fetch$1("PreferencesReq:getGDPRPreferences", "PreferencesRes:getGDPRPreferences", {}), DEFAULT_TIMEOUT, "Fetch timeout!");
          return Promise.resolve(data);
        } catch (error) {
          console.log(`Failed with reason: ${error}`);
          timeout2.clear(...timeout2.ids);
          return Promise.reject(error);
        }
      }
    } catch (error) {
      return Promise.reject(error);
    }
    return Promise.resolve(user);
  }
  static async getCasTicket(service) {
    let casTicket = null;
    try {
      if (window && window.electron) {
        casTicket = await window.electron.getCasTicket(service);
      } else if (window && window.nsWebViewInterface) {
        const timeout2 = new MyTimeout();
        try {
          const timestamp = Date.now();
          const data = await timeout2.wrap(fetch$1("get-cas-ticket-service", "get-cas-ticket-service-response" + timestamp, { service, timestamp }), DEFAULT_TIMEOUT, "Fetch timeout!");
          casTicket = data && data.casTicket;
        } catch (error) {
          console.log(`Failed with reason: ${error}`);
          timeout2.clear(...timeout2.ids);
          return Promise.reject(error);
        }
      }
    } catch (error) {
      return Promise.reject(error);
    }
    return Promise.resolve(casTicket);
  }
  static async getDeviceOSVersion() {
    let info;
    try {
      if (window && window.electron && window.electron.getDeviceOSVersion) {
        info = await window.electron.getDeviceOSVersion();
      } else if (window && window.nsWebViewInterface) {
        const timeout2 = new MyTimeout();
        try {
          const timestamp = Date.now();
          info = await timeout2.wrap(fetch$1("get-device-os-version", "get-device-os-version-response" + timestamp, { timestamp }), DEFAULT_TIMEOUT, "Fetch timeout!");
        } catch (error) {
          console.log(`Failed with reason: ${error}`);
          timeout2.clear(...timeout2.ids);
          return Promise.reject(error);
        }
      }
    } catch (error) {
      return Promise.reject(error);
    }
    return Promise.resolve(info);
  }
}
class Preferences {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  constructor() {
  }
  // set the application dynamic properties, e.g. app version
  static setAppProperties(props) {
    if (!props)
      return;
    if (props.appVersion && typeof props.appVersion !== "undefined")
      this._appVersion = props.appVersion;
    this._appProperties = props;
  }
  static getAppProperties() {
    return this._appProperties;
  }
  // set the app behavior preferences, e.g. swym will check the app native behaviors
  static setAppPreferences(appSettings) {
    AppFeaturePreference.setAppPreferences(appSettings);
  }
  // check if in native application
  static isNativeApp() {
    if (window && typeof window.electron !== "undefined")
      return true;
    else if (window && window.nsWebViewInterface)
      return true;
    return false;
  }
  static isMobileApp() {
    return window && window.nsWebViewInterface && !window.electron;
  }
  static isDesktopApp() {
    return window && window.electron;
  }
  // check if on same native localhost
  static isNativeSameDomain(url) {
    return window && window.location && window.location.hostname === url.hostname;
  }
  // get custom requirejs configuration for native app
  static getNativeAppRequireConfig() {
    if (!this.isNativeApp())
      return void 0;
    else {
      if (window && window.nsWAP && window.nsWAP.appVersion && typeof window.nsWAP.appVersion !== "undefined") {
        this._appVersion = window.nsWAP.appVersion;
      }
      return {
        dsEnv: {
          env: "MOBILE"
        },
        proxies: {
          ajax: `${window.location.origin}/_api/proxy/ajax`,
          passport: `${window.location.origin}/_api/proxy/passport`
        },
        requirePaths: {
          "WAP": `${window.location.origin}/wap/${this._appVersion}`,
          "WAP2": `${window.location.origin}/WapInterface/${this._appVersion}`
        }
      };
    }
  }
  // get the pairing platform from native app
  static async getAppPairingPlatform() {
    return await AppNativeBridge.getSelectedPlatform();
  }
  static async getTrackerData() {
    return await AppNativeBridge.getTrackerData();
  }
  static async getGDPRPreferences() {
    return await AppNativeBridge.getGDPRPreferences();
  }
  static async getCasTicket(service) {
    return await AppNativeBridge.getCasTicket(service);
  }
  static async getDeviceOSVersion() {
    return await AppNativeBridge.getDeviceOSVersion();
  }
  // initialize the native app preferences...
  static async intializeNativeAppPreferences() {
    try {
      const appPref = await AppNativeBridge.getAppPreferences();
      if (appPref) {
        this.setAppProperties(appPref.appProperties);
        this.setAppPreferences(appPref.appFeatureActivation);
      }
      console.log("intializeNativeAppPreferences", appPref);
    } catch (error) {
      console.log("intializeNativeAppPreferences error", error);
    }
    return Promise.resolve();
  }
}
Preferences._appVersion = "1.0";
reactive(/* @__PURE__ */ new Map());
function parseCookie() {
  var _a3;
  try {
    return (_a3 = globalThis == null ? void 0 : globalThis.document) == null ? void 0 : _a3.cookie.split(";").map((v) => v.split("=")).reduce((acc, [key, value]) => {
      acc[decodeURIComponent(key == null ? void 0 : key.trim())] = decodeURIComponent(value == null ? void 0 : value.trim());
      return acc;
    }, {});
  } catch {
    return {};
  }
}
var _a;
((_a = parseCookie()) == null ? void 0 : _a.swymlang) || "en";
const _pageTitle = ref("");
const _topFrameTitle = ref("");
useTitle(_pageTitle);
readonly(_pageTitle);
readonly(_topFrameTitle);
class HTTPError extends Error {
  constructor(response, request, options) {
    const code = response.status || response.status === 0 ? response.status : "";
    const title = response.statusText ?? "";
    const status = `${code} ${title}`.trim();
    const reason = status ? `status code ${status}` : "an unknown error";
    super(`Request failed with ${reason}: ${request.method} ${request.url}`);
    __publicField(this, "response");
    __publicField(this, "request");
    __publicField(this, "options");
    this.name = "HTTPError";
    this.response = response;
    this.request = request;
    this.options = options;
  }
}
class NonError extends Error {
  constructor(value) {
    let message = "Non-error value was thrown";
    try {
      if (typeof value === "string") {
        message = value;
      } else if (value && typeof value === "object" && "message" in value && typeof value.message === "string") {
        message = value.message;
      }
    } catch {
    }
    super(message);
    __publicField(this, "name", "NonError");
    __publicField(this, "value");
    this.value = value;
  }
}
class ForceRetryError extends Error {
  constructor(options) {
    const cause = (options == null ? void 0 : options.cause) ? options.cause instanceof Error ? options.cause : new NonError(options.cause) : void 0;
    super((options == null ? void 0 : options.code) ? `Forced retry: ${options.code}` : "Forced retry", cause ? { cause } : void 0);
    __publicField(this, "name", "ForceRetryError");
    __publicField(this, "customDelay");
    __publicField(this, "code");
    __publicField(this, "customRequest");
    this.customDelay = options == null ? void 0 : options.delay;
    this.code = options == null ? void 0 : options.code;
    this.customRequest = options == null ? void 0 : options.request;
  }
}
const supportsRequestStreams = (() => {
  let duplexAccessed = false;
  let hasContentType = false;
  const supportsReadableStream = typeof globalThis.ReadableStream === "function";
  const supportsRequest = typeof globalThis.Request === "function";
  if (supportsReadableStream && supportsRequest) {
    try {
      hasContentType = new globalThis.Request("https://empty.invalid", {
        body: new globalThis.ReadableStream(),
        method: "POST",
        // @ts-expect-error - Types are outdated.
        get duplex() {
          duplexAccessed = true;
          return "half";
        }
      }).headers.has("Content-Type");
    } catch (error) {
      if (error instanceof Error && error.message === "unsupported BodyInit type") {
        return false;
      }
      throw error;
    }
  }
  return duplexAccessed && !hasContentType;
})();
const supportsAbortController = typeof globalThis.AbortController === "function";
const supportsAbortSignal = typeof globalThis.AbortSignal === "function" && typeof globalThis.AbortSignal.any === "function";
const supportsResponseStreams = typeof globalThis.ReadableStream === "function";
const supportsFormData = typeof globalThis.FormData === "function";
const requestMethods = ["get", "post", "put", "patch", "head", "delete"];
const responseTypes = {
  json: "application/json",
  text: "text/*",
  formData: "multipart/form-data",
  arrayBuffer: "*/*",
  blob: "*/*",
  // Supported in modern Fetch implementations (for example, browsers and recent Node.js/undici).
  // We still feature-check at runtime before exposing the shortcut.
  bytes: "*/*"
};
const maxSafeTimeout = 2147483647;
const usualFormBoundarySize = new TextEncoder().encode("------WebKitFormBoundaryaxpyiPgbbPti10Rw").length;
const stop = Symbol("stop");
class RetryMarker {
  constructor(options) {
    __publicField(this, "options");
    this.options = options;
  }
}
const retry = (options) => new RetryMarker(options);
const kyOptionKeys = {
  json: true,
  parseJson: true,
  stringifyJson: true,
  searchParams: true,
  prefixUrl: true,
  retry: true,
  timeout: true,
  hooks: true,
  throwHttpErrors: true,
  onDownloadProgress: true,
  onUploadProgress: true,
  fetch: true,
  context: true
};
const vendorSpecificOptions = {
  next: true
  // Next.js cache revalidation (revalidate, tags)
};
const requestOptionsRegistry = {
  method: true,
  headers: true,
  body: true,
  mode: true,
  credentials: true,
  cache: true,
  redirect: true,
  referrer: true,
  referrerPolicy: true,
  integrity: true,
  keepalive: true,
  signal: true,
  window: true,
  duplex: true
};
const getBodySize = (body) => {
  if (!body) {
    return 0;
  }
  if (body instanceof FormData) {
    let size = 0;
    for (const [key, value] of body) {
      size += usualFormBoundarySize;
      size += new TextEncoder().encode(`Content-Disposition: form-data; name="${key}"`).length;
      size += typeof value === "string" ? new TextEncoder().encode(value).length : value.size;
    }
    return size;
  }
  if (body instanceof Blob) {
    return body.size;
  }
  if (body instanceof ArrayBuffer) {
    return body.byteLength;
  }
  if (typeof body === "string") {
    return new TextEncoder().encode(body).length;
  }
  if (body instanceof URLSearchParams) {
    return new TextEncoder().encode(body.toString()).length;
  }
  if ("byteLength" in body) {
    return body.byteLength;
  }
  if (typeof body === "object" && body !== null) {
    try {
      const jsonString = JSON.stringify(body);
      return new TextEncoder().encode(jsonString).length;
    } catch {
      return 0;
    }
  }
  return 0;
};
const withProgress = (stream, totalBytes, onProgress) => {
  let previousChunk;
  let transferredBytes = 0;
  return stream.pipeThrough(new TransformStream({
    transform(currentChunk, controller) {
      controller.enqueue(currentChunk);
      if (previousChunk) {
        transferredBytes += previousChunk.byteLength;
        let percent = totalBytes === 0 ? 0 : transferredBytes / totalBytes;
        if (percent >= 1) {
          percent = 1 - Number.EPSILON;
        }
        onProgress == null ? void 0 : onProgress({ percent, totalBytes: Math.max(totalBytes, transferredBytes), transferredBytes }, previousChunk);
      }
      previousChunk = currentChunk;
    },
    flush() {
      if (previousChunk) {
        transferredBytes += previousChunk.byteLength;
        onProgress == null ? void 0 : onProgress({ percent: 1, totalBytes: Math.max(totalBytes, transferredBytes), transferredBytes }, previousChunk);
      }
    }
  }));
};
const streamResponse = (response, onDownloadProgress) => {
  if (!response.body) {
    return response;
  }
  if (response.status === 204) {
    return new Response(null, {
      status: response.status,
      statusText: response.statusText,
      headers: response.headers
    });
  }
  const totalBytes = Math.max(0, Number(response.headers.get("content-length")) || 0);
  return new Response(withProgress(response.body, totalBytes, onDownloadProgress), {
    status: response.status,
    statusText: response.statusText,
    headers: response.headers
  });
};
const streamRequest = (request, onUploadProgress, originalBody) => {
  if (!request.body) {
    return request;
  }
  const totalBytes = getBodySize(originalBody ?? request.body);
  return new Request(request, {
    // @ts-expect-error - Types are outdated.
    duplex: "half",
    body: withProgress(request.body, totalBytes, onUploadProgress)
  });
};
const isObject$2 = (value) => value !== null && typeof value === "object";
const validateAndMerge = (...sources) => {
  for (const source of sources) {
    if ((!isObject$2(source) || Array.isArray(source)) && source !== void 0) {
      throw new TypeError("The `options` argument must be an object");
    }
  }
  return deepMerge({}, ...sources);
};
const mergeHeaders = (source1 = {}, source2 = {}) => {
  const result = new globalThis.Headers(source1);
  const isHeadersInstance = source2 instanceof globalThis.Headers;
  const source = new globalThis.Headers(source2);
  for (const [key, value] of source.entries()) {
    if (isHeadersInstance && value === "undefined" || value === void 0) {
      result.delete(key);
    } else {
      result.set(key, value);
    }
  }
  return result;
};
function newHookValue(original, incoming, property) {
  return Object.hasOwn(incoming, property) && incoming[property] === void 0 ? [] : deepMerge(original[property] ?? [], incoming[property] ?? []);
}
const mergeHooks = (original = {}, incoming = {}) => ({
  beforeRequest: newHookValue(original, incoming, "beforeRequest"),
  beforeRetry: newHookValue(original, incoming, "beforeRetry"),
  afterResponse: newHookValue(original, incoming, "afterResponse"),
  beforeError: newHookValue(original, incoming, "beforeError")
});
const appendSearchParameters = (target, source) => {
  const result = new URLSearchParams();
  for (const input of [target, source]) {
    if (input === void 0) {
      continue;
    }
    if (input instanceof URLSearchParams) {
      for (const [key, value] of input.entries()) {
        result.append(key, value);
      }
    } else if (Array.isArray(input)) {
      for (const pair of input) {
        if (!Array.isArray(pair) || pair.length !== 2) {
          throw new TypeError("Array search parameters must be provided in [[key, value], ...] format");
        }
        result.append(String(pair[0]), String(pair[1]));
      }
    } else if (isObject$2(input)) {
      for (const [key, value] of Object.entries(input)) {
        if (value !== void 0) {
          result.append(key, String(value));
        }
      }
    } else {
      const parameters = new URLSearchParams(input);
      for (const [key, value] of parameters.entries()) {
        result.append(key, value);
      }
    }
  }
  return result;
};
const deepMerge = (...sources) => {
  let returnValue = {};
  let headers = {};
  let hooks = {};
  let searchParameters;
  const signals = [];
  for (const source of sources) {
    if (Array.isArray(source)) {
      if (!Array.isArray(returnValue)) {
        returnValue = [];
      }
      returnValue = [...returnValue, ...source];
    } else if (isObject$2(source)) {
      for (let [key, value] of Object.entries(source)) {
        if (key === "signal" && value instanceof globalThis.AbortSignal) {
          signals.push(value);
          continue;
        }
        if (key === "context") {
          if (value !== void 0 && value !== null && (!isObject$2(value) || Array.isArray(value))) {
            throw new TypeError("The `context` option must be an object");
          }
          returnValue = {
            ...returnValue,
            context: value === void 0 || value === null ? {} : { ...returnValue.context, ...value }
          };
          continue;
        }
        if (key === "searchParams") {
          if (value === void 0 || value === null) {
            searchParameters = void 0;
          } else {
            searchParameters = searchParameters === void 0 ? value : appendSearchParameters(searchParameters, value);
          }
          continue;
        }
        if (isObject$2(value) && key in returnValue) {
          value = deepMerge(returnValue[key], value);
        }
        returnValue = { ...returnValue, [key]: value };
      }
      if (isObject$2(source.hooks)) {
        hooks = mergeHooks(hooks, source.hooks);
        returnValue.hooks = hooks;
      }
      if (isObject$2(source.headers)) {
        headers = mergeHeaders(headers, source.headers);
        returnValue.headers = headers;
      }
    }
  }
  if (searchParameters !== void 0) {
    returnValue.searchParams = searchParameters;
  }
  if (signals.length > 0) {
    if (signals.length === 1) {
      returnValue.signal = signals[0];
    } else if (supportsAbortSignal) {
      returnValue.signal = AbortSignal.any(signals);
    } else {
      returnValue.signal = signals.at(-1);
    }
  }
  if (returnValue.context === void 0) {
    returnValue.context = {};
  }
  return returnValue;
};
const normalizeRequestMethod = (input) => requestMethods.includes(input) ? input.toUpperCase() : input;
const retryMethods = ["get", "put", "head", "delete", "options", "trace"];
const retryStatusCodes = [408, 413, 429, 500, 502, 503, 504];
const retryAfterStatusCodes = [413, 429, 503];
const defaultRetryOptions = {
  limit: 2,
  methods: retryMethods,
  statusCodes: retryStatusCodes,
  afterStatusCodes: retryAfterStatusCodes,
  maxRetryAfter: Number.POSITIVE_INFINITY,
  backoffLimit: Number.POSITIVE_INFINITY,
  delay: (attemptCount) => 0.3 * 2 ** (attemptCount - 1) * 1e3,
  jitter: void 0,
  retryOnTimeout: false
};
const normalizeRetryOptions = (retry2 = {}) => {
  if (typeof retry2 === "number") {
    return {
      ...defaultRetryOptions,
      limit: retry2
    };
  }
  if (retry2.methods && !Array.isArray(retry2.methods)) {
    throw new Error("retry.methods must be an array");
  }
  if (retry2.statusCodes && !Array.isArray(retry2.statusCodes)) {
    throw new Error("retry.statusCodes must be an array");
  }
  const normalizedRetry = Object.fromEntries(Object.entries(retry2).filter(([, value]) => value !== void 0));
  return {
    ...defaultRetryOptions,
    ...normalizedRetry
  };
};
class TimeoutError extends Error {
  constructor(request) {
    super(`Request timed out: ${request.method} ${request.url}`);
    __publicField(this, "request");
    this.name = "TimeoutError";
    this.request = request;
  }
}
async function timeout(request, init, abortController, options) {
  return new Promise((resolve2, reject) => {
    const timeoutId = setTimeout(() => {
      if (abortController) {
        abortController.abort();
      }
      reject(new TimeoutError(request));
    }, options.timeout);
    void options.fetch(request, init).then(resolve2).catch(reject).then(() => {
      clearTimeout(timeoutId);
    });
  });
}
async function delay(ms, { signal }) {
  return new Promise((resolve2, reject) => {
    if (signal) {
      signal.throwIfAborted();
      signal.addEventListener("abort", abortHandler, { once: true });
    }
    function abortHandler() {
      clearTimeout(timeoutId);
      reject(signal.reason);
    }
    const timeoutId = setTimeout(() => {
      signal == null ? void 0 : signal.removeEventListener("abort", abortHandler);
      resolve2();
    }, ms);
  });
}
const findUnknownOptions = (request, options) => {
  const unknownOptions = {};
  for (const key in options) {
    if (!Object.hasOwn(options, key)) {
      continue;
    }
    if (!(key in requestOptionsRegistry) && !(key in kyOptionKeys) && (!(key in request) || key in vendorSpecificOptions)) {
      unknownOptions[key] = options[key];
    }
  }
  return unknownOptions;
};
const hasSearchParameters = (search) => {
  if (search === void 0) {
    return false;
  }
  if (Array.isArray(search)) {
    return search.length > 0;
  }
  if (search instanceof URLSearchParams) {
    return search.size > 0;
  }
  if (typeof search === "object") {
    return Object.keys(search).length > 0;
  }
  if (typeof search === "string") {
    return search.trim().length > 0;
  }
  return Boolean(search);
};
function isHTTPError(error) {
  return error instanceof HTTPError || (error == null ? void 0 : error.name) === HTTPError.name;
}
function isTimeoutError(error) {
  return error instanceof TimeoutError || (error == null ? void 0 : error.name) === TimeoutError.name;
}
const _Ky = class _Ky {
  // eslint-disable-next-line complexity
  constructor(input, options = {}) {
    __privateAdd(this, _Ky_instances);
    __publicField(this, "request");
    __privateAdd(this, _abortController);
    __privateAdd(this, _retryCount, 0);
    // eslint-disable-next-line @typescript-eslint/prefer-readonly -- False positive: #input is reassigned on line 202
    __privateAdd(this, _input);
    __privateAdd(this, _options);
    __privateAdd(this, _originalRequest);
    __privateAdd(this, _userProvidedAbortSignal);
    __privateAdd(this, _cachedNormalizedOptions);
    var _a3, _b2, _c3;
    __privateSet(this, _input, input);
    __privateSet(this, _options, {
      ...options,
      headers: mergeHeaders(__privateGet(this, _input).headers, options.headers),
      hooks: mergeHooks({
        beforeRequest: [],
        beforeRetry: [],
        beforeError: [],
        afterResponse: []
      }, options.hooks),
      method: normalizeRequestMethod(options.method ?? __privateGet(this, _input).method ?? "GET"),
      // eslint-disable-next-line @typescript-eslint/prefer-nullish-coalescing
      prefixUrl: String(options.prefixUrl || ""),
      retry: normalizeRetryOptions(options.retry),
      throwHttpErrors: options.throwHttpErrors ?? true,
      timeout: options.timeout ?? 1e4,
      fetch: options.fetch ?? globalThis.fetch.bind(globalThis),
      context: options.context ?? {}
    });
    if (typeof __privateGet(this, _input) !== "string" && !(__privateGet(this, _input) instanceof URL || __privateGet(this, _input) instanceof globalThis.Request)) {
      throw new TypeError("`input` must be a string, URL, or Request");
    }
    if (__privateGet(this, _options).prefixUrl && typeof __privateGet(this, _input) === "string") {
      if (__privateGet(this, _input).startsWith("/")) {
        throw new Error("`input` must not begin with a slash when using `prefixUrl`");
      }
      if (!__privateGet(this, _options).prefixUrl.endsWith("/")) {
        __privateGet(this, _options).prefixUrl += "/";
      }
      __privateSet(this, _input, __privateGet(this, _options).prefixUrl + __privateGet(this, _input));
    }
    if (supportsAbortController && supportsAbortSignal) {
      __privateSet(this, _userProvidedAbortSignal, __privateGet(this, _options).signal ?? __privateGet(this, _input).signal);
      __privateSet(this, _abortController, new globalThis.AbortController());
      __privateGet(this, _options).signal = __privateGet(this, _userProvidedAbortSignal) ? AbortSignal.any([__privateGet(this, _userProvidedAbortSignal), __privateGet(this, _abortController).signal]) : __privateGet(this, _abortController).signal;
    }
    if (supportsRequestStreams) {
      __privateGet(this, _options).duplex = "half";
    }
    if (__privateGet(this, _options).json !== void 0) {
      __privateGet(this, _options).body = ((_b2 = (_a3 = __privateGet(this, _options)).stringifyJson) == null ? void 0 : _b2.call(_a3, __privateGet(this, _options).json)) ?? JSON.stringify(__privateGet(this, _options).json);
      __privateGet(this, _options).headers.set("content-type", __privateGet(this, _options).headers.get("content-type") ?? "application/json");
    }
    const userProvidedContentType = options.headers && new globalThis.Headers(options.headers).has("content-type");
    if (__privateGet(this, _input) instanceof globalThis.Request && (supportsFormData && __privateGet(this, _options).body instanceof globalThis.FormData || __privateGet(this, _options).body instanceof URLSearchParams) && !userProvidedContentType) {
      __privateGet(this, _options).headers.delete("content-type");
    }
    this.request = new globalThis.Request(__privateGet(this, _input), __privateGet(this, _options));
    if (hasSearchParameters(__privateGet(this, _options).searchParams)) {
      const textSearchParams = typeof __privateGet(this, _options).searchParams === "string" ? __privateGet(this, _options).searchParams.replace(/^\?/, "") : new URLSearchParams(__privateMethod(_c3 = _Ky, _Ky_static, normalizeSearchParams_fn).call(_c3, __privateGet(this, _options).searchParams)).toString();
      const searchParams = "?" + textSearchParams;
      const url = this.request.url.replace(/(?:\?.*?)?(?=#|$)/, searchParams);
      this.request = new globalThis.Request(url, __privateGet(this, _options));
    }
    if (__privateGet(this, _options).onUploadProgress) {
      if (typeof __privateGet(this, _options).onUploadProgress !== "function") {
        throw new TypeError("The `onUploadProgress` option must be a function");
      }
      if (!supportsRequestStreams) {
        throw new Error("Request streams are not supported in your environment. The `duplex` option for `Request` is not available.");
      }
      this.request = __privateMethod(this, _Ky_instances, wrapRequestWithUploadProgress_fn).call(this, this.request, __privateGet(this, _options).body ?? void 0);
    }
  }
  static create(input, options) {
    var _a3, _b2, _c3;
    const ky2 = new _Ky(input, options);
    const function_ = async () => {
      var _a4, _b3, _c4, _d3, _e, _f2, _g;
      if (typeof __privateGet(ky2, _options).timeout === "number" && __privateGet(ky2, _options).timeout > maxSafeTimeout) {
        throw new RangeError(`The \`timeout\` option cannot be greater than ${maxSafeTimeout}`);
      }
      await Promise.resolve();
      let response = await __privateMethod(_a4 = ky2, _Ky_instances, fetch_fn).call(_a4);
      for (const hook of __privateGet(ky2, _options).hooks.afterResponse) {
        const clonedResponse = __privateMethod(_b3 = ky2, _Ky_instances, decorateResponse_fn).call(_b3, response.clone());
        const modifiedResponse = await hook(ky2.request, __privateMethod(_c4 = ky2, _Ky_instances, getNormalizedOptions_fn).call(_c4), clonedResponse, { retryCount: __privateGet(ky2, _retryCount) });
        if (modifiedResponse instanceof globalThis.Response) {
          response = modifiedResponse;
        }
        if (modifiedResponse instanceof RetryMarker) {
          await Promise.all([
            (_d3 = clonedResponse.body) == null ? void 0 : _d3.cancel(),
            (_e = response.body) == null ? void 0 : _e.cancel()
          ]);
          throw new ForceRetryError(modifiedResponse.options);
        }
      }
      __privateMethod(_f2 = ky2, _Ky_instances, decorateResponse_fn).call(_f2, response);
      if (!response.ok && (typeof __privateGet(ky2, _options).throwHttpErrors === "function" ? __privateGet(ky2, _options).throwHttpErrors(response.status) : __privateGet(ky2, _options).throwHttpErrors)) {
        let error = new HTTPError(response, ky2.request, __privateMethod(_g = ky2, _Ky_instances, getNormalizedOptions_fn).call(_g));
        for (const hook of __privateGet(ky2, _options).hooks.beforeError) {
          error = await hook(error, { retryCount: __privateGet(ky2, _retryCount) });
        }
        throw error;
      }
      if (__privateGet(ky2, _options).onDownloadProgress) {
        if (typeof __privateGet(ky2, _options).onDownloadProgress !== "function") {
          throw new TypeError("The `onDownloadProgress` option must be a function");
        }
        if (!supportsResponseStreams) {
          throw new Error("Streams are not supported in your environment. `ReadableStream` is missing.");
        }
        return streamResponse(response.clone(), __privateGet(ky2, _options).onDownloadProgress);
      }
      return response;
    };
    const result = __privateMethod(_a3 = ky2, _Ky_instances, retry_fn).call(_a3, function_).finally(async () => {
      var _a4, _b3;
      const originalRequest = __privateGet(ky2, _originalRequest);
      const cleanupPromises = [];
      if (originalRequest && !originalRequest.bodyUsed) {
        cleanupPromises.push((_a4 = originalRequest.body) == null ? void 0 : _a4.cancel());
      }
      if (!ky2.request.bodyUsed) {
        cleanupPromises.push((_b3 = ky2.request.body) == null ? void 0 : _b3.cancel());
      }
      await Promise.all(cleanupPromises);
    });
    for (const [type, mimeType] of Object.entries(responseTypes)) {
      if (type === "bytes" && typeof ((_c3 = (_b2 = globalThis.Response) == null ? void 0 : _b2.prototype) == null ? void 0 : _c3.bytes) !== "function") {
        continue;
      }
      result[type] = async () => {
        ky2.request.headers.set("accept", ky2.request.headers.get("accept") || mimeType);
        const response = await result;
        if (type === "json") {
          if (response.status === 204) {
            return "";
          }
          const text = await response.text();
          if (text === "") {
            return "";
          }
          if (options.parseJson) {
            return options.parseJson(text);
          }
          return JSON.parse(text);
        }
        return response[type]();
      };
    }
    return result;
  }
};
_Ky_static = new WeakSet();
normalizeSearchParams_fn = function(searchParams) {
  if (searchParams && typeof searchParams === "object" && !Array.isArray(searchParams) && !(searchParams instanceof URLSearchParams)) {
    return Object.fromEntries(Object.entries(searchParams).filter(([, value]) => value !== void 0));
  }
  return searchParams;
};
_abortController = new WeakMap();
_retryCount = new WeakMap();
_input = new WeakMap();
_options = new WeakMap();
_originalRequest = new WeakMap();
_userProvidedAbortSignal = new WeakMap();
_cachedNormalizedOptions = new WeakMap();
_Ky_instances = new WeakSet();
calculateDelay_fn = function() {
  const retryDelay = __privateGet(this, _options).retry.delay(__privateGet(this, _retryCount));
  let jitteredDelay = retryDelay;
  if (__privateGet(this, _options).retry.jitter === true) {
    jitteredDelay = Math.random() * retryDelay;
  } else if (typeof __privateGet(this, _options).retry.jitter === "function") {
    jitteredDelay = __privateGet(this, _options).retry.jitter(retryDelay);
    if (!Number.isFinite(jitteredDelay) || jitteredDelay < 0) {
      jitteredDelay = retryDelay;
    }
  }
  const backoffLimit = __privateGet(this, _options).retry.backoffLimit ?? Number.POSITIVE_INFINITY;
  return Math.min(backoffLimit, jitteredDelay);
};
calculateRetryDelay_fn = async function(error) {
  __privateWrapper(this, _retryCount)._++;
  if (__privateGet(this, _retryCount) > __privateGet(this, _options).retry.limit) {
    throw error;
  }
  const errorObject = error instanceof Error ? error : new NonError(error);
  if (errorObject instanceof ForceRetryError) {
    return errorObject.customDelay ?? __privateMethod(this, _Ky_instances, calculateDelay_fn).call(this);
  }
  if (!__privateGet(this, _options).retry.methods.includes(this.request.method.toLowerCase())) {
    throw error;
  }
  if (__privateGet(this, _options).retry.shouldRetry !== void 0) {
    const result = await __privateGet(this, _options).retry.shouldRetry({ error: errorObject, retryCount: __privateGet(this, _retryCount) });
    if (result === false) {
      throw error;
    }
    if (result === true) {
      return __privateMethod(this, _Ky_instances, calculateDelay_fn).call(this);
    }
  }
  if (isTimeoutError(error) && !__privateGet(this, _options).retry.retryOnTimeout) {
    throw error;
  }
  if (isHTTPError(error)) {
    if (!__privateGet(this, _options).retry.statusCodes.includes(error.response.status)) {
      throw error;
    }
    const retryAfter = error.response.headers.get("Retry-After") ?? error.response.headers.get("RateLimit-Reset") ?? error.response.headers.get("X-RateLimit-Retry-After") ?? error.response.headers.get("X-RateLimit-Reset") ?? error.response.headers.get("X-Rate-Limit-Reset");
    if (retryAfter && __privateGet(this, _options).retry.afterStatusCodes.includes(error.response.status)) {
      let after = Number(retryAfter) * 1e3;
      if (Number.isNaN(after)) {
        after = Date.parse(retryAfter) - Date.now();
      } else if (after >= Date.parse("2024-01-01")) {
        after -= Date.now();
      }
      const max = __privateGet(this, _options).retry.maxRetryAfter ?? after;
      return after < max ? after : max;
    }
    if (error.response.status === 413) {
      throw error;
    }
  }
  return __privateMethod(this, _Ky_instances, calculateDelay_fn).call(this);
};
decorateResponse_fn = function(response) {
  if (__privateGet(this, _options).parseJson) {
    response.json = async () => __privateGet(this, _options).parseJson(await response.text());
  }
  return response;
};
retry_fn = async function(function_) {
  try {
    return await function_();
  } catch (error) {
    const ms = Math.min(await __privateMethod(this, _Ky_instances, calculateRetryDelay_fn).call(this, error), maxSafeTimeout);
    if (__privateGet(this, _retryCount) < 1) {
      throw error;
    }
    await delay(ms, __privateGet(this, _userProvidedAbortSignal) ? { signal: __privateGet(this, _userProvidedAbortSignal) } : {});
    if (error instanceof ForceRetryError && error.customRequest) {
      const managedRequest = __privateGet(this, _options).signal ? new globalThis.Request(error.customRequest, { signal: __privateGet(this, _options).signal }) : new globalThis.Request(error.customRequest);
      __privateMethod(this, _Ky_instances, assignRequest_fn).call(this, managedRequest);
    }
    for (const hook of __privateGet(this, _options).hooks.beforeRetry) {
      const hookResult = await hook({
        request: this.request,
        options: __privateMethod(this, _Ky_instances, getNormalizedOptions_fn).call(this),
        error,
        retryCount: __privateGet(this, _retryCount)
      });
      if (hookResult instanceof globalThis.Request) {
        __privateMethod(this, _Ky_instances, assignRequest_fn).call(this, hookResult);
        break;
      }
      if (hookResult instanceof globalThis.Response) {
        return hookResult;
      }
      if (hookResult === stop) {
        return;
      }
    }
    return __privateMethod(this, _Ky_instances, retry_fn).call(this, function_);
  }
};
fetch_fn = async function() {
  var _a3;
  if ((_a3 = __privateGet(this, _abortController)) == null ? void 0 : _a3.signal.aborted) {
    __privateSet(this, _abortController, new globalThis.AbortController());
    __privateGet(this, _options).signal = __privateGet(this, _userProvidedAbortSignal) ? AbortSignal.any([__privateGet(this, _userProvidedAbortSignal), __privateGet(this, _abortController).signal]) : __privateGet(this, _abortController).signal;
    this.request = new globalThis.Request(this.request, { signal: __privateGet(this, _options).signal });
  }
  for (const hook of __privateGet(this, _options).hooks.beforeRequest) {
    const result = await hook(this.request, __privateMethod(this, _Ky_instances, getNormalizedOptions_fn).call(this), { retryCount: __privateGet(this, _retryCount) });
    if (result instanceof Response) {
      return result;
    }
    if (result instanceof globalThis.Request) {
      __privateMethod(this, _Ky_instances, assignRequest_fn).call(this, result);
      break;
    }
  }
  const nonRequestOptions = findUnknownOptions(this.request, __privateGet(this, _options));
  __privateSet(this, _originalRequest, this.request);
  this.request = __privateGet(this, _originalRequest).clone();
  if (__privateGet(this, _options).timeout === false) {
    return __privateGet(this, _options).fetch(__privateGet(this, _originalRequest), nonRequestOptions);
  }
  return timeout(__privateGet(this, _originalRequest), nonRequestOptions, __privateGet(this, _abortController), __privateGet(this, _options));
};
getNormalizedOptions_fn = function() {
  if (!__privateGet(this, _cachedNormalizedOptions)) {
    const { hooks, ...normalizedOptions } = __privateGet(this, _options);
    __privateSet(this, _cachedNormalizedOptions, Object.freeze(normalizedOptions));
  }
  return __privateGet(this, _cachedNormalizedOptions);
};
assignRequest_fn = function(request) {
  __privateSet(this, _cachedNormalizedOptions, void 0);
  this.request = __privateMethod(this, _Ky_instances, wrapRequestWithUploadProgress_fn).call(this, request);
};
wrapRequestWithUploadProgress_fn = function(request, originalBody) {
  if (!__privateGet(this, _options).onUploadProgress || !request.body) {
    return request;
  }
  return streamRequest(request, __privateGet(this, _options).onUploadProgress, originalBody ?? __privateGet(this, _options).body ?? void 0);
};
__privateAdd(_Ky, _Ky_static);
let Ky = _Ky;
/*! MIT License © Sindre Sorhus */
const createInstance = (defaults) => {
  const ky2 = (input, options) => Ky.create(input, validateAndMerge(defaults, options));
  for (const method of requestMethods) {
    ky2[method] = (input, options) => Ky.create(input, validateAndMerge(defaults, options, { method }));
  }
  ky2.create = (newDefaults) => createInstance(validateAndMerge(newDefaults));
  ky2.extend = (newDefaults) => {
    if (typeof newDefaults === "function") {
      newDefaults = newDefaults(defaults ?? {});
    }
    return createInstance(validateAndMerge(defaults, newDefaults));
  };
  ky2.stop = stop;
  ky2.retry = retry;
  return ky2;
};
const ky = createInstance();
const REGISTRY_URL = ref("");
function getRegistryUrl() {
  if (!REGISTRY_URL.value)
    throw new Error("Registry URL is not defined");
  return REGISTRY_URL.value;
}
const PLATFORM_ID = ref("");
function getPlatformId() {
  if (!PLATFORM_ID.value)
    throw new Error("Platform ID is not defined");
  return PLATFORM_ID.value;
}
async function getServicesUrl(services, platform) {
  if (!platform)
    return getServicesUrl(services, getPlatformId());
  if (!globalThis.document)
    return getServicesUrlByRegistryAPI(services, platform);
  const registryUrl = getRegistryUrl();
  const i3DXCompassRegistryClient = await requirejsOne("DS/i3DXCompassPlatformServices/i3DXCompassRegistryClient");
  const response = await i3DXCompassRegistryClient.getServicesByPlatform({
    platforms: Array.isArray(platform) ? [...platform] : [platform],
    services,
    config: { url: registryUrl }
  });
  return response;
}
async function getServicesUrlByRegistryAPI(services, platform) {
  const registryUrl = getRegistryUrl();
  const apiURL = "/api/v1/platform/service/instance";
  return await ky.get(`${registryUrl}${apiURL}`, {
    searchParams: {
      serviceId: services.join(","),
      platformId: Array.isArray(platform) ? platform.join(",") : platform
    }
  }).json();
}
async function getServiceUrl(service, platformId) {
  var _a3, _b2, _c3;
  if (!platformId)
    return getServiceUrl(service, getPlatformId());
  const platforms = await getServicesUrl([service], platformId);
  if (!platforms.length)
    return "";
  return ((_c3 = (_b2 = (_a3 = platforms[0]) == null ? void 0 : _a3.services) == null ? void 0 : _b2.find((el2) => el2.id.toLowerCase() === service.toLowerCase())) == null ? void 0 : _c3.url) || "";
}
var SERVICE = /* @__PURE__ */ ((SERVICE2) => {
  SERVICE2["AIAI"] = "AIAssistantInfra";
  SERVICE2["ANALYTICS"] = "3danalytics";
  SERVICE2["COMMENT"] = "3DComment";
  SERVICE2["COMPASS"] = "3DCompass";
  SERVICE2["DASHBOARD"] = "3DDashboard";
  SERVICE2["DRIVE"] = "3DDrive";
  SERVICE2["LEAN"] = "3dlean";
  SERVICE2["LLMINFERENCE"] = "LLMINFERENCE";
  SERVICE2["MEDICOLLAB"] = "MedicalCollaboration";
  SERVICE2["MESSAGING"] = "3DMessaging";
  SERVICE2["ODE"] = "OfficeDocumentEditor";
  SERVICE2["ORGANIZATION"] = "3DOrganization";
  SERVICE2["PASSPORT"] = "3DPassport";
  SERVICE2["RTCOEDIT"] = "RTCoedit";
  SERVICE2["SEARCH"] = "3DSearch";
  SERVICE2["SOCIAL_MEDIA"] = "SocialMedia";
  SERVICE2["SPACE"] = "3DSpace";
  SERVICE2["SWYM"] = "3DSwym";
  SERVICE2["TAGGER"] = "6WTAG";
  SERVICE2["USERS_GROUP"] = "UsersGroup";
  return SERVICE2;
})(SERVICE || {});
async function logout(path, { redirectToPassportLogin } = { redirectToPassportLogin: true }) {
  const passportUrl = await getServiceUrl(SERVICE.PASSPORT);
  if (window.location.href.startsWith(passportUrl))
    return;
  const passportLogoutUrl = new URL(`${passportUrl}/logout`);
  const redirectTo = window.location.href;
  if (redirectToPassportLogin) {
    const passportLoginUrl = new URL(`${passportUrl}/login`);
    passportLoginUrl.searchParams.set("service", redirectTo);
    passportLogoutUrl.searchParams.set("redirect", btoa(passportLoginUrl.toString()));
  } else {
    passportLogoutUrl.searchParams.set("service", redirectTo);
  }
  window.location.href = passportLogoutUrl.toString();
}
const AUTH_PASSPORT_EXPIRED_ERROR = "invalid_client";
const AUTH_PASSPORT_EXPIRED_STATUS = 401;
const passportAuth = async (_request, options, response) => {
  if (options.handlePassportExpiredError === false)
    return;
  if (response.status !== AUTH_PASSPORT_EXPIRED_STATUS)
    return;
  const result = await response.json();
  if (result.error !== AUTH_PASSPORT_EXPIRED_ERROR)
    return;
  logout();
};
const XRequestIdHeader = "X-Request-ID";
const addRequestId = (request) => {
  if (!request.headers.has(XRequestIdHeader))
    request.headers.append(XRequestIdHeader, crypto.randomUUID());
  if (!request.headers.get(XRequestIdHeader))
    request.headers.delete(XRequestIdHeader);
};
ky.extend({
  credentials: "include",
  redirect: "error",
  headers: {
    "x-requested-with": "XMLHttpRequest",
    "content-type": "application/json;charset=UTF-8"
  },
  timeout: 25e3,
  hooks: {
    beforeRequest: [addRequestId],
    afterResponse: [passportAuth]
  }
});
({
  [SERVICE.PASSPORT]: {},
  [SERVICE.DRIVE]: {},
  [SERVICE.SPACE]: {},
  [SERVICE.SWYM]: {},
  [SERVICE.COMMENT]: {},
  [SERVICE.SEARCH]: {},
  [SERVICE.MEDICOLLAB]: {}
});
class CommonUtils {
  static removeDotSegments(input) {
    const output = [];
    function handleDotSegments(p2) {
      if (p2 === "/..") {
        output.pop();
      } else {
        output.push(p2);
      }
    }
    input.replace(/^(\.\.?(\/|$))+/, "").replace(/\/(\.(\/|$))+/g, "/").replace(/\/\.\.$/, "/../").replace(/\/?[^/]*/g, handleDotSegments);
    return output.join("").replace(/^\//, input.charAt(0) === "/" ? "/" : "");
  }
  /**
   * Build an url from linkHref param in same context than widgetUrl param.
   *
   * @example
   * CommonUtils.Utils.buildUrl("http://example.org/mywidget.html", "/index.html");
   * // will return "http://example.org/index.html"
   *
   * @param {String} url - A full absolute url (e.g "http://example.org")
   * @param {String} href - Another url or simple file path (e.g. "/index.html")
   * @return {String} A new url related to widgetUrl url absolute value.
   *
   * @alias CommonUtils.Utils.buildUrl
   */
  static buildUrl(url, href) {
    const defaultScheme = window.location && window.location.protocol ? window.location.protocol : "http:";
    url = String(url);
    href = String(href);
    if (!CommonUtils.isAbsoluteUrl(url)) {
      throw new Error("First argument should be a absolute url.");
    }
    if (url.substring(0, 2) === "//") {
      url = defaultScheme + url;
    }
    let i, l, path = "";
    const splitScheme = String(url).split("://"), scheme = splitScheme[0], resource = splitScheme[1], splitDirectory = resource.split("/"), domain = splitDirectory[0];
    if (href.substring(0, 2) === "//") {
      url = scheme + ":" + href;
    } else if (href.split("://").length > 1) {
      url = href;
    } else if (href.substring(0, 1) === "/") {
      url = scheme + "://" + domain + href;
    } else {
      for (i = 1, l = splitDirectory.length - 1; i < l; i++) {
        path += "/" + splitDirectory[i];
      }
      url = scheme + "://" + domain + CommonUtils.removeDotSegments(path + "/" + href);
    }
    return url;
  }
  /**
   * Parse an Url to extract uri parts (protocol, domain, ...). The URL should include the protocol (http://).
   *
   * @example
   * CommonUtils.Utils.parseUrl("http://me@example.com:80");
   * // will return {"source":"http://me@example.com:80","protocol":"http","authority":"me@example.com:80","domain":"me@example.com","port":"80",...}
   *
   * @param {String} sourceUri - A valid url (e.g. "http://netvibes.com")
   * @return {Object} Object with following properties: source, protocol, authority, domain, port, path, directoryPath, fileName, query, anchor.
   *
   * @alias CommonUtils.Utils.parseUrl
   */
  static parseUrl(sourceUri) {
    const uriPartNames = ["source", "subprotocol", "protocol", "authority", "user", "password", "domain", "port", "path", "directoryPath", "fileName", "query", "anchor"], uriPartRegExp = new RegExp(
      "^(?:(?:(?:([^#.:]+):)?([^#.:]+):)?//)?((?:([^:/]+)(?::([^/]*?))?@)?([^:/?#]*)(?::(\\d*))?)?((/(?:[^?#](?![^?#/]*\\.[^?#/.]+(?:[\\?#]|$)))*/?)?([^?#/]*))?(?:\\?([^#]*))?(?:#(.*))?"
      // anchor
    );
    let i;
    const uriParts = uriPartRegExp.exec(sourceUri), uri = {};
    for (i = 0; i < uriPartNames.length; i++) {
      uri[uriPartNames[i]] = uriParts[i] || "";
    }
    if (uri.subprotocol) {
      uri.source = uri.source.substr(uri.subprotocol.length + 1);
    }
    if (!uri.port) {
      uri.port = uri.protocol === "https" ? "443" : "80";
    }
    if (uri.directoryPath && uri.directoryPath.length > 0) {
      uri.directoryPath = uri.directoryPath.replace(/\/?$/, "/");
    }
    uri.domain = uri.domain.toLocaleLowerCase();
    uri.protocol = uri.protocol.toLocaleLowerCase();
    return uri;
  }
  /**
   * Build an Url to from uri parts (protocol, domain, ...).
   *
   * @example
   * CommonUtils.composeUrl({
   *    protocol: "https",
   *    subprotocol: "feed",
   *    authority: "example.org:8080",
   *    domain: "example.org",
   *    port: "8080",
   *    path: "/mypath/mywidget.html",
   *    directoryPath: "/mypath/",
   *    fileName: "mywidget.html",
   *    query: "lorem=ipsum&hello=world",
   *    anchor: "sit"
   * });
   * // will return "feed:https://example.org:8080/mypath/mywidget.html?lorem=ipsum&hello=world#sit"
   *
   *
   * @param {String} parts - An object with following properties: source, protocol, authority, domain, port, path, directoryPath, fileName, query, anchor
   * @return {String} A valid url (e.g. "http://netvibes.com").
   *
   * @alias CommonUtils.composeUrl
   */
  static composeUrl(parts) {
    let result = "";
    if (parts.protocol) {
      result = parts.protocol + "://";
      if (parts.subprotocol) {
        result = parts.subprotocol + ":" + result;
      }
    }
    if (parts.domain) {
      result += parts.domain;
      if (parts.port && (parts.protocol !== "http" || parseInt(parts.port, 10) !== 80) && (parts.protocol !== "https" || parseInt(parts.port, 10) !== 443)) {
        result += ":" + parts.port;
      }
    } else if (parts.authority) {
      result += parts.authority;
    }
    if (parts.path) {
      result += parts.path;
    }
    if (parts.query) {
      result += "?" + parts.query;
    }
    if (parts.anchor) {
      result += "#" + parts.anchor;
    }
    return result;
  }
  /**
   * Compare two url to check if their domain, protocol and port match.
   *
   * @example
   * // Following will return `true`
   * CommonUtils.Utils.matchUrl('http://example.com/mypath/index.html', '/mypath/index.html');
   * CommonUtils.Utils.matchUrl('http://example.com/mypath/index.html', 'mypath/index.html');
   * CommonUtils.Utils.matchUrl('http://example.com/mypath/index.html', 'http://example.com/index.html');
   *
   * // Following will return `false`
   * CommonUtils.Utils.matchUrl('http://example.com/mypath/index.html', 'http://example.org/mypath/index.html');
   * CommonUtils.Utils.matchUrl('http://example.com/mypath/index.html', 'https://example.com/mypath/index.html');
   *
   * @param {String} originaUrl - First url to compare
   * @param {String} requestUrl - Second url to compare
   * @return {Boolean} `true` if urls match else `false`.
   *
   * @alias CommonUtils.Utils.matchUrl
   */
  static matchUrl(originaUrl, requestUrl) {
    let originalUrlParts, requestUrlParts, matchUrl = false;
    const originalIsAbsolute = CommonUtils.isAbsoluteUrl(originaUrl), requestIsAbsolute = CommonUtils.isAbsoluteUrl(requestUrl);
    if (!originalIsAbsolute && !requestIsAbsolute) {
      matchUrl = true;
    } else {
      if (!originalIsAbsolute) {
        originaUrl = CommonUtils.buildUrl(requestUrl, originaUrl);
      } else if (!requestIsAbsolute) {
        requestUrl = CommonUtils.buildUrl(originaUrl, requestUrl);
      }
      originalUrlParts = CommonUtils.parseUrl(originaUrl);
      requestUrlParts = CommonUtils.parseUrl(requestUrl);
      matchUrl = ["domain", "protocol", "port"].every(function(name2) {
        return originalUrlParts[name2] === requestUrlParts[name2] || // Protocol less match
        name2 === "protocol" && (originalUrlParts[name2] === "" || requestUrlParts[name2] === "");
      });
    }
    return matchUrl;
  }
  /**
   * Test if an URL is absolute.
   *
   * @example
   * CommonUtils.Utils.isAbsoluteUrl('http://foo.com');
   * // will return true
   *
   * CommonUtils.Utils.isAbsoluteUrl('/foo');
   * // will return false
   *
   * @param {String} url - the url to check
   * @return {Boolean} `true` if the URL is absolute.
   *
   * @alias CommonUtils.Utils.isAbsoluteUrl
   */
  static isAbsoluteUrl(url) {
    const re = /^((https?|ftp|file):)?\/\//;
    return re.test(url);
  }
  /**
   * Encode an Object to a url string.
   *
   * @example
   * CommonUtils.Utils.toQueryString({
   *    example: 1,
   *    array: ['hello', 'world'],
   *    object: {key: 'value'}
   * });
   * // will return "example=1&array[0]=hello&array[1]=world"
   *
   * CommonUtils.Utils.toQueryString({
   *    toQueryString: function () {
   *        return 'hello'
   *    }
   * });
   * // will return "hello"
   *
   * @param {Object} myObject - Object to encode
   * @param {Object} base - Sub object to encode
   * @return {String} Encoded string.
   *
   * @alias CommonUtils.Utils.toQueryString
   */
  static toQueryString(myObject, base) {
    let key, value, result, queryString;
    if (CommonUtils.is(myObject, "string")) {
      queryString = myObject;
    } else if (CommonUtils.is(myObject.toQueryString, "function")) {
      queryString = myObject.toQueryString();
    } else {
      queryString = [];
      for (key in myObject) {
        if (myObject.hasOwnProperty(key)) {
          value = myObject[key];
          if (base) {
            key = base + "[" + CommonUtils.capitalize(key) + "]";
          }
          if (CommonUtils.is(value)) {
            if (CommonUtils.is(value, "object") || CommonUtils.is(value, "array")) {
              result = CommonUtils.toQueryString(value, key);
            } else if (!CommonUtils.is(value, "function")) {
              result = CommonUtils.encodeUrl(key) + "=" + CommonUtils.encodeUrl(value);
            }
          }
          if (result) {
            queryString.push(result);
          }
          result = void 0;
        }
      }
      queryString = queryString.join("&");
    }
    return queryString;
  }
  static is(object, type) {
    if (object && type)
      return typeof object === type;
    else if (object)
      return typeof object !== "undefined";
    else
      return false;
  }
  /**
   * Simple encodeURIComponent wrapper to escape "." to "%2e" also.
   *
   * @example
   * CommonUtils.Utils.encodeUrl("hello?/e;&.");
   * // will return "hello%3F%2Fe%3B%26%2e"
   *
   * @param {String} str - String to encode
   * @return {String} Encoded string.
   *
   * @alias CommonUtils.Utils.encodeUrl
   */
  static encodeUrl(str) {
    return encodeURIComponent(str).replace(/\./g, "%2e");
  }
  static capitalize(str, delimiter = "-") {
    const words = str.split(delimiter);
    for (let i = 0; i < words.length; i++) {
      words[i] = words[i][0].toUpperCase() + words[i].substr(1);
    }
    return words.join(delimiter);
  }
}
class CommonRequest {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  constructor() {
  }
  /**
   * UWA Proxy urls indexed by types.
   */
  static get proxies() {
    const nativeProxies = Preferences.getNativeAppRequireConfig();
    if (nativeProxies && nativeProxies.proxies) {
      return nativeProxies.proxies;
    }
    const hosts = window.UWA && window.UWA.hosts || {};
    const exposition = hosts.exposition;
    const expositionProxy = exposition + "/proxy/";
    const _proxies = hosts.proxies || {};
    return Object.assign(Object.assign({}, _proxies), {
      ajax: expositionProxy + "ajax",
      resolve: expositionProxy + "resolve",
      xml: expositionProxy + "xml",
      spreadsheet: expositionProxy + "spreadsheet",
      soap: expositionProxy + "soap",
      feed: expositionProxy + "feed",
      icon: expositionProxy + "icon",
      richIcon: expositionProxy + "richIcon",
      rss: expositionProxy + "feed"
      // Deprecated alias support
    });
  }
  static normalizeRequestHeaders(request) {
    const strHeaders = {};
    request.headers.forEach((header, key) => {
      strHeaders[key] = header;
    });
    return strHeaders;
  }
  static checkHasProxyPath(url) {
    if (Preferences.isNativeApp()) {
      const nativeProxies = Preferences.getNativeAppRequireConfig();
      if (nativeProxies && nativeProxies.proxies) {
        for (const proxy in nativeProxies.proxies) {
          if (url.startsWith(nativeProxies.proxies[proxy]))
            return true;
        }
      }
    }
    return false;
  }
  /**
   * This method is used to create a Request that can be proxified to do CORS requests.
   * It supports the 2 proxy options:
   *      - ajax (for non-authenticated request)
   *      - passport (for authenticated request)
   * It allows to create a proxified Request that use these proxies.
   *
   * __Specific proxy options__:
   *
   * You can pass specific options into an object that have the same name than the used proxy
   * to configure proxy behavior, see Proxy Examples bellow for detailed usage.
   *
   * | Proxy       |  Description                                |
   * | ------------| --------------------------------------------|
   * | `ajax`      | cors request without authenticated session  |
   * | `passport`  | authenticated session request               |
   *
   *
   * @param {String}  url - the URL of the data source
   * @param {Object}  options - NormalizedOptions header object
   * @param {String}  [options.method=GET] - GET, POST (in uppercase!)
   * @param {Object}  [options.body={}] - GET or POST params as object
   * @param {String}  [options.proxy=false] - ajax, passport
   * @param {Object}  [options.headers] - Headers object.
   * @param {Request} originRequest - Original request.
   *
   * @return {Request} A request that is proxified or not
   * If the options.proxy is set && the proxification condition is satisfied, a new request will be created with a proxified URL.
   * Otherwise, the original request will be return.
   *
   * @alias CommonRequest.request
   */
  static async request(url, options, originRequest) {
    if (!url) {
      throw new Error("Bad or missing url argument.");
    }
    if (!Preferences.isNativeApp() || CommonUtils.matchUrl(url, window.location))
      return originRequest;
    options = options || {};
    let request;
    const currentUrl = window.location, defaultProxy = "ajax", requestOptions = options;
    if (requestOptions.headers && !requestOptions.normalizedHeaders) {
      requestOptions.normalizedHeaders = CommonRequest.normalizeRequestHeaders(originRequest);
    }
    if (requestOptions.proxy) {
      url = CommonRequest.proxifyUrl(url, requestOptions);
    } else if (CommonUtils.matchUrl(url, currentUrl) === false && CommonRequest.allowCrossOriginRequest === false) {
      url = CommonRequest.proxifyUrl(url, Object.assign(Object.assign({}, requestOptions), { proxy: defaultProxy }));
    }
    const body = ["GET", "HEAD"].includes(originRequest.method) ? void 0 : await originRequest.blob();
    requestOptions.body = body;
    request = new Request(url, requestOptions);
    return request;
  }
  /**
   * Proxify an URL using UWA proxy.
   *
   * @param {String} url - url to proxify
   * @param {String} options - some options from <request> options parameter
   * @return {String} full url to proxy with proxified options (header, data) has parameter.
   *
   * @alias CommonRequest.proxifyUrl
   */
  static proxifyUrl(url, options) {
    if (!url) {
      throw new Error("Bad or missing url argument.");
    }
    let proxyUrl;
    const params = [], proxy = options.proxy, toQueryString = CommonUtils.toQueryString, proxies = CommonRequest.proxies;
    if (proxy && proxies[proxy]) {
      if (!Preferences.isNativeApp() || CommonUtils.matchUrl(url, window.location))
        return url;
      proxyUrl = proxies[proxy];
      if (url.indexOf(proxyUrl) === 0) {
        return url;
      }
      if (url.startsWith("https://") && url.indexOf(":443") > -1) {
        url = url.replace(":443", "");
      }
      if (options.body && options.method && options.method.toUpperCase() === "GET") {
        url += (url.indexOf("?") > -1 ? "&" : "?") + toQueryString(options.body);
        console.log("DEBUG test proxification handle get", url);
      }
      if (typeof options.proxy === "object") {
        params.push(toQueryString(options.proxy));
      }
      if (options.normalizedHeaders) {
        params.push(toQueryString(options.normalizedHeaders, "headers"));
      } else if (options.headers) {
        params.push(toQueryString(options.headers, "headers"));
      }
      if (options.method) {
        params.push(toQueryString({
          method: options.method
        }));
      }
      params.push(toQueryString({
        url
      }));
      url = proxyUrl + "?" + params.join("&");
      if (url.length > 4096) {
        console.log("Error proxify url too long", url);
        throw new Error("Proxified url is more than 4096 characters, that is the limit for most of the browsers.");
      }
    } else {
      throw new Error("Invalid proxy");
    }
    return url;
  }
  /**
   * Deproxify an URL.
   *
   * @param url - url to deproxify
   * @returns {String} original URL without proxification
   */
  static deproxifyUrl(url) {
    try {
      const decodeURL = new URL(url);
      if (CommonRequest.checkHasProxyPath(url) && decodeURL.searchParams.has("url")) {
        return decodeURL.searchParams.get("url");
      }
    } catch (error) {
      console.error("Deproxify URL error", error);
    }
    return url;
  }
}
CommonRequest.allowCrossOriginRequest = false;
function proxifyUrl(url, options) {
  try {
    return CommonRequest.proxifyUrl(url, { proxy: "passport", ...options });
  } catch (error) {
    logger.error(url, error);
    return url;
  }
}
var USER_PICTURE_SIZE = /* @__PURE__ */ ((USER_PICTURE_SIZE2) => {
  USER_PICTURE_SIZE2["TINY"] = "tiny";
  USER_PICTURE_SIZE2["SMALL"] = "small";
  USER_PICTURE_SIZE2["MEDIUM"] = "medium";
  USER_PICTURE_SIZE2["NORMAL"] = "normal";
  USER_PICTURE_SIZE2["BIG"] = "big";
  USER_PICTURE_SIZE2["BIGGER"] = "bigger";
  USER_PICTURE_SIZE2["BLARGE"] = "blarge";
  USER_PICTURE_SIZE2["LARGE"] = "large";
  return USER_PICTURE_SIZE2;
})(USER_PICTURE_SIZE || {});
const _hoisted_1$n = {
  key: 1,
  class: "user-picture relative bg-inherit"
};
const _hoisted_2$d = ["src"];
/* @__PURE__ */ defineComponent({
  __name: "user-picture",
  props: {
    login: {},
    hashKey: {},
    fingerprint: {},
    hidePresence: { type: Boolean },
    size: { default: USER_PICTURE_SIZE.MEDIUM }
  },
  setup(__props) {
    const props = __props;
    const login = toRef$3(props, "login");
    const hashKey = toRef$3(props, "hashKey");
    const swymUrl = computedAsync(() => getServiceUrl(SERVICE.SWYM));
    const computeAvatarUrl = computed(() => {
      if (swymUrl.value) {
        let url = "/api/user/getpicture/";
        if (login.value)
          url += `login/${login.value}`;
        else if (hashKey.value)
          url += `hash_key/${hashKey.value}`;
        if (props.fingerprint)
          url += `fingerprint/${props.fingerprint}`;
        return proxifyUrl(`${swymUrl.value}${url}`);
      } else {
        return void 0;
      }
    });
    const userPresence = useTemplateRef("userPresence");
    const PresenceComponentV3 = shallowRef();
    const hidePresence = computed(() => props.hidePresence || props.size === USER_PICTURE_SIZE.TINY);
    const userPictureSize = computed(() => {
      switch (props.size) {
        case USER_PICTURE_SIZE.TINY:
          return "size-[18px]";
        case USER_PICTURE_SIZE.SMALL:
          return "size-[26px]";
        case USER_PICTURE_SIZE.MEDIUM:
          return "size-[38px]";
        case USER_PICTURE_SIZE.NORMAL:
          return "size-[44px]";
        case USER_PICTURE_SIZE.BIG:
          return "size-[84px]";
        case USER_PICTURE_SIZE.BIGGER:
          return "size-[104px]";
        case USER_PICTURE_SIZE.BLARGE:
          return "size-[120px]";
        case USER_PICTURE_SIZE.LARGE:
          return "size-[154px]";
      }
    });
    async function loadPresenceComponent() {
      if (!login.value) {
        PresenceComponentV3.value = null;
        return;
      }
      try {
        const RTVueUserPresenceAPI = await requirejsOne(
          "DS/RTVueUserPresenceAPI/RTVueUserPresenceAPI"
        );
        PresenceComponentV3.value = RTVueUserPresenceAPI.vue3cmp;
      } catch (error) {
        logger.error(error);
      }
    }
    watch(login, async () => {
      await loadPresenceComponent();
    }, { immediate: true });
    onBeforeUnmount(() => {
      PresenceComponentV3.value = null;
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock(Fragment, null, [
        withDirectives((openBlock(), createBlock(resolveDynamicComponent(PresenceComponentV3.value), {
          ref_key: "userPresence",
          ref: userPresence,
          login: login.value,
          "swym-url": unref(swymUrl),
          "tenant-id": unref(getPlatformId)()
        }, null, 8, ["login", "swym-url", "tenant-id"])), [
          [vShow, false]
        ]),
        _ctx.$slots["custom-picture"] ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass(["custom-picture", userPictureSize.value])
        }, [
          renderSlot(_ctx.$slots, "custom-picture", { class: "size-full" })
        ], 2)) : (openBlock(), createElementBlock("div", _hoisted_1$n, [
          createBaseVNode("img", {
            class: normalizeClass(["relative rounded-full", userPictureSize.value]),
            src: computeAvatarUrl.value
          }, null, 10, _hoisted_2$d),
          userPresence.value && !hidePresence.value ? (openBlock(), createElementBlock("div", {
            key: 0,
            class: normalizeClass(["absolute flex items-center justify-center ring-white bg-white rounded-full", {
              "size-[8px] ring-2 right-0 bottom-0": __props.size === unref(USER_PICTURE_SIZE).SMALL,
              "size-[10px] ring-2 right-0 bottom-0": __props.size === unref(USER_PICTURE_SIZE).MEDIUM,
              "size-[12px] ring-2 right-0 bottom-0": __props.size === unref(USER_PICTURE_SIZE).NORMAL,
              "size-[18px] ring-[3px] bottom-[4px] right-[4px]": __props.size === unref(USER_PICTURE_SIZE).BIG,
              "size-[24px] ring-[3px] bottom-[2px] right-[2px]": __props.size === unref(USER_PICTURE_SIZE).BIGGER,
              "size-[28px] ring-[3px] bottom-[2px] right-[2px]": __props.size === unref(USER_PICTURE_SIZE).BLARGE,
              "size-[36px] ring-4 bottom-[6px] right-[6px]": __props.size === unref(USER_PICTURE_SIZE).LARGE
            }])
          }, [
            createVNode(VuIcon, {
              class: normalizeClass(["presence-icon", [userPresence.value.fonticon, {
                "text-[8px] size-[8px] leading-[8px]": __props.size === unref(USER_PICTURE_SIZE).SMALL,
                "text-[10px] size-[10px] leading-[10px]": __props.size === unref(USER_PICTURE_SIZE).MEDIUM,
                "text-[12px] size-[12px] leading-[12px]": __props.size === unref(USER_PICTURE_SIZE).NORMAL,
                "text-[18px] size-[18px] leading-[18px]": __props.size === unref(USER_PICTURE_SIZE).BIG,
                "text-[24px] size-[24px] leading-[24px]": __props.size === unref(USER_PICTURE_SIZE).BIGGER,
                "text-[28px] size-[28px] leading-[28px]": __props.size === unref(USER_PICTURE_SIZE).BLARGE,
                "text-[36px] size-[36px] leading-[36px]": __props.size === unref(USER_PICTURE_SIZE).LARGE
              }]]),
              icon: userPresence.value.fonticon
            }, null, 8, ["class", "icon"])
          ], 2)) : createCommentVNode("", true)
        ]))
      ], 64);
    };
  }
});
checkIsIphoneOrIpadDevice();
[...Array(256).keys()].map((index) => index.toString(16).padStart(2, "0"));
({
  _modals: shallowReactive([])
});
function _process(v, mod) {
  var i;
  var r;
  if (typeof mod === "function") {
    r = mod(v);
    if (r !== void 0) {
      v = r;
    }
  } else if (Array.isArray(mod)) {
    for (i = 0; i < mod.length; i++) {
      r = mod[i](v);
      if (r !== void 0) {
        v = r;
      }
    }
  }
  return v;
}
function parseKey(key, val) {
  if (key[0] === "-" && Array.isArray(val) && /^-\d+$/.test(key)) {
    return val.length + parseInt(key, 10);
  }
  return key;
}
function isIndex(k) {
  return /^\d+$/.test(k);
}
function isObject$1(val) {
  return Object.prototype.toString.call(val) === "[object Object]";
}
function isArrayOrObject(val) {
  return Object(val) === val;
}
function isEmptyObject(val) {
  return Object.keys(val).length === 0;
}
var blacklist = ["__proto__", "prototype", "constructor"];
var blacklistFilter = function(part) {
  return blacklist.indexOf(part) === -1;
};
function parsePath(path, sep) {
  if (path.indexOf("[") >= 0) {
    path = path.replace(/\[/g, sep).replace(/]/g, "");
  }
  var parts = path.split(sep);
  var check = parts.filter(blacklistFilter);
  if (check.length !== parts.length) {
    throw Error("Refusing to update blacklisted property " + path);
  }
  return parts;
}
var hasOwnProperty = Object.prototype.hasOwnProperty;
function DotObject(separator, override, useArray, useBrackets) {
  if (!(this instanceof DotObject)) {
    return new DotObject(separator, override, useArray, useBrackets);
  }
  if (typeof override === "undefined") override = false;
  if (typeof useArray === "undefined") useArray = true;
  if (typeof useBrackets === "undefined") useBrackets = true;
  this.separator = separator || ".";
  this.override = override;
  this.useArray = useArray;
  this.useBrackets = useBrackets;
  this.keepArray = false;
  this.cleanup = [];
}
var dotDefault = new DotObject(".", false, true, true);
function wrap(method) {
  return function() {
    return dotDefault[method].apply(dotDefault, arguments);
  };
}
DotObject.prototype._fill = function(a, obj, v, mod) {
  var k = a.shift();
  if (a.length > 0) {
    obj[k] = obj[k] || (this.useArray && isIndex(a[0]) ? [] : {});
    if (!isArrayOrObject(obj[k])) {
      if (this.override) {
        obj[k] = {};
      } else {
        if (!(isArrayOrObject(v) && isEmptyObject(v))) {
          throw new Error(
            "Trying to redefine `" + k + "` which is a " + typeof obj[k]
          );
        }
        return;
      }
    }
    this._fill(a, obj[k], v, mod);
  } else {
    if (!this.override && isArrayOrObject(obj[k]) && !isEmptyObject(obj[k])) {
      if (!(isArrayOrObject(v) && isEmptyObject(v))) {
        throw new Error("Trying to redefine non-empty obj['" + k + "']");
      }
      return;
    }
    obj[k] = _process(v, mod);
  }
};
DotObject.prototype.object = function(obj, mods) {
  var self2 = this;
  Object.keys(obj).forEach(function(k) {
    var mod = mods === void 0 ? null : mods[k];
    var ok = parsePath(k, self2.separator).join(self2.separator);
    if (ok.indexOf(self2.separator) !== -1) {
      self2._fill(ok.split(self2.separator), obj, obj[k], mod);
      delete obj[k];
    } else {
      obj[k] = _process(obj[k], mod);
    }
  });
  return obj;
};
DotObject.prototype.str = function(path, v, obj, mod) {
  var ok = parsePath(path, this.separator).join(this.separator);
  if (path.indexOf(this.separator) !== -1) {
    this._fill(ok.split(this.separator), obj, v, mod);
  } else {
    obj[path] = _process(v, mod);
  }
  return obj;
};
DotObject.prototype.pick = function(path, obj, remove2, reindexArray) {
  var i;
  var keys;
  var val;
  var key;
  var cp2;
  keys = parsePath(path, this.separator);
  for (i = 0; i < keys.length; i++) {
    key = parseKey(keys[i], obj);
    if (obj && typeof obj === "object" && key in obj) {
      if (i === keys.length - 1) {
        if (remove2) {
          val = obj[key];
          if (reindexArray && Array.isArray(obj)) {
            obj.splice(key, 1);
          } else {
            delete obj[key];
          }
          if (Array.isArray(obj)) {
            cp2 = keys.slice(0, -1).join(".");
            if (this.cleanup.indexOf(cp2) === -1) {
              this.cleanup.push(cp2);
            }
          }
          return val;
        } else {
          return obj[key];
        }
      } else {
        obj = obj[key];
      }
    } else {
      return void 0;
    }
  }
  if (remove2 && Array.isArray(obj)) {
    obj = obj.filter(function(n) {
      return n !== void 0;
    });
  }
  return obj;
};
DotObject.prototype.delete = function(path, obj) {
  return this.remove(path, obj, true);
};
DotObject.prototype.remove = function(path, obj, reindexArray) {
  var i;
  this.cleanup = [];
  if (Array.isArray(path)) {
    for (i = 0; i < path.length; i++) {
      this.pick(path[i], obj, true, reindexArray);
    }
    if (!reindexArray) {
      this._cleanup(obj);
    }
    return obj;
  } else {
    return this.pick(path, obj, true, reindexArray);
  }
};
DotObject.prototype._cleanup = function(obj) {
  var ret;
  var i;
  var keys;
  var root;
  if (this.cleanup.length) {
    for (i = 0; i < this.cleanup.length; i++) {
      keys = this.cleanup[i].split(".");
      root = keys.splice(0, -1).join(".");
      ret = root ? this.pick(root, obj) : obj;
      ret = ret[keys[0]].filter(function(v) {
        return v !== void 0;
      });
      this.set(this.cleanup[i], ret, obj);
    }
    this.cleanup = [];
  }
};
DotObject.prototype.del = DotObject.prototype.remove;
DotObject.prototype.move = function(source, target, obj, mods, merge) {
  if (typeof mods === "function" || Array.isArray(mods)) {
    this.set(target, _process(this.pick(source, obj, true), mods), obj, merge);
  } else {
    merge = mods;
    this.set(target, this.pick(source, obj, true), obj, merge);
  }
  return obj;
};
DotObject.prototype.transfer = function(source, target, obj1, obj2, mods, merge) {
  if (typeof mods === "function" || Array.isArray(mods)) {
    this.set(
      target,
      _process(this.pick(source, obj1, true), mods),
      obj2,
      merge
    );
  } else {
    merge = mods;
    this.set(target, this.pick(source, obj1, true), obj2, merge);
  }
  return obj2;
};
DotObject.prototype.copy = function(source, target, obj1, obj2, mods, merge) {
  if (typeof mods === "function" || Array.isArray(mods)) {
    this.set(
      target,
      _process(
        // clone what is picked
        JSON.parse(JSON.stringify(this.pick(source, obj1, false))),
        mods
      ),
      obj2,
      merge
    );
  } else {
    merge = mods;
    this.set(target, this.pick(source, obj1, false), obj2, merge);
  }
  return obj2;
};
DotObject.prototype.set = function(path, val, obj, merge) {
  var i;
  var k;
  var keys;
  var key;
  if (typeof val === "undefined") {
    return obj;
  }
  keys = parsePath(path, this.separator);
  for (i = 0; i < keys.length; i++) {
    key = keys[i];
    if (i === keys.length - 1) {
      if (merge && isObject$1(val) && isObject$1(obj[key])) {
        for (k in val) {
          if (hasOwnProperty.call(val, k)) {
            obj[key][k] = val[k];
          }
        }
      } else if (merge && Array.isArray(obj[key]) && Array.isArray(val)) {
        for (var j = 0; j < val.length; j++) {
          obj[keys[i]].push(val[j]);
        }
      } else {
        obj[key] = val;
      }
    } else if (
      // force the value to be an object
      !hasOwnProperty.call(obj, key) || !isObject$1(obj[key]) && !Array.isArray(obj[key])
    ) {
      if (/^\d+$/.test(keys[i + 1])) {
        obj[key] = [];
      } else {
        obj[key] = {};
      }
    }
    obj = obj[key];
  }
  return obj;
};
DotObject.prototype.transform = function(recipe, obj, tgt) {
  obj = obj || {};
  tgt = tgt || {};
  Object.keys(recipe).forEach(
    (function(key) {
      this.set(recipe[key], this.pick(key, obj), tgt);
    }).bind(this)
  );
  return tgt;
};
DotObject.prototype.dot = function(obj, tgt, path) {
  tgt = tgt || {};
  path = path || [];
  var isArray2 = Array.isArray(obj);
  Object.keys(obj).forEach(
    (function(key) {
      var index = isArray2 && this.useBrackets ? "[" + key + "]" : key;
      if (isArrayOrObject(obj[key]) && (isObject$1(obj[key]) && !isEmptyObject(obj[key]) || Array.isArray(obj[key]) && !this.keepArray && obj[key].length !== 0)) {
        if (isArray2 && this.useBrackets) {
          var previousKey = path[path.length - 1] || "";
          return this.dot(
            obj[key],
            tgt,
            path.slice(0, -1).concat(previousKey + index)
          );
        } else {
          return this.dot(obj[key], tgt, path.concat(index));
        }
      } else {
        if (isArray2 && this.useBrackets) {
          tgt[path.join(this.separator).concat("[" + key + "]")] = obj[key];
        } else {
          tgt[path.concat(index).join(this.separator)] = obj[key];
        }
      }
    }).bind(this)
  );
  return tgt;
};
DotObject.pick = wrap("pick");
DotObject.move = wrap("move");
DotObject.transfer = wrap("transfer");
DotObject.transform = wrap("transform");
DotObject.copy = wrap("copy");
DotObject.object = wrap("object");
DotObject.str = wrap("str");
DotObject.set = wrap("set");
DotObject.delete = wrap("delete");
DotObject.del = DotObject.remove = wrap("remove");
DotObject.dot = wrap("dot");
["override", "overwrite"].forEach(function(prop) {
  Object.defineProperty(DotObject, prop, {
    get: function() {
      return dotDefault.override;
    },
    set: function(val) {
      dotDefault.override = !!val;
    }
  });
});
["useArray", "keepArray", "useBrackets"].forEach(function(prop) {
  Object.defineProperty(DotObject, prop, {
    get: function() {
      return dotDefault[prop];
    },
    set: function(val) {
      dotDefault[prop] = val;
    }
  });
});
DotObject._process = _process;
ref();
function isLeapYear(year) {
  return year % 4 === 0 && year % 100 !== 0 || year % 400 === 0;
}
function getDaysInMonth(year, month) {
  return [31, isLeapYear(year) ? 29 : 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][month];
}
function compareDates(a, b) {
  if (!b)
    return false;
  return a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate();
}
function isValidDate(obj) {
  return obj instanceof Date && !Number.isNaN(obj.getTime());
}
function parse(a) {
  let value;
  if (isValidDate(a)) {
    value = a;
  } else if (a && typeof a === "string") {
    const parsedDate = new Date(Date.parse(a));
    if (isValidDate(parsedDate)) {
      value = parsedDate;
    } else {
      value = void 0;
    }
  }
  return value;
}
function getWeekdaysLabels() {
  const { $i18n: $i18n2 } = useTranslations("date");
  const weekdaysLabels = computed(() => [
    $i18n2("sunday"),
    $i18n2("monday"),
    $i18n2("tuesday"),
    $i18n2("wednesday"),
    $i18n2("thursday"),
    $i18n2("friday"),
    $i18n2("saturday")
  ]);
  return weekdaysLabels;
}
function useRangeable(modelValue, props) {
  function setBoundary(boundaryValue, boundary) {
    const update = [
      boundary === "min" ? modelValue.value[0] < boundaryValue : modelValue.value[0] > boundaryValue,
      boundary === "min" ? modelValue.value[1] < boundaryValue : modelValue.value[1] > boundaryValue
    ];
    return update.map((val, index) => val ? boundaryValue : modelValue.value[index]);
  }
  function anyOutOfRange(boundaryValue, boundary) {
    return modelValue.value.some((val) => {
      return boundary === "min" ? val < boundaryValue : val > boundaryValue;
    });
  }
  const checkBoundary = (value, boundary) => {
    if (!modelValue.value)
      return;
    const isRange = Array.isArray(modelValue.value);
    const isDate2 = modelValue.value instanceof Date;
    if (isDate2 && Number.isNaN(modelValue.value.getTime())) {
      modelValue.value = props.max;
      return;
    }
    if (isRange) {
      if (anyOutOfRange(value, boundary)) {
        modelValue.value = setBoundary(value, boundary);
      }
    } else {
      if (["min"].includes(boundary) && modelValue.value < value || ["max"].includes(boundary) && modelValue.value > value) {
        modelValue.value = value;
      }
    }
  };
  watch(() => props.min, (v) => {
    if (v)
      checkBoundary(v, "min");
  }, { immediate: true });
  watch(() => props.max, (v) => {
    if (v)
      checkBoundary(v, "max");
  }, { immediate: true });
  onMounted(() => {
    if (props.min)
      checkBoundary(props.min, "min");
    else
      checkBoundary(Number.NEGATIVE_INFINITY, "min");
    if (props.max)
      checkBoundary(props.max, "max");
    else
      checkBoundary(Number.POSITIVE_INFINITY, "max");
  });
}
const _hoisted_1$m = {
  cellpadding: "0",
  cellspacing: "0",
  class: "datepicker-table table-fixed"
};
const _hoisted_2$c = {
  key: 0,
  scope: "col"
};
const _hoisted_3$9 = ["title"];
const _hoisted_4$8 = {
  key: 0,
  class: "datepicker-week"
};
const _hoisted_5$5 = ["data-day"];
const _hoisted_6$4 = ["data-day", "data-month", "data-year"];
const _sfc_main$p = /* @__PURE__ */ defineComponent({
  __name: "vu-datepicker-table-date",
  props: /* @__PURE__ */ mergeModels({
    firstDay: { default: 0 },
    max: { default: () => /* @__PURE__ */ new Date("2099-12-31T23:59:59.999Z") },
    min: { default: () => /* @__PURE__ */ new Date("1900-01-01T00:00:00.000Z") },
    month: {},
    showWeekNumber: { type: Boolean },
    unselectableDaysOfWeek: { default: () => [] },
    weekdaysLabels: {},
    weekdaysShortLabels: {},
    year: {}
  }, {
    "modelValue": {},
    "modelModifiers": {}
  }),
  emits: /* @__PURE__ */ mergeModels(["select"], ["update:modelValue"]),
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit2 = __emit;
    const date = useModel(__props, "modelValue");
    const defaultWeekdaysLabels = getWeekdaysLabels();
    const weekdaysLabels = computed(() => props.weekdaysLabels ?? defaultWeekdaysLabels.value);
    const weekdaysShortLabels = computed(() => props.weekdaysShortLabels ?? defaultWeekdaysLabels.value.map((label) => label.slice(0, 3)));
    useRangeable(date, props);
    function renderDayName(day, abbr) {
      let d = day + props.firstDay;
      while (d >= 7) {
        d -= 7;
      }
      return abbr ? weekdaysShortLabels.value[d] : weekdaysLabels.value[d];
    }
    function renderWeek(rowIndex) {
      const y = props.year;
      const m = props.month;
      const d = rowIndex * 7 + 1;
      const oneJan = new Date(y, 0, 1);
      const daysSinceJan1 = (new Date(y, m, d).getTime() - oneJan.getTime()) / 864e5;
      const adjustedDays = daysSinceJan1 + oneJan.getDay() + 1;
      const weekNum = Math.ceil(adjustedDays / 7);
      return weekNum;
    }
    function onSelect(event) {
      const target = event.target;
      const year = target.getAttribute("data-year");
      const month = target.getAttribute("data-month");
      const day = target.getAttribute("data-day");
      if (year && month && day)
        emit2("select", new Date(Number(year), Number(month), Number(day)));
    }
    const rows = computed(() => {
      const now = /* @__PURE__ */ new Date();
      now.setHours(0, 0, 0, 0);
      const days = getDaysInMonth(props.year, props.month);
      let before = new Date(props.year, props.month, 1).getDay();
      const elements = [];
      let row = [];
      let cells;
      let after;
      if (props.firstDay > 0) {
        before -= props.firstDay;
        if (before < 0) {
          before += 7;
        }
      }
      cells = days + before;
      after = cells;
      while (after > 7) {
        after -= 7;
      }
      cells += 7 - after;
      for (let i = 0, r = 0; i < cells; i++) {
        const day = new Date(props.year, props.month, 1 + (i - before));
        const minDate = props.min;
        const maxDate = props.max;
        const isDisabled = minDate && day.getTime() < minDate.getTime() || maxDate.getTime() && day.getTime() > maxDate.getTime() || props.unselectableDaysOfWeek && props.unselectableDaysOfWeek.includes(day.getDay());
        const isSelected = date.value && isValidDate(date.value) ? compareDates(day, date.value) : false;
        const isToday = compareDates(day, now);
        const isEmpty = i < before || i >= days + before;
        row.push({
          day: 1 + (i - before),
          classes: [
            isDisabled ? "is-disabled" : "",
            isToday ? "is-today" : "",
            isSelected ? "is-selected" : "",
            isEmpty ? "is-empty" : ""
          ].filter(Boolean).join(" "),
          isEmpty
        });
        if (++r === 7) {
          elements.push(row);
          row = [];
          r = 0;
        }
      }
      return elements;
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("table", _hoisted_1$m, [
        createBaseVNode("thead", null, [
          createBaseVNode("tr", null, [
            __props.showWeekNumber ? (openBlock(), createElementBlock("th", _hoisted_2$c)) : createCommentVNode("", true),
            (openBlock(), createElementBlock(Fragment, null, renderList(7, (i) => {
              return createBaseVNode("th", {
                key: i,
                scope: "col"
              }, [
                createBaseVNode("abbr", {
                  title: renderDayName(i - 1)
                }, toDisplayString(renderDayName(i - 1, true)), 9, _hoisted_3$9)
              ]);
            }), 64))
          ])
        ]),
        createBaseVNode("tbody", null, [
          (openBlock(true), createElementBlock(Fragment, null, renderList(rows.value, (row, rowIndex) => {
            return openBlock(), createElementBlock("tr", { key: rowIndex }, [
              __props.showWeekNumber ? (openBlock(), createElementBlock("td", _hoisted_4$8, toDisplayString(renderWeek(rowIndex)), 1)) : createCommentVNode("", true),
              (openBlock(true), createElementBlock(Fragment, null, renderList(row, (day, dayIndex) => {
                return openBlock(), createElementBlock("td", {
                  key: dayIndex,
                  class: normalizeClass(day.classes),
                  "data-day": day.day
                }, [
                  !day.isEmpty ? (openBlock(), createElementBlock("button", {
                    key: 0,
                    class: "datepicker-button datepicker-name",
                    "data-day": day.day,
                    "data-month": __props.month,
                    "data-year": __props.year,
                    type: "button",
                    onClick: onSelect
                  }, toDisplayString(day.day), 9, _hoisted_6$4)) : createCommentVNode("", true)
                ], 10, _hoisted_5$5);
              }), 128))
            ]);
          }), 128))
        ])
      ]);
    };
  }
});
const _hoisted_1$l = { class: "datepicker-calendar" };
const _hoisted_2$b = { class: "datepicker-title" };
const _hoisted_3$8 = { class: "datepicker-label" };
const _hoisted_4$7 = ["disabled", "value"];
const _hoisted_5$4 = { class: "datepicker-label" };
const _hoisted_6$3 = ["value"];
const _sfc_main$o = /* @__PURE__ */ defineComponent({
  __name: "vu-datepicker",
  props: /* @__PURE__ */ mergeModels({
    className: {},
    firstDay: { default: 1 },
    max: { default: () => /* @__PURE__ */ new Date("2099-12-31T23:59:59.999Z") },
    min: { default: () => /* @__PURE__ */ new Date("1900-01-01T00:00:00.000Z") },
    monthsLabels: {},
    show: { type: Boolean, default: true },
    showWeekNumber: { type: Boolean },
    unselectableDaysOfWeek: {},
    weekdaysLabels: {},
    weekdaysShortLabels: {},
    yearRange: { default: 10 }
  }, {
    "modelValue": {},
    "modelModifiers": {}
  }),
  emits: ["update:modelValue"],
  setup(__props) {
    const props = __props;
    const { $i18n: $i18n2 } = useTranslations("date");
    const date = useModel(__props, "modelValue");
    const month = ref(0);
    const year = ref(0);
    const innerShow = ref(false);
    const monthsLabels = computed(() => props.monthsLabels ? props.monthsLabels : [
      $i18n2("january"),
      $i18n2("february"),
      $i18n2("march"),
      $i18n2("april"),
      $i18n2("may"),
      $i18n2("june"),
      $i18n2("july"),
      $i18n2("august"),
      $i18n2("september"),
      $i18n2("october"),
      $i18n2("november"),
      $i18n2("december")
    ]);
    const currentMonth = computed(() => monthsLabels.value[month.value]);
    const minYear = computed(() => new Date(props.min).getFullYear());
    const minMonth = computed(() => new Date(props.min).getMonth());
    const maxYear = computed(() => new Date(props.max).getFullYear());
    const maxMonth = computed(() => new Date(props.max).getMonth());
    const hasPrevMonth = computed(() => !(year.value === minYear.value && (month.value === 0 || minMonth.value >= month.value)));
    const hasNextMonth = computed(() => !(year.value === maxYear.value && (month.value === 11 || maxMonth.value <= month.value)));
    const selectableMonths = computed(() => monthsLabels.value.map((label, index) => {
      const disabled = year.value === minYear.value && index < minMonth.value || year.value === maxYear.value && index > maxMonth.value;
      return { value: index, label, disabled };
    }));
    const selectableYears = computed(() => {
      const minSelectableYear = Math.max(year.value - props.yearRange, minYear.value);
      const maxSelectableYear = Math.min(year.value + 1 + props.yearRange, maxYear.value + 1);
      return Array.from({
        length: maxSelectableYear - minSelectableYear
      }, (_, index) => ({ value: minSelectableYear + index }));
    });
    function setCurrent() {
      const dateValue = parse(date.value) || /* @__PURE__ */ new Date();
      month.value = dateValue.getMonth();
      year.value = dateValue.getFullYear();
    }
    function onSelect(value) {
      month.value = value.getMonth();
      year.value = value.getFullYear();
      date.value = value;
    }
    watch(() => date, () => {
      setCurrent();
    });
    watch(month, (monthValue) => {
      if (monthValue > 11) {
        year.value++;
        month.value = 0;
      } else if (monthValue < 0) {
        month.value = 11;
        year.value--;
      }
    });
    watch(() => props.show, (v) => {
      innerShow.value = v;
    }, { immediate: true });
    onMounted(() => {
      setCurrent();
    });
    return (_ctx, _cache) => {
      return innerShow.value ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["datepicker datepicker-root", __props.className])
      }, [
        createBaseVNode("div", _hoisted_1$l, [
          createBaseVNode("div", _hoisted_2$b, [
            createBaseVNode("div", _hoisted_3$8, [
              createTextVNode(toDisplayString(currentMonth.value) + " ", 1),
              withDirectives(createBaseVNode("select", {
                "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => month.value = $event),
                class: "datepicker-select datepicker-select-month"
              }, [
                (openBlock(true), createElementBlock(Fragment, null, renderList(selectableMonths.value, (option) => {
                  return openBlock(), createElementBlock("option", {
                    key: option.value,
                    disabled: option.disabled,
                    value: option.value
                  }, toDisplayString(option.label), 9, _hoisted_4$7);
                }), 128))
              ], 512), [
                [vModelSelect, month.value]
              ])
            ]),
            createBaseVNode("div", _hoisted_5$4, [
              createTextVNode(toDisplayString(year.value) + " ", 1),
              withDirectives(createBaseVNode("select", {
                "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => year.value = $event),
                class: "datepicker-select datepicker-select-year"
              }, [
                (openBlock(true), createElementBlock(Fragment, null, renderList(selectableYears.value, (option) => {
                  return openBlock(), createElementBlock("option", {
                    key: option.value,
                    value: option.value
                  }, toDisplayString(option.value), 9, _hoisted_6$3);
                }), 128))
              ], 512), [
                [vModelSelect, year.value]
              ])
            ]),
            createBaseVNode("button", {
              class: normalizeClass(["datepicker-prev", { "is-disabled": !hasPrevMonth.value }]),
              type: "button",
              onClick: _cache[2] || (_cache[2] = ($event) => hasPrevMonth.value && month.value--)
            }, null, 2),
            createBaseVNode("button", {
              class: normalizeClass(["datepicker-next", { "is-disabled": !hasNextMonth.value }]),
              type: "button",
              onClick: _cache[3] || (_cache[3] = ($event) => hasNextMonth.value && month.value++)
            }, null, 2)
          ]),
          createVNode(_sfc_main$p, {
            modelValue: date.value,
            "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => date.value = $event),
            "first-day": __props.firstDay,
            max: __props.max,
            min: __props.min,
            month: month.value,
            "months-labels": monthsLabels.value,
            "show-week-number": __props.showWeekNumber,
            "unselectable-days-of-week": __props.unselectableDaysOfWeek,
            "weekdays-labels": __props.weekdaysLabels,
            "weekdays-short-labels": __props.weekdaysShortLabels,
            year: year.value,
            onSelect: _cache[5] || (_cache[5] = ($event) => onSelect($event))
          }, null, 8, ["modelValue", "first-day", "max", "min", "month", "months-labels", "show-week-number", "unselectable-days-of-week", "weekdays-labels", "weekdays-short-labels", "year"])
        ])
      ], 2)) : createCommentVNode("", true);
    };
  }
});
const _hoisted_1$k = {
  key: 0,
  class: "control-label"
};
const _hoisted_2$a = {
  key: 0,
  class: "label-field-required"
};
const _hoisted_3$7 = {
  ref: "activator",
  class: "input-date"
};
const _hoisted_4$6 = ["disabled", "placeholder", "value"];
const _hoisted_5$3 = {
  key: 1,
  class: "form-control-helper-text"
};
const _sfc_main$n = /* @__PURE__ */ defineComponent({
  __name: "vu-input-date",
  props: /* @__PURE__ */ mergeModels({
    clearable: { type: Boolean },
    contentClass: {},
    contentStyle: {},
    dateFormatLocale: { default: "en" },
    dateFormatOptions: { default: () => ({
      weekday: "short",
      year: "numeric",
      month: "short",
      day: "2-digit"
    }) },
    disabled: { type: Boolean },
    firstDay: { default: 1 },
    hideOnSelect: { type: Boolean, default: true },
    max: { default: () => /* @__PURE__ */ new Date("2099-12-31T23:59:59.999Z") },
    min: { default: () => /* @__PURE__ */ new Date("1900-01-01T00:00:00.000Z") },
    monthsLabels: {},
    showWeekNumber: { type: Boolean },
    unselectableDaysOfWeek: {},
    weekdaysLabels: {},
    weekdaysShortLabels: {},
    yearRange: { default: 10 },
    required: { type: Boolean },
    rules: {},
    helper: {},
    label: {},
    options: {},
    placeholder: {},
    type: {}
  }, {
    "modelValue": {},
    "modelModifiers": {}
  }),
  emits: ["update:modelValue"],
  setup(__props) {
    const props = __props;
    const { $i18n: $i18n2 } = useTranslations("date");
    const date = useModel(__props, "modelValue");
    const placeholder = computed(() => props.placeholder ?? $i18n2("selectAValue"));
    const { classes, validate, errorBucketRef } = useValidatable(date, props);
    useRegistrableInput({
      validate: () => validate(date.value)
    });
    useTemplateRef("activator");
    useTemplateRef("input");
    const open = ref(false);
    const stringifiedValue = ref("");
    const isEmpty = computed(() => date.value === null || date.value === void 0);
    function handleSelect(event) {
      date.value = event;
      if (props.hideOnSelect) {
        open.value = false;
      }
    }
    function clearDate() {
      date.value = null;
    }
    watch(date, (value) => {
      if (value) {
        stringifiedValue.value = new Intl.DateTimeFormat(props.dateFormatLocale, props.dateFormatOptions).format(value);
      } else {
        stringifiedValue.value = "";
      }
    }, { immediate: true });
    return (_ctx, _cache) => {
      var _a3, _b2;
      const _directive_click_outside = resolveDirective("click-outside");
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(["form-group", unref(classes)])
      }, [
        ((_a3 = __props.label) == null ? void 0 : _a3.length) ? (openBlock(), createElementBlock("label", _hoisted_1$k, [
          createTextVNode(toDisplayString(__props.label) + " ", 1),
          __props.required ? (openBlock(), createElementBlock("span", _hoisted_2$a, " * ")) : createCommentVNode("", true)
        ])) : createCommentVNode("", true),
        withDirectives((openBlock(), createElementBlock("div", _hoisted_3$7, [
          createBaseVNode("div", {
            class: normalizeClass(["input-container", { border: __props.clearable }])
          }, [
            createBaseVNode("input", {
              ref: "input",
              class: normalizeClass(["form-control input-date", { "filled": !isEmpty.value, "no-border": __props.clearable }]),
              disabled: __props.disabled,
              placeholder: placeholder.value,
              readonly: "",
              type: "text",
              value: stringifiedValue.value,
              onClick: _cache[0] || (_cache[0] = ($event) => open.value = true)
            }, null, 10, _hoisted_4$6),
            __props.clearable ? (openBlock(), createElementBlock("span", {
              key: 0,
              class: "input-date-reset fonticon fonticon-clear",
              onClick: clearDate
            })) : createCommentVNode("", true)
          ], 2),
          createVNode(_sfc_main$o, {
            modelValue: date.value,
            "onUpdate:modelValue": [
              _cache[1] || (_cache[1] = ($event) => date.value = $event),
              handleSelect
            ],
            class: normalizeClass(__props.contentClass),
            "first-day": __props.firstDay,
            max: __props.max,
            min: __props.min,
            "months-labels": __props.monthsLabels,
            show: open.value,
            "show-week-number": __props.showWeekNumber,
            style: normalizeStyle([{ position: "absolute", top: "38px" }, __props.contentStyle]),
            "unselectable-days-of-week": __props.unselectableDaysOfWeek,
            "weekdays-labels": __props.weekdaysLabels,
            "weekdays-short-labels": __props.weekdaysShortLabels,
            "year-range": __props.yearRange,
            onBoundaryChange: _cache[2] || (_cache[2] = ($event) => date.value = $event.value)
          }, null, 8, ["modelValue", "class", "first-day", "max", "min", "months-labels", "show", "show-week-number", "style", "unselectable-days-of-week", "weekdays-labels", "weekdays-short-labels", "year-range"])
        ])), [
          [_directive_click_outside, () => {
            open.value = false;
          }]
        ]),
        (openBlock(true), createElementBlock(Fragment, null, renderList(unref(errorBucketRef), (error, pos) => {
          return openBlock(), createElementBlock("span", {
            key: `${pos}-error-${error}`,
            class: "form-control-error-text",
            style: { "display": "block" }
          }, toDisplayString(error), 1);
        }), 128)),
        ((_b2 = __props.helper) == null ? void 0 : _b2.length) ? (openBlock(), createElementBlock("span", _hoisted_5$3, toDisplayString(__props.helper), 1)) : createCommentVNode("", true)
      ], 2);
    };
  }
});
const vuInputDate = /* @__PURE__ */ _export_sfc(_sfc_main$n, [["__scopeId", "data-v-797b8121"]]);
const _hoisted_1$j = { class: "icon-link__link" };
const _sfc_main$m = /* @__PURE__ */ defineComponent({
  __name: "vu-icon-link",
  props: {
    label: {},
    icon: {},
    isActive: { type: Boolean }
  },
  setup(__props) {
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("a", {
        class: normalizeClass(["vu-icon-link", { active: __props.isActive }])
      }, [
        __props.icon ? (openBlock(), createBlock(VuIcon, {
          key: 0,
          class: "icon-link__icon",
          icon: __props.icon
        }, null, 8, ["icon"])) : createCommentVNode("", true),
        createBaseVNode("span", _hoisted_1$j, [
          renderSlot(_ctx.$slots, "default", {}, () => [
            createTextVNode(toDisplayString(__props.label), 1)
          ], true)
        ])
      ], 2);
    };
  }
});
const VuIconLink = /* @__PURE__ */ _export_sfc(_sfc_main$m, [["__scopeId", "data-v-bd96fc30"]]);
const _hoisted_1$i = { class: "alert-message-wrap" };
const _hoisted_2$9 = ["innerHTML"];
const _sfc_main$l = /* @__PURE__ */ defineComponent({
  __name: "vu-message",
  props: /* @__PURE__ */ mergeModels({
    text: {},
    isClosable: { type: Boolean, default: true },
    color: { default: "primary" },
    link: {},
    linkHandler: {},
    timeout: {}
  }, {
    "show": { default: true },
    "showModifiers": {}
  }),
  emits: /* @__PURE__ */ mergeModels(["clickLink"], ["update:show"]),
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit2 = __emit;
    let activeTimeout;
    const show = useModel(__props, "show");
    const classes = computed(() => [
      `alert-${props.color}`,
      { "alert-closable": props.isClosable }
    ]);
    function dispose() {
      show.value = false;
    }
    function clearTimers() {
      clearTimeout(activeTimeout);
    }
    function setTimeoutHandler() {
      clearTimers();
      if (show.value && props.timeout) {
        activeTimeout = setTimeout(dispose, props.timeout);
      }
    }
    watch([() => props.timeout, () => props.text, show], setTimeoutHandler, { immediate: true });
    onBeforeUnmount(clearTimers);
    return (_ctx, _cache) => {
      return show.value ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["vu-message relative alert-has-icon", classes.value])
      }, [
        _cache[2] || (_cache[2] = createBaseVNode("span", { class: "icon fonticon" }, null, -1)),
        createBaseVNode("span", _hoisted_1$i, [
          renderSlot(_ctx.$slots, "default", {}, () => [
            createBaseVNode("div", { innerHTML: __props.text }, null, 8, _hoisted_2$9)
          ], true)
        ]),
        __props.link ? (openBlock(), createBlock(VuIconLink, {
          key: 0,
          class: "vu-message_link",
          label: __props.link,
          onClick: _cache[0] || (_cache[0] = () => {
            var _a3;
            emit2("clickLink");
            (_a3 = __props.linkHandler) == null ? void 0 : _a3.call(__props);
          })
        }, null, 8, ["label"])) : createCommentVNode("", true),
        __props.isClosable ? (openBlock(), createElementBlock("span", {
          key: 1,
          class: "close fonticon fonticon-cancel",
          onClick: _cache[1] || (_cache[1] = ($event) => dispose())
        })) : createCommentVNode("", true)
      ], 2)) : createCommentVNode("", true);
    };
  }
});
const VuMessage = /* @__PURE__ */ _export_sfc(_sfc_main$l, [["__scopeId", "data-v-7ece491c"]]);
function tryOnScopeDispose(fn) {
  if (getCurrentScope()) {
    onScopeDispose(fn);
    return true;
  }
  return false;
}
function toValue(r) {
  return typeof r === "function" ? r() : unref(r);
}
const isClient = typeof window !== "undefined" && typeof document !== "undefined";
typeof WorkerGlobalScope !== "undefined" && globalThis instanceof WorkerGlobalScope;
const toString = Object.prototype.toString;
const isObject = (val) => toString.call(val) === "[object Object]";
const noop = () => {
};
const isIOS = /* @__PURE__ */ getIsIOS();
function getIsIOS() {
  var _a3, _b2;
  return isClient && ((_a3 = window == null ? void 0 : window.navigator) == null ? void 0 : _a3.userAgent) && (/iP(ad|hone|od)/.test(window.navigator.userAgent) || ((_b2 = window == null ? void 0 : window.navigator) == null ? void 0 : _b2.maxTouchPoints) > 2 && /iPad|Macintosh/.test(window == null ? void 0 : window.navigator.userAgent));
}
function toRef(...args) {
  if (args.length !== 1)
    return toRef$3(...args);
  const r = args[0];
  return typeof r === "function" ? readonly(customRef(() => ({ get: r, set: noop }))) : ref(r);
}
function unrefElement(elRef) {
  var _a3;
  const plain = toValue(elRef);
  return (_a3 = plain == null ? void 0 : plain.$el) != null ? _a3 : plain;
}
const defaultWindow = isClient ? window : void 0;
function useEventListener(...args) {
  let target;
  let events;
  let listeners;
  let options;
  if (typeof args[0] === "string" || Array.isArray(args[0])) {
    [events, listeners, options] = args;
    target = defaultWindow;
  } else {
    [target, events, listeners, options] = args;
  }
  if (!target)
    return noop;
  if (!Array.isArray(events))
    events = [events];
  if (!Array.isArray(listeners))
    listeners = [listeners];
  const cleanups = [];
  const cleanup = () => {
    cleanups.forEach((fn) => fn());
    cleanups.length = 0;
  };
  const register = (el2, event, listener, options2) => {
    el2.addEventListener(event, listener, options2);
    return () => el2.removeEventListener(event, listener, options2);
  };
  const stopWatch = watch(
    () => [unrefElement(target), toValue(options)],
    ([el2, options2]) => {
      cleanup();
      if (!el2)
        return;
      const optionsClone = isObject(options2) ? { ...options2 } : options2;
      cleanups.push(
        ...events.flatMap((event) => {
          return listeners.map((listener) => register(el2, event, listener, optionsClone));
        })
      );
    },
    { immediate: true, flush: "post" }
  );
  const stop2 = () => {
    stopWatch();
    cleanup();
  };
  tryOnScopeDispose(stop2);
  return stop2;
}
function resolveElement(el2) {
  if (typeof Window !== "undefined" && el2 instanceof Window)
    return el2.document.documentElement;
  if (typeof Document !== "undefined" && el2 instanceof Document)
    return el2.documentElement;
  return el2;
}
function checkOverflowScroll(ele) {
  const style = window.getComputedStyle(ele);
  if (style.overflowX === "scroll" || style.overflowY === "scroll" || style.overflowX === "auto" && ele.clientWidth < ele.scrollWidth || style.overflowY === "auto" && ele.clientHeight < ele.scrollHeight) {
    return true;
  } else {
    const parent2 = ele.parentNode;
    if (!parent2 || parent2.tagName === "BODY")
      return false;
    return checkOverflowScroll(parent2);
  }
}
function preventDefault(rawEvent) {
  const e = rawEvent || window.event;
  const _target = e.target;
  if (checkOverflowScroll(_target))
    return false;
  if (e.touches.length > 1)
    return true;
  if (e.preventDefault)
    e.preventDefault();
  return false;
}
const elInitialOverflow = /* @__PURE__ */ new WeakMap();
function useScrollLock(element, initialState = false) {
  const isLocked = ref(initialState);
  let stopTouchMoveListener = null;
  let initialOverflow;
  watch(toRef(element), (el2) => {
    const target = resolveElement(toValue(el2));
    if (target) {
      const ele = target;
      if (!elInitialOverflow.get(ele))
        elInitialOverflow.set(ele, initialOverflow);
      if (isLocked.value)
        ele.style.overflow = "hidden";
    }
  }, {
    immediate: true
  });
  const lock = () => {
    const el2 = resolveElement(toValue(element));
    if (!el2 || isLocked.value)
      return;
    if (isIOS) {
      stopTouchMoveListener = useEventListener(
        el2,
        "touchmove",
        (e) => {
          preventDefault(e);
        },
        { passive: false }
      );
    }
    el2.style.overflow = "hidden";
    isLocked.value = true;
  };
  const unlock = () => {
    var _a3;
    const el2 = resolveElement(toValue(element));
    if (!el2 || !isLocked.value)
      return;
    isIOS && (stopTouchMoveListener == null ? void 0 : stopTouchMoveListener());
    el2.style.overflow = (_a3 = elInitialOverflow.get(el2)) != null ? _a3 : "";
    elInitialOverflow.delete(el2);
    isLocked.value = false;
  };
  tryOnScopeDispose(unlock);
  return computed({
    get() {
      return isLocked.value;
    },
    set(v) {
      if (v)
        lock();
      else
        unlock();
    }
  });
}
function onScrollLock() {
  let isMounted = false;
  const state = ref(false);
  return (el2, binding) => {
    state.value = binding.value;
    if (isMounted)
      return;
    isMounted = true;
    const isLocked = useScrollLock(el2, binding.value);
    watch(state, (v) => isLocked.value = v);
  };
}
onScrollLock();
const _hoisted_1$h = { class: "INTFCenter-filter-section" };
const _hoisted_2$8 = { class: "INTFCenter-date-filter-section INTFCenter-start-date" };
const _hoisted_3$6 = { class: "INTFCenter-color-brown text-sm block" };
const _hoisted_4$5 = { class: "INTFCenter-date-filter-section INTFCenter-end-date" };
const _hoisted_5$2 = { class: "INTFCenter-filter-search-section" };
const _hoisted_6$2 = { class: "INTFCenter-badge-searches" };
const _hoisted_7$1 = { class: "INTFCenter-search-input-container" };
const _hoisted_8$1 = { class: "INTFCenter-filter-checkbox INTFCenter-color-brown" };
const _hoisted_9 = { class: "INTFCenter-read-checkbox" };
const _hoisted_10 = { class: "INTFCenter-starred-checkbox" };
const _hoisted_11 = { key: 0 };
const _sfc_main$k = {
  __name: "FilterOptions",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const store = useFilterStore();
    const settings2 = useSettingsStore();
    const lang = ref("en");
    const search = inject("search");
    const getTenants2 = computed(() => {
      const tenants = [];
      settings2.tenants.tenants.forEach((tenant) => {
        if (!tenant.NAME)
          return;
        const tenantInfo = {
          value: tenant.PLATFORMID,
          label: tenant.NAME
        };
        tenants.push(tenantInfo);
      });
      return tenants;
    });
    function addSearch() {
      const { addSearch: addSearch2 } = useFilterStore();
      addSearch2(search);
    }
    onMounted(async () => {
      const I18n = await getI18n();
      lang.value = I18n.getCurrentLanguage();
    });
    const maxDate = computed(() => {
      const date = store.filter.last_date;
      if (!date)
        return new Date(Date.now());
      return new Date(date);
    });
    const minDate = computed(() => {
      const date = store.filter.first_date;
      if (!date)
        return void 0;
      return new Date(date);
    });
    return (_ctx, _cache) => {
      const _component_vu_badge = resolveComponent("vu-badge");
      const _component_vu_input = resolveComponent("vu-input");
      const _component_vu_single_checkbox = resolveComponent("vu-single-checkbox");
      const _component_vu_multiple_select = resolveComponent("vu-multiple-select");
      return openBlock(), createElementBlock(Fragment, null, [
        createBaseVNode("div", _hoisted_1$h, [
          createBaseVNode("div", _hoisted_2$8, [
            createVNode(unref(vuInputDate), {
              modelValue: unref(store).filter.first_date,
              "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => unref(store).filter.first_date = $event),
              clearable: true,
              "content-class": "INTFCenter-filter-date INTFCenter-start-date",
              "date-format-locale": lang.value,
              max: maxDate.value,
              placeholder: unref($i18n2)("filterDateDefaultValue")
            }, null, 8, ["modelValue", "date-format-locale", "max", "placeholder"])
          ]),
          createBaseVNode("span", _hoisted_3$6, toDisplayString(unref($i18n2)("to")), 1),
          createBaseVNode("div", _hoisted_4$5, [
            createVNode(unref(vuInputDate), {
              modelValue: unref(store).filter.last_date,
              "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => unref(store).filter.last_date = $event),
              clearable: true,
              "content-class": "INTFCenter-filter-date INTFCenter-end-date",
              "date-format-locale": lang.value,
              max: new Date(Date.now()),
              min: minDate.value,
              placeholder: unref($i18n2)("filterDateDefaultValue")
            }, null, 8, ["modelValue", "date-format-locale", "max", "min", "placeholder"])
          ])
        ]),
        createBaseVNode("div", _hoisted_5$2, [
          createBaseVNode("div", _hoisted_6$2, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(unref(store).filter.searches, (text) => {
              return openBlock(), createBlock(_component_vu_badge, {
                key: text,
                closable: "",
                color: "default",
                selectable: "",
                onClose: ($event) => unref(store).filter.searches.splice(unref(store).filter.searches.indexOf(text), 1)
              }, {
                default: withCtx(() => [
                  createTextVNode(toDisplayString(text), 1)
                ]),
                _: 2
              }, 1032, ["onClose"]);
            }), 128))
          ]),
          createBaseVNode("div", _hoisted_7$1, [
            createVNode(_component_vu_input, {
              modelValue: unref(search),
              "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => isRef(search) ? search.value = $event : null),
              class: "INTFCenter-filter-input",
              placeholder: unref($i18n2)("searches"),
              onKeyup: withKeys(addSearch, ["enter"])
            }, null, 8, ["modelValue", "placeholder"])
          ])
        ]),
        createBaseVNode("div", _hoisted_8$1, [
          createBaseVNode("div", _hoisted_9, [
            createVNode(_component_vu_single_checkbox, {
              modelValue: unref(store).filter.read,
              "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => unref(store).filter.read = $event),
              class: "INTFCenter-read-read-checkbox",
              label: unref($i18n2)("read")
            }, null, 8, ["modelValue", "label"]),
            createVNode(_component_vu_single_checkbox, {
              modelValue: unref(store).filter.unread,
              "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => unref(store).filter.unread = $event),
              class: "INTFCenter-read-unread-checkbox",
              label: unref($i18n2)("unread")
            }, null, 8, ["modelValue", "label"])
          ]),
          createBaseVNode("div", _hoisted_10, [
            createVNode(_component_vu_single_checkbox, {
              modelValue: unref(store).filter.starred,
              "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => unref(store).filter.starred = $event),
              class: "INTFCenter-starred-starred-checkbox",
              label: unref($i18n2)("starred")
            }, null, 8, ["modelValue", "label"]),
            createVNode(_component_vu_single_checkbox, {
              modelValue: unref(store).filter.unstarred,
              "onUpdate:modelValue": _cache[6] || (_cache[6] = ($event) => unref(store).filter.unstarred = $event),
              class: "INTFCenter-starred-unstarred-checkbox",
              label: unref($i18n2)("unstarred")
            }, null, 8, ["modelValue", "label"])
          ])
        ]),
        !unref(settings2).hidePlatformSelection && !!unref(settings2).isTenantAgnostic ? (openBlock(), createElementBlock("div", _hoisted_11, [
          createVNode(_component_vu_multiple_select, {
            modelValue: unref(store).filter.tenants,
            "onUpdate:modelValue": _cache[7] || (_cache[7] = ($event) => unref(store).filter.tenants = $event),
            class: "INTFCenter-filter-platform-section",
            options: getTenants2.value,
            placeholder: unref($i18n2)("platformsSelection")
          }, null, 8, ["modelValue", "options", "placeholder"])
        ])) : createCommentVNode("", true)
      ], 64);
    };
  }
};
function useFilterManagement() {
  const store = useFilterStore();
  const notifStore = useNotificationsStore();
  const applyFilter = (search) => {
    if (search.value.trim().length) {
      if (!store.filter.searches.includes(search.value.trim())) {
        if (store.filter.searches.length < 6) {
          store.filter.searches.push(search.value.trim());
        }
        search.value = "";
      }
      return;
    }
    store.saveToLocalStorage();
    notifStore.resetStates();
    notifStore.setIsLoading(true);
    filter(store.getFilter());
    if (!store.filter.unread && !store.filter.read && !store.filter.starred && !store.filter.unstarred && !store.filter.first_date && !store.filter.last_date) {
      getUnreadTotal();
    }
    store.setOpened(false);
    notifStore.setFilterQuery(true);
  };
  const resetFilter = () => {
    store.resetFilter();
    notifStore.resetStates();
    notifStore.setIsLoading(true);
    filter({ ...store.getFilter(), unread: true });
    getUnreadTotal();
    notifStore.setFilterQuery(true);
  };
  return {
    applyFilter,
    resetFilter
  };
}
const _sfc_main$j = {
  __name: "FilterBody",
  setup(__props) {
    const { setOpened } = useFilterStore();
    const { notificationFilterViewTracker } = NotificationTrackerUsage();
    const filterViewTracker = notificationFilterViewTracker();
    const search = ref("");
    provide("search", search);
    function _resetFilter() {
      filterViewTracker.resetFilterFields();
      const { resetFilter } = useFilterManagement();
      resetFilter();
    }
    function _applyFilter() {
      const { applyFilter } = useFilterManagement();
      applyFilter(search);
      setTimeout(() => {
        filterViewTracker.clickFilterButton();
      }, 5e3);
    }
    function closeFilter() {
      filterViewTracker.cancelFilterView();
      setOpened(false);
    }
    return (_ctx, _cache) => {
      const _component_vu_form = resolveComponent("vu-form");
      return openBlock(), createBlock(_component_vu_form, { class: "INTFCenter-filter" }, {
        default: withCtx(() => [
          createVNode(_sfc_main$k),
          createVNode(_sfc_main$s, {
            onApply: _applyFilter,
            onClose: closeFilter,
            onReset: _resetFilter
          })
        ]),
        _: 1
      });
    };
  }
};
const _hoisted_1$g = { class: "INTFCenter-filter-tool" };
const _sfc_main$i = {
  __name: "Filter",
  setup(__props) {
    const storeFilter = useFilterStore();
    const opened = computed(() => {
      return storeFilter.opened;
    });
    const { $i18n: $i18n2 } = useTranslations$1();
    function setOpened() {
      const { notificationFilterViewTracker } = NotificationTrackerUsage();
      const filterViewTracker = notificationFilterViewTracker();
      if (opened.value) {
        storeFilter.setOpened(false);
        filterViewTracker.clicknotifFilterIconToHide();
      } else {
        storeFilter.setOpened(true);
        filterViewTracker.clicknotifFilterIconToShow();
      }
    }
    return (_ctx, _cache) => {
      const _component_vu_btn = resolveComponent("vu-btn");
      const _directive_tooltip = resolveDirective("tooltip");
      return openBlock(), createElementBlock("div", _hoisted_1$g, [
        withDirectives((openBlock(), createBlock(_component_vu_btn, {
          class: normalizeClass(["INTFCenter-btn-transparent INTFCenter-filter-btn", {
            "INTFCenter-btn-active": opened.value || unref(storeFilter).applied,
            "INTFCenter-btn-hover": !opened.value && !unref(storeFilter).applied
          }]),
          large: "",
          onClick: setOpened
        }, {
          default: withCtx(() => [..._cache[0] || (_cache[0] = [
            createBaseVNode("span", { class: "fonticon fonticon-filter INTFCenter-toolbar-icon" }, null, -1)
          ])]),
          _: 1
        }, 8, ["class"])), [
          [
            _directive_tooltip,
            !unref(mobileDevice) ? unref($i18n2)("filter") : void 0,
            void 0,
            { left: true }
          ]
        ])
      ]);
    };
  }
};
const _hoisted_1$f = { class: "INTFCenter-select-notification" };
const _hoisted_2$7 = ["title"];
const _sfc_main$h = {
  __name: "SelectTool",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const store = useNotificationsStore();
    function activateSelectionMode() {
      const { multipleSelectionActionTracker } = NotificationTrackerUsage();
      const selectionModeTracker = multipleSelectionActionTracker();
      store.selectionMode = !store.selectionMode;
      if (store.selectionMode)
        selectionModeTracker.enableSelection();
      else
        selectionModeTracker.disableSelection();
    }
    const length = computed(() => {
      return store.selected.length;
    });
    const lengthText = computed(() => {
      return length.value > 99 ? "99+" : length.value;
    });
    return (_ctx, _cache) => {
      const _component_vu_btn = resolveComponent("vu-btn");
      const _directive_tooltip = resolveDirective("tooltip");
      return openBlock(), createElementBlock("div", _hoisted_1$f, [
        withDirectives((openBlock(), createBlock(_component_vu_btn, {
          class: normalizeClass(["INTFCenter-btn-transparent", { "INTFCenter-btn-active": unref(store).selectionMode, "INTFCenter-btn-hover": !unref(store).selectionMode }]),
          large: "",
          onClick: activateSelectionMode
        }, {
          default: withCtx(() => [
            createBaseVNode("span", {
              class: normalizeClass(["fonticon INTFCenter-multi-select-icon", {
                "fonticon-select-off ": !unref(store).selectionMode,
                "fonticon-select-on ": unref(store).selectionMode
              }])
            }, null, 2)
          ]),
          _: 1
        }, 8, ["class"])), [
          [
            _directive_tooltip,
            !unref(mobileDevice) ? unref($i18n2)("multiSelection") : void 0,
            void 0,
            { left: true }
          ]
        ]),
        withDirectives(createBaseVNode("span", {
          class: "text-xs cursor-default INTFCenter-color-3ds INTFCenter-nb-notif-selected",
          title: `${length.value > 99 ? `${length.value} ${unref($i18n2)("selected")}` : ""}`
        }, toDisplayString(lengthText.value), 9, _hoisted_2$7), [
          [vShow, length.value > 0]
        ])
      ]);
    };
  }
};
async function tracker(report = {}) {
  report.appID = "X3DNTFC_AP";
  report.eventCategory = "notification.center.v2";
  try {
    const trackerAPI = await getTrackerAPI();
    trackerAPI.trackPageEvent(report);
  } catch (error) {
    console.log(`Analytics request failed for event
      action: ${report.eventAction} with ${report.eventLabel} label: ${error}`);
  }
}
async function trackPreferences(eventAction, { setting = null, state, optionType } = {}) {
  const settingsStore = useSettingsStore();
  const report = {
    eventLabel: settingsStore.isPlatformPreferences ? "PlatformPreferences" : "Preferences",
    eventAction,
    eventValue: state ? 1 : 0,
    persDim: {}
  };
  if (setting) {
    report.persDim.pd2 = setting.servicename;
    report.persDim.pd3 = setting.service;
    report.persDim.pd4 = setting.name;
  }
  if (optionType)
    report.persDim.pd5 = optionType;
  await tracker(report);
}
const { $i18n: $i18n$4 } = useTranslations$1();
function markSelectedAsRead(read2) {
  const { multipleSelectionActionTracker } = NotificationTrackerUsage();
  const { getSelected, getNotificationById } = useNotificationsStore();
  const selected2 = getSelected();
  let length = 0;
  selected2.forEach((id2) => {
    const notification = getNotificationById(id2);
    if (notification.isRead !== read2) {
      length++;
      const data = {
        action: "notificationRead",
        id: notification.ID,
        clusterId: notification.CLUSTER_ID,
        archive: !!notification.FROM_ARCHIVE,
        read: read2,
        ...notification.isGroup && { groupID: notification.GROUPID, hiddenMerged: true }
      };
      notificationRead(data);
    }
  });
  multipleSelectionActionTracker().readSelectedNotifications({ read: read2, length });
}
function markSelectedAsStarred(starred2) {
  const { multipleSelectionActionTracker } = NotificationTrackerUsage();
  const { getSelected, getNotificationById } = useNotificationsStore();
  const selected2 = getSelected();
  let length = 0;
  selected2.forEach((id2) => {
    const notification = getNotificationById(id2);
    if (notification.isStarred !== starred2) {
      length++;
      const data = {
        action: "notificationRead",
        archive: !!notification.FROM_ARCHIVE,
        id: notification.ID,
        starred: "starred",
        param: starred2 ? 1 : 0,
        clusterId: notification.CLUSTER_ID,
        ...notification.isGroup && { groupID: notification.GROUPID, hiddenMerged: true }
      };
      notificationRead(data);
    }
  });
  multipleSelectionActionTracker().starSelectedNotifications({ starred: starred2, length });
}
function unsubscribeSelected(forHour) {
  const { multipleSelectionActionTracker } = NotificationTrackerUsage();
  const { getSelected, getNotificationById } = useNotificationsStore();
  const copySelected = [...getSelected()];
  const notifServices = [];
  copySelected.forEach((id2) => {
    const notification = getNotificationById(id2);
    const find = notifServices.find((notif) => notif.SERVICE_ID === notification.SERVICE_ID);
    if (!find) {
      notifServices.push(notification);
    }
  });
  notifServices.forEach((notification) => {
    const data = {
      id: notification.ID,
      subscribe: 0,
      ...forHour === true && { forHour: true }
    };
    unsubscribe(data);
  });
  multipleSelectionActionTracker().unsubscribeSelectedNotifications({ length: copySelected.length });
}
function deleteSelected() {
  const { multipleSelectionActionTracker } = NotificationTrackerUsage();
  const { getSelected, getNotificationById } = useNotificationsStore();
  const { tryNotificationDeletion } = useNotificationManagement();
  const { alertConfirmNotice } = usePreferencesManagement();
  alertConfirmNotice({
    title: $i18n$4("deleteSelectedTitle"),
    msg: $i18n$4("deleteSelectedMsg")
  }).then((confirmed) => {
    if (confirmed) {
      const copySelected = [...getSelected()];
      copySelected.forEach((id2) => {
        const notification = getNotificationById(id2);
        tryNotificationDeletion({
          id: notification.ID,
          merge: notification.isMerge,
          group: notification.isGroup,
          archive: !!notification.FROM_ARCHIVE,
          withCancelation: false
        });
      });
      multipleSelectionActionTracker().deleteSelectedNotifications({ length: copySelected.length });
    }
  });
}
function selectMenuDisabled(selectAllNotification) {
  return [
    {
      fonticon: "",
      name: "selectAll",
      text: $i18n$4("selectAll"),
      handler: () => {
        selectAllNotification();
      }
    },
    {
      class: "divider"
    },
    {
      fonticon: "fonticon fonticon-bell-alt",
      name: "read",
      disabled: true,
      text: $i18n$4("markSelectedAsRead")
    },
    {
      fonticon: "fonticon fonticon-bell",
      name: "read",
      disabled: true,
      text: $i18n$4("markSelectedAsUnread")
    },
    {
      fonticon: "fonticon fonticon-star",
      name: "important",
      disabled: true,
      text: $i18n$4("markSelectedAsStarred")
    },
    {
      fonticon: "fonticon fonticon-favorite-off",
      name: "important",
      text: $i18n$4("markSelectedAsUnstarred"),
      disabled: true
    },
    {
      class: "divider"
    },
    {
      fonticon: "fonticon fonticon-list-delete",
      name: "unsubscribe",
      disabled: true,
      text: $i18n$4("unsubscribeSelected")
    },
    {
      fonticon: "fonticon fonticon-trash",
      text: $i18n$4("deleteSelected"),
      disabled: true,
      name: "delete"
    }
  ];
}
function selectMenu({
  canSelectMore,
  moreThanOneSelected,
  selectAll: selectAll2,
  unselectAll: unselectAll2,
  canMarkAsStarredMore,
  canMarkSelectedAsUnstarred,
  canMarkAllAsRead,
  canMarkAllAsUnread
}) {
  const { multipleSelectionActionTracker } = NotificationTrackerUsage();
  let menu = [];
  const firstOption = {
    fonticon: "",
    name: "selectAll",
    text: canSelectMore ? $i18n$4("selectAll") : $i18n$4("unselectAll"),
    handler: (item) => {
      if (item.text === $i18n$4("unselectAll")) {
        unselectAll2();
        multipleSelectionActionTracker().selectionAction({ action: "unselectAllNotifications", length: 0 });
        item.text = $i18n$4("selectAll");
      } else {
        selectAll2();
        item.text = $i18n$4("unselectAll");
      }
    }
  };
  menu.push(firstOption);
  menu.push({
    class: "divider"
  });
  if (moreThanOneSelected) {
    const restOfMenu = [
      {
        fonticon: "fonticon fonticon-bell-alt",
        name: "read",
        text: $i18n$4("markSelectedAsRead"),
        disabled: !canMarkAllAsRead.value,
        handler: (item) => {
          markSelectedAsRead(true);
          item.disabled = true;
        }
      },
      {
        fonticon: "fonticon fonticon-bell",
        name: "read",
        text: $i18n$4("markSelectedAsUnread"),
        disabled: !canMarkAllAsUnread.value,
        handler: (item) => {
          markSelectedAsRead(false);
          item.disabled = true;
        }
      },
      {
        fonticon: "fonticon fonticon-star",
        name: "important",
        text: $i18n$4("markSelectedAsStarred"),
        disabled: !canMarkAsStarredMore.value,
        handler: (item) => {
          markSelectedAsStarred(true);
          item.disabled = true;
        }
      },
      {
        fonticon: "fonticon fonticon-favorite-off",
        name: "important",
        text: $i18n$4("markSelectedAsUnstarred"),
        disabled: !canMarkSelectedAsUnstarred.value,
        handler: (item) => {
          markSelectedAsStarred(false);
          item.disabled = true;
        }
      },
      {
        class: "divider"
      },
      {
        fonticon: "fonticon fonticon-list-delete",
        name: "unsubscribe",
        text: $i18n$4("unsubscribeSelected"),
        items: [
          {
            fonticon: "fonticon fonticon-list-delete",
            text: $i18n$4("unsubscribeForHour"),
            name: "anHour",
            handler: () => {
              unsubscribeSelected(true);
            }
          },
          {
            fonticon: "fonticon fonticon-list-delete",
            text: $i18n$4("unsubscribeIndefinitely"),
            name: "indefinetly",
            handler: () => {
              unsubscribeSelected(false);
            }
          }
        ]
      },
      {
        fonticon: "fonticon fonticon-trash",
        text: $i18n$4("deleteSelected"),
        name: "delete",
        handler: () => {
          deleteSelected();
        }
      }
    ];
    menu = menu.concat(restOfMenu);
  }
  const menuItems = ref(menu);
  return menuItems.value;
}
function readAllNotifications(read2) {
  const { settingIconActionTracker } = NotificationTrackerUsage();
  const iconsActionTracker = settingIconActionTracker();
  const { isFilterApplied, getFilter } = useFilterStore();
  const { filterNotifIds } = useNotificationsStore();
  if (isFilterApplied()) {
    notificationRead({
      action: "notificationRead",
      read: read2,
      scope: true,
      filter: true,
      filterData: getFilter()
    });
    read2 ? iconsActionTracker.readAllFilteredNotifications() : iconsActionTracker.unreadAllFilteredNotifications();
  } else {
    notificationRead({ action: "notificationRead", read: read2, scope: true, filter: false });
    read2 ? iconsActionTracker.readAllNotifications() : iconsActionTracker.unreadAllNotifications();
  }
}
function settingsMenu() {
  const { isEmpty } = useNotificationsStore();
  const {
    isTenantAgnostic,
    getCurrentTenant,
    hidePlatformSelection,
    setWindoShow,
    getPlatformPreferencesAccess
  } = useSettingsStore();
  const { alertConfirmNotice } = usePreferencesManagement();
  const { isFilterApplied } = useFilterStore();
  const { settingIconActionTracker } = NotificationTrackerUsage();
  const iconsActionTracker = settingIconActionTracker();
  const readAll = {
    fonticon: "",
    name: "readAll",
    text: isFilterApplied() ? $i18n$4("markAllFilterAsRead") : $i18n$4("markAllAsRead"),
    disabled: isEmpty,
    handler: () => {
      readAllNotifications(true);
    }
  };
  const unreadAll = {
    fonticon: "",
    name: "unreadAll",
    text: isFilterApplied() ? $i18n$4("markAllFilterAsUnread") : $i18n$4("markAllAsUnread"),
    disabled: isEmpty,
    handler: () => {
      readAllNotifications(false);
    }
  };
  const deleteAll2 = {
    fonticon: "",
    name: "deleteAll",
    text: isFilterApplied() ? $i18n$4("deleteFilteredNotif") : $i18n$4("deleteAll"),
    disabled: isEmpty,
    handler: () => {
      alertConfirmNotice(
        {
          title: isFilterApplied() ? $i18n$4("deleteFilteredTitle") : $i18n$4("deleteAllTitle"),
          msg: isFilterApplied() ? $i18n$4("deleteFilteredMsg") : $i18n$4("deleteAllMsg"),
          cancelLabel: $i18n$4("cancel")
        }
      ).then(
        (confirmed) => {
          if (confirmed) {
            if (isFilterApplied()) {
              const { filterNotifIds } = useNotificationsStore();
              const { getFilter } = useFilterStore();
              deleteAllFilteredNotification$1({
                notificationID: filterNotifIds,
                filterData: getFilter()
              });
              iconsActionTracker.deleteAllFilteredNotifications();
            } else {
              deleteAllNotification$1();
              iconsActionTracker.deleteAllNotifications();
            }
          }
        }
      );
    }
  };
  const selectPlatform = {
    fonticon: "",
    name: "tenantAgnosticMode",
    text: $i18n$4("notificationsDisplay"),
    items: [
      {
        fonticon: "",
        text: $i18n$4("currentPlatform"),
        name: "currentPlatform",
        selected: !isTenantAgnostic,
        handler: () => {
          updateTenantAgnosticMode({
            isTenantAgnostic: 0,
            hidePlatformSelection
          });
          iconsActionTracker.switchTenantNotifications({
            tenant: getCurrentTenant() ?? "current",
            persDim: {
              pd1: getCurrentTenant() ?? "current"
            }
          });
        }
      },
      {
        fonticon: "",
        text: $i18n$4("allPlatforms"),
        name: "allPlatforms",
        selected: isTenantAgnostic,
        handler: () => {
          updateTenantAgnosticMode({
            isTenantAgnostic: 1,
            hidePlatformSelection
          });
          iconsActionTracker.switchTenantNotifications({
            tenant: "all",
            persDim: {
              pd1: "all"
            }
          });
        }
      }
    ]
  };
  const preferences2 = {
    fonticon: "",
    name: "preferences",
    text: $i18n$4("preferencesMenu"),
    handler: () => {
      setWindoShow("PREFERENCES");
      iconsActionTracker.invokenotifSettingView();
    }
  };
  const platformPreferences = {
    fonticon: "",
    name: "platformPreferences",
    text: $i18n$4("platformPreferencesMenu"),
    handler: () => {
      setWindoShow("PLATFORM_PREFERENCES");
      trackPreferences("openPlatformPreferences");
      iconsActionTracker.invokenotifSettingView();
    }
  };
  const menu = [
    // actions on all notifications
    readAll,
    unreadAll,
    deleteAll2,
    // platform selection
    ...hidePlatformSelection ? [] : [selectPlatform],
    // preferences
    {
      class: "divider"
    },
    preferences2,
    ...getPlatformPreferencesAccess() ? [platformPreferences] : []
  ];
  return menu;
}
const _hoisted_1$e = { class: "INTFCenter-setting-div" };
const _sfc_main$g = {
  __name: "Setting",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const store = useNotificationsStore();
    const settingsStore = useSettingsStore();
    const filterStore = useFilterStore();
    const more = ref(false);
    const settingModel = ref([]);
    const menu = ref(settingsMenu());
    watch(
      () => [
        store.isEmpty,
        filterStore.isFilterApplied(),
        settingsStore.hidePlatformSelection,
        settingsStore.getPlatformPreferencesAccess()
      ],
      () => menu.value = settingsMenu()
    );
    return (_ctx, _cache) => {
      const _component_vu_btn = resolveComponent("vu-btn");
      const _component_vu_dropdownmenu = resolveComponent("vu-dropdownmenu");
      const _directive_tooltip = resolveDirective("tooltip");
      return openBlock(), createElementBlock("div", _hoisted_1$e, [
        createVNode(_component_vu_dropdownmenu, {
          modelValue: settingModel.value,
          "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => settingModel.value = $event),
          items: menu.value,
          position: "bottom-right",
          onClose: _cache[2] || (_cache[2] = ($event) => more.value = false)
        }, {
          default: withCtx(() => [
            withDirectives((openBlock(), createBlock(_component_vu_btn, {
              class: normalizeClass(["INTFCenter-btn-transparent INTFCenter-setting-btn", { "INTFCenter-btn-active": more.value, "INTFCenter-btn-hover": !more.value }]),
              large: "",
              onClick: _cache[0] || (_cache[0] = ($event) => more.value = true)
            }, {
              default: withCtx(() => [..._cache[3] || (_cache[3] = [
                createBaseVNode("span", { class: "fonticon fonticon-cog INTFCenter-toolbar-icon" }, null, -1)
              ])]),
              _: 1
            }, 8, ["class"])), [
              [
                _directive_tooltip,
                !unref(mobileDevice) ? unref($i18n2)("settings") : void 0,
                void 0,
                { left: true }
              ]
            ])
          ]),
          _: 1
        }, 8, ["modelValue", "items"])
      ]);
    };
  }
};
const _hoisted_1$d = { class: "INTFCenter-notif-center-toolbar INTFCenter-color-brown text-lg" };
const _hoisted_2$6 = { class: "INTFCenter-select-wrapper" };
const _sfc_main$f = {
  __name: "Toolbar",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const { multipleSelectionActionTracker } = NotificationTrackerUsage();
    const store = useNotificationsStore();
    const selectionMode = computed(() => {
      return store.selectionMode === true;
    });
    const hasNotification = computed(() => {
      return store.notifications.size > 0;
    });
    const canSelectMore = computed(() => {
      return store.selected.length < store.notifications.size;
    });
    const nothingSelected = computed(() => {
      return store.selected.length === 0;
    });
    const canMarkAsStarredMore = computed(() => {
      for (const key of store.notifications.keys()) {
        if (store.selected.includes(key) && store.notifications.get(key).STARRED === 0) {
          return store.notifications.get(key).STARRED === 0;
        }
      }
      return store.selected.length === -1;
    });
    const canMarkSelectedAsUnstarred = computed(() => {
      for (const key of store.notifications.keys()) {
        if (store.selected.includes(key) && store.notifications.get(key).STARRED === 1) {
          return store.notifications.get(key).STARRED === 1;
        }
      }
      return store.selected.length === -1;
    });
    const canMarkAllAsRead = computed(() => {
      for (const key of store.notifications.keys()) {
        if (store.selected.includes(key) && store.notifications.get(key).READ_DATE === null) {
          return !store.notifications.get(key).READ_DATE;
        }
      }
      return store.selected.length === -1;
    });
    const canMarkAllAsUnread = computed(() => {
      for (const key of store.notifications.keys()) {
        if (store.selected.includes(key) && store.notifications.get(key).READ_DATE !== null) {
          return !!store.notifications.get(key).READ_DATE;
        }
      }
      return store.selected.length === -1;
    });
    function selectAllNotification() {
      for (const key of store.notifications.keys()) {
        if (!store.selected.includes(key) && !store.notifications.get(key).deleted)
          store.selected.push(key);
      }
      multipleSelectionActionTracker().selectionAction({ action: "selectAllNotifications", length: store.selected.length });
    }
    const menu = ref([]);
    const menuDisabled = ref(selectMenuDisabled(selectAllNotification));
    function unselectAllNotification() {
      const length = store.selected.length;
      for (let i = 0; i < length; i++) {
        store.selected.pop();
      }
    }
    watch(canSelectMore, (n) => {
      if (menu.value.length > 0)
        menu.value[0].text = n ? $i18n2("selectAll") : $i18n2("unselectAll");
      if (menuDisabled.value.length > 0)
        menuDisabled.value[0].text = n ? $i18n2("selectAll") : $i18n2("unselectAll");
    });
    watch(
      () => store.notifications.get(store.selected[0]),
      (n) => {
        if (store.selected.length === 1) {
          menu.value = selectMenu({
            canSelectMore: canSelectMore.value,
            moreThanOneSelected: false,
            selectAll: selectAllNotification
          }).concat(n.getOptions);
        }
      },
      { deep: true }
    );
    watch(
      () => [store.selected.length, canMarkAsStarredMore, canMarkSelectedAsUnstarred, canMarkAllAsRead, canMarkAllAsUnread],
      async (n) => {
        if (n[0] === 0) {
          menu.value = menuDisabled.value;
        } else {
          if (n[0] === 1) {
            menu.value = selectMenu({
              canSelectMore: canSelectMore.value,
              moreThanOneSelected: false,
              selectAll: selectAllNotification,
              unselectAll: unselectAllNotification,
              canMarkAsStarredMore,
              canMarkSelectedAsUnstarred,
              canMarkAllAsRead,
              canMarkAllAsUnread
            }).concat(store.getNotificationById(store.selected[0]).getOptions);
          } else {
            menu.value = selectMenu({
              canSelectMore: canSelectMore.value,
              moreThanOneSelected: true,
              selectAll: selectAllNotification,
              unselectAll: unselectAllNotification,
              canMarkAsStarredMore,
              canMarkSelectedAsUnstarred,
              canMarkAllAsRead,
              canMarkAllAsUnread
            });
          }
        }
        await nextTick();
      },
      { deep: true }
    );
    watch(selectionMode, () => {
      unselectAllNotification();
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$d, [
        createBaseVNode("div", _hoisted_2$6, [
          createVNode(TransitionGroup, { name: "select-menu" }, {
            default: withCtx(() => [
              selectionMode.value && hasNotification.value ? (openBlock(), createBlock(_sfc_main$D, {
                key: 0,
                class: "INTFCenter-select-menu INTFCenter-select-menu-notification",
                options: nothingSelected.value ? menuDisabled.value : menu.value
              }, null, 8, ["options"])) : createCommentVNode("", true)
            ]),
            _: 1
          }),
          hasNotification.value ? (openBlock(), createBlock(_sfc_main$h, { key: 0 })) : createCommentVNode("", true)
        ]),
        createVNode(_sfc_main$i),
        createVNode(_sfc_main$g)
      ]);
    };
  }
};
const _hoisted_1$c = { class: "INTFCenter-notif-center-unread-counter" };
const _hoisted_2$5 = { key: 0 };
const _hoisted_3$5 = {
  key: 0,
  class: "INTFCenter-color-brown"
};
const _hoisted_4$4 = {
  key: 1,
  class: "INTFCenter-color-brown"
};
const _hoisted_5$1 = { key: 1 };
const _hoisted_6$1 = {
  key: 0,
  class: "INTFCenter-color-3ds"
};
const _hoisted_7 = {
  key: 1,
  class: "INTFCenter-color-3ds"
};
const _hoisted_8 = { key: 2 };
const _sfc_main$e = {
  __name: "UnreadCounter",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const store = useNotificationsStore();
    const filterStore = useFilterStore();
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$c, [
        !unref(filterStore).applied ? (openBlock(), createElementBlock("div", _hoisted_2$5, [
          unref(store).unreadTotal > 1 ? (openBlock(), createElementBlock("strong", _hoisted_3$5, toDisplayString(unref(store).unreadTotal) + " " + toDisplayString(unref($i18n2)("unreadNotifications")), 1)) : unref(store).unreadTotal === 1 ? (openBlock(), createElementBlock("strong", _hoisted_4$4, toDisplayString(unref(store).unreadTotal) + " " + toDisplayString(unref($i18n2)("oneUnreadNotification")), 1)) : createCommentVNode("", true)
        ])) : (openBlock(), createElementBlock("div", _hoisted_5$1, [
          unref(store).filterTotal ? (openBlock(), createElementBlock("strong", _hoisted_6$1, toDisplayString(unref(store).filterTotal) + " " + toDisplayString(unref($i18n2)("foundNotifications")), 1)) : unref(store).filterTotal === 1 ? (openBlock(), createElementBlock("strong", _hoisted_7, toDisplayString(unref(store).filterTotal) + " " + toDisplayString(unref($i18n2)("oneFoundNotification")), 1)) : createCommentVNode("", true)
        ])),
        unref(store).filterTotal === 0 || unref(store).unreadTotal === 0 ? (openBlock(), createElementBlock("div", _hoisted_8)) : createCommentVNode("", true)
      ]);
    };
  }
};
const _hoisted_1$b = { class: "INTFCenter-center-header-container" };
const _hoisted_2$4 = { class: "INTFCenter-notif-center-header" };
const _hoisted_3$4 = {
  key: 0,
  class: "INTFCenter-filter-container"
};
const _sfc_main$d = {
  __name: "CenterHeader",
  setup(__props) {
    const storeFilter = useFilterStore();
    const opened = computed(() => {
      return storeFilter.opened;
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$b, [
        createBaseVNode("div", _hoisted_2$4, [
          createVNode(_sfc_main$e),
          createVNode(_sfc_main$f)
        ]),
        createVNode(Transition, { name: "filter" }, {
          default: withCtx(() => [
            opened.value ? (openBlock(), createElementBlock("div", _hoisted_3$4, [
              createVNode(_sfc_main$j)
            ])) : createCommentVNode("", true)
          ]),
          _: 1
        })
      ]);
    };
  }
};
const _hoisted_1$a = { class: "INTFCenter-notif-center-base-center" };
const _hoisted_2$3 = { class: "INTFCenter-header-container" };
const _hoisted_3$3 = { class: "INTFCenter-main-center-notification" };
const _hoisted_4$3 = { class: "INTFCenter-base-center" };
const _hoisted_5 = {
  key: 0,
  class: "INTFCenter-spinner-container"
};
const _hoisted_6 = {
  key: 0,
  class: "INTFCenter-center-section"
};
const _sfc_main$c = {
  __name: "BaseCenter",
  setup(__props) {
    const { loadMoreNotifications } = useNotificationManagement();
    const store = useNotificationsStore();
    const scroller = ref(null);
    const loading = computed(() => store.isLoading);
    const filterQuery = computed(() => store.filterQuery);
    const isEmpty = computed(() => store.isEmpty);
    const loadMoreFlag = computed(() => store.loadingMore);
    const { y } = useScroll(scroller, { behavior: "smooth" });
    watch(filterQuery, (newValue) => {
      if (newValue && y.value > 0) {
        const el2 = document.getElementById("INTFCenter-scroller");
        el2.scrollTo({ top: 0, behavior: "smooth" });
        store.setFilterQuery(false);
      }
    });
    useInfiniteScroll(
      scroller,
      () => {
        loadMoreNotifications();
      },
      {
        // check if can load more
        canLoadMore: () => {
          return store.canLoadMoreHistory === true || store.canLoadMoreFromArchive === true;
        },
        // distance from the bottom to trigger the load more
        distance: 50,
        // interval between each load more
        interval: 500
      }
    );
    return (_ctx, _cache) => {
      const _component_vu_spinner = resolveComponent("vu-spinner");
      const _component_vu_scroller = resolveComponent("vu-scroller");
      return openBlock(), createElementBlock("div", _hoisted_1$a, [
        createBaseVNode("div", _hoisted_2$3, [
          createVNode(_sfc_main$d)
        ]),
        createBaseVNode("div", _hoisted_3$3, [
          createBaseVNode("div", _hoisted_4$3, [
            createVNode(_component_vu_scroller, {
              id: "INTFCenter-scroller",
              ref_key: "scroller",
              ref: scroller,
              class: normalizeClass(["INTFCenter-center-body", { "INTFCenter-empty-loading": loading.value || isEmpty.value, "INTFCenter-loading-more": loadMoreFlag.value }]),
              infinite: loadMoreFlag.value
            }, {
              default: withCtx(() => [
                loading.value ? (openBlock(), createElementBlock("div", _hoisted_5, [
                  createVNode(_component_vu_spinner)
                ])) : createCommentVNode("", true),
                createVNode(Transition, { name: "center" }, {
                  default: withCtx(() => [
                    !loading.value ? (openBlock(), createElementBlock("div", _hoisted_6, [
                      isEmpty.value ? (openBlock(), createBlock(_sfc_main$G, { key: 0 })) : (openBlock(), createBlock(_sfc_main$t, { key: 1 }))
                    ])) : createCommentVNode("", true)
                  ]),
                  _: 1
                })
              ]),
              _: 1
            }, 8, ["class", "infinite"])
          ])
        ])
      ]);
    };
  }
};
const _hoisted_1$9 = ["alt", "src"];
const _sfc_main$b = {
  __name: "AppIcon",
  props: {
    setting: {
      type: Setting,
      required: true
    }
  },
  setup(__props) {
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(`INTFCenter-appIcon-${__props.setting.service}`)
      }, [
        createBaseVNode("img", {
          alt: `${__props.setting.name}icon`,
          class: "INTFCenter-appIcon-img",
          src: __props.setting.icon
        }, null, 8, _hoisted_1$9)
      ], 2);
    };
  }
};
const { $i18n: $i18n$3 } = useTranslations$1();
function useAppStatus({ setting }) {
  const now = useNow();
  const remainingTime = computed(() => {
    const currentTime = new Date(now.value).getTime();
    const unsubTime = new Date(setting.value.unsubscribe_date).getTime();
    const elapsedMilliseconds = Math.abs(currentTime - unsubTime);
    const elapsedMinutes = Math.round(elapsedMilliseconds / (1e3 * 60));
    return elapsedMinutes > 60 ? 60 : elapsedMinutes;
  });
  const status = computed(() => {
    let status2 = $i18n$3("centerIconText");
    if (setting.value.notif_by_uiEnabled)
      status2 += `, ${$i18n$3("alertIconText")}`;
    if (setting.value.notif_by_emailEnabled)
      status2 += `, ${$i18n$3("emailIconText")}`;
    if (setting.value.notif_by_browserEnabled)
      status2 += `, ${$i18n$3("browserIconText")}`;
    return status2;
  });
  const statusText = computed(() => {
    if (!setting.value.enable) {
      return $i18n$3("settingOff");
    }
    if (setting.value.unsubscribe_date !== null) {
      const timeValue = remainingTime.value === 60 ? 1 : remainingTime.value;
      const timeUnit = remainingTime.value === 60 ? $i18n$3("hourLeft") : $i18n$3("minLeft");
      return `${$i18n$3("settingOffForAnHour")} (${timeValue} ${timeUnit})`;
    }
    return status.value;
  });
  return {
    statusText
  };
}
const _hoisted_1$8 = { class: "INTFCenter-preference-appStatus" };
const _sfc_main$a = {
  __name: "AppStatus",
  props: {
    setting: {
      type: Setting,
      required: true
    }
  },
  setup(__props) {
    const props = __props;
    const { statusText } = useAppStatus({ setting: toRef$3(props, "setting") });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$8, toDisplayString(unref(statusText)), 1);
    };
  }
};
const _hoisted_1$7 = { class: "INTFCenter-preference-appTitle" };
const _sfc_main$9 = {
  __name: "AppTitle",
  props: {
    appTitle: {
      type: String,
      required: true
    }
  },
  setup(__props) {
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$7, toDisplayString(__props.appTitle), 1);
    };
  }
};
function useGlobalApp(setting) {
  const store = useSettingsStore();
  function openAppSetting() {
    store.setCurrentSetting(setting.value.id);
    store.setAppSettingOpened(true);
    trackPreferences("openApp", { setting: setting.value });
  }
  const needStatus = !store.isPlatformPreferences;
  return {
    openAppSetting,
    needStatus
  };
}
const _hoisted_1$6 = { class: "INTFCenter-pref-appname-right" };
const _sfc_main$8 = {
  __name: "GlobalApp",
  props: {
    setting: {
      type: Setting,
      required: true
    }
  },
  setup(__props) {
    const props = __props;
    const { openAppSetting, needStatus } = useGlobalApp(toRef$3(props, "setting"));
    return (_ctx, _cache) => {
      return __props.setting ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["INTFCenter-preference-appname", { "INTFCenter-preference-appname-off": !__props.setting.enable }]),
        "data-testid": "global-app-container",
        onClick: _cache[0] || (_cache[0] = (...args) => unref(openAppSetting) && unref(openAppSetting)(...args))
      }, [
        createVNode(_sfc_main$b, { setting: __props.setting }, null, 8, ["setting"]),
        createBaseVNode("div", _hoisted_1$6, [
          createBaseVNode("div", null, [
            createVNode(_sfc_main$9, {
              "app-title": __props.setting.servicename
            }, null, 8, ["app-title"]),
            unref(needStatus) ? (openBlock(), createBlock(_sfc_main$a, {
              key: 0,
              setting: __props.setting
            }, null, 8, ["setting"])) : createCommentVNode("", true)
          ]),
          _cache[1] || (_cache[1] = createBaseVNode("div", { class: "INTFCenter-btn-transparent-default INTFCenter-btn-hover INTFCenter-app-setting-btn" }, [
            createBaseVNode("span", { class: "fonticon fonticon-chevron-right INTFCenter-back-icon" })
          ], -1))
        ])
      ], 2)) : createCommentVNode("", true);
    };
  }
};
const _hoisted_1$5 = { class: "INTFCenter-preferences INTFCenter-preferences-home" };
const _sfc_main$7 = {
  __name: "PreferencesHome",
  setup(__props) {
    const store = useSettingsStore();
    const globalApp = computed(() => {
      return store.getAllGroupSettings;
    });
    return (_ctx, _cache) => {
      const _component_vu_scroller = resolveComponent("vu-scroller");
      return openBlock(), createElementBlock("div", _hoisted_1$5, [
        createVNode(_component_vu_scroller, { class: "INTFCenter-preferences-body" }, {
          default: withCtx(() => [
            createVNode(TransitionGroup, { name: "list" }, {
              default: withCtx(() => [
                (openBlock(true), createElementBlock(Fragment, null, renderList(globalApp.value, (setting) => {
                  return openBlock(), createElementBlock("div", {
                    key: setting.id
                  }, [
                    createVNode(_sfc_main$8, { setting }, null, 8, ["setting"])
                  ]);
                }), 128))
              ]),
              _: 1
            })
          ]),
          _: 1
        })
      ]);
    };
  }
};
const { $i18n: $i18n$2 } = useTranslations$1();
function useTogglePreference({ needDropDown, lock, toggleValue }) {
  const settingStore = useSettingsStore();
  const preferenceAdmin = settingStore.isPlatformPreferences;
  const isOpen = ref(false);
  const isRevertingToggle = ref(false);
  const lockContainerClass = preferenceAdmin ? "cursor-pointer" : "";
  const lockTitle = computed(() => preferenceAdmin ? lock.value ? $i18n$2("forcedPreferenceOption") : $i18n$2("notForcedPreferenceOption") : $i18n$2("forcedPreference"));
  const chevronClass = computed(() => isOpen.value ? "INTFCenter-app-setting-drop-down-open fonticon-expand-down" : "fonticon-expand-right");
  const needLock = computed(() => settingStore.isPlatformPreferences && lock.value !== null || lock.value);
  const lockClass = ref(getLockClassValue(lock.value));
  watch(lock, () => {
    lockClass.value = getLockClassValue(lock.value);
  });
  const needToggle = computed(() => toggleValue.value !== null);
  const toggleEnable = ref(toggleValue.value);
  watch(toggleValue, (newResult) => {
    toggleEnable.value = newResult;
  });
  const isDisabled = computed(() => preferenceAdmin ? false : lock.value);
  function toggleDropdown() {
    if (needDropDown.value) {
      isOpen.value = !isOpen.value;
      return isOpen.value;
    }
    return null;
  }
  function getLockClassValue(value) {
    return `${value ? "fonticon-lock" : "fonticon-lock-open"}
    ${preferenceAdmin ? "fonticon-clickable" : ""}`.trim();
  }
  function canUpdateLock() {
    if (preferenceAdmin) {
      return true;
    } else {
      const { messageNotice } = usePreferencesManagement();
      messageNotice($i18n$2("forcedPreference"), 3e3);
      return false;
    }
  }
  const revertLock = () => {
    lockClass.value = getLockClassValue(lock.value);
  };
  function handleClickNotAllowed(event) {
    const label = event.target.closest("label");
    if (isDisabled.value && label) {
      const { messageNotice } = usePreferencesManagement();
      messageNotice($i18n$2("forcedPreference"), 3e3);
    }
  }
  const revertToggleEnable = (oldValue) => {
    isRevertingToggle.value = true;
    toggleEnable.value = oldValue;
  };
  return {
    lockContainerClass,
    isRevertingToggle,
    lockTitle,
    chevronClass,
    needLock,
    lockClass,
    needToggle,
    toggleEnable,
    isDisabled,
    revertToggleEnable,
    getLockClassValue,
    canUpdateLock,
    revertLock,
    handleClickNotAllowed,
    toggleDropdown
  };
}
const _hoisted_1$4 = { class: "INTFCenter-toggle-preference-container grid-container" };
const _hoisted_2$2 = {
  key: 0,
  class: "logo-container",
  style: { "padding": "0 14px" },
  title: "Mail notifications disabled"
};
const _hoisted_3$2 = { class: "INTFCenter-appSetting-appTitle" };
const _hoisted_4$2 = ["title"];
const _sfc_main$6 = {
  __name: "TogglePreference",
  props: {
    needDropDown: {
      type: Boolean,
      default: false
    },
    icon: {
      type: String,
      default: null
    },
    description: {
      type: String,
      default: ""
    },
    lock: {
      type: Boolean,
      default: null
    },
    toggleValue: {
      type: Boolean,
      default: null
    }
  },
  emits: ["chevronUpdate", "lockUpdate", "toggleUpdate"],
  setup(__props, { emit: __emit }) {
    const props = __props;
    const emit2 = __emit;
    const {
      lockTitle,
      isRevertingToggle,
      lockContainerClass,
      chevronClass,
      needLock,
      lockClass,
      needToggle,
      toggleEnable,
      isDisabled,
      revertToggleEnable,
      toggleDropdown,
      getLockClassValue,
      revertLock,
      canUpdateLock,
      handleClickNotAllowed
    } = useTogglePreference({
      needDropDown: toRef$3(props, "needDropDown"),
      lock: toRef$3(props, "lock"),
      toggleValue: toRef$3(props, "toggleValue")
    });
    function toggleDropdownUpdate() {
      const newValue = toggleDropdown();
      if (newValue !== null)
        emit2("chevronUpdate", newValue);
    }
    function switchLockValue() {
      if (canUpdateLock()) {
        lockClass.value = getLockClassValue(!props.lock);
        emit2("lockUpdate", !props.lock, revertLock);
      }
    }
    watch(toggleEnable, (newValue, oldValue) => {
      if (newValue !== props.toggleValue && !isRevertingToggle.value) {
        emit2("toggleUpdate", newValue, revertToggleEnable.bind(null, oldValue));
      } else {
        isRevertingToggle.value = false;
      }
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", _hoisted_1$4, [
        __props.needDropDown ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: "INTFCenter-app-setting-drop-down INTFCenter-btn-preference-chevron cursor-pointer item-1",
          "data-testid": "toggle-preference-dropdown",
          onClick: toggleDropdownUpdate
        }, [
          createBaseVNode("span", {
            class: normalizeClass(["fonticon", unref(chevronClass)]),
            style: { "margin": "auto", "font-size": ".5rem", "line-height": "1", "max-width": "8px", "padding": "auto" }
          }, null, 2)
        ])) : createCommentVNode("", true),
        createBaseVNode("div", {
          class: normalizeClass(["item-2", {
            /* 'INTFCenter-preference-appname-off': settingOff() */
            "cursor-pointer": __props.needDropDown
          }]),
          "data-testid": "description-preference-dropdown",
          style: { "margin": "auto auto auto 0" },
          onClick: toggleDropdownUpdate
        }, [
          __props.icon ? (openBlock(), createElementBlock("div", _hoisted_2$2, [
            createBaseVNode("span", {
              class: normalizeClass(["fonticon logo fonticon-2x", __props.icon])
            }, null, 2)
          ])) : createCommentVNode("", true),
          createBaseVNode("div", _hoisted_3$2, toDisplayString(__props.description), 1)
        ], 2),
        unref(needLock) ? (openBlock(), createElementBlock("div", {
          key: 1,
          class: normalizeClass(["INTFCenter-toggle-preference-lock INTFCenter-btn-transparent-default item-3", unref(lockContainerClass)]),
          "data-testid": "toggle-preference-lock-container",
          onClick: switchLockValue
        }, [
          createBaseVNode("span", {
            class: normalizeClass(["fonticon", unref(lockClass)]),
            style: { "margin": "0px" },
            title: unref(lockTitle)
          }, null, 10, _hoisted_4$2)
        ], 2)) : createCommentVNode("", true),
        unref(needToggle) ? (openBlock(), createElementBlock("div", {
          key: 2,
          class: normalizeClass(["item-4 INTFCenter-appSetting-option", { "INTFCenter-toggle-preference-not-allowed": unref(isDisabled) }]),
          "data-testid": "toggle-preference-switch-container",
          onClick: _cache[1] || (_cache[1] = (...args) => unref(handleClickNotAllowed) && unref(handleClickNotAllowed)(...args))
        }, [
          createVNode(unref(VuSingleCheckbox), {
            modelValue: unref(toggleEnable),
            "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => isRef(toggleEnable) ? toggleEnable.value = $event : null),
            disabled: unref(isDisabled),
            switch: ""
          }, null, 8, ["modelValue", "disabled"])
        ], 2)) : createCommentVNode("", true)
      ]);
    };
  }
};
const TogglePreference = /* @__PURE__ */ _export_sfc$1(_sfc_main$6, [["__scopeId", "data-v-33680b95"]]);
const { $i18n: $i18n$1 } = useTranslations$1();
function useToggleHelper(settingId, option = null) {
  const settingStore = useSettingsStore();
  const isPlatformPreferences = settingStore.isPlatformPreferences;
  const setting = computed(() => settingStore.getSetting(settingId.value));
  const state = computed(() => option ? setting.value.getNotifOptionEnabled(option.value.name) : setting.value.enable);
  const isReadOnly = computed(() => option ? setting.value.getNotifOptionReadOnly(option.value.name) : setting.value.readOnly);
  function createWarning(isEnable, isEnforced) {
    if (isEnable && isEnforced)
      return $i18n$1("settingTurnedOnAndForced");
    else if (isEnable)
      return $i18n$1("settingTurnedOnNotForced");
    else if (isEnforced)
      return $i18n$1("settingTurnedOffAndForced");
    else
      return $i18n$1("settingTurnedOffNotForced");
  }
  async function updateState(newValue, revert) {
    const { alertConfirmNotice } = usePreferencesManagement();
    if (isPlatformPreferences) {
      const msg = option ? createWarning(newValue, setting.value.getNotifOptionReadOnly(option.value.name)) : createWarning(newValue, setting.value.readOnly);
      const confirmation = await alertConfirmNotice({
        title: $i18n$1("warningTitlePlatformPreferences"),
        msg,
        okLabel: $i18n$1("confirmButtonLabel"),
        cancelLabel: $i18n$1("cancel")
      });
      if (!confirmation) {
        revert();
        return;
      }
    }
    if (option) {
      setting.value.setNotifOptionEnabled(option.value.name, newValue);
      trackPreferences("setEnable", { setting: setting.value, state: newValue, optionType: option.value.name });
    } else {
      setting.value.setEnable(newValue);
      trackPreferences("setEnable", { setting: setting.value, state: newValue, optionType: "notification center" });
    }
  }
  async function updateReadOnly(newValue, revert) {
    const { alertConfirmNotice } = usePreferencesManagement();
    if (isPlatformPreferences) {
      const msg = option ? createWarning(setting.value.getNotifOptionEnabled(option.value.name), newValue) : createWarning(setting.value.enable, newValue);
      const confirmation = await alertConfirmNotice({
        title: $i18n$1("warningTitlePlatformPreferences"),
        msg: newValue ? msg : $i18n$1("settingTurnedUnlocked"),
        okLabel: $i18n$1("confirmButtonLabel"),
        cancelLabel: $i18n$1("cancel")
      });
      if (!confirmation) {
        revert();
        return;
      }
    }
    if (option) {
      setting.value.setNotifOptionReadOnly(option.value.name, newValue);
      trackPreferences("setReadOnly", { setting: setting.value, state: newValue, optionType: option.value.name });
    } else {
      setting.value.setReadOnly(newValue);
      trackPreferences("setReadOnly", { setting: setting.value, state: newValue, optionType: "notification center" });
    }
  }
  return {
    setting,
    state,
    isReadOnly,
    isPlatformPreferences,
    updateState,
    updateReadOnly
  };
}
const _sfc_main$5 = {
  __name: "AppGlobalSetting",
  props: {
    settingId: {
      type: Number,
      required: true
    }
  },
  setup(__props) {
    const props = __props;
    const {
      setting,
      state,
      isPlatformPreferences,
      updateState
    } = useToggleHelper(toRef$3(props, "settingId"));
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(["INTFCenter-appList-top", { "INTFCenter-preference-appname-off": !unref(setting).enable }])
      }, [
        createVNode(_sfc_main$9, {
          "app-title": unref(setting).servicename
        }, null, 8, ["app-title"]),
        !unref(isPlatformPreferences) ? (openBlock(), createBlock(TogglePreference, {
          key: 0,
          "toggle-value": unref(state),
          onToggleUpdate: unref(updateState)
        }, null, 8, ["toggle-value", "onToggleUpdate"])) : createCommentVNode("", true)
      ], 2);
    };
  }
};
const { $i18n } = useTranslations$1();
function useAppOption({ settingId, option }) {
  const settingStore = useSettingsStore();
  const setting = computed(() => settingStore.getSetting(settingId.value));
  const needToggle = computed(() => settingStore.isPlatformPreferences);
  const displayOption = computed(() => settingStore.isPlatformPreferences || setting.value.enable || setting.value.getNotifOptionReadOnly(option.value.name));
  const cursorReadOnly = computed(() => !settingStore.isPlatformPreferences && (option.value.name === "center" || setting.value.getNotifOptionReadOnly(option.value.name)));
  const optionEnabled = computed(() => option.value.name === "center" || setting.value.getNotifOptionEnabled(option.value.name));
  const tooltipText = computed(() => optionEnabled.value ? $i18n(`${option.value.name}NotificationTooltipEnabled`) : $i18n(`${option.value.name}NotificationTooltipDisabled`));
  async function updateSetting() {
    const { settingUpdateVerification } = usePreferencesManagement();
    if (!settingStore.isPlatformPreferences && (setting.value.readOnly || setting.value.getNotifOptionReadOnly(option.value.name))) {
      const { messageNotice } = usePreferencesManagement();
      messageNotice($i18n("forcedPreference"), 3e3);
    } else if (option.value.name !== "center" && (settingStore.isPlatformPreferences || await settingUpdateVerification(setting.value, option.value.name))) {
      const { updateState } = useToggleHelper(settingId, option);
      updateState(!setting.value.getNotifOptionEnabled(option.value.name));
    }
  }
  return {
    displayOption,
    cursorReadOnly,
    needToggle,
    optionEnabled,
    tooltipText,
    updateSetting
  };
}
const _hoisted_1$3 = ["title"];
const _sfc_main$4 = {
  __name: "AppOption",
  props: {
    option: {
      type: Object,
      required: true
    },
    settingId: {
      type: Number,
      required: true
    }
  },
  setup(__props) {
    const props = __props;
    const { $i18n: $i18n2 } = useTranslations$1();
    const {
      state,
      isReadOnly,
      updateState,
      updateReadOnly
    } = useToggleHelper(toRef$3(props, "settingId"), toRef$3(props, "option"));
    const {
      displayOption,
      cursorReadOnly,
      tooltipText,
      needToggle,
      optionEnabled,
      updateSetting
    } = useAppOption({
      settingId: toRef$3(props, "settingId"),
      option: toRef$3(props, "option")
    });
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock(Fragment, null, [
        unref(displayOption) ? (openBlock(), createElementBlock("div", {
          key: 0,
          class: normalizeClass(["INTFCenter-desktop", [
            `INTFCenter-option-${__props.option.name}`,
            { "INTFCenter-cursor-readonly": unref(cursorReadOnly) }
          ]]),
          "data-testid": "app-option-logo-container",
          title: unref(tooltipText),
          onClick: _cache[0] || (_cache[0] = (...args) => unref(updateSetting) && unref(updateSetting)(...args))
        }, [
          createBaseVNode("span", {
            class: normalizeClass([__props.option.fonticonName, `intfnotif-${unref(optionEnabled) ? "on" : "off"}`])
          }, null, 2),
          createBaseVNode("span", {
            class: normalizeClass(`intfnotif-${unref(optionEnabled) ? "on" : "off"}`)
          }, toDisplayString(unref($i18n2)(`${__props.option.name}IconText`)), 3)
        ], 10, _hoisted_1$3)) : createCommentVNode("", true),
        unref(needToggle) ? (openBlock(), createBlock(TogglePreference, {
          key: 1,
          lock: unref(isReadOnly),
          "toggle-value": unref(state),
          onLockUpdate: unref(updateReadOnly),
          onToggleUpdate: unref(updateState)
        }, null, 8, ["lock", "toggle-value", "onLockUpdate", "onToggleUpdate"])) : createCommentVNode("", true)
      ], 64);
    };
  }
};
function useAppSetting({ settingId }) {
  const settingStore = useSettingsStore();
  const setting = computed(() => settingStore.getSetting(settingId.value));
  const listOption = [
    // { name: 'center', icon: 'icon-desktop-center' },
    // { name: 'alert', icon: 'icon-desktop-alert' },
    // { name: 'email', icon: 'icon-desktop-email' },
    // { name: 'browser', icon: 'icon-desktop-empty' },
  ];
  const displaySetting = computed(() => settingStore.isPlatformPreferences || settingStore.getSettingGroup(setting.value).enable || setting.value.hasAtLeastOneReadonly);
  const needDropDown = computed(() => settingStore.isPlatformPreferences || setting.value.hasAtLeastOneNotifOptionReadonly);
  const parentSetting = computed(() => settingStore.getSettingGroup(setting.value));
  const hideToggle = computed(() => !settingStore.isPlatformPreferences && setting.value.hasAtLeastOneNotifOptionReadonly && !parentSetting.value.enable && !setting.value.readOnly);
  const settingOffClass = computed(() => {
    return !settingStore.isPlatformPreferences && (setting.value.readOnly || !parentSetting.value.enable || !setting.value.enable) ? "INTFCenter-preference-appname-off" : "";
  });
  const isOpen = ref(false);
  function handleChevronUpdate(newValue) {
    isOpen.value = newValue;
  }
  function optionOffClass(option) {
    return setting.value.getNotifOptionReadOnly(option.name) && !settingStore.isPlatformPreferences ? "INTFCenter-preference-appname-off" : "";
  }
  return {
    hideToggle,
    displaySetting,
    needDropDown,
    listOption,
    isOpen,
    settingOffClass,
    optionOffClass,
    handleChevronUpdate
  };
}
const _sfc_main$3 = {
  __name: "AppSetting",
  props: {
    settingId: {
      type: Number,
      required: true
    }
  },
  setup(__props) {
    const props = __props;
    const {
      setting,
      state,
      isReadOnly,
      updateState,
      updateReadOnly
    } = useToggleHelper(toRef$3(props, "settingId"));
    const {
      displaySetting,
      hideToggle,
      settingOffClass,
      handleChevronUpdate
    } = useAppSetting({ settingId: toRef$3(props, "settingId") });
    return (_ctx, _cache) => {
      return unref(displaySetting) ? (openBlock(), createElementBlock("div", {
        key: 0,
        class: normalizeClass(["INTFCenter-appSetting-container", unref(settingOffClass)])
      }, [
        createVNode(TogglePreference, {
          class: "app-setting-toggle-preference",
          description: unref(setting).name,
          lock: unref(isReadOnly),
          "toggle-value": unref(hideToggle) ? null : unref(state),
          onChevronUpdate: unref(handleChevronUpdate),
          onLockUpdate: unref(updateReadOnly),
          onToggleUpdate: unref(updateState)
        }, null, 8, ["description", "lock", "toggle-value", "onChevronUpdate", "onLockUpdate", "onToggleUpdate"])
      ], 2)) : createCommentVNode("", true);
    };
  }
};
const AppSetting = /* @__PURE__ */ _export_sfc$1(_sfc_main$3, [["__scopeId", "data-v-c3f32fa3"]]);
function useSettingHomeList(settingId) {
  const settingStore = useSettingsStore();
  const setting = computed(() => settingStore.getSetting(settingId.value));
  const AppOptionContainerClass = settingStore.isPlatformPreferences ? "INTFCenter-option-platform-preferences-list" : "INTFCenter-option-list";
  const openAppSettingsId = computed(() => {
    return settingStore.getGroupIndividualSettingsId(setting.value);
  });
  function getListOption() {
    const mailOption = { name: "email", fonticonName: "icon-desktop-email" };
    if (settingStore.isPlatformPreferences)
      return [mailOption];
    const acceptedParam = (name2) => {
      return setting.value.enable || name2 !== "center" && setting.value.getNotifOptionReadOnly(name2);
    };
    return [
      ...acceptedParam("center") ? [{ name: "center", fonticonName: "icon-desktop-center" }] : [],
      ...acceptedParam("alert") ? [{ name: "alert", fonticonName: "icon-desktop-alert" }] : [],
      ...acceptedParam("email") ? [mailOption] : [],
      ...acceptedParam("browser") ? [{ name: "browser", fonticonName: "icon-desktop-empty" }] : []
    ];
  }
  return {
    setting,
    AppOptionContainerClass,
    openAppSettingsId,
    getListOption
  };
}
const _hoisted_1$2 = {
  key: 0,
  class: "INTFCenter-appList-container INTFCenter-preferences"
};
const _sfc_main$2 = {
  __name: "SettingHomeList",
  props: {
    settingId: {
      type: Number,
      required: true
    }
  },
  setup(__props) {
    const props = __props;
    const {
      setting,
      AppOptionContainerClass,
      openAppSettingsId,
      getListOption
    } = useSettingHomeList(toRef$3(props, "settingId"));
    return (_ctx, _cache) => {
      const _component_vu_scroller = resolveComponent("vu-scroller");
      return unref(setting) ? (openBlock(), createElementBlock("div", _hoisted_1$2, [
        createVNode(_sfc_main$5, { "setting-id": __props.settingId }, null, 8, ["setting-id"]),
        createVNode(Transition, { name: "center" }, {
          default: withCtx(() => [
            createVNode(_component_vu_scroller, { class: "INTFCenter-preferences-body" }, {
              default: withCtx(() => [
                (openBlock(true), createElementBlock(Fragment, null, renderList(unref(openAppSettingsId), (id2) => {
                  return openBlock(), createBlock(AppSetting, {
                    key: id2,
                    "setting-id": id2
                  }, null, 8, ["setting-id"]);
                }), 128)),
                createBaseVNode("div", {
                  class: normalizeClass(unref(AppOptionContainerClass))
                }, [
                  (openBlock(true), createElementBlock(Fragment, null, renderList(unref(getListOption)(), (option) => {
                    return openBlock(), createBlock(_sfc_main$4, {
                      key: option.name,
                      option,
                      "setting-id": __props.settingId
                    }, null, 8, ["option", "setting-id"]);
                  }), 128))
                ], 2)
              ]),
              _: 1
            })
          ]),
          _: 1
        })
      ])) : createCommentVNode("", true);
    };
  }
};
const _hoisted_1$1 = { class: "INTFCenter-base-preferences" };
const _hoisted_2$1 = { class: "INTFCenter-color-brown" };
const _hoisted_3$1 = {
  key: 1,
  class: "INTFCenter-spinner-container"
};
const _hoisted_4$1 = { class: "INTFCenter-preferences" };
const _sfc_main$1 = {
  __name: "BasePreferences",
  setup(__props) {
    const { $i18n: $i18n2 } = useTranslations$1();
    const store = useSettingsStore();
    const loading = computed(() => store.settingState.isLoading.settings === true);
    const isDND = computed(() => store.isDND === true);
    function backToCenterOrSetting() {
      const { notificationSettingViewTracker } = NotificationTrackerUsage();
      const settingsViewTracker = notificationSettingViewTracker();
      if (store.appSettingOpened) {
        store.setAppSettingOpened(false);
        settingsViewTracker.backtoNotificationSettingsView();
      } else {
        store.setWindoShow("CENTER");
        settingsViewTracker.backtoNotificationCenterView();
      }
    }
    const textBackButton = store.getWindowShow() === "PREFERENCES" ? "preferences" : "platformpreferences";
    const alertText = computed(() => store.getWindowShow() === "PLATFORM_PREFERENCES" ? `${$i18n2("messagePreventplatformpreferences")} ${store.getCurentTenantName()}` : isDND.value ? $i18n2("dndAlertMsg") : null);
    onBeforeMount(() => {
      const { loadSettings } = usePreferencesManagement();
      loadSettings();
    });
    const components = {
      PreferencesHome: _sfc_main$7,
      SettingHomeList: _sfc_main$2
    };
    return (_ctx, _cache) => {
      const _component_vu_spinner = resolveComponent("vu-spinner");
      return openBlock(), createElementBlock("div", _hoisted_1$1, [
        createBaseVNode("button", {
          class: "INTFCenter-preferences-go-back-button INTFCenter-btn-transparent-default",
          "data-testid": "base-preferences-go-back",
          onClick: backToCenterOrSetting
        }, [
          _cache[0] || (_cache[0] = createBaseVNode("span", { class: "fonticon INTFCenter-btn-hover fonticon-chevron-left INTFCenter-back-icon" }, null, -1)),
          createBaseVNode("span", _hoisted_2$1, toDisplayString(unref($i18n2)(unref(textBackButton))), 1)
        ]),
        alertText.value ? (openBlock(), createBlock(unref(VuMessage), {
          key: 0,
          color: "warning",
          "is-closable": false,
          text: alertText.value
        }, null, 8, ["text"])) : createCommentVNode("", true),
        loading.value ? (openBlock(), createElementBlock("div", _hoisted_3$1, [
          createVNode(_component_vu_spinner)
        ])) : (openBlock(), createBlock(Transition, {
          key: 2,
          name: "setting"
        }, {
          default: withCtx(() => [
            createBaseVNode("div", _hoisted_4$1, [
              createVNode(Transition, { name: "base-center" }, {
                default: withCtx(() => [
                  (openBlock(), createBlock(resolveDynamicComponent(components[unref(store).appSettingOpened ? "SettingHomeList" : "PreferencesHome"]), {
                    "setting-id": unref(store).currentSetting
                  }, null, 8, ["setting-id"]))
                ]),
                _: 1
              })
            ])
          ]),
          _: 1
        }))
      ]);
    };
  }
};
const _hoisted_1 = { class: "INTFCenter-app-vue-notif" };
const _hoisted_2 = ["innerHTML"];
const _hoisted_3 = { class: "INTFCenter-message-modal" };
const _hoisted_4 = { class: "INTFCenter-notification-app" };
const _sfc_main = {
  __name: "App",
  setup(__props) {
    const { watchSettingWithUnsubDate } = usePreferencesManagement();
    const { reliveSocketIfKO, setSocketStatusTimeout } = useConnection();
    const { loadFilter } = useFilterStore();
    const store = useNotificationsStore();
    const settingStore = useSettingsStore();
    provide("vuDebug");
    function loadData() {
      const { isFilterApplied, getFilter } = useFilterStore();
      const payload = getFilter();
      if (!isFilterApplied()) {
        getUnreadTotal();
      }
      filter(payload);
    }
    onMounted(async () => {
      store.setIsLoading(true);
      loadFilter();
      if (store.clusterId)
        loadData();
      else watchOnce(() => store.clusterId, loadData);
      watchSettingWithUnsubDate();
      setSocketStatusTimeout(1e4);
      reliveSocketIfKO();
    });
    return (_ctx, _cache) => {
      const _component_vu_modal_container = resolveComponent("vu-modal-container");
      const _component_vu_message_container = resolveComponent("vu-message-container");
      return openBlock(), createElementBlock("div", _hoisted_1, [
        createVNode(_component_vu_modal_container, null, {
          "modal-body": withCtx(() => [
            unref(store).hasRenderToDoc ? (openBlock(), createElementBlock("div", {
              key: 0,
              innerHTML: unref(store).renderToDoc
            }, null, 8, _hoisted_2)) : createCommentVNode("", true)
          ]),
          _: 1
        }),
        createBaseVNode("div", _hoisted_3, [
          createVNode(_component_vu_message_container)
        ]),
        createBaseVNode("div", _hoisted_4, [
          createVNode(Transition, { name: "base-center" }, {
            default: withCtx(() => [
              unref(settingStore).getWindowShow() === "CENTER" ? (openBlock(), createBlock(_sfc_main$c, { key: 0 })) : (openBlock(), createBlock(_sfc_main$1, { key: 1 }))
            ]),
            _: 1
          })
        ])
      ]);
    };
  }
};
const eventHandlers = {
  setTenantAgnosticMode,
  deleteAllFilteredNotification,
  addNotification,
  setUnreadTotal,
  setHistory,
  setTenants,
  setDNDStatus,
  refreshForRollback,
  readNotification,
  deleteAllNotification,
  deleteNotification,
  setAppInfos,
  setServices,
  setMerge,
  setSetting,
  setNotifications,
  setSettings,
  setServicesSettingsOfTenant,
  setWhoSettings,
  refreshCenterView,
  rollbackPopup,
  resetSettingUnsubscriptionDate,
  setSocketStatus,
  notifCenterOpened,
  // when center is opened
  resetNotifications
  // when center is closed
};
function notifCenterOpened(data) {
  const { setCenterFrameOpened } = useNotificationsStore();
  setCenterFrameOpened(data.centerOpened);
}
function resetNotifications(data) {
  const { setCenterFrameOpened } = useNotificationsStore();
  setCenterFrameOpened(data.centerOpened);
}
function setSocketStatus(data) {
  const { setSocketStatus: setSocketStatus2, setCheckingSocketStatus } = useConnectionStore();
  setSocketStatus2(data.socketStatus);
  setCheckingSocketStatus(false);
  if (data.centerOpened) {
    const { setCenterFrameOpened } = useNotificationsStore();
    setCenterFrameOpened(data.centerOpened);
  }
}
function resetSettingUnsubscriptionDate(data) {
  const { getSetting, removeUnsubscribe_dateSetting } = useSettingsStore();
  const setting = getSetting(data.id);
  if (setting) {
    setting.unsubscribe_date = null;
    removeUnsubscribe_dateSetting(setting.id);
    refreshCenterView(false);
  }
}
function rollbackPopup(data) {
  const { rollbackPopupAction } = useNotificationManagement();
  rollbackPopupAction(data);
}
function refreshCenterView(data) {
  const { refreshCenterView: refreshCenterView2 } = useNotificationManagement();
  refreshCenterView2(true, !!data.serviceSettings);
}
function setSettings(data) {
  const {
    setSettingLoadState,
    setSettingLoadingState,
    setSettingsData,
    setPlatformPreferencesAccess
  } = useSettingsStore();
  const { setClusterId } = useNotificationsStore();
  setClusterId(data.clusterId);
  setSettingsData(data);
  setSettingLoadState("settings", true);
  setSettingLoadingState("settings", false);
  setPlatformPreferencesAccess(data.userRole);
}
function setServicesSettingsOfTenant(data) {
  const { setSettingLoadState, setSettingLoadingState, setServicesSettingsData } = useSettingsStore();
  setServicesSettingsData(data);
  setSettingLoadState("servicesSettings", true);
  setSettingLoadingState("servicesSettings", false);
}
function setWhoSettings(data) {
  console.log("setWhoSettings not implemented", data);
}
function setNotifications(data) {
  const { durationTime, resetAnalytics } = useAnalyticsStore();
  const { notificationsLoadTimeTracker } = NotificationTrackerUsage();
  if (durationTime() > 0) {
    notificationsLoadTimeTracker().notificationFilterLoaded({ time: durationTime() });
    resetAnalytics();
  }
  const { addHistory } = useNotificationManagement();
  addHistory(data);
}
function setTenantAgnosticMode(data) {
  const { setTenantAgnosticData } = useSettingsStore();
  setTenantAgnosticData(data);
  if (data.update) {
    const { isFilterApplied, getFilter } = useFilterStore();
    const store = useNotificationsStore();
    const { resetStates, setIsLoading } = useNotificationsStore();
    resetStates();
    setIsLoading(true);
    if (isFilterApplied()) {
      getFilter().tenants = [];
      const data2 = {
        ...getFilter(),
        ...!store.canLoadMoreHistory && store.canLoadMoreFromArchive && {
          archive: true
        }
      };
      filter(data2);
    } else {
      getHistory({
        type: "all",
        ...!store.canLoadMoreHistory && store.canLoadMoreFromArchive && {
          archive: true
        }
      });
      getUnreadTotal();
    }
  }
}
function setSetting({ setting, notifID }) {
  const { notificationSettingViewTracker } = NotificationTrackerUsage();
  const settingsViewTracker = notificationSettingViewTracker();
  settingsViewTracker.notifServiceOrSettings({ setting });
  settingsViewTracker.notifAlertSetting({ setting });
  const { unsubscribeNotification } = useNotificationManagement();
  const { setSettingData } = useSettingsStore();
  unsubscribeNotification({ setting, notifID });
  setSettingData(setting);
}
function addNotification(data) {
  const {
    unreadTotal,
    filterTotal,
    setFilterTotal,
    setUnreadTotal: setUnreadTotal2,
    getNotificationById,
    setNotification,
    addFilterNotifIds
  } = useNotificationsStore();
  const { canBeAddedToGroup, notificationInFilterRange, refreshWithoutReset } = useNotificationManagement();
  const { cleanNotificationData } = useNotificationCleaning();
  const { resolveNotificationAction, resolveNotificationIcon } = useNotificationResolution();
  const { setNotificationMenu } = useNotificationMenu();
  const { isFilterApplied, getFilter } = useFilterStore();
  const options = {
    appName: data.appName,
    currentTenant: data.currentTenant
  };
  data.notification.COUNT = 1;
  data.notification.ACTION = null;
  data.notification.ACTION_DATE = null;
  data.notification.ACTOR_DATA = null;
  data.clusterId = data.clusterId || data.notification.CLUSTER_ID;
  cleanNotificationData(data.notification);
  if (isFilterApplied()) {
    if (!notificationInFilterRange(data.notification, getFilter()))
      return;
  }
  if (canBeAddedToGroup(data.notification)) {
    refreshWithoutReset(false, data.notification.GROUPID);
  } else {
    setNotification(data.notification, options);
    if (isFilterApplied()) {
      setFilterTotal(filterTotal + 1);
      addFilterNotifIds([data.notification.ID], data.clusterId, false);
    } else {
      setUnreadTotal2(unreadTotal + 1);
    }
    refreshWithoutReset(false, data.notification.GROUPID);
    const id2 = `${data.notification.ID}-${data.clusterId}`;
    const notif = getNotificationById(id2);
    resolveNotificationIcon(notif);
    resolveNotificationAction(notif);
    setNotificationMenu(notif);
  }
}
function setUnreadTotal({
  unread: unread2
  /* , clusterId */
}) {
  let counter = 0;
  if (typeof unread2 === "number") {
    counter = unread2;
  } else {
    counter = Number.parseInt(unread2);
  }
  if (typeof counter === "number") {
    const { setUnreadTotal: setUnreadTotal2 } = useNotificationsStore();
    setUnreadTotal2(counter);
  } else {
    throw new TypeError("[setUnreadTotal] Missing data.unread");
  }
}
function setTenants({ tenants }) {
  const { setTenantsData } = useSettingsStore();
  setTenantsData(tenants);
}
async function setHistory(data) {
  const { durationTime, resetAnalytics } = useAnalyticsStore();
  const { notificationsLoadTimeTracker } = NotificationTrackerUsage();
  if (durationTime() > 0) {
    await notificationsLoadTimeTracker().notificationLoaded({ time: durationTime() });
    resetAnalytics();
  }
  const { addHistory } = useNotificationManagement();
  addHistory(data);
}
function refreshForRollback() {
}
function readOneNotification(data) {
  const { setMergeReadState, setNotificationReadState, setGroupReadState } = useNotificationManagement();
  const { getMergeById, getNotificationById } = useNotificationsStore();
  const { getFilter } = useFilterStore();
  if (data.id) {
    data.id = linkIds(data.id, data.clusterId);
    if (!data.hiddenMerged) {
      const notif = getNotificationById(data.id);
      const merge = getMergeById(data.id);
      if (merge) {
        setMergeReadState(merge, data);
      }
      if (notif) {
        if (notif.isGroup) {
          if (!notif.mergesFetched) {
            getMerge({
              parentNotif: {
                id: notif.ID,
                groupID: notif.GROUPID,
                clusterId: notif.CLUSTERID,
                fromArchive: notif.FROM_ARCHIVE,
                ignoreSearchesForChildren: !!notif.ignoreSearchesForChildren
              },
              filterData: getFilter()
            });
          }
        } else {
          setNotificationReadState(notif, data);
        }
      }
    } else {
      setGroupReadState(data.id, data);
    }
  }
}
function readNotification(data) {
  const { setAllReadState } = useNotificationManagement();
  const { updateLabel, setActioned } = useNotificationManagement();
  if (data.id) {
    readOneNotification(data);
  } else if (data.scope) {
    setAllReadState(data);
  } else if (data.updateLabel) {
    data.params.id = linkIds(data.params.id, data.clusterId);
    updateLabel(data.params);
  } else if (data.action === "actioned") {
    data.params.id = linkIds(data.params.id, data.clusterId);
    setActioned(data.params);
  } else if (data.notificationID) {
    data.notificationID.forEach((notif) => {
      if (notif.id) {
        readOneNotification({ ...data, id: notif.id });
      }
    });
  }
}
function deleteNotification(data) {
  const { removeNotification, removeNotificationGroup } = useNotificationManagement();
  data.id = linkIds(data.id, data.clusterId);
  if (!data.groupID)
    removeNotification(data.id);
  else removeNotificationGroup(data.id);
}
function deleteAllNotification() {
  const { resetStates } = useNotificationsStore();
  resetStates();
}
function deleteAllFilteredNotification() {
  const { resetStates, setIsLoading } = useNotificationsStore();
  const { fetchMoreNotificationsIfNecessary } = useNotificationManagement();
  resetStates();
  setIsLoading(true);
  fetchMoreNotificationsIfNecessary();
}
function setAppInfos(data) {
  const { setNotifAppInfos } = useNotificationsStore();
  data.notifID = linkIds(data.notifID, data.clusterId);
  setNotifAppInfos(data);
}
function setServices(data) {
  const { setListOfService } = useSettingsStore();
  setListOfService(data);
}
function setMerge(data) {
  const { setNotifMerges } = useNotificationManagement();
  setNotifMerges(data);
}
function setDNDStatus(data) {
  const { setDNDStatus: setDNDStatus2 } = useSettingsStore();
  setDNDStatus2(data);
}
function responseHandler(response) {
  if (!eventHandlers[response.action])
    return;
  try {
    eventHandlers[response.action](response.data, response.TxID);
  } catch (error) {
    throw new Error(`Error when processing response: ${error}`);
  }
}
async function mount(container, app) {
  requirejs$1(["css!DS/UIKIT/UIKIT"]);
  await translationsPromiseSetup;
  app.mount(container);
}
async function loadApp(app, widget) {
  widget.initFlag = true;
  const div = document.createElement("div");
  div.setAttribute("id", "INTFCenter-app");
  document.body.innerHTML = "";
  document.body.appendChild(div);
  document.documentElement.style.fontSize = "inherit";
  await mount(div, app);
}
async function startWidget(widget, app) {
  widget.onRefresh = function() {
    window.location.reload();
  };
  widget.onLoad = async function() {
    widget.initFlag = false;
    widget.counterRequest = 0;
    widget.maxRequest = 5;
    commonDriver.init(widget);
    await commonDriver.addCallback(responseHandler);
    await initAppData();
    await loadApp(app, widget);
  };
  widget.launch();
}
function initAppData() {
  return Promise.allSettled([
    getServices(),
    getSocketStatus(),
    resetBadge({ notifCenterOpened: true }),
    getTenants(),
    getTenantAgnosticMode(),
    getSettings()
  ]);
}
(() => {
  if (window.widget === void 0 || window.widget === null)
    throw new Error("No widget found");
  const widget = window.widget;
  const app = createApp(_sfc_main);
  app.use(Zv, { globalRegister: true });
  app.use(createPinia());
  startWidget(widget, app);
})();
//# sourceMappingURL=NotificationCenterWidget.js.map
